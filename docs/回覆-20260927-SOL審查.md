共找到 3 項確定問題，未找到需列為「可能」的 bug。

1. 中等：AI refine／重貼會白打一次付費斷句模型

   - 檔案：[main.py:7847](</E:/GitHub/TVBS-AICG-PG/main.py:7847>)、[main.py:8741](</E:/GitHub/TVBS-AICG-PG/main.py:8741>)、[main.py:6911](</E:/GitHub/TVBS-AICG-PG/main.py:6911>)、[main.py:8877](</E:/GitHub/TVBS-AICG-PG/main.py:8877>)
   - 觸發條件：十點 AI 標題，或 YT news／hot／hourly 單題 AI 標題；標題任一段至少 8 字，且 refine 後以 `background_image_base64` 重貼固定元素。
   - 錯誤結果：入口先呼叫付費文字模型取得斷句 hints，但隨後直接使用已含標題的背景圖，只重貼固定元素；hints 不參與成品。每次 refine 額外產生費用與最高約 10 秒等待，斷句服務異常時還會徒增逾時等待。
   - 信心：確定。

2. 中等：YT AI 標題 fallback 時，永久稽核遺失實際生圖 prompt

   - 檔案：[main.py:8553](</E:/GitHub/TVBS-AICG-PG/main.py:8553>)、[main.py:8943](</E:/GitHub/TVBS-AICG-PG/main.py:8943>)、[main.py:9094](</E:/GitHub/TVBS-AICG-PG/main.py:9094>)
   - 觸發條件：YT news／hot／hourly 使用 AI 標題、GPT、單張原圖底圖；模型已回傳透明標題圖層，但 overlay 四道閘任一失敗，系統退回 Pillow 程式壓字。
   - 錯誤結果：實際送出的完整 prompt 已被 `collected_cover_image_prompt()` 記住，但 fallback 將 `ai_title` 改成 `False`；歸檔時因此改存只有 visual 的 `log_prompt`。後台「最終 prompt」無法還原這次失敗圖層真正收到的 prompt，違反 B109 的稽核要求。
   - 信心：確定。

3. 低等：本機 request log 對所有 AI 封面仍記錄錯誤 prompt

   - 檔案：[main.py:7724](</E:/GitHub/TVBS-AICG-PG/main.py:7724>)、[main.py:7942](</E:/GitHub/TVBS-AICG-PG/main.py:7942>)、[main.py:9064](</E:/GitHub/TVBS-AICG-PG/main.py:9064>)
   - 觸發條件：任一十點或 YT AI 標題成功生成。
   - 錯誤結果：`request_log.log_generation()` 仍保存 `FULL/L/R` visual 或 `log_prompt`，不是實際送給生圖模型的完整 prompt；同一 `request_id` 的 JSONL 與永久 audit archive 內容互相矛盾，從短期 request log 排查時看不到真正分行及最終規則。
   - 信心：確定。

測試缺口：

- [test_b107_cover_original_no_ai_label.py:221](</E:/GitHub/TVBS-AICG-PG/tests/test_b107_cover_original_no_ai_label.py:221>) 的 YT 切版重設，以及同檔第 217 行的 refine 接線，只搜尋 `app.js` 原始碼字串，沒有執行 UI 狀態轉移或驗證實際 request payload。
- [test_b110_broadcast_hole_asis.py:79](</E:/GitHub/TVBS-AICG-PG/tests/test_b110_broadcast_hole_asis.py:79>) 的前端測試只計算 `hole_side` 字串出現次數，無法抓出欄位值錯誤、錯誤版型狀態或 refine 漏傳。
- [test_b109_ai_title_keep_words.py:158](</E:/GitHub/TVBS-AICG-PG/tests/test_b109_ai_title_keep_words.py:158>) 只驗 AI 成功路徑，沒有覆蓋標題圖層閘門失敗後的 fallback 歸檔，因而漏掉第 2 項。
- B105、B108、B111 已有實際重試、排版函式或左右幾何行為測試，未發現額外確定 bug。

測試未能執行：系統 Python 沒有 `pytest`，repo `.venv` 指向已不存在的 Python 3.14；全程未修改檔案。

確定 3 項／可能 0 項

---

# 修正回報

已完成 B112，僅修改兩個檔案，未 commit／push／bump version，也未修改 `MASTER-列管清單.md`。

- [main.py](E:\GitHub\TVBS-AICG-PG\main.py:7729)
  - 十點與 YT 的 AI refine＋background 跳過斷句模型，仍以 `compose.set_break_hints({})` 清空狀態。
  - composite＋background 與 AI 無 background 仍執行斷句。
  - 三處 `request_log.log_generation()` 改記實際收集到的生圖 prompt，沒有才沿用原值。
  - 十點及 YT archive 統一使用 `collected_cover_image_prompt() or log_prompt`，YT fallback 不再因 `ai_title=False` 遺失 prompt。
  - 已確認兩個端點入口皆重設 prompt ContextVar；composite 不會誤讀前次請求。
  - 已確認十點 AI refine 下游只補固定元素，B107 標籤與 fallback 壓字均不讀 hints，與任務假設一致。

- [tests/test_b112_review_fixes.py](E:\GitHub\TVBS-AICG-PG\tests\test_b112_review_fixes.py:49)
  - 覆蓋十點／YT 三種斷句條件。
  - 覆蓋 YT overlay 閘門 fallback 的 archive 與 request log 實際 prompt。
  - 覆蓋十點 full／split request log。
  - 覆蓋 composite prompt 維持原值與 ContextVar 請求間重設。

驗證：

- 指定 pytest 指令無法執行：目前 Python 缺少 pytest，repo `.venv` 又指向已不存在的 Python。
- 改用同範圍 `unittest`：共 34 項，全數通過。
- `py_compile` 通過。
- `git diff --check` 通過。
- 初次紅燈測試曾漏 mock `resolve_yt_cover_plan`，外部連線立即失敗、未成功呼叫或產生成本；隨即補上 mock，最終測試全部隔離外部／付費 API。