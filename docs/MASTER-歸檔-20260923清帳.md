# MASTER 清帳歸檔（2026-09-23）

gpt-5.6-sol 逐項對程式碼與 git 歷史清帳、Claude 抽查後，從主清單移出的列，原文一字不改保留於此。
最後一欄寫明原行號、清帳歸類與證據。

| ID | 標題 | 來源 | 狀態 | 說明 | 清帳歸類與證據 |
|---|---|---|---|---|---|
| B40 | **OpenRouter key 額度耗盡時整條消化全面 502，且失敗不留痕** | 2026-09-15 本機量測時撞到 | ⏸ **使用者 2026-09-15 裁：先不查正式站額度，專心修 524** | 本機 exp 的 `.env` 那把 OpenRouter key 已耗盡：104 字短稿都吐 `402 This request requires more credits... You requested up to 6000 tokens, but can only afford 334`（`limit_source: openrouter_key_limit`），`DIGEST_ATTEMPTS=5` 五次全卡在同一個 402、10.9 秒就收攤，回 502。**重點不是本機這把沒錢**（那是個人帳號、已知只剩約 $1.68），而是**這個故障模式**：額度一旦見底，消化端**全站**立刻 502，使用者只看到「HTTP 502」這種無意義字串（B38），後台照樣不留紀錄（F31），沒有人會知道原因是沒錢。而且**重試在這個情境下毫無意義**——402 不會因為再試四次就變成 200，只是把錯誤延後 10 秒。**待查（已裁緩）**：正式站用的是哪一把 key、餘額多少、有沒有餘額告警。**順手可修**：402／餘額類錯誤應該**跳過重試直接回報**，並給人看得懂的訊息 | 原第 85 行｜✅ 已做完（清帳補登）｜`26d5dcd` 已讓 402 立即停手；`6379fa8` 補消化失敗歸檔 |
| B41 | **思考封頂在兩條路徑上都沒用——原生路徑根本不送，OpenRouter 路徑送了但模型不遵守** | 2026-09-15 本機量測時發現 | ⬜ **第一波，已升級**（2026-09-15 實測改寫） | `digest_reasoning_body()`（`main.py:220`）第一行就是 `if DIGEST_BACKEND != "openrouter" ... return {}`，所以 `DIGEST_REASONING_MAX_TOKENS=2000` 這道保險**只有 OpenRouter 後端吃得到**。走原生 `DIGEST_BACKEND=openai` 時**完全不送任何思考上限**，思考可以一路吃到 `DIGEST_MAX_TOKENS=6000` 為止。本機實測（`gpt-5.5`，2930 字稿）：`standard` reasoning **4606/6000（ratio 0.91）**、`maximum` 一次 **6000/6000（ratio 1.00）finish=length、raw content 空字串、parse failed**，重試第二次才拿到結果——**同一組條件耗時從 38 秒跳到 149 秒，差了快 4 倍**，變異全部來自「第一次有沒有被截斷」。註解裡寫的理由是「OpenAI 系走 effort，這裡不送」，但**實際上也沒有送 effort**，等於原生路徑裸奔。⚠**不要拿這個去解釋正式站的 524**：正式站是 OpenRouter，思考被封在 2000、留 4000 給正文（觀測正文最大 1361），這條保險在正式站是有效的。**影響範圍**：①任何人在本機用原生後端量消化，數字都不可信、且會被截斷重試汙染；②哪天正式站切回原生就會立刻踩到。**修法**：原生路徑也要有等效封頂（OpenAI 系送 `reasoning.effort`），或至少在啟動時對「原生後端＋無思考上限」印一條明確警告 ❗**2026-09-15 重大更正（本機四次實驗，見 B48）**：本列原本只說「原生路徑沒有上限」，**實測發現 OpenRouter 這條也形同虛設**——送出 `reasoning.max_tokens=2000`、請求被接受（沒有觸發 `main.py:1948` 的「模型不吃 reasoning 上限」退路），但 `reasoning_tokens` 實測 1603／2038／2144／5442／6000，**沒有一次低於 2000**。**所以這不是「保護不完整」，是「保護不存在」**，嚴重度與範圍都比原本記的大。與 **B48** 是同一個根因，**建議合併處理**。 | 原第 86 行｜✅ 已做完（清帳補登）｜`9bde3c5` 改送 `reasoning.effort`；`main.py:327-383` |
| B42 | **改圖（refine）線失敗仍然只有 3 秒 toast，沒有錯誤橫幅** | B38 驗收時發現（2026-09-15） | ⬜ | B38 只把**一鍵生成**那條（`handleOneClickGenerate`）改成可關閉的錯誤橫幅，**改圖那條沒動**——`app.js:3563` 失敗後仍是 `showToast` 3 秒淡出，使用者一樣會錯過。**這不是漏做，是本單刻意框的範圍**，但不能就這樣留著：曹雪卿 2026-09-15 那 9 筆裡**有 2 筆是改圖**（10:26、10:50），改圖在實際工作流裡佔比不低。封面／YT 封面那幾條同理。**修法**：把 B38 的橫幅機制抽成共用，讓改圖與封面線共用同一套；`showGenerateErrorBanner` 目前綁死 `oneClickErrorBanner` 這個 id，要先參數化 | 原第 87 行｜🗃 已過時／被取代｜雖仍用 toast（`app.js:3934-3937`），但 F42 訊息歷史已永久保留，`app.js:4024-4046` |
| B44 | **大標題出現壞字：「騙」的右半邊筆畫錯亂，同一張圖裡別處的「騙」卻是對的** | 曹雪卿 0915 回饋文件（AI CG回饋，hikaru.tsao@gmail.com） | ⬜ 🌊**第二波**（2026-09-15 使用者裁定：等第一波收工才動工） | 使用者原話「騙字變怪字」。監督下載正式站成品逐字放大核對（`20260915-102630`，標題「AI接管世界是騙局」）：標題那個「騙」右半邊「扁」的結構畫壞，**但同一張圖第 1 格「全球暖化騙局」的「騙」完全正確**——所以不是字庫缺字、也不是這個字天生難畫，是**同一張圖內的逐次渲染失敗**。⚠**與已撤銷的 B35 是同一現象**（B35 當時被撤是因為那次確實是監督看錯字，不是缺陷；這次是使用者回報＋監督原尺寸放大複核，**證據等級完全不同，不要拿 B35 的結論套用**）。**動工前要先查清楚**：資料圖表的大標題到底是生圖模型畫的、還是程式壓的？YT 封面那條線是程式壓字（同一個「騙」在 `20260915-133528` 上完全正確），若資料圖表也走程式壓字，那病灶就不在模型。**這題沒查清楚就改，一定改錯地方。** | 原第 89 行｜🗃 已過時／被取代｜後續 B58 複驗判定不是壞字並作廢；`6eeab78`、`5ca72d3` |
| B45 | **YT 整點直播把一位真實具名人物配上 AI 捏造的臉** | 曹雪卿 0915 回饋文件（AI CG回饋，hikaru.tsao@gmail.com） | ⬜ 🌊**第二波**（2026-09-15 使用者裁定：等第一波收工才動工） | 使用者原話「沒有指定圖，但他給了一個不正確的人物照片(如果不是名人，或許示意圖會較好)」。監督調成品核對（`20260915-133528-e0c0cdf30aa4.png`，13:35:28，YT整點直播（雙則））：**右半張是一張 AI 生成的虛構亞洲男性面孔，正下方的標題寫著「陳一新警告人工智慧威脅政權」**——臉與姓名同框，觀眾讀起來就是「這個人是陳一新」。畫面右上角**有**掛「AI示意圖」角標，但角標不能抵銷「具名真人＋捏造臉孔」這件事。⚠**監督判斷這條的嚴重度高於使用者自己的描述**：這正是 2026-08-05 出過事、`portrait_subjects` 那整套肖像規則要擋的情境（見 `GenerateResponse.portrait_subjects` 的註解），**屬播出風險，不是美感問題**。要查：YT 整點直播這條線有沒有接上肖像流程，查不到參考照時是否應該**完全不畫臉**而不是自由發揮。 🎯**2026-09-15 使用者指示插隊調查（只查不修，B45 仍是🌊第二波）：根因已完整定位，四層。****①肖像流程有接上，不是沒接。**雙則走 `yt_dual_panel_requests` → `yt_dual_panel_plan` → `yt_dual_background` → `_cover_panel_image` → `_cover_apply_portraits`，肖像規則與參考照都會注入。**②真正斷掉的是英文名。**本機實跑 `derive_yt_cover_plan("陳一新警告人工智慧威脅政權")`，模型回 `portrait_subjects_en=['Yi-hsin Chen']`（臺灣拼法）；用它查 `find_reference_photo` **查無**，於是 `keep_subjects_with_photos` 判定查不到 → `resolve_portraits` 退回 `no_reference`，日誌原文 `[portrait] 查不到參考照（陳一新），整張退回不生成臉孔`、`參考照=0 張`。**而改用維基條目名 `Chen Yixin` 就查得到正確的人**（`https://en.wikipedia.org/wiki/Chen_Yixin`，即中國國安部長本人，照片已存 `D:\Downloads\AICG\後台紀錄60915\陳一新_參考照.jpg`）。⚠**連帶更正本帳本先前的記載**：B48 行裡寫過「離線查三人全部 FOUND」，那次是用 `Chen Yixin` 查的，**不是系統實際會用的名字**；系統實際拿到的是 `Yi-hsin Chen`，查不到。先前那句話結論正確但推不到這裡，特此更正。**③退回不畫臉沒有用，因為 visual 已經寫死要一張臉。**同一次 derive 回的 visual 是「室內昏黃燈光下,一位年長學者神情凝重端坐受訪,背景虛化的書架若隱若現,鏡頭聚焦其深鎖眉頭與嚴肅眼神。」——**畫面描述命令模型拍一張近距離人臉特寫**，肖像規則只說「不生成臉孔」，兩者直接打架，模型照著 visual 畫，於是捏出一張臉。這正是 `keep_subjects_with_photos` docstring 早就警告過的同一類矛盾。**④derive 還認錯人。**「年長學者＋書架」是淡江大學教授陳一新的形象，不是中國國安部長——`YT_COVER_DERIVE_SYSTEM` 只餵**一句標題**、不餵新聞原文，同名同姓無從分辨，英文名拼成 `Yi-hsin Chen` 也與這個誤判一致。🚩**附帶發現（更嚴重的退路）**：`derive_yt_cover_plan` 失敗時回空 dict，呼叫端退路是 `visual = 標題原句`、`subjects=[]`、參考照 0 張、**肖像規則一條都不注入**——實測撞到一次（本機金鑰 401），最終 prompt 的 Subject 直接變成「陳一新警告人工智慧威脅政權」。**這條退路比正常路徑更容易捏臉。**❌**使用者假說「跟創意階梯有關」已在程式碼層推翻**：雙則底圖用的是十點那支 `editor_formats.COVER_VISUAL_PROMPT_TEMPLATE`，**模板裡沒有 `{creativity}` 槽**（單則走的 `YT_COVER_VISUAL_PROMPT_TEMPLATE` 才有），`_cover_panel_image` 的參數列也沒有 creativity。本機實測最終 prompt **出現 `creativ` 0 次**。⚠**但這件事本身是缺陷**：同一顆創意拉桿，單則有作用、雙則完全沒作用，另開 **A7** 列管。 | 原第 90 行｜✅ 已做完（清帳補登）｜`7d4748b` 建立四層肖像退路；`main.py:5989-6015` |
| B47 | **播出鏡面挖左框／挖右框，位置都偏高** | 曹雪卿 0915 回饋文件（AI CG回饋，hikaru.tsao@gmail.com） | ⚖ **2026-09-15 使用者已裁：不修位置，維持蓋框預設 OFF**｜🌊**第二波**（若之後要做示意圖標示才排） | 使用者原話「播出鏡面挖左框／左邊挖框位置偏高」「播出鏡面挖右框／右邊挖框位置偏高」，兩條各附截圖（字多版、字少版）。監督當時查不到對應成品（後台 0915 曹雪卿 19 筆沒有播出鏡面類型），原本要向她問是哪一筆。⚖**使用者裁決（2026-09-15）**：「我認為不可控因素太大，蓋框位置太死，無法根據生成圖片判斷哪裡可以蓋框，所以我才預設關閉 OFF 蓋框，只讓生圖模型自己留空位。」**所以這不是缺陷，是已知的設計取捨**——蓋框位置是固定座標，但生圖的構圖每次都不同，沒有辦法從成品反推哪裡蓋得下去；與其蓋錯，不如預設關閉、由生圖模型自己在構圖時留白。**動作**：①不要去調挖框座標（調了也只是換一種蓋錯）；②要查的是**曹雪卿為什麼會遇到**——預設是 OFF，代表她自己開了，需要確認介面上有沒有講清楚開了會有什麼後果；③她另外提的「示意圖是否應標示在挖圓框的地方」（F35 ①）在這個前提下**更值得做**，因為那正是「讓使用者自己看得出哪裡能蓋」的替代方案。**本列不再追位置，關掉。** | 原第 92 行｜🗃 已過時／被取代｜使用者已裁定不修位置、預設關閉；本列原缺陷方向被推翻 |
| B48 | **長稿消化實際上只有約 140 秒可用，超過就失敗——曹雪卿說的「很容易失敗」其實沒被 B39 解決** | staging `260915-01` 實測（2026-09-15），B39 驗收時發現 | ⬜ **第一波，優先序高** | **B39 只換掉失敗的表示法，沒有降低失敗率。** staging 兩次實測：「字超多」成功（4 分 16 秒），**同一篇改「字多」在約 150 秒失敗**——2 次 1 敗。監督到 staging `/admin` 核對，**成功那筆有紀錄（15:11:36）、失敗那筆完全沒有留痕**（順帶再證實一次 F31）。**根因已對上程式碼**：`main.py:2330` 的死線守門是 `time.monotonic() + DIGEST_TIMEOUT_SECONDS > deadline`，即 `230 - 90 = 140 秒`——**消化只要跑超過約 140 秒就一定會被擋下**，實測失敗在 ~150 秒完全吻合。⚠**注意這裡有個前提可能也是錯的**（跟 B39 當初那個「照 Cloud Run 300 秒設計」的錯誤是同一類）：`DIGEST_DEADLINE_SECONDS=230` 的註解寫的是要避開 **Cloud Run 的 300 秒硬上限**，但 `docs/HANDOVER-2026-08-25.md` 寫 staging／正式站**都跑在 AI 部門自架的 Ubuntu 上、systemd 管理、走 Cloudflare Tunnel**，不是 Cloud Run。**若實際部署形態沒有 300 秒硬上限，230 這個數字就沒有依據**，而且 B39 之後連線已經不會靜默斷線，放寬死線是安全的。**動工前必須先查證部署形態**（問同事、或看那台的 systemd／反向代理設定），**不要憑註解就改數字**——B39 就是這樣踩過一次。**候選方向**（查證後再選）：①確認無硬上限就放寬 `DIGEST_DEADLINE_SECONDS`；②查為什麼「字多」會比「字超多」慢到爆（直覺上工作量更小，這個反直覺本身就是線索，可能是 OpenRouter provider 輪替，見 B39 詳述末段）；③長稿在送出前就預估並提示。**⚠不要一上來就放寬數字**——②那個反直覺沒查清楚，放寬只是把失敗往後延。 📊**2026-09-15 第二輪數據（staging 4 次＋本機 1 次）**：staging 實際跑了 4 次，監督到 `/admin` 核對成功列為 **15:11:36／15:23:42／15:27:04 共 3 筆**，**即 4 次 3 成功 1 失敗（失敗率 25%）**——不是系統性必敗，是間歇性。**本機用同一篇、同一檔位（`standard`）、同一後端（`.env` 原樣：openrouter／claude-sonnet-5）跑一次：59.19 秒乾淨成功**，`finish=stop`（沒截斷）、`completion_tokens=4720`（預算 6000 的 79%）、`provider=Anthropic`，**stdout 完全沒有任何 `[generate] attempt N/5` 行**。⚠**監督先前那三個猜測（截斷／簡體字品質閘／其他品質閘）本輪全部沒有出現，暫時排除但未證偽**（只有 1 次樣本，且這次根本沒失敗）。🔑**真正浮出來的是速度差**：同樣的工作，**本機 59 秒，staging 失敗那次卻在 ~150 秒被死線擋下，差 2.5 倍**。兩個候選解釋，**都還沒查**：**(a) 第二段消化**——`main.py:2457` 的 `apply_photo_availability()` 在「有具名人物且查不到參考照」時**會整個重跑一次 generate()**（只重試一次）。本機那次 stdout **沒有 `[portrait]` 那行**，代表沒觸發；而那篇稿點名了川普／黃仁勳／**陳一新**，陳一新（中國國安部長）很可能維基查無條目 → staging 觸發第二段消化 → **59×2 ＋ 維基查詢與重試 sleep ≈ 140~150 秒，剛好踩到死線守門**。**這個算術對得太準，要優先查。** **(b) OpenRouter provider 輪替**——本機這次落在 `provider=Anthropic`，staging 可能落到較慢的 provider。**✅下一步是零成本的**：不要再花錢亂試（25% 的命中率等於平均要燒 4 次才看得到 1 次失敗）。先看本機那次回應裡的 `portrait_subjects` 有沒有人、再用維基查照的 helper 離線查「陳一新」查不查得到——**兩件都不花 AI 的錢**。若 (a) 成立，B48 的修法跟「放寬逾時」完全無關，而是「第二段消化要不要也吃同一條死線」的設計問題。💡**連帶結論：F30／F31 的優先序應該往前提。** 這次查不下去的根本原因是**正式站／staging 的失敗完全不留痕、成功也不記消化耗時與 provider**；把這兩項補上之後，下一次真實失敗自己就會把答案送上門，不必再花錢重現。 ❌**2026-09-15 監督自己的假說 (a) 已被推翻（零成本查證）**：本機那次的 `portrait_subjects` 只有 `["川普", "黃仁勳"]`，**消化端根本沒把陳一新列進去**（他在稿中是被引述的官員，不是版面要重建場景的人物）；而且離線用 `photo_lookup.find_reference_photo()` 查三人，**川普／黃仁勳／陳一新全部 FOUND**（監督已核對 `lookup_portrait_photos()` 就是包這支 helper，測法有效）。**所以「因為查無陳一新而觸發第二段消化」這個具體原因不成立。** ⚠**但要分清楚被推翻的是「原因」不是「機制」**：`apply_photo_availability()` 只要判定有人查不到就會整個重跑一次 `generate()`，這條路徑仍然是把耗時直接翻倍的最大單一嫌疑。而且 `main.py:3632` 寫得很清楚——**查照時任何例外（網路、逾時）都當成「這位查不到」**，所以 **staging 上只要維基查詢偶發逾時一次，就會觸發整段第二次消化**，而本機網路順暢的情況下永遠複製不出來。**這正好也符合 25% 的間歇失敗率。****這個猜測同樣不要再花錢試**——它跟前一個一樣，沒有日誌就只能空想。🚩**結論：B48 在 F30／F31 補上之前查不下去了。** 需要的三個欄位都不存在於現有紀錄：①失敗完全不留痕（F31）；②成功不記消化耗時；③不記 OpenRouter 的 provider 與有沒有跑第二段消化。**建議把 F30／F31 提到 B48 前面做**，以 25% 的失敗率算，補完後等一天就會有真實樣本自己送上門，比花錢重現便宜也準確得多。 🎯**2026-09-15 本機四次實驗，根因確認（監督已逐行核對 `D:\Downloads\AICG\後台紀錄\20260915\B48本機四次\run*.log` 原始日誌，非採信轉述）**：**病灶是思考把整個 completion 預算吃光，不是逾時、不是 portrait、不是 provider。** 四次共 8 次 API 呼叫，**5 次 `finish=length`、`ratio=1.00`**，日誌原文如 `budget=6000 completion_tokens=6000 reasoning_tokens=6000 ratio=1.00 finish=length TRUNCATED`——**思考吃掉全部 6000，留給 JSON 正文的是 0**，raw content 是空字串（run4 是寫到一半的 unterminated string），於是 `parse failed` → 重試。**run1 連撞兩次 ratio=1.00，第三次還沒開始就被 230 秒死線擋下 → 503**，訊息與 staging 那次一字不差；run3／run4 各撞一次、第二次救回（173s／180s），run2 一次過（108s）。**`[portrait]` 四次全部沒出現 → 第二段消化的猜測正式排除**；provider 八次全是 `Anthropic`，也不是輪替。❗**最關鍵：思考封頂根本沒有生效。** `digest_reasoning_body()` 確實送了 `reasoning.max_tokens=2000`（預設 2000、`.env` 未覆寫、`min(2000, 6000-2500)=2000` 成立），且**四份日誌都沒有「模型不吃 reasoning 上限」那行**（`main.py:1948` 的退路沒觸發），代表欄位是**被接受**的——**但實測 `reasoning_tokens` 跑到 5442～6000，連成功那三次也是 1603／2038／2144，全部觸頂或遠超。****結論：OpenRouter／Anthropic 收下了這個欄位但沒有遵守。** 💡**修法方向（監督建議，待裁 D21）**：`DIGEST_MAX_TOKENS=6000` 是照「total 觀測最大值再留六成」訂的，而那份觀測的前提是**思考封頂有效**。封頂既然無效，6000 對這個模型就是不夠。同一支程式的 `MAP_DIGEST_MAX_TOKENS` 已經是 **10000**，有現成先例。**放寬幾乎不花錢**——token 照實際生成量計費，沒用到不付；而現在每次截斷是**整整 6000 token 全部白付**、再付一次重試，**放寬反而省錢**。⚠**但不要只放寬就收工**：要同時查 `reasoning.max_tokens` 為何沒生效（參數名／模型是否支援／是否該改用 `reasoning.effort`），否則思考量會跟著規則條數繼續漲，放寬只是把牆往後推。 | 原第 93 行｜✅ 已做完（清帳補登）｜`9bde3c5` 換低思考快速模型；`main.py:327-395` |
| B50 | **重試是純擲骰子——五次 attempt 送的是位元組相同的請求，失敗一次跟失敗五次的機率不獨立也不收斂** | 監督讀 B48 四次實驗日誌後提出（2026-09-15） | ⬜ **第一波，下一批第一優先**（2026-09-15 使用者採納監督建議：與 D21 甲案同批做） | **現況**：`generate()` 的重試迴圈每一次 attempt 都用**完全相同的 payload**重送（同 model、同 prompt、同 `max_tokens`、同 `reasoning.max_tokens`）。B48 實測證明失敗模式是「思考失控吃光預算」，而這是**同一個請求的隨機性**——所以重試等於**再擲一次同一顆骰子**：run3／run4 第二次擲贏了（救回），run1 連輸兩次（真的失敗）。**重試次數 `DIGEST_ATTEMPTS=5` 看起來很多，但死線只夠擲 2 次**（見 B48）。💡**修法**：重試時**要改變條件**，讓它收斂而不是碰運氣。至少三種可疊加：①偵測到 `finish=length` 且 `reasoning_tokens ≈ budget`（這個簽章 `log_digest_usage()` 已經算得出來，`ratio` 與 `TRUNCATED` 旗標都現成）時，**下一次 attempt 改用更小的思考預算**（`reasoning.max_tokens` 降到下限 1024，或改送 `reasoning.effort=low`）；②或反向——下一次把 `max_tokens` 再加碼；③兩者擇一連續兩次仍失敗才放棄。**這比單純放寬預算更根本**：放寬只是讓骰子的壞面不那麼致命，改變重試條件才是讓它收斂。⚠**與 D21 甲案的交互作用，動工前務必一起看**：從 B48 四次的總耗時反推，單次呼叫大約 **85～105 秒**（run2 單次成功總耗時 108.59s、run3 兩次共 173.32s、run4 兩次共 180.30s），而 `DIGEST_TIMEOUT_SECONDS=90` 是**單次呼叫**的上限——**現在已經幾乎沒有餘裕**。甲案把 `DIGEST_MAX_TOKENS` 放大之後，思考失控的那次會**生成更多 token、跑更久**，**很可能從「回傳截斷結果」變成「撞上 90 秒單次逾時」**，那樣甲案就白做了。**所以甲案的驗收一定要量「單次呼叫實際耗時」，不能只看成敗**——這正是本專案一再踩到的同一種坑（B39：預算照著錯的天花板設計）。 | 原第 95 行｜✅ 已做完（清帳補登）｜`d8cb1ee`；重試會附前次錯因，`tests/test_generate_retry.py:288-320` |
| B54 | **「補畫面描述」失敗時的退路完全沒有肖像保護，比正常路徑更容易捏臉** | 由 B45 根因調查獨立出來（2026-09-15，監督本機實際撞到一次） | ⬜ 🌊**第二波**（跟 B45 同一條線） | `derive_yt_cover_plan` 失敗時回空 dict，呼叫端（`resolve_yt_cover_plan`／`yt_dual_panel_plan`）的退路是 `visual = 標題原句`、`subjects = []`、`english = []`，於是 **`_cover_apply_portraits` 一條肖像規則都不注入、參考照 0 張**。監督本機實際撞到（金鑰 401），最終送給生圖模型的 Subject 就是「陳一新警告人工智慧威脅政權」這句話本身——**一個具名真人的名字直接當畫面題目，而且沒有任何「不准捏臉」的約束**。**正常路徑至少還會退回 `no_reference`（不生成臉孔）；這條退路連那層都沒有。**修法方向：推導失敗時應該視為「有具名真人但查不到照片」的最保守情形，而不是視為「沒有人」。 | 原第 100 行｜✅ 已做完（清帳補登）｜四層肖像退路已涵蓋封面補述；`main.py:5989-6015`、`7d4748b` |
| B73 | **F40 把「鮑爾」判成「連維基條目都查不到」，直接落到第 4 層不畫這個人** | 2026-09-16 B66 實拍第一輪（使用者授權的付費實拍），兩次都沒測到要測的東西 | 🟠 **未修**｜**程式邏輯沒錯，錯在譯名查不到**——但鮑爾是臺灣財經新聞最常出現的人之一，這個漏法會讓一整類報導畫不出人｜✅ **2026-09-16 已實作（兩半都做，依使用者裁定）**：①`photo_lookup.TW_PORTRAIT_NAME_ALIASES` 譯名對照表，**13 位常見人物從第 4 層救回第 2 層**；②`PORTRAIT_NO_ENTRY_FALLBACK`（預設開）讓查不到條目的人仍走第 3 層。⚠尚未實拍驗收——②的效果要看圖才算數| **實測（免費的維基查詢，不花錢就能重現）**：`lookup_portrait_outcomes(['鮑爾'], [])` → `entry_found=False`；`lookup_portrait_outcomes(['鮑爾'], ['Jerome Powell'])` → `entry_found=True` 且**有合格首圖**（en.wikipedia/Jerome_Powell）。對照組：`['川普']`、`['黃仁勳']` 單獨查都直接命中中文維基。👉**差別就在英文退路有沒有被觸發**。`lookup_portrait_photos()` 的 `english_names` 來自消化端的 `portrait_subjects_en`，B66 那兩次消化**沒有給**，於是英文退路從來沒跑，四層分流一路掉到第 4 層「畫面不安排這個人」（`logs/generations-20260916.jsonl` 兩筆 `portrait_mode=none` 可查）。💡**修法方向（監督建議，待裁）**：甲｜**消化端把 `portrait_subjects_en` 變成必填**（現在是可有可無，模型愛給不給）；乙｜後端自備一張常見譯名對照表當第二層退路（鮑爾→Jerome Powell、葉倫→Janet Yellen 這類），不依賴模型；丙｜兩個都做。⚠**不要用「查不到就讓模型自畫」解**——那是 F40 第 3 層，而第 3 層的前提是**確認查得到條目**；這裡的狀況是條目查不到，放行等於回到 B63 捏臉。 ⚠**2026-09-16 更正本列的根因判斷（監督自我更正）**：上面寫「`portrait_subjects_en` 現在是可有可無，模型愛給不給」**是錯的**。回讀 `main.py:1759` 原文，消化端的規則是**明文禁止**：「If the English or Latin name does not appear in the source material or the user's input, that entry **MUST be an empty string**. Never translate a Chinese name, never guess a spelling, and **never fill the English name from common knowledge**.」👉**模型沒給英文名不是偷懶，是照規則辦事**。B66 那篇測試稿只寫「鮑爾」沒寫 Jerome Powell，消化端依規則必須回空字串，英文退路因此從來不可能被觸發。**這是刻意的設計取捨，不是 bug**，但代價是：**任何只寫中文譯名的稿子（也就是絕大多數臺灣新聞稿）裡的外國人，都畫不出人**。 🔬**2026-09-16 補測（免費維基查詢，可重現）**：`鮑爾` zh 條目存在但**沒有 wikibase_item**（姓氏／消歧義頁）→ 被擋；`鮑威爾` zh 拿到 `Q224335` 但 **`_is_human` 判 False**（那是消歧義項目）→ 被擋；`葉倫` zh **根本沒有這個條目**（真正的條目是「珍妮特·耶倫」）。**兩道防呆都擋對了**，`_lookup_lang` 用的是精確標題查詢（`titles=` ＋ `redirects=1`），臺灣簡稱譯名對不上條目名就是查無。**但中文維基其實有這些人**：`傑羅姆·鮑威爾` → entry＋photo 都有；`珍妮特·耶倫` → entry＋photo 都有。差別只在**用的是全名還是臺灣新聞的簡稱**。 🔑**真正的風險不是拼錯，是同名（監督補，這點先前沒寫）**：「鮑爾」在臺灣媒體同時指**聯準會主席 Jerome Powell** 與**前國務卿 Colin Powell（柯林·鮑爾）**。`_is_human` 只驗「這是不是一個人」，**不驗「是不是對的那個人」**——放寬成「讓模型憑常識補名字」時，拼錯多半只會查無（安全），**抓到同名的另一個人才是會出事的那條**，而且畫出來的是一張有臉、有真名、看起來完全正常的成品，沒有人會發現。**這是使用者要裁的核心取捨。** 💡**修法方向更新**：甲｜放寬 `main.py:1759`，允許消化端從常識補英文名（**必須同時想清楚同名**——可要求模型連同職稱／機構一起給，下游拿 Wikidata 的 P39／P108 對一次）；乙｜後端自備常見譯名對照表（安全、可控，但覆蓋不了長尾，要人維護）；丙｜**新增「中文全名」退路**——實測「傑羅姆·鮑威爾」「珍妮特·耶倫」都查得到，所以請消化端補中文全名也能解，**但那和甲一樣是憑常識猜，同名風險完全相同**，只是換個語言；丁｜甲＋乙一起做，對照表當第一層、模型當長尾退路。⚠**仍然不要用「查不到就讓模型自畫」解**（理由見上，會回到 B63 捏臉）。 📌**使用者 2026-09-16 提問「為什麼鮑爾畫不出來？第三層規則應該就要畫出來」的答覆**：第 3 層的前提是**確認查得到條目**，鮑爾從來沒走到第 3 層——它在「查得到條目嗎」這一關就判 False，直接掉第 4 層。所以**要修的是人名解析，不是放寬第 3 層的門檻**。 🗳**2026-09-16 使用者傾向（尚未定案，原話）**：「人臉畫不出來 我考慮改 **就算維基查不到還是走第三層 讓生圖AI自己判斷**」。 ✅**這個方向有道理的地方**：鮑爾、葉倫這種國際政經人物，生圖模型**本來就知道長相**，查不到純粹是臺灣簡稱對不上維基條目名的**查詢問題**，不是「模型不認識這個人」。拿查詢失敗當「不准畫」的理由，確實是用錯了訊號。 ⚠**但要先想清楚代價，因為第 4 層擋掉的不只是鮑爾**：現行第 4 層同時擋住**模型真的不認識的人**——地方民代、受害者家屬、一般民眾、冷門外國官員。這些名字一旦放行，模型會**照樣畫一張臉出來並掛上真名**（生圖模型不會說「我不認識」），那正是 **B63** 捏臉的原型。而且這種成品**看起來完全正常**，沒有人會發現畫錯人。**同名問題也會跟著放大**（鮑爾＝Jerome Powell／Colin Powell，見上）。 💡**監督建議的收斂做法（三選一或併用，待裁）**：甲｜**放行 ＋ 強制程式端示意圖標籤**——與 **B70** 綁在一起做，Pillow 疊的標籤不會漏也不會寫錯字，「畫錯人」的風險靠「畫面明確標示為 AI 示意」來承擔，**這是三個裡最實際的**；乙｜**放行前加一道身分驗證**——要求消化端連同人名給出**職稱／機構**，後端拿去對 Wikidata 的 P39（職位）／P108（雇主），對得上才放行，對不上仍走第 4 層；丙｜**只對「查得到條目」以外但屬於特定類別的人放行**（例如國際政要／央行／科技業 CEO），其餘維持第 4 層——覆蓋面最窄但最安全。 ⚠**不論採哪一個，都建議與 B70 的程式端標籤同批上線**，不要在標籤還會漏、還會寫錯字的狀態下先放寬畫人的門檻。 📌**這條若放行，第 3 層的語意就變了**：從「查得到條目所以可以畫」變成「一律可以畫」，`portrait_entry_only_notice()` 的通知文字與 `resolve_portraits()` 的註解都要跟著改寫，否則帳本與程式會再次不一致（B66 已經犯過一次）。 🔬**2026-09-17 R7 免費前置批：譯名缺口實測量化（維基查詢免費，8 個案例）**。**落第 4 層（連條目都查不到）**：`鮑爾`、`葉倫`、`鮑威爾`；**落第 2 層（有條目有照片）**：`傑羅姆·鮑威爾`（zh）、`珍妮特·耶倫`（zh）、`川普`、`黃仁勳`。👉**確認無誤：程式邏輯沒錯，錯的是查詢字串**——臺灣財經新聞慣用的短譯名全部對不上維基條目名，而正式全名一查就中。**修的是譯名解析，不是放寬分層規則。**⚖**2026-09-17 使用者已裁：兩個都做**——先補譯名解析（短譯名→正式條目名），查不到時再放行第三層讓生圖 AI 自己判斷。順序是「先精準、後兜底」。⚠**放行第三層的已知風險（監督提醒，使用者知情下裁定）**：`鮑爾` 同時也是 Colin Powell 的臺灣譯名，`_is_human()` 只驗「是不是人」、**不驗「是不是對的那個人」**，放行後可能畫出一張長相正確但身分錯誤的臉。 🛠**2026-09-16 實作完成，但做法與原先設想的不同，理由是實測**。 ❌**兩種自動譯名解析都被實測否決，所以最後用人工對照表**：①**中文全文搜尋**——`photo_lookup.py` 舊註解早就記著否決理由（4 個譯名有 2 個搜到完全不相干的條目：阿拉奇→阿布拉莫維奇、巴薩尼→威尼斯商人）。②**Wikidata 別名搜尋（`wbsearchentities`）**——監督 2026-09-16 另外實測，**在最該解決的那個案例上最糟**：查「鮑爾」回來的是蒙大拿州一個人口普查區、一位荷蘭軍官、一位英國海軍上將和兩個姓氏，**傑羅姆·鮑威爾根本不在結果裡**；「葉倫」「卡利巴夫」「阿拉奇」直接 0 筆。👉**結論：沒有安全的自動解析**，只能逐條實查、確認落在第 2 層才寫進表。 📊**對照表成效（每一條都實查過）**：`鮑爾`→傑羅姆·鮑威爾、`葉倫`→珍妮特·耶倫、`貝森特`、`盧比歐`、`范斯`→JD·萬斯、`奧特曼`→Sam Altman、`蘇納克`→Rishi Sunak、`普拉伯沃`、`安瓦爾`→安瓦爾·易卜拉欣、`洪瑪奈`→Hun Manet、`卡利巴夫`／`阿拉奇`／`瓦希迪`→英文條目名。**13 人全部從第 4 層變第 2 層**，對照組（不存在的人名）仍正確落第 4 層。🔑**其中卡利巴夫／阿拉奇／瓦希迪 正是 `photo_lookup.py` 舊註解點名「整類國際新聞的肖像都卡在這裡」的那幾位**——那句話從今天起不成立了。 ⚠**同名風險是這張表的固有代價，而且程式無法自動化解**：`_is_human()` 只驗「是不是人」、不驗「是不是對的那個人」。「鮑爾」同時是 Colin Powell 的譯名，這張表等於**替短譯名釘死一個解釋**（選臺灣新聞的壓倒性用法）。使用者知情後仍裁定要做。 🔧**第二半改了兩個地方，不是一個**：只改 `resolve_portraits` 不夠——網頁版的 `apply_photo_availability` 會**先在消化階段**用 `exclude_people` 把查不到的人踢出版面，`portrait_subjects` 一空就根本走不到那個分流。兩處共用同一個開關。 🔁**退路**：`PORTRAIT_NO_ENTRY_FALLBACK=off` 兩處一起回到舊行為（整張畫成無人場景）。這是一條「明知有 [B45] 風險、使用者知情後仍裁定要開」的開關，**出事時第一個關的就是它**。 ⛔**還沒驗的**：第二半的實際效果（生圖模型替查不到的人捏出來的臉長什麼樣、notice 有沒有確實顯示）**必須實拍才算數**，這一輪 R7 沒涵蓋。 🔴**2026-09-16 收工前的重要落差，明天第一件事**：第二半（放行第三層）的程式已經寫好，但**預設暫時關著**，而使用者裁定的是開。原因：**打開之後有 20 道既有測試轉紅**，且不是雜項——包含 `test_scene_upload_does_not_lift_iron_rule`（「非肖像用途的上傳不解除鐵律」）、`test_one_missing_photo_blocks_every_face`（「全有或全無」）這類**安全不變式的守門測試**。它們釘的正是 2026-08-18 那次實驗的結論：「有照片的畫、沒照片的畫剪影」生圖模型辦不到，沒照片的那位會被憑空捏臉還掛真名。👉**這 20 題不是過期的測試，而是這條裁定真正要推翻的東西**——逐題判斷哪些該改、哪些是真的防線，要清醒著做，不能在收工前趕。**已隔離驗證**：關掉開關 → 1990 題全綠；打開 → 19 failures＋1 error，**全部可歸因於這一個開關**，沒有其他副作用。啟用只需 `PORTRAIT_NO_ENTRY_FALLBACK=true`，不必改程式碼。⚠**譯名對照表那一半不受影響，已經生效**。| 原第 118 行｜✅ 已做完（清帳補登）｜狀態欄已寫 2026-09-16 已實作；B96 再把退路開關打開（4280aa2） |
| B74 | **消化的 provider 白名單自相矛盾：名單裡有兩個端點不支援 strict JSON schema** | 2026-09-16 監督做 D21 免費驗證時查出（零成本，打 OpenRouter endpoints API） | 🟠 **未修**｜**尚未造成已知事故**（R6 十次都路由到 Anthropic），**但這是上游過載時才觸發的潛在故障，最難重現、也最容易被誤判成「模型壞掉」**｜✅ **2026-09-16 已隨 D21 一併解掉**：`DIGEST_PROVIDER_ORDER` 白名單整組移除，改送 `provider.require_parameters: true`，不支援 `structured_outputs` 的端點（`azure/global`／`amazon-bedrock/global`）會被 OpenRouter 直接排除在候選之外，不會再有被 fallback 選中、strict schema 遭靜默丟棄的機會。測試已釘住新 payload，並以 `hasattr` 反向斷言舊常數確實不存在| **白名單存在的理由與內容互相矛盾。** `main.py:262` 的註解寫明它的用意：「google-vertex 刻意不列：查 `/models/.../endpoints` 的 `supported_parameters`，三個 vertex 端點都**不支援 structured_outputs**，而這條線全程用 strict json_schema。」**但同一份查詢方法套到白名單自己身上，結果是**（2026-09-16 實查 `/api/v1/models/anthropic/claude-sonnet-5/endpoints`）：Anthropic ✅／Claude Platform on AWS ✅／**Azure ❌**／**Amazon Bedrock ❌**（Google ❌，本來就沒列入）。👉**現行 `DIGEST_PROVIDER_ORDER` 預設值 `anthropic,claude-on-aws,azure/global,amazon-bedrock/global`（`main.py:268`）裡，後兩個端點都不支援 strict schema**——正是那段註解想擋掉的東西。 ⚠**觸發條件**：`digest_provider_body()` 送的是 `allow_fallbacks: true` 且**沒有設 `require_parameters`**（OpenRouter 該參數預設 `false`＝允許路由到不支援的 provider 並**靜默丟棄**該參數）。所以第一方與 AWS 都過載時，請求會落到 Azure／Bedrock，**strict json_schema 被無聲拿掉**，模型改用自由格式回答 → 高機率 `parse_failed`。**而後台不會留下任何線索說「這次沒有套 schema」**（與 **B72** 同一個觀測缺口）。 🔎**範圍要講清楚，不要誤植**：**R6 那 10 次的 provider 欄全部是 Anthropic**，所以 **2026-09-16 實測的 3 次 parse_failed 不是這條造成的**（那 3 次是 `finish=length` 截斷）。**本列是潛在風險，不是已發生的事故。** 💡**修法方向（監督建議，待裁）**：甲｜**把 `azure/global`／`amazon-bedrock/global` 移出白名單**——最小改動，但會讓過載時可用的端點從 4 個減為 2 個，**與這份白名單當初「換 provider 不換模型」的初衷衝突**，要先想清楚哪個風險比較大：「沒有端點可用而失敗」還是「schema 被靜默丟棄而 parse_failed」；乙｜**加 `provider.require_parameters: true`**，讓 OpenRouter 自己過濾（Codex 已查證：它是「先過濾、再在合格候選集內 fallback」，與 `allow_fallbacks` 不衝突），**這條比甲精準**，因為它是按「這次實際送了哪些參數」過濾，不是寫死名單；丙｜**兩個都做**，並在稽核裡記下實際命中的 provider（併 **B72**）。📌**監督建議乙＋丙**。⚠但乙要留意：全部候選都不合格時請求會直接失敗而不是降級，**上線前要確認這種失敗有沒有被 `DIGEST_ATTEMPTS` 的重試吃掉、使用者看到的錯誤訊息講不講得清楚**。 | 原第 119 行｜✅ 已做完（清帳補登）｜狀態欄已寫 2026-09-16 隨 D21 解掉（白名單整組移除） |
| B75 | **AI 標題模式下，斷句模型每次都白打一通**——結果沒有任何人讀 | 2026-09-16 使用者提問「斷句不是早就改成生圖模型自己判斷了？」，監督查證後確認 | ⚖ **2026-09-16 使用者已裁：要修，與斷句換 Gemini 同一批**｜🟠 **未修**｜✅ **2026-09-16 已實作並實跑驗證**：`apply_title_break_hints` 加 `composite=` 守衛，`mode=composite` 打 1 次、`mode=ai` **打 0 次**。測試釘住兩條路| **使用者的提問**：「我記得斷句消化現在已經沒有再用了，早就改成生圖模型自己判斷了？」**查證結果：一半對**。**沒有被拿掉**——兩個封面端點入口仍無條件呼叫 `apply_title_break_hints`（十點 `main.py:6454`、YT `main.py:7262`，只有 live24 例外）。**但使用者記得的那件事是真的**：標題分 `ai`／`composite` 兩種模式，**`ai` 模式下整張封面（含標題）由生圖模型畫，程式完全不壓字**（YT `draw_title(s)=not ai_title`，`main.py:7432`／`7440`／`7451`／`7468`；十點 `_cover_ai` 只補貼 Logo／節目標籤／AI示意圖），**斷句算出來的詞組邊界一個字都不會被讀到**。只有 `composite` 會用到——邊界進 `compose._hint_cuts()` 餵給 `_split_line_near_middle()`。 📌**關鍵在預設值**：前台 `state.coverAiTitle` / `ytAiTitle` 預設 `false`（創意 0＝程式壓字），但**拉桿一動到 1 級以上就自動變 `true` 且鎖成必勾**（`app.js:1620`／`1688`）。👉**創意 ≥1 的每一張封面都白打一通斷句 API**。 🔬**實跑驗證（非純讀碼）**：patch 掉 `digest_completion` 後打 `/api/editor/cover`，`mode=composite` 與 `mode=ai` **兩者都是「斷句模型被呼叫次數 : 1」**，送出的模型都是 `google/gemini-3.8-flash`。 💰**代價**：斷句實測中位 2.2 秒（見 D21 的斷句實測），等於創意 ≥1 的封面每張白花約 2–3 秒＋一次付費呼叫。 🔗**這是 [B33] 的放大版**：B33 只抓到「零成本路徑也先打了斷句」，真正的大頭是**所有 AI 標題封面**，而那是 1 級以上的預設。兩件事同一處修。 🛠**修法（範圍明確，無歧義）**：兩個端點在呼叫斷句**之前**就已經算好 `resolved_mode`（十點 `main.py:6422`、YT `main.py:7243`），資料就在手上——把 `apply_title_break_hints` 移到 `composite` 分支之後、或直接加 `if resolved_mode == composite` 守衛即可。⚠**注意 B33 指出的另一半**：呼叫位置還排在 `background_image_base64` 零 API 分支之前，要一起挪才算修完。⚠**不要順手改 live24**：它本來就跳過（單行標題不拆段），那是對的。 | 原第 120 行｜✅ 已做完（清帳補登）｜狀態欄已寫 2026-09-16 已實作並實跑驗證（composite 守衛） |
| B78 | **「無字」檔位在創意階梯 ≥1 時仍畫出創意版面的框與設計元素，應該只剩示意圖插圖** | 2026-09-20 使用者追加 | ⬜ **2026-09-20 使用者列入下一批動刀** | 使用者原話「無字 創意高的時候 也不該出現創意階梯的框或設計 只要示意圖插圖」。**與 D14／F20 同一條線**：D14 已裁「拉桿六段、無字放最左端」，F20 已實作 `NO_TEXT_DENSITY_RULES` ＋ `NO_TEXT_FINAL_REMINDER`（逐項關掉 `[標題]`／`[內文小標]`／`<蓋章>`／`<底帶>`／卡片數／創意階梯的**字級**條文）——**關掉的只有字級，沒關掉創意階梯的版面框與設計元素**，所以創意 ≥1 時框還在。**動工前先量現行成品**（無字 × 創意 0–4 各一張），確認「框」是創意階梯條文畫的、還是版型底稿本來就有；兩者修法完全不同。 | 原第 123 行｜✅ 已做完（清帳補登）｜`2c7faad`；守門測試 `tests/test_stage2_density_seed_no_text.py:604-614` |
| B79 | **十點不一樣在創意階梯模式下標題實質被鎖成三行，兩行不被接受** | 2026-09-20 使用者追加 | ⬜ **2026-09-20 使用者列入下一批動刀**｜⚠**待實證**（使用者原話是「好像」） | 使用者原話「十點不一樣 創意階梯時 可以接受只有兩行標題（現在好像強制都是三行）」。**程式現況（本 session 查過）**：`editor_formats.py:1107` 註解寫的是「**最多** 3 行」，不是強制三行；但 `compose.py:419-473` 記著「**白黃紅三行是固定的視覺**」——2026-09-08 第二輪裁決，雙切一度放寬到 4 行，使用者看樣張後改回 3 行，理由是「第四行沒有顏色可配」。**所以嫌疑是逐行配色（`COVER_TITLE_LINE_COLOURS`）反過來逼出三行，不是行數上限。****先查再改**：①撈成品確認兩行標題實際出不出得來；②看兩行時是不是只剩白黃、被判定視覺不完整。**若要把「兩行合法」寫死，等於追加裁決 D16（大標題各檔字數與行數），動工前要問。** ⚖**2026-09-20 已查證（AICG-wt-ladder worktree）**：本列的配色假說**在創意 1 級起不成立**——`compose.py` 註解自己寫明（2026-09-11 起）配色耦合只剩創意 0 級，1 級起改由 `editor_formats.cover_line_annotation` 依內容決定，跟行數完全脫鉤；**0 級兩行也不成立**——兩行只需要白＋黃兩色，不需要湊出第三個紅，沒有「被判定視覺不完整」這件事。生圖 prompt 那邊（`main._lines_block`）送給模型的行數說明是動態算的 `exactly {len(lines)} lines`，不是寫死 3。**①撈成品**：正式站 2026-09-18～09-20 十點雙切最近 23 筆生成紀錄（46 個標題格）實測 **31 格（67%）是 2 段、15 格（33%）是 3 段**——2 段在近期成品裡是多數，不是被強制成 3，「現在好像強制都是三行」這個印象與正式站近期實況不符。全鏈路唯一真的寫著「3 段優先」的地方是消化 prompt（`COVER_TITLE_DIGEST_SYSTEM_TEN`／`_TEN_FULL` 的「Prefer 3 segments」那句）——已改成中性措辭（段數由內容判斷，不再預設 3、也不再把 2 講成退路），2／3 段合法範圍與字數上下限一個數字都沒動，**不需要 D16**。這句改動對模型實際輸出的影響**未經驗證**（要打付費消化模型才能量到差異，本輪未獲授權），是「這句話讀起來確實偏向 3」的文字修正，不是量測到行為改變後的修正。順帶修正 `COVER_TITLE_DIGEST_SYSTEM_TEN_FULL` 上方一句過期的模組註解（原寫「一律 3 段」，是 09-08 舊裁決字面，09-11 已 relax 成 2 或 3 但註解沒跟著改）。 | 原第 124 行｜✅ 已做完（清帳補登）｜`2c7faad`；兩行已中性化，`tests/test_cover_brands_and_digest_chips.py:142-159` |
| B88 | 🔴**簡稱對照表只接了兩條封面路徑，一般 CG（記者／編輯CG）完全沒接——「川習」被畫成背影** | 2026-09-22 使用者回報正式站後台 14:55:07 那筆（操作者 許岱軒）：「川習被畫成背影了 明明已經有對照表」 | 🟢**已接（2026-09-22，VERSION 260922-05）**｜B82 修的是 `resolve_cover_visuals`／`derive_yt_cover_plan` 兩條；`name_aliases` 的模組 docstring 當時寫「**不碰一般 CG 那條路**…要開另案評估」（那是上一輪的範圍註記，不是使用者裁決）。結果 B82 的因果鏈在一般 CG 上原封不動重演：簡稱推導不出人名 → `portrait_subjects` 交白卷 → 查不到參考照 → `news_prompt.py:334`「沒有附照片的具名真人必須畫成背影或剪影」→ **背影**（B82 那次是零參考照＋模型自己編臉，這次多了一步退到背影，成因同一個）。修法：`generate()` 的 `digest_completion(news_text=...)` 接上 `alias_hint_block(news_text, user_instruction)`；措辭從封面專用的「the side whose headline」改成中性的「in the material above」。LINE 走 `generate_news_image → generate()`，一併涵蓋；`hybrid_digest` 的 schema 沒有 `portrait_subjects`，不需要。**2026-09-22 使用者追加：「"川習" 也要列入條目」**——標題常寫「川習通話」「川習互動」，三個字的「川習會」比對不到。已收，但它違反原本「只收複合事件詞」的精神，兩個字會在**跨詞邊界**碰撞（「四川習俗」「四川習近平視察」），誤命中的代價是把川普畫進一則四川新聞裡，比畫成背影嚴重得多。所以配一張 `BLOCKING_PREFIXES`（`川習` → 前一個字是 四銀品香德市河旭湯 就不算命中），逐次判定而非整篇放棄，仍然是查表不是推論；長短形只留先命中的那一條（「川習會」的素材也含「川習」，兩條都回會讓同一組名字出現兩次）。守門測試加了「兩個字的條目一律要有阻擋字表」這條通則。⚠未實機驗證。 | 原第 132 行｜✅ 已做完（清帳補登）｜`2bb7797`、`e9d45c0`；一般 CG 已注入簡稱表，`main.py:3145-3155` |
| B95 | 🔴**DEV 消化全倒、正式站正常** | 2026-09-22 使用者回報「DEV全部失敗 但正式版正常」 | 🟢**真因確定＋順手修掉白等（2026-09-22，VERSION 260922-11）**｜B94 上線後第一筆失敗就寫出真話：`上游 402 · This request requires more credits, or fewer max_tokens. You requested up to 16000 tokens, but can only afford 15030.`——**DEV 那把 OpenRouter 金鑰餘額見底**，`MAP_DIGEST_MAX_TOKENS=16000` 的請求 OpenRouter 直接拒收。正式站是另一把金鑰所以不受影響。**兩個候選都證偽**：跟 B91 的 prompt 無關（402 在請求送進模型之前就擋掉了，模型根本沒跑），跟份量也無關。⚠**儲值是使用者那邊的事，程式面修不了**。程式面修掉的是另一件事：402 走通用 `APIError` 分支，被當成「上游間歇脫軌」**重試滿 `DIGEST_ATTEMPTS=5`**——餘額不會因為重試變多，那五次是純粹的等待，使用者每按一次空等 9 秒才看到錯誤。新 `credits_exhausted_error(exc)`：狀態碼 402 就立刻 raise「AI 服務額度不足，請儲值後再試（含上游原文與差多少）」，`generate()` 與 `hybrid_digest()` 兩條都排在寫 `last_detail` 之前。金鑰無效（`AuthenticationError`）與用量超限（`RateLimitError`）本來就是不重試的，402 漏掉只因為 SDK 把它丟成一般的 `APIStatusError`。⚠只認 402 一個碼，其他 4xx 照既有分類走。守門測試釘「402 停手、500/502/503/403/429 照常重試」與「停手一定排在 `last_detail` 之前」。 | 原第 136 行｜✅ 已做完（清帳補登）｜`26d5dcd`：402 不重試並顯示原始原因 |
| B90 | **一般 CG 的來源標籤不該被程式強加「畫面來源：」前綴** | 2026-09-22 使用者裁決：「『畫面來源:』欄位 預設不要任何文字 讓使用者根據需要填寫 因為也可能是資料來源 也可能是兩者都要寫」 | 🟢**已改（2026-09-22，VERSION 260922-07）**｜`compose.paste_disclaimer_note(kind="source")` 原本套 `vstrip_source_text`，自動補「畫面來源：」。那個自動補在 2026-09-09 是**為直標設計的**——那邊 AI 判出來的是光禿禿的來源名（「美聯社」），前綴本來就該由程式補。但一般 CG 這一欄是**使用者自己打字**，自動補前綴會讓「資料來源：中央社」變成「畫面來源：資料來源：中央社」，而「兩者都要寫」根本打不出來。修法：這一條路改成**逐字照貼**（`(source_text or "").strip()`），`vstrip_source_text` 本身與**所有直標／十點／YT 呼叫端一個字都不動**。UI 連帶改：兩頁的欄位名「畫面來源」→「來源標籤」，placeholder 改示範整句（「整句照打，例：畫面來源：美聯社」）。守門測試不用 spy 用**成品位元**當 oracle——自動補前綴時「美聯社」與「畫面來源：美聯社」會貼出一模一樣的圖，逐字照貼則必然不同，這是 `vstrip_source_text` 有沒有偷偷介入的唯一可靠證據。⚠`maxlength` 仍是 40（前後端一致），兩種來源都寫大約 20 字內夠用，不夠再放寬。⚠未實機驗證。 | 原第 149 行｜✅ 已做完（清帳補登）｜`b84036c`；一般 CG 改逐字貼，`compose.py:455-465` |
| F37 | **上傳真實照片時沒有地方填「畫面來源」，而且一律被壓上「AI生成示意圖」** | 王結玲 2026-09-15 LINE 回報（她用十點不一樣），使用者轉述 | ⬜ 🌊**第二波**（2026-09-15 使用者裁定：等第一波收工才動工）｜**全站通用，不是十點專屬** | **王結玲原話**：「我找到一個BUG／如果我上傳照片，不要AI示意圖，也要打上照片來源，沒有地方可以打」。**使用者回覆原話**：「沒錯，現在真的沒有這個欄位，這是漏做了。現在預設都是強制用程式壓「AI生成示意圖」，我要把這個欄位開出來，讓使用者可以選填，如果是放置原圖的話，讓使用者可以在這個欄位寫畫面來源」。⚖**使用者已定調**：欄位要開、選填、**全站通用**（她雖然是在十點不一樣遇到，但不要只做那一個版型）。✅**監督查證：輪子已經有一半，不要重造**——「畫面來源」欄位**已經實作在 YT 直播直標那條線**（`index.html:630`「只要填來源名，『畫面來源：』會自動補上」、`main.py:5218` 由 compose 補前綴；成品實例見 `D:\Downloads\AICG\後台紀錄\20260915\曹雪卿成圖\131247-ec36fb8a7ae8.png` 右上「畫面來源：美聯社」）。**本單應該是把這個既有欄位推廣到其他版型，不是新造一套。** ⚠**但有一個既有規則會擋路，動工前先確認是不是要一起改**：`main.py:3932` 的註解明寫「親自提供的真實素材——那個 override 的語意不成立，『AI示意圖』標籤照舊要留」，也就是**現在有一條刻意寫下的規則：就算使用者自己提供真實素材，AI示意圖照壓**。王結玲要的「不要AI示意圖」直接牴觸這一條。**這一條當初是為了播出合規寫的還是順手寫的，要查清楚**——若是合規要求，該做的是「有來源標示時改壓來源、而不是無條件拿掉標籤」。與 **B28**（畫真人掛真名時的 AI示意圖 標籤）同一叢，**建議一起看**。另 `main.py:4374`／`5736` 已經有「底圖是不是 AI 生的」這個旗標，判斷基礎是現成的。 | 原第 163 行｜✅ 已做完（清帳補登）｜`31a68e8`、`5c456aa`；來源／示意標籤已全站化 |
| B2 | web-refine 輸出改掉尺寸（1920×1080 → 1748×924） | YT 封面生圖模式…R3 | ⬜ | 非 16:9，後續任何疊圖都會歪。可能是 B1 的上游真因；要查 refine 線有沒有鎖長寬比 | 原第 426 行｜🗃 已過時／被取代｜後來 D24 重訂規格；高字量不置框、低字量維持舊尺寸，`2bb7797`、`tests/test_plan_three_features.py:198-225` |
| B3 | 前台 YT 塊高說明與後端表對不上 | 創意階梯三條線對齊清點 G6 | ⬜ | app.js `YT_CREATIVITY` 寫 26/31/36/41%，`YT_BRIEF_SPECS` 是 32/36/40/44%。`git log -S'"32%"'` 指到 `b2e0e98`「YT 塊高表訂錯改正」——**表改了、前台說明沒跟上**。純文字修正，零風險，可單獨先送 | 原第 427 行｜✅ 已做完（清帳補登）｜前後端高度改由同組常數推算，`editor_formats.py:1605-1623` |
| B4 | `cover_ai_title_style_clause` docstring 過期 | 創意階梯三條線對齊清點 G8 | ⬜ | 還寫「招式段夾在設計條文與 FIXED 之間」，但招式 2026-09-11 第二輪就搬進 `cover_design_brief` 了 | 原第 428 行｜✅ 已做完（清帳補登）｜`5934a30` 修正兩處過期 docstring |
| B5 | `cover_design_brief` docstring 與行為矛盾 | 創意階梯三條線對齊清點 G2 | ⬜ | docstring 寫「一顆 seed ＝一種長相，重現得出來」，實際呼叫端沒傳 seed（吃系統熵）。D1 已裁走「請求帶 seed」，改完行為後 docstring 就對了 | 原第 429 行｜✅ 已做完（清帳補登）｜`5934a30` 修正兩處過期 docstring |
| B6 | 消化品質閘只裝在 `/api/generate`，封面線全部裸奔 | 消化品質閘只裝在 `/api/generate` | ⬜ | 截斷／字元污染／頻道洩漏／簡體異體字四道檢查，五個 `digest_completion` 呼叫點一道都沒過 | 原第 430 行｜✅ 已做完（清帳補登）｜封面 schema、違規檢查及重試已存在；`editor_formats.py:1335-1375`、`main.py:7238-7258` |
| B7 | `DIGEST_MAX_TOKENS` 上限太緊，複雜稿件常截斷 | 同名章節 | ⬜ | 未重新確認 | 原第 431 行｜✅ 已做完（清帳補登）｜`d8cb1ee`；目前一般／地圖為 12000／16000，`main.py:411-426` |
| B8 | `/api/generate` 缺重試機制 | 同名章節 | ⬜ | 未重新確認 | 原第 432 行｜✅ 已做完（清帳補登）｜`6b9689b` 首次加入重試；現行重試與死線在 `main.py:386-430` |
| B10 | 附圖尺寸失控：文件類附圖被縮到看不清 | 自動判斷版型不穩 Y3 | ⬜ | 同一批裡兩個極端：16:30 佔滿上半部但下方留大空白、16:43 縮成左側小圖完全看不清。使用者明寫「有兩張照片可以使用」，系統沒有「文件類附圖要大到看得清楚」這條規則 | 原第 435 行｜🗃 已過時／被取代｜已由「原圖放置」用途取代單一參考圖流程；`news_prompt.py:339-346` |
| B11 | 網頁版缺獨立指令欄位，使用者把指令寫進新聞原文 | 自動判斷版型不穩 Y4 | ⬜ | `(有兩張照片可以使用)`／`(粗體字)`／`(ICON)`／`(蓋字章)`。目前沒被畫進圖裡、粗體確實生效，但需求訊號明確；與 F13「LINE 訊息內帶指令」是同一件事的網頁版 | 原第 436 行｜✅ 已做完（清帳補登）｜`3254a17` 新增獨立指令欄並接後端 |
| B12 | 行內指令時靈時不靈 | 原文內嵌指令要規則化 Y6 | ⬜ | 現況：文內解析規則第 1 條已涵蓋「plain prose」指令，與專用指令欄**並存機制早就明文寫死**（兩者都遵守、衝突時專用欄優先、互不取消）。問題在**圓括號夾注**沒有明文承認，靠模型自律 → `(ICON)` 四次只中一次、`(蓋字章)` 完全沒中。基準已量：1/4 | 原第 437 行｜🗃 已過時／被取代｜網頁版已改用獨立指令欄；`3254a17` |
| B15 | 外框配色跑掉（紫色 letterbox） | 追加修改連打語意漂移 Z4 | ⬜ | 11:47 那張上下留白帶是紫色，其餘都是標準深藍。要查是版型底色還是模型把背景畫成紫色後被裁切帶進來 | 原第 440 行｜🗃 已過時／被取代｜安全框與滿版流程已多次重寫，現行規則明禁外框；`news_prompt.py:160-170` |
| B19 | density 完全管不到大標題長度 | 大標題字數要納入字多／字少 | ⬜ | 編輯版樣板只寫「大標題強制拆分為兩行」**沒寫字數**；「每行不超過 15 字」管的是 [內文小標]；四個 density 區塊（含 MINIMAL 的 12 字）全部只釘內文與總字數。**拉桿從最左拉到最右，畫面上最大的那行字理論上一個字都不會變**——與 F1「字多字少差距不夠」是同一病灶的另一面 | 原第 444 行｜✅ 已做完（清帳補登）｜`707bf12` 實作四檔標題上限 |
| B22 | 附圖是資訊載體時仍被當裝飾素材 | 許岱軒 0914 下午 H2 | ⬜ | **B10 的加強版**：他附的真 ISW 勢力圖就是這張圖存在的理由（原文寫明「來源: ISW」），卻被縮成中央小圖，約**七成畫布**給了 AI 戰情室裝飾背景，地圖英文完全看不清。要補的是**佔比與主從**規則：附圖被判定為資訊載體（地圖／圖表／文件／截圖）時它就是主視覺 | 原第 447 行｜✅ 已做完（清帳補登）｜原圖放置已成預設用途；`app.js:1146`、`news_prompt.py:339-346` |
| B25 | AI 憑空畫地理＋自行加入原文沒有的地名 | 許岱軒 0914 下午 H4 | ⬜ | 15:44 後全是 AI 畫的葉門國界與勢力範圍（最高風險項，見 `docs/error-cases/2026-09-10-台灣行政區界-錯誤-分析.md`）。**地理正確性標待人工複核不自行判定**；但可確定成品出現原文沒有的地名「沙那」，違反 `CONTENT_FIDELITY` 第 1 條 | 原第 450 行｜✅ 已做完（清帳補登）｜地圖改用驗證點位，無點位不得加標記；`news_prompt.py:307-311`、`dce64ee` |
| A3 | 三份創意說明表的單一真相源 | 創意階梯三條線對齊清點 G7 | ⬜ | B3 | `CG_CREATIVITY`／`COVER_TITLE_CREATIVITY`／`YT_CREATIVITY` 三份手寫陣列，沒有東西釘住它們跟後端表一致——B3 就是這樣漏掉的 | 原第 458 行｜✅ 已做完（清帳補登）｜抽籤池集中至 `creativity.py:335-336`；`6dab2cc` |
| A4 | 版型功能對齊盤點剩餘缺口 | 版型功能對齊盤點 | ⬜ | D9、D10 | 缺口 1、2 已補；剩 3（CG asis 未定義）與 5（hybrid 全無），兩者都已拆成 D9／D10 | 原第 459 行｜🗃 已過時／被取代｜已由能力矩陣取代人工盤點；`editor_formats.py:2493-2583` |
| A7 | **創意拉桿在 YT 雙則底圖上完全沒有作用（單則有、雙則沒有）** | 由 B45 調查展開（2026-09-15，監督本機實測） | ⬜ 🌊**第二波**（跟 B45 同一條線） | 單則走 `_yt_cover_background` → `editor_formats.YT_COVER_VISUAL_PROMPT_TEMPLATE`，模板有 `{creativity}` 槽；**雙則走 `yt_dual_background` → `_cover_panel_image` → 十點那支 `COVER_VISUAL_PROMPT_TEMPLATE`，模板沒有 `{creativity}` 槽，函式參數列也收不到 creativity**。本機實測雙則面板的最終 prompt 出現 `creativ` **0 次**。使用者在前台拉了拉桿、雙則的底圖長相不會有任何改變——這與 2026-09-13 使用者對 live24 的裁決「標題完全沒有被創意階梯影響 這是錯的」是同一種病，只是這次發生在**底圖**而不是標題。⚠**修之前要先裁**：雙則兩格是半版方圖，十點那支模板是為方圖寫的；直接換成 YT 那支 16:9 模板會連構圖規則一起換掉。 | 原第 461 行｜✅ 已做完（清帳補登）｜`4491a11` 將創意階梯接上合成底圖 |
| A8 | **封面五版型的安全框開關在前台是隱藏的，但它的殘值仍會被送進後端** | 由 B51／B52 盤查展開（2026-09-15） | ⚖ **2026-09-16 使用者已裁：要做**——切到封面版型時 `state.safeFrame`（與同樣被隱藏的 `stamp`）強制歸零，不再把看不見的殘值偷渡進請求｜⬜ **待裁** | `ten_cover`／`yt_live_cover`／`yt_hourly_cover`／`yt_live24_cover`／`yt_hot_cover` 五個版型的 `hides` 都有 `safeFrame: true`，使用者在封面版型下**看不到也改不了**那顆開關；但 `state.safeFrame` 不會因為切版型而重置，追加修改（B52 的合成版路徑）與任何讀 `state.safeFrame` 的地方拿到的是**使用者上一次在 CG 模式留下的值**。**隱藏而不歸零＝把一個看不見的狀態偷渡進請求**，使用者無從得知也無從更正。待裁：切到封面版型時是否應該把 `state.safeFrame`（以及同樣被隱藏的 `stamp`）強制歸零／或在送出時一律忽略。 | 原第 462 行｜✅ 已做完（清帳補登）｜`cbd1e95`；測試 `tests/test_a8_cover_safe_frame_reset.py:43-89` |
| F19 | 行內指令規則化實作 | 原文內嵌指令要規則化 | ⬜ | 依賴 D12。三塊：明文承認圓括號夾注（含與內容括號的區分反例）、局部指令綁定到相鄰句子、版面元素標記對應 marker。驗收：同段文字跑 N 次統計標記命中率 | 原第 470 行｜🗃 已過時／被取代｜主要使用情境已改走獨立指令欄；圓括號舊方案不再是唯一入口 |
| F23 | 大標題字數納入 density 實作 | 大標題字數要納入字多／字少 | ⬜ | 依賴 D16。注意封面線另有 pydantic 40／60 字與 compose「標題太長」400 三道**不同層**的上限（那是使用者自己打的標題），別打架；逐字模式與無字檔兩個端點要明確排除。**與 F1 同一批驗收**，不要分兩次跑 | 原第 474 行｜✅ 已做完（清帳補登）｜與 B19 同由 `707bf12` 實作 |
| F3 | 畫風池實作 | 畫風池 | ⬜ | 依賴 D8 | 原第 478 行｜✅ 已做完（清帳補登）｜變化池已集中實作；`creativity.py:87-336`、`765dc08` |
| F7 | 「以圖生圖」 | 同名章節 | ⬜ | 未重新確認 | 原第 482 行｜✅ 已做完（清帳補登）｜已有 AI 改圖用途；`news_prompt.py:348-364`、`14017db` |
| F9 | 真實人物圖須參考真實樣貌 | 同名章節 | ⬜ | 未重新確認 | 原第 484 行｜✅ 已做完（清帳補登）｜肖像查照與最多三人參考已上線；`304d3c7`、`main.py:4699-4876` |
| F10 | 禁止生成未提及的品牌 LOGO | 同名章節 | ⬜ | 未重新確認 | 原第 485 行｜✅ 已做完（清帳補登）｜未提品牌禁止規則已存在；`news_prompt.py:183-198` |
| F11 | 使用者上傳參考圖（人物照＋一般視覺參考） | 同名章節 | ⬜ | 未重新確認 | 原第 486 行｜✅ 已做完（清帳補登）｜`3254a17`、`14017db`；四種附圖用途已接通 |
| F12 | LINE 編輯版＋未選角色一律擋下 | 兩個同名章節（07-31、08-04） | ⬜ | 未重新確認 | 原第 487 行｜🗃 已過時／被取代｜現行產品改採記住角色並預設記者；`line_bot.py:54-113` |
| F15 | Webhook 訊息過濾器 | 同名章節 | ⬜ | 未重新確認 | 原第 490 行｜✅ 已做完（清帳補登）｜非文字／空白事件已過濾；`line_bot.py:393-424`、`tests/test_line_bot.py:87-99` |
| F16 | 漲跌配色符合台灣習慣 | 同名章節 | ⬜ | 規則已注入 CG 線；**與 D2 的禁綠正面衝突**，兩者要一起裁 | 原第 491 行｜✅ 已做完（清帳補登）｜紅漲綠跌規則已在兩階段生效；`news_prompt.py:200-206` |
| F17 | 給主管的專案進度報告（單頁 HTML） | 同名章節 | ⬜ | 未重新確認 | 原第 492 行｜🗑 使用者裁定不做｜使用者 2026-09-23 清帳時勾選丟掉 |
| F18 | 以主管報告為底稿做投影片 | 同名章節 | ⬜ | 未重新確認 | 原第 493 行｜🗑 使用者裁定不做｜使用者 2026-09-23 清帳時勾選丟掉 |
| F26 | 生成速度：生圖端的兩個旋鈕（已評估，暫不動） | 同 F25 | ⬜ **不建議動** | `quality` 現為 `medium`（`main.py:3000`，吃 `OPENAI_IMAGE_QUALITY`），降 `low` 會快但畫質掉；換 `flare` 比 `sunburst` 快，但 `main.py:2619` 註解記明當初就是為畫質選 sunburst。**兩個都是拿畫質換速度**，上鏡的東西不值得。留紀錄避免日後有人重新發現又重新評估一次 | 原第 496 行｜🗃 已過時／被取代｜帳本已評估「不建議動」；現行策略改以模型、推理與解析度分流 |
| F28 | 生成速度：思考上限可能還有空間 | 同 F25 | ⬜ | `DIGEST_REASONING_MAX_TOKENS = 2000`（`main.py:202`）是保守值。註解裡觀測到的思考 603–4873 是**封頂前**的數字，封頂後實際用多少沒量過。往下壓要小心截斷，**依賴 F25 的量測**。與 F20（無字檔位直接跳過整個 digest 呼叫）同向 ⚠**2026-09-16 更正：本列引用的 `DIGEST_REASONING_MAX_TOKENS = 2000` 已經不存在了**（D21 動刀時整組刪除，見 D21）。而且更根本的是——**那個旋鈕從頭到尾沒有生效過**：實測 Sonnet 5 會忽略 `reasoning.max_tokens`，送 2000、實際吃 4005～11999。所以本列原本寫的「封頂後實際用多少沒量過」問錯了問題，正確的答案是「根本沒封到」。現行封頂改送 `reasoning.effort`（預設 `low`），實測 reasoning_tokens 從 4005–11999 掉到 0–842，**本列想要的效果已由 D21 達成**。狀態欄留給使用者裁，監督不自行結案。| 原第 498 行｜🗃 已過時／被取代｜舊 `reasoning.max_tokens` 已被 `effort` 與 Gemini 取代；`main.py:327-383`、`9bde3c5` |
| M5 | **架構矩陣化：橫向＝版型、縱向＝模組，改模組即連動所有版型** | 2026-09-16 使用者在 B61 事故後提出 | ⚖ **2026-09-16 使用者已裁：先做 L1 就好**（出口強制層止血，解掉 B61／B62）。**L2 矩陣擴充與 L3 前端不再自組 prompt 暫不動工**，等 L1 上線看效果再談，屆時重新提案。⚠ 本列其餘內容是當時的評估紀錄，保留備查 | **使用者原話**：「現在多了各種版型，常常是 A 版型有修、B 版型沒有收到，這代表我當初的架構沒有設好……把功能拆成模組化，橫向是不同版型，縱向是不同模組，以後修改就改模組連動」。✅**監督：問題判斷完全正確，而且今天有六個獨立證據**——B61（六個封面模板各缺真人條款）、B62（hybrid.js 與 app.js 不同步）、B65（三支能力函式各寫一份環境判斷、拼法還不同）、A3（三份創意說明表沒有單一真相源）、A4（版型功能對齊盤點剩兩個缺口）、A7（創意拉桿在雙則底圖完全沒作用）。**這些全都是同一個形狀：同一條規則被複製到 N 個地方，新版型只複製了構圖沒複製規則。**⚠**但監督反對「一次性重新架構整個網站」，理由三條**：①**風險**——1768 個測試、prompt 凍結快照、天天上線在用，一次性重寫會讓所有進行中的修法停擺數週，而 prompt 行為的回歸**無法自動驗證**（視覺輸出要人眼看）；②**今天的洞不需要等重構才能補**——出口強制層一刀就能讓 B61／B62 同時消失；③**起點已經有了**：`editor_formats.FORMAT_CAPABILITIES`（2026-09-14 模組化第 1 步）**就是使用者說的那張橫縱表**，只是目前 13 欄、只管前端顯示與旗標，**沒有管 prompt 規則**。應該是**擴充它**，不是另起爐灶。💡**監督建議的漸進三層（每層可單獨上線、單獨驗收）**：**L1 出口強制層**（＝下一批第一刀）——鐵則在出口一次注入，版型模板只留構圖差異，配一支「所有生圖路徑都必須含鐵則」的測試；**L2 矩陣擴充**——把 prompt 模組（真人／附圖／標題／日期牌／創意／安全框／示意圖）變成矩陣的欄，版型只宣告用哪個模組、參數多少，M1（`refs.py`）／M2（`title_policy.py`）是它的前兩塊；**L3 前端不再自組 prompt**——`app.js` 與 `hybrid.js` 現在各自拼 prompt、靠 parity 測試逐字比對維持同步（B62 就是這樣漏的），最終要改成後端提供組裝、前端只送參數。⚠**硬限制**：L1／L2 都會改到 prompt 文字，**撞 `tests/test_reporter_prompt_frozen.py` 的凍結快照**，動工前要先要到重凍預算。📌**待裁**：甲＝照上面三層漸進（監督建議）；乙＝使用者仍要一次性重寫，那要先凍結新功能、排一段專用時間，並接受期間不上新版型。 | 原第 606 行｜✅ 已做完（清帳補登）｜已完成裁決範圍 L1 出口強制層；`65d4ec3` |
| R1 | 十點封面「側邊小標籤」 | 同名章節 | ⏸ | 前端已先藏，等驗收 | 原第 612 行｜✅ 已做完（清帳補登）｜`7b9ace5`；欄位與規則在 `editor_formats.py:358-378`、`main.py:6660-6676` |
