# Stage 2 實作計畫（2026-09-14）

> **實作前提**：本計畫只涵蓋 `docs/plan-20260914-第一波三刀.md` 文末〈Stage 2 重排＋D16 修訂〉、〈CP1 放行＋分工〉、〈D16 二次修訂〉所列的 2-1 至 2-9。`F2` 不在範圍；不得改 `editor_formats.py` 的 `_BROADCAST_STAMP_ON`／`_BROADCAST_STAMP_OFF` 第 6 條。
>
> **分工**：實作者不跑付費生圖驗收；生圖輪另開交辦單給「測試」session。每輪前先報張數 × `$0.04` 與使用哪把 key。

**Goal：** 讓 CG 的文字密度、標題上限、螢光綠安全、seed 可重現性、創意變化池與無字檔位，依既定裁決依序落地。

**Architecture：** `main.py` 是 CG 消化 prompt、Pydantic request 與端點的匯流點；`creativity.py` 應持有可跨線重用的抽籤資料／純函式；`editor_formats.py` 只保留封面／YT 特有的幾何與 prompt 組裝。前端僅保存與遞送使用者可見的 density、seed、創意值，不在前端複製後端規則。

**Tech Stack：** FastAPI、Pydantic、Python `unittest`、原生 JavaScript。

## 全域限制

- 分支固定 `exp/redesign-20260903`；程式碼**只留此分支**，不 cherry-pick 到 `main`。本文件與 MASTER／TODO 類帳本才依規則回 `main`。
- 先重新確認工作目錄、分支和 `git status --short --branch`。目前工作樹已有 Stage 1 的 `main.py`／`editor_formats.py`／`news_prompt.py` 未提交修改；Stage 1 完成前不得編輯它們。
- 先寫失敗測試，再寫最小實作；全套命令是 `PYTHONIOENCODING=utf-8 .venv/Scripts/python.exe -X utf8 -m unittest discover -s tests`。
- 禁綠僅指**螢光綠／chroma-key green**；深綠、墨綠、橄欖綠及臺灣財經「下跌＝綠」皆允許。
- D16 字數一律以「移除所有空白、`<`、`>` 後」的可見字元計算；`verbatim` 不限。
- 編輯版兩行規則只在 `main.py:967` 的樣板處理；播出鏡面的單一 `[標題]` 行是正確的後置格式約束，完全不改。
- CP1 允許 2-1／2-2 刻意改變記者 prompt，但只可在 2-3 **重凍一次**；F2 另走 CP1b。

## 會修改／新增的檔案

| 檔案 | 責任 | Stage 2 修改範圍 |
|---|---|---|
| `main.py` | CG request、density prompt、創意 prompt、`/api/generate` | D16、CG 全等級螢光綠規則、F0、CG 接變化池、D3、F1、無字 density |
| `creativity.py` | 共用變化池、seeded RNG、共用配件選取 | 把配件件數與不依賴封面幾何的選取邏輯移入，收窄「禁綠」池定義 |
| `editor_formats.py` | 十點／YT 的封面專屬 prompt 與幾何 | 改為呼叫共用配件 API、收窄封面禁綠用語；不動 broadcast headline 規則 |
| `app.js` | density／創意／封面／YT 請求 payload 與重新生成狀態 | 六段 density UI、三條線 seed 建立／遞增／送出 |
| `index.html` | density 拉桿的可見端點與提示 | 六段拉桿值域與「無字」標籤 |
| `tests/test_reporter_prompt_frozen.py` | 記者＋新增編輯的 digest prompt 快照守門 | 重凍後改為雙角色四組 digest fixture |
| `tests/fixtures/*-digest-*.txt` | digest prompt 的逐位元基準 | 更新 4 份記者、**新增 4 份編輯** |
| `tests/test_digest_density_ladder.py` | density 順序、前後端 parity、各檔語意 | 改為六段並釘四個標題上限／無字排第一 |
| `tests/test_cg_creativity.py` | CG 創意條文與 request wiring | 釘 seed 帶動 pool、3／4 級配件與 level 0 禁螢光綠 |
| `tests/test_creativity_module_20260911.py` | 共用模組持有權與 UI 名稱同步 | 釘移轉後的配件 API 與不帶封面幾何依賴 |
| `tests/test_cover_title_creativity.py`、`tests/test_yt_hourly_creativity_20260911.py`、`tests/test_creativity_p3_rng_pins_20260911.py` | 既有封面／YT seed 穩定性 | 調整 import／別名斷言，保留既有 RNG pin；除非有意變更既有封面件數，fixture 不重產 |
| 新增 `tests/test_stage2_density_seed_no_text.py` | 跨功能回歸測試 | D16、D2、F0、D3、無字的精準契約，避免把測試散進舊檔而失去可讀性 |

---

## 2-1｜D16 標題字數＋行數（先破凍結）

**修改位置**

- `main.py:963-976`：僅編輯樣板的 `[標題]` 格式說明。
- `main.py:989-1054`：四份 density block。
- `main.py:1495-1598`：確認注入順序不變；無須為 D16 增加分支。
- `tests/test_digest_density_ladder.py:33-100`、新增 `tests/test_stage2_density_seed_no_text.py`：靜態規則與 endpoint model 測試。

**實作規格**

1. 將編輯樣板 `main.py:967` 的硬句改成「預設兩行；只有 `minimal` 可依可讀性單行，非強制單行」的**基礎規則**。因樣板本身不知道 density，該句不得讓 `simplified`／`standard`／`maximum` 也鬆綁。
2. 在四個 density block 以後置 override 明寫標題限制，並每檔都重複完整計數口徑：

   | block | 實際條文責任 | 上限／行數 |
   |---|---|---|
   | `SIMPLIFIED_DENSITY_RULES` (`main.py:1016`) | 補上現行完全缺失的 headline 字數，並壓回兩行 | `13`；兩行 |
   | `MINIMAL_DENSITY_RULES` (`main.py:1033`) | 蓋掉 simplified 的 13 與兩行硬規則 | `10`；可單行但不強制 |
   | `STANDARD_DENSITY_RULES` (`main.py:989`) | 補字多檔護欄 | `18`；兩行 |
   | `MAXIMUM_DENSITY_RULES` (`main.py:1046`) | 蓋掉 standard 的 18 | `22`；兩行 |

   四段皆要求：「`[標題]` 的可見字元不超過指定值；計數前移除 whitespace 與 `<`／`>` marker；marker 本身不算；不得為了壓字而刪改原有事實。」
3. 不在 `VERBATIM_DENSITY_RULES` 增加上限，也不改它「使用者文字不動」的語意。
4. 不改 `editor_formats.py:108-112` 的 `exactly one [標題] line`。對 `broadcast`，D16 只驗標題字數；行數驗收明確排除。

**先寫的失敗測試**

- `test_editor_title_template_only_allows_minimal_to_use_one_line`：在 prompt 中確認 `minimal` 文字允許 single line；其他三檔仍要求 two lines。
- `test_each_density_declares_the_visible_headline_cap`：對四段逐一 assert `10`／`13`／`18`／`22`、`whitespace`、`<`、`>` 都存在，避免只改樣板、忘記真正的 simplified 根因。
- `test_broadcast_single_headline_rule_is_unchanged`：比對 `_BROADCAST_STAMP_ON`／`_OFF` 包含原本的 `exactly one [標題] line`。

**驗證命令**

```bash
PYTHONIOENCODING=utf-8 .venv/Scripts/python.exe -X utf8 -m unittest tests.test_digest_density_ladder tests.test_broadcast_density tests.test_stage2_density_seed_no_text
```

預期：新測試先失敗，最小改動後全綠；此時 `test_reporter_prompt_frozen` 必須暫時失敗，直到 2-3。

---

## 2-2｜D2 全等級禁螢光綠（與 D16 同批重凍）

**修改位置**

- `main.py:1315` 的 `DIRECTIONAL_COLOR_RULES` 附近：新增 CG 消化階段全等級、非創意條件的 chroma-key 綠規則。
- `main.py:1522-1575`：規則須在 density／`cg_creativity_rules(0)` 也必經的位置；不得只塞進 `cg_creativity_rules()`。
- `editor_formats.py:856-900`：`COVER_NO_GREEN_RULE`、`COVER_NO_GREEN_ROW` 與 `cover_title_colour_rule()`。
- `editor_formats.py:725-734`：把 L4 `never green` 的過寬文字改成 chroma-key green 專指。
- `creativity.py:118-136`：`COVER_BANNED_COLOUR_WORDS` 的命名、註解與測試改為只排除 chroma-key／neon-key 綠描述；現有 palette 未含任何綠，無須為了「允許深綠」強行加綠 palette。
- `tests/test_cover_title_creativity.py:694-717`、新增 `tests/test_stage2_density_seed_no_text.py`。

**實作規格**

1. CG prompt 用一段全域、全 density、全創意級都會注入的條文，範圍限定：文字填色、文字描邊／陰影、文字底板、tag／chip、純裝飾圖形。禁止的字詞要精準指向 `chroma-key green`、`neon/lime key green`，不得再出現「所有 green-family colour」或把 teal／mint／olive 一概列禁。
2. 同段明確保留 `DIRECTIONAL_COLOR_RULES` 的內容語意：股價／數據「下跌、損失、負向＝綠」可保留，且深綠、墨綠、橄欖綠不因 D2 被替換。這是防止兩條 prompt 指令互相衝突的必要句，不是額外設計。
3. 封面線全部改同一窄定義：
   - `COVER_NO_GREEN_RULE` 與 brief 行用「禁 chroma-key green」取代「禁 green-family」。
   - `COVER_BANNED_COLOUR_WORDS` 不再把 `teal`／`mint`／`olive`／`emerald` 等廣義綠列為禁詞；若保留此常數，改名為能如實反映窄範圍的名稱，並同步全部引用與測試。
   - `COVER_PALETTES` 維持目前不含綠即可；本項不是「為了證明允許」而加入會破壞既有設計的顏色。
4. `cg_creativity_rules(0)` 仍回空字串，確保 0 級「不追加創意條文」不變；D2 由它外面的共通 safety rule 達成。

**先寫的失敗測試**

- `test_cg_chroma_key_rule_exists_at_creativity_zero_for_all_densities`：四個非 verbatim density × `visual_creativity=0` 都含窄禁令。
- `test_cg_chroma_key_rule_keeps_directional_market_green`：同份 prompt 同時含下跌＝green 的內容規則與「non-chroma data green allowed」的例外，避免日後改回全面禁綠。
- `test_cover_rule_no_longer_bans_olive_teal_or_mint`：封面規則沒有 `green-family`、沒有舊的列舉禁詞，且有 chroma-key green。
- `test_existing_cover_palettes_are_not_mutated_only_to_prove_permission`：目前 palette tuple 原樣存在；這是刻意避免無關視覺變更。

**驗證命令**

```bash
PYTHONIOENCODING=utf-8 .venv/Scripts/python.exe -X utf8 -m unittest tests.test_cover_title_creativity tests.test_cg_creativity tests.test_stage2_density_seed_no_text
```

---

## 2-3｜一次重凍、凍結 docstring 與新增編輯快照

**修改位置**

- `tests/test_reporter_prompt_frozen.py:1-67`。
- 更新既有 4 份：
  - `tests/fixtures/reporter-digest-standard-fullbleed.txt`
  - `tests/fixtures/reporter-digest-standard-safearea.txt`
  - `tests/fixtures/reporter-digest-simplified-fullbleed.txt`
  - `tests/fixtures/reporter-digest-simplified-safearea.txt`
- **新增 4 份編輯 fixture**：
  - `tests/fixtures/editor-digest-standard-fullbleed.txt`
  - `tests/fixtures/editor-digest-standard-safearea.txt`
  - `tests/fixtures/editor-digest-simplified-fullbleed.txt`
  - `tests/fixtures/editor-digest-simplified-safearea.txt`

**實作規格**

1. `ReporterDigestFrozenTests` 改為 role × density × full_bleed 三層迴圈，角色資料表：`記者 → reporter`、`編輯 → editor`。image prompt snapshot 類不屬本波，保持原本兩份 reporter image fixture，不混進本次重凍。
2. docstring 的「快照更新記錄」追加一筆，格式比照 2026-09-09／09-10：
   - 明寫 2026-09-14 CP1 使用者放行；
   - 明寫 D16 以 10／13／18／22 接上標題字數，D2 收窄為螢光綠；
   - 明寫這是有意改記者 prompt，不是誤傷；
   - 明寫同次補了編輯 standard／simplified × fullbleed／safearea 四份快照。
3. 用當下實際 `build_digest_instructions(role, density, "資料圖表", full_bleed=...)` 寫入 8 份 digest fixture；不要用手改字串。產檔小工具僅本機一次性使用，不提交。
4. 最後讀出 8 個 fixture，確認沒有 TODO／placeholder、且只有本批 D16／D2／角色樣板差異；這是作者的靜態檢查，生圖行為由測試 session 複核。

**驗證命令**

```bash
PYTHONIOENCODING=utf-8 .venv/Scripts/python.exe -X utf8 -m unittest tests.test_reporter_prompt_frozen
PYTHONIOENCODING=utf-8 .venv/Scripts/python.exe -X utf8 -m unittest discover -s tests
```

預期：前者與全套皆綠。此處是 CP1 的唯一重凍點；後續 2-4 至 2-9 若令該測試紅，視為範圍外回歸，不更新 fixture。

---

## 2-4｜F0：三條線 seed 欄位

**修改位置**

- `main.py:501-539` `GenerateRequest`、`main.py:3180-3214` `NewsImageGenerateRequest`、`main.py:3980-4044` `TenCoverRequest`、`main.py:5355-5410` `YtCoverRequest`。
- `main.py:2059` `generate()`、`main.py:3809` `generate_news_image()`，以及十點／YT endpoint 中所有目前自行組字串 seed 的位置（現見 YT `main.py:5761`）。
- `app.js:330-401` state、`app.js:2149-2171` digest payload、`tenCoverFields()`（`app.js:2222-2239`）及 YT payload 組裝處。
- `tests/test_cg_creativity.py`、`tests/test_cover_title_creativity.py`、`tests/test_yt_hourly_creativity_20260911.py`、新增 `tests/test_stage2_density_seed_no_text.py`。

**實作規格**

1. 三條線都收一個明確整數 `seed`：CG（`GenerateRequest`／一次到位 `NewsImageGenerateRequest`）、十點、YT。定義一個共用的 `next_generation_seed()` 或等價單一入口：未帶 seed 時建立不可預測 seed；同一個請求內不要各階段重抽。
2. 前端將每條線的目前 seed 分開保存；首次生成設 seed，按「重新生成」才遞增，普通重貼／只改文字不得遞增。每個 API payload 均送出目前 seed。
3. 後端把 request seed 原樣傳入所有既有 `cover_design_brief`／`yt_design_brief`、CG 新池的選擇；禁止 YT 繼續以 `f"{req.title}|{date_text}"` 充當唯一 seed，否則同標題重生仍同貌。
4. 成品「籤」的實體視覺位置與資料欄位要沿用現有可後貼／可歸檔的機制；若現有 compose 沒有可加的 seed metadata slot，先回報監督，不可悄悄將 seed 畫成模型文字。API response 至少回傳實際 seed，供前端、稽核和測試交叉驗證。

**先寫的失敗測試**

- 同 seed 的 CG／十點／YT 呼叫得到逐字相同的 design brief；不同 seed 至少讓各自的變化池輸出不同。
- `NewsImageGenerateRequest` 轉成 `GenerateRequest` 時保留 seed（現有 `generate_news_image` forwarding 是易漏點）。
- 前端原始碼釘三個 payload 都包含 seed，且重新生成路徑使用遞增而不是改標題／日期偷湊 seed。

**驗證命令**

```bash
PYTHONIOENCODING=utf-8 .venv/Scripts/python.exe -X utf8 -m unittest tests.test_cg_creativity tests.test_cover_title_creativity tests.test_yt_hourly_creativity_20260911 tests.test_stage2_density_seed_no_text
```

---

## 2-5｜A1 前置重構：配件件數與選取去耦合

**修改位置**

- `editor_formats.py:560-673`：目前 `COVER_ACCESSORY_COUNTS`、旗標判斷、`cover_accessories()`。
- `creativity.py:149-258`：既有 pool、`Draw`、`draw()`。
- `tests/test_creativity_module_20260911.py:44-180`、`tests/test_creativity_p3_rng_pins_20260911.py`、`tests/test_cover_flag_accessory_20260911.py`。

**實作規格**

1. 將「跨拉桿可共用」的 `COVER_ACCESSORY_COUNTS` 與 seed/RNG 驅動的選取函式移到 `creativity.py`，新函式只接受資料需求，例如 `level`、`rng/seed`、`visuals`、`count_policy`、`placement_note` callback；不得接受 `titles`、`full_width` 這種十點版型資訊。
2. `editor_formats.cover_accessories()` 改成十點 adapter：保留現有參數／公開名稱及原始幾何文字，轉呼叫共用函式，再由自身提供 `_accessory_geometry_note(full_width)`。這樣既有 cover／YT RNG fixture 在本步仍逐字相同。
3. 不改既有封面件數 `{0:0, 1:0, 2:1, 3:2, 4:3}`；CG 的獨立 3／4 policy 到 2-6 才接，不藉重構偷改封面行為。

**先寫的失敗測試**

- 靜態測試：共用件數與核心選取只在 `creativity.py` 定義，`editor_formats` 保留 adapter 而非副本。
- 回歸：既有 52 筆 `cover_design_brief_rng_pins_20260911.json` 全數不變。
- 旗標有／無、雙切／滿版的既有選取結果不變。

---

## 2-6｜A1＋A5＋A2：CG 接既有變化池與程式決定配件

**修改位置**

- `main.py:1100-1155`：`_CG_L1` 至 `_CG_CREATIVITY_BLOCKS`、`cg_creativity_rules()`。
- `creativity.py`：使用 2-5 的共用 draw／配件 API；如需 CG 專用語句，新增純資料／純 formatter，不能把 CG template 塞入十點 adapter。
- `tests/test_cg_creativity.py`、新增 `tests/test_stage2_density_seed_no_text.py`。

**實作規格**

1. `cg_creativity_rules(level, *, seed=None, visuals=...)` 在 level 0 仍回空字串；level 1–4 以同一 seed 的 `creativity.draw()` 選 pool：plate shape、stagger、typeface、palette、anchor、tilt direction。輸出為明確命令句，不寫「可自行選」。
2. CG 只接合適的資訊圖層級描述，不照搬封面專屬的標題高度、雙切 seam、日期牌或 `full_width` 幾何。
3. A2 依使用者字面，CG policy 為 `{0:0, 1:0, 2:0, 3:2, 4:3}` 或另一個由監督確認的明確表；每件由程式列出、無字、不可覆蓋內容。**注意：2-5 的封面表從 level 2 開始有 1 件，不可直接拿去當 CG policy。**
4. 先讓 CG 的 `GenerateRequest.seed` 穿過 `build_digest_instructions(..., seed=...)`，再由 endpoint 呼叫；不可只在 unit test 直接呼叫函式而漏掉真實資料流。

**先寫的失敗測試**

- CG level 3／4 prompt 有固定數量的程式挑選配件；level 0–2 不含 CG 配件清單。
- 同 seed 的 CG 規則相同，不同 seed 跨 12 個樣本至少出現兩種 pool 組合。
- 每個 level 的 FIXED block、點數／安全框／不新增文字不變，且 broadcast 的格式 block 仍在創意條文之前。

---

## 2-7｜D3：僅 `MAXIMUM_DENSITY_RULES` 授權「拆原有內容」

**修改位置**

- `main.py:1046-1054`。
- `tests/test_digest_density_ladder.py:63-86`、新增 `tests/test_stage2_density_seed_no_text.py`。

**實作規格**

1. 僅在 maximum block 的第 3 條後新增「可把同一來源已明示的複合事實拆為兩條／兩個 point」；必須同時列出不能做的事：不可新增未提的原因、人物、時間、數字、地點、因果或背景。
2. `STANDARD_DENSITY_RULES` 不加此授權；避免字多與字超多再次塌成同一檔。
3. 不改創意 FIXED 的「不新增文字」：D3 是內容切分，非創意拉桿例外。

**先寫的失敗測試**

- maximum 有 `split`、`already present in the source`、`no new fact` 等必要語意。
- standard／simplified／minimal 無相同拆分許可。

---

## 2-8｜F1：拉開 density 級距

**修改位置**

- `main.py:989-1054`。
- `tests/test_digest_density_ladder.py`、`tests/test_broadcast_density.py`。

**實作規格**

1. 以已完成的 0-2 校準為準：minimal 維持一點／約 12 字，simplified 維持 1–3 點，standard 維持至多 6 點，maximum 維持至多 8 點與較長行／總長度；本步只補足因 D3 的 maximum 可拆分差異與 D16 已新增的標題差異，不重開數字裁決。
2. 明確檢查 broadcast 的卡數仍是版面實體限制：standard／maximum 4 張，其他 3 張；F1 不改 `editor_formats._broadcast_point_count()` 的既定差異。
3. 實作 session 只跑 mock／文字測試。正式站實拍由測試 session 以同一稿六檔（含 2-9 無字）量標題與內文，並將 broadcast 4 格行數排除但保留字數採樣。

**先寫的失敗測試**

- density order 與前端 labels／slider 同步，字數上限單調 `10 < 13 < 18 < 22`。
- maximum 的可拆分條文不會出現在 standard。
- broadcast row count 既有測試不回歸。

---

## 2-9｜D14＋F20：無字檔（拉桿最左端）

**修改位置**

- `main.py:484-489`：`DigestDensity` 與 `DIGEST_DENSITY_ORDER`。
- `main.py:989-1179`：新增 `NO_TEXT_DENSITY_RULES`，不汙染既有 density blocks。
- `main.py:1495-1598`：新增 `density == "no_text"` 分支；決定是否仍呼叫 digest 必須遵守下列實作邊界。
- `app.js:653`、`app.js:1144-1150`、`app.js:1223-1236`：labels、order、toast。
- `index.html:698-703`：range `max=5`、最左端文案與 title。
- `tests/test_digest_density_ladder.py`、新增 `tests/test_stage2_density_seed_no_text.py`。

**實作規格**

1. enum／順序為 `("no_text", "verbatim", "minimal", "simplified", "standard", "maximum")`；預設仍 `simplified`，不改。
2. 無字 block 要明確覆蓋所有文字產物：不輸出 `[標題]`、`[內文小標]`、`<蓋章>`、`<底帶>`、任何數字、日期、來源、地名、圖例、標籤、logo、caption；`style`／`structure` 只可描述無字視覺與既有安全框／附圖／內容忠實度限制。
3. `stamp=True`、broadcast、directional colour、safe frame 等與無字衝突的區塊，必須在 no-text 的最終規則中明確被關閉或改成無文字視覺；不得把「有蓋章」與「完全無字」兩條矛盾 prompt 一起送出。
4. **先不跳過 digest API。** F20 的 MASTER 曾提出「無字可跳過整個 digest」作為 token 效益，但現有生圖仍需 `style`／`structure`／圖表類型／地圖與肖像結果；直接跳過會讓架構改成另一件大工作，也不在 D14 的已裁形式內。先以 no-text digest 產生無文字的 style／structure，速度最佳化另立工作。
5. `/api/news-image/generate` 也必須接受並完整轉送 `no_text`；否則網頁可用、LINE／整合端悄悄失效。

**先寫的失敗測試**

- Pydantic 接受 `no_text`，兩個 request model 都成立；未知值仍拒絕。
- 前端順序、六個 label、range max=5、預設 simplified 完整同步。
- no-text prompt 包含無標題／無 stamp／無數字的 final override，且不帶 `VERBATIM MODE`。
- `news_image_generate` forwarding 在 mock 下帶著 `density="no_text"`。

**驗證命令**

```bash
PYTHONIOENCODING=utf-8 .venv/Scripts/python.exe -X utf8 -m unittest tests.test_digest_density_ladder tests.test_density_and_stamp tests.test_broadcast_density tests.test_stage2_density_seed_no_text
PYTHONIOENCODING=utf-8 .venv/Scripts/python.exe -X utf8 -m unittest discover -s tests
```

---

## 提交與驗收切點

1. 2-1／2-2 完成後**不可提交重凍前的紅色 fixture 狀態**；接著直做 2-3，先讓 CP1 回綠。
2. 2-3 是第一個 reviewer gate：由非作者 review D16／D2 diff、8 份 digest fixture、frozen docstring；不得由作者自行核准。
3. 2-4 至 2-9 每一項可獨立 commit，commit 只留 exp 分支；提交訊息須加：`Co-Authored-By: Claude Code <noreply@anthropic.com>`。
4. 全套 green 後，建立給「測試」session 的生圖驗收交辦：D16 六檔、D2 綠幕情境／下跌綠資料、同 seed／遞增 seed、CG L3／L4 配件、maximum 拆分忠實度、無字。實作 session 不自行宣告視覺驗收完成。

## 需監督確認／計畫不得自行替代裁決的項目

1. **A2 CG 的精確件數表**：使用者原話是「創意 3／4 加掛配件」，本計畫預設 `{0:0,1:0,2:0,3:2,4:3}`，但數量 `2/3` 來自封面既有梯度、不是文字裁決本身。動 2-6 前由監督確認；若不同，僅替換 CG policy，不動封面表。
2. **F0 成品籤的具體表現**：D1 已裁「seed 印在成品籤上」，但目前查到的三條線沒有共用的既有 seed metadata／後貼籤 API。必須先定位 compose／response／稽核現行落點；若會讓 seed 變成模型畫字，停下回報，不能把 seed 拼進 prompt。
3. **F20 不是可安全併進「跳過 digest」的小改**：本計畫採最小可用解（仍產生 style／structure、只禁止 variable 的所有文字）。若監督要同時省掉 API，需另立設計與測試，不能在 2-9 順手加。
4. **D16 數字的正式站驗證仍未完成**：10／13／18／22 已裁定、可實作，但 0-2 量測用的是 `gpt-5.5`，正式站是 `claude-sonnet-5`；測試 session 的實拍若明顯不合格，回報監督重新裁數字，不在實作中自行微調。
