# 交辦單：Stage 1 ＋ Stage 2 合併實拍輪（2026-09-15）

**交辦對象**：「測試」session
**開單人**：監督（AICG session）
**預算**：**24 張 × $0.04 ＝ $0.96**，使用者 2026-09-15 核定
**用哪把 key**：**OpenAI key**（使用者 2026-09-15 指定）

---

## 零、動工前必讀的兩個限制

### 1. 這輪證不了正式站的消化模型

`DIGEST_BACKEND` 與 `IMAGE_BACKEND` 是**兩個獨立開關**（`main.py:80`／`main.py:2823`）。
走 OpenAI key ＝ 消化端跑 `gpt-5.5`，而 **D16 的級距數字（10／13／18／22）本來就是
在 `gpt-5.5` 上量出來的**（見 `docs/0-2-標題字數量測結果-20260914.md`）。

所以這一輪：

| 查核點 | 這輪能證明什麼 |
|---|---|
| B26+D9／B27／D4／B1／B28／A2 | **完全有效**——影像端與程式端，與消化模型無關 |
| D16／D3／D2 的資料圖那半 | **只證明「在 gpt-5.5 上照裁決生效」**，正式站 `claude-sonnet-5` 仍待另一輪 |

**回報時請把這兩類分開寫**，不要讓讀的人以為正式站已經驗過了。

### 2. 不合格不要自己微調

D16 若量出來不合格（標題字數沒有照 10／13／18／22 分開），
**回報監督重裁數字**。測試端與實作端都不得自行微調級距——
那些數字是使用者裁決過的（CP4／D16），改它要走裁決流程。

---

## 一、受測的東西在哪

- **分支**：`exp/redesign-20260903`（程式碼只在這條，`main` 只有文件）
- **HEAD**：`f2109b6`
- **本機測試現況**：1755 綠，凍結快照 8 份未動

Stage 1（生圖端，commit `bbe0439`）與 Stage 2（消化端，commit `e4fdec0`～`75da80f`）
**彼此獨立**，一張圖出問題時歸因不會混淆——這正是使用者裁決合併成一輪的理由。

---

## 二、24 張怎麼分

| # | 查核點 | 張數 | 要看什麼 | 判定「過」的條件 |
|---|---|---:|---|---|
| 1 | **D16 級距** | **6** | 六個消化檔位（無字／不改字／字極少／字少／字多／字超多）各 1 張，**同一篇新聞** | 標題去空白字元數落在 0／原文／≤10／≤13／≤18／≤22。**計數要去掉空白與 `<` `>` 標記**，不要數 CJK 個數 |
| 2 | B26+D9 多張原圖放置 | 2 | 帶 3 張附圖生成 | 三張**都**出現在畫面上，不是只放一張 |
| 3 | B27 YT 封面附圖位 | 2 | YT 封面指定附圖 | 底圖確實來自附圖，不是 AI 另畫 |
| 4 | D4 左右措辭 | 2 | 雙格版型、兩格都給圖 | LEFT／RIGHT 各自吃到正確那張 |
| 5 | B1 重複日期 | 2 | 成品回餵再生一次 | 畫面上日期**只出現一次** |
| 6 | B28 具名真人 | 2 | 指定一位具名真人 | 「AI示意圖」標籤仍在，沒被洗掉 |
| 7 | D2 禁綠 | 2 | 一張綠幕情境、一張「下跌＝綠」資料圖 | 禁掉的**只有螢光綠／chroma key 綠**；深綠、墨綠、橄欖綠與漲跌的綠都要**留著** |
| 8 | F0 seed 可重現 | 2 | 同一顆 seed 跑兩次 | 兩張長相一致；另確認 seed **沒有印在圖上**（只存後台） |
| 9 | A2 配件件數 | 2 | 創意 3 級、4 級各 1 張 | 看得到的配件分別是 **2 件**與 **3 件** |
| 10 | D3 maximum 拆分 | 2 | 字超多檔、一篇事實密集的稿 | 拆出來的每一點**都在原文裡**——沒有新增原文沒有的原因、人物、時間、數字、地點或後果 |

合計 **24 張**。

第 1 項（D16）是這輪最關鍵、也最可能要回頭重裁的一項，所以獨佔 6 張。
其餘九項各 2 張，足夠分辨「有沒有生效」，不足以做統計——**這是刻意的**，
使用者選的是精簡輪，目的是抓有無生效，不是量分布。

---

## 三、回報格式

1. **實際花費**（張數 × 單價，與預算 $0.96 的落差）
2. **逐項判定**：過／不過／無法判定，各附一張代表圖的路徑
3. **不過的項目**：寫清楚看到什麼、預期看到什麼，**不要自己解釋成因**
4. **D16 的原始數字**：六檔各自的標題去空白字元數，原始值照列
5. 明確標出哪些結論**只對 `gpt-5.5` 成立**（見第零節）

檔案落點照慣例：`D:\Downloads\AICG\後台紀錄\20260915\`。

---

## 四、不要做的事

- 不要改任何程式碼。看到疑似 bug **回報**，不要順手修。
- 不要更新 `tests/fixtures/` 的任何凍結快照。它們本波只被授權重凍過一次，那次已用掉。
- 不要改 `MASTER-列管清單.md` 的 `D-` 項。
- 超出 24 張要先問，不要自己加跑。
