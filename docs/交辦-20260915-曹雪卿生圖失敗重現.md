# 交辦：曹雪卿「生圖很容易失敗」前台重現（2026-09-15，插隊最優先）

**執行**：「測試」session ／ **驗收**：AICG 監督 session
**優先序**：**插隊**。原本的 18 張階梯實拍繼續暫停，等這件結案再說。

---

## 一、監督已查到的事（不用重查）

正式站後台 200 筆全撈過，結論：

1. **後台完全沒有失敗紀錄。** 表頭只有 `時間｜使用者｜類型｜成圖｜內容`，
   167 列全部都有成圖；全檔搜不到 `失敗／錯誤／error／timeout` 任何字樣。
   **代表後台只記成功，失敗不留痕。** 所以「很容易失敗」無法從後台統計，
   只能在前台實測。（這件本身要獨立開缺陷，監督處理，你不用管。）

2. **曹雪卿今天（09-15）成功的 9 筆**：
   `09:50:47 / 09:55:56 / 09:59:58 / 10:14:12 / 10:23:52 / 10:26:30 /
   10:40:13 / 10:50:32 / 11:34:17`
   類型：前 8 筆都是 **資料圖表**，最後 1 筆 `11:34:17` 是 **自動判斷**。
   稿長：771 / 771 / 1071 / 1464 / 1464 / 1464 / 1444 / 1444 / 104 字。

3. **11:34 之後到現在（12:00）再無任何成功紀錄。**
   若她這段時間仍在試，那些嘗試就是失敗的那批——但沒有證據，只能重現。

4. 已幫你把四筆的完整後台內容抓成檔（含最終 prompt 與改圖需求）：
   `D:\Downloads\AICG\後台紀錄\20260915\曹雪卿重現素材\`
   - `r_2026-09-15_113417.txt` — 104 字 iPhone 盜刷，**自動判斷**，新生成
   - `r_2026-09-15_105032.txt` — 1444 字 超級央行週，**資料圖表**，**改圖**
   - `r_2026-09-15_102630.txt` — 1464 字 川普 AI 騙局，**資料圖表**，**改圖**
   - `r_2026-09-15_095958.txt` — 1071 字 太空武器，**資料圖表**，新生成

   每個檔開頭就是「新聞原文（N 字）」，整段原文可直接複製貼進前台輸入框。

---

## 二、你要做的事

**站台：正式站 `https://aicg.tvbs.ai`**（她用的就是這台；測試站的行為不算數）。

### 上限（硬規定）
- **最多 12 次生成嘗試**。成功一張 = 公司 key $0.04，最壞情況 $0.48。
- 跑到 12 次就停，不管結論有沒有收斂。
- **不准為了「多試幾次看看」而超額**，超額要先回報監督。

### 步驟

**A. 先重現「失敗」本身（最多 6 次）**

依序跑，每次都完整記錄（見下方記錄格式）：

| # | 素材 | 類型 | 動作 |
|---|---|---|---|
| 1 | `r_..._113417.txt` 的原文（104 字） | 自動判斷 | 新生成 |
| 2 | `r_..._095958.txt` 的原文（1071 字） | 資料圖表 | 新生成 |
| 3 | `r_..._102630.txt` 的原文（1464 字） | 資料圖表 | 新生成 |
| 4 | 同 #3 成功後的圖 | — | 用該檔記錄的改圖需求原文再改一次 |
| 5 | `r_..._105032.txt` 的原文（1444 字） | 資料圖表 | 新生成 |
| 6 | 同 #5 成功後的圖 | — | 用該檔記錄的改圖需求原文再改一次 |

**只要任何一次失敗，立刻停手回報監督**，不要自己往下猜。
失敗當下**務必**抓齊：
- 前台畫面截圖（含錯誤訊息全文，別只寫「出錯了」）
- `read_console_messages` 全部紅字
- `read_network_requests` 裡那一發請求的 **HTTP status、回應 body 全文、耗時秒數**

**B. 若 A 六次全過（= 當下重現不出來，最多再 6 次）**

那就要往「什麼情況才會失敗」壓。優先試這四個方向，一個方向一次：

7. **更長的稿**：把 #3 的 1464 字原文貼兩份（約 2900 字）→ 資料圖表
8. **敏感題材**：#1 的 iPhone 盜刷稿（涉及犯罪／金融詐騙）→ 資料圖表
9. **連續快打**：#2 的稿，送出後不等結果就在另一分頁再送一次
10. **改圖連環**：對 #3 的圖連續改三次（每次都用短需求，例如「把第二格補一行」）

11、12 保留給「把某個失敗再重現一次以確認不是偶發」。

---

## 三、記錄格式（每一次嘗試都要）

```
#N ｜ 時間 HH:MM:SS ｜ 素材（字數） ｜ 類型 ｜ 新生成/改圖
結果：成功 / 失敗
耗時：__ 秒（按下送出到畫面有結果）
成功 → 後台檔名（去 /admin 對最新一列拿 png 檔名）
失敗 → 前台錯誤訊息全文：
        HTTP status：
        回應 body：
        console 紅字：
```

全部寫進 `D:\Downloads\AICG\後台紀錄\20260915\曹雪卿失敗重現.md`，
並把失敗的截圖存同一個資料夾。

---

## 四、三條紅線

1. **不要改任何程式碼。** 這輪純觀測。
2. **不要改前台設定去「繞過」失敗**（例如換版型、縮短稿子就算過）。
   目的是抓失敗條件，不是求成功。
3. **不要用測試站代替正式站**。她遇到的是正式站。

做完（或中途遇到第一次失敗）回報監督，我來判是缺陷還是環境。
