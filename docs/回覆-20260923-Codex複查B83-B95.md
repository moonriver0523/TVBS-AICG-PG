`git log` 顯示此範圍實際包含 10 筆 commit。審查結果如下。

## Spec／行為發現

### 1

- 嚴重度：🟠特定條件下錯
- 檔案:行號：[app.js:1304](/E:/GitHub/TVBS-AICG-PG/app.js:1304)、[app.js:2965](/E:/GitHub/TVBS-AICG-PG/app.js:2965)、[main.py:5570](/E:/GitHub/TVBS-AICG-PG/main.py:5570)
- 具體失敗情境：以「編輯＋安全框 OFF＋字多」產生 2K 圖後，把份量切成「字少」再移動標籤。`refineSource` 沒被清除，restamp 卻送當下的 `state.density`，後端遂把原本不後製的 2K 圖重新套成 1748×924 對位框。角色、安全框、引擎或尺寸切換也有同型問題。
- 證據：`switchDigestDensity()` 只改 `state.density`；restamp payload 使用 `density: state.density`、`safe_frame: state.safeFrame`、`safe_frame_profile: state.currentRole`，而非原成品參數。後端再以這組值呼叫 `resolve_frame_plan(...)`。
- 信心：確定

### 2

- 嚴重度：🟠特定條件下錯
- 檔案:行號：[app.js:2944](/E:/GitHub/TVBS-AICG-PG/app.js:2944)、[app.js:2955](/E:/GitHub/TVBS-AICG-PG/app.js:2955)、[app.js:2984](/E:/GitHub/TVBS-AICG-PG/app.js:2984)
- 具體失敗情境：快速連按「左上」再按「右上」。兩個 restamp 請求併發；若舊的左上請求較晚完成，會覆蓋右上成品，但 toast 讀取最新 state，仍顯示「標籤已移到右上」。
- 證據：每次按鈕都直接呼叫 `restampDisclaimer()`，沒有 request id、取消或結果序號；回應一律執行 `state.refineDisplay = data`，訊息則讀 `state.disclaimerCorner`。
- 信心：確定

### 3

- 嚴重度：🟠特定條件下錯
- 檔案:行號：[name_aliases.py:80](/E:/GitHub/TVBS-AICG-PG/name_aliases.py:80)、[name_aliases.py:94](/E:/GitHub/TVBS-AICG-PG/name_aliases.py:94)、[name_aliases.py:141](/E:/GitHub/TVBS-AICG-PG/name_aliases.py:141)、[main.py:1997](/E:/GitHub/TVBS-AICG-PG/main.py:1997)
- 具體失敗情境：輸入「永川習俗」、「山川習性」或「冰川習性」。它們都跨詞包含「川習」，但前一字不在人工清單，會被判成川普＋習近平，進而查照片、設為具名真人，可能把兩人畫進完全無關的新聞。
- 證據：`BLOCKING_PREFIXES["川習"]` 只有 `四銀品香德市河旭湯`；判定僅檢查 `blob[start - 1] not in blocked`。命中後 prompt 將展開稱為 `established fact`，B91 又要求視為已逐字寫明並列入 `portrait_subjects`。實際探測確認上述三例都命中。
- 信心：確定

### 4

- 嚴重度：🟠特定條件下錯
- 檔案:行號：[compose.py:1085](/E:/GitHub/TVBS-AICG-PG/compose.py:1085)、[compose.py:1238](/E:/GitHub/TVBS-AICG-PG/compose.py:1238)、[compose.py:1242](/E:/GitHub/TVBS-AICG-PG/compose.py:1242)、[test_ten_cover_title_fit.py:65](/E:/GitHub/TVBS-AICG-PG/tests/test_ten_cover_title_fit.py:65)
- 具體失敗情境：過寬標題只有一個長拉丁字串時，例如 `SUPERCALIFRAGILISTIC`，所有內部斷點都被保護，最後卻無條件從中點切成 `SUPERCALIF／RAGILISTIC`。`AT&T` 也未被 regex 視為一個記號，可切成 `AT／&T`。
- 證據：合法斷點找不到時，最後仍執行 `return text[:mid], text[mid:]`；`_ATOMIC_TOKEN_RE` 亦不包含 `&`、`+`、`#`。測試只檢查切點兩側都是 ASCII alphanumeric，因此切在 `&` 前不會失敗。
- 信心：確定

### 5

- 嚴重度：🟠特定條件下錯
- 檔案:行號：[main.py:626](/E:/GitHub/TVBS-AICG-PG/main.py:626)、[main.py:3152](/E:/GitHub/TVBS-AICG-PG/main.py:3152)、[test_b94_upstream_error_detail.py:93](/E:/GitHub/TVBS-AICG-PG/tests/test_b94_upstream_error_detail.py:93)
- 具體失敗情境：OpenRouter 回 `403 Model not allowed for this key`。這是確定性的權限錯誤，重試不會成功，程式卻仍跑滿消化重試次數、讓使用者白等。
- 證據：`credits_exhausted_error()` 只攔 402；403 的 `PermissionDeniedError` 是 `APIError`、不是 `AuthenticationError`，因此進入通用分支、`sleep(1.5)` 後繼續。新測試甚至明文把 403 釘成「keep retrying」，正是把錯誤行為寫成規格的案例。
- 信心：確定

### 6

- 嚴重度：🟡風險或測試缺口
- 檔案:行號：[main.py:5513](/E:/GitHub/TVBS-AICG-PG/main.py:5513)、[main.py:5530](/E:/GitHub/TVBS-AICG-PG/main.py:5530)、[main.py:5581](/E:/GitHub/TVBS-AICG-PG/main.py:5581)、[compose.py:475](/E:/GitHub/TVBS-AICG-PG/compose.py:475)
- 具體失敗情境：直接送 `disclaimer_kind="source"` 且文字空白，請求模型會接受，直到 Pillow 階段才拋錯並回 500，而不是輸入錯誤的 4xx。`safe_frame_profile`、`density`、`aspect_ratio`、MIME 也多為任意字串，拼錯可能靜默套用 reporter/default 行為。
- 證據：`disclaimer_source_text` 預設空字串，沒有跨欄位 validator；`paste_disclaimer_note()` 才檢查空值，外層將其包成 HTTP 500。成功 restamp 確實會以 `web-restamp` 歸檔；但 `broadcast_hole` 的 400 在 `try` 之前拋出，不會留下失敗稽核。
- 信心：確定

### 7

- 嚴重度：🟡風險或測試缺口
- 檔案:行號：[main.py:588](/E:/GitHub/TVBS-AICG-PG/main.py:588)、[main.py:601](/E:/GitHub/TVBS-AICG-PG/main.py:601)、[audit_archive.py:41](/E:/GitHub/TVBS-AICG-PG/audit_archive.py:41)
- 具體失敗情境：若上游例外文字意外含 `Authorization: Bearer …`、Gemini `AIza…` 或其他非 `sk-/pk-` 敏感值，新 B94 detail 會原樣回到瀏覽器。
- 證據：`upstream_error_detail()` 只遮蔽 `sk-`／`pk-`。稽核寫盤前另有較完整的 `sanitize_error_summary()`，所以常見 Bearer/API key/AIza 不會寫入 audit；缺口是在回前端之前沒有共用同一支 sanitizer。
- 信心：可能

## Standards

沒有發現硬性程式規範違反。判斷性的主要 smell 是 restamp 將 provider、尺寸、density、框設定及標籤三元組反覆以散落原始值搬運，直接導致發現 1；生圖與 refine 的標籤閘門亦重複，增加未來新增路徑再漏接的風險。

## 查過且確認沒問題

四個 `resolve_frame_plan` 呼叫端都已傳入 `density`；`HIGH_RES_OUTPUT_CANVAS` 是單一 `(2560, 1440)`，正常參數下記者 21:9 生成會置入 16:9 畫布，不會直接交付 3360×1440。生圖、refine、restamp、封面重組及 LINE `generate_news_image` 的正常標籤路徑未發現消失或雙貼；封面 refine 使用乾淨的 `source_image_base64` 重組。B90 一般 CG 來源文字逐字照貼，其他版型仍保留前綴正規化。B94 稽核寫盤有二次去敏；B95 的 402 本身只停手一次，未誤吃其他狀態碼。restamp 成功結果有歸檔並分類為「合成」。B85 沒附圖時不注入新增規則，prompt 路徑逐字不變。