# TODO

## 本檔的定位（2026-09-14 起）

**列管與狀態一律看 [`MASTER-列管清單.md`](MASTER-列管清單.md)**——那是唯一的列管帳本，
也是 session／agent 之間的交接媒介。本檔從此只負責**完整敘事**：查證數據、根因分析、
取捨理由，以及已完成項目的歷史。

兩份檔案的分工：

- `MASTER-列管清單.md`＝索引、狀態、優先序、依賴、裁決旗標。開工先讀它。
- `TODO.md`（本檔）＝每一項的完整章節。**要動工某一項時才展開對應章節**。

**不要在本檔另外維護一張狀態表**（2026-09-14 早上曾放過一張「列管總表」，同日改建成
MASTER 後移除）——兩張表必然失聯，變成第二個真相源。

敘事**只加不改**：寫錯就再加一行更正，保留判斷軌跡。

---

## 待處理：新增「完全不要文字」檔位，併進字多／字少拉桿（2026-09-14 使用者列入）

使用者看到戴元利的紀錄後列入：**「完全不要文字」這可以融入現有的字多字少 BAR，
當成一種固定格式。**

### 需求來源：戴元利 2026-09-14 17:23–17:27，三筆

| 時間 | 新聞原文 | 成品 |
|---|---|---|
| 17:23:12 | 一名婦人被囚禁在大型的冷凍櫃中死亡 | 正常：標題＋副標＋背影人物＋右下「示意圖」小籤 |
| 17:26:30 | 一名婦人被囚禁在大型的冷凍櫃中死亡 **我不要字 我只要圖片** | **純畫面，零文字**，只剩右下「示意圖」小籤 |
| 17:27:35 | 一名婦人被誘騙進大型冷凍庫後被凍死，**我不要字，我只要圖片** | 同上 |

**兩件事同時被證明**：

1. **功能上做得到**——行內指令「我不要字 我只要圖片」確實讓成品完全沒有文字，
   而且「示意圖」小籤仍然保留（這是對的，示意圖必須標示）。
2. **這又是一個把指令寫進原文的實例**（見 D12／F19）。他連打兩次、第二次還加了逗號，
   看得出他不確定系統會不會照做——**因為介面上沒有這個開關，他只能用講的。**

### 設計要點（待裁的細節）

**W1｜它跟 density 不同層，不能只是加一格就算。**
現有五檔（不改字／字極少／字少／字多／字超多）控制的是**消化**——`variable` 裡放多少字。
「完全不要文字」要的是**成品畫面上不要出現文字**。字極少那一檔仍然會產出 1 點文字。
所以無字檔要同時做到：`variable` 空（或只留給程式用的標記）、`structure` 只描述畫面。
**要確認**：拉桿最左端多一格（六段），還是另設一個獨立開關？使用者說「融入現有 BAR
當成一種固定格式」，所以預設走六段拉桿，但實作時要確認「不改字」那一端的語意
不會跟「不要字」打架——一個是「原文一字不改地放上去」，一個是「一個字都不要放」，
兩個極端擠在同一條拉桿的同一側會不會讓使用者選錯。

**W2｜標題也不要嗎？** 17:26／17:27 兩張連標題都沒有。要確認這是使用者要的
（推測是——他要的是純畫面素材，文字另外上字卡），還是應該保留標題。

**W3｜「示意圖」小籤保留。** 目前行為是保留，這是對的，不要一起拿掉。
要在規則裡明講「無字模式的唯一例外是示意圖標示」，否則下次有人把它當成文字清掉。

**W4｜連帶失效的區塊要盤點。** 無字模式下這些東西全都沒有意義或會打架：
蓋章、色調對比（針對文字可讀性那幾條）、安全框內的文字留白、標題斷句、
消化品質閘的截斷／簡體字檢查、創意階梯裡所有關於字級落差／描邊／反白的條文。
**要逐一決定是「不注入」還是「注入但改寫」**，不能放著讓它們互相矛盾——
repo 已經記過多次「留矛盾句，模型每次都挑最寬鬆的那句遵守」。

**W5｜能力矩陣要加一欄。** 哪些版型支援無字模式？封面線（十點／YT）本來就有
「標題由程式壓字」的路，無字模式在那裡是什麼意思要想清楚——是連程式壓的字
也不壓，還是只有 AI 不畫字。`FORMAT_CAPABILITIES` 加旗子。

**W6｜省下來的 token。** 無字模式不需要消化，理論上可以跳過整個 digest 呼叫、
直接生圖。這是順手可拿的效益，但會改動流程分支，**先確認 W1 的形式再談**。

### 驗收

每個版型各跑一次無字模式，人工確認：畫面零文字、「示意圖」小籤還在、
沒有殘留的空白文字區塊（版面要重新分配，不是把文字區留白）。

---

## 待處理：不宜播出的視覺內容要有避開清單（2026-09-14 使用者列入）

使用者看到「路死動物處理流程」那批後列入：**遺體、棺材、屍袋、排泄物、尿液等等
不宜播出的內容要避開。**

### 現況：完全沒有這條規則

grep 過 `main.py`／`news_prompt.py`／`editor_formats.py`，**「遺體／血腥／gore／corpse／
body bag／graphic content／不宜」一個命中都沒有**。目前唯一沾到邊的是
`REAL-WORLD ACCURACY`（main.py:1278，管的是「畫得對不對」）與
`CONTENT_FIDELITY_RULES`（管文字與數字），**兩者都不管「這東西該不該出現在畫面上」**。

也就是說：只要新聞文字提到了，模型就會照畫。09:56:08 那張就是這樣——
「集體火化」被畫成焚化爐**爐內有動物遺體**，直接可以播出。

### 這批要擋的東西（使用者列舉＋同類延伸，待確認範圍）

- **遺體類**：屍體、遺體、白骨、殘肢，人與動物都算（路死動物那批是動物）。
- **殯葬類**：棺材、屍袋、裹屍布、靈柩、冰櫃。
- **排泄／體液類**：排泄物、尿液、嘔吐物、血泊、痰。使用者明確點名「尿液」——
  注意這跟葉怡君下午那批「尿檢濃度」是同一個題材，**尿液檢體容器**畫出來就踩線
  （16:43 那張確實畫了兩個裝著黃色液體的檢體瓶）。
- **待使用者確認是否納入**：手術／傷口特寫、自殘、動物虐待、槍口對人、吸毒器具、
  遺照。這些在電視新聞有各自的處理慣例，不要自己替使用者決定。

### 規則要放哪一層（設計題）

三個位置，效果與風險都不同：

1. **消化階段**（`build_digest_instructions`）：在 `structure` 就不要求這些畫面。
   優點是從源頭擋掉；缺點是消化模型不見得知道自己要求的東西會長成什麼樣
   （「集體火化」本身沒有任何不當字眼）。
2. **生圖 prompt 層**（`news_prompt`／各封面模板）：加一段負面清單。最直接，
   但負面清單有反效果風險——repo 記過「排除條文埋在一長串否定句中間，模型會照樣犯」
   （commit `73ae198`），所以**不能只是多加幾個 no**，要給正面替代做法。
3. **正面替代法（建議一起做）**：不是只說「不要畫遺體」，而是說「這一格要表達
   『集體火化』時，畫**空的焚化爐外觀**／**設施外觀**／**象徵性的火焰**」。
   B13（refine 移除元素時用紙箱補位）證明了只講「拿掉」會讓語意掉——
   **必須同時給替代物**，否則就是製造 B13。

### 與既有項目的關係

- **B13**（refine 用不相關的東西補位）是同一件事的下游：她之所以要 refine，
  就是因為第一次生成畫了遺體。**這條規則做對了，B13 那次 refine 根本不會發生。**
- **R2 地圖驗收**、`REAL-WORLD ACCURACY` 是「畫得對不對」，這條是「該不該畫」，
  兩者不重疊，應各自獨立成段。

### 驗收

拿一組已知會踩線的題材（路死動物、尿檢、命案、喪禮、醫療）各跑 N 次，
人工看圖判定有無踩線；改之前先跑一輪當基準。**這題不能用自動檢查驗收**——
畫面適播與否是人的判斷。

---

## 待處理：消化階段的主被動與順序方向，以及使用者「→」的判定（2026-09-14 使用者列入）

使用者看到卡拉OK那批後列入：**這個案例也是語意消化時主被動、順序問題；
另外使用者的「→」判定也要規則化。**

### 案例

葉怡君的原文：

> 嗨唱卡拉OK變調! 黃男(ICON) 攻擊→ 范男(ICON) 林女(ICON) 回擊(范男→黃男) 3人擦挫傷(蓋字章)

11:47:30 那張把左上角標成 **「黃男遭攻擊」**——原文是黃男**發動**攻擊，成品變成黃男
**受害**。施動者與受動者整個對調。同一張的箭頭方向反而畫對了（黃男→范男 攻擊、
范男→黃男 回擊），所以**是文字標籤寫反，不是圖畫反**——錯在消化階段的 `variable`。

### 現況：`CONTENT_FIDELITY_RULES` 沒有這一條

現有九條管的是：不得杜撰事實數字（1–4）、不得把推測寫成肯定（5）、版型名不是新聞（6）、
**地點與事實的配對不得拆散或亂配（7）**、不同事件不得串成因果（8）、僅在原文明示時才可補充（9）。

第 7 條最接近——它說「配對本身就是一種主張」——但管的是**地點↔事實**的配對，
**沒有一條管「誰對誰做了什麼」的方向**。壓縮標題時把 `A 攻擊 B` 寫成 `A 遭攻擊`
在現有規則下是完全合法的。

要補的是一條明確的：**施動者／受動者不可對調，動作方向不可反轉**。這在壓縮時特別容易
出事，理由跟第 5 條（hedge 是最便宜的字）同構——「遭」「被」「受」都是一個字，
縮句時最容易順手加上或拿掉。

### 順序也是同一件事

「黃男攻擊 → 范男回擊」是**時序**：先攻後回。把兩件事在圖上並排而不標先後，
或把回擊畫在前面，等於改寫了事件經過。路死動物那批的「掃描→無晶片→火化」同樣
是時序，那一批的箭頭順序倒是都對。**要一併規則化：原文給了先後，成品就必須保住先後。**

### 「→」的判定：同一個符號兩種意思

使用者在原文裡用 `→`，但兩種用法混在一起：

| 寫法 | 意思 |
|---|---|
| `黃男 攻擊→ 范男` | **動作方向**：施動者 → 受動者 |
| `回擊(范男→黃男)` | 同上，括號內註明方向 |
| `掃描確認有無晶片→若無依無主動物屍體處理→集體火化` | **流程時序**：步驟先後 |

現在完全沒有規則講怎麼讀 `→`。grep 過 `main.py`／`news_prompt.py`，
唯一的箭頭條文是漲跌配色那段（main.py:1310，講上漲箭頭／下跌箭頭），與這件事無關。

要規則化的至少三點：

1. **`→` 一律是使用者在標示方向或順序，不是新聞文字**，絕不能被當成內容畫進圖裡
   （目前沒踩到，但沒有規則保證）。
2. **怎麼分辨「動作方向」與「流程時序」**：初步判準是「箭頭兩端是不是人／主體」——
   兩端都是主體 → 動作方向；兩端是動作或狀態 → 流程時序。這只是起點，
   要拿實際樣本驗證，**不要直接寫死**。
3. **讀出來之後要怎麼落到成品**：動作方向 → 圖上的箭頭＋文字標籤兩邊都要保持同向；
   流程時序 → 卡片排列順序不得重排。**文字與圖必須同向**——卡拉OK 那張就是圖對字反。

### 與既有項目的關係

- **B14** 記的是這次的實際錯誤（活貓、角色顛倒），本節是它的規則化。
- **D12／F19**（行內指令規則化）與這件事相鄰但不同：`(ICON)` 是**指令**，
  `→` 是**內容的結構標記**。兩者都寫在原文裡，都要被讀懂，但一個不能出現在成品、
  另一個必須反映在成品的排列上。**做 F19 時要一併想清楚這條界線**，不要混成一套。

### 驗收

造一組帶方向與順序的測試文字（攻擊／回擊、先後步驟、A 告 B、A 賠 B），
各跑 N 次，逐句對施動者與順序。**這種錯在成品上看起來完全正常，不逐句對抓不到。**

---

## 待處理：原文內嵌指令要規則化（2026-09-14 使用者裁示「要放寬，有真實需求」）

使用者看到葉怡君的紀錄後裁示：**使用者習慣直接把指令寫進新聞原文，而不是用 AI 指令欄，
這確實要放寬、也有真實需求——因為使用者可以針對細部某幾句話微調。**

### 現況查證：看得懂，而且「並存」早就是明文設計

- `USER_INSTRUCTION_RULES`（main.py:1368，文內解析）第 1 條**已經**寫明指令
  「usually on its own line and may be marked 指示:／指令:／備註:, **but it may also be
  plain prose**」，並列舉風格、版面、長度、逐字保留四類。第 4 條：指令文字絕不能出現在
  `variable`、不能畫進圖、也不能被描述。第 7 條：沒有指令時這一段什麼都不改。
- `DEDICATED_INSTRUCTION_RULES_TEMPLATE`（main.py:1388，專用指令欄）明文
  「If the news material ALSO contains instructions, **obey both**. When the two conflict
  on the same point, this dedicated instruction wins; on every other point each instruction
  still binds — **neither cancels the other**.」

**所以「跟原有 AI 指令並存」沒問題——優先序早就寫死了：專用欄位優先，但兩者互不取消。**
不需要重新設計並存機制，要補的是下面三個缺口。

### 實測證據（葉怡君 2026-09-14）

**有效的**：
- `(粗體字)` → 16:30 那張「未達公共危險之程度」確實做成粗體黃底。
- `(ICON)` → 11:23:34 那張畫成人形剪影＋姓名籤＋攻擊／回擊箭頭，完全照她的意思。
- 指令文字本身沒有被畫進圖裡（第 4 條守住了）。

**失效的**：
- `(蓋字章)` → 11:23:34 的「3人擦挫傷」是白色平體字，**不是蓋章**。這一則的
  `<蓋章>` marker 沒有被觸發。
- `(ICON)` **在同一段文字的後續生成中遺失** → 11:47:30、11:51:40 兩次重生都畫成
  寫實人物，剪影不見了。新聞事件當事人用寫實人像本身就有風險（捏臉／誤認）。

### 三個要規則化的缺口

**Y6｜行內夾注沒有明文涵蓋。** 規則第 1 條說指令「通常自成一行」，但葉怡君的寫法是
**夾在句子中間的圓括號**（`尿檢濃度(有兩張照片可以使用)`、`黃男(ICON) 攻擊→`）。
現在靠模型自己判斷，所以會時靈時不靈（`(ICON)` 11:23 靈、11:51 不靈）。
要補：明文承認「圓括號夾注也是指令」這個形式，並給出反例（真正屬於內容的括號，
例如 `(案發時間)`、金額單位、法條括號）怎麼區分。

**Y7｜局部指令沒有「綁定到哪一句」的概念。** 這是使用者講的重點——
「使用者可以針對細部某幾句話微調」。現有兩塊規則都把指令當成**全域**的
（進 `style`／`structure`），沒有「這個指令只作用在緊鄰的那一句／那個詞」的語意。
`(粗體字)` 要粗的是前一句、`(ICON)` 要圖示化的是前面那個人名、`(蓋字章)` 要蓋章的是
那一行——全域規則表達不了這件事。**這是真正要新增的能力，不是措辭修補。**

**Y8｜版面元素標記與風格指令混在一起。** `(ICON)`／`(蓋字章)` 不是 style 指令，
是**版面元素標記**，對應系統已有的 `[標題]`／`[內文小標]`／`<蓋章>` marker 體系。
要不要把使用者的口語標記（ICON／蓋章／粗體／框起來／放大）正式對應到 marker，
還是另立一套行內語法，是設計題。**先不要自己選一套語法**——使用者已經在用的寫法
（圓括號＋口語詞）本身就是最低學習成本的候選。

### 驗收

同一段帶行內指令的文字連跑 N 次，統計每個標記的命中率；改之前先跑一輪當基準
（`(ICON)` 現在的基準是 4 次中 1 次）。

---

## 待處理：追加修改連打會讓畫面語意漂移（2026-09-14 正式站實例，葉怡君上午）

葉怡君 2026-09-14 上午兩批，共 12 筆，其中 8 筆是 `web-refine`。

### 批一：路死動物處理流程（09:51–10:02，2 筆生圖＋6 筆 refine）

| 時間 | 動作 | 第三格「集體火化」畫的是 | 主體 |
|---|---|---|---|
| 09:51:39 | 生圖（3D示意／流程） | **焚化廠**（煙囪＋爐火），正確 | 躺在路面的死狗，正確 |
| 09:56:08 | 生圖（3D示意／流程） | 焚化爐（爐內有動物遺體） | **坐著的活貓** |
| 09:58:31 | refine | **紙箱堆**，語意完全掉 | 坐著的活貓 |
| 10:02:29 | refine | 紙箱堆 | 坐著的活貓 |

**Z1｜要求移除某個元素時，模型用不相關的東西補位。** 09:56 那版爐內有動物遺體，
播出不宜，合理推測她要求拿掉；模型不是換成空爐或象徵性處理，而是**換成一疊紙箱**——
「集體火化」配紙箱，語意整個錯掉，而且後續 refine 再也救不回來。要補的是：
refine 移除元素時，**替代物必須仍然承載該格的文字語意**，否則寧可留空。

**Z2｜主體事實漂移：路死（死）動物變成活貓。** 首版是躺在路面的死狗（正確），
換版型重生後變成端坐的活貓。題目是「路死動物處理流程」，活生生坐著的貓是事實錯誤。
這屬於既有的「內容忠實度」規則沒守住，但表現在**圖像**而非文字上。

### 批二：卡拉OK衝突（11:23–11:51，3 筆生圖＋1 筆 refine）

| 時間 | 人物畫法 | 問題 |
|---|---|---|
| 11:23:34 | **人形剪影＋姓名籤**，箭頭方向正確 | `(蓋字章)` 沒生效：「3人擦挫傷」是白色平體字不是章 |
| 11:47:30 | 寫實人物 | `(ICON)` 遺失；**左上標「黃男遭攻擊」與原文「黃男攻擊→范男」角色顛倒**；外框變紫色letterbox，非標準深藍 |
| 11:51:40 | 寫實人物 | `(ICON)` 遺失 |

**Z3｜角色顛倒（內容忠實度）。** 原文明寫「黃男(ICON) 攻擊→ 范男(ICON) 林女(ICON)
回擊(范男→黃男)」，11:47 那張把黃男標成「遭攻擊」，攻守顛倒。這種錯在播出畫面上
看起來完全正常，**驗收時不逐句對就抓不到**。

**Z4｜外框配色跑掉。** 11:47 那張的上下留白帶是紫色，其餘都是標準深藍。要查是
版型自己的底色還是模型把背景畫成紫色後被裁切帶進來。

**Z5｜新聞當事人用寫實人像的風險。** 11:47／11:51 把刑案當事人畫成寫實人物並掛上
真實姓氏籤（黃男／范男／林女）。這比剪影危險——觀眾可能誤認為真人照片。
與既有的「真實人物圖須參考真實樣貌」「捏臉」兩條 TODO 相關，但方向相反：
**這一類（刑案當事人、未公開身分）應該強制剪影或去識別化，而不是畫得更像。**
要不要訂成規則是裁決題。

證據圖（640px 縮圖已看過，原圖在後台）：
`20260914-095139`／`095608`／`095831`／`100229`（路死動物四階段）、
`20260914-112334`／`114730`／`115140`（卡拉OK三版）。

---

## 待處理：自動判斷版型不穩＋附圖位置／尺寸失控（2026-09-14 正式站實例，葉怡君）

使用者指名查葉怡君 2026-09-14 的紀錄。當日 17 筆，最後一批 16:30–16:43 共 5 筆
（3 筆生圖、2 筆 web-refine），**輸入文字五次完全相同**：

> 控獲不起訴處分 車牌難取回! 尿檢濃度(有兩張照片可以使用) 未達公共危險之程度(粗體字)
> 仍觸犯行政罰 道交條例第35條.第42條

| 時間 | 動作 | 自動判斷選到的版型 |
|---|---|---|
| 16:30:56 | 生圖 | **資料圖表** |
| 16:32:19 | web-refine | — |
| 16:37:09 | 生圖 | **情境示意圖** |
| 16:39:03 | web-refine | — |
| 16:43:02 | 生圖 | **3D示意／流程** |

**Y1｜自動判斷在同一段文字上不穩，三次挑了三種版型。** 每按一次「生成」就換一整套
版面邏輯，使用者等於在賭。這不是創意變化——是分類結果本身在跳。要查的是
`resolve_effective_type_label` 那條兩段式判斷有沒有溫度／隨機性，以及同一段文字
重複呼叫的實際分佈（先量測，跑 N 次統計命中分佈，再決定要不要釘死或給使用者鎖定鈕）。

**Y2｜附圖左右位置每次重生／refine 都會互換。** 她附了兩張文件照（尿檢報告、警方
函文，都有紅框標記）。16:30 是「左報告／右函文」，16:32 refine 後變成「左函文／右報告」，
16:37 又換回來，16:39 再換一次。**這跟王結玲那件是同一個病，但發生在 `/api/generate`
自動判斷線，不是 YT 封面線**——所以 D4 的範圍要從「YT 生圖模式」擴大成「全線」。
文件類附圖的左右順序有語意（哪張是哪份文件），互換等於資訊錯置。

**Y3｜附圖尺寸失控，同一批裡兩個極端。** 16:30／16:32 兩張文件照佔滿畫面上半部、
下方留一條大空白（版面沒填滿）；16:43 的 3D 流程版反過來把兩張照片縮成左側小圖，
**文件內容完全看不清**。她在原文明寫「(有兩張照片可以使用)」，顯然是要人看得到文件
內容的，但系統沒有「文件類附圖要大到看得清楚」這條規則——附圖只被當成裝飾素材。

**Y4｜使用者把指令寫在新聞原文裡。** `(有兩張照片可以使用)`、`(粗體字)`、`(ICON)`、
`(蓋字章)`（當日 11:23–11:51 那批也是同樣寫法）。這些是給系統的指示不是新聞內容。
好消息是沒有被當成文字畫進圖裡（16:30 的「未達公共危險之程度」確實做成粗體黃底，
指令有生效）；但這是明確的需求訊號——**網頁版需要一個獨立的指令欄位／語法**，
與既有的「LINE 訊息內帶指令」是同一件事的網頁版對應。

證據圖：`D:\Downloads\20260914_葉怡君_自動判斷不穩_對照.jpg`（四張直排：16:30 資料圖表／
16:32 refine 後左右互換／16:37 情境示意圖／16:43 3D流程，照片縮到看不清）。

**未查**：她當日上午兩批（09:51–10:02 路死動物流程 7 筆、11:23–11:51 卡拉OK衝突 4 筆）
都是連續 refine，看得出同樣在反覆修，但沒有逐張看圖，不知道是不是同一組病灶。

---

## 待處理：YT 封面生圖模式下附圖位置由模型決定（2026-09-14 正式站實例）

使用者問「附圖的順序適合成的順序嗎」，並指名去後台查王結玲 2026-09-14 下午的
YT 封面紀錄。查證結果（正式站 `/admin`，2026-09-14 16:06–16:17 共 7 筆，
主題都是「北東轉雨略降溫」）：

| 時間 | model | 成品版位 |
|---|---|---|
| 16:06:46 | gpt-image-2.5-sunburst | 單一 AI 場景，無地圖 |
| 16:09:51 | gpt-image-2.5-sunburst | 左照片／右地圖，右下多三個配件圖示 |
| 16:11:22 | gpt-image-2.5-sunburst | 左照片／右地圖（乾淨，日期 09/14） |
| 16:12:58 | web-refine | 輸出 1748×924，標題與 09/15 已燒進圖裡 |
| 16:13:08 | **yt-cover:overlay** | 把上一張當附圖疊圖 → **兩個日期** |
| 16:14:18 | gpt-image-2.5-sunburst | **左地圖／右照片**，地圖壓到左上 24H LIVE 牌 |
| 16:17:26 | gpt-image-2.5-sunburst | 又跳回左照片／右地圖 |

**R1｜位置不穩的根因不是「附圖順序」。** YT 早就有 `asis_left`／`asis_right` 兩個
獨立上傳位（2026-09-07 裁決：左右格各一位，才不會分不清哪張是左），走
`yt-cover:overlay` 時是程式切格、順序確定。但跳位的那幾筆 model 是
`gpt-image-2.5-sunburst`——**生圖模式**，附圖是拿去當生圖參考被模型重畫，落在哪一格
由模型決定，程式管不到。所以 16:14 與 16:17 同樣兩個元素會左右互換。

要裁的是：生圖模式下要不要也讓使用者指定左右？三個方向——(a) 前台在生圖模式明講
「位置由 AI 決定」，不做保證；(b) prompt 裡明寫哪個元素放哪一側（措辭推得動與否要實測）；
(c) 有附圖時一律導向 overlay（程式切格），生圖只負責沒附圖的那一格。

**查不到、要問使用者的一件事**：她是把圖放進「原圖放置」上傳位還是一般附圖位？
後台只存最終成品與 prompt，**不存附圖**，所以從紀錄推不出來。這決定要修 R1 的哪一支。

**R2｜已合成的成品再當附圖餵回去，日期會重複。** 16:12:58 的 web-refine 輸出已經把
標題與 `2026/09/15` 燒進畫面，16:13:08 拿它當附圖做 overlay，程式又在左上角貼一次
日期牌 → 成品同時有左上 09/15 與右下 09/15。目前沒有任何防呆。可考慮：overlay 前
偵測附圖是否已含合成元素（尺寸非 16:9 就是一個強訊號——那張是 1748×924），或至少在
前台提示「這張看起來已經合成過」。

**R3｜web-refine 會改掉尺寸。** 1920×1080 進去、1748×924 出來，非 16:9。後續任何
疊圖都會歪。這一條可能是 R2 的上游真因，要單獨查 refine 線有沒有鎖長寬比。

證據圖：`D:\Downloads\20260914_王結玲YT封面_位置對照.jpg`（三張直排）、
`D:\Downloads\20260914_王結玲_重複日期_161308.jpg`。

---

## 待處理：字多／字少的檔位差距不夠明顯（2026-09-14 列入）

使用者回報：消化程度拉桿在「字少」跟「字多」之間拉，出來的成品文字量差得不夠
多，兩端看起來太像。

**紙面上的規格其實差很多**（`main.build_digest_instructions`，main.py:1533-1541）：

| 檔位 | 規則區塊 | 紙面規格 |
|---|---|---|
| 字極少 `minimal` | SIMPLIFIED ＋ MINIMAL | 1 點、該行約 12 字 |
| 字少 `simplified` | SIMPLIFIED | 1–3 點、短行 |
| 字多 `standard` | STANDARD | 至多 6 點、每行約 24 字、總計 240–320 字 |
| 字超多 `maximum` | STANDARD ＋ MAXIMUM | 至多 8 點、每行約 30 字、總計 360–480 字 |
| 不改字 `verbatim` | VERBATIM | 另一條線 |

所以問題不在「沒寫清楚」，而在**寫下去的數字不會真的發生**。三個具體嫌疑，排查
順序由強到弱：

1. **兩個上檔都是天花板不是地板。** STANDARD 第 7 條與 MAXIMUM 第 3 條都明文寫
   「THIS LICENSES NOTHING NEW ... this setting raises the ceiling, it does not
   set a quota」「If the material supports only three points, write three」。同一篇
   新聞素材有限時，字多與字超多會一起塌到素材能支撐的點數，兩檔輸出當然一樣。
   要不要改成「素材夠就必須寫滿」是**取捨題**：那條禁令當初就是為了擋模型自己
   補料（封面／CG 上編出來的數字是對外事故），鬆綁前要先想清楚防線怎麼補。
2. **版型區塊會把點數蓋掉。** STANDARD 第 6 條與 MAXIMUM 第 5 條都讓「後面的
   format-specific 區塊指定的確切張數」勝出。在播出鏡面那種卡片堆版型上，字多與
   字超多的點數是被版型釘死的同一個數字，拉桿在那些版型上**本來就不會有差**。
   要先確認使用者感覺不夠的是哪個版型——若是卡片堆版型，這不是 bug 是設計。
3. **字少／字極少端要分開看。** MINIMAL 給的是硬上限（1 點、12 字），這一側應該
   是有差的；若使用者感覺的是「字少跟字多沒差」，那重點全在上檔那兩條。

驗收方式：同一篇新聞跑五檔，數實際成品字數與點數，列表比對，確認單調遞增且級距
夠大。這題是「感覺差不多」，**沒有量測就不算修好**；而且要分版型量（卡片堆版型
與自由版型各一），否則會把第 2 點的設計行為誤判成 bug。

---

## 待處理：CG 線（記者版／編輯版）創意 4 級不夠亮，要導入變化池（2026-09-14 列入）

使用者把記者版與編輯版的創意階梯拉到最高等級，成品仍不夠讓人眼睛一亮；使用者推測
「應該有導入元素池吧」——查證正確，**CG 線沒有池子**。

完整查證與修法已併入下方「創意階梯三條線對齊清點」的 **G1**（沒有變化池）與
**G4**（配件件數交給模型自選），可搬／不可搬的池子清單與位元組釘死範圍也在那一節。
動工前要先裁 **G2 的 seed 政策**。

---

## 待處理：最終 prompt 仍有多餘符號，評估改走 Markdown 規格大瘦身（2026-09-14 列入）

使用者回報：組出來的最終 prompt 還帶著多餘的符號，想評估能不能整份改成 Markdown
規格順便大幅瘦身。

要做的：

1. **先量再改。** 把一份代表性的最終 prompt 落檔，統計字元數／token 數，標出哪些
   是真正的指令、哪些是裝飾（分隔線、全形括號、編號括號、重複的 OVERRIDE 宣告）。
   沒有基準數字就無法判斷「大瘦身」有沒有發生。
2. **最大的坑：位元組被測試釘死。** `creativity.py` 開頭（creativity.py:13-14）明文
   寫著記者 prompt 的位元組是釘死的，「項次字母本身就是位元組的一部分，抽走幾條
   再重新編號就會紅」。瘦身等於改位元組，一定會打到那批測試——動工前要先判斷
   那些測試是「防手滑」還是「防行為改變」，再決定是改測試還是這一段不能動。
3. **順序有語意，不能順手重排。** density → 蓋章 → 色調的注入順序是刻意的
   （main.py:1543-1553 的註解明寫「順序不能倒」，因為後面的條文會引用前面的例外）。
   換版面時若連帶動到拼接順序，會安靜地改掉行為。
4. **符號可能是刻意的。** 現有規則大量使用全大寫 ＋「THIS BLOCK OVERRIDES ...」
   這類措辭來建立優先權，那不是裝飾而是覆蓋機制。改 Markdown 前要先確認換成
   `#`／`-` 之後，模型還認不認得誰蓋過誰。
5. **驗收：** 瘦身前後同一批輸入各跑一輪，比對成品差異；只有「成品沒變壞」才算數，
   光看 token 少了不算。

---

## 待處理：消化品質閘只裝在 `/api/generate`，封面線全部裸奔（2026-09-11 列入）

`digest_quality_problem` 全檔只有一處呼叫（`main.py:1888`，generate 的重試圈內）。
也就是說截斷、字元污染、頻道洩漏、簡體／異體字這四道檢查，**只有 CG 一鍵生成
那條線有**。以下五個 `digest_completion` 呼叫點一道都沒過：

| 呼叫點 | 是什麼 |
|---|---|
| `hybrid_digest`（約 2060） | 一鍵成圖 hybrid.html |
| `resolve_cover_visuals`（約 3879） | 十點封面自動補畫面描述 |
| `cover_titles`（約 4392） | 十點／YT／直標的「AI 消化標題」 |
| `derive_yt_cover_plan`（約 4768） | YT 封面補畫面描述、分行 |
| `classify_chart_type`（約 589） | 類型分類器（回 enum，風險最低） |

風險最高的是 `cover_titles`：它的輸出**直接變成封面上的字**，模型寫出「导致」
就會原樣印上成品，而封面是要上鏡的。2026-09-05 建這道閘時只接了 generate，
後來加的封面線一條都沒跟上——跟 2026-09-11 修好的後台稽核歸檔是同一種病
（共用函式存在，但新端點沒人記得去接）。

做的時候要注意兩件事，不能照抄：

1. **品質閘的欄位名是寫死的**（`variable` / `style` / `structure`），封面線的
   schema 是 `title_left` / `title_right` / `info_chips` 之類，得先把「要檢查
   哪些欄位」參數化，否則接上去等於什麼都沒檢查、測試還會全綠。
2. **失敗要怎麼辦**。generate 有五次 attempt 可以重問；`cover_titles` 只有
   `TEN_DIGEST_MAX_ATTEMPTS` 次而且那是留給三段字數規格用的，
   `resolve_cover_visuals` 與 `derive_yt_cover_plan` 更是「失敗就走退路」。
   要嘛共用重試圈，要嘛檢查到就退回退路——不能直接 raise 502。

順帶：`cover_titles` **完全沒有 API 錯誤重試**（任何上游例外 → 直接 502），
要不要一起補，使用者尚未裁決。

## 待處理：入門手冊截圖解析度太差（2026-09-10 列入，下一輪修手冊時處理）

使用者：「手冊截圖解析度太差，下一輪修手冊時處理。」

現況（`docs/user-guide-page1.html`，2026-09-10 量的）：

- **版型對照表**十張：一律寬 960 的 JPEG，單張 71–137KB。在一般筆電上是 2x DPR，
  等於用一半解析度放大顯示，字邊會糊。
- **分區截圖**那批：540–700px 高，同樣是 JPEG 壓過。
- **行內按鈕圖**（`class="btn"`）8 張：18–40px 高，是從 1x 螢幕截的，縮放後最糊。

處理方向（下一輪一起做，不要零星改）：

- [ ] 全部改成 **2x 來源**：截圖時把瀏覽器縮放或 DPR 開到 2，出圖寬度加倍，
      再用 CSS 限制顯示寬度（版型對照表維持顯示 960，實際像素 1920）。
- [ ] 行內按鈕圖改用 **PNG** 不要 JPEG：純色底＋細字，JPEG 的色塊雜訊在小圖上特別明顯。
- [ ] 重新盤點檔案大小上限。現在整份 2.69MB，全面 2x 之後會爆；
      版型對照表那十張可考慮改成點擊放大（縮圖 960、原圖 1920）而不是直接內嵌大圖。
- [ ] 決定要不要改用外部圖檔而不是內嵌 base64——Artifact 只吃單檔，
      要外連就得先確認 CDN 白名單擋不擋（目前只允許 cdnjs／jsdelivr／fonts.googleapis）。


## 待驗收：十點封面「側邊小標籤」（2026-09-10 列入，前端已先藏）

2026-09-10 做好但**功能還沒測夠**，同日使用者裁決先降規模：

- **前端欄位藏起來**：`index.html` 的 `#coverSideLabels` 加了 `hidden`。欄位**留在 DOM**，
  `app.js` 照樣讀得到空字串、後端也照樣接受 `side_labels`，要放出來只要拿掉 `hidden`。
- **0／1／2 級一律不畫**：`main.py` 的 `side_labels_block` 只在 `creativity_level() >= 3`
  才組出來。理由是規矩／微設計／有設計那三級版面本來就滿，多一排籤會擠掉標題。
  守門測試 `tests/test_cover_title_creativity.py::SideLabelTests::test_low_creativity_does_not_draw_the_chips`。

**放出來之前要驗的**：

- [ ] **3／4 級各跑數輪**，確認籤永遠只出現在右格、不撞標題、不進標頭帶與底部光帶、
      不被程式貼的「AI示意圖」小標壓到。目前只實拍過 4 級、三輪才調對位置。
- [ ] **滿版版型**（單張照片）下的落點還沒實拍過，規則寫的是「畫面右側」。
- [ ] **字數與數量的邊界**：1 個籤、6 個籤、每個都滿 6 字的情況，會不會擠或縮到看不清。
- [ ] **逐字正確性**：籤上的字是使用者打的，錯一個字就是上鏡錯字。要抽驗夠多輪才敢放。
- [ ] **與反色底字同時出現**（3 級起兩者都會有）會不會搶同一塊空間。
- [ ] 決定放出來時前端要不要**只在創意 3 以上才顯示欄位**（現在是整個藏起來，
      不是依創意級數顯示），否則使用者填了字卻在低創意下不出現，會以為壞掉。

## 待建立：把十點封面的創意階梯移植回通用版創意階梯（2026-09-10 列入）

**由來**：2026-09-10 把「十點不一樣」標題階梯（`editor_formats.py` 的
`cover_title_creativity_block`）改成**結構槓桿**後，0→4 的差異變得肉眼可辨；
同日通用版 CG 創意階梯（`main.py` 的 `_CG_L1`–`_CG_L4_EXTRA`）也改過一輪、
方向對了但變化幅度仍不如十點封面。使用者要求：清查十點那套有哪些槓桿可以移植。

**候選槓桿（十點有、通用版目前沒有或很弱）**：

- [ ] **反色底字**（`REVERSE A KEY WORD OUT OF A SOLID BLOCK`）。十點是 3 級起強制、
      且指示**釘在行清單的那一行上**才生效（寫在條文區會被忽略，2026-09-10 實測）。
      通用版目前完全沒有這條。移植時要一起移植「釘在資料那一行」的寫法。
- [ ] **標題以外的元素**（十點的 `BUILD SUPPORTING ARTWORK … never captions`）。
      通用版有 `_CG_L3_EXTRA` 的 cut-out，但沒有「允許長出配件、但一個字都不准加」
      這條雙向約束。注意與 `_CG_CREATIVITY_FIXED (i)` 不衝突（加字者槓桿作廢）。
- [ ] **量化的尺寸落差**（十點寫「兩倍到兩倍半」「2.5–3 倍」）。通用版只寫
      hero >= 2x，級與級之間沒有遞增數字，這是 1 太像 0 的老病根。
- [ ] **多層邊緣／毛邊塊**（`MULTI-LAYER EDGES`）。純視覺、零文字風險，最安全的移植項。
- [ ] **強制傾斜**（十點 4 級的 mandatory tilt）。通用版 `_CG_L4_EXTRA` 已有
      `NO LONGER ORTHOGONAL`，但 2026-09-10 實拍傾斜仍不明顯，要對照十點的寫法補強。

**注意事項**：
- 通用版階梯是**記者版與編輯各版型（含播出鏡面）共用**，移植會同時影響三條線，
  每一級都要各自出圖驗收，不能只看一種版型。
- `_CG_CREATIVITY_FIXED (h)` 已把槓桿綁到「字多字少」的點數上，移植的新槓桿
  也必須寫成隨點數自適應，否則會與消化程度打架（2026-09-10 已確認打架是真的）。
- 記者版 prompt 有凍結測試 `tests/test_reporter_prompt_frozen.py`，紅了要先確認
  是刻意改動再重生 fixture。

## 待建立：色調亮／暗改成 5 段拉桿（2026-09-10 列入）

**現況**：`index.html` 是兩顆按鈕（`tone-btn`，色調亮／色調暗），靛藍配色，
`app.js` 存成 `state.tone` 的二值。2026-09-04 修過一次外觀（選中的那顆不再
「長成它代表的色調」，改成統一高亮）。

**要改成**：比照「標題創意」「消化程度」那兩條，做成 0–4 五段 `<input type="range">`，
最左＝最亮、最右＝最暗，右側顯示文字級別。

**待決定（動工前要問使用者）**：
- [ ] 五段的**文字標籤**叫什麼（例：極亮／偏亮／中性／偏暗／極暗）。
- [ ] 五段實際餵給 prompt 的**明度描述**是什麼。現在只有兩句話，中間三段是新寫的
      文案，不是把舊兩句內插——這是這件事真正的工作量所在。
- [ ] **預設落在第幾段**。現況預設「色調暗」，直接對應 4 會讓所有既有用法變最暗，
      對應 3 才接近現行結果，需實拍比對。
- [ ] 舊值相容：LINE 版與任何存下來的請求若還送二值 `tone`，後端要能接住。

**注意**：色調刻意不用紅色（紅是角色與消化程度的顏色），拉桿也不要用紅，
沿用現行靛藍系；`accent-violet` 是創意拉桿的專屬色，不要混用。

## 待建立：地圖繼續驗收（2026-09-10 列入，接續當日修補）

2026-09-10 已把地圖底圖改走原生 `images.edit`（`main.py` 的
`supports_map_basemap()` ＋ `_native_reference_files()`），離島（澎湖／金門／馬祖）
相對位置實拍已正確，分析與修補記錄在
`docs/error-cases/2026-09-10-台灣行政區界-錯誤-分析.md`。**以下是同批仍未收斂的**：

- [ ] **整張臺灣圖仍會裁掉南部**。已加 `MAP_ACCURACY_RULES` 12–14（禁行政區著色／
      禁虛構或裁切陸地／全部單位或一個都不畫），但實拍仍發生，要判斷是底圖畫幅
      本身沒含到南部，還是模型二次裁切。
- [ ] **距離與比例尺不符**。實例：颱風路徑「約 400 公里」畫成約 800 公里的長度。
      比例尺是空間精確的東西，按 `safe_frame.py` 的原則應該由程式畫、不該交給模型。
- [ ] **`images.edit` 路徑可能弱化文字忠實度**。同日測試有一則標題被模型改寫。
      要確認是 `images.edit` 的通病還是單次抽樣，若是通病要決定：地圖類犧牲文字
      由程式壓字，或分兩階段（先 edit 出底圖再 generate 壓字）。
- [ ] **要繼續加測的地點型態**：純縣市層級（無經緯度可查的行政中心）、
      跨國兩點連線、海域／航道（沒有陸地錨點）、以及只提北部但畫面比例很寬的情況。

## 襯底改為取自 CG 邊緣＋編輯版兩種框模式（2026-08-19 完成，未部署）

分支 `feat/backdrop-match`（`46cf939`、`5c2aae6`）。三件事一起做，**尚未部署**。

### 1. 安全框襯底不再憑空壓暗（記者＋編輯都受影響）

線上 10 張成品實測，襯底**每一張都比 CG 背景亮**（貼邊量測色差 11–22），
深底 CG 上會讀成一圈灰紫色外框。兩個成因疊加，只修一個會讓誤差翻面：

- 取樣：`_edge_colour` 取整張外圈的**平均**，被標題列與光暈拉高
  （實測外圈平均 (44,44,44)，內容緊鄰邊界處其實只有 (12,12,12)）。
- 合成：`0.62`／`0.44` 是與內容亮度無關的固定壓暗係數。

改成取自內容邊緣往內 1–4% 的**中位數**，壓暗只留 5%（`BACKDROP_DIM` 0.95），
投影 170 → 90。

**2026-08-19 二次修改：使用者要求襯底改單色，不要漸層。** 漸層版上下端點分開取，
能重現 CG 自己的明暗走勢；單色只能取一個值。關鍵是**取哪一邊**——拿 10 張線上
成品實測（數字＝襯底與內容邊緣平均色差的最差邊，愈小愈好）：

| 單色取法 | 最差邊 |
|---|---|
| **只取上緣（採用）** | **7.2** |
| 上下左右整圈 | 17.8 |
| 上＋左右 | 22.6 |
| 只取左右 | 28.1 |

差距大到不像雜訊，原因也說得通：CG 底部常有 `<蓋章>` 橫幅、出處行與暗角，
左右常是側欄卡片，都不是背景；上緣才是乾淨的背景色。第一版寫「整圈中位數」
量出來比舊版還糟（17.6/17.5），改上緣後回到 7.6/7.1。
**先天限制**：內容上下色差大時單色只能貼合上緣，下緣一定會偏（有測試明寫）。
端點分開取是必要的：同一張圖上緣 (31,48,66)、下緣 (29,38,43) 差很多。

驗證方式值得記下來——**從已置框成品把 `safe_rect` 那塊裁出來就等於還原生成圖**，
可以離線重組任何襯底方案並排比較，零付費生圖。以後調襯底都該這樣驗。

- 結果：貼邊色差 上 15.6 → 8.0、下 13.7 → 5.9，10/10 兩邊都改善；四角亮度 46–101。
- ⚠️ 這推翻 2026-07-30 選定的「刻意做成襯底（deliberate mat）」方向，
  是使用者看過對照圖後的決定。`BLUR`／`CLAMP` 兩條路徑未動。
- ⚠️ **LINE 版記者出圖的外觀也會一起變**（同一條程式碼路徑）。

### 2. 編輯版兩種模式對調

- **OFF ＝ 原本的 ON**：滿版 16:9 → 拉伸填滿對位框 1748×924
- **ON ＝ 新的**：滿版 16:9 → 四邊各壓 4% → 完整 1920×1080
- 原本 OFF 那條「靠 prompt 叫模型自己縮小置中、完全不後製」**已廢除**

4% 的算術剛好成立：77/43 的留白讓內容區 1766×994（比例 1.7767）幾乎正好是
16:9，FIT 進去零裁切零拉伸零補邊。這是它與對位框（1.892，必須在裁切／拉伸／
補邊三者間二選一）最大的差別。2026-08-19 當日調過 2%→4%→3%，2026-08-20
使用者再裁決改回 4%；試過的三個值與 16:9 的比例差為 2%→0.0021、3%→0.0022、
4%→0.0011，差距都遠小於容差——**選幾 % 是視覺取捨，不是幾何限制**，
改一筆 PROFILES ＋測試的 THIN_FRAME_RATIO 即可。

**投影必須跟著關掉**：投影伸展 SHADOW_DROP+SHADOW_BLUR＝36px，薄邊只有 43px，
開著會整條被壓暗、到畫布邊都回不到襯底色（實測亮度 200 的內容，襯底真色 175：
4% 的 43px 是 132→167、2% 的 22px 是 128→139）。`_shadow_fits()` 因此用
「留白 > 投影伸展的兩倍」判定，不是「剛好大於」——4% 只差 7px 就會通過剛好
大於，實測卻一樣難看。3% 的 32px 同樣停用。回到 4% 後 43px 仍由同一條通則
停用，程式不必改。記者框最窄 109px 不受影響。

最容易漏的是**編輯 OFF 仍然要後製**：舊的 `if not safe_frame: return` 會讓它
退回「完全不置框」，圖照樣出得來、不報錯，只是尺寸與版面全錯。因此新增
`main.resolve_frame_plan()` 當唯一解析點。`refine_image` 原本就漏接，一併補上。

prompt 分流依據改成「後端會不會水平拉伸」而非安全框開關本身：拉伸路徑要
`EDITOR_FULL_BLEED_RULES` 的上下背景帶吃掉失真；薄框走 FIT 不拉伸，
留帶只會讓實際邊界遠超過指定的 4%。

### 3. 待辦

- [ ] **線上是 3% 那版**（`tvbs-aicg-linebot-00026-4k6`，2026-08-19 部署）；
      2026-08-20 改回 4% 後**尚未部署**。也**尚未用真實生圖驗過**——上面所有數據都來自既有成品的
      還原重組，只證明幾何與顏色，不涵蓋「模型拿到新 prompt 會畫成怎樣」。
      薄框那檔的 prompt 從 `EDITOR_FULL_BLEED_RULES` 換成 `FULL_BLEED_RULES`，
      這一換沒有任何真實生圖證據。
- [ ] `EDITOR_SAFE_AREA`（news_prompt/app.js）與 `EDITOR_LAYOUT_SAFE_AREA`
      （main.py）隨編輯版舊 OFF 一起退場，現在是死程式碼。留著當歷史，
      要清的話兩份要一起清（有 parity 測試盯著）。
- [ ] 記者版的 ON/OFF 語意沒動——只有編輯版對調。若日後記者也要薄框，
      `resolve_frame_plan` 是唯一要改的地方。

## 待建立：「整理包」封面（議題取向雙主題疊層）（2026-09-06 列入）

型錄 `D:\Downloads\aicg_font_20260906\catalog\16.png`（「全球3100條躍動冰川」＋「美軍空襲伊朗」）
原本被歸在十點不一樣底下，使用者裁決：**這是另一種規格「整理包」**，議題取向，
所以**沒有節目名稱、沒有日期時間**，不能拿十點封面的樣板改。

版面特徵（僅此一張樣本）：單一大背景；左側主題三行大字（白／黃特大／白）；
右側副主題小字兩三行；一個紅色斜角標籤（例「氣候定時炸彈」）；左上「AI示意圖」。

使用者要先蒐集更多範例再定規格，**等使用者拿樣本回來再動工**。屆時需決定：
輸入欄位（主題／副主題／標籤字）、是否消化、是否共用十點的斜切拼圖與逐行配色程式。

## 待建立：YT 直播封面再兩種頻道版型「氣象站」「國會風雲」（2026-09-06 列入）

掃 TVBS NEWS 頻道「直播」分頁時發現，除了已做的「YT國內外新聞直播」「YT整點直播」
（`docs/yt-live-cover-spec.md`），頻道還固定用兩種設計版型，使用者裁決先列待辦：

- **氣象站**：左上「氣象站」標籤＋一排天氣小圖示、右上 LIVE＋日期、右側主播去背半身照、
  中間雨量／衛星圖、底部兩行標題（關鍵字紅／藍變色）。需要主播去背素材。
- **國會風雲**：左上「國會風雲」標籤＋LIVE＋日期時間、右上「主播+立委線上聊」、
  雙人去背（主播＋立委）、底部兩行標題。需要主播與來賓去背素材。

共同前提：去背人像素材來源與授權（是否由使用者上傳、還是建素材庫）；
關鍵字變色規則（哪些詞標紅／藍）需要編輯定義。對照圖：
`D:\Downloads\aicg_yt_cover_20260905\channel_survey\01~03_頻道直播縮圖總覽_*.png`。

## /api/generate 缺重試機制（2026-07-23 實測發現）

主流程 digest 端點無重試，上游（OpenRouter 多 provider 輪替）偶發 502／不合 schema
回傳時直接失敗——R4 對照實驗連續兩次被打斷。`/api/hybrid/digest` 已有
「呼叫＋解析全包」3 次×1.5s 重試（`main.py` hybrid_digest）可直接作範本移植。

- [ ] 把 hybrid 的重試迴圈套用到 `/api/generate`（預估 15 分鐘）

## 地圖／位置圖：自動生成 Prompt 缺乏地理準確規則（2026-07-23 清查）

**現象**：自動生成的地圖類 CG，島嶼／地點位置常與真實地理位置有明顯落差。
案例：沖之鳥島 EEZ 實彈射擊爭議圖 — 沖之鳥島被畫得太靠近日本本土／沖繩（實際在東京南方約 1,700 公里，N20°25′31″ E136°04′11″）。

**清查結論**（懷疑屬實，且有四層原因）：

1. **自動流程零地理規則**（`main.py`）
   - 自動判斷只有「何時選地圖類型」的規則（`main.py:86`），選定後 style/structure 生成指令對地圖零要求：無座標、無北方朝上、無比例尺、無「不得移動地物」。
2. **SAFE AREA 規則反向禁止座標**（`SYSTEM_PROMPT_TEMPLATE` 內）
   - 「NEVER express any position... as a percentage, pixel, ratio, or number of any kind」為防版面數字入圖，副作用是 digest 模型連經緯度都不敢寫。
3. **唯一有座標紀律的是手動模板**（`app.js:192`「指定國家 標記」）
   - 要求 marker 依真實座標放置＋使用者手填 `lat,long`，但自動流程不會經過它。
4. **Digest 模型查不到座標**
   - 後端無 GIS／搜尋，座標只能靠模型記憶，冷門地點（如沖之鳥島）容易錯。

**修法方案**：

- [ ] `build_digest_instructions()`：當 `chart_type == "地圖／位置"` 時條件注入 MAP ACCURACY RULES 區塊：
  - 北方朝上（north-up）、東西南北方位固定
  - 已知地名輸出經緯度，marker 固定於真實座標，不得為構圖移動／壓縮／重排地物
  - 距離線（如 180 公里）須依同一比例尺按比例繪製，附方位角
  - 大範圍＋細節建議雙層地圖（locator overview + detailed map）
  - EEZ 等爭議範圍標示「主張範圍 示意」
  - 「simplified」僅限線條簡化，不得改變座標、方位、相對距離
- [ ] SAFE AREA 禁數字條款加豁免：「地理座標、距離數值、比例尺」不在禁止之列
- [ ] （進階）最穩定路線：GIS／官方底圖作為參考圖上傳，模型只處理配色、資訊卡、標題與風格，不得改動陸地輪廓與標記座標 — 參見 `docs/hybrid-rendering-proposal.md`

**參考資料**：`docs/error-cases/2026-07-23-沖之鳥島-位置偏移-分析.md`（完整分析，含可直接替換的 MAP ACCURACY RULES prompt 段落與 STRUCTURE 改寫範例）＋同名 `.jpg` 錯誤案例圖。

**2026-07-31 進度與裁決**（已隨 `feat/visual-fidelity-and-input-filter` merge 回 main，`e5b3e21`）：

MAP ACCURACY RULES 已實作（消化＋生圖兩階段，含座標定位、north-up、雙層地圖、
主張範圍示意等），並跑了四輪地圖實測（A 沖之鳥島／C 仁愛礁／D 北韓飛彈／E 紅海，
完整分析見 `docs/error-cases/2026-07-31-視覺忠實度規則-合併驗收-分析.md`）：

- digest 端座標四輪全對（含好望角 34.36S 18.47E 級精度），座標路線在消化端站穩
- 生圖端 GPT 3/4 合格（僅首輪偏移，其後距離皆經比例尺實測成比例）；
  Gemini 拓撲通常對但家具缺陷高頻（比例尺數字錯 3/4、括號入圖 2/4、
  繁中亂碼 1/4、點位偏離自身網格 1/4）——皆屬 Gemini 既有病史
- 座標入圖裁決：可接受，但 prompt 不主動要求列出（規則措辭已改中性）

- [x] MAP ACCURACY RULES 條件注入（含自動判斷模式的文字自我限縮）
- [x] 禁數字條款豁免——以範圍受限的新區塊實作，未改動 layout 常數與
      CONTENT_FIDELITY_RULES；座標只定位、不主動要求列出
- [ ] **地圖類暫定優先用 GPT 引擎（2026-07-31 拍板）：勉強接受，仍有改進空間**。
      未解：GPT 偶發不照座標畫（1/4）、Gemini 家具缺陷（非 prompt 能修）、
      單張仍需人工複核。改進方向：(a) 以圖生圖落地後改走 GIS 底圖路線
      （使用者附真實底圖，模型不得改動陸地輪廓／位置／比例尺），優先於
      模型記憶座標；(b) 驗收攔截（量測圖上點位 vs 宣告座標）；(c) 每型別
      引擎路由（地圖→GPT）做成正式機制而非人工記得選
- [x] **休達案例四個根因（2026-08-01）**，詳見
      `docs/error-cases/2026-08-01-休達地圖-根因分析.md`：
      - [x] 地圖類消化 token 預算不足導致必然截斷 → `MAP_DIGEST_MAX_TOKENS = 6000`
      - [x] 截斷／污染的髒資料語法合法就被收下送生圖 → `digest_quality_problem()`
            擋截斷、外文字元污染、英文自言自語、逐詞灌行失控（實測 91 行）
      - [x] 標題讓「回應國」變成主詞，生圖端因此把該國 callout 畫到事件地上
            → MAP ACCURACY RULES 第 11 條：只有事件發生地能當標題主詞
      - [x] 消化模型間歇性脫軌 → `.env` 的 `DIGEST_MODEL` 換回
            `anthropic/claude-sonnet-5`（實測污染率 gpt-5.6-terra 4/10 vs
            claude-sonnet-5 9/10），重試 3 → 5 次
- [ ] **地物輪廓錯誤：兩個生圖引擎都畫錯，且錯法不同（2026-08-01 人工＋Google Maps
      對照確認）**。休達複驗圖經使用者判定形狀仍錯，開 Google Maps 衛星圖逐項比對：
      - 休達實況：西側整條約 8 公里**陸地邊界**貼摩洛哥（Benzú 到 Fnideq）；
        開闊海域在北（直布羅陀海峽）與東（地中海）；市區中央一道**窄地峽**，
        以東是**阿喬山半島**，港口舊城都在那裡。
      - GPT 版錯法：把西側陸地邊界**畫成海**，休達變成往東北伸出的陸塊，靠底部
        一條細地峽連摩洛哥（陸路入境箭頭穿過該處）；阿喬山半島與市區地峽消失，
        港口被移到東北角。等於唯一畫對的地峽被放錯位置、賦予錯的意義。
      - Gemini 版錯法（`87bd45b4df1d`）：休達直接畫成**四面環海的孤島**，與摩洛哥
        之間隔開闊海域、完全沒有陸地相連；圖上卻同時畫了步行者與汽車圖示、
        箭頭跨海指向該島，與自家文案「陸路及海路」「與摩洛哥接壤」直接矛盾。
        另比例尺單位標成「1m」（應為公里），兩張圖皆是。
      - **兩者的定位總覽圖都合格**（休達點在海峽南岸、西班牙南端下方），錯的
        只有細部圖 → 問題不在座標、不在消化階段，而在生圖模型**憑文字描述
        即興畫海岸線**，與引擎無關。規則層補不到根本。
      - Gemini 版另有進步可記錄：法國 callout 正確落在法西邊界（第 11 條有效）、
        無杜撰地名、有「示意」標籤、兩張圖都有指北針。
      - 圖：`docs/error-cases/2026-08-01-休達地圖-GPT-修正後複驗.png`、
        `docs/error-cases/2026-08-01-休達地圖-Gemini-畫成孤島.png`
- [ ] **待決策（使用者 2026-08-01 表示要再想）：短期拓撲規則要不要做**。
      提案是在 MAP ACCURACY RULES 加一條：畫小型飛地／半島／島嶼時，`structure`
      必須用文字寫明每一側是海還是陸（哪側為陸地邊界、哪側臨海、地峽在哪、
      往哪個方位伸出），而非只給座標讓模型自由發揮。
      - 效益：擋得掉「陸地邊界畫成海」「飛地畫成孤島」這種與自家文案打臉的錯。
      - 限制：不保證輪廓正確，治標。
      - 替代選項：跳過此步，把資源直接押到 (a) GIS 底圖／以圖生圖。
- [ ] AI 無法可靠自我複核地理正確性（沖之鳥島、休達兩次前科），單張仍需人工看過。
      上面 (a)(b)(c) 三個改進方向維持有效。

## 真實世界場景／物件的憑空杜撰風險（不限地圖）（2026-07-31 內測期間提出）

**現象**：上面「地圖／位置圖」的問題只涵蓋地理座標／方位這一類。使用者
提出更廣的疑慮：只要示意圖牽涉**真實世界存在的場景或物件**，AI 都有可能
杜撰出跟真實樣貌差異很大的假畫面——例如衛星空拍圖（真實地貌、建物配置
被憑空捏造）、真實高速公路示意圖（實際路型、交流道結構、路牌與現況不符）。
這類錯誤不像地圖座標偏移那麼容易靠「加座標規則」解決，因為問題核心是
「AI 對該場景/物件沒有可查證的視覺依據，純粹用訓練記憶亂猜外觀」。

**與既有待辦的關係**：地圖類已有 MAP ACCURACY RULES（見上）處理座標／方位，
但沒有處理「地貌、建物、路型等視覺細節是否符合現實」；這是同一大類問題
（AI 生成 vs 真實世界準確性）的更廣版本，兩者修法方向可能重疊
（尤其「GIS／官方底圖作為參考圖上傳」這條路線）。

**待辦**：
- [x] ~~評估 SYSTEM_PROMPT 層級通用規則~~ **已實作並實測驗收（2026-07-31，v2）**：
      `REAL_WORLD_FIDELITY_RULES`（消化）＋`REAL_WORLD_RENDERING_RULES`（生圖，
      app.js 逐字同步）。方向經使用者裁決修正過一次：v1「優先示意化」→
      **v2「盡量照真實場景畫；凡通用替身／重建／沒把握，必須明確標註『示意圖』」**
      （digest 把「示意圖」寫進 variable 並指定位置，生圖端不得省略）。
      寧邊核設施誘餌題兩輪實測：未偽造真實配置、v2 輪示意圖標籤 4/4 出現。
      詳見 `docs/error-cases/2026-07-31-視覺忠實度規則-合併驗收-分析.md`。
- [ ] 盤點常見稿型風險清單（衛星圖、道路示意圖、建物外觀、特定機型/艦艇/裝備），
      逐類評估——規則已上線，此項降為觀察性盤點
- [ ] 進階：比照地圖的「參考圖上傳」路線，讓使用者可附真實照片/衛星圖當
      基礎（與「以圖生圖」待辦重疊，可合併規劃）

## 混合版型模式（hybrid rendering）— 評估中（2026-07-23 列入）

**提案**：`docs/hybrid-rendering-proposal.md`（2026-07-15 建立，狀態：待決策）

**核心概念**：AI 負責視覺創意，APP 負責資訊正確性。
AI 只生成無文字背景／插圖；所有中文、數字、圖表、單位、資料來源由網頁依固定版型繪製，輸出 1280×720 PNG。現有完整 AI 生圖模式保留，依新聞類型二選一。

**為什麼重新浮上檯面**（2026-07-23 查證結論）：

1. 生圖模型對文字 prompt 裡的數字（px 或 % 都一樣）沒有量測能力，版面精準度天生是「憑感覺」——安全區留邊不一致、地圖位置偏移都是同一根因。
2. 學界／業界要像素級精準都是換通道：bounding box 條件、ControlNet、底圖參考——混合版型就是這條路線的 APP 實作版。
3. 已知痛點三連發都能一次解決：繁中錯字亂碼、安全區數字被畫進圖、地圖地理位置偏移（配 GIS 底圖）。

**已評估並放棄的外部工具**（2026-07-23，勿重複評估）：

| 工具 | 放棄原因 |
|---|---|
| LayoutDiffusion (CVPR 2023) | 研究程式碼、COCO 詞彙表、256×256 畫質、不會中文、要自架 GPU |
| ControlNet（canny/depth/seg） | 量產級但綁死自架 SD/Flux 生態，繁中渲染更差，Gemini/GPT API 不支援 |
| Bounding Boxes（GLIGEN 系） | 同上；且 APP 固定版型直接取代其功能 |
| Segmentation Map | 概念最有價值（GIS 分區圖鎖地理），但在現有 API 上有原生等價物 |

共同結論：這些工具的核心能力在 Gemini／GPT API 上的**原生等價物是參考圖輸入**——
「上傳 GIS 正確底圖＋指令不得改動地物」即 ControlNet 精神的 API 版，零新基礎設施。
採用路線：參考圖輸入（鎖地理／構圖）＋混合版型（APP 疊文字）。

**決策紀錄（2026-07-23）**：
- [x] **已批准實作**——% 版安全框實驗收手後（prompt-only 天花板實證，見 `docs/error-cases/2026-07-23-百分比安全框-分析.md`），使用者批准建構混合版型
- [x] 首個版型：三欄數據比較（market-three-column，美股三大指數型數據卡）
- [x] **階段0 技術原型完成**（分支 `feat/hybrid-rendering`，獨立頁面 `hybrid.html`/`hybrid.js`）：
  - AI 無文字背景（實測 Gemini 1 張：構圖讓開三面板區與標題區 ✅，但背景場景仍偷渲染一個微小 NYSE 招牌——「NO logos」防護非 100%，好在落在底部安全區且極淡）
  - SVG 真字型繁中／數字疊加，匯出後依然銳利（SVG→canvas 系統字型疑慮解除）
  - 重生背景不清除已填內容 ✅；1280×720 PNG 匯出 ✅（ffprobe 驗證）
  - 提案 §15 六項驗證全過
- [x] **一鍵成圖完成（2026-07-23，`0660c18`）**——記者外勤情境：手機貼新聞原文 → `/api/hybrid/digest`（strict schema 結構化消化）→ 自動填格 → 生背景 → 自動下載 PNG，全程無人值守。B-1 非財經新聞實測通過（質性欄位如「起飛基地：英國空軍基地」也能成卡）
  - 踩坑紀錄：Anthropic 結構化輸出不支援 minItems/maxItems（0/1 除外）；上游偶發失敗＋不合 schema 回傳是常態，重試圈須涵蓋「呼叫＋解析」全程
- [x] **併入 main 並整進主頁面（2026-07-28）**——`feat/hybrid-rendering` 合併回 main；主工具最上方分頁列新增「第三頁 · 混合版型」，介面改用主站 Tailwind 樣式但元素 id 與獨立頁完全一致，`hybrid.html` 保留可單獨開啟
  - 踩坑紀錄：`hybrid.js` 與 `app.js` 都有頂層 `state`／`IMAGE_BACKEND_URL`，同頁載入會直接 SyntaxError → hybrid.js 整段包 IIFE，只對外露出 `window.initHybrid()`，由 `switchPage(3)` 首次切頁時才初始化（重複呼叫自行擋掉，避免三欄欄位長出第二組）
  - 第三頁不走 Prompt 流程，故無 `outputMount-3`；共用的 Final Prompt 面板留在隱藏的前兩頁，切回去仍正常
- [ ] 與地圖修法方案的關係：地圖類直接跳過 prompt 修補、走底圖合成？還是兩者都做？（仍待決策）
- [ ] 下一步：使用者實際試用原型 → 決定是否進階段1（4–6 版型＋AI 自選版型＋文字溢位精細檢查；一鍵成圖已提前做掉結構化 digest）

**TVBS 官方安全框工具交叉驗證（2026-07-24 新增）**：

實測 TVBS 既有工具 `tools.tvbs.ai/tools/locked-frame/`（Studio Locked-Frame），拿到官方安全框標準與畫框做法，完整記錄於 `docs/examples/tvbs-safe-frame-locked-tool.md`。重點：

- 官方安全框（1920×1080 基準）：X=140,Y=109,W=1634,H=751 → 上10.09%／左7.29%／右7.60%／下20.37%，四邊不對稱
- 對照現有 `safeArea: {top:72,left:80,right:80,bottom:144}`（1280×720）：上下比例幾乎一致（10%／20%），左右可再放寬且不必對稱
- 該工具的畫框做法（DOM＋JS bundle 驗證，非猜測）：單一 `<canvas>`，先 `drawImage` 畫素材，再疊 `rgba(0,0,0,alpha)` 半透明黑，安全框範圍內 `clearRect` 挖空還原——全前端即時 render，不靠生成模型畫框，正好呼應下方「像素 vs %」結論

- [ ] 正式製作版型時把 `safeArea` 改成對照 TVBS 官方的非對稱比例（1280×720 換算：**上 73／左 93／右 97／下 147px**；原記的 129／261 是把上下比例誤乘寬度，已於 2026-07-30 更正）。實作時直接呼叫 `safe_area_spec.required_margins_px()`，不要手算
- [ ] 混合版型編輯器若要加「即時安全框預覽」，可直接沿用該工具驗證過的 canvas 疊遮罩＋挖空做法，不必另外設計 DOM 圖層方案

## 備忘：安全區「像素 vs %」討論結論（2026-07-23）

**問題**：安全區為何不用像素或 % 數字規定？

**事實整理**：

1. 安全區曾經用 %（exactly 15%，commit `6cf86db`），2026-07-22 `2b26df5` 全面拆除——實測根因是 **% 數字被 Gemini 當文字畫進圖**、safe zone 概念被畫成框線＋刷淡。
2. **像素從未出過事也從未被實測**——禁像素是預防性連坐，不是實證結論（`8b03a30` 移除「Resolution: 1K」只因 API 已寫死尺寸，與誤畫無關）。
3. 查證後確認「生圖模型量不出像素」**屬實**：
   - 文字 prompt 天生無法精準定位，學界因此發展 GLIGEN／InstanceDiffusion（bounding box 走獨立通道，不寫進文字）
   - OpenAI 官方指南只教定性語言（logo top-right），不教數字定位
   - Gemini prompt 裡寫「4K」不改變任何像素，尺寸靠 API 參數——數字在 prompt 裡只是語意詞彙
   - 對模型而言 288px 和 15% 同樣是「一串可能被畫出來的文字」，精準度同樣憑感覺
4. **結論**：走回數字路線無意義；要像素級精準只能換通道（bounding box／ControlNet／底圖參考）→ 即上方混合版型提案。

若日後有人再提議「安全區改回數字規定」，先讀這段。

**2026-07-23 更新：像素路線已實測蓋棺**。`feat/big-rework` 分支做了像素版對照實驗
（1920×1080 基準＋明文禁畫數字防護），4 張全數失敗：Gemini 2 張把 px 標籤畫進圖
（防護條款擋不住）、其餘 2 張（Gemini/GPT 各一）完全無視邊距、比純文字版更差。
完整紀錄：`docs/error-cases/2026-07-23-像素安全框-分析.md`。
「數字會被畫進圖」與「模型量不出像素」皆從假設升級為實證。

**2026-07-23 續：% 版實驗（feat/percent-safe-area，3 輪 15 張，調參中）**。
8–10%/18–22%＋approximately＋「禁畫數字」防護：數字入圖 0/14（穩定戰果）；
但版面精度打地鼠——過縮→貼邊→底部壓帶→括號入圖，每修一項冒一項，
prompt-only 收斂不到廣播級。完整紀錄 `docs/error-cases/2026-07-23-百分比安全框-分析.md`。
再度佐證混合版型為正解。

## 安全框：已改為後端程式化置框（2026-07-30 落地）

`safe_frame.py` 上線：滿版生成後由程式把整張圖等比置入 TVBS 安全框並補 blur-fill 背景，
輸出 1920×1080。安全框合格與否不再取決於模型，改由數學保證。開關是各 request 的
`safe_frame` 旗標（前端有按鈕、LINE Bot 預設開、`LINE_SAFE_FRAME=0` 可關）。

實測（原生 Gemini，端到端含消化，同一份新聞原文）：

| 引擎 | 送出比例 | 上 | 左 | 右 | 下 | 判定 |
|---|---|---|---|---|---|---|
| Gemini | 16:9（預設） | 14.54% | 16.09% | 16.35% | 20.74% | 四邊合格，左右偏寬 |
| Gemini | 21:9 | 13.80% | 8.18% | 8.49% | 24.35% | 四邊合格且零浪費 |
| GPT ×2 | 16:9 | 10.00% | 15.05% | 15.36–15.42% | 20.28% | 四邊合格，左右偏寬 |

（數字為 2026-07-30 修訂後的量測儀器所測；儀器改用高頻能量法，見下方待辦說明。）

**待辦**：

- [ ] **21:9 是否改為 `safe_frame` 模式的預設**：安全框本身是 2.175:1，所以 21:9 幾何上完全貼合、
      左右不再浪費；但唯一那張 21:9 樣本出現括號入圖（`[供給減少]`／`<減產約12%>`），
      同一份規則的 16:9 樣本則乾淨。依 R4 分析，括號入圖是 Gemini 隨機行為，n=1 無法歸因，
      需要各跑 4 張以上再定。GPT 側尚未測（OpenRouter 撞週用量上限）。
- [x] ~~**GPT 側的置框實測**~~ 已完成（2026-07-30）：改用另一把原生 OpenAI 金鑰繞過
      OpenRouter 配額，`gpt-image-2` 跑 2 張 16:9 端到端，四邊全合格。置框是純幾何運算，
      兩張數字幾乎相同——結果不再有變異數，這正是取代 prompt 路線的理由。
      仍未做：GPT 側**引導圖**對照（需 OpenRouter；優先度低，B 階段結論靠機制不靠引擎）。
- [x] ~~**內容忠實度**~~ 規則已落地（見下一節「內容忠實度規則」）；**生圖實測驗收仍未做**。
- [ ] **量測儀器已修訂（2026-07-30）**：`measure_safe_area.py` 原用邊緣強度（FIND_EDGES），
      會把置框後 blur-fill 背景的模糊亮斑判成內容（GPT 置框圖上緣誤報 0.09%）。改用高頻能量
      （原圖與輕微模糊之差）後誤判消失，且更貼合驗收原意（背景可滿版、關鍵元素不得越界）。
      歷史案例回測判定全部不變。已有回歸測試；日後若再調門檻，先跑
      `tests/test_safe_area_spec.py` 的量測段。
- [ ] 決定 `cover` 模式（填滿安全框、會裁掉約 9% 上下）要不要開給使用者選；目前只有 `fit`。
- [ ] `hybrid.js` 的 `TEMPLATE.safeArea` 仍是對稱 80/80 草案，尚未改成官方非對稱值。

**不要再做的事**：安全框寫進 prompt 的路線已四輪蓋棺（像素／百分比／純文字／參考圖與遮罩），
底部安全區累計 0 次合格。第五輪實驗（2026-07-30）另外釘死兩件事：
`gpt-image-2` 的遮罩是軟遮罩、整張重繪，**API 層給不了硬保證**（實測留白區被完全重畫，
且該模型不支援 `input_fidelity`）；引導圖只影響構圖排列、不影響比例。

## 安全框四周背景：改用 backdrop 襯底（2026-07-30 決策）

**問題**：原本的 blur-fill（把原圖放大重度模糊當底）在大螢幕上被讀成「畫面出錯」——
重度模糊把文字糊成鬼影、又與中央清晰內容形成硬邊。

**做法對照**（用實際生成圖跑，未呼叫付費 API，四種並排）：

| 做法 | 觀感 | 成本 |
|---|---|---|
| blur（舊） | 上緣黃色糊斑、下緣紅褐汙痕，像壞掉 | 免費 |
| clamp 邊緣延伸 | 最無縫，幾乎看不出內容邊界；外圈無新細節 | 免費 |
| 生成背景＋clamp 混合 | 外圈有真實質感，接縫由 clamp 保證無縫 | 每張多一次生圖 |
| **backdrop 襯底（採用）** | 不假裝無縫：邊緣色漸層襯底＋內容加投影，像刻意設計的卡片 | 免費 |

**決策**：預設 `backdrop`（`safe_frame.DEFAULT_BACKGROUND`），保留 `clamp` 與 `blur` 可選，
`SAFE_FRAME_BACKGROUND` 環境變數可切換。`solid` 已被 backdrop 取代並移除。

**生成背景那條的評估結果**：技術上可行且可落地（用現有 GEMINI_API_KEY，參考圖對齊風格，
B 階段已證實參考圖不會滲色），但每張多一次付費呼叫與約 30 秒延遲，且生成背景可能在留白區
長出銳利紋理——`measure_safe_area.py` 會把它判成內容而誤報 fail（置框本身仍由單元測試保證
正確）。若日後要開這個選項，這件誤報要一併寫進驗收說明。

## 安全框上／左／右感覺留白偏多，底部剛好（2026-07-30 查證）

**使用者觀察**：backdrop 上線後看實圖，上／左／右留白感覺抓太多，底部剛好。

**查證：置放框的幾何完全正確，不是 safe_frame.py 的 bug。**
16:9 來源用 `plan_placement()` 算出的置放框，逐邊比對官方需求：

| 邊 | 官方需求 | 幾何置放框實際值 | 差距 |
|---|---|---|---|
| 上 | 10.09% | 10.09% | 0（完全命中，height 是綁定軸） |
| 下 | 20.37% | 20.37% | 0（完全命中） |
| 左 | 7.29% | 15.05% | +7.76pp（16:9 塞 2.176:1 安全區的餘裕，已知問題） |
| 右 | 7.60% | 15.42% | +7.82pp（同上） |

上下的幾何**分毫不差**。那使用者感覺到的「多」，是量到的**內容本身**留白，不是置放框：

| 邊 | 官方需求 | 置放框 | 實測內容 | 內容自帶 |
|---|---|---|---|---|
| 上 | 10.09% | 10.09% | 14.54% | +4.45pp |
| 左 | 7.29% | 15.05% | 19.06% | +4.27pp |
| 右 | 7.60% | 15.42% | 19.38% | +4.28pp |
| 下 | 20.37% | 20.37% | 25.28% | +4.91pp |

**四邊的「內容自帶」留白幾乎等量（4.27–4.91pp）**，這正好對應
`news_prompt.py` `FULL_BLEED_RULES` 第三句：「Keep the breathing space roughly even
on all four sides」——模型確實照做了，這不是模型亂加，是**我們自己要求的**。
直接量測未置框的原始生成圖也印證：內容自身邊界內縮約 6–7%（`2K-content-gemini.jpg`
實測 上6.58／左6.18／右6.18／下7.23%），乘上內容在最終畫布的縮放比例（約69.5%）
換算回來正好等於上表的「內容自帶」欄。

**為什麼底部「感覺剛好」、上／左／右「感覺多」**：不是底部真的沒有多留，
底部實際多留的量（4.91pp）反而是四邊裡最大的。差別在於**分母**——底部官方需求
本來就深達 20.37%，多 4.91pp 只是「深上加深」，比例上不明顯；上／左／右官方需求
只有 10.09／7.29／7.60%（本來就薄），同樣量級的內容留白疊上去，相對值直接翻倍甚至
兩倍以上，視覺上就特別扎眼。這是「同樣的絕對留白，除以不同的基準」造成的錯覺，
不是四邊處理方式不一致。

**根因與可行的修法**：「breathing space 避免元素被切到」這句規則，寫的時候是為了
防呆——但現行 production 管線（純 prompt 滿版＋`safe_frame.py` 用 FIT 縮放置入安全區）
**從來不會裁切生成圖**，FIT 只是整張等比縮小，不管模型畫到多貼邊，內容都會被完整保留。
也就是說，這句防呆規則保護的風險在目前管線裡根本不存在——它只對「遮罩硬裁切」
（B 階段測過、已確認失敗放棄）或未來若改用 `cover` 模式（會裁切）才有意義。
在純 FIT 管線下，這句規則只有壞處：讓模型多留約 6-7% 用不到的呼吸空間。

**下一步（尚未動手，需要生圖驗證才能信）**：把 `CANVAS_FULL_BLEED_LINE` 與
`FULL_BLEED_RULES` 的呼吸空間語句改弱／拿掉，理論上能讓上／左／右／下都往官方
需求值靠近（尤其上緣可望從 14.54% 降到接近 10%）。**依前五輪安全框實驗的教訓，
文字調整的效果必須用真實生成圖驗證才能相信，不能只憑推理判定「應該有效」**——
下次要動這句時，先各跑 4 張以上做前後對照，同 digest 才乾淨。左右的餘裕另有結構性
成因（16:9 vs 2.176:1 長寬比），拿掉呼吸空間也解不掉，仍要靠 21:9 或 cover 模式處理
（見上方「21:9 是否轉預設」待辦）。

## 21:9 驗證：n=4 樣本結果（2026-07-30 續，使用者選擇推進驗證）

補測 2 張湊到 4 張門檻（走 production 完整管線：`generate_news_image`，
`DIGEST_BACKEND=gemini`＋`IMAGE_BACKEND=native`，皆繞過 OpenRouter；同一份
新聞原文，`safe_frame=True`＋`aspect_ratio=21:9`）。

**幾何：4/4 全數合格，且左右留白數字高度穩定**：

| 樣本 | 上 | 左 | 右 | 下 |
|---|---|---|---|---|
| #1 | 14.63% | 8.39% | 8.70% | 25.00% |
| #2 | 17.22% | 7.24% | 7.55% | 22.87% |
| #3 | 12.78% | 7.29% | 7.60% | 23.06% |
| #4 | 12.69% | 7.24% | 7.50% | 22.96% |

左右四張都落在官方需求（7.29/7.60%）附近 ±1pp 內，證實幾何預測穩固可靠，
21:9 確實解決了左右留白過寬的結構性問題。

**內容品質：兩種獨立瑕疵，發生率不低**：

- **角括號滲入畫面 2/4**（樣本 #2、#3，如 `<3.8美元>`／`<12%>`）。與長寬比無關，
  是三輪前百分比實驗就記錄過的 Gemini 既有隨機行為（見既有 error-cases 分析）。
- **新發現：捏造來源機構名＋文字亂碼 1/4**（樣本 #4，底部出現
  「資料來源：金髒賣貨機種新聞研告」——這幾個字本身不是正常繁中詞彙，且
  新聞原文完全沒提到任何來源機構）。**這是今天稍早才加的內容忠實度規則
  （`news_prompt.py` FINAL OUTPUT RULE 明文禁止生圖階段自行加來源行）第一次
  被實測突破**——規則有寫不等於模型會遵守，這正是加規則時就記在 TODO 的提醒。
  「文字亂碼」本身也有既有前科（`2026-07-22-繁中標題亂碼-關稅資料圖-OpenRouter.png`，
  但那次是走 OpenRouter；這次是原生 Gemini API，管道不同，需要另外留意是否為
  同一根因或獨立問題）。

**四張合計：2/4 出現至少一種內容瑕疵**（括號或捏造來源擇一），無樣本兩種瑕疵疊加。

**待裁決**：幾何面 21:9 已經驗證到可信賴；內容面兩種瑕疵發生率合計約 50%，
不算低。是否切換正式預設、要不要先處理內容瑕疵再切、或接受現況先切換，
已交由使用者決定，見對話紀錄。

**裁決結果（同日）**：使用者選擇接受現狀、直接切換正式預設。已實作於
`main.py`：`NewsImageGenerateRequest.aspect_ratio` 改成 `str | None`（預設
`None`），新增 `resolve_aspect_ratio()`——`safe_frame=True` 且未明確指定時
自動用 21:9，`safe_frame=False` 維持 16:9，呼叫端明確指定時一律尊重覆寫。
只改 `generate_news_image()`（LINE Bot＋WorkCord `/api/news-image/generate`
共用這支函式），**沒有動網頁版**——網頁版走 `app.js` 自己組 prompt 直打
`/api/images/generate` 的另一條路徑，今天沒有驗證過，不在這次拍板範圍內。

3 個新測試釘住這個邏輯（`tests/test_news_image_endpoint.py`）：
`safe_frame=True` 且未指定時必須解析成 21:9、`safe_frame=False` 維持 16:9、
明確指定時必須尊重覆寫。全套 137 測試與 Ruff 皆綠。

**待辦（新）**：括號滲入與捏造來源這兩種內容瑕疵仍未修，且捏造來源那條
是內容忠實度規則上線後第一次被實測突破，需要獨立追蹤（見上方段落的分析）。
切換 21:9 預設不等於這兩個問題解決了，只是決定先接受現狀上線。


## 左右留白過寬：裁切路線失敗，改走長寬比路線（2026-07-30，重要教訓）

**使用者用 TVBS 官方對框工具實測比對**，指出左右安全框抓太寬，應該減半。查證確認：
FIT 模式下 16:9 內容塞進 2.176:1 安全區，左右餘裕（~15%）是官方需求（7.29/7.60%）的
整整兩倍，是結構性的長寬比不合，不是隨機誤差。

### 第一次嘗試：裁切減半（已失敗、已撤銷，記錄下來避免重踩）

在 `safe_frame.py` 加了 `mode: float`（0.0=FIT 到 1.0=COVER 線性內插，改把
`FIT`/`COVER` 從字串改成數字常數），把預設值設成 0.5——數學上精確地讓左右餘裕減半
（有推導：內插在「餘裕像素數」上也是線性的），且用兩張既有樣本目視驗證看似安全
（沒有切到標題或卡片）。

**上線後使用者用真實生成圖實測，回報底部 `<蓋章>` 橫幅被裁掉。** 這暴露了驗證方法的
根本問題：模型每次生成留的「呼吸空間」份量不固定（不同素材、不同次生成都不一樣），
兩張樣本的目視檢查只能證明「這兩張沒事」，不能證明「所有情況都沒事」——裁的量
（mode=0.5 約裁掉內容自身高度的 5%）雖然通常小於模型自留的呼吸空間（通常 6-7%），
但那只是平均值，個別樣本可能留得更少，一旦更少就會裁到真正的內容。

**已撤銷**：`DEFAULT_CROP_RATIO` 改回 `FIT`（0.0，零裁切，完全安全）。`mode` 參數
本身保留（連續內插的機制是對的、程式碼有正確性測試釘住），但**不能拿來當預設**，
只能是刻意想裁切的呼叫端自己選用。

**過程中還抓到並修好一個獨立的程式錯誤**：一開始把超出「畫布」邊界的部分裁掉，
但正確的裁法應該是裁到「安全區」邊界——畫布比安全區大，只裁到畫布邊界會讓超出
安全區、還沒超出畫布的那圈直接侵蝕官方留白（實測會把上緣從精準的 10.09% 吃到
只剩 2.3%）。已修正並補上回歸測試 `test_cropping_mode_never_cuts_into_the_official_zone_on_any_axis`
——這條測試第一版用純色圖＋clamp 背景寫，因為 clamp 會把邊緣色直接延伸、跟純色
內容同色，測試誤判通過沒抓到 bug；改用 backdrop 背景（會把邊緣色壓暗）才真正抓到。
兩次「以為驗證過了、其實沒有」的教訓都記在這裡。

### 第二次嘗試：改生成長寬比（使用者提議，驗證中，方向正確）

使用者提議：改讓 AI 生成「拉長滿版」的長寬比，而不是 16:9，這樣 FIT 模式（零裁切）
本身餘裕就會變小，不需要裁切也不會有裁到內容的風險。查證這是比裁切更好的結構性解法：

若生成長寬比精準等於安全區本身的 2.176:1，FIT 模式零裁切下四邊會精準命中官方需求，
完全零浪費。但生圖 API（Gemini interactions）**只接受固定的長寬比清單**
（`1:1`/`2:3`/`3:2`/`3:4`/`4:3`/`4:5`/`5:4`/`9:16`/`16:9`/`21:9`/`1:8`/`8:1`…），
沒有自訂精準比例的選項（已實測 `2176:1000`／`2.176:1`／`87:40` 皆被 400 拒絕）。
清單裡 **21:9（2.333）是離 2.176 最近的**，也是之前百分比實驗已經驗證過能正常生成的值。

實測（原生 Gemini，走完整生產管線，safe_frame=True＋FIT 零裁切）2 張：

| 樣本 | 上 | 左 | 右 | 下 | 判定 |
|---|---|---|---|---|---|
| 21:9 #1 | 14.63% | 8.39% | 8.70% | 25.00% | 四邊合格，左右幾乎精準命中官方需求 |
| 21:9 #2 | 17.22% | 7.24% | 7.55% | 22.87% | 四邊合格，左右幾乎精準命中官方需求 |

左右從 16:9+FIT 的 ~15%（超出需求一倍）降到 ~7.2-8.7%（官方需求 7.29/7.60%），
且全程零裁切、零額外風險——符合理論預測，比裁切路線安全得多。

**但發現一個獨立問題**：樣本2 出現角括號滲入畫面（`<3.8美元 每磅>`／`<減產約12%>`等）。
**使用者確認這是 Gemini 既有的獨立隨機行為**（三輪前百分比實驗 R2/R4 就記錄過，
與長寬比無關），**列入待辦、之後處理，不擋這輪的長寬比驗證**。連同先前那張 21:9
樣本，累計 3 個樣本中 2 個中招——尚未到 R4 分析要求的 n=4 門檻，且問題與這次的
主題（左右留白）是兩件事，不要混著判斷。

**待辦**：

- [ ] 21:9 補到 4 張以上樣本，同時觀察左右留白數字是否穩定、以及括號滲入的發生率
      （這是獨立追蹤項目，不因為這次驗證是為了留白問題就跳過）
- [ ] 若 21:9 驗證通過，評估是否把 `safe_frame=True` 模式的預設生成長寬比從 16:9
      改成 21:9（需與「21:9 是否轉 safe_frame 模式預設」那條既有待辦合併判斷）
- [ ] 括號滲入問題本身needs its own fix track（不屬於本節，見既有 error-cases 分析）

## 解析度：剪接台放大會模糊（2026-07-30 查證，使用者決定暫不改）

**使用者情境**：編輯把 AICG 放進新聞帶時會手動調位置、可能放大。

**查證結果**：會模糊，**但瓶頸不在置框，在生成解析度**。GPT 寫死 `1280x720`
（`main.py` `generate_gpt_image`），Gemini 預設 `1K`（實測回 1376×768）。置框後內容區在
1920×1080 畫布佔 1335×751，GPT 那條出廠就已被放大 1.04 倍。在 1080p timeline 上放大到
125% 就超過 1.15× 原生、開始看得出軟化；150% 是 1.56×。

**關鍵陷阱**：單純把置框畫布拉到 4K **沒有用**——實算兩排數字完全一樣，因為真實細節被
生成端的 1280px 鎖死，置框放大只是插值。要改必須改生成端。

**若日後要改，三處一起**：Gemini 預設 1K→2K（實測回 2752×1536，線性細節 2 倍）；
GPT 解掉 `size="1280x720"`（`gpt-image-2` 支援任意 16 的倍數，標準上限 2560×1440）；
置框畫布改 3840×2160，讓 2K 內容變成略微縮小而非放大。已用同一張圖做過受控對照
（2K→3840 明顯較利），代價是每張更貴更慢。

**順帶查到的硬限制**：Gemini interactions API **只能輸出 JPEG**，`image/png` 直接回 400
（`Supported values: 'image/jpeg'`），所以那條天生帶一層 JPEG 壓縮，拿不到無損。
GPT 原生那條可以出 PNG（現在就是）。

**待辦**：
- [ ] 交付形式：使用者決定**只給置框成品**，不另給未置框滿版原圖。要注意的副作用是
      編輯若放大成品，連襯底與留白一起放大，安全框比例會跑掉——安全框的目的被抵銷。
      若未來實際發生，再回頭討論是否加交付滿版原圖。

## 內容忠實度規則（2026-07-30 落地）

**規則（使用者拍板）**：一律嚴格照使用者貼的原始文稿。只有原文本身出現「幫我補充」
之類的指示時，才可以在**消化階段**增補；**生圖階段完全不得自行添加內容**。

起因：GPT 生的圖自行畫出來源沒有的完整季線數值（2.10／2.28／…／3.80）與
「資料來源 ICE／Trading Economics／USDA／ICO」，來源只提到均價 3.8 美元、
減產 12%、落後約 2 個月。新聞產品不得出現模型發明的數據與來源機構。

實作分兩層，職責不同：

- 消化階段 `main.py` 的 `CONTENT_FIDELITY_RULES`：只能用原文有的；禁止虛構數列／
  座標軸刻度／統計值／來源機構／分析師名／「更新於」日期；稿件內容少就少寫，
  不得補滿；不得把「約／可能／預估」升級成斷言。唯一例外是原文明確要求補充。
  刻意排在密度規則之前——簡化模式可以覆寫長度與格式，但不得覆寫忠實度。
- 生圖階段 `news_prompt.py`／`app.js` 的 FINAL OUTPUT RULE：宣告模型只是渲染器；
  沒給數列就把圖表畫成無標籤的示意線條；不得加來源行／機構名／logo／浮水印／時間戳；
  「版面看起來空」不是添加內容的理由（實測病灶正是這個）。

`PROMPT_VERSION` 已遞增為 `v2-2026-07-30`（外部整合方靠這個追規則版本；
2026-07-31 隨視覺忠實度規則族再遞增為 `v3-2026-07-31`）。
15 個測試涵蓋兩層規則與雙源逐字一致：`tests/test_content_fidelity.py`。

**2026-07-31 註記：本規則收緊（項目 11）本輪刻意延後**——當日新增的所有規則
（視覺忠實度族、地圖、配色、指令）一律以新區塊追加，`CONTENT_FIDELITY_RULES`
一字未改（`tests/test_map_accuracy.py` 有測試機械性釘住），待使用者日後單獨
拉出檢視。當日實測觀察到的違規樣本歸入此追蹤：GPT 杜撰「國際社會持續密切
監控中」（原文只說美日韓）、GPT「周邊設施擴建中」推衍添加、Gemini 亂改複製句
「美日韓監控相關動態聲明」——皆輕微改寫/添加型，非虛構數據型。

**2026-08-01 追加證據（休達案例複驗圖）**：GPT 在地圖上補了原文沒有的地名
「伊倫昂代」（Irún／Hendaye，真實的法西邊境關口但原文未提），並在休達與該處
之間畫了一條黃色虛線，看起來像在描述一條原文沒有的移民路線——消化端 structure
明確寫了「不是路線」，生圖端仍然畫了。這是**地圖情境下的識別性細節杜撰**，比
先前的樣本更具誤導性（讀者會以為那是報導內容），收緊時應一併涵蓋。
圖：`docs/error-cases/2026-08-01-休達地圖-GPT-修正後複驗.png`

**待辦**：

- [x] ~~實測驗證~~（2026-07-31 隨合併驗收完成：9 輪 18 張中虛構數據 0 件、
      虛構來源行 0 件——原始病灶未再現；輕微文字添加 3 件見上方註記）
- [ ] 收緊方向（延後單獨檢視）：輕微改寫/添加型違規的修法——依既有教訓
      不是再加強措辭，而是改走**驗收攔截**：比對生成圖中的文字/數字與
      variable 欄位，發現多出來的就重生成。

## 生成請求落檔／線上回查（2026-08-01 落地）

**起因**：休達案例回報時，使用者要求「從後台回撈 PROMPT 看看哪一步驟有錯」，
才發現後端**只在失敗路徑 print，成功的請求完全不留痕跡**，連原始新聞文字都沒帶
上。當時只能請使用者把原文再貼一次；原文若已遺失，案例就查不下去。

- [x] `request_log.py`：成功的生成寫成 JSONL（輸入、消化結果、最終 prompt）
- [x] 三條路徑都接上——`generate_news_image()`（LINE／WorkCord／API）記完整一筆；
      `/api/generate`（網頁版在前端自組生圖 prompt，後端看不到最終 prompt）只記到
      消化為止，仍足以重跑重現。用 contextvar 擋掉內層重複記錄
- [x] LINE 成圖另記一筆 `request_id` → 檔名對照。使用者回報時傳來的是 LINE 下載
      的圖、檔名已被換掉，只靠時間戳在同一秒有多筆請求時會對不準
- [x] `scripts/find_generation.py` 回查入口（依時間／關鍵字／request_id／成圖檔名）
- [x] 日期切檔、預設保留 14 天（新聞原文是使用者內容，不該無限期堆著），
      `logs/` 已進 .gitignore；寫檔失敗一律吞掉不波及請求，有測試釘住

**限制（與 input_filter 的狀態同性質）**：寫本機磁碟，Render 之類的平台每次重新
部署都會清空，也不跨實例共享。要長期保存需改推外部儲存——與下方「LINE Bot
永久化：改雲端部署」同一批處理。

## LINE Bot 永久化：改雲端部署（2026-07-29 決議，demo 後執行）

原型跑通後暴露兩個痛點，常被混為一談：

1. **重開網址就變** — cloudflared 快速隧道每次隨機給網址，得回 LINE Console 重貼 webhook
2. **依賴筆電開著** — 後端跑本機，闔蓋／斷網就死；這才是 5–15 人內測的真正阻礙

原訂用 cloudflared **具名隧道**取得固定網址，實查後否決：帳號
（Moonriver0523@gmail.com）Cloudflare 上**沒有任何網域**，而具名隧道要綁 DNS、
需要一個託管在 Cloudflare 的 zone；且它只解決問題 1，仍要筆電 24 小時開機。

**改採 Render 免費層**：平台本身配固定子網域（`xxx.onrender.com`），
不必買網域，並一併解決問題 2。免費層閒置會休眠、冷啟動 30–60 秒；
首次 webhook 可能逾時但 LINE 會自動重送，且生圖本來就要 30–120 秒，影響有限。
要免休眠約 US$7/月。

- [ ] **先確認公司政策**：是否允許 `OPENROUTER_API_KEY`、LINE token 放外部雲端平台。
      本 repo 含電視台內部 prompt 資產（已轉 Private），不逕行決定；
      可沿用 TVBS-Aigent-Fable 已訂的「測試期雲端、正式期搬公司內網 NAS」原則。
- [ ] 部署到 Render：Procfile／start command、環境變數搬移、確認 `static/generated/`
      在免費層的檔案系統是暫存性質（重啟即消失，但圖只需存活到 LINE 抓取完畢）
- [ ] 換 LINE webhook URL 為 Render 固定網址（最後一次重貼）
- [ ] 部署後把 `dev-line.sh` 定位改為「純本機開發用」，正式流量走雲端

**相關**：`docs/line-bot-setup.md`「永久化方案」段落有同一份決議紀錄。

## dev-line.sh 的 --reload 會被 git commit 弄死（2026-07-29 實際發生）

**現象**：demo 前約 100 分鐘，LINE Bot 後端無預警停止（uvicorn exit 144），
`/line/webhook` 完全無回應，但 cloudflared 隧道仍活著——**從外面看不出來，
只有實際發訊息才會發現失效**。

**根因**：`dev-line.sh` 啟動 uvicorn 時帶 `--reload`，其 WatchFiles 監看整個專案目錄；
執行 `git commit` 時 pre-commit（gitleaks）會把未暫存檔案 stash 到 ~/.cache 再 restore，
造成短時間內大量檔案增刪，reloader 因此崩潰並終止整個 uvicorn 行程。
log 尾端可見 `Stopping reloader process`。

**當下處置**：改用不帶 `--reload` 的方式重啟（隧道未受影響，webhook URL 得以保留）。

**待辦（2026-07-29 已修，見下方查證與結果）**：

- [x] `dev-line.sh` 預設**不要**帶 `--reload`；要熱重載時另用參數開啟
      （`./dev-line.sh --watch`），避免正式測試時被 commit 打斷
- [x] ~~加 `--reload-exclude` 排除 `.git/**`、`static/**`、`.omc/**`~~
      **查證後發現這條無效、已移除**：watchfiles 的 `DefaultFilter` 本來就
      內建忽略 `.git`（`ignore_dirs` 含 `.git`），而 uvicorn `--reload` 預設
      只監看 `*.py`，`static/`、`.omc/` 裡本來就沒有會觸發重載的檔案——
      三個排除規則全是空氣，並未真正防到當初的崩潰。真正病灶是
      pre-commit 把**一般 `.py` 檔本身**（例如 `main.py`）stash 再 restore，
      短時間內兩次寫入同一個被監看的檔案，沒有乾淨的 exclude 能擋這個。
      `--watch` 模式仍保留這個殘餘風險，靠下面的健康檢查兜底。
- [x] 加健康檢查：每 15 秒 ping `/openapi.json`，連續 2 次失敗（～30 秒）
      自動重啟並印警告；`cleanup()` 改用「照 port 找目前 PID」而非
      `$BACKEND_PID`（看門狗重啟過的話那個變數在主 shell 是舊的，
      靠 port 查詢才不會殺錯／殺不到）。**已實測**：手動 kill -9 後端，
      8 秒內偵測到並自動重啟成功，隧道網址不受影響、cleanup 也正確
      收乾淨（8787 port 完全釋放）。
- [ ] 雲端部署後這個問題自然消失（平台自帶 process 監管與重啟），
      見「LINE Bot 永久化：改雲端部署」——**已跟使用者確認過**：不一定
      要雲端部署，買網域＋cloudflared 具名隧道也能解決「網址重開會變」
      （但不解決「依賴筆電開著」），使用者今天先選擇處理這項而非雲端／
      具名隧道，兩案都還沒動

**教訓**：本機跑的服務「沒有人在看」，失效是靜默的。任何 demo 前的既定流程
（commit、安裝套件、改設定）都可能連帶弄停它，上台前必須實際發一則訊息驗收，
而不是只看 process 還在不在。

## WorkCord（Roomcord）Agent 整合：端到端測試已成功（2026-07-29）

`/api/news-image/generate` 給 WorkCord 的「我的 Assistant」（A2A 代理，
帳號為許岱軒）串接，經過一輪來回：

1. 指出對方範例的 `type_label: "新聞資訊圖表"` 不在系統四選一 enum、
   會跟 JSON schema 衝突造成靜默失準 → 對方已修正為省略該欄位或用
   `"自動判斷"`，並更新了他們的代理指令
2. **端到端測試已成功**（對方回報）：測試端點可用、`X-API-Key` 驗證
   可用、圖片生成已成功測過

**事故與待辦**：測試期間 `NEWS_IMAGE_API_KEY` 的實際值被誤貼進 WorkCord
聊天室一則訊息（已刪除，但對方當下已讀到並用它完成測試，故該值視為
「已交付」）。使用者決定當下不重新產生，先信賴繼續；但**正式上線或換成
穩定網址／固定 domain 前，必須重新產生一組新 key**（`openssl rand -hex 32`）
並用安全管道（非聊天室）交給對方——這點對方自己也主動提出同樣建議。

- [ ] 上線前：重新產生 `NEWS_IMAGE_API_KEY`，更新 `.env`，安全管道通知對方換新值
- [ ] 上線前：處理「LINE Bot 永久化」待辦裡的穩定網址問題（此端點與
      LINE Bot 共用同一個 cloudflared 隧道，網址不穩定的風險兩邊共通）

## 待建立：「以圖生圖」功能（2026-07-31 內測期間提出）

**現象**：目前 LINE Bot／網頁版流程只接受新聞文字輸入，AI 消化文字生成 Prompt
後再生圖。使用者內測時提出：可能會直接貼一張圖片（例如既有素材、參考畫面）
而非純文字，現有流程無法處理這種輸入。

**待評估**：
- [ ] LINE 端：`line_bot.py` 目前 webhook handler 只處理 text message event，
      需新增 image message event 的處理分支（LINE 圖片訊息走 content API
      下載原圖）
- [ ] 生圖端：`generate_via_openrouter` / `generate_gemini_image` 目前只送
      文字 prompt，需確認 OpenRouter 統一圖片端點與 Gemini 原生端點是否支援
      「參考圖 + 文字指令」的 image-to-image／inpainting 輸入格式
      （`docs/hybrid-rendering-proposal.md`、`docs/error-cases/2026-07-30-參考圖引導與遮罩-分析.md`
      已有參考圖引導的相關探索，可先查是否已有可沿用的機制）
- [ ] 需求釐清：使用者貼圖後預期行為為何——把貼圖當「風格／構圖參考圖」，
      還是直接要求「修改這張圖」（例如換色、加字卡）？兩者對應到不同的
      prompt 組裝方式

**情境補充（2026-07-31 續）**：使用者貼一張參考圖或真實照片＋一段文字，
希望：(a) 參考該圖片的「風格」來生成新聞圖，或 (b) 以真實照片為基礎
（例如當事人、現場照）生成，而非純文字描述憑空生圖。這跟上面「修改這張圖」
不同——輸入是圖＋文字兩者並存，圖片角色是「風格參照／內容基礎」而非
「待修改的目標」，對應到的 prompt 組裝與 image-to-image 呼叫方式也不同，
待與上面的需求釐清一併展開成完整規格。

## 待建立：LINE 編輯版（role=編輯，安全框不同於記者）（2026-07-31 內測期間提出）

**2026-08-14 已落地**：同一個帳號、訊息內指令切換。編輯另用對位框
（1920×1080 內緣 X=90 Y=70 W=1748 H=924，量自 TVBS對位框.png）、
生圖固定 GPT、16:9 滿版後強制壓框。記者維持 21:9＋官方 Locked-Frame。

- [x] 同一個官方帳號用指令切換（開頭「編輯」或 `指示: 編輯`）
- [x] `line_bot.py` 的 role 不再寫死
- [x] 編輯專用對位框＋GPT＋16:9 置框
- [x] 編輯框改紅框內最大 16:9（142,70,1643,924）＋FIT，避免 COVER 裁上下
- [ ] 對位框若官方工具改座標，需重新量 TVBS對位框.png 更新 `PROFILES["編輯"]`

## 待建立：Webhook 訊息過濾器（防垃圾訊息／非正常使用）（2026-07-31 內測期間提出）

**現況**：`line_bot.py` 的 webhook handler 收到任何文字訊息就直接送進
digest+生圖流程，沒有任何前置過濾——不管是垃圾訊息、惡意灌訊息、還是
明顯不是新聞文字的內容都會照樣觸發一次完整的 AI 呼叫（生圖成本、
OpenRouter 配額都會被吃掉）。這個問題不限 LINE，未來其他行動端／
webhook 型入口（例如 WorkCord A2A 那類）都會有一樣的暴露面。

**已完成（2026-07-31，`input_filter.py`，隨 merge 進 main）**：
- [x] 前置過濾器：長度 15–5000 字、資訊性字元比／亂碼初判、系統導向注入樣式、
      dedup 60 秒（刻意不更長——重貼同文字是 LINE 目前唯一重骰方式）、
      頻率限制 30 秒間隔＋10 分鐘 5 次
- [x] 分層：共用模組 `input_filter.py`；`line_bot.py` 在 ack 前做完整檢查
      （含頻率限制，client_id=聊天室），`generate_news_image` 內層做無狀態
      內容檢查當縱深防禦（`client_id` 為選擇加入，WorkCord 等入口可傳）
- [x] 回覆設計：被擋訊息用同一個免費 replyToken 回覆具體原因後結束，
      不進 ack+push 流程、不耗 push 額度；API 端回 400＋原因
- [x] 與「訊息內帶指令」的分界：只擋針對系統 prompt 的攻擊，編務指令
      （「指示: …」）一律放行，雙向測試釘住

**殘留限制（列入未來）**：
- [ ] 頻率限制狀態是 process-local dict——重啟／重新部署即清空、不跨
      uvicorn worker 或多實例。公開對外（雲端部署後）前需改 Redis／Postgres

## 待建立：LINE 訊息內帶指令（風格／排版）與「已消化完稿」直出模式（2026-07-31 內測期間提出）

**現象一：訊息內帶指令**——使用者提出，未來 LINE 訊息可能不只是純新聞文字，
還會夾帶指令，例如指定風格（如「用手繪風」）、指定排版（如「標題放左邊」）。
目前 `line_bot.py` 把整段輸入原封不動當「新聞文字」送進 digest，沒有
指令解析層，這類夾帶指令會被當成新聞內容的一部分被 AI 誤讀或忽略。

**現象二：已消化完稿直出**——使用者可能會直接貼一份**已經人工消化完的
CG 文字底稿**（每一個字都要原封不動出現在圖上），這種情況不需要、也不該
再讓 AI 重新消化一次文字內容（消化可能改字、換句，導致跟使用者要的
底稿不一致）——AI 只需要負責排版與生圖，文字內容必須逐字保留。

**這兩者都指向同一個缺口**：目前的 pipeline 假設輸入永遠是「未消化的
新聞素材」，缺乏區分「純素材」vs「帶指令」vs「已完稿逐字排版」三種輸入
模式的機制。

**已完成（2026-07-31，`USER_INSTRUCTION_RULES`，隨 merge 進 main）**：
- [x] 輸入格式：**自然語言偵測為主＋可選「指示:」「指令:」標記**（使用者拍板，
      兼顧手機打字）。digest 每次消化前先把輸入拆成「新聞素材」與「指令」，
      指令要照做且永不入圖；區塊固定放組裝最後、明文壓過 SIMPLIFIED MODE
      OVERRIDE（LINE 預設 simplified，衝突每次都會發生，位置＋明文雙保險）
- [x] 逐字模式（VERBATIM MODE）：偵測「完全依照文字」「不要刪減」「這是完稿」
      等語句即逐字重現進 variable，不摘要不增刪；實測台股文本兩引擎
      一字不差、simplified 未砍行——土法附註已升級為常設機制
- [ ] 風格/排版指令與網頁版手動模板參數共用同一套解析結果（未做；目前
      指令由 digest 端自然語言處理，與手動模板是兩條路）

**實測更新（2026-07-31 續，內測期間驗證）**：現象二目前有一個免改動的
土法煉鋼解法已驗證有效——使用者在貼的文字後面附註一行「指示: 完全依照
文字 不要刪減 不要添加文字或數字」，AI（digest 端）會遵照這個自然語言
指示，生圖結果沒有刪字也沒有加字。代表現有 digest 的 system prompt
本來就有能力遵守「逐字保留」這種指令，只是沒有正式的輸入格式/機制，
使用者得每次手動附註才行。上面「逐字模式」待辦的優先序可以降低（有堪用
的臨時解法），但仍建議之後做成正式機制，一來降低使用者每次手動附註的
負擔與漏打風險，二來附註文字本身也會被算進 digest 的輸入 token。

## 待建立：禁止生成使用者文字中未提及的品牌 LOGO（2026-07-31 內測期間提出）

**現象**：AI 生圖時可能會自行「腦補」畫出品牌 LOGO（例如場景裡的招牌、
包裝、螢幕畫面等視覺元素被模型自動補上具體品牌標誌），即使使用者提供的
新聞文字裡完全沒有提到該品牌。這類憑空冒出的 LOGO 有肖像權/商標權風險，
也可能造成報導失真（暗示某品牌涉入其實無關的事件）。

**已完成（2026-07-31，隨 merge 進 main）**：
- [x] NO UNSOURCED BRANDS 條款——`REAL_WORLD_FIDELITY_RULES`（消化）＋
      `REAL_WORLD_RENDERING_RULES`（生圖，app.js 同步）：招牌／包裝／螢幕／
      車身塗裝等一律去識別化；原文有的品牌只能以純排版文字出現、永不重繪
      標誌；明文「not even a small, faint, distant or background one」
      直接針對 NYSE 偷渲染前例
- [x] 盤查結論：error-cases 原本**沒有**品牌憑空生成的圖檔樣本（只有
      TODO 文字記述）；2026-07-31 台股實測提到美股也**沒有**畫出交易所
      招牌——前例直接重測通過。與內容忠實度規則不合併：忠實度管 variable
      文字，本條管畫面，職責不同、分開維護
- 附註：**國旗不在禁令內**（美日韓實測入畫，使用者未裁為缺陷）

## 待建立：漲跌配色須符合台灣習慣（漲紅跌綠）（2026-07-31 內測期間提出）

**現象**：股市／財經類示意圖若牽涉漲跌顏色標示，AI 預設可能套用西方
習慣（漲綠跌紅），與台灣（及多數東亞市場）「漲紅跌綠」的慣例相反，
會造成閱聽眾直覺誤讀（看到綠色以為在跌，實際上是漲）。

**已完成（2026-07-31，隨 merge 進 main）**：
- [x] 盤查結論：原本零顏色語意規則
- [x] `DIRECTIONAL_COLOR_RULES`（消化）＋`TW_DIRECTIONAL_COLOR_RULES`（生圖，
      app.js 同步）：漲/增/正向＝紅、跌/減/負向＝綠，全元素一致、同圖
      有漲有跌一紅一綠。台股實測兩引擎全對（台積電紅漲、聯發科綠跌，
      GPT 連美股收黑都自動用綠色下行線）
- [x] 禁數字疑慮已明文澄清：規則第 3 條寫明「顏色語意與箭頭方向屬內容、
      非版面幾何，禁數字條款只管畫布位置與尺寸」——為迴避數字而含糊帶過
      方向本身就是缺陷

## 待建立：LINE／行動版補做「重新生成」功能（2026-07-31 內測期間提出）

**現象**：網頁版（`index.html:428`）流程是「改內容 → 即時預覽 → 生成背景 →
匯出」，使用者對背景不滿意可以單獨重新生成背景圖，不會動到已經填好的
文字與數字——等於有一個低成本的「重骰」機制。LINE Bot 目前每次都是
從頭整段新聞文字重新走一次 digest+生圖，使用者若對圖不滿意，只能整段
文字重貼再跑一次完整流程（多花一次 digest 呼叫，且不保證重新消化出
一樣的文字內容/版面）。

**待辦**：
- [ ] 設計 LINE 端的「重新生成」觸發方式（例如針對某張已生成的圖片回覆
      特定指令/表情符號，或用 LINE Quick Reply 按鈕）
- [ ] 後端需要保留「上一輪已消化完的 digest 結果（文字/結構）」，重新生成
      只重跑生圖那一步，不重跑 digest——避免像網頁版一樣「重新生成背景
      不動文字」，也省一次 digest 呼叫成本
- [ ] 確認這個「保留上一輪 digest 結果」的狀態要存在哪裡（LINE 沒有像
      網頁版前端那樣的長駐 session，需要後端自己記使用者最近一次的結果，
      注意多人同時使用時的狀態隔離與過期清理）

## 待建立：追蹤修訂功能——原圖不整張重生，只改細部（2026-07-31 內測期間提出）

**現象**：使用者實際使用習慣是拿到一張圖後會有後續追蹤修改的需求（例如
「這句話改一下」「這個顏色換掉」「這個位置的圖案不要」），跟上面「重新
生成」不同——「重新生成」是整張背景重骰換一張新的，這裡要的是**針對
已生成圖片的特定細節做局部修改，其餘畫面盡量保持不變**，這對網頁版跟
LINE／行動版都是缺口，且是更高階的需求（局部編輯技術上比整張重生更難，
需要模型或流程支援針對既有圖片的定點修改，而不是重新從文字描述生成
整張新圖）。

**與既有待辦的關係**：
- 跟「重新生成」待辦不同層級——重新生成是換一張新圖（文字不變、背景重骰），
  這裡是同一張圖只動局部（背景與其餘文字都要盡量維持原樣）
- 技術上可能跟「以圖生圖」待辦（image-to-image／inpainting）共用底層機制——
  以圖生圖是「拿參考圖生新圖」，這裡是「拿已生成圖做局部修改」，兩者都
  需要生圖端支援「圖片＋局部修改指令」的輸入格式，可合併規劃

**待辦**：
- [ ] 需求釐清：使用者實際會想改的類型有哪些（純文字修訂 vs 版面位置
      調整 vs 顏色/視覺元素調整），優先支援哪一類
- [ ] 評估 OpenRouter／Gemini／GPT 的圖片端點是否支援「原圖 + 局部修改
      指令」的 inpainting／編輯模式，技術可行性待確認（與「以圖生圖」
      待辦的技術評估項目重疊，建議合併調查）
- [ ] 網頁版與 LINE／行動版的觸發介面各自要怎麼設計（網頁版可能較容易做
      成類似「選取區域＋輸入修改指令」的 UI；LINE 端受限於純文字/圖片
      訊息介面，需要另外設計）

## 待建立：真實人物圖須參考真實樣貌（2026-07-31 內測期間提出）

**現象**：新聞素材若提到具名的真實人物（政治人物、名人、當事人等），AI
生圖時目前沒有任何機制參考該人物的真實長相，完全憑模型訓練記憶（或純粹
瞎猜）畫出一個「看起來像人」的角色，跟本人樣貌可能天差地遠。這跟上面
「真實世界場景／物件的憑空杜撰風險」是同一大類問題的人物版本，但風險
更高——畫錯地貌頂多是失真，畫錯人物長相且被誤認為在描繪特定人士，
有肖像權、名譽權疑慮（尤其若該人物在畫面中處於負面情境）。

**與既有待辦的關係**：與「真實世界場景／物件」「以圖生圖」兩個待辦都
相關——同樣可能靠「使用者附真實照片當參考圖」解決（image-to-image／
參考圖引導），也可能需要專屬規則（例如不確定樣貌時改用去識別化的
示意人物剪影，而非嘗試畫出具體五官）。

**已完成（2026-07-31，隨 merge 進 main；預設策略經兩輪實測後由使用者拍板）**：
- [x] 盤查結論：error-cases 原本沒有人物畫錯的樣本；證據於本日 F/H 兩輪收集
- [x] 預設策略（v2，使用者裁決）：**允許正臉肖像**——力求貼近本人真實樣貌、
      不得醜化誇張；畫不像就退回背影／剪影＋標「示意圖」；不得把人物置入
      原文沒有描述的情境。v1（一律禁五官）實測過即被推翻。
      實測：H-Gemini 直接產出川普＋普欽神似側臉肖像（插畫風），
      H-GPT 背影＋引言卡小肖像，皆合規
- [x] 與內容忠實度規則不重疊：忠實度管文字，本條在 `REAL_WORLD_FIDELITY_RULES`
      規則 5／`REAL_WORLD_RENDERING_RULES`（app.js 同步）管人物畫面
- [ ] 進階：「使用者附真實照片」路線與「以圖生圖」待辦合併規劃
      （對不知名人物——地方新聞當事人等——模型記憶沒有樣貌，仍需照片參考）

**進度（2026-08-01，尚未 commit）**：自動查參考照的路線已實作於後端——
`photo_lookup.py`（Wikimedia／Wikipedia 查圖，免金鑰）＋ `news_prompt` 的
`PORTRAIT_WITH_REFERENCE_RULES`／`PORTRAIT_NO_REFERENCE_RULES`／`PORTRAIT_MODES`，
消化端只填 `portrait_subject`、畫法由後端依有無照片決定，未知 mode 一律退回不畫臉。

**2026-08-14 網頁版已接上查圖**：消化回傳的 `portrait_subjects` 會在
`/api/images/generate` 由 `apply_portrait_to_image_request()` 查維基、注入規則、
附參考照。`PORTRAIT_*` 常數仍不進 `app.js`（前端只傳名單）。多人一律不畫臉的
fail-safe 與 LINE 相同。

**裁決（2026-08-03）：採用 B —— GPT ＋ 參考照**，即現行預設行為（查得到照片就附圖），
不需改程式。

⚠️ **前一版這裡寫「B 已於 2026-08-01 蓋棺」是錯的，已更正。** 當時依據是 n=2、且兩張
都是同一個人同一張參考照；8/3 三次附參考圖（川普 LINE 實測 ＋ 黃崇仁單一變因對照 A／B）
全部拿回 21:9，其中 A 是 8/1 失敗條件的逐項重現。**8/1 的失敗無法重現**，最可能是供應商
端當時的狀態，本地無法證實。完整證據見
`docs/error-cases/2026-08-01-真人肖像與生圖模型清查-分析.md` 開頭的更正區塊。

**2026-08-05 線上事故已修**：使用者要「鄭明典／吳軒彤兩顆人頭」，系統只認得第一個人，
第二格被模型憑空捏臉還掛上真名姓名條——`fail-safe` 因為只看單一 `portrait_mode` 而完全沒生效。
已把 `portrait_subject` 改成陣列 `portrait_subjects`、**兩人以上一律不畫臉**、
`PORTRAIT_NO_REFERENCE_RULES` 改為複數語意並涵蓋並排肖像。
完整分析：`docs/error-cases/2026-08-05-多人肖像-保護失效-分析.md`。

- [x] **「哪一張對應哪一格」已於 2026-08-18 實測**（使用者授權付費，5 張 gpt-image-2）：
      全員都有照片時 **9/9 張臉正確對應、0 交換 0 捏臉**，連「三位同族裔同年齡層
      西裝男性」都沒出錯。鐵律的原始成因（通道只能對應一人）確實已消失。
- [ ] **放寬到 3 人（可做，但判定必須在程式端）**：同一次實驗發現，**只要有一人
      查不到照片，那一位就會被憑空捏臉並掛上真名（2/2 重現，性別還畫錯）**，
      即使 prompt 已明文禁止。可實作的規則是「全有或全無」：
      `具名真人數 <= 3 且每一位都查到照片 → 全部畫臉；否則全員不畫臉`。
      上限 3 是保守值（只驗到 3）。**不要只把 `> 1` 改成 `> 2`**，那樣就是放行捏臉。
- [ ] 放寬時需要自己的規則區塊：`USER_REFERENCE_PORTRAIT_RULES` 的措辭但
      **保留「示意圖」標籤**——現行 `reference_images` 會觸發「不再標示意圖」
      override，那個 override 的語意是「照著使用者提供的實景生成」，套到自動查
      維基的照片上不成立。寫實照片感＋真名＋無示意圖標籤是最糟組合。
- [ ] 結論只涵蓋 `gpt-image-2`，Gemini 未測，不可外推。
      完整實驗：`docs/error-cases/2026-08-18-多人肖像放寬到3人-實驗-分析.md`

## 肖像放寬到 3 人＋捏臉缺陷熱修（2026-08-18 已上線）

**已部署**：revision `tvbs-aicg-linebot-00023-76w`（2026-08-18），取代
`tvbs-aicg-linebot-00022-95m`。回滾用 `.\rollback.ps1`（見 README「部署與回滾」），
只切流量不重新建置，幾秒生效。

四條裁定的落點見 commit `304d3c7`。**尚未做的驗證**：新的
`PORTRAIT_MULTI_WITH_REFERENCE_RULES` 沒有用實際生圖驗過——消化層已用真實 API 驗過
（5 人→挑 3 人、查不到照片者被排出版面），但「臉有沒有對到正確的人」「示意圖標籤
還在不在」只有 8/18 實驗的間接證據。

- [x] 付費生圖驗證已跑（2 張）。**三人全有照片：對應正確、示意圖標籤保留** ✅
      （蔣萬安的維基照是 2015 年的，畫出來偏年輕，建議人工複核）
- [x] 「被排除的人連名字都沒留在圖上」是誤判，已更正：那次只比對了 VARIABLE 欄位
      字串，模型其實把她的話留在版面、把出處寫成「氣象中心」。**維持非強制**
      （使用者 2026-08-18 裁決），不改 `EXCLUDED_PEOPLE_RULES_TEMPLATE`。
- [ ] 結論只涵蓋 `gpt-image-2`：Gemini 的多人對應能力未驗證。網頁版可選 Gemini，
      目前沒有針對 provider 的額外閘門（只有 `supports_multiple_reference_images()`）。

### ⚠️ 待裁決（線上是這個行為）：被排除者的姓名會被貼到別人的臉旁邊

驗收第 2 張抓到：吳軒彤（查不到照片）被正確排出版面後，**賴清德的臉被擺在
「— 吳軒彤 氣象專家」旁邊**——真人的臉配上另一個真人的名字。消化端寫得毫不含糊
（純文字框、不畫人、名字絕不緊鄰任何人物圖形），**是生圖模型違反**，與 8/18 上午
「沒照片的人不要畫臉」失效是同一類：逐人區分的規則靠 prompt 守不住。

**這是本次放寬引入的新風險**：改動前「2 人、1 人沒照片」是全員不畫臉，沒有臉可被
錯標；改動後會畫一張臉。觸發條件（多人＋其中一位台內主播／地方當事人）很常見。

**使用者裁決（2026-08-18）：維持現狀，不改。** 接受這個風險，靠人工驗收把關
——成品上線前本來就會有人看過。曾評估但未採用的三個選項留在下面供日後翻案參考：

- 回滾（`.\rollback.ps1` 切回 `tvbs-aicg-linebot-00022-95m`）：放寬功能會一起收回。
- 被排除者改用職稱／機構代替本名：版面會畫臉時不出現本名。成本最低又不失功能，
  模型在沒有強制時本來就會這樣做一半。**若日後有人回報踩到，這是第一順位的修法。**
- 有人缺照片就整張不畫臉：最安全，但一位台內主播入鏡就讓整張回到黑影人頭。

**因此這是一個已知且被接受的風險，不是待辦。** 驗收時要特別看的點：
畫面上有臉時，臉旁邊的姓名是不是那個人。

完整分析與證據：`docs/error-cases/2026-08-18-被排除者姓名貼到別人臉旁-分析.md`

## ⚠️ 已上線缺陷：網頁版上傳肖像照，沒附照片的人仍會被捏臉（2026-08-18 實測發現，同日已修）

**嚴重度高，且 2026-08-17 已上線。** 2026-08-18 的多人肖像實驗（C 組）走的就是
網頁版上傳肖像照那條路徑（`reference_images` purpose="portrait"），實測 2/2 重現：

**使用者上傳 2 張照片、但畫面上有 3 位具名真人時，第 3 位被畫出清晰正面的臉、
掛著真名姓名條，而且性別畫錯。** 這是 2026-08-05 事故的完整重演。

`USER_REFERENCE_PORTRAIT_RULES` 第 4 條（「沒附照片的仍不畫臉」）是這個功能的
安全網，實測證明**這條 prompt 規則不成立**——逐人區分「有照片的畫、沒照片的不畫」
靠模型自律辦不到。

**使用者裁決（2026-08-18）**：人數對不上時，**缺照片的那位回頭走自動查圖流程幫
使用者補上**（不是整張退回）。補得到就照樣畫臉，等於把上傳與自動查圖串成一條鏈。

- [ ] 實作補照片鏈：`具名真人數 > 已附肖像照數` 時，對沒有照片的人逐一跑
      `photo_lookup.find_reference_photo()`，補進 `reference_images`。
      **前置**：`feat/wikidata-portrait-lookup` 的身分閘門（2026-08-18，見下一節），
      沒有那道閘門就等於把「抓到同名非人物」的風險接進上傳流程。
- [ ] **⚠️ 尚未裁決：補照片也查不到的人怎麼辦**（使用者 2026-08-18 明示「稍後
      跟自動查圖那條一起裁定」）。**在裁決前，這個缺陷在線上仍然活著**——
      只要查不到照片，第 N 位就會被捏臉掛真名。裁決不宜久拖。
      選項：(a) 整張退回全員不畫臉（最保守，等同 8/05 後的現行行為）、
      (b) 只有那一位不畫臉——但實測證明 prompt 辦不到，要靠程式擋、
      (c) 擋下不生圖並回覆請使用者補照片。
- [ ] 修好前若要止血，最省的做法是把「使用者上傳肖像照解除鐵律」限縮成
      「照片數 == 具名真人數」才解除。
- 證據：`docs/error-cases/2026-08-18-多人肖像-缺照片者被捏臉-C1.png`

## 查參考照加上 Wikidata 身分閘門（2026-08-18 完成，待合併）

分支 `feat/wikidata-portrait-lookup`（`6be2c1b`）。原本只要條目標題對得上就拿首圖
用，實測會抓到**根本不是人**的條目：「馬斯克」在 Wikidata 的同名實體之一是
「馬斯克（姓氏）」，「賴清德」「卓榮泰」也各有「內閣」「彈劾案」同名條目。
現在先用 `pageprops` 取 `wikibase_item`，確認 `P31` 含 `Q5`（是人）才採用。

- **入口用 wikipedia `pageprops` 而非 `wbsearchentities`**：後者沒有重導向機制，
  「普欽」「澤倫斯基」這些台灣譯名會整批查不到（實測確認）。
- **首圖優先、`P18` 只當備援**（不是反過來）：實測 `P18` 常比首圖差——金正恩的
  P18 是與普欽的**合照**、柯文哲的 P18 是 2014 舊照，首圖則分別是單人照與 2026
  近照。這個順序讓改動純粹是加一道閘門，現在查得到的人拿到的照片完全不變。
- 驗證：375 個測試全過＋22 個人名實跑對照，零覆蓋率回歸；不含付費生圖。
- [ ] **待處理：兩個來源都不保證單人照**——習近平的首圖與 P18 都是會面合照。
      合照當肖像參考正是 8/18 實驗中導致畫錯臉的輸入類型。這是「台內素材庫／
      政府官方照」等身分與構圖都可控的來源長期勝出的理由，非本次範圍。

## 待建立：使用者上傳參考圖（人物照片＋一般視覺參考）（2026-08-05 提出）

**起因**：2026-08-05 使用者要「鄭明典／吳軒彤的人頭」，吳軒彤是台內氣象主播，
**維基百科不會有她的照片**——自動查圖這條路對台內人物、地方新聞當事人永遠無解。
唯一的出路是讓使用者自己給照片。

**方向（使用者裁決 2026-08-05）**：
- 原則上仍由系統自動搜尋真實人物照片（現行維基查圖不變）
- **使用者在 LINE 上傳照片時，優先採用使用者的照片**
- 使用者不一定會說明照片對應哪個人名，**對不上就直接擋下不生圖**

### A. 人物照片：對應關係的防呆判定表

`P` ＝ 收到的照片數、`N` ＝ 畫面上的具名真人數。

| P | N | 處置 |
|---|---|---|
| 0 | 0 | 不注入肖像區塊（現行） |
| 0 | 1 | 自動查維基（現行） |
| 0 | ≥2 | 不畫臉（現行） |
| **1** | **1** | **用使用者照片**（優先於維基）—— 唯一無歧義的情況 |
| 1 | ≥2 | **擋下不生圖**，回覆請指明照片是誰、或一次做一位 |
| ≥2 | 1 | **擋下不生圖**，回覆照片數與人物數不符 |
| ≥2 | ≥2 | **擋下不生圖**，回覆教學：文字裡寫 `1=鄭明典 2=吳軒彤` 再重送 |

- [ ] **只有 P=1 且 N=1 自動成立**，其餘一律要使用者明示。照片順序與傳送時間都不是
      可靠的對應依據——猜錯的代價是「掛著 A 的名字、用了 B 的臉」，比不畫臉嚴重得多
- [ ] **擋下＝不生圖**（使用者裁決）：不是「畫剪影還是出圖」，是整個請求擋在生圖之前。
      生圖付費，對不上就不該花這筆錢
- [ ] 使用者明示的寫法要能解析：`1=鄭明典 2=吳軒彤`、「左：鄭明典 右：吳軒彤」
- [ ] 使用者提供的照片**不做人臉驗證**（與維基那條同樣的限制），但來源責任回到使用者身上，
      這點比維基首圖更可控

### B. 一般視覺參考圖（不限人物）

- [ ] 使用者傳一張圖當**視覺參考**（場景、風格、構圖、實際物件），生圖時一併附上。
      用途例如：實際的災損現場照、公司提供的產品照、指定的版型樣式
- [ ] 要與人物照片路徑區分：有具名真人時走 A 的判定表，否則當一般參考圖
- [ ] 附參考圖會不會影響長寬比已有前例（見 `2026-08-01-真人肖像與生圖模型清查-分析.md`），
      新流程上線後仍受 `verify_output_aspect_ratio` 保護，但要留意間歇性失敗

### C. 實作前必須先解的兩件事

- [ ] **LINE 的圖文是兩則獨立訊息**，中間還可能夾其他訊息。需要一個配對視窗
      （例如同一使用者 90 秒內的圖片先暫存，等文字訊息到達才一起處理），
      並處理「只傳圖沒傳文字」與「先傳文字後傳圖」兩種順序
- [ ] **暫存的照片是個人資料**：要訂保留期限與清除機制，不能無限期躺在磁碟上
      （現行 `static/generated/` 是 7 天自動清，可沿用同一原則；2026-08-08 由
      24 小時延長，理由見下方「生圖失敗完全沒留痕」的紀錄）
- [ ] **「示意圖」標籤的顯著度**：事故那張的標籤被放到右下角極不顯眼，
      規則只說要「clearly visible」，沒有約束位置與大小

**採用 B 之後仍待處理**（都不是尺寸問題）：
- [ ] **姿勢照抄參考照**：GPT 連「舉手講話」的姿態一起複製，用在訃聞語境不搭；
      規則明文要求「只取長相、姿勢跟 STRUCTURE 走」，GPT 沒遵守（Gemini 有遵守）
- [ ] **插畫化程度不足**：實測偏寫實照片感，`PORTRAIT_WITH_REFERENCE_RULES` 要的是
      手繪編輯插畫。關係到肖像權與觀眾誤認為真實照片的風險
- [ ] **維基首圖未經身分驗證**：`pageimages` 只抓條目首圖，可能不是本人（同名、
      消歧義、活動大合照）。目前靠人工回查來源頁兜底

## 待建立：給主管的專案進度報告（單頁 HTML）（2026-08-03 提出）

**目的**：向主管報告 AICG 專案現況，不是技術文件。**讀者不寫程式**，所以全篇用
編務語言，不出現分支名、函式名、commit 編號；效益要講「省多少時間、少幾次重生」，
不要講「新增了幾個測試」。

**三個段落**：

- [ ] **一、專案進度**：目前能做到什麼、已經解決哪些痛點（安全框自動置框、
      中文不亂碼、內容忠實度、真人肖像參考照）、還沒解決什麼。
      對照原提案的 KPI（單張 20–30 分鐘 → 約 10 分鐘、每日 8 張 → 10 張以上、
      CG 錯誤投訴每季 ≤1 件），誠實標出哪些已達成、哪些還沒量過。
- [ ] **二、LINE 版操作流程**：從加好友、貼新聞稿、選角色／密度、到收圖的完整步驟，
      最好搭配實際截圖或成品圖。要寫清楚限制：一次一則、生圖 30–120 秒、
      隧道網址會變（目前是本機測試環境，尚未部署成常設服務）。
- [ ] **三、未來拓展**：混合版型（AI 只生背景、APP 畫中文字）、局部修訂
      （不整張重生）、部署成多人可用的服務、記者／網新／社群的延伸使用情境。
      每項標出「要不要額外成本」與「大概多久」。

**產出形式**：單一 HTML 檔、自帶樣式與圖片（base64 內嵌），**不依賴外部網路**，
主管用瀏覽器直接開或轉寄都不會破版。放 `docs/` 底下。

**素材來源**：`docs/product-proposal-summary.md`（原提案 KPI 與痛點）、
`docs/line-bot-setup.md`（操作與已知限制）、`docs/hybrid-rendering-proposal.md`
（混合版型）、`docs/error-cases/`（做過哪些驗證、避免報喜不報憂）。

## 待建立：以主管報告為底稿製作報告投影片（HTML 架構）（2026-08-09 提出）

**要什麼**：投影片形式的主管報告，HTML 架構（不是 PPT/Keynote）。**範圍不只
AICG**——一次報告三個專案的進度，AICG 這個 repo 的「給主管的專案進度報告
（單頁 HTML）」只是其中一段的底稿來源。

**三大部分**（2026-08-09 使用者定案的頁數與內容）：

- [ ] **一、AICG 生成器**（≤4 頁）：說明濃縮在四頁以內，其中一頁**專講安全框規定**、
      另一頁**專講 LINE 版本介紹**。效益：單張製圖時間 20 分鐘 → 2–3 分鐘。
      內容底稿＝本 repo 上面那筆「給主管的專案進度報告」的三段素材。
- [ ] **二、自動寫稿配音剪接**（2 頁）：第一頁說明，第二頁**內嵌 DEMO 影片**。
      效益：原本 2 人次×2 小時 → 縮短成 0 人 30 分鐘（全自動）。
- [ ] **三、自動掃帶系統**（3 頁）：前兩頁說明介紹（**不要寫太深**），第三頁放
      成品連結：`https://script.google.com/macros/s/AKfycbyswTqWV2LWtiV58Rr4ZVZTtHBreQDZw_8yGsmQqOZxsDX2q8j4Qt3LjA2gXmCJoRtj1A/exec`。
      效益：24/7 無人值守自動處理，每天省 2.5 人力（早晚班掃帶班）。

**排版要求**：三大部分各自的效益數字（20 分鐘→2–3 分鐘／2 人次 2 小時→0 人 30 分鐘／
每天省 2.5 人力）**要放在顯眼位置強調**——不能埋在說明文字裡，每個部分至少要有
一處視覺上一眼就看到效益的區塊（例如大字級數字卡、Before/After 對比）。

- [ ] **最後一頁（總結頁）**：再放一次三大系統各自的**一句話功能摘要＋效益**
      （即上面三段效益數字的濃縮版，一頁三行/三張卡片並列，收尾用）。

**素材來源確認**：
- [x] **二、自動寫稿配音剪接**：素材已有，在 `G:\我的雲端硬碟\Claude共用\aicg進度報告.html`
      第 207–234 行「四、同場加映：另一條產線（AIHUNTER）」——寫稿／配音／剪輯
      三步驟流程說明＋ DEMO 影片連結（Google Drive：
      `https://drive.google.com/file/d/1gMSQrZGj6ob7aCR8eZQ8KMbDgukpEWc8/view?usp=drive_link`）。
      **注意**：目前是外部連結按鈕，不是內嵌播放器；投影片要求「內嵌 DEMO 影片」，
      屆時要嘛把影片下載下來轉成內嵌 `<video>`（若檔案不大、且允許內嵌到自包含 HTML），
      要嘛退而求其次做成縮圖＋播放連結。
- [ ] **三、自動掃帶系統**：使用者會提供圖，建議需要的圖（待使用者確認/提供）：
      1. 系統架構／流程示意圖（S1–S9 掃帶流程，說明用，對應前兩頁）
      2. 實際運作畫面截圖（例如看門狗/監控腳本執行中的狀態、或掃帶產出的紀錄畫面）
      3. 產出成品截圖（第三頁連結背後 Google Sheet／表單實際內容的截圖，
         讓看的人不用點連結也能感受成品，連結本身當「按我看更多」）
      4. （選配）人力對比示意圖，用於效益卡片視覺化「早晚班掃帶班」的人力節省

- [ ] 產出方式：`Artifact` 直接發布 HTML 投影片，或另存檔案

## 待建立：LINE 版支援編輯角色＋未選角色一律擋下（2026-08-04 提出）

**2026-08-14 裁決並落地**：不擋未選角色。LINE **預設記者**；使用者有給
「編輯」指令才走編輯（開頭 `編輯`／`編輯：`、獨立一行 `指示: 編輯`，
或只傳「編輯」兩字記住之後貼稿）。編輯固定 `standard` 密度，安全框與記者
同一套官方置框。新聞內文的「編輯部」不會誤判。

- [x] 同一個官方帳號可走編輯
- [x] 預設記者，指令才切編輯（取代「未選一律擋下」）
- [x] 只傳角色兩字時用免費 `reply` 確認、不生圖
- [x] 記住上次角色（process-local，重啟即忘）
- [ ] 狀態改跨重啟／多實例（與頻率限制同一批）
- [ ] Quick Reply／Rich Menu（非必要，關鍵字已夠用）

## 待處理：`DIGEST_MAX_TOKENS` 上限太緊，非地圖類複雜稿件常截斷（2026-08-11 發現）

**現況**：`main.py` 的 `DIGEST_MAX_TOKENS = 1500`（地圖類另有 `MAP_DIGEST_MAX_TOKENS = 6000`，
足夠）。2026-08-11 直接呼叫 pipeline 測試「表格呈現數據比較」與逐字保留模式的地震統計卡時，
`type_label="資料圖表"` 的消化請求連續 5 次全部 `finish_reason=length` 被截斷失敗
（`digest_quality_problem` 判定 variable 為空或格式壞掉），需要在 news_text 加一句
「style 與 structure 請務必精簡扼要，各控制在120字以內」的指示自行限制篇幅才成功。

**根因**：模型（走 OpenRouter）在英文 style/structure 描述上偏冗長，複合式報告
（多欄位比較、表格、多重年份數據）很容易讓 `style`+`structure`+`variable` 三欄加總
超過 1500 tokens，尤其模型有時把 style 一欄就寫到 700+ tokens。

- [ ] 評估把「資料圖表」「情境示意圖」「3D示意／流程」三類的預設上限也調高
      （例如比照地圖類抓 3000–4000），而非只有地圖類特殊處理
- [ ] 或者：在 `SYSTEM_PROMPT_TEMPLATE`／`build_digest_instructions` 固定加一句
      「style 與 structure 各不超過 N 字」的篇幅上限，從系統面解決，不必靠每次呼叫端
      自己加指示
- [ ] 兩者擇一即可，但現狀（複雜稿件常態性重試到 502）不該維持——複雜稿件正是
      表格／多欄比較這類最需要這功能的場景，卻最常失敗

**相關驗證**：同一輪測試也手動驗證了「一般視覺參考圖」（見上方
「使用者上傳參考圖」章節 B 段）的技術可行性——直接把 `ImageGenerateRequest.
reference_image_data_url` 指向一張板塊分布圖（非人物），透過 OpenRouter 的
`input_references` 成功讓生圖模型參考該圖畫出地理準確的板塊構造圖。證實 B 段規劃
的技術路線是通的，LINE 端要做的只剩訊息配對與暫存那幾件事（見 C 段）。

## 網頁版歷史紀錄（prompt+圖檔）備份進 Google Drive（2026-08-16 部分完成）

**背景**：網頁版的圖過去完全沒有伺服器端留存（`static/generated/` 只有 LINE 版在寫），
Cloud Run 本機磁碟也不持久，重部署就清空。

**已完成（route 1）**：`gcs_archive.py` 在 `/api/images/generate` 與 `/api/news-image`
（排除 `source=line`）成功生成後，把圖＋prompt/消化結果 metadata 一起丟進
`gs://tvbs-aicg-history`（90 天自動清理，asia-east1）。**刻意不做自動排程同步進
Drive**——使用者要的是隨要隨拉，之後用時再請 agent 手動跑 `gcloud storage` 把新檔案
搬進 `G:\我的雲端硬碟\Claude共用\AICG\TVBS AICG 網頁版 歷史紀錄\`（本機 Drive 桌面
同步資料夾），不必事先寫排程或 Task Scheduler。

- [ ] **route 2（Cloud Run 即時直連 Drive API，暫緩）**：使用者原本選這條，做到一半
      （已 `gcloud services enable drive.googleapis.com`；已決定用 `drive.file` scope
      非 full drive）又改選 route 1。若之後要撿回來：
  - 需使用者在 Console 建 Desktop OAuth Client（`https://console.cloud.google.com/auth/clients?project=tvbs-aicg-linebot`）
    ＋把 consent screen 從 Testing 發佈成 Production（`drive.file` 非敏感 scope，
    理論上不用排 Google 人工審查）——**這步只有使用者能做**，見
    [Manage App Audience](https://support.google.com/cloud/answer/15549945?hl=en)。
  - **地雷**：Testing 狀態發出的 refresh token 7 天就失效，會讓歸檔部署一週後
    悄悄死掉，發佈成 Production 才是無限期。
  - `drive.file` scope 看不到既有的「Claude共用」資料夾，app 只能自己在 My Drive
    根目錄建新資料夾，使用者要手動拖進 `Claude共用/AICG/` 一次；之後程式存資料夾
    ID 直寫，不受路徑影響。
  - 已知涵蓋範圍：`/api/images/generate`（line ~859）與 `/api/news-image`
    （line ~1532，排除 `source=line`）兩處都要掛；`/api/generate` 純消化沒有圖，
    不在範圍內。

## LINE 版補做「附圖」與「追加修改」：實作評估與工時（2026-08-17 評估）

把上面三筆散開的待辦（「以圖生圖」、「LINE／行動版補做重新生成」、「追蹤修訂功能」、
「使用者上傳參考圖」C 段）收攏成一份可直接開工的評估。**這裡只評估，沒有動工。**

**現況確認（讀 code＋線上設定，非推測）**：LINE 版這兩個機制目前都不存在。
`line_bot.py` 的 `_handle_webhook` 只處理 `message.type == "text"`，圖片訊息直接
`continue` 略過（使用者貼圖等於沒反應、連錯誤訊息都不會回）；每則文字訊息都是
獨立跑完一次 digest+生圖，後端唯一保留的狀態是角色（`_role_by_client`，
process-local）。2026-08-17 上線的三項功能（PLAN.md）全部只做在網頁版，
`PLAN.md` 明文「LINE 不動」。

### 結論：不需要動架構，改動集中在 `line_bot.py` ＋ `main.py` 幾個欄位

生圖層、prompt 層、安全框置框**完全不動**。三個最貴的零件已經現成：

- **refine 的輸入在 LINE 路徑上是免費的**：`NewsImageGenerateResponse` 已回傳
  `source_image_base64`／`source_mime_type`（置框前原圖），`generate_and_push`
  現在就拿得到，只是丟掉沒用。
- **多張參考圖通道已經是通的**：線上 Cloud Run 未設 `IMAGE_BACKEND`（預設
  `openrouter`）且 `OPENROUTER_API_KEY` 有設，`supports_multiple_reference_images()`
  現況回 True；`LINE_IMAGE_PROVIDER=gpt`。原本擔心的「Gemini 原生只吃單張」
  在現行部署下不成立。
- **`refine_image` 只需從端點抽成可重用函式**，改圖邏輯一行都不用重寫。

### 工時（基準：使用者＋agent 的實作節奏，非純手刻）

| 版本 | 內容 | 估時 |
|---|---|---|
| 最小可用 | 一般參考圖（不含人物防呆）＋追加修改，refine 狀態寫 GCS | **1.5–2 天** |
| 完整 | 加人物照 P/N 判定表、`1=鄭明典 2=吳軒彤` 解析、個資清除、完整測試 | **3–4 天** |

**校準依據**：2026-08-17 PLAN.md 三項功能從規劃書（`362f115`）到 merge 進 main
（`5af4a42`）就在同一天內完成，本案規模小於那三項。

### 真正要花時間的三件事（不是寫 code）

- [ ] **狀態存哪要分兩種，不能一招打通**。Cloud Run 未設 `min-instances`（預設 0），
      閒置就回收實例——所以：
  - **圖文配對（90 秒視窗）**：記憶體 dict 就夠，實例撐得過 90 秒。
  - **追加修改的「上一張原圖」**（人的時間尺度，可能隔數十分鐘才想改）：**必須寫
    GCS**。`gcs_archive.py` 的 client／bucket 現成，但它目前**排除 `source=line`**
    且只存置框「後」的成品，而 refine 要的是置框「前」原圖——是一段專用寫入，
    不是改個開關。`min-instances=1` 是替代方案但要常態付錢。
- [ ] **群組配對必須以發話者 `userId` 為 key，不能用聊天室**。現行 `_client_key` 是
      聊天室層級（角色與頻率限制刻意如此），若配對沿用它，群組內兩人同時傳圖會
      **互相配錯**——把 A 的照片配到 B 的新聞稿上。
- [ ] **P/N 判定表的卡點位置：消化「之後」、生圖「之前」**。消化便宜且正好給出 N
      （具名真人數），生圖付費。等於 `generate_news_image` 要多收一個照片數輸入，
      並多一條 4xx＋教學回覆的路徑。判定表本身見上面「使用者上傳參考圖」A 段。

### 風險與驗收

- **最大不確定性在驗收不在開發**：每輪生圖 30–120 秒且付費，「參考圖有沒有真的被
  吃進去」「追加修改後版面有沒有走鐘」只能靠實際出圖看，來回通常才是時間大宗。
- **兩條紅線測試**：`tests/test_reporter_prompt_frozen.py`（誤傷記者 prompt）與
  `tests/test_prompt_parity.py`（`news_prompt.py` vs `app.js` 兩份規則同步）。
  紅了就是改壞了，去改程式不要改 fixture。
- `dev-line.sh` 可本機跑 LINE 流程，不必每測一次就部署。

### 建議順序

- [ ] **先做追加修改**：成本最低、現成零件最多（原圖已回傳、refine 已寫好）、
      使用者最有感（現在不滿意只能整段重貼，digest＋生圖兩筆錢再付一次）。
- [ ] **再做附圖**：確認 GCS 狀態機制沒問題後，配對視窗與暫存才有地方放。


## 已做：創意 0 的「標題由 AI 生成」由鎖住改成開放（2026-09-14 晚使用者裁決，VERSION 260914-05）

使用者原話：「創意等級 0 預設關閉AI生標題 現在選項也是鎖住 可以開放 但預設關閉」。

當日早上那條裁決（創意 0 **一律**程式壓字、勾選框降成 `disabled` 唯讀鏡像）保留精神、放寬手段：

- **前台**：`coverAiTitle`／`ytCoverAiTitle` 的 `disabled` 從 HTML 拿掉，改由 JS 依等級切
  （0 級可勾、1 級以上鎖成必勾）。新增 `state.coverAiTitle`／`state.ytAiTitle` 兩顆真值，
  初值 `false`（＝預設關閉）；`setCoverTitleCreativity`／`setYtCreativity` 一動就把真值重設成
  該級的預設（0→關、1 以上→開），手動勾的選擇不跨拉桿記憶。送出讀的是真值，不再是 `creativity >= 1`。
  **坑**：`updateCoverTitleStyleButton`／`updateYtCreativityBar` 每次切版型都會跑，勾選框只能從
  state 畫回去；沿用舊的 `aiBox.checked = creativity >= 1` 會把使用者剛勾的選擇洗掉。
- **後端**：`TenCoverRequest.mode`／`YtCoverRequest.title_mode` 的硬預設 `"ai"` 改成 `None`（＝沒指定）；
  `title_mode_for_creativity(creativity, title_mode, has_background, zero_program_text=True)` 改成
  「明點就照辦、None 才套預設（0 級且沒帶底圖 → composite）」。端點那段從「強制改寫」變成「定案」，
  **無條件執行**（下游全部假設已是字串），`zero_program_text=False` 的版型照樣拿到 `ai`。
  硬預設不能留 `"ai"`：省略欄位的 API 呼叫端會在 0 級拿到 AI 標題，正是使用者要關掉的那個。

## 待評估：版型功能對齊盤點（2026-09-14 使用者：「要去對齊其他所有版型，尤其是有需要附圖的」）

盤點依據：`editor_formats.EDITOR_FORMATS`、`app.js` 各 format 的 `hides`、四個生圖端點的實際判斷（HEAD `b73068b`）。

| 版型 | 附圖位（每格） | 原圖放置 | AI改圖／多圖融合 | 參考用途 scene/portrait/map | 原圖上限 4 張 400 | 創意階梯（管什麼） | 創意 0＝程式壓字 | 只改文字 | 追加修改 | 指令欄 |
|---|---|---|---|---|---|---|---|---|---|---|
| 預設 CG（/api/generate） | 共用附圖區 | **待查**（無程式貼圖，asis 可能只當參考） | 有措辭注入 | 有 | 無 | `visual_creativity` 管版面槓桿（分區／英雄區／傾斜） | 不適用（字全 AI 畫） | 無 | 有 | 有 |
| 播出鏡面 | 同上 | 同上 | 同上 | 有 | 無 | 同上＋挖空側 | 不適用 | 無 | 有 | 有 |
| 十點不一樣 | 左右格各一位 | 1 張鋪滿／N 張切格／雙切各格 | 有（滿版混一張即整格鎖） | 有 | 有（滿版） | `title_creativity` 管標題造型 | **有** | 滿版合成版 | 有 | 有 |
| YT 國內外新聞直播 | 一位（2026-09-14 補上） | 1 整版／2 雙切／3 三切 | 有 | 有 | 有（單則） | `creativity` 管標題 | **有** | 合成版有（更正：原表誤寫「無」） | 有 | 有 |
| YT 整點直播 | 左右格各一位 | 有 | 有 | 有 | 有（單則） | 標題＋日期牌 | **有** | 合成版單則／雙則都有（更正） | 有 | 有 |
| YT 24H LIVE | 左右格各一位 | 有（full／blend／inset） | 有 | 有 | 有（單則） | 標題 | **有** | 合成版有（更正） | 有 | 有 |
| YT 今日熱搜 | 一位（2026-09-14 補上） | 有 | 有 | 有 | 有（單則） | 標題 | **有** | 合成版有（更正） | 有 | 有 |
| YT 直播直標 | 無 | 無 | 無 | 無 | — | 無 | — | 無 | 無 | 無 |
| hybrid 一鍵成圖 | 無 | 無 | 無 | 無 | — | 無 | — | 無 | — | — |

對齊缺口（待使用者裁決優先序）：
1. ~~**YT 新聞直播／今日熱搜沒有附圖位**~~ **已補（2026-09-14，VERSION 260914-03）**：能力矩陣翻旗＋app.js 表加 slots／收共用區，後端單則附圖位併清單那條路本來就在，1 整版／2 雙切／3 三切照走。
2. ~~**「只改文字」只有十點滿版有**~~ **更正**：YT 四版型合成版（創意 0）單則／雙則本來就有（`ytCoverRecomposeBtn`），盤點時看漏。~~真正還沒有的只剩**十點雙切**~~ **已補（2026-09-14，VERSION 260914-04，commit 4b545fc）**：雙切合成版回應帶「壓字前的拼好底圖」，帶回來時 `compose_ten_cover(prebuilt_split=True)` 只重壓文字，逐像素跟第一次相同；新欄位 `background_right_is_ai`／`background_layout`，版面變了（滿版↔雙切）明講 400。能力矩陣「只改文字」一欄至此全填齊。
   - 同日另補（9e747a7）：**換版型清空十點與 YT 的附圖位**（第八輪 U8 殘留），重按同一版型不清、標題欄不清。
3. **預設 CG／播出鏡面的附圖用途 asis／aiedit 行為沒定義**：generate 沒有程式貼圖這條路，原圖放置在那裡到底是「貼」還是「參考」要先查再裁。指令欄→AI改圖 是否生效同樣待查。
4. ~~**三套創意階梯語意不同但同名 0–4**~~ **已展開成獨立一節**（2026-09-14「創意階梯三條線對齊清點」，見下方 G1–G8）：原文只點出同名，實際錯位有八處，含 CG 線完全沒有變化池、三條線三種 seed 政策。原文：CG 管版面、十點管標題、YT 管標題＋日期牌。使用者看到同一條拉桿會以為一樣，至少說明文字要分開；長期看要不要統一成「版面／標題／畫風」三條獨立拉桿（見畫風池待辦）。
5. hybrid 一鍵成圖線什麼都沒接（附圖、創意、品質閘），要嘛補齊、要嘛明說它是簡化版。

## 待處理：創意階梯三條線對齊清點（2026-09-14 使用者：「清點創意階梯沒有對齊的地方　要修就要一次修好」）

盤點依據：`creativity.py`、`main.cg_creativity_rules`、`editor_formats.cover_design_brief`／
`yt_design_brief`、`app.js` 三條拉桿，HEAD `45de07c`。取代上面「對齊缺口」第 4 條
（三套創意階梯同名不同義）——那一條只點出「同名」，這裡是逐軸的完整清點。

### 逐軸對照表

| 軸 | CG（記者／播出鏡面） | 十點不一樣 | YT 四版型 |
|---|---|---|---|
| 欄位名 | `visual_creativity` int=0 | `title_creativity` int\|None（＋舊別名 `title_style`） | `creativity` int=0 |
| 進入點 | `cg_creativity_rules(level)` | `cover_ai_title_style_clause`＋`cover_design_brief` | `yt_design_brief`＋`yt_layout_rules`＋`yt_fixed_block`＋`yt_background_creativity`＋`_DATE_PLATE_STYLES` |
| 0 級 | 回 `""` | 回 `""` | 回 `""` |
| 管什麼 | 版面排法（分區→英雄區→破格→斜切） | 標題造型 | 標題造型＋日期牌＋底圖攝影指向 |
| **幅度數字** | **沒有數字**，全是形容詞（"far larger"、"at least twice"） | 塊高 18/24/30/36%、字級落差 –/1.8/2.5/3 | 塊高 32/36/40/44%、落差同十點 |
| **變化池** | **無** | `creativity.draw(seed, anchor=True)` 抽 6 顆 | `creativity.draw(seed, anchor=False)` 抽 5 顆 |
| **seed** | **無此概念** | **`None`→ OS 熵，每次生成都換**（main.py:4547 沒傳 seed） | **`f"{title}\|{date}"`→ 同標題同日期永遠同一組** |
| 配件件數 | 模型自選（3 級「一兩個」、4 級「至多三個」；2 級 0 件） | `COVER_ACCESSORY_COUNTS` 0,0,1,2,3 | 同一張表 |
| 標題落點 | 模型自選 | 3 級起從 `COVER_ANCHORS` 抽 | 固定左下（不抽） |
| 傾斜 | 4 級必須傾斜，方向沒指定 | 4 級，方向從池抽 | 4 級，方向從池抽，5–8 度 |
| 反白字數 | 沒有這個概念 | 0,1,1,2 | 0,1,1,2 |
| 配色 | 「一個重點色」，**沒有池、沒有禁綠條文** | 配色池（池內無綠）＋`COVER_NO_GREEN_RULE` | 同左 |
| 0→1 標題模式閘 | 不適用（沒有 `title_mode`） | `title_mode_for_creativity` | 同左 |
| 3 級起附加 | 無 | 側邊標籤＋畫面小籤＋反色底字 | 無 |
| 前台 | 拉桿常駐、無勾選框 | 依版型顯示、有 AI 標題勾選框 | 依版型顯示、有勾選框 |
| 等級名稱 | `creativity.LEVEL_NAMES` 共用（規矩／微設計／有設計／奔放／最狂） | 同左 | 同左 |
| 說明表 | `CG_CREATIVITY`（app.js:1201） | `COVER_TITLE_CREATIVITY`（app.js:1513） | `YT_CREATIVITY`（app.js:1575） |
| FIXED 段 | `_DIGEST_FIXED` 九條 | `_IMAGE_FIXED`＋十點專屬 | `_IMAGE_FIXED`＋依 layout 抽條 |
| 測試釘法 | `test_cg_creativity` 只釘關鍵詞存在／層層包含；`test_reporter_prompt_frozen` **只凍 0 級** | 配件在 seed=0 釘死 | 156 筆 fixture（3 layout × 4 級 × 13 seed） |

### 缺口清單（依嚴重度）

**G1｜CG 線完全沒有變化池 —— 使用者回報「最高級還是不夠亮」的直接原因。**
四級每次拿到一字不差的 prompt，成品自然每次同一個長相；而 L4 那段文字本身又只描述
一種美術（斜切、molten metal／neon、標題傾斜）。修法見下一節的池子移植。

**G2｜seed 政策三條線三種行為，而且兩種都不對。**

- 十點：`seed=None` → `random.Random(None)` 吃系統熵 → 每次換。這是刻意的
  （main.py:4537 註解、`cover_ai_title_style_clause` docstring 明寫「重生會換一組
  ——這就是使用者要的」），但代價是**同一張圖重現不出來**，而
  `cover_design_brief` 的 docstring 卻寫著「一顆 seed ＝ 一種長相，重現得出來」。
  文件與行為互相矛盾。
- YT：`seed=f"{title}|{date}"` → 同一則新聞按幾次「重新生成」都抽到**完全一樣**的
  長相。程式碼沒有任何註解說明為什麼選這個 seed，研判是 P4 那輪為了 fixture 可重現
  （156 筆逐字元核對）留下來的，不是產品決策。**這條線有池子卻等於沒有變化**，跟
  G1 是同一個病。
- CG：沒有 seed。

**這是「一次修好」的關鍵前置**——三條線的池子怎麼接、要不要重現，全看這一題怎麼裁。
三個自洽的選項：

1. **每次隨機**（十點現況）：變化最大，但同一張圖回不去，使用者說「剛剛那張比較好」時救不回。
2. **內容衍生、重生同貌**（YT 現況）：可重現，但重生無效——與使用者要的「眼睛一亮」相反。
3. **請求帶明確 seed 欄位**，前端「重新生成」時遞增：**既可重現又會變**，而且能在成品籤
   上印出 seed，使用者回報「這組好」時撈得回來。代價是前後端都要多一個欄位。

**G3｜幅度的表達方式不一致。** 十點／YT 用具體百分比與倍數（寫在 CANVAS 正後方才生效，
2026-09-11 實拍證明過），CG 用形容詞。要不要給 CG 也訂一張數字表，是裁決題——CG 的畫面
沒有固定版位（不像封面有塊高），數字要釘在什麼上面需要先想清楚。

**G4｜配件件數 CG 交給模型自選。** 十點／YT 由 `COVER_ACCESSORY_COUNTS` 決定件數、程式抽
哪幾件；CG 只寫「一兩個」「至多三個」。repo 自己記過「許可句推不動模型」（creativity.py:149-151），
所以 CG 的配件實質上等於不存在。

**G5｜禁綠只有封面線有。** `COVER_PALETTES` 池內無綠＋`COVER_NO_GREEN_RULE`；CG 線的
`_CG_L1`–`_CG_L4` 只說「一個重點色」「對比色」，沒有任何綠色禁令。**而且 CG 線還有一條
反向規則**：台灣漲跌配色（main.py:1309 附近）明文要求「下跌／減少＝green」。
**待查再裁**：播出鏡面／記者版的成品會不會疊在綠屏前？會的話這兩條規則正面衝突，
不會的話 CG 就不該套禁綠、但要把「為什麼封面禁、CG 不禁」寫下來，免得下次又被當成缺口。

**G6｜前台 YT 塊高說明與後端表對不上（已確認方向）。**
`YT_CREATIVITY`（app.js:1575-1581）寫 26/31/36/41%，`YT_BRIEF_SPECS`（editor_formats.py:1656）
是 32/36/40/44%。`git log -S'"32%"'` 指到 `b2e0e98`「YT 塊高表訂錯改正」——**表改了，前台
說明沒跟上**。這一條是純文字修正，可以單獨先做。

**G7｜三條說明表各寫各的。** `CG_CREATIVITY`／`COVER_TITLE_CREATIVITY`／`YT_CREATIVITY`
三份手寫陣列，等級名稱雖然共用 `creativity.LEVEL_NAMES`，但說明文字（也就是 G6 出錯的
那一份）沒有任何東西釘住它跟後端表一致。要嘛由後端吐出，要嘛加測試釘住數字。

**G8｜文件與程式不同步（順手修）。** `cover_ai_title_style_clause` 的 docstring 還寫著
「招式段夾在設計條文與 FIXED 之間」，但程式是 `block + _TITLE_FIXED_BLOCK`——招式在
2026-09-11 第二輪就搬進 `cover_design_brief` 了。

### 移植池子到 CG 線：可搬／不可搬

| 池子 | 能不能搬 |
|---|---|
| `COVER_PLATE_SHAPES`（底板形狀 8 款） | 可 |
| `COVER_STAGGER_PATTERNS`（錯位 5 款） | 可 |
| `COVER_TYPEFACES`（字體骨架 6 款） | 可 |
| `COVER_PALETTES`（配色 9 組，池內無綠） | 可，但先解 G5 |
| `COVER_TILT_DIRECTIONS` | 可 |
| `COVER_ANCHORS`（標題落點） | 不可，十點專屬 |
| `COVER_ACCESSORY_POOL`＋`cover_accessories()` | 不可直接搬，跟 titles／full_width 耦合 |

**好消息：位元組釘死的範圍比想像小。** `test_reporter_prompt_frozen` 呼叫
`build_digest_instructions("記者", density, "資料圖表", …)`，`visual_creativity` 取預設 0，
而 `cg_creativity_rules(0)` 回空字串——**四份 fixture 凍的全是 0 級**。池子文字進的是
`_CG_L1`–`_CG_L4_EXTRA`，碰不到 fixture。`test_cg_creativity` 也只釘關鍵詞存在與
「高級包含低級」（`test_higher_levels_repeat_the_lower_level_requirements_verbatim`
只比對 "pulled out visually"／"SHAPE LANGUAGE" 兩個字串），不是整份逐字比對。
`creativity.py:13-14` 那句警告針對的是 `_DIGEST_FIXED` 的項次字母，不是等級區塊。

### 一次修好的順序（待裁決後動工）

0. **先裁 G2 的 seed 政策**——其餘全部都依賴它。
1. G6、G8 純文字修正，可先送（零風險）。
2. G5 查「播出鏡面會不會疊綠屏」，決定 CG 要不要套禁綠。
3. G1＋G4：把四池接進 `cg_creativity_rules`，配件件數改成程式抽（沿用
   `COVER_ACCESSORY_COUNTS`）。
4. G3、G7：CG 幅度數字表與三份說明表的單一真相源，一起做。
5. 驗收：同一篇新聞在最高級跑 N 次，比對長相重複度；改之前先跑一輪當基準。

## 待評估：模組化（2026-09-14 使用者：「版型跟規則越加越多，是不是都可以模組化」）

現況數字：`main.py` 6320 行／158 個 def、`editor_formats.py` 2455 行、`compose.py` 2996 行、`app.js` 3485 行。已經模組化的：`creativity.py`（等級名稱、變化池、配件池、固定條文）、前端上傳模組（2026-09-13）、斷句（2026-09-14）。還沒的：附圖規則散在 `main.py` 五個函式＋`news_prompt.py` 六段模板；「哪個版型有什麼功能」散在四個端點的 if 與 `app.js` 的 `hides`。

建議切法（每步一個 PR、行為零變更、測試不改即綠）：
1. ~~**能力矩陣資料化**~~ **已做（2026-09-14，VERSION 260914-02）**：`editor_formats.FORMAT_CAPABILITIES`＋`capability_for`／`yt_format_key`／`hides_for`／`format_catalogue`，端點讀 `asis_max`／`zero_program_text`，`GET /api/editor/formats` 免 key 吐出，app.js 靜態表由 `tests/test_format_capabilities_20260914` 釘住一致（前台改成讀端點是第 5 步）。上面盤點表的缺口現在就是表裡的 False 旗子。原文：**能力矩陣資料化（先做，風險最低，直接服務上面的對齊題）**：`editor_formats` 每個版型一筆 capability（附圖位／原圖放置／融合／上限／創意管什麼／壓字規則／只改文字／追加修改／指令欄），後端端點與前端 `hides` 都改讀它，不再各自手寫。對齊缺口變成「把表填齊」而不是「找 if」。
2. **附圖規則模組 `refs.py`**：`UserReferenceImage`、`merge_mixed_slot_to_aiedit`、`lock_half_slot_asis`、`reject_excess_asis`、`apply_user_references_to_image_request`、`USER_REFERENCE_*` 模板搬成一處，「用途→措辭→鎖定→上限」一目了然。
3. **標題政策模組 `title_policy.py`**：`title_mode_for_creativity`、`creativity_level`、日期牌歸屬、只改文字可用性。
4. **端點拆 router**：`routes_cover.py`／`routes_yt_cover.py`／`routes_generate.py`（FastAPI `APIRouter`），`main.py` 只剩 app 組裝與共用工具。
5. **`app.js` 依版型拆 capability 讀表**，`hides` 併進第 1 步的表由後端吐給前端（單一真相源）。

## TODO：畫風池（2026-09-14 使用者列入）

使用者回饋創意階梯獲好評，元素池可再擴大，**畫風不進元素池，獨立一個畫風池**——畫風是整體視覺、跟板形／字體／配色／配件不同階級。設計要點（待裁決細節）：
- 池子獨立於 `creativity.Draw` 的變化池（板形／錯位／字體／配色／落點／傾斜）與配件池；一張圖只抽一種畫風。候選：寫實攝影、電影劇照、雜誌插畫、水彩、版畫／剪紙、報紙拼貼、霓虹賽博、膠片顆粒、3D 渲染、像素／復古……先列 8～12 種實拍過再定。
- 抽法：預設隨機（跟 `Draw` 同一顆 seed，可重現）；前端可讓使用者從池裡指定；等級門檻待裁（建議 2 級起才允許非寫實，新聞封面 0～1 級維持寫實）。
- prompt 位置：CANVAS 之後、配色規則之前，明講「畫風只換畫法，不換內容、不換字句、不動版位」；文字禁綠、人臉規則、原圖放置不重畫都優先於畫風。
- 十點／YT／CG 共用一池，但每版型可設白名單（整點直播不適合水彩之類）。
- 成品籤要不要標出畫風名（方便使用者回報「這種好」）待裁。
