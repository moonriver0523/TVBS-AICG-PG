# AICG MASTER 列管清單（唯一列管帳本）

- **建立**：2026-09-14（使用者：「把 TODO 建立成 MASTER 列管表格，這是以後要修改的指引與溝通媒介」）
- **定位**：TVBS-AICG-PG 所有待辦的**唯一狀態帳本**，不同 session／agent 之間以本表交接。
  規格、查證敘事與理由**不重抄**——看「來源」欄指向的 `TODO.md` 章節；本表只管
  **狀態、優先序、依賴與裁決**。
- **衝突時**：使用者裁決 > 本表狀態 > `TODO.md` 敘事 > 程式註解。
- **💰本機 OpenRouter key（2026-09-15 起有新額度）**：額度是新的，**不是可以隨便燒**。
  跑任何會打上游的實驗前先估花費、先報數字，不要為了「多試幾次看看」加跑。
  額度耗盡的失敗模式見 B40（會全站 502 且完全不留痕）。
- **兩份檔案的分工**：本表＝索引與狀態；`TODO.md`＝完整敘事、查證數據、根因分析與
  已完成項目的歷史。開工先讀本表，**真的要動某一項才去讀它的 `TODO.md` 章節**。

## 使用規則（每個 session／agent 都要遵守）

1. **開工先讀本表**，用「一句話現況」判斷要不要認領。要動工再展開來源章節。
2. 認領：狀態改 🔶 並寫日期；實質進度寫進 `TODO.md` 對應章節（只加不改）。
3. **不刪列、不重用 ID**。完成的改 ✅ 並移到最下方「已完成歸檔」；新項目往後編號。
4. `TODO.md` 的敘事**只加不改**——寫錯就再加一行更正，保留判斷軌跡。本表的
   「一句話現況」是摘要不是軌跡，過期就直接改寫成最新一句話。
5. **動手前檢查「依賴」欄。`D-` 開頭是裁決項，沒有使用者明示不得自行動工**，
   不論它看起來多小、多明顯。

5b. **🌊波次**：狀態欄標了「🌊**第二波**」的項目，**第一波全部收工前一律不得動工**
   （2026-09-15 使用者裁定，針對曹雪卿 0915 回饋那一批 B43-B47／F32-F36／D18-D19）。
   **裁決不受波次限制**——`D-` 項使用者隨時可以裁，裁完仍排進第二波才動手。
   哪些算第一波：本表沒有 🌊 標記的既有未結項。
6. 本表與 `TODO.md` 隨 `main` 走（見 `feedback_aicg_todo_docs_on_main`）：在功能分支
   改了要 cherry-pick 回 main，改完就 commit＋push。
7. 標「未重新確認」＝這一輪清點沒有回去驗證它現在是否還成立，**不等於「確定還沒做」**。
   要動之前先讀章節並回查程式。

**狀態代號**：⬜待做｜🔶進行中｜⏸等證據｜🗳待使用者裁決｜✅完成（移歸檔）｜❌不做

**ID 分類**：`D-`裁決｜`B-`缺陷｜`A-`對齊／一致性｜`F-`功能待建立｜`M-`模組化／技術債｜`R-`待驗收／等證據

**來源**：除另行標註外，一律指 `TODO.md` 的同名 `##` 章節。

---

## 動刀分組（2026-09-14 使用者要求：列出可以一起動刀修的內容）

> **落地順序與範圍見 `docs/plan-20260914-第一波三刀.md`**（2026-09-14 建立）。
> 下方各表的「卡在」欄位已因八項裁決全數解除，以該計畫的 Stage 順序為準。

這一節是**分組視角**，不是新的列管項——每個 ID 的狀態仍以上面各表為準。
目的：同一次改動要一次改到位，避免同一段程式被拆成好幾次修、或先重構完才發現
形狀被後來的裁決推翻。

**共通前提**：`D-` 開頭的沒有使用者明示不得動工（見使用規則第 5 條）。
下面每一刀都先列它卡在哪幾個裁決。

---

### 第一刀：附圖（reference images）

**先澄清「是不是同一模組」——前端是，後端不是。**

| 層 | 現況 |
|---|---|
| 前端 | ✅ 已模組化。`renderRefList` 共用，`app.js:3091` 註解點名 `tests/test_ref_upload_module_20260913.py` 的 parity 測試釘住，改一邊要改兩邊 |
| 後端 | ❌ **還沒**。M1（`refs.py`）仍是 ⬜——規則散在 `main.py` 五個函式＋`news_prompt.py` 六段模板 |
| 路徑 | ❌ **兩條路，不是一條**。封面線走 `slot_*` 家族（main.py:606–660：`slot_reference_list`／`slot_placement_url`／`slot_generation_refs`／`merge_mixed_slot_to_aiedit`／`lock_half_slot_asis`／`reject_excess_asis`）再進 `apply_user_references_to_image_request`；**CG 線 `slots=False`，整層插槽跳過**，只碰 main.py:3558 那一個函式 |

所以「附圖已經同一模組」這個前提只對前端成立。**第一刀的實質內容之一，就是把後端這兩條路收斂成一條**（M1）。

**成員**

| ID | 項目 | 卡在 |
|---|---|---|
| B26 | 多張原圖放置措辭是單數 | 🔶 **已修待驗（exp，2026-09-14）**——`USER_REFERENCE_ASIS_MULTI_RULES_TEMPLATE`，asis ≥2 取代單數「One of」 |
| B27 | **YT 封面生圖路徑完全不讀附圖位欄位** | 王結玲 0914 下午／使用者追問「是不是附圖順序問題」 | 🔶 **已修待驗（exp，2026-09-14）** | `_yt_cover_background`／`_yt_cover_full_image` 改讀 `slot_refs`（`_yt_cover_asis_list`／`_yt_cover_gen_refs`／`_yt_cover_all_refs`）。單則攤平路徑維持。**2026-09-14 查證（修前）**：`_yt_cover_background`(main.py:5532) 與 `_yt_cover_full_image`(5598) 兩支生圖路徑都只讀攤平的 `req.reference_images`，**從不呼叫 `slot_refs()`**，且 5571 那行把 `asis` 整個濾掉。`slot_refs()` 只出現在合成／overlay 路徑。**等於 2026-09-10 裁的「第一／第二標題底下各有自己的附圖位」在生圖模式下被靜默丟棄**，左右身分根本沒送到模型手上——這就是重生會左右互換的機制層原因，比「模型自由發揮」嚴重。與 D4 同一族但**不需要裁決**：欄位已存在、只是沒接。**2026-09-14 已撈後台確認**：王結玲 16:06–16:17 共 6 筆，版型**確定是 24H LIVE**（成品左上角 24H LIVE 角標），5 筆走 `openai/gpt-image-2.5-sunburst` 生圖、1 筆 `yt-cover:overlay`。**症狀比原記的「左右互換」更廣：是位置完全不受控**——氣象圖／颱風路徑圖在六張裡分別落在右半、中偏右、左上、中間偏左。單則滿版本來就沒有左右格，所以不是互換，是**沒有任何位置約束**。這正是 D4 要處理的，而 B27 是讓 D4 無從生效的機制層原因。⚠**證據邊界**：後台歸檔的 `prompt` 是 `f"FULL: {visual}"`（main.py:5107），**只有畫面描述、不是送進模型的完整 prompt**，所以「規則有沒有注入」後台看不出來，B27 的依據是程式碼不是後台紀錄。證據圖 `D:\Downloads\AICG\後台紀錄\20260914\20260914_王結玲_YT封面六連生.jpg` |
| B28 | **畫真人＋掛真名時「AI示意圖」標籤會被附圖規則洗掉** | D13 裁決的前提查證 | 🔶 **已修待驗（exp，2026-09-14）** | `portrait_subjects` 有非空白姓名時不注入 `USER_REFERENCE_NO_DISCLAIMER_RULES`。原：`apply_user_references_to_image_request` 的 2026-08-17 裁決：有使用者上傳就注入 `USER_REFERENCE_NO_DISCLAIMER_RULES`（不標示意圖），僅 `portrait_reference_data_urls` 與 `aiedit` 兩種例外保留標籤。D13 裁示「可以畫人可以掛名，因為已標 AI示意圖」——**那個前提在有附圖時不成立**。要補：畫真人並掛真實姓名時標籤強制保留 |
| B29 | 稽核歸檔只存畫面描述，不存送進模型的完整 prompt | 王結玲撈證時發現 | 🔶 **截斷那一半已修待驗（exp `000da0d`，2026-09-16）；「只存畫面描述」那一半仍未動** | `_archive_generation` 存的是 `f"FULL: {visual}"`（main.py:5107 等），不是 `apply_user_references_to_image_request` 注入後的完整 prompt。使用者回報「規則沒生效」時無法從後台反查，只能讀程式碼推論。不擋本波。**本波建議（不做）**：後台 `_row` 渲 `density`；歸檔改存 VARIABLE 段（或放寬 `MAX_PROMPT_CHARS=4000`），否則 D16 按檔位量測永遠 n=0。🔶 **2026-09-16 截斷那一半已修待驗（exp `000da0d`）**：`MAX_PROMPT_CHARS` 4000 → **24000**（exp `febeae2` 自我更正，先前那版寫 12000 是**估**出來的、而且是拿已截斷的殘骸去估，作廢），`request_log` 與 `audit_archive` 同步。**證據**：正式站 09-15 曹雪卿 22 筆裡 8 筆 prompt 剛好 4000 字，逐筆看截點**全部斷在 `STRUCTURE (LAYOUT RULES)` 中段，`VARIABLE FIELDS` 整段在截點之後**；本機 logs 再查，118 筆走消化生圖的紀錄**沒有一筆**落在 4000 以內，等於這條路徑一律被截。**24000 是量過的**：拿 `news_prompt.build_prompt()`（純函式）餵本機 log 最長的 style(1255)／structure(2840)／variable(336)，跑遍 role×safe_frame×no_text×type_label×portrait_mode **32 種組合，最壞 19786 字**（`VARIABLE FIELDS` 起點 5878–6305），取實測上限加兩成。**測試改成現場組最壞 prompt 再比，不釘死數字**——規則區塊只會愈加愈多，寫死數字下次會靜靜再被截一次，那正是 B29 本身的病。⚠**只對未來生效，0915 那 8 筆救不回來**。「`_row` 渲 density」那一半見 F39。`f"FULL: {visual}"` 只存畫面描述那一半**仍未動**。✅**2026-09-16 已合併進公司 main（PR #25，merge `9534b11`）；⚠仍未部署到正式站** |
| B30 | CG 線的「示意圖」浮水印沒有程式疊，只能求模型畫 | 董碧惠 2026-09-14 20:39 後台證據 | ✅ **2026-09-15 已裁：浮水印由程式負責**（待動工，展開章節見 `TODO.md` 末節） | **裁決（使用者 2026-09-15）**：CG 線的「示意圖」浮水印**改由程式疊，不再交給生圖模型畫**。⚠**兩端要一起改**：程式開始蓋之後，`news_prompt.py:194`／`229`／`238`／`261` 與 `app.js` 對應副本那幾條「keep the 示意圖 label visible」會讓模型**也**畫一個，同一個 commit 要改掉；那些條文受 `tests/test_prompt_parity.py` 逐字驗，且可能落在凍結快照內，**動工前先確認重凍需求**。四個待決細節（觸發條件、樣式、落點、防重複）已寫進 TODO 章節。 ／ **原分析**：`WATERMARK_TEXT = "示意圖"`（`compose.py:305`）只由 `apply_broadcast_hole`（`compose.py:352`）貼，**僅播出鏡面**；封面線另有程式疊的 `AI示意圖`（`compose.py:445`／`719`／`1439`）。**情境示意圖這條 CG 線兩者都沒有**——`app.js:1957` 是叫模型自己把 VARIABLE 給的示意圖標籤畫出來，等於把中文字交給生圖模型，本來就不可靠。證據：董碧惠同一則金門金牌竊案連生 4 次（20:35／20:37 重抽、20:39 改圖、20:40 再重抽），20:39 的 USER CHANGE REQUEST 原文就是「右下圖片下方 加 示意圖」，改完隔一分鐘又退回重生。**建議**：CG 線比照封面線，改成程式後疊（同一支 `compose` 已有現成字型與半透明底樣式），並在前端給開關。不擋本波。
| B31 | **消化死線守門對第一次 attempt 完全不生效，單次 attempt 最壞 270 秒** | 子代理速度調查 2026-09-14 | ✅**已修，監督驗收通過（2026-09-14）**——獨立重跑全套 `unittest discover`**1687 測試全綠**，`-w` 差異確認實質只有 34 加 6 減（其餘是包 try 的縮排），contextvar 有在 `finally` reset，prompt 字串與 `BadRequestError` 退路皆未動。⚠**尚未在 Cloud Run 線上驗證**，逾時行為要等下次部署後觀察。commit `a7e7961`（`max_retries=0`／attempt 0 也查死線／第二次 `generate()` 共用 deadline／原生 images timeout 180） | `DIGEST_DEADLINE_SECONDS=230`（`main.py:253`）的檢查寫在 `main.py:2088`，條件是 `if attempt and ...`——**attempt 0 根本不執行**。同時 `main.py:84/96/104` 三個 `OpenAI(...)` 都沒帶 `max_retries`，SDK 預設還會自己重試 2 次，所以 `payload["timeout"]=90`（`main.py:1699`）是**每通 HTTP** 的上限、不是整次呼叫的：一次 attempt 常見最壞 90×3=**270 秒**（對得上 `main.py:155-200` 註解記的「重試後 269 秒」），撞上 `BadRequestError` 退路（`main.py:1721-1756`）再乘 3 = 810 秒。**已超過 Cloud Run 300 秒硬砍**，保險在最需要的第一次就是關的。另 `apply_photo_availability`（`main.py:2033`）會再呼叫一次 `generate()`，而 `main.py:2081` 的 deadline **重新計算**，單一請求理論上限變成 230+230。**修法（純靜態即可確定，不必等實測）**：明確設 `max_retries`、把守門改成每個 attempt 前都查（含第 0 次）、第二次 `generate()` 沿用同一 deadline。生圖端反向問題：原生路徑 `images.generate`／`images.edit`（`main.py:3020`／`3028`）**完全沒傳 timeout**，吃 SDK 預設 read 600 秒 |
| B32 | 維基查照在消化與生圖各查一次，且完全沒有快取 | 同 B31 | ✅**已修，監督驗收通過（2026-09-15）**——`a2ad333` ＋ `a8467d4`。獨立複核：鎖與 `time.monotonic()` 正確、HIT 15 分／MISS 60 秒分開、筆數 256 與位元組 64 MB 雙上限（值是整張圖 base64，只用筆數會有無上界成長路徑）、`main.py` 零改動、B32 相關 65 筆測試綠、全套 1712 綠。⚠**線上未驗**：實際節省的查照時間要等 F25 分段計時才量得到。| `/api/generate` 走 `apply_photo_availability` → `lookup_portrait_photos`（`main.py:2021`）查一輪；前端接著打 `/api/images/generate`，`apply_portrait_to_image_request` → `resolve_portraits`（`main.py:2417`／`3404`／`3372`）**又查一輪**。`photo_lookup.py` 無 `lru_cache`／無 `_CACHE`。單人最壞 2 候選名 × 2 語言（`photo_lookup.py:48`）×（query＋`_is_human`＋可能 P18＋圖片下載，`photo_lookup.py:151/170/175`），每通 timeout 10 秒（`photo_lookup.py:50`），且逐人序列（`main.py:3308-3321`）。加行程內 TTL 快取即可，不犧牲任何畫質 |
| B34 | **編輯版「字極少」實際出 3 點，違反它自己的「ONE point. Not one to three — one.」** | 子代理 stage2 的 2-8 逐格證據（2026-09-15），監督獨立複驗 | 🔶 **2026-09-15 已裁：1～3 點都合格、上限 3 點；下一波修** | **裁決（使用者 2026-09-15）**：編輯版「字極少」**不必硬壓成 1 點**，**1～3 點都算合格，但最多 3 點**。所以要動的是條文而不是範例——`EDITOR_SYSTEM_PROMPT_TEMPLATE` 的「ONE point. Not one to three — one.」放寬成「至多三點」，並把 3 點釘成硬上限。**現行實測值（3 點）在新標準下即合格**，本輪實拍 1／2／3 點一律判過，已告知測試端。**仍排下一波**：改條文一樣要重凍，等下次重凍額度。 ／ **原證據**：實測 `title_length_24.json`：`mid／編輯` 與 `long／編輯` 的 `minimal` 都是 **3 點**，**同樣兩則素材的記者版都是 1 點**——差別只在角色，所以病灶在編輯樣板而不在 density block。**根因（監督複驗過，且比回報的更乾淨）**：`main.py:1005-1006` 的 variable 格式範例列了整整三行 `[內文小標]`（聯準會降息那則），而**記者樣板 `main.py:979` 根本沒有範例**——這個角色不對稱把現象解釋完了。範例是模型最強的錨點，壓過 `MINIMAL_DENSITY_RULES` 第 1 條，正是本 repo 記過多次的「具體範例壓過抽象規則」。**修法**：讓範例隨 density 變，或在 minimal 明文作廢該範例的行數。**為什麼本波不做**：`EDITOR_SYSTEM_PROMPT_TEMPLATE` 逐位元存在於 4 份編輯凍結快照（`grep 聯準會三度降息 tests/fixtures/editor-digest-*` 四中），動它就要重凍，而 CP1 批准的唯一一次重凍已在 2-3 用掉。**下次開重凍額度時一併修** |
| B37 | **D16 的標題上限條文讓標題全面變短，連沒觸及上限的檔位也縮水——級距反而被壓平** | 監督主導的重現實驗（2026-09-15），「測試」執行 | ⬜ **要重裁 D16，裁決前不得動工** | **實驗設計（②③ 已排除）**：同一支腳本 `scripts/measure_title_length.py`、同一個 295 字 `long` 樣本、同一個 `native`／`gpt-5.5` 後端、同為記者版 default，**只有程式碼日期不同**——量測路徑、素材題材、模型後端三個變因全部固定。**時間軸鐵證**：0-2 量於 `b0adc93`（2026-09-14 22:44），當時 **`HEADLINE LIMIT` 條文還不存在**；上限是 `707bf12`（2026-09-15 01:18，Stage 2-1）才進 `main.py:1032`／`1061`／`1075`／`1091`。**數據**：minimal 上限 10，0-2 為 13 → 今天 **4**（−9）；simplified 上限 13，13 → **11**（−2）；standard 上限 18，19 → **10**（−9）；maximum 上限 22，20 → **15**（−5）。**關鍵在後兩格**：`standard` 原本 19、上限 18，只差 1 個字卻掉到 10；`maximum` 原本 20、上限 22，**根本沒超標、完全不需要壓縮**，仍掉了 5 個字。**所以這不是「被上限裁掉」，是「出現上限這件事本身讓模型全面寫短」**——四格同向全為負，典型的模型看到數字上限就往遠低於上限寫。**後果**：D16 的意圖是「拉桿要讓標題看得出長短」，實測反而壓成 4／11／10／15 且不單調（standard 10 < simplified 11），**比沒有 D16 的 13／13／19／20 更沒有級距**。**內文也一起縮**：`simplified` 從 0-2 的 2 點／50 字掉到 1 點／11 字，`standard` 從 7 點掉到 6 點。**限制**：每格 n＝1，模型有隨機性；但四格同向、且兩格在上限不具約束力時仍大幅下降，不是雜訊能解釋的量級。**修法方向（待裁，不得自行動工）**：①上限改成區間或目標值（「約 N 字」而非「不超過 N 字」）；②只在超標時才提上限，不要無條件塞進 prompt；③拿掉上限，回到 0-2 的自然長度。**⚠正式站未受影響**：`707bf12` 不在 `main` 上，線上仍是 `260914-05`，所以**本件與 B36 無關**，不要混為一談。原始資料 `D:\Downloads\AICG\後台紀錄\20260915\title_long_4_20260915.json`，費用 $0.476 |
| B36 | **資料圖表同一張圖裡各資訊塊行數不一致，使用者得手動補齊** | 曹雪卿 2026-09-15 10:40／10:50 後台證據，監督查證 | ⬜ | **證據**：1444 字「超級央行週」稿，10:40 生成的六宮格資料圖表裡，**四塊是三行（標籤／數字／補充），兩塊只有兩行（標籤／數字，缺補充）**——日本央行那塊停在 `1.25%`、南韓那塊停在 `3%`，另四塊都有第三行。10:50 使用者的 USER CHANGE REQUEST 逐條就是在**補那兩塊**：「左下日圓那塊最下面加一行字：日圓14日回貶至154價位」「右下韓幣那塊最下面加一行字：美元轉強 韓幣同步面臨壓力」。改圖五條全部正確套用。**所以病灶不是「整體字太少」，而是同一個版型內部不齊**——模型把六個平行的格子填成不一樣的深度，使用者每次都要手動抓出缺的那幾格。**與 F1／D16 的差別**：那兩件管的是「拉桿能不能把整體字量拉開」，本件管的是「同一張圖內部平行元素要不要一致」，是另一個軸，**不要混在一起修**。**修法方向**：在資料圖表的版型條文裡明寫平行格子的行數必須一致（要嘛全部帶補充行、要嘛全部不帶），而不是靠模型自己平衡。⚠**歸因受阻**：後台沒記錄當時的消化檔位／角色／版型（見 F30），所以**無法判定她是拉在哪一檔**——若她本來就在低檔位，這張的稀疏有一部分是合理的。修之前要先補那個欄位，否則會對著錯的基準改。**順帶觀察（不另立項）**：她同時把「創逾2週新高」改成「創9/2以來新高」，模型把原文的具體日期概括成模糊表述；這與 D3「只能重排不能新增」同向但反面，屬忠實度議題，先記著看會不會再出現。證據圖 `D:\Downloads\AICG\後台紀錄\20260915\曹雪卿查核\` |
| F30 | **後台紀錄沒有消化檔位／角色／版型欄位，使用者回報問題時無法歸因** | 監督查 B36 時發現（2026-09-15） | ✅ **2026-09-16 已修（commit `f24dba1`）＋ 2026-09-20 補第二個實證缺口（worktree `wt/admin-fields-20260920`）** | ⚠**本行狀態曾停在「⬜要做」超過三天，f24dba1（13:14）晚於裁決本行的 e75fe51（11:21）但沒回來勾狀態；2026-09-20 派工複查時才發現。改完程式碼那一刻就該回來勾狀態，不要等下一次派工才順便發現。** 🔍**2026-09-20 正式站實查（team-lead 派人撈 09-18～09-20 後台 91 筆）又挖出本行沒點到的另一個缺口**：後台 `type` 欄位把「這張圖畫的是什麼內容」跟「這是不是追加修改」混成同一格——91 筆裡真正的追加修改（`/api/images/refine`）有 36 筆，但 `type` 只有 1 筆標成 `web-refine`，其餘 35 筆被 `_enrich_archive_fields()`／`_recall_digest()` 回填成「使用者最近一次消化」的內容分類（如「自動判斷」「資料圖表」），把「是不是追加修改」這個問題蓋掉了。**修法**：新增 `audit_archive.record_action()`，純從既有的 `source` 欄位（`web-refine`→追加修改／`digest`｜`hybrid-digest`｜`cover-titles`→消化／`editor-yt-overlay-*`→合成／其餘→新生成）推導，不動 `type_label` 的既有回填邏輯（那對「內容分類」仍然正確），也不需要新欄位或回溯遷移就能對正式站既有 360 筆歷史紀錄立刻生效。`/admin` 新增「動作」欄。測試見 `tests/test_digest_stage_audit_20260920.py::ActionColumnTests`。 | `/admin` 每列只有 ts／who／type／news_text／最終 prompt／model／provider／request_id（2026-09-16 前的舊描述，已修）。**沒有 density（字多字少檔位）、沒有角色（記者／編輯）、沒有版型**。後果：使用者回報「資訊量太少」時，**無法分辨是規則有問題還是他自己拉在低檔位**——B36 就卡在這裡，只能標「歸因受阻」。生圖 prompt 有記，但那是**生圖端**的，消化端的檔位不會出現在裡面。**修法**：生成時把這三個值一起寫進紀錄列並顯示在 `/admin` 列表。成本低、對後續每一件現場回報都有用，**建議排在 B36 之前**——沒有它，B36 改了也不知道有沒有改對 |
| F31 | **後台只記成功、失敗完全不留痕，故障率無法統計** | 曹雪卿回報「生圖很容易失敗」，監督查證（2026-09-15） | ✅ **2026-09-16 已修生圖端（commit `f24dba1`）＋ 2026-09-20 修消化端（worktree `wt/admin-fields-20260920`），兩段都補完才算真的解掉** | ⚠**2026-09-16 的修法只涵蓋了一半，狀態欄卻先勾了 ✅，2026-09-20 team-lead 派人撈正式站 09-18～09-20 後台真實紀錄（91 筆，全月 360 筆）才揪出來**：`audit_archive` 從 f24dba1 起確實會記生圖端點的失敗，**但完全沒有涵蓋消化階段**（`/api/generate`、`/api/hybrid/digest`、`/api/editor/cover-titles`）——這三支的 502／524（Cloudflare 逾時）／認證失敗全部直接 `raise HTTPException` 往前端丟，從來沒有寫進 `request_log` 或 `audit_archive`。正式站實查證實：91 筆失敗數是 0、全月 360 筆失敗數也是 0，但同期已知有消化階段的 Cloudflare 524（見 B39）與上游 502，「成功率 100%」是假的，因為分母根本沒把消化階段算進去。**修法（2026-09-20）**：`main.generate()`（`/api/generate` 背後的實作、也是 LINE pipeline 共用的那支）、`hybrid_digest()`、`editor_cover_titles()` 三支端點比照生圖端點的模式，成功與失敗都寫入 `request_log`＋`audit_archive`（消化階段沒有圖，`_archive_generation` 補一個「沒圖就跳過 GCS 那份備份」的判斷，否則會在 `gcs_archive.archive_generation` 的必填參數上直接 `TypeError`）；`generate()` 會被 `apply_photo_availability` 巢狀呼叫，用既有的 `_inside_pipeline` 旗標算出 `own_clock`，確保巢狀呼叫不會重複記一筆（測試：`tests/test_generate_retry.py::GenerateRetryTests::test_photo_availability_retry_reuses_the_same_deadline` 已補斷言剛好 1 筆）。新增測試 `tests/test_digest_stage_audit_20260920.py`。**誠實的殘留限制**：這裡記的是 `generate()` 內部能攔到的 `HTTPException` 出口，攔不到 Cloud Run 300 秒硬砍這種「連程序都被砍掉」的情況；另外若上游在 Cloudflare 已經回 524 之後才真的完成（例如 240 秒後），這裡會記成 `ok` 但 `duration_ms` 很大，不是「這筆其實失敗了」——不是每一種真實故障都會被這次的修法完整反映成「失敗」。 | 正式站 `/admin` 200 筆全撈：表頭只有 `時間｜使用者｜類型｜成圖｜內容`，**167 列全部都有成圖**，全檔搜不到 `失敗／錯誤／error／timeout` 任何字樣。代表**生成失敗時根本不寫紀錄列**。後果：使用者說「很容易失敗」，我們**連失敗率是 5% 還是 50% 都答不出來**，也不知道失敗集中在哪種稿長／版型／改圖，只能靠前台人工重現（已派「測試」，交辦單 `docs/交辦-20260915-曹雪卿生圖失敗重現.md`）。**修法**：失敗也寫一列，至少要有 上游 HTTP status、錯誤類別（逾時／內容政策／配額／解析失敗）、耗時、重試次數；`/admin` 用顏色或欄位標出來。**與 F30 同一個檔案、建議同一刀改完**——兩件都是「後台缺欄位導致無法歸因」，分兩次改是白做工。⚠**2026-09-16 補一個本條原本沒點到的限制：「200 筆全撈」的 200 是上限不是總數**——`audit_archive.list_records()` 的簽章是 `limit: int = 200` 寫死，`admin_index()` 也沒有把它開成查詢參數。09-16 再撈一次同樣停在 200 筆、最舊只到 09-07。所以「167 列全部都有成圖」這個分母是**最新的 200 筆**，不是全部歷史；失敗率若真要算，這個上限也得一起放開（或加分頁）。**其中 `role`／`density`／`seed` 三欄已由 F39 補上渲染（exp `000da0d`），本條剩下的是失敗列與耗時／provider** |
| B38 | **消化逾時的錯誤只有一個一閃而過的 toast，內容是「HTTP 524」** | 曹雪卿回報＋2026-09-15 正式站重現 | ✅ **2026-09-15 已修已驗（exp `9e42f96`）** | 現場：進度條卡在「消化新聞 35%」約 60 秒後**靜默歸位**成初始紅色按鈕，「生成結果」欄**仍留著上一次的舊圖**，使用者看起來像什麼都沒發生。⚠**監督查證修正**：`app.js` 的 `handleOneClickGenerate` catch 裡**有** `showToast(err.message)`，不是完全吞掉——但 `err.message` 是 `_apiError()` 對 524 產出的 **「HTTP 524」**，對使用者零意義，且 toast 一閃即逝、測試端當場沒看到。**所以病灶不是「沒提示」而是「提示等於沒提示」**。後果：使用者判定「生圖很容易失敗」然後直接重按，每重按一次就再燒一次消化。**修法**：①逾時／5xx 要給看得懂的中文與可行動建議（「新聞太長，消化超過上限，請縮短或改低一階字數」）；②失敗時結果欄要標示這是上一次的舊圖，不能沉默留著；③錯誤要持續顯示到使用者關掉，不能用 toast。與 B39 同一個現場、不同層，**兩張單要一起修才有意義** ✅**監督驗收 2026-09-15**：逐行看過 diff。三處都做對——fallback 依狀態碼給可行動中文、後端 `detail` 優先的邏輯保留、紅色橫幅可關閉不自動消失、舊圖上方標「⚠ 這是上一次的結果，本次生成失敗」、成功時一併清除；四條紅線（prompt／逾時數字／`digest_reasoning_body`／分支）都沒碰；1755 測試全綠。⚠**驗收時擋下一處並已補**：`_apiError()` 是 12 個呼叫點共用的，初版把「縮短新聞、降字數拉桿」硬套到生圖（`app.js:2975`）與改圖（`app.js:3563`）——那兩條不吃新聞稿也沒有字數拉桿，**給錯的下一步比不給更糟**，正是 B38 自己要治的病。已加第三參數 `context`，只有 `_digestFetch` 傳 `'digest'`，其餘走中性版。✅**staging 實測驗收 2026-09-15（`260915-01`）**：**沒有用 devtools 模擬，是撞上一次真實失敗**（字多對照組，約 150 秒消化逾時）。三件事全部到位——①訊息是「AI 服務這次太久沒有回應，請縮短新聞內容或稍後重試」而不是「HTTP 524」；②頂部紅色橫幅**持續顯示**不自動消失（toast 另外也有）；③舊圖右上角標「⚠ 這是上一次的結果，本次生成失敗」。**真實逾時比模擬攔截是更硬的證據**。連帶開出 **B42**（改圖線仍只有 toast） |
| B39 | **Cloudflare 約 100–120 秒就切斷 `/api/generate`（524），整套逾時預算是照錯的天花板設計的** | 2026-09-15 正式站實拍重現 | ✅ **2026-09-15 已修已驗（staging `260915-01` 實測通過）**：`/api/generate` 改成 NDJSON 串流（`main.py` 新增 `generate_stream`／`_generate_ndjson_lines`，`generate()` 本體維持純函式不變，繼續被 `generate_news_image`／`apply_photo_availability`／LINE 那條路徑直接呼叫，不經 HTTP，串流只是外層 wire format）；背景執行緒跑 `generate()`，主執行緒每 `DIGEST_HEARTBEAT_SECONDS=7` 秒送一行 `{"type":"ping"}`，結束送 `result` 或帶原 status／detail 的 `error`。前端 `_digestFetch` 改逐行解析：只有串流開始前（驗證失敗等）才看 `response.status`，開始串流後一律看內容判斷成敗，`_apiError()` 沿用 B38 那套（見 `context="digest"`），連線中途斷線另有獨立訊息。`DIGEST_DEADLINE_SECONDS`／`DIGEST_TIMEOUT_SECONDS`／`DIGEST_ATTEMPTS` 一個都沒動。`uv run pytest` 1760 全綠——查證後**既有三支測試（`test_generate_retry.py`／`test_request_log.py`／`test_admin_console_formats.py`）其實不需要改**，它們測的是 `generate()` 這個純函式或路由註冊表本身，本來就沒經過 HTTP／串流那層，已跟監督說明這點；新增 `tests/test_generate_stream_20260915.py` 五支涵蓋串流層（成功／後端錯誤／未預期例外／心跳／驗證失敗）。本機用真實 2,930 字稿（native `gpt-5.5`）實跑一次，`curl --trace-ascii` 證實 73 秒內分十次收到 ping 才收到 result，位元組確實持續流入；前端解析邏輯另用模擬 ReadableStream 驗證三種情境（成功／串流內錯誤／中途斷線）訊息互不相同。正式站驗收未做，等使用者授權那段花錢的部分。⚠**監督複驗抓到一個稽核歸檔缺陷並已修復**：`threading.Thread` 起於全新空 context，不會自動帶著 `_current_user` 這個 contextvar（Clerk middleware 在 request context 裡 `set()`）——改之前 `generate()` 跑在 Starlette 用 anyio `to_thread.run_sync` 開的執行緒，那條路徑會複製 context，換成自己開 thread 之後若不手動複製，背景執行緒裡 `current_user()` 會是空字典，`generate()` 尾端的 `_remember_digest()` 因此悄悄跳過不寫，導致生圖那發靠 `_recall_digest()` 補回的新聞原文變成空的——每一次網頁版生成的後台「內容」欄都會無聲漏記，跟 0911 查到的「後台缺欄位」同一類。**修法**：`generate_stream()` 在還沒離開 middleware context 時 `contextvars.copy_context()`，`_generate_ndjson_lines()` 背景執行緒改成 `threading.Thread(target=ctx.run, args=(worker,))`。新增回歸測試 `test_background_thread_still_sees_the_logged_in_user`（從 `generate_stream()` 進去，不是內層的 `_generate_ndjson_lines()`），已驗證：復原成沒複製 context 的版本時這支測試會紅（`{} != {'user_id': 'u1', ...}`），修好後綠。`uv run pytest` 1761 全綠。 ✅**監督驗收 2026-09-15（第二輪）**：逐行看過 diff，`copy_context()` 寫在 `generate_stream()` 這一層是對的——StreamingResponse 的 body 要到 middleware 的 `finally`（`_current_user` 已 reset）之後才被消耗，複製寫進 generator 函式體裡會太晚。**回歸測試不是空氣測試，監督獨立驗過**：把 `target=ctx.run` 改回 `target=worker` 後 `test_background_thread_still_sees_the_logged_in_user` 確實變紅，還原後綠；四支相關測試（串流／重試／請求記錄／後台格式）共 39 passed + 20 subtests。另查證 `/api/generate` **全專案只有 `app.js` 一個 HTTP 消費者**（`generate_news_image`／LINE 都是直接呼叫純函式），改成 200＋內容判成敗沒有其他破口；`generate()` 回傳的就是 `GenerateResponse` 實例，`model_dump(mode="json")` 與原本 `response_model` 的序列化欄位一致。順手改掉註解裡一個簡體字（`换`→`換`）。**尚未在正式站驗**（要花錢），與 B38 一起排隊等使用者授權。✅**staging 實測驗收 2026-09-15（`260915-01`）**：拿 09-15 在正式站打出 524 的**同一篇 2,930 字稿**×「字超多」×資料圖表，15:07:35 送出、15:11:51 完成（4 分 16 秒），`/api/generate` **全程 200，沒有 524**；在原本會斷線的 122 秒那個時間點連線仍然活著。監督獨立到 staging `/admin` 核對，**該筆成功紀錄確實在（15:11:36，資料圖表）**。**B39 的目標達成——長稿不再被 Cloudflare 切斷。** ⚠**但這不等於曹雪卿的問題解決了**：同一篇改「字多」的對照組**仍然失敗**（見 B48），只是失敗方式從「524 斷線」變成「看得懂的 503」。**B39 修的是失敗的表示法，不是失敗率本身。** 📌**留待評估、非本輪阻塞**：使用者中途關分頁時背景那個 daemon thread 會跑完全程，token 照燒。 | 實測：2,930 字稿 ×「字超多」× 資料圖表，送出後 **約 122 秒**收到 **HTTP 524**（Cloudflare "A timeout occurred"），死在**消化那發**。但程式裡的三道預算全部比它大：前端 `DIGEST_FETCH_TIMEOUT_MS=290`、後端 `DIGEST_DEADLINE_SECONDS=230`、單次 `DIGEST_TIMEOUT_SECONDS=90`，註解明寫「時間設在 Cloud Run 的 300 秒之內，讓後端的訊息永遠有機會先回來」。**那個前提是錯的**——真正的天花板是 CF 的切線，**後端根本來不及回它那個講得清楚的 503**，所以使用者永遠拿不到有用的錯誤。連帶：`DIGEST_ATTEMPTS=5 × 90 秒` 的重試設計在 CF 底下**最多只跑得完一次多**，重試等於形同虛設。**修法方向（待查證後再定）**：①把後端死線壓到 CF 切線以內（要先確認 CF 實際設定值，不要用猜的）；②消化改成非同步／輪詢，不讓單一請求扛長工；③長稿在送出前就擋下並提示。⚠**先量再修**：本機重現（無 CF）正在跑，要先知道消化端在「字超多」是不是本來就慢到 100 秒以上——若是，壓死線只會換一種失敗 ⚠**2026-09-15 本機量測排除一條假說**：測試端在原生 `gpt-5.5` 上量到「思考吃光 6000 預算→截斷→重試→耗時 38 秒變 149 秒」，看似就是 524 的成因，但**那是原生路徑沒有思考封頂的產物（見 B41），正式站走 OpenRouter 有封在 2000**，不能拿來解釋 524。❌**2026-09-15 稍晚，監督更正自己這一句：這個排除是錯的。** 本機四次實驗（見 B48）用的是 **OpenRouter／claude-sonnet-5，與正式站完全相同的後端**，同樣量到 `reasoning_tokens=6000／ratio=1.00／finish=length`——**「正式站有封在 2000」這個前提不成立，封頂根本沒生效。**測試端當初那條「思考吃光預算→截斷→重試→耗時暴增」的假說**是對的，是監督誤判把它排除掉的**。這條路徑現在是 B48 的已確認根因。正式站這一側仍未取得後端日誌（`aicg.tvbs.ai` 由公司 GCP 專案服務，本機 gcloud 只看得到個人專案 `tvbs-aicg-linebot`，該時段無任何流量）。**剩下的主嫌**：claude-sonnet-5 的 provider 輪替重試（九個端點、過載換手，每換一次就是一整次呼叫的時間） ⚖**2026-09-15 使用者裁決**：公司 Cloudflare 的 proxy timeout **不能調**，所以走串流＋心跳——`/api/generate` 開始處理就持續送位元組，CF 看得到連線活著就不會 524。**明確不採**「壓後端死線」：實測 #7 是花 95～110 秒才成功的，壓到 80 秒等於自己砍掉本來會過的長稿。完整非同步（job＋輪詢）留作底牌，本波不做。**排在 B38 之後**——錯誤表示法兩邊要一致，同時動會打架。交辦單 `docs/交辦-20260915-B39串流心跳.md` |
| B43 | **追加修改指定了主標題斷行，「斥」仍然留在第一行** | 曹雪卿 0915 回饋文件（AI CG回饋，hikaru.tsao@gmail.com） | ⬜ 🌊**第二波**（2026-09-15 使用者裁定：等第一波收工才動工） | 使用者在追加修改處下的指令是「修正主標題為：川普致電黃仁勳　斥AI接管世界是騙局」（全形空格處即為她要的斷點），成品仍是「川普致電黃仁勳　斥／AI接管世界是騙局」，**斥被留在第一行行尾**。監督已調正式站成品核對（`20260915-102630-23b0215e41ca.png`，後台 10:26:30，資料圖表改圖），**確認屬實**。要查的是：追加修改的文字有沒有把斷行意圖帶到生圖那一層，還是 `apply_title_break_hints` 只認自己的規則、把使用者寫的空格吃掉。 |
| B46 | **YT 直播直標：LIVE 角標與直標之間有一段空欄** | 曹雪卿 0915 回饋文件（AI CG回饋，hikaru.tsao@gmail.com） | 🔶 **已上線待實拍驗收（commit 1c93504「0920 第一波」，已在公司 main；2026-09-23 帳本補登）**｜⬜ **2026-09-20 使用者改列下一批動刀（🌊 第二波解除）**｜**根因已查明**：`VSTRIP_TOP_RATIO=0.166`（一般版）／`VSTRIP_TOP_WITH_LABEL_RATIO=0.191`（有原音呈現／AI即時翻譯小標）是兩個固定比例，**沒勾小標時那一格仍被留著**，所以空一段｜與 **B80** 同批做 | 使用者原話「YT直播直標，我在提供底圖? 另外LIVE和標中間有空欄」。監督調成品核對（`20260915-131247-ec36fb8a7ae8.png`，13:12:47）：LIVE 紅框下緣約 y=175，直標深色塊上緣約 y=240，**中間約 65px 垂直空隙**，兩者左緣都對齊 x=50。前半句「我在提供底圖?」另開 D18（是產品行為問題，不是缺陷）。 |
| B49 | **貼「版面指令稿」（整段用 `[欄位]` 標記）時，「不改字」在結構上就不可能成立——使用者無從選起** | 許岱軒 2026-09-15 15:40:39 正式站實例＋使用者回報 | ⬜ 🌊**第二波**（2026-09-15 使用者裁定列入下一波）｜**設計已定調，見 D20** | **現場**：使用者貼的不是新聞稿，是一份版面規格——`[大標題 變色強調] 第二波155架軍機`／`[次標題] 美軍為救1名飛官出動`／`[左 標籤+數字圓圈] 轟炸機／4`／`[內文1]`／`[背景 小插畫]` …，意圖是「`[]` 裡的是指令，`[]` 外的字一個都不要動」。**但這個意圖在現有的檔位選單裡無處可選**：選「不改字」應該最接近，選「字少」則會把他挑好的字改掉。✅**監督查證：這不是使用者誤解，是程式結構上就矛盾**——`main.py:2067` 的 `_VERBATIM_MARKER_RE = re.compile(r"\[標題\]|\[內文小標\]|<蓋章>|<底帶>|[<>]")` **只認這四個寫死的標記**。`verbatim_fidelity_problem()`（`main.py:2128`）拿「原文」與「輸出」各自剝掉這些標記後**逐字比對**，而使用者寫的 `[大標題 變色強調]`、`[左 標籤+數字圓圈]`、`[背景 小插畫]` **通通不在白名單裡**，所以會被當成**原文的一部分**參與比對。**結果是二選一，兩個都壞**：①模型照使用者意思把標記當指令執行、只輸出內容字 → 比對缺一大串字 → 判定不符 → 重試 5 次 → 使用者收到「AI 回傳內容異常」（`502`），**而且完全看不出是自己的寫法被擋掉**；②模型為了通過比對把 `[大標題 變色強調]` 原樣抄回去 → **最終 prompt 裡那條 `Any text written as [text] → Remove brackets → Apply highlight color` 會把「大標題 變色強調」這幾個字當成要強調的內容畫到圖上**。⚠**根本衝突**：`[...]` 這個語法**已經被系統佔用為「強調這段文字」**（見最終 prompt 的 Text Styling Rules），使用者卻拿它當「欄位標籤」。**同一個符號兩種語義，不先分開就無解。** 📌**今天那次之所以成品漂亮**（`20260915-154039-fd5b6c672642.png`，標記全部正確當成版面指令、文字逐字保留），**是因為他沒有選「不改字」**——但**後台沒有記檔位（F30），所以連他當時選了什麼都查不到**，這本身就是 F30 該做的又一個理由。**修法要先裁 D20**（語法怎麼分家），程式面最小修正是把 `_VERBATIM_MARKER_RE` 從白名單改成「任何 `[...]` 一律剝掉再比對」——只影響比對、不影響輸出，可讓「不改字」至少不再誤殺；但這會讓真正想強調的 `[重點]` 在比對階段也被剝掉（可接受，因為比對兩邊同時剝）。**與 B11（缺獨立指令欄位）／B12（行內指令時靈時不靈）同一叢，建議一起看。** ✅**2026-09-15 使用者補充：成功那次選的是「字少」（預設）。監督查證後回答「是設計上支援的用法，但不是可靠的用法」**——`USER_INSTRUCTION_RULES`（`main.py:1568`，組裝時放最後）第 4／5 條明文寫了：指令文字**永遠不得出現在 variable**，且「使用者給的已經是完稿 CG 腳本」時 variable 必須逐字重現，**「VERBATIM MODE OVERRIDES … INCLUDING THE SIMPLIFIED MODE OVERRIDE」**。所以選字少會生效不是巧合，是這條軟性 override 在擋住字少的壓縮。❌**但它沒有任何強制力，而且這次已經改了字**：使用者輸入 `[左 標籤+數字圓圈] 轟炸機／4`，**最終 prompt 裡是 `轟炸機4架, 戰機64架, 加油機48架, 搜救機13架`——消化端自己加了「架」**（監督從後台最終 prompt 原文核對，不是從成品猜的；亦即不是生圖模型加的）。規則 5 白紙黑字寫 `do not … add one single character`，它加了，**而且沒有任何閘門會抓**——`verbatim_fidelity_problem()` 只在 `density == "verbatim"` 時才跑（`main.py:2404` 附近）。**這次加「架」剛好讓圖更好讀，但同一個機制同樣可以吞掉一個數字或壓掉一句話，一樣不會被擋。** 🚩**所以現況是：「字少」是目前最不壞的選擇，不是正確答案。** 使用者要的「只改指令、其餘一字不動」在系統裡**沒有一個受保護的選項**——真正逐字受保護的只有「不改字」，而那一檔對這種輸入結構上就不成立（本列上半）。**這也是 D20 要裁的真正理由**：不是介面標示問題，是缺一個「受閘門保護的照排模式」。 ⚖**2026-09-15 使用者裁決：目標是把「不改字」做對，不是換語法也不是加檔位** ——「理想是不改字，但讀懂 `[]` 等等符號，是指令不該入題。但要考慮使用者真的把 `[]` 當作文字不是指令時，還是要出現 `[]`。」詳見 **D20**。**本列的程式面工作**：①`verbatim_fidelity_problem()` 的比對要兩邊同時剝掉指令型 `[...]`；②「不改字」要把指令型 `[...]` 排除在 variable 之外（呼應 `USER_INSTRUCTION_RULES` 第 4 條）；③加逐字閘門保護——現在選「字少」之所以能動，全靠一條沒有強制力的軟性 override，實測已經證明它會被違反（消化端擅自把「轟炸機／4」改成「轟炸機4架」）。**⚠ D20 還有一個子問題沒裁（使用者怎麼表達「這個 `[]` 是字」），動工前要先問。** |
| B51 | **追加修改把封面置框：五個封面版型的 Logo／角標消失＋出現四邊安全框** | 王結玲（後台紀錄，2026-09-15 使用者回報） | 🔶 **2026-09-16 已實作待實拍驗收（exp `e0f4df6`，Stage 6）**：照片保護擴到 YT 四版型＋封面 refine 不置框｜⬜ **下一波優先要修**（2026-09-15 使用者裁定）｜根因已證實，Logo 那半待實證 | 使用者原話「24HLIVE 他重複修改圖之後 24HLIVE的相關LOGO都不見了 程式有BUG 沒有壓LOGO」。**程式碼初查（監督，2026-09-15）**：`compose.compose_yt_live24_cover` 裡角標、日期、兩層版 Logo 都是**無條件**貼的（只有標題受 `draw_title` 控制），註解也明寫「那三個是頻道識別，交給模型畫就會有錯字與走樣的版本」——**所以只要成品有經過這支函式，Logo 不可能消失**。因此病灶應該在**追加修改回來那條路徑有沒有再進 compose**：`app.js` 的 `refineSourceFromResponse` 只在 `data.mode === 'composite'` 時才給得出無文字底圖，AI 標題模式下拿到的很可能是**已經貼過 Logo 的成品**，送回生圖模型改圖時 Logo 被一起重畫／洗掉；若之後又走了某條不重貼固定元素的分支，Logo 就永久消失。**這正是 `_cover_ai` docstring 已經警告過的情境**（「把貼過 Logo 的成品餵回生圖模型，模型會把 Logo 一起重畫，那是播出事故」）。**下一步（零成本優先）**：①到正式站 `/admin` 撈王結玲今天 24H LIVE 的紀錄，確認是第幾次改圖開始掉、`model` 欄是不是 `yt-cover:overlay`；②本機用同一條路徑連改三次，看第幾次開始 compose 沒被呼叫。**不要先改程式**。⚠**這是播出識別消失，嚴重度等同事故，不是美感問題。** 🎯**2026-09-15 使用者補充第二個症狀，根因當場對上：「王結玲修改過後居然出現四邊安全框，這是 YT 版型沒有的東西」。****已在程式碼層證實，兩個症狀同一個源頭。** `app.js:3602-3603` 的 `handleRefine` 送出的是 `safe_frame: isCover ? false : state.safeFrame`（封面強制 false，**寫對了**）**但下一行 `safe_frame_profile: state.currentRole` 是無條件送的，沒有跟著封面例外**。後端 `refine_image` 拿這兩個值去問 `resolve_frame_plan(role, safe_frame)`（`main.py:3533`），而那支對編輯版是**「兩檔都是滿版生成＋後製，開關只決定後製方式」**——本機實跑證實：`resolve_frame_plan('編輯', False)` → `(True, True, '編輯')`，`resolve_frame_plan('記者', False)` → `(False, False, '記者')`。**也就是說：只要使用者身分是「編輯」，YT 封面按下追加修改就一定會被置框，`safe_frame=False` 完全無效。**王結玲是編輯，YT 封面又本來就是編輯在用的工具（`app.js:3064` 連生圖後端都因為編輯身分改走 gpt），**每一次改圖都會中**。**這同時解釋了 Logo 消失**：置框是把整張畫面縮放／填進對位框，程式先前貼好的角標與兩層版 Logo 會跟著被縮放或推出版面，而不是 compose 沒有貼——與前一段的初判（懷疑沒進 compose）相比，**這個解釋只需要一個原因就同時說明兩個症狀，優先度更高**。⚠**但 Logo 這一半仍是推論，尚未實證**：置框後前端是拿 `data.source_image_base64`（置框前原圖）去 recompose 的，理論上 Logo 應該還會被重貼一次，所以還缺一步——**要確認 `finalize_image_result` 置框後回的那張『置框前原圖』到底是不是真的沒被動過**，以及 recompose 那一發有沒有真的跑完。**修法方向（待裁，不得自行動工）**：`handleRefine` 在 `isCover` 時把 `safe_frame_profile` 一起改送記者（最小改動），或在 `refine_image` 端讓 `safe_frame=False` 對編輯版也有效（動到編輯版 2026-08-19 的既有裁決，**不能自己決定**）。**下一步**：①撈王結玲後台紀錄確認身分欄與 `model` 欄；②本機用編輯身分跑一次 YT 追加修改，看成品是不是同時有框、沒 Logo。 📋**2026-09-15 全版型盤查結果（使用者指示「一定不止這個版型，其他版型也查」）**：全站 8 個版型逐一查過，**`safe_frame_profile` 漏接影響的是「所有會走追加修改的封面版型」，不只 24H LIVE**——`yt_live_cover`（國內外新聞直播）／`yt_hourly_cover`（整點直播）／`yt_live24_cover`（24H LIVE）／`yt_hot_cover`（今日熱搜）四個 YT 版型，加上**十點不一樣的 AI 版**，共 **5 個版型**走同一行 `safe_frame_profile: state.currentRole`，只要使用者是編輯就一定被置框。✅**兩個版型不受影響**：`yt_vstrip`（直播直標）的 `hides` 明寫 `refine: true`、而且完全不生圖，沒有這條路徑；`default`／`broadcast` 是 CG 本身，`safe_frame` 與 profile 成對送出、語意一致，**這才是這兩個參數原本正確的用法**。❗**盤查另外揪出一個更嚴重的漏網**：**十點合成版**連封面例外都沒吃到，另開 **B52**。💡**為什麼只有「修改過後」才看得到框**：封面的**初次生成**走 `_cover_panel_image`／`_cover_ai`／`_yt_cover_full_image`，那幾支是直呼 `generate_image_raw` 並自帶 `safe_frame=False`，**繞過 `finalize_image_result`**，所以不會置框；只有 `refine_image` 這一條會問 `resolve_frame_plan`。**這與使用者「修改過後才出現」的描述完全吻合，是根因成立的旁證。** |
| B52 | **追加修改的「封面例外」漏掉十點合成版：它被當成一般 CG 改圖，改完不重貼 Logo／節目標籤，還吃 CG 的比例與安全框設定** | 由 B51 全版型盤查展開（2026-09-15，使用者指示「一定不止這個版型，其他版型也查」） | ⚖ **2026-09-16 使用者裁決：要開放**——十點合成版也要能自然語言追加修改，**改列為新能力**（原「缺陷」描述已查證不成立，見本列後段）。計畫已完成：`docs/plan-20260916-B52-十點合成版追加修改.md`（Codex 擬案、本 session 審過）。**2026-09-16 追裁：合成版含原圖放置（asis）時一律擋下不讓改**（B55 的保護優先），混合版也擋、不做「警告後放行」。⚠ 計畫查出兩件事：①雙切的左右 panel **沒有分開保存**，`background_image_base64` 是 `split_canvas()` 拼好含斜切白線的整張，所以只能整張送 refine，另一格與白線都可能漂移；②refine 後 `background_is_ai`／`right_is_ai` **都必須設 true**，否則被重畫過的格子不會貼「AI示意圖」標示｜⬜ **下一波優先要修**（2026-09-15 使用者裁定，與 B51 同批）｜程式碼路徑明確，尚未實拍證實 | `app.js:3585-3590` 的封面判定是 `isYtCover = inputs === 'yt_cover'`、**`isTenCover = inputs === 'cover' && state.tenCoverMode === 'ai'`**——**十點不一樣的「合成版」(`tenCoverMode === 'composite'`) 兩個都不符合，`isCover` 為 false**，於是整包封面例外全部失效：①`aspect_ratio` 變成 `currentAspectRatio()`（CG 的比例，編輯＋安全框 ON 時是 16:9、OFF 時也是 16:9，但那是 CG 的語意不是封面的）；②`safe_frame` 送的是 `state.safeFrame`——**而十點與 YT 五個版型的 `hides` 都寫了 `safeFrame: true`，前台根本看不到那顆開關**，值是使用者在 CG 模式留下來的殘值；③`text_free` 為 false；④**最嚴重：`app.js:3615-3617` 的 `shown = isYtCover ? recomposeYtCover : isTenCover ? recomposeTenCover : data`，合成版落到 `data`——改完的模型圖直接當成品顯示，完全不走程式後貼**，所以正版 Logo、十點節目標籤、日期全部不會被重貼回去。⚠**而且 `safe_frame` 送 false 也沒用**：見 B51，編輯身分在 `resolve_frame_plan` 一律回 `(True, True, '編輯')`。**待實證**：十點合成版的追加修改按鈕是否真的開放給使用者（`hides` 沒有關掉 refine，推測開放），以及改完的成品是不是真的沒有 Logo。 | 🔻**2026-09-16 動工時查證：計畫描述的病灶在 UI 上打不進去，B52 不是修 bug 而是開新能力。****依據（本 session 複驗過，非採信轉述）**：`app.js:2511` `const tenCoverSource = data.mode === 'ai' ? refineSourceFromResponse(data) : null;`——合成版生成後 `refineSource` 被設成 null，`updateRefineControls()` 的 `refineBtn.disabled = !state.refineSource` 因此把追加修改鈕灰掉；切版型（`app.js:1168` 一帶）也會清空，沒有殘留舊 AI 版來源可以繞過去；`handleRefine()` 一進來就 `if (!state.refineSource) return`。**所以目前會走 `handleRefine`／`isTenCover` 的只有 AI 版。**👉**帳本原本那句「待實證：十點合成版的追加修改是否真的開放給使用者」，答案是沒有開放。**📌**合成版現有的是「只改文字」**（`recomposeTenCoverText()`，2026-09-14 `4b545fc`），不是自然語言改圖。⚖**因此 B52 要做＝新增能力，需要先定規格（待使用者裁）**：①composite 的 refine 來源要用哪張（`state.tenCoverBackground` 的無文字底圖？雙切有左右兩個半底圖，改哪一個還是兩個都送）；②`recomposeTenCover()` 現在寫死 `mode:'ai'`（`app.js:2375`），合成版要一條新的 recompose 路徑，不是重用既有的。📌**附帶事實**：YT 封面的合成模式（`ytTextFree`）走得通、能 refine——**只有十點合成版是特例**，所以這也可以視為版型一致性缺口（同 A4 那一族）。
| B55 | **原圖放置的照片被生圖模型整張重畫：創意階梯應該只能改標題設計，不得重置原圖** | 王結玲（2026-09-15 使用者回報＋當場裁定規則） | ✅**2026-09-22 高度上限實機驗收通過，本線結案**（使用者：「驗收也 ok 了」）｜以下為三輪修正的完整過程——使用者在 dev 站測三次全被擋，判斷是「標題加的裝飾小圖示被誤判成改動原圖」。查後台三筆（`20260921-171342` / `-171432` / `-171554`）**證實是誤判，但真兇不是裝飾圖示，是日期牌保護框**。🔬**證據**：三筆全部擋在**第 b 道閘**（保護區被畫 53081／51016／52224 px）；把歸檔的圖層（`*-layer.png`，本輪新加的診斷才留得下來）叫出來看——**模型做的完全正確**，回的是真正的透明底標題圖層，照片一個像素都沒碰，被畫到的只有 `(50,524,568,626)` 那一塊，也就是**程式畫日期牌的位置**；頁首帶 `(50,26,1870,292)` 與角標 `(1695,367,1859,420)` 完全空白。💡**根因是設計自相矛盾**：創意 1 級起「整個日期牌交給模型畫」是 2026-09-11 使用者裁決，四道閘卻不分等級一律保護那塊，等於一邊叫模型畫、一邊禁止它畫。⚠**第二個雷**：三筆可疊區佔比 56.25%／51.38%／52.02%，全部超過 c 閘的 50%——那條上限當初「沿用 PHOTO_PROTECT_MAX_CHANGE_RATIO，不另訂數字」是張冠李戴：那個常數量的是「照片被改動多少」，這裡量的是「標題圖層蓋住多少畫面」，滿版標題設計蓋掉下半部是預期行為。⚙**2026-09-21 使用者裁決「兩個都改」**：①`yt_cover_protect_boxes(protect_date_tab=...)`／`_yt_hourly_fixed_boxes(include_date_tab=...)`，創意 ≥1 且 AI 標題時日期牌不列入保護區，**條件與 `compose_yt_hourly_cover(draw_date=...)` 逐字相同**（測試釘住兩處一致，兩邊各自判斷就會出現「程式畫了牌卻沒保護」這種成品上看不出來的錯）；0 級程式畫牌時照舊保護。②新常數 `TITLE_LAYER_MAX_PAINT_RATIO = 0.70` 取代借用值，`PHOTO_PROTECT_MAX_CHANGE_RATIO` 維持 0.5 不動（差異遮罩那條路不受影響），測試釘住兩者不准合併。Logo／LIVE 章全程保護不變。✅**回放驗證**：拿後台那三張真實圖層重跑放寬後的閘門，**三張全部通過**，保護區被畫從 5 萬多 px 降為 **0 px**（反證頁首帶與角標本來就沒被碰），可疊區 57.70%／52.86%／53.55%，離 70% 上限仍有餘裕。🧪`tests/test_b55_transparent_title_layer.py` 新增：日期牌開關只准少掉那一塊（其餘保護區逐一比對）、news／hot／live24 三個版型不受新參數影響、只畫日期牌的圖層在 0 級擋下／1–4 級放行、`protect_date_tab` 與 `draw_date` 必須同一條件、兩個上限常數不准合併。🐛順修後台顯示 bug：`_esc` 的 `value or ""` 把 0 吃掉，`alpha 0–255` 被印成「alpha –255」。📌**實證**：commit `15920d0`（exp/redesign-20260903）→ 公司 PR [#33](https://github.com/TVBS-AI/aicg-champion-daihsuan/pull/33) 已合併進 main（merge commit `0a5a680`，2026-09-21T09:33:59Z）；合併前後各跑一次全套，皆 **2201 題全綠**。VERSION `260921-03`。⚠**兩件已回報、刻意沒動的**：①c 閘只有一道常數，所以十點滿版的透明標題圖層（`overlay_title_layer_over_cover`）也一起到 70%——使用者實證只有 YT 整點，但不同步放寬會變成同一道閘兩個標準；②`provider=gemini` 走差異遮罩那條路（`restore_yt_cover_photo`）取保護區時日期牌仍在裡面，創意 ≥1 時有一模一樣的矛盾，修法是同一個條件再傳一次，**等使用者裁決**。🩹**2026-09-21 追加修正（使用者回報）**：十點不一樣「原圖放置」時**頂部藍底標頭帶消失**。同樣是設計矛盾——純 AI 版的標頭帶一直都是模型畫的，但原圖放置會把字帶以上一律還原成 base（gpt 走透明圖層＝整片列保護區，gemini 走差異遮罩＝硬邊界），模型畫的帶必定被抹掉，而 base 是使用者原圖、本來就沒有帶，於是 `paste_cover_logo` 把 Logo／節目標籤／日期／ON AIR 直接貼在照片上。**兩條路徑都中**，不只 gpt。修法：新增 `compose.paste_cover_header_band()`，在 `_post_paste(draw_header_band=...)` 裡於 `fit_cover_canvas` **之後**補畫（之前畫會被放大裁滿切掉一截），幾何與合成版 `_draw_cover_header` 用同一組 `COVER_HEADER_*` 常數，兩版長得一樣。純 AI 版（base is None）不補，帶仍由模型畫。⚠`63d56c2` 是同一件事的第二刀：帶只補在成品上不夠，**回傳的追加修改源圖（`source_image_base64`）也要有帶**——否則使用者按追加修改、改完回來走 `req.background_image_base64` 那條路，而那條路不會再補帶，藍底又不見。📌**實證**：commit `5c456aa`＋`63d56c2`（exp/redesign-20260903）→ 公司 PR [#34](https://github.com/TVBS-AI/aicg-champion-daihsuan/pull/34) 已合併進 main（merge commit `d474093`，2026-09-21T10:00:21Z）；合併前後各跑一次全套，皆 **2221 題全綠**。VERSION `260921-04`。🩹**2026-09-21 第二次誤判（使用者回報，dev `20260921-180313`）**：十點滿版原圖放置被 b 閘擋下、退回程式壓字。量歸檔圖層：保護區被畫 117304 px＝**1920×61**，是頂端一條整條實心的深藍標頭帶，帶以下到字帶（y 80–400）一個像素都沒畫。真因是設計矛盾——十點 prompt 模板自己寫著「Across the very top runs a deep-navy header band」，**模型是照指示畫的**；而那一帶疊完圖後會被 `paste_cover_header_band` 整條蓋掉，模型畫在那裡的東西一個像素都進不了成品，為了看不見的像素把整張退掉是純誤擋。修法：`_overlay_title_layer_core` 新增 `discard_protect_paint`，十點這條路的 b 閘由**擋下**改成**丟掉**（疊圖遮罩改用 `painted_in_editable`），保護區的保證完全沒鬆——丟完那一帶仍逐位元等於 base。YT 四版型維持硬擋（那邊保護的是 Logo／角標，prompt 明講要留白）。「只畫在保護區」的圖層在十點改由 d-empty 閘擋，一樣擋。🧪拿真實圖層重播：pass／保護區 117304 px 已丟棄／可疊區 40.322%／保護區逐位元＝base。🩹**2026-09-21 第三刀（使用者下令補完）**：`provider=gemini` 的差異遮罩路徑`restore_yt_cover_photo` **根本沒有 `protect_date_tab` 參數**，一路吃預設的 True。同一個矛盾在兩條路上症狀不同，這也是它拖到最後才被發現的原因——gpt 那條會**擋下**（使用者看得見錯誤訊息，就是 0921 那三筆），gemini 那條是**默默把模型畫好的日期牌抹掉還原成 base**，成品看起來只是「牌不見了」，沒有任何錯誤、沒有任何紀錄。修法：參數穿透到 `yt_cover_protect_boxes`，呼叫端傳的條件與 gpt 那條、與`compose_yt_hourly_cover(draw_date=)` **三處逐字相同**。🧪測試：①原本的 parity 測試只用 `assertIn`＝「至少有一處對」，正是它讓這個漏接過關的，改成用 regex 抓出**所有** `protect_date_tab=` 呼叫點、數量必須是 2、每一個都要逐字相同；②實跑差異遮罩：模型在牌的位置畫東西，False 時必須留下、True 時必須被還原。📐**2026-09-21 c 閘上限拆成兩個常數（使用者裁決「要拆 怕誤判」）**：`TITLE_LAYER_MAX_PAINT_RATIO_TEN=0.75`／`TITLE_LAYER_MAX_PAINT_RATIO_YT=0.85`。拆的理由**不是**兩邊用量不同——當天八筆實測比例幾乎一樣（十點 28.7／32.5／40.3／56.5%，YT 整點 41.5／51.4／52.0／56.3%）——而是**分母不同**：這道閘量的是「佔可疊區的比例」，可疊區＝畫布扣掉保護區，十點滿版 88.4%、hot 82.4%、live24 76.3%、hourly 74.0%、news 67.5%（佔畫面）。換算回佔整張畫面：十點 56.5%→50.0%、YT 56.3%→41.7%，也就是**同一份設計放到 YT、因為分母小算出來的比例天生偏高**，YT 那側會先撞天花板，兩邊寫同一個數字等於讓 YT 吃虧。餘裕：十點 1.33 倍、YT 1.51 倍；「模型整片畫背景」逼近 100%，兩邊都還擋得住。🧪測試：兩個常數必須不同且 YT 較寬、兩支疊圖函式的簽章預設值各自對應（讀 signature 不讀原始碼字串）、以及**實跑**一張蓋住可疊區約 80% 的圖層——卡在兩數之間，十點必須擋在 c 閘、YT 必須過，證明拆開有真的接到判斷上。📏**2026-09-21 標題過大（使用者回報「AI生成純標題 太大了 幾乎遮住整個版面」）**：查證用同一則新聞、同一使用者、同一天的兩張 dev 成品對照——17:42 `5ef0a6d8a6e7` **純 AI 版**標題塊約佔畫面高 **38%**（兩張臉完整露出、招式擺在標題旁邊）；19:16 `e1bac5f58636` **原圖放置版**約 **65%**（蓋掉兩人身體與半張臉、招式疊在標題下方又加高一截）。兩張的 DESIGN BRIEF 一字不差（同一張 COVER_TITLE_BRIEF_SPECS，推測同為 L4＝36%），結果差快一倍→**病不在塊高數字**，改小它只會連本來正常的純 AI 版一起縮掉。真因在 `AI_TITLE_LAYER_ONLY_NOTE`：它叫模型「附圖只是參考、不要重畫照片」「畫布是透明的」，模型面對空畫布沒有人物可襯托，字就長到把框填滿；純 AI 版因為照片是它自己畫的，會自己讓開。修法（使用者裁決「先只改 prompt」）：新增 `AI_TITLE_LAYER_SIZE_NOTE`，講三件原本沒講的事——①畫布空但成品不空，照參考圖人物位置讓開、不准蓋臉；②塊高就是 brief 那個數字，**數字重複釘在 prompt 最前端**（0911 已證明離 CANVAS 遠的條文壓不動模型）；③那個數字管的是**整份美術**（底板／光暈／招式全要塞進同一條帶），招式擺旁邊不可另起一排疊下面撐高總高——19:16 多出來的那一截正是如此。數字由 `cover_title_block_height()`／`yt_title_block_height()` 直接讀規格表，不手打。🔍**順補兩個診斷欄**（查這件事時發現後台答不了）：`painted_bbox_height_ratio`＝圖層高度佔畫面（面積比例答不了「有多高」——滿版一行大字與散落小元件可以同一個百分比），以及**創意等級**（稽核歸檔本來完全沒存，當時只能從成品數招式件數、看有沒有傾斜反推）。兩欄並排才判得出是模型沒照做還是規格訂太大。🧪測試 8 題；2237 題全綠。⏭**待驗收**：prompt 改動要實機生圖才知道有沒有效，下次生成看後台那兩欄。| **使用者裁定的規則**：24H LIVE 只上傳一張「原圖放置」＝**原圖滿版放置**，**創意階梯只能改標題設計，不可重置原圖**。**程式現況（監督逐段核對）**：意圖是對的，但**只靠 prompt 講，沒有任何驗證**。單張 asis 時 `_yt_cover_background` 走 `compose.crop_background_16x9(raws[0])`——**純裁切、不打生圖模型，這一段正確**；接著 `ai_over_base` 成立（`title_mode=ai` ＋ asis≥1），底圖當**唯一附圖**送進 `_yt_cover_full_image`，並由 `with_base_image_note` 注入 `AI_TITLE_BASE_IMAGE_NOTE`：「Reproduce it as the picture: same subjects, same framing... Do not replace it with another scene, do not re-compose, re-crop, mirror or zoom it」。❗**但這條路是把整張畫布交給生圖模型重畫**——模型不可能逐像素複製，照片必然被重新生成一次，那段 note 只是**請求**，`generate_image_raw` 之後只有 `verify_output_aspect_ratio`（只驗比例），**完全沒有任何『底圖有沒有被改掉』的檢查**。使用者看到的就是原圖被重置。💡**修法方向（監督建議，待裁）**：原圖放置＋AI 標題時，不要讓模型重畫整張。甲｜**讓模型只產標題圖層**（透明／純色底），程式把它疊到未經觸碰的原圖上——創意階梯照舊只影響標題設計，照片保證是原檔；乙｜退回程式壓字（`title_mode=composite`），代價是標題失去設計感，等於 2026-09-13 那條裁決作廢，**不建議**；丙｜維持現況但加驗證（比對輸出與 base 的相似度，不像就重試）——治標且會多花錢。⚠**甲與現有的 `_cover_ai`／`_yt_cover_full_image` 兩段式架構衝突最小，但等於新增一種輸出模式，要先裁。****待實證**：王結玲那次的 `model` 欄是不是 `yt-cover:asis` 加生圖模型（＝確實走了 ai_over_base 這條），要撈後台確認。 ⚖ **2026-09-15 使用者已裁：甲，並補上張數分流規則**（原話）：**「如果使用者只有附一張圖 要求原圖放置 就只畫標題；如果超過兩張都是原圖放置 就需要 AI 生成融合」**。即：**asis = 1 張 → 照片是原檔，模型只產標題圖層，程式疊上去，一個像素都不准動**；**asis ≥ 2 張 → 允許 AI 生成融合**（多張本來就要合成一張，模型介入是必要的）。🚩**動工前要注意：≥2 張的現況跟「融合」不是同一回事。** 目前 `_yt_cover_background` 在 2–4 張時走 `compose.split_backgrounds(raws)`，那是**程式硬切左右／三切／四切**（2026-09-06 使用者裁決），切完再進 `ai_over_base` 交給模型重畫——**是「硬切格子再被重畫」，不是「融合」**。（live24 雙則那條才有羽化 `blend_backgrounds_lr`，但那條要兩個附圖位都有東西才成立。）**所以這次裁決對 ≥2 張的部分等於改了行為，不是照現況實作**，要先確認使用者要的是①保留現有的切格版面、只是接受模型重畫；還是②真的改成讓模型把多張照片融合成一個連續場景。**這一題監督不自行決定，實作前要問。** 1 張那一半的規則明確、可直接動工。 ❓**2026-09-15 使用者追問**：「原圖放置 只要選了 能不能要求模型真的不要改原圖的這張圖，其他地方或其他圖片有指定 AI 改都可以改，但使用者選了原圖放置**這張圖**就是不可以改」。**監督回覆（已查證）**：①**「要求模型」這個槓桿本身無效**——`AI_TITLE_BASE_IMAGE_NOTE` 已經寫到不能再死（same subjects／same framing／do not re-compose／re-crop／mirror／zoom），照樣被改。生圖模型是**重畫整張**、不是編輯圖層，prompt 永遠只是請求，沒有保證。②**保證只能來自程式後製回貼**，而且回貼可以**分區**，正好對應「只鎖這張、其他照改」：`asis` 那格的矩形回貼原檔，`aiedit`／實景那格不回貼，讓模型自由發揮。③**現成的保證路徑其實已經在跑**：不勾「標題由 AI 生成」（`title_mode=composite`）時，單張 asis 走 `crop_background_16x9` 純裁切、程式用 PIL 壓字，**照片一個像素都沒動**。問題**只出在 2026-09-13 那條裁決**（原圖放置＋AI 標題不再強制程式壓字）打開的 AI 標題分支。④**三個做法**：甲｜**差異遮罩回貼**——比對模型輸出與 base 的逐像素差異，模型真的畫了字的地方保留模型像素，其餘一律還原成原檔。AI 標題的設計感全留、照片保證原檔，是唯一能同時滿足兩邊的做法；風險是模型若整張色調位移會讓遮罩整片觸發，**必須加面積防呆**（改動面積超過門檻就判定失敗，退回程式壓字，退回不用再打模型、不花錢）。乙｜**限定字帶**——AI 標題只准畫在指定帶狀區，帶外一律回貼原檔；保證最強但砍掉標題落點自由度。丙｜**分區回貼**（多圖情境）——只回貼 asis 格，其他格不動；這條是甲／乙的正交補強，**多圖時必做**。⑤**監督建議：甲＋丙，乙不採**。⚠**順帶查到的文件缺陷**：`main.py:4326` 的註解寫「有任何 asis 就強制 composite（**真照不進生圖模型**）」，但 `main.py:5397` 的 `ai_over_base = has_asis and req.mode == COVER_MODE_AI` 表示**十點的 AI 模式同樣把真照送進模型**——註解已過時且與程式相反，會誤導後續實作，動工時一併改掉。 🔬**2026-09-16 已實拍量測（使用者授權，4 次封面生成）：門檻不是重點，這條路本來就走不通**。量法：把 `_restore_outside_protected_boxes` 換成**只記錄 change_ratio、門檻放到 1.01 永不擋**的版本——照原樣跑只會知道「它擋了」，看不到真實比例是 0.55 還是 0.95，而那才是訂門檻唯一需要的數字。底圖固定用 1920×1080 的現成成品（`曹雪卿重現_2_太空武器.png`），只變動創意等級。📊**change_ratio：十點滿版 創意0=68.1%、創意2=73.6%、創意4=77.8%；YT live24 創意2=93.2%**。**四次全部超過現行門檻 0.5，而且隨創意等級單調上升**——使用者裁定的「創意階梯只能改標題設計，不得重置原圖」，程式現況**完全做不到**。🔍**68% 不是量測誤差**：對照圖 `20260916_B55_對照_1_a原圖.png` vs `_b模型輸出.png` 可見模型把標題從上方搬到左下、三條內容列全刪、衛星重新構圖、地球重算——**它不是在加標題，是重做了一張**。🚫**調高門檻不是解法**：`20260916_B55_十點滿版_創意0.png` 是門檻放寬後硬回貼的結果，上緣看得到原標題的**殘影**（保護帶強制還原 base、下面卻是模型的新畫面），這種圖不能上鏡。✅**監督建議：`PHOTO_PROTECT_MAX_CHANGE_RATIO` 維持 0.5 不動**——它擋得對，該修的是本列既有的**修法方向甲｜讓模型只產標題圖層（透明底），程式疊到未經觸碰的原圖上**。那是唯一能同時滿足「標題有設計感」與「原圖一個像素都不動」的做法。⚠**現況等於功能不可用**：每一次 原圖放置＋AI 標題 都會被防呆擋下並回錯誤訊息。📁明細 `D:\Downloads\AICG\後台紀錄\20260916\B55_照片保護門檻實拍.jsonl`。 ⚖ **2026-09-16 使用者回應：「目前先這樣，還需要更多測試才知道怎麼修」**——本次**不動工**，`PHOTO_PROTECT_MAX_CHANGE_RATIO` 維持 0.5。**使用者傾向**：「原圖放置就不可開創意，但排列組合可能要測」，並表示要再思考。 ⚠**監督必須先講清楚一件事，否則這個傾向會落空**：**鎖創意等級解決不了這件事**。本輪實測 **創意 0 就已經是 68.1%**（創意 2＝73.6%、創意 4＝77.8%），**把拉桿鎖在 0 仍然遠超過門檻 0.5**。👉**創意等級只是讓情況更糟，不是原因**；原因是**只要走 AI 標題分支，整張畫布就會交給生圖模型重畫**。**「原圖放置不可開創意」若要真的成立，等價於「原圖放置 → 不進生圖模型 → 程式壓字」**，也就是本列的**修法乙（退回 `title_mode=composite`）**，而那等於推翻 2026-09-13「原圖放置＋AI 標題不再強制程式壓字」那條裁決。**要嘛接受標題失去設計感（乙），要嘛做修法甲（模型只產透明底標題圖層，程式疊到未動過的原圖上）——沒有第三條路能同時保住設計感與像素保證。** 📌**排列組合若要再測，建議的自變數不是創意等級，而是「AI 標題 開／關」×「asis 張數 1／2」**——前者才是真正的開關，後者關係到 2026-09-15 那條「≥2 張允許 AI 融合」的裁決。 🗳**2026-09-16 使用者傾向：考慮做修法甲（模型只產透明底標題圖層，程式疊上去），但先問「能不能成功？測過了嗎？」** ✅**答覆：沒有測過，一次都沒有。** 現有的 4 次實拍（本列上方）測的是**現行整張重畫路徑的 change_ratio**，完全沒有碰到透明底標題圖層這條路。`docs/plan-20260916-下一批全修.md:233` 的 Stage 6 計畫也只寫到「**模型若不能只回 alpha/title layer**，採 deterministic mask/diff」——**計畫本身就沒把握**，所以留了退路。 🔬**但監督已用零成本的方式查到關鍵事實（2026-09-16，打 OpenRouter 的 `/api/v1/images/models`，查模型能力不計費）**：**① `openai/gpt-image-2.5-sunburst` 官方支援 `background` 參數，enum 值是 `auto`／`transparent`／`opaque`**。👉**修法甲在 GPT 路徑上不是空想，透明底輸出是這個模型公告支援的能力**，不需要靠 prompt 拜託它「背景請留空」。**② `google/gemini-3-pro-image`（Nano Banana Pro）沒有 `background` 參數**（它只有 `resolution`／`aspect_ratio`／`n`／`input_references`）。👉**修法甲在 Gemini 路徑上做不到**，若採甲案，**原圖放置＋AI 標題必須限定 GPT**，或 Gemini 走 deterministic mask 那條退路。這是一個會影響 UI（供應商選單）的限制，要先想清楚。**③ 順帶查到 sunburst 的 `n` 最多 10**——同一次呼叫可以要多個標題圖層候選，對創意階梯可能有用（本列不展開，記著）。 🧪**建議的驗證方式（要花錢，需授權）**：**寫死 2～3 次呼叫**即可判定，不必跑完整管線。測項是三個，缺一不可：**(a) 回傳的 PNG 到底有沒有 alpha 通道**（程式一行就驗得出來，不是用看的）；**(b) 透明區域是不是真的乾淨**——模型很可能還是畫了一塊深色底或陰影，那樣疊上去照片一樣會被蓋住；**(c) 文字品質有沒有因為沒有背景參照而變差**（標題設計感正是這條路要保住的東西，如果為了透明底把字畫醜了，甲案就失去意義）。⚠**(b) 是最可能失敗的一項**，建議排最前面。 🔧**2026-09-20 已實作（AICG-wt-ladder worktree，0917 積壓批次）**：修法甲（透明底標題圖層）已接線，**但 (a)(b)(c) 三項驗證仍未打過任何一次付費生圖**——本輪任務明文禁止呼叫付費生圖 API，這裡做的是把程式路徑、三道閘、資料流接對，不是驗證模型真的會吐出乾淨的透明底。做了什麼：①`main.ImageGenerateRequest` 新增 `transparent_background` 旗標，`generate_via_openrouter`（送 `background: "transparent"`，只在 model 是 `openai/gpt-image-*` 時送）與原生 `generate_gpt_image`（`images.generate`／`images.edit` 都吃 `background` kwarg）都接了；②`editor_formats.AI_TITLE_LAYER_ONLY_NOTE`＋`with_title_layer_note`：新的 prompt note，跟舊的 `AI_TITLE_BASE_IMAGE_NOTE`（「重現整張照片」）語意相反，兩者互斥，`_cover_ai`（十點滿版）與 `_yt_cover_full_image`（YT 四版型）依 `protect_base and provider=="gpt"` 二選一注入；③`compose._overlay_title_layer_core`＋`overlay_title_layer_over_cover_band`／`overlay_title_layer_over_yt_cover`：疊圖核心，**三道閘**——(a) alpha 全不透明＝模型忽略透明背景要求，擋；(b) 保護區（頁首帶／Logo／角標，沿用既有的 `cover_title_band_top_ratio()`／`yt_cover_protect_boxes()`，不另訂座標）內只要有一個像素被畫過就擋；(c) 可疊區域非透明比例超過 `PHOTO_PROTECT_MAX_CHANGE_RATIO`（沿用既有門檻，不另訂數字）就擋，判定模型畫的是背景不是標題；三道閘任何一道沒過就丟 `ComposeError`，`_compose_error_status` 已擴充認得這三句訊息、轉成 400。**provider=="gemini" 完全不動**——維持原本的差異遮罩回貼（哪怕 2026-09-16 那輪實測已經量到這條路 change_ratio 常態超標、功能等同不可用，這裡沒有更好的替代，只是沒有拿掉）。**驗證缺口**：(a)(b)(c) 全部只驗了「程式邏輯本身」（用 Pillow 合成的假透明圖層跑三道閘），**沒有驗過真正的 gpt-image 模型收到 `background=transparent` 後實際回什麼**——本列原本列的三項付費驗證需求依然成立、依然沒做，需要使用者另外授權付費呼叫才能知道甲案能不能真的成功。**待裁清單（不得自行動工，已寫進本次回報）**：Gemini 路徑要不要也想辦法補（deterministic mask 退路，或原圖放置＋AI 標題直接限定只能選 GPT，會動到 UI 供應商選單）；asis ≥2 的「AI 融合」語意（現況仍是「硬切格子再被重畫」，跟 2026-09-15 那條「融合」裁決是否算同一件事，帳本上面那段已經點過，這次沒有再往下動）；三道閘任一失敗時要 400 讓使用者重試（目前做法，跟既有差異遮罩防呆一致）還是悄悄退回程式壓字（另一個選項，這次沒有採用）。 🔍**2026-09-20 獨立複查補洞（exp `ae75f9d`）**：使用者指定由 `gpt-5.6-sol` 對「B55 三道閘」與「今晚所有刪除行」做獨立審查（理由：驗收與編排同一個 session，不算獨立）。抓到**兩個洞，team-lead 都已實測重現，不是理論風險**：**洞一｜判定標準與疊圖標準不一致**——三道閘一律用 `alpha > TITLE_LAYER_ALPHA_THRESHOLD`（16）判定「模型有沒有畫」，但 `alpha_composite` 吃的是**原始 alpha**。一張全畫布 `alpha=16` 的薄層三道閘全過（處處算「沒畫」），疊圖時卻以 16/255 把整張照片染色，**連保護區都染**——實測紅底 `(255,0,0)` 疊上 `(0,0,255,16)` 得到 `(239,0,16)`。這**直接打破這條路唯一的賣點**（「alpha=0 的像素定義上就是模型沒動過」）。而且低 alpha 不是只有惡意情境才有：layer 尺寸與 base 不符時的 LANCZOS 縮放本來就會在字的邊緣內插出 1~15 的殘值。**修法**：疊圖前先 `ImageChops.multiply(alpha, painted)` 把低於門檻的 alpha 真的歸零，兩邊語意對齊。**洞二｜沒有一道閘管「畫太少」**——前三道全是「畫太多／畫錯地方」的上限。模型回一張**完全透明**的圖時 (a) alpha 最小值 0、(b) 沒有畫過的像素、(c) 比例 0，**三道全過**，程式**成功回傳一張跟原圖一模一樣、一個字都沒有的成品**。那比 400 更糟：400 使用者看得見，靜默的無字成品會被當成品拿去上鏡。**修法**：新增第四道閘（可疊區一個像素都沒畫就擋），`_compose_error_status` 同步認得新訊息、維持回 400。⚠**新增待裁（不得自行決定）**：第四道只擋「**完全沒畫**」這個零歧義的情況；**「畫得極少」**（例如只吐出幾十個像素的殘渣）同樣會產生近乎無字的成品，但要擋它就得訂一個「標題至少該占可疊區多少面積」的門檻，**本 repo 沒有現成數字可沿用，另訂數字屬裁決事項**。📌**同一輪複查另外點名一個覆蓋缺口，已一併修掉**：`tests/test_b55_transparent_title_layer.py` 的 YT 端點測試原本只斷言 `status_code == 200`、fake 完全不看 request，等於 YT／gpt 這條路**漏設 `transparent_background` 或漏換專用 note 都不會變紅**；而那 5 支既有測試又剛好在同一次改動裡被切到 `provider=gemini`，**新的預設路徑（`YtCoverRequest.provider` 預設就是 gpt）因此失去端對端覆蓋**。已改成逐版型（news／hourly／hot／live24）檢查旗標與 note，四個版型全過——順帶證實接線本身是對的，先前只是沒人驗。🔎**洞三｜prompt 自相矛盾（同一輪複查第四項，2026-09-20 當晚已修）**：修法甲把程式拼好的底圖當成唯一附圖送進模型，兩個呼叫端（`main._cover_ai`、`main._yt_cover_full_image`）都標成 `purpose="aiedit"`，於是 `apply_user_references_to_image_request()` 會把 `USER_REFERENCE_AIEDIT_RULES` 接在 prompt 尾巴——那段第一句是 **Re-draw that same picture**，而同一份 prompt 前面剛被 `with_title_layer_note()` 注入 **Do NOT reproduce, redraw, repaint or recreate that photograph**。兩句正面對撞還同時送出，等於把修法甲要擋的「模型重畫整張照片」又請回來一次；四道閘是最後的攔截網，不該拿來扛 prompt 自相矛盾（真撞上只會變成使用者看到 400，而不是拿到好成品）。**修法**：新增**內部用途** `purpose="titlelayer"`（`news_prompt.USER_REFERENCE_TITLE_LAYER_RULES`），措辭改成「附圖只當位置與配色參考、輸出是透明圖層、照片由程式疊」；前台下拉不含這一項（`editor_formats.REF_PURPOSE_ORDER` 不動，已加測試確認 `app.js` 裡沒有這個字），`provider=="gemini"` 那條路確實要重畫整張，維持 `aiedit` 不動。另把 `apply_user_references_to_image_request()` 末段的「有上傳就不標示意圖」override 一併認 `titlelayer`，維持修法甲上線前的行為（該段內文會引用封面 prompt 裡不存在的 REAL-WORLD ACCURACY／NAMED REAL PERSON 區塊，注進去只是懸空指涉）。新增 5 題守門（`AttachedImagePurposeContradictionTests`），其中一題先證明「兩段措辭真的互斥」這個前提成立，一題釘住 gemini 路徑不可被一起改掉。　📐**2026-09-21 第二輪：招式從源頭拿掉（PR #39）**。使用者實拍創意 1–4 各一張（dev 後台），量到圖層高度佔畫面 **45.0／50.0／64.9／88.4%**（規格 18／24／30／36%），可疊區 32.2／36.5／55.7／**72.0%**。使用者判定 1、2 偏高可接受，3、4 明顯太高，原因是「多加設計元素裝飾把整體墊高了」，裁決「只要設計標題字」。⚠**急迫性不在『太高』而在守門**：創意 4 可疊區 72.0%，c 閘上限 75%，再多一件招式就會被擋下變成生圖失敗。招式件數（`cover_accessories`，0／1／2／3 件）正是 L2→L3→L4 的爬升來源。修法＝`cover_design_brief()`／`yt_design_brief()` 各加 `layer_mode`，True 時整段 supporting artwork **不發**——**從源頭不發，不是另外加一句「不要畫」**，後者正是當天已踩三次的「prompt 叫它畫、守門當它竄改」同型矛盾。順帶拆掉兩處會抵銷本次修改的舊措辭：`AI_TITLE_LAYER_ONLY_NOTE` 原本明文許可 plates／accent shapes／wordless devices（正好許可了要拿掉的東西），以及尺寸守則裡「招式擺在標題旁邊、不可疊在下面」（招式都不畫了，留著等於「你可以畫、只是要擺旁邊」的許可）。純 AI 那條路 **byte-identical 有測試釘住**（同級實拍 38%、兩張臉完整露出）；YT 四版型跟著改但**沒有實拍證據**（使用者只測十點），跟改是因為共用同一段 note 與守門。🔬**這只拿掉一根槓桿**：L1 完全沒有招式仍量到 45%，塊高的『底』超標 2.5 倍是另一回事（傾斜與字級落差倍數），**預期 L4 落在 60% 上下、不會回到 36%**，待實機重生創意 3、4 驗收。測試 2243 題全綠（＋6）。📐**2026-09-22 高度上限（使用者裁定「壓縮階梯 44/46/48/50」＋縮放下限 0.6）**：0921 那刀（招式從源頭拿掉）部署後實測五張（dev 後台 0922 00:50~00:54，全部原圖放置＋AI 標題）：十點 L3 **61.0%**／L4 **69.0%**，YT live24 L4 **46.9%**／hourly L3 **53.9%**／news L4 **59.0%**。🔬**招式那刀只解得了最上面一級**：L4 88.4%→69.0%，L3 卻是 61.1%（0921 22:17，招式 2 件）→ 61.0%（0922 00:50，招式 0 件），**動都沒動**。🔬**prompt 這根槓桿已證偽**：十點 prompt 要 18/24/30/36% 畫出 45/50/61/69%，YT 要 32/36/40/44% 畫出 47~59%——**十點要得比 YT 少（18–36 對 32–44）卻畫得比 YT 多（45–69 對 47–59）**。模型讀得懂「L4 要比 L1 張揚」這種相對語氣，但絕對百分比根本沒進去。所以 `COVER_TITLE_BRIEF_SPECS`／`YT_BRIEF_SPECS` 那些塊高數字不要再當成成品的預測。⚙**修法＝程式縮，不是再調 prompt**：`TITLE_LAYER_MAX_HEIGHT_RATIOS = {1:.44, 2:.46, 3:.48, 4:.50}`，量到的 `painted_bbox_height_ratio` 超過上限時把**整張圖層等比例縮**到上限之內再疊，錨點是 bbox 的**底邊中點**（標題坐在下半部，底邊＝視覺基線；錨畫布中心的話字會往中間飄、正好飄向照片主體）。縮放下限 `TITLE_LAYER_MIN_HEIGHT_SCALE = 0.60`——要縮到 0.6 倍代表原始高度超過 73%，使用者裁定寧可那一張高一點也不要把字縮到看不清楚。🔢**數字怎麼來的**：拿使用者自己驗收過的話當刻度——十點 L1 45.0%／L2 50.0%「偏高但可以接受」，L3 61.0%／L4 69.0%「誇張、完全遮住原圖」，YT 46.9~59.0%「勉強可以接受的邊緣」。50% 正好落在兩側之間。保留 44/46/48 三階是為了不把創意階梯的高低差抹平（單一 50% 會讓 L2 L3 L4 一樣高）。🛡**構造上不可能造成失敗**：縮排在四道閘**全過之後**（原始碼順序有測試釘住），而且縮完**重量一次**——踩到保護區或掉到 d 閘下限就**退回那張已經過閘的原圖層**，不是退回程式壓字。縮小不是構造上安全的：圖層由分開兩塊組成時往中心縮會蓋住中間的空隙，而 hourly 的日期牌就在畫面中段。c 閘不用重驗（縮完只會畫更少）。🔍後台診斷同時印**縮完**與**原始**兩個數字（只印一個都對不上：只印縮完的看不出模型本來畫多大，只印原始的對不上成品）。VERSION 不跳號，這一行是下次實測唯一能證明新版上線的東西。🧪測試 16 題（表的形狀與單調性／等級夾取／低於上限位元組不變／超過就縮到上限／同張圖 L1 比 L4 縮更多／下限咬住時仍高於上限／預設 0＝不縮／底邊中點錨點／踩保護區退回不縮／縮完掉到下限退回不縮／閘門順序／診斷字串／兩個呼叫端都接上等級）。⏭**待實機驗收**：重生十點創意 3、4 與任一 YT，後台該出現「已縮 0.xx×，原 XX%、上限 XX%」。等比例縮會讓字**同時變窄**，成品可能變成「標題小一塊浮在下半部、周圍空掉」——這點測試證不了，只能實拍。✅**2026-09-22 實機驗收通過**（PR [#40](https://github.com/TVBS-AI/aicg-champion-daihsuan/pull/40)，merge `0cfa863`，2026-09-21T17:16:15Z）：使用者重測後回覆「驗收也 ok 了」，等比例縮沒有造成先前擔心的「標題縮成一小塊浮在下半部、左右空掉」。🐛**順帶抓到一個流程缺口**：`5c456aa` 之後 **14 個 commit 全部漏跳版本號**（PR #38／#39／#40 三批全中），`index.html` 右上那顆標籤一直停在 `260921-04`。VERSION 是**會顯示在畫面上給人看的**（`tests/test_app_version.py` 釘住它必須與 index.html 一致），不是純內部檔案；2026-09-09 使用者已裁定「每次改動（不是佈署）都要更新版本號」。補在 PR [#41](https://github.com/TVBS-AI/aicg-champion-daihsuan/pull/41)（`260922-01`，merge 2026-09-21T17:26:11Z），做法是跑 `scripts/bump_version.py`。**教訓**：「VERSION 不會自動跳號」這個事實先前被我拿去推出「所以不用管它」，正確的結論是「所以要手動跳」——commit 前順手確認 VERSION 有沒有跟著改。 | ⚖**2026-09-20～21 兩項裁決落地（`3f2afda` 起）**：**(一)｜閘門失敗改成退回程式壓字**——理由是 `provider="gpt"` 已經是編輯身分的預設路徑，閘門一擋使用者只能空手重按。退回走 `title_mode="composite"` 那條路（照片仍是使用者那張未經模型的原圖，標題由 Pillow 畫），並在回應 `notices` 明講「版面與字體跟 AI 標題不一樣」，**不做靜默替換**。兩個呼叫端：`main._cover_ai`（十點滿版，直接呼叫 `compose.compose_ten_cover`；它自己會貼標頭／Logo／圓章，所以**不能**再套 `_post_paste`）與 `main.editor_yt_cover`（把 `background` 換回 `base`、`ai_title` 關掉，讓既有的 `draw_titles=not ai_title` 改由 Pillow 壓字）。順帶修掉兩件會對觀眾說謊的事：退回後 `background_is_ai` 翻成 `False`（照片沒經過模型，標「AI示意圖」等於說這張是 AI 生的）、`title_mode` 照實回 `"composite"`。**(二)｜第四道閘加下限**——先裁 1%，2026-09-21 獨立複查（gpt-5.6-sol 第三輪）用**實際字形**量過之後**改判 0.1%**：可疊區（畫布扣保護區）十點滿版 1,833,600px、YT news 1,399,680px、hourly 1,533,936px、hot 1,708,800px、live24 1,581,654px；而**十點一字標題 9,143px＝0.50%、二字一行 18,145px＝0.99%、YT news 每行一字 0.98%、細字重無描邊單字 3,464px＝0.19%**——這些全是合法輸入（兩個 request 都允許一字標題），在 1% 下會被**誤擋**。正常長度標題離下限很遠（十點兩行 5.91%、YT 4.83~13.46%）。0.1% 下最瘦合法案例仍有約 1.9 倍餘裕，殘渣（測試用 60×20＝1,200px＝0.065%）仍擋得住。📌 我給使用者的選項說明「一行大標題遠超過 1%」**只對長標題成立**，短標題講錯了，已更正並重裁。 🔎**同一輪複查另抓到一個我自己引入的缺陷，已修**：退回程式壓字時**沒有補打斷句**——端點入口的 `apply_title_break_hints` 帶的是 `composite=False`（B75：AI 標題模式下 compose 不壓字、斷句沒人讀），所以走到 fallback 時**詞組邊界是空的**，`compose._split_line_near_middle` 只能按規則硬切，會把詞腰斬（複查的例子：「全球半導體供應鏈重新洗牌」切成「全球半導體供／應鏈重新洗牌」）。修法：只在真的退回時補打一次 `composite=True`，正常路徑不受影響（live24 單行不拆，本來就不打）。另補一題**逐像素**確認退回來的成品真的長出標題——原本那幾題只驗200＋notice＋欄位，漏掉 `ai_title=False` 也會全綠卻輸出無標題的原圖。 ✅**複查明確判定無問題的部分**：十點不套 `_post_paste` 的判斷正確（`compose_ten_cover` 已自行貼標頭／Logo／日期／圓章／來源標籤，再套會重貼）；YT fallback 確實走回原 composite 的各版型 composer，沒有固定元素漏貼或重貼；`title_layer_fallback` 只可能在「單則＋單張 asis＋gpt 透明圖層失敗」時為真，`dual`／recompose 不會誤用；退回後貼「畫面來源」是合理的來源歸屬（失敗的圖層沒有進入成品，裁切／縮放與程式壓字不會讓來源宣稱失真）。
| B53 | **YT／十點封面的「補畫面描述」只餵一句標題、不餵新聞原文，導致認錯同名人物並拼錯英文名，肖像查照因此失敗** | 由 B45 根因調查獨立出來（2026-09-15） | ✅ **2026-09-16 前端那一半也完成（exp `283b7fa`，Stage 5b）**：`tenCoverFields()`／`ytCoverFields()` 與十點生成路徑把 `coverNewsText`／`ytCoverNewsText` **原樣**送為 `news_text`，不摘要、不截斷、不抽人名｜🔶 **2026-09-16 已實作（exp `e0f4df6`，Stage 5a `7d4748b`）**：`TenCoverRequest`／`YtCoverRequest` 加 `news_text`，推導材料同時含標題與原文、兩者都標成 source material｜⬜ **下一波優先要修**（2026-09-15 使用者裁定：「這個也列入下一波」，緊接在陳一新查照真因查證之後）｜原列 🌊第二波，已提前 | `derive_yt_cover_plan` 給模型的 material 只有 `Headline: <標題>`（＋分行提示、＋使用者指令），**沒有任何新聞原文**。實測後果（標題「陳一新警告人工智慧威脅政權」）：①模型把人認成淡江大學教授陳一新（visual 寫「一位年長學者⋯背景虛化的書架」），不是中國國安部長；②`portrait_subjects_en` 回 `'Yi-hsin Chen'`（臺灣拼法），**查維基查無**；改用條目名 `'Chen Yixin'` 立刻查到正確的人。**這是 B45『捏造臉孔』的上游**——查照失敗才會退回不生成臉孔，而 visual 又命令要一張臉。⚠`YT_COVER_DERIVE_SYSTEM` 第 3 條已經寫明「empty string ONLY when you genuinely do not know it；never guess a spelling you are unsure of」，**模型並沒有遵守，它猜了一個**。**候選方向（待查，不要直接改 prompt）**：①把新聞原文一起餵進去（前台這條線使用者本來就只貼標題，可能根本沒有原文可餵——要先確認）；②查照端對同一個人多試幾種拼法；③英文名拼不確定時寧可回空字串並明確走「不畫臉且 visual 不准描述臉」。 ⚖❌**2026-09-15 使用者提出防呆方向，監督查證後不成立（但查出真因）**。使用者原話：「陳一新本來就是中文名稱，用英文名稱當然會有機率找錯人，應該要防呆，**如果英文找不到就找中文**」。**查證結果：這個防呆已經存在，而且順序比使用者要求的更早——中文本來就是第一順位。** `photo_lookup.find_reference_photo()` 的候選順序是 `candidates = [name, *alt_names]`，`name` 就是中文名，`DEFAULT_LANGS=("zh","en")` 又讓**每個候選都在中英兩個維基各查一次**；函式 docstring 也明寫「中文優先（臺灣新聞的人物多半中文條目較貼近本地認知）」。**所以不是『先英文再中文』，是『中文查完查不到才輪到英文』。** **本機實測（不花錢，維基 API）**：`陳一新`＋無英文名 → **查無**；`陳一新`＋`Yi-hsin Chen`（系統實際產出的拼法）→ **查無**；`陳一新`＋`Chen Yixin` → **找到**。逐語系拆開看：`zh 陳一新 → 無`、`en 陳一新 → 無`、`zh Chen Yixin → 無`、`en Chen Yixin → 有`、`zh/en Yi-hsin Chen → 無`。🎯**真因查到了：中文維基的「陳一新」是消歧義頁，沒有首圖。** 直接打維基 API 確認：`zh.wikipedia` 的 `陳一新` 重導向到 `陈一新`，該頁 `pageprops` 帶 `"disambiguation":""`、**完全沒有 `pageimages`**，`_lookup_lang` 因此回 `None`。**中文這條路在這個人身上是死路，補幾次都一樣。** 💡**但使用者的直覺指到了一個真的缺口，只是位置不同**：**消歧義頁＝這個名字在中文世界本來就有多個人**（淡江教授／中共國安部長），而程式現在只是「查不到就算了」，**完全沒有利用這個訊號**。同一份資料其實已經告訴我們「這個名字有歧義、需要更多上下文才能定人」，那正是 B53 主症狀（只餵標題導致認錯人）的客觀證據。**建議修法（待裁）**：①**B53 本體照修**——餵新聞原文給 derive，英文拼法才會對（`Chen Yixin` vs `Yi-hsin Chen` 的差別就是有沒有上下文）；②**偵測到消歧義頁時不要沉默**，記進紀錄並視為「此人需要上下文」的明確訊號，而不是與「查無此人」混為一談；③**不要加「中文全文搜尋」當退路**——`find_reference_photo` 的 docstring 已經記載實測結果：4 個譯名裡 2 個搜到完全不相干的條目（阿拉奇→阿布拉莫維奇、巴薩尼→威尼斯商人），**抓錯人比查不到嚴重得多**，這條路先前已經被否決過，不要重蹈。 |
| B56 | **使用者附圖上本來就有的文字，現況被硬性要求「不要複製」，等於一律被改掉／變亂碼，而且使用者無法控制** | 2026-09-15 使用者主動提出（監督當場查證確認為真） | ✅ **2026-09-16 已實作（exp `e0f4df6`，Stage 7 `bf1fa6e`）**：aiedit／fusion 規則拆成 PRESERVE-EXISTING／DO-NOT-INVENT OR REUSE／EXPLICIT-REMOVAL ONLY 三條。**保留條款刻意把來源文字寫成 “requested content”**，否則會被 B61 的出口鐵律（「不得畫 prompt 沒要求的文字」）反過來蓋掉。⚠**OCR／視覺保留率仍待人工實拍驗收**｜⚖ **2026-09-16 使用者已裁：要做**——規則要區分「保留附圖上本來就有的字」與「不要憑空抄新字」，不再一律禁止複製｜⬜ **待查怎麼處理**（2026-09-15 使用者：「補一筆紀錄要查怎麼處理」）｜落地方式需一條 D 裁決 | **使用者原話**：「我先前沒有想過，使用者附圖的圖上本來有文字，這些文字可能被 AI 改圖改掉。我的想法是，讓使用者在指令欄補充：圖要怎麼放置、要怎麼 AI 改、字可不可以改、還是去除文字只要圖…等等」。**監督查證（`news_prompt.py`）——問題比預期嚴重，現況不是「可能被改掉」，是「規則叫模型一定要拿掉」**：①`USER_REFERENCE_AIEDIT_RULES`（單張）與 `USER_REFERENCE_AIEDIT_FUSION_RULES_TEMPLATE`（多張融合）都硬寫 `Do not copy readable text or brand marks visible inside the attached image`——**AI改圖一律去字，寫死在 prompt 裡，使用者沒有任何開關**。模型做不到「乾淨去字」時，結果就是亂碼或自創文字，正是使用者看到的現象。②對照組：`USER_REFERENCE_ASIS_MULTI_RULES_TEMPLATE`（原圖放置）反而明文允許保留——`Any brand marks, logos, readable text or real human faces already present in the attached images may remain exactly as supplied`。**同一張圖選 asis 就留字、選 aiedit 就去字，差異是硬編的、前台沒說明。**③**使用者構想的「寫在指令欄」現況會撞到兩道牆**（都在 `USER_REFERENCE_AIEDIT_INSTRUCTION_TEMPLATE`）：**牆一**——那段明文把指令欄定義成「`a direction about what to change in the picture, never words to render — do not write any of it, or any translation of it, anywhere in the image`」，所以寫「保留圖上的中文字」極可能被理解成「不要畫這幾個字」，**語意正好相反**；**牆二**——那段的權限只凌駕「the content does not change」**那一句**，並明文寫 `It does not relax the brand-mark, human-face or NAMED REAL PERSON rules above`，而去字那條是與商標同一行（`readable text or brand marks`），**指令欄吃不動它**。👉**結論：照現況直接叫使用者在指令欄寫，不會生效，必須改 prompt 架構。** 💡**待裁的落地方式（監督列，不自行決定）**：甲｜**在用途下拉旁加一個「圖上文字」小選單**（保留原字／去除文字／交給 AI 判斷），選項直接對應三段互斥的 prompt 區塊，**明確、可測、不靠模型解讀自然語言**；乙｜**純指令欄**，照使用者原構想，但要把上面兩道牆拆掉（指令欄改成可凌駕去字規則、並區分「要畫的字」與「對既有字的處置」），**彈性最大但最難驗收，也最容易被模型誤解**；丙｜甲＋乙併用，下拉定調、指令欄補特例。**監督傾向甲**——這一題的選項本來就只有三種，用自然語言表達反而失真。⚠**還要查的**：①「去除文字只要圖」在生圖模型上到底做不做得到（模型是重畫，去字區塊會被它自己重新編造，需實拍驗證，屬**要花錢的驗證**）；②`asis` 若採 B55 的回貼方案，圖上原字自然全保留，**那條路不需要這個開關**——所以本條的適用範圍只有 `aiedit`／`scene`，動工前要先把範圍講死；③前台現況完全沒有向使用者說明 asis 留字、aiedit 去字的差異，屬獨立的說明缺陷，是否併入本條待定。 |
| B57 | **字數拉桿在正式站的消化模型上疑似大幅失效：選「字超多」只拿到約「字少」的份量** | 2026-09-15 使用者看 F38 成品時當場質疑「應該要測字超多，不是測字少」，追查後浮出 | 🔶 **2026-09-16 已實作（exp `e0f4df6`，Stage 3 `622f570`）**：`density_point_bounds()` 單一數字來源＋後端塊數防呆。⚠**B60 的「連打 5 次全落在 ±1」驗收還沒跑**｜⚖ **2026-09-16 使用者已裁：兩個一起做**（prompt 下限＋後端塊數防呆，一次到位）。⚠**會改 prompt 文字＝撞凍結快照，動工前要先要到重凍預算**｜⬜ **根因已確認，待裁修法**（2026-09-15）｜真因＝`maximum` 只解除上限、沒有下限，**10 次分布證實兩個角色都會隨機塌陷、機率相近**｜🔻**已撤回「編輯版系統性偏少」與「今天的回歸」兩個結論——都是小樣本假象**｜另發現乙型塌陷（內容在但不拆塊）｜**監督建議先做後端塊數防呆（不撞凍結快照），prompt 下限併入下次重凍** | **事實（皆有原始檔佐證，非轉述）**：本機用 `density="maximum"` 打 `/api/generate`，同一篇 D16 的 276 字颱風稿，消化端實際產出 **6 塊／內文 49 字**（含標題 61 字）——標題「銀河颱風逼近海上警報生效」12 字，六塊小標分別 9／10／8／8／7／7 字。原始檔：`D:\Downloads\AICG\後台紀錄\20260915\解析度對照\reso_digest_payload.json`＋`reso_digest_response.ndjson`。**對照 D16 同一篇稿的實拍**：字極少 約 41 字、字少 約 39 字、字多 131 字、**字超多 123 字**。👉**現在的「字超多」只拿到 49 字，落在 D16「字極少／字少」那一檔的量級，不到字超多的四成。** 若成立，等於**頂端兩檔實質塌陷**，與 D16 當初「內文兩軸級距成立」的結論相反。**request 層已排除**：`density` 是 pydantic Literal，值不合法會 422，不存在「靜靜落回 standard」；payload 實檔可驗。🚩**但監督必須先講一個還沒排除的干擾項，不要急著當成模型缺陷**：**這次與 D16 有兩個差異，不是一個。** ①消化模型：D16 是 `gpt-5.5`（`DIGEST_BACKEND=native`），現在 `.env` 是 `DIGEST_BACKEND=openrouter`／`DIGEST_MODEL=anthropic/claude-sonnet-5`（**與正式站相同**，所以若成立就是線上現行行為）；②**角色**：D16 那輪是**記者版**，這次 payload 是 `"role": "編輯"`。**編輯版本來就可能因為版面較小而給較少文字**，這個差異足以單獨解釋一部分落差，**在排除它之前不能斷言是模型問題**。✅**下一步（便宜、決定性、不用生圖）**：用**現行模型**、**同一篇 276 字颱風稿**、**記者版**、`density=maximum` 只打消化端一次，數塊數與字數。與 D16 的 123 字直接可比，**只花消化 token、不花生圖的 $0.04**。若記者版仍然只有 50 字上下 → 確認是模型／規則失效，B57 成立；若記者版回到 120 字上下 → 落差來自角色，B57 改成「編輯版的字超多偏輕」另議，嚴重度大幅下降。💡**可能的關聯（未證實）**：0911 查過「思考量隨約束數而非字元數成長」，以及 B48 的「思考吃光 completion 預算」——若 claude-sonnet-5 把預算耗在思考上，正文自然被壓縮。**這條只是候選，不要當成結論。** ✅**2026-09-15 干擾項已排除，角色是主因，模型無罪；但監督再往下挖一層，找到真正的根因。****決定性對照（只打消化端、沒生圖）**：同一篇稿、同一個模型（`claude-sonnet-5`）、`density=maximum`，**唯一改動是角色**——**記者版 8 塊／138 字**（逐塊：18／15／25／12／14／19／18／17），**編輯版 6 塊／49 字**。記者版不但回到 D16 量級，還比 D16 的 123 字略多。👉**所以 `claude-sonnet-5` 沒有把字超多做壞，D16 的級距在記者版上依然成立。**證據：`b57_digest_payload.json`／`b57_digest_response.ndjson`／`b57_char_count_report.txt`。🎯**但「編輯版就是比較輕」不是可以接受的答案——監督把兩個角色的消化 prompt 逐段 diff，找到真正的根因，而且它同時傷到兩個角色**：①先排除兩個常見嫌疑——`editor_formats.digest_rules('default', role, ...)` 對**兩個角色都回空字串**（本機實跑確認），`EDITOR_LAYOUT_SAFE_AREA` 與 `REPORTER_LAYOUT_SAFE_AREA` 也近乎逐字相同（差別只有「side panels／data charts」、多兩個禁用詞、以及提到 `<蓋章>`），**都不足以解釋三倍的落差**。②真正的差異在 `MAXIMUM_DENSITY_RULES` 本身，**它的措辭是隨角色變的**：**記者版**寫 `LINE LENGTH: There is no per-line character cap at this setting`／`TOTAL LENGTH: There is no total-length cap at this setting`；**編輯版**寫 `LINE LENGTH: The 「每行不超過 15 字」 limit above is LIFTED`／`TOTAL LENGTH: The 「總字數嚴禁超過 150-180 個字」 target above is LIFTED`。🔑**兩者都只做一件事：解除上限。沒有任何一句設下限。**「字超多」對模型而言只是「你可以寫很多」，**不是「你必須寫很多」**——模型寫 49 字完全沒有違規。而編輯版那個 `is LIFTED` 的說法又比記者版的 `There is no cap` 更弱：它把注意力導回被解除的那個數字（15 字／150-180 字），等於在提醒模型原本的天花板長什麼樣。**再疊上編輯版「scaled down／thick empty margin」的版面語氣，模型自然往少的方向收。**💡**修法方向（監督建議，待裁）**：**字超多與字多都要給下限，不能只解除上限。**例如明寫「至少 N 塊、內文總字數不少於 M 字」，並把編輯版那句 `is LIFTED` 改成與記者版同樣的肯定句式，不要再回頭引用被解除的數字。⚠**動工前必須注意兩件事**：①這是 prompt 文字，**會撞凍結快照**（`tests/test_reporter_prompt_frozen.py`），本波沒有重凍預算，要先確認誰來重凍；②**不要只改編輯版**——記者版同樣沒有下限，只是這篇稿剛好寫得夠多，換一篇稿一樣可能縮水，**兩個角色要一起修**。📌**對 D16 的連帶影響**：D16 說「內文兩軸級距成立」，但那是**記者版＋`gpt-5.5`** 的結論；**編輯版從來沒被量過**，而正式站的編輯版才是多數使用者在用的那條。D16 要不要補量編輯版，併入 D16 一起裁。📌**對 F38 的連帶影響**：要真正測「解析度能不能救錯字」，**素材條件要用記者版**（那才會產出 8 塊／138 字的密集版面）；用編輯版永遠測不到錯字出現的條件。 🔻**2026-09-15 第三輪（5+5 分布，零成本）：撤回「編輯版系統性偏少」這個結論——那是小樣本假象。但根因診斷（只解除上限、沒有下限）不但沒被推翻，反而被這組數字證實得更強。****觸發原因**：測試在舊 commit `4c11bd4` 上用同一份 payload 打出 **8 塊**，一度判為「今天的回歸」。**監督否證**：`git diff 4c11bd4..HEAD` 逐檔確認 `main.py`／`news_prompt.py`／`editor_formats.py`／`request_log.py` **全部空 diff**，改的只有 `MASTER`／`VERSION`／`app.js`＋`index.html`（分頁隱藏那 9 行）＋一份 docs。**消化端程式碼完全一致，8 vs 5 不可能是版本差異。** 已擋下 `git bisect`（會是純浪費）。**改做分布測試**：同一份完整 14 欄位 payload、同一篇 1464 字稿、在 HEAD 上，**編輯版 `maximum` 連打 5 次、記者版 `maximum` 連打 5 次**，只打消化端、不生圖。🔑**結果（10 筆全為 HTTP 200 有效樣本，非挑選；原始檔在 `D:\Downloads\AICG\後台紀錄\20260915\密度對照\`，`b57_dist_summary.txt` 有整理）**：**編輯版：8／8／8／4／8**　**記者版：1／1／8／8／8**。👉**兩組分布高度重疊——兩邊都是 5 次裡 3–4 次落在 8 塊。不是「編輯版系統性偏少、記者版穩定」這種乾淨的角色差異。****所以本條先前那個「決定性對照」（記者 8 塊／138 字 vs 編輯 6 塊／49 字）站不住——那是各抽一次，剛好一次抽到分布高峰、一次抽到塌陷，被單樣本騙了。****同理，舊 commit 那個 8 也只是落在高峰，不是版本差異的證據。**🎯**修正後的結論：`maximum` 密度下，兩個角色都有相近機率塌陷，只是塌陷的樣子不同。****甲型（編輯版那個 4）＝內容真的變少**，模型只給 4 項；**乙型（記者版那兩個 1）＝內容都在、但格式塌陷**，整篇被擠進同一個內文小標欄位、中間夾原始換行，沒有拆成 8 個獨立的 `[內文小標]` 標籤。**乙型不是「字少」，是解析層拿不到分塊**，前端會渲染成一大坨——嚴重度不比甲型低。✅**根因診斷維持不變，而且更站得住**：`MAXIMUM_DENSITY_RULES` 只解除上限、沒有任何下限，**兩個角色都沒有下限**——所以本來就預期「兩邊都會隨機塌陷」，而不是「某一個角色固定較弱」。**這正是這組分布看到的形狀。**「編輯版 `is LIFTED` 措辭較弱」這一點降級為**次要嫌疑、未證實**（10 筆看不出編輯版更容易塌陷），修法時可以順手改成肯定句，但**不要當成主因**。💡**修法方向（更新後，待裁）**：①**給下限**——明寫「至少 N 個 `[內文小標]`、內文總字數不少於 M 字」，兩個角色一起加；②**格式硬性要求**——針對乙型，明寫每一項必須各自帶一個 `[內文小標]` 標籤、不得用換行代替；③**後端補防呆**——消化回來若塊數低於下限或只解析到 1 塊，直接重打一次（現成機制：消化端本來就有 `attempt N/5` 重試），比改 prompt 更可靠、且不撞凍結快照。**監督建議 ③ 優先做（不撞快照、可立即上線），①②併入下一次重凍一起改。**⚠**測試回報的操作面事實（採信，已由程式碼佐證方向）**：背靠背連打會觸發 OpenRouter 503 逾時，需重試才拿得到 200——**這與 B59「背景執行失敗率較高」可能是同一個病**（上游逾時走進串流中斷分支），查 B59 時要把這條當已知事實一起看。📌**連帶修正**：本條先前寫給 F38 的建議「素材條件要用記者版才測得到密集版面」**作廢**——記者版一樣會塌陷（10 次裡 2 次），**F38／B58 這類需要密集版面的測試，正確做法是每次生圖前先驗塊數，不足就重打消化，不要靠選角色。**📌**對 D16 的連帶影響**：D16 的「級距成立」是單樣本結論，**在這個分布之下同樣不可靠，要不要重量併入 D16 一起裁。**✅**成本**：本輪 10 次全是消化端，**沒有生圖、沒有 $**。 🔬**2026-09-16 跨模型對照帶來的重大發現：這條不是純粹的 prompt 缺陷，而是「prompt 缺下限」與「模型在低 effort 下不主動補滿」的交互結果**。同一篇稿、同一份 prompt、同樣 `effort=low`、同樣 maximum 檔各跑 5 次：**claude-sonnet-5 → 3/5 被退，退件原因全是 `塊數不足（observed=0 required=7）`**；**google/gemini-3.8-flash → 5/5 全過，每一次都給滿 8 塊**；**openai/gpt-5.6-luna → 4/5 過，但傾向超量（9／13／9／10 塊）**。👉**同一份缺下限的 prompt，換個模型就不會塌**。 ⚠**監督先前的推論「換模型不會自動修好塊數問題」已被本輪實測推翻，在此更正。** 💡**但這不代表 B57 可以靠換模型結案**：①Sonnet 在**高思考**的 R6 那輪，maximum 也曾產出正常塊數（`maximum-4`／`maximum-5` 都過），所以 Sonnet 不是「做不到」，是**低 effort 時不主動補滿**；②luna 的**超量**（13 塊）證明沒有下限約束時，另一個方向的失控同樣存在；③**後端塊數防呆仍然該做**——它是與模型無關的保證，而且本帳本已記明「不撞 prompt 凍結快照、可立即上線、不需要新裁決就能開始設計」。**換模型只是讓這個缺陷不容易被觸發，不是修好它。** ✅**2026-09-17 R7 免費前置批驗收通過**：同稿同參數 `maximum` 連打 5 次，塊數 **8／7／7／8／7**，**5/5 全部落在目標 8±1**——正是使用者裁定的驗收標準。🔑**這一輪一張圖都沒生**：塊數讀的是消化回傳的 `variable` 欄位裡 `[內文小標]` 的行數，跟生圖無關，所以不必動用實拍額度。明細 `R7_免費前置批.jsonl`。|
| B58 | **大標題的壞字：3D 立體擠出＋發光的標題字會把筆畫吃掉，同一張圖的平面內文字卻正確** | 由 F38 錯字結案時分岔出來（2026-09-15，監督在既有正式站成品裡找到） | ❌ **2026-09-15 使用者裁決：結案，判定為誤報——「騙」字算繪是正確的，本條從頭到尾都不是缺陷。**｜**不修法、不追蹤、不再花錢。甲／乙／丙三個修法選項一併作廢。**｜**保留本列不刪，作為判讀誤判的教訓紀錄** | **證據（既有檔案，沒有另外花錢）**：`D:\Downloads\AICG\後台紀錄\20260915\曹雪卿成圖\證據_標題騙字壞掉_放大.png` 與 `證據_同圖第1格騙字正確_放大.png`。**同一張正式站成品裡，「騙」出現兩次**——**大標題那個（全圖最大的字）馬部下方四點變成亂筆、筆畫糊成一團，是壞字**；**內文區塊那個（小得多）筆畫乾淨、完全正確**。同圖、同字、同一次生成、同一個解析度。🎯**這是 F38 錯字結案的依據，也是本條成立的起點**：既然小的對、大的錯，**字級不是變因**，剩下最明顯的差異就是**樣式**——`REPORTER_TEXT_RULES`／`EDITOR_TEXT_RULES` 對主標題都明寫 `Rendered in bold 3D extruded typography with strong depth and lighting`，內文則只要求 `Clean and highly legible`。**假說：3D 擠出、投影、發光、描邊這些效果在筆畫密集的漢字上會把細部糊掉**，字越複雜（騙、籲、颱、蔬、警、籲）越容易壞。⚠**這只是假說，不要當結論**。已知的反面條件：這一組對照的變因**不只樣式一個**（字級也不同，只是方向與直覺相反），而且**樣本數 = 1**。✅**下一步（零成本，不要花錢生圖）**：正式站後台已經有大量成品，**直接撈既有成品逐張看標題字**，統計「標題壞字」的出現頻率，並記錄壞掉的是哪些字。要回答三件事：①頻率有多高（偶發還是常態）；②壞的是不是都集中在筆畫多的字；③內文區塊有沒有出現過同等程度的壞字（若有，樣式假說就不成立）。**這件事不需要生任何一張新圖。**💡**若假說成立，修法方向（都要先裁）**：甲｜**降低標題樣式的複雜度**（例如筆畫多的字改用較淺的擠出或取消發光）；乙｜**標題改程式壓字**（與 B55 的「模型只畫標題圖層」方向相反，這裡是反過來不讓模型畫標題），可靠但失去 AI 標題的設計感，等於動到 2026-09-13 的裁決；丙｜**在 prompt 裡加一條自檢**（要求模型檢查每個字的筆畫完整），**監督對丙持保留**——本 repo 已多次證實模型做不到的事情靠自律規則救不回來（見 `compose.py` 開頭那段安全框五輪實驗）。⚠**動工注意**：甲、丙都會改動 prompt 文字，**撞凍結快照**（`tests/test_reporter_prompt_frozen.py`），本波沒有重凍預算。 🔬**2026-09-15 監督做完零成本頻率掃描，結果比預期強：不是偶發，是穩定重現。**使用者指定重現後台紀錄 **2026-09-15 10:14:12 曹雪卿**（川普 AI 騙局，1464 字，資料圖表，編輯版）。查後台原始紀錄 `曹雪卿0915全紀錄\20260915_101412.txt` 與成品 `曹雪卿成圖\101412-31c5ea39447a.png`——**這一則就是 B58 那兩張證據圖的來源**。**關鍵發現：曹雪卿對同一則連生三次**（10:14:12／10:23:52／10:26:30），**三張的標題都含「騙局」**，監督逐張原尺寸放大 300–400% 檢視，**「騙」的馬部下方四點在每一張都塌成斜筆／糊塊，失效模式完全一致**。👉**同一個字、三次獨立生成、三次都壞——這不是隨機雜訊，是穩定重現的系統性缺陷。**（而且她連生三次這件事本身，很可能就是因為標題不能看。）✅**同圖對照再次成立**：同一張圖的第 1 格內文「全球暖化騙局」是**黃色平面字，「騙」完全正確**；標題那個是 3D 擠出＋發光，壞掉。**字級更大、樣式更複雜的那個壞，平面的那個對。**⚠**監督自己差點誤判、已避開的一個坑**：同一行標題的「黃仁**勳**」在縮圖上看起來像「黃仁動」，**原尺寸放大後確認是正確的「勳」**（灬 四點在）。這正是既有的教訓「判字要用原尺寸，縮圖會吃掉部件（勳→動）」——本條所有判定皆以原尺寸為準。📌**所以 B58 的前半段（頻率）已經不用再查，零成本部分完成**；剩下要證的是**因果**：到底是不是 3D 擠出＋發光造成的。已派決定性對照，見下。 🧪**2026-09-15 標題樣式 A/B 跑完（2 張，使用者授權）——方向支持假說，但監督複驗後認為證據強度低於測試端的說法，不足以當定論。****設計**：共用一次消化（曹雪卿 1464 字全文、編輯版、資料圖表），標題抽到「川普轟AI威脅論／是場騙局」，含「騙」；唯一變因是 `news_prompt.py` `EDITOR_TEXT_RULES` 標題樣式那一句——A＝原句 `bold 3D extruded typography with strong depth and lighting`，B＝`bold flat typography with a clean solid fill and a simple contrasting outline, no 3D extrusion, no bevel, no glow, no drop shadow`。兩張皆 1920×1080、同模型。證據：`D:\Downloads\AICG\後台紀錄\20260915\標題樣式對照\`。**監督自己看過 `b58_A_pian_only.png` 與 `b58_B_pian_only.png` 並排（非採信轉述）**：**A 的「騙」馬部下方四點，左側兩筆確實收斂糊成楔형塊；B 的同位置四筆分得開。方向成立。**⚠**但有三個理由不能把它當定論，監督要求寫進結論**：①**變因沒有乾淨隔離**——B 出來的不是預期的「平面粗體」，而是**深色填滿＋金色描邊**，**連填色與對比都變了**，不只是少了擠出。所以「B 比較好」可能有一部分來自填色，不能全歸給 3D。②**單樣本**，沒有做同一句話重複生圖的變異度控制；「騙」在正式站三連拍都壞，但 B 只生了一張，**一張乾淨不等於穩定乾淨**。③**差距是「比較清楚」不是「從錯變對」**——A 是糊、B 是清楚，兩者都還在可辨識範圍，與正式站那三張塌成糊塊的程度不完全同級。📌**另一個監督注意到、測試端沒提的觀察**：**B 的標題整體顯著性下降**（深色填滿在深色底圖上的跳脫程度不如金色立體字）。**這題不是「筆畫清楚就贏」——標題要上鏡、要搶眼，把立體與發光整個拿掉是有代價的。**所以修法不該直接跳到「拿掉 3D」，應該往**保留顯著性但降低筆畫破壞**的方向試（例如保留擠出但取消發光／降低斜角深度、或只對筆畫數高的字放寬樣式）。📌**一個未解釋的觀察**：耗時 A 84.4 秒 vs B 34.4 秒，**差 2.5 倍**。同樣的工作量不該差這麼多，**疑似 A 那次有重試**，但本輪沒有留下足以判斷的日誌。若之後要再測，請一併抓 `[generate] attempt` 那類日誌行。✅**邊界確認（監督已收到並接受）**：`news_prompt.py` 已 `git checkout --` 還原、`.env` 未動、未碰 staging／正式站、未改任何測試檔、2 張未超額。 🔻**2026-09-15 反證：3D 擠出「不足以」造成壞字——假說要收窄。**使用者指定再測後台紀錄 `2026-09-15 17:28:51 曹雪卿`（中國出入境新規，資料圖表，編輯版）。撈回原紀錄與成品（`曹雪卿0915全紀錄\20260915_172851.txt`＋`曹雪卿0915全紀錄\曹雪卿成圖\172851-3a68a9330f6d.png`）。**這筆的條件跟 10:14:12 那批同量級**：主標 2 行＋1 個強調數字塊＋7 張兩行卡片，密集版面。標題是「中國出入境新規上路／科技業出境門檻升高」，含 **「檻」（18 畫）**。**監督自己裁切放大 400% 複驗（非採信轉述）：「檻」的木部＋監部結構完整，皿部三豎清楚分開、等距，完全沒有糊塊／收斂／斜筆。整條標題無壞字。**🔑**而這張標題明顯是 3D 立體擠出（銀色金屬質感，斜角與厚度都很明顯）。**👉**所以「3D 擠出＋筆畫多 ⇒ 壞字」不成立**——同樣是 3D 擠出、同樣是高筆畫數的字，這張完全正常。**3D 擠出是必要條件的候選，但明確不是充分條件。** 假說必須收窄。💡**收窄後的候選變因（待測）**：①**發光／外光暈**——壞掉的那三張是藍色漸層＋強發光，這張是銀色金屬、沒有明顯外發光；②**特定字的結構**——「騙」的馬部底下是 **灬 四點**，**四個細小且必須彼此分離的元素**，是最容易被任何厚度效果糊在一起的結構；「檻」的皿部是三豎，筆畫較粗、間距較大，天生比較耐糊。**②這條特別值得注意：真正的風險可能不是「筆畫多」，而是「有密集小點／短撇」的部件**（灬、氵、冫、爫、宀 上的點），這跟「筆畫總數」是兩回事，先前的敘述（籲、轟、蔬）可能找錯方向。📌**這筆不適合當 B58 的測試素材**（沒有壞字就沒有可改善的空間），但**留作反證樣本很有價值**，不要刪。✅**下一輪的正確素材就是 10:14:12 那篇**——它的「騙」在三次獨立生成裡三次都壞，是目前唯一已知可穩定重現的案例。**條件必須釘死：`density="maximum"`**（見 F39，後台不記這欄，先前就是在這裡出錯測成字多）。📌**順帶記一筆**：17:32:40 那筆是對 17:28:51 的 **web-refine 追加修改**，不是獨立重生成。與 B51／B52 的追加修改缺陷同一條路徑，但本筆是 `default` 版型、不是封面，**所以不受那兩條的封面例外影響**；若要查追加修改的行為，這是一個現成的非封面樣本。 🛑**2026-09-15 第二輪（發光變因，4 張）結果與假說相反，監督據此收掉「樣式是元凶」這條路線——真正的常數是『部件』不是樣式。****實測**：同一份消化（標題「川普稱AI是騙局／力挺輝達技術」），A＝原句（3D 擠出＋lighting）×2、B＝只刪 ` and lighting`（保留 3D 擠出）×2。**監督自己四張裁切並排複驗（非採信轉述）：A 兩張的馬部四點清楚分開；B 兩張的左側兩筆收斂成尖角糊在一起，兩張位置形狀高度一致，不是隨機。**👉**拿掉發光沒有讓字變乾淨，反而穩定變糊——「發光是元凶」被否證。**🔑**把四輪樣本擺在一起，樣式這條線整個站不住**：①`17:28:51`「檻」＝3D＋銀色金屬 → **乾淨**；②`10:14:12`「騙」＝3D＋藍色漸層發光 → **三次全壞**；③B58-1 的 A＝原句 → **壞**、B＝整句換成 flat＋fill＋outline → **乾淨**；④B58-2 的 A＝原句 → **兩張都乾淨**、B＝原句減 lighting → **兩張都壞**。**③與④的 A 是同一段 prompt 文字，結果一個壞一個乾淨。****同一條規則、相反的結果 ⇒ 樣式子句根本不是決定因素，這是隨機性。**🎯**四輪下來唯一穩定成立的事實只有一個**：**壞掉的位置永遠是「馬」的 `灬`**——從來沒有壞在「扁」，也沒有壞在標題的其他字（境、業、檻、升、高、局、技、術都正常）。**所以真正的變因是『部件』：`灬` 這種「多個細小、必須彼此分離」的結構，在任何厚度／描邊處理下都容易被糊成一塊。與字級無關（F38 已證）、與解析度無關（F38 已證）、與 3D 擠出無關（①）、與發光無關（本輪）。**💡**因此修法方向要換掉，不要再調樣式文字**（四輪證明調不動）。改成三條：**甲｜標題改程式壓字**——最可靠，一勞永逸，代價是失去 AI 標題的設計感，且等於動到 2026-09-13 的裁決（與 B55 甲案的「模型只畫標題圖層、程式合成」是同一套機制，**可以合併做**）；**乙｜產出後自動驗字**——用 OCR／比對偵測標題是否與指定文字一致，不一致就重生，治標、會多花錢，但不動視覺；**丙｜規避**——消化端在選標題用字時避開含 `灬` 的字，**監督不建議**：會扭曲新聞用字（「騙局」是這則新聞的核心詞，不能為了算繪換掉）。**監督建議甲，並與 B55 合併評估。**📌**附帶查證**：`[generate] attempt N/5` 這類重試日誌**只存在於消化端**，生圖端 `/api/images/generate` 沒有對應的逐次重試日誌，HTTP 層只看得到一次 200。**所以 B58-1 那個 84.4 秒 vs 34.4 秒的 2.5 倍落差，用現有日誌無法驗證**，本輪也沒再出現離群值（最慢 38.5s、最快 33.2s，差 16%）。**這條併入 F30**（成功不記耗時與 provider）——生圖端缺重試與計時日誌，是同一個病。✅**邊界確認**：`news_prompt.py` 已還原、`.env` 未動、4 張未超額。**測試線到此為止，不要再為這題花錢**——樣式已測到盡頭，下一步是裁修法。 ✅**2026-09-15 第五輪（4 張重複生圖）——本條證據面到此結案，出現率 4/4＝100%。****條件**：記者版、`density="maximum"`、完整 14 欄位 payload、素材沿用 10:14:12 那篇 1464 字稿，**每張都先過關卡（`[內文小標]` 標籤數 ≥8 才准生圖）**，四張都是 8 塊密集版面——**這是第一次在「確定密集版面」的前提下做重複觀測，前面幾輪都沒有這道關卡。****四張的標題措辭、視覺樣式、seed 全部不同**：「川普稱AI控世界是騙局」（銀白金屬＋藍底）、「川普稱AI威脅論是騙局」×2（紅框金黃／藍白漸層）、「川普：AI接管世界是騙局」（藍白立體）。🔑**結果：四張的「騙」字，馬部下方的 `灬` 全部壞掉，右側「扁」（戶＋冊）四張全部正常。****監督自己開原圖裁切放大複驗（非採信轉述）**：r1 的四點最左兩筆收斂成尖角、交叉黏在一起；r3／r4 的四點被拉長成下垂豎筆並在底部相連。**變形的細部形狀不完全相同，但位置與性質完全一致——永遠是 `灬`，永遠是「該分開的細小元素黏成一塊」。**📌**監督在此修正測試的一個措辭**：測試回報「崩壞形狀完全一致」，**嚴格說是「位置與失效模式一致、細部形狀不同」**；這個差別不影響結論，但帳本要寫準——**不是同一個 bug pattern 被複製，是同一個結構在不同噪聲下各自糊掉。**👉**所以「樣式是元凶」徹底出局**：四種樣式、四個 seed、三種措辭，壞法一樣。**「字級／解析度是元凶」也早已出局（F38）。剩下唯一的常數就是那個部件本身。**✅**同一批的反證也一起記下（監督全圖複驗 r1）**：同一張圖裡，**「黃仁勳」的 `勳` 字（左下也是 `灬`）在小字平面體裡算繪正確**、**「習近平」三字正確**、標題其餘字（川、普、稱、控、世、界、是、局）全部正確。⚠**監督自記一個判讀教訓**：在測試提供的**放大裁切圖**上，「習近平」一度看起來像「習新平」，**回到 1920×1080 原圖才確認是正確的**。**裁切圖已經被放大過，不能拿它當判字依據——判字一律回原尺寸檔**（與既有的「縮圖會吃掉部件」同一條紀律的反面）。🎯**修法維持三選一，監督仍建議甲（程式壓字），並與 B55 合併評估。****乙（產出後驗字）在本條的數據下價值上升**：既然是 100% 重現而不是偶發，**重生一次也不會變好**，乙必須搭配「換字或改走程式壓字」的退路，單純重試無效。📌**證據**：`D:\Downloads\AICG\後台紀錄\20260915\字超多生圖\`（四張成品 `r*_image.png`、裁切 `r*_pian2.png`、各自的 `r*_prompt.txt`／`r*_digest_final.ndjson`、以及 `報告-字超多重複生圖判讀.md`）。💰**成本**：生圖 4 張＝$0.16；消化 6 次（r1 因 OpenRouter 503 重打 2 次）。 ❌**2026-09-15 使用者裁決：本條作廢，判定為誤報。****使用者原話：「我看過了 騙都是正確的 不是錯的 結案」。**👉**四張成品裡的「騙」字算繪正確，`灬` 沒有壞。****先前五輪（含監督自己的逐張裁切複驗）把立體字的厚度、斜角與陰影誤判成「四點糊在一起」。****這一條從第一輪起就是建立在錯誤的判讀上，後續所有推論（部件脆弱性、樣式無關、100% 重現）全部隨之作廢。**⚠**教訓（監督自記，比本條本身更有價值，請保留）**：①**放大裁切會製造假象**——已知「縮圖會吃掉部件」，本輪反向踩到：**放大同樣會把立體效果的斜角與陰影誇大成筆畫變形**。原尺寸檔也不夠，**判字要看「這個字在成品上實際被閱讀時的樣子」**。②**我自己複驗過四張、判斷一致，結果仍然是錯的**——**「監督親自複驗」不等於正確**，一致只代表我每次都用同一套錯誤標準。③**應該更早交給使用者肉眼確認**：這是人眼一秒可判的事，卻連續五輪用付費生圖去追。**往後「字有沒有壞」這類人眼可判的問題，先給使用者看一張，不要先開測試線。**💰**代價**：B58 這條線累計付費生圖約 10 張（$0.40 量級）＋多輪消化 token，**全部是白花的**。📌**連帶影響**：本條先前建議的「甲＝標題改程式壓字」作廢；**B55 的甲案不受影響**（那是為了「原圖放置不被改」的保證，與壞字無關），仍待裁。**F38 的「解析度救不了錯字」結論也不受影響**（那是用同圖不同字級證的，不依賴騙字壞掉）；但 F38 裡「錯字與字級反相關」這句的立論來源是本條，**已不成立，需要一併修正**。 |
| B59 | **「背景執行」時失敗機率明顯較高** | 曹雪卿 0915 回饋文件第 1 條（2026-09-15 使用者指定列管；此前整份清單只有這條與 24H LIVE 兩行那條沒被列管） | ⬜ **待查證**｜零成本的第一步是先把「背景執行」的定義問清楚 | **使用者原話**：「試了幾次背景執行，失敗機率較高（遇過幾次都是這樣，不曉得是剛好或是真的會有這樣問題）」。⚠**第一件事是釐清「背景執行」指什麼**，這決定要查哪裡，**不要憑猜開工**：①**送出後把瀏覽器分頁切到背景／切去做別的事**（最可能的解讀）；②同時開多個分頁一起送；③其他。**問清楚之前不要動手。**💡**若是解讀①，有一個很具體的技術候選**：B39 之後 `/api/generate` 改成 NDJSON 串流，前端 `_digestFetch` 用 `response.body.getReader()` **逐行持續讀**，並靠 `DIGEST_FETCH_TIMEOUT_MS=290` 的 `AbortController` 控總時長。**Chrome 對背景分頁會節流計時器與部分工作**，長工期間讀取被拖慢或中斷，就會走進 B38 那條「串流中途斷線」的錯誤分支——**使用者看到的就是失敗率變高**。這條完全符合「前景會過、背景較常失敗」的現象。📌**已知的相鄰事實**：B39 末尾記過「使用者中途關分頁時，後端那個 daemon thread 會跑完全程，token 照燒」——那是**成本**問題；本條是**成功率**問題，兩者是同一個場景的兩個面向，修的時候要一起看。✅**下一步（零成本）**：①向使用者確認「背景執行」的定義；②若為①，本機起服務用長稿實跑兩次——一次全程停在前景、一次送出後立刻切到別的分頁——比對成功率與 console 是否出現串流中斷。**不用打正式站、不用生圖，只花消化 token。**⚠**注意**：B48 已知長稿本來就有約 25% 的間歇失敗率，**所以樣本要夠才分得出「背景更容易失敗」與「本來就會失敗」**，兩次不夠下定論，先當方向指標。 |
| B60 | **同參數、同一篇稿，輸出份量毫無連貫性——`maximum` 塊數在 1～8 之間亂跳** | 2026-09-15 使用者看 B57 的 5+5 分布後當場指出「這個數字要檢討，沒有連貫」，指定列管 | 🔶 **2026-09-16 已連動修法（見 B57，Stage 3 `622f570`）——但離散度還沒量**。驗收要等付費額度｜⚖ **2026-09-16 使用者已裁 驗收標準：同參數連打 5 次，塊數全部落在目標值 ±1 才算修好**；份量類結論一律附 5 次分布，不接受單次示範｜⬜ **待裁修法**｜**這是獨立於 B57 的一條**：B57 問「為什麼會變少」，本條問「為什麼同一組輸入每次不一樣」｜**監督判斷：可重現性應該當成驗收條件，不是附帶現象** | **事實（零成本實測，10 筆全為 HTTP 200 有效樣本，非挑選）**：同一份完整 14 欄位 payload、同一篇 1464 字稿、同一個 commit（HEAD）、同一個模型（`claude-sonnet-5`）、`density="maximum"`，連打 10 次——**編輯版：8／8／8／4／8**　**記者版：1／1／8／8／8**。原始檔：`D:\Downloads\AICG\後台紀錄\20260915\密度對照\`（`b57_dist_summary.txt` 有整理）。👉**輸入完全相同，輸出份量相差最多 8 倍。** 使用者按同一顆按鈕、貼同一篇稿，**拿到的成品可能是密集 8 塊，也可能是 4 塊或塌成一坨——這件事本身就是缺陷，跟「字超多夠不夠多」是兩個問題。**🔑**監督的判斷：不要把這個歸成「LLM 本來就有隨機性」就結案。** 隨機性解釋得了 8 vs 7，**解釋不了 8 vs 1**。8 倍的離散度代表規則沒有把輸出鎖進一個可接受的區間，**模型每次都在重新決定「要寫多少」，而不是在執行一個被指定的份量。**🎯**根因與 B57 同源但不同面向**：`MAXIMUM_DENSITY_RULES` 只解除上限、沒有下限，**等於沒有給出任何目標值**——沒有目標值就沒有收斂，自然每次都不一樣。**所以修 B57 的下限，理論上會同時收掉本條的離散度；但「有沒有真的收掉」必須另外量，不能假設。**⚠**這條直接影響前面所有結論的可信度，要寫在最前面當教訓**：今天有兩個結論就是被單樣本騙掉的——「編輯版系統性偏少」（各抽一次，剛好抽到兩端）、「舊 commit 是回歸」（其實消化端零改動）。**在離散度收斂之前，任何「改了 X 之後變好／變差」的判斷都不能只打一次就下結論**，**最少 5 次看分布**。這條適用於 B57／B58／F38／D16 以及之後所有消化端的對照測試。✅**下一步（零成本，建議做在修法之後當驗收）**：①先量現況基線——`standard`／`simplified` 各連打 5 次，看**離散度是不是只有 `maximum` 特別大**，還是每一檔都這樣（若每一檔都亂跳，問題比 B57 更廣）；②修法（B57 的下限＋塊數防呆）上線後，同樣連打 5 次，**驗收標準建議訂成「5 次的塊數全部落在目標值 ±1 之內」**，不到就不算修好。💡**監督建議的驗收條件（待裁）**：**份量類的規格從此不接受單次示範**，帳本上任何跟字數／塊數有關的結論，都要附上 5 次的分布，不是一個數字。 📌**2026-09-16 跨模型量測順帶提供的一項旁證**（本列談的是輸出份量的連貫性）：把三輪的內文小標逐條量字數——**`claude-sonnet-5` 在 effort=low 下出現過一條 186 字的小標**（50 條的平均是 23.8 字），而 **`google/gemini-3.8-flash` 兩輪 116 條的最長值只有 32 字**（平均 22.4～24.7）。**平均值相近但極端值差了 6 倍**，與本列「同參數同稿連打結果跳動」的觀察方向一致：**份量不穩定的是模型，不是參數**。📌紀律照舊：份量類結論一律附分布，不接受單次示範。 ✅**2026-09-17 R7 免費前置批：離散度量到了，而且過關**。使用者裁定的標準是「同參數連打 5 次，塊數全部落在目標值 ±1」，實測 **8／7／7／8／7（目標 8）→ 5/5 通過**，全距只有 1。對照 0915 同一條線「`maximum` 塊數在 1～8 之間亂跳」的原始觀察，**跳動已經消失**。⚠**但要說清楚這一輪同時變動了兩件事**：B57 的後端塊數防呆（Stage 3 `622f570`）**與**主消化模型換成 `google/gemini-3.8-flash`（D21）。**這 5 次分不出功勞是哪一邊的**——真要歸因得拿 Claude 跑同一組對照，但那對「修好了沒有」這個問題沒有影響。明細 `R7_免費前置批.jsonl`。|
| B61 | **封面各版型的生圖 prompt 沒有「不得畫具名真人臉」的預設防線 → 模型自己把新聞主角畫成過期人物** | 2026-09-16 正式站事故（王結玲 09:21–09:25、曹雪卿 09:00–09:11），子代理全路徑盤點 | ✅ **2026-09-16 已實作（exp `e0f4df6`，Stage 4 `65d4ec3`）**：改成**出口強制層**而非逐個模板補條款——`generate_image_raw()` 開頭冪等注入 `FINAL_IMAGE_BASELINE`（marker `=== FINAL IMAGE POLICY BASELINE ===`），七個呼叫點全數覆蓋，兩支 provider 進入點只能從它裡面走。⚠**條文刻意限定在「prompt 沒有要求的」文字／標誌／真人**，否則會當場打死 AI 標題模式｜⬜ **待裁修法**｜**這是 0916 事故的直接破口，兩位使用者同一個早上各自再現** | **事實**：標題「美國聯準會利率決策出爐」**不含任何人名**，畫面描述也只寫大樓與國旗，**四次生圖四次都被模型自己加上一張鮑爾（前主席）的寫實人臉**。🔑**根因鏈**：headline 無人名 → derive／消化回空 `portrait_subjects` → `keep_subjects_with_photos`（main.py:4536）`if not subjects` 直接回空 → `apply_portrait_to_image_request`（main.py:3717-3719）同樣因 subjects 空**原封不動 return** → 送進模型的最終 prompt **完全沒有一句話**限制真人臉。📋**盤點結果（子代理，唯讀）——有洞的六條**：十點雙切（`COVER_VISUAL_PROMPT_TEMPLATE`，editor_formats.py:204）、十點滿版（`COVER_VISUAL_FULL_PROMPT_TEMPLATE`:192）、十點 AI 標題（`COVER_AI_PROMPT_TEMPLATE`:236／`COVER_AI_FULL_PROMPT_TEMPLATE`:268）、YT 合成底圖（`YT_COVER_VISUAL_PROMPT_TEMPLATE`:1402）、**YT AI 標題四版型**（`YT_COVER_FULL_PROMPT_NEWS`:1535／`HOURLY`:2036／`HOT`:2160／`LIVE24`:2072，事故走的就是 NEWS）。**有防線的對照組**：`/api/news-image`（`build_prompt` 無條件注入 `REAL_WORLD_RENDERING_RULES`，news_prompt.py:560／597，預設條款在 :196）、`/api/images/refine`（`REFINE_REAL_WORLD_RULES` 無條件疊加）、`/api/images/generate` 走 app.js 時（前端自己插，app.js:1896／2008）。👉**系統性落差**：防線寫在主流程，**封面各版型自己另組 prompt 時沒帶上**，每加一個新版型就漏一次。💡**修法方向（待裁）**：六個模板補上與主流程等義的預設條款（無肖像區塊時不得畫可辨識具名真人臉，只能背影／剪影／非具名臉）。⚠**動工前先確認**：這會改 prompt 文字，要確認撞不撞凍結快照。📌**證據**：`D:\Downloads\AICG\後台紀錄\20260916\王結玲聯準會\`（四步對照圖、四筆後台原始紀錄、查因報告） |
| B62 | **`hybrid.js`（一鍵成圖的獨立前端）自組 prompt，真人與品牌防線全都沒有** | 同 B61 的盤點順帶發現（2026-09-16） | ✅ **2026-09-16 隨 B61 一併解決（exp `e0f4df6`，Stage 4 `65d4ec3`）**：鐵律 ownership 留在後端出口，`hybrid.js` 不需要也不准複製條文｜⬜ | `hybrid.js` 的 `buildBgPrompt()`（hybrid.js:323-336）直打 `/api/images/generate`，**全檔 grep `REAL_WORLD`／`NAMED REAL PERSON`／`silhouette` 零命中**——app.js 有做同步（app.js:1881-1900），hybrid.js 沒有。**後端對 `/api/images/generate` 不補防線**（只做 `apply_portrait_to_image_request`，subjects 空就 return），所以這條路完全裸奔。與 **A4 缺口 5「hybrid 全無」**同一叢，建議一起看。 |
| B63 | **追加修改無法指名換臉：使用者說「人頭換成某某人」在結構上必定失敗** | 2026-09-16 王結玲 09:22:33 web-refine 實例 | ✅ **2026-09-16 已實作（exp `283b7fa`，Stage 5b）**：`ImageRefineRequest` 新增**結構化** `replacement_person`，**不從自由文字抽姓名**（F41「只抽取、不推斷」）。四種結果：①使用者上傳照片→直接用｜②維基有合格照片→附為 reference｜③只有條目沒照片→允許畫並回 notice｜④**連條目都查不到→回 400 擋下**，不讓模型捏臉。`REFINE_REAL_WORLD_RULES` 只在未填 replacement 時套用，一般 refine 行為完全不變。前端偵測到換臉語意但欄位留空時阻止送出——**只判斷意圖、不猜姓名**。⚠**刻意沒有寫「查無條目時請模型不要捏臉」那條規則**：那種防線本來就是錯的，0916 換出第三張臉正是因為交給模型自律，唯一可靠的擋法是程式端不送出去。⚠**待實拍驗收**：換臉相似度要生圖給使用者看｜⬜ **待裁修法** | **實例**：她下「人頭要換成新的FED 主席華許」，結果換成**誰都不是的第三張臉**。🔑**兩個原因疊在一起**：①`news_prompt.REFINE_REAL_WORLD_RULES`（news_prompt.py:485）硬寫死「no reference photograph is attached to this edit… **Faces already present stay exactly as they are — do not restyle, replace, age, beautify or re-render them**」，**與使用者指令正面對撞**；②**這是後端事實不是前端漏傳**——`ImageRefineRequest`（main.py:3950）**沒有** `reference_images`／`portrait_subjects` 欄位，`refine_image` 也不查 `photo_lookup`，所以那句「沒有附參考照」**永遠成立，沒有任何呼叫方式能讓它不成立**。👉**模型在衝突下折衷捏一張臉，程式端沒有任何檢查會擋下或提示**。✅**對照組（同一天、同一個 bug 的另一半）**：曹雪卿 09:11:32 下「左邊的鮑爾不要!!!!」→ **刪臉成功**。**刪得掉、換不掉**，正好對上規則措辭。💡**修法方向（待裁）**：refine 加人名／參考照管道，偵測到「換成某某人」就查照片、附上並改用允許換臉的規則；**查不到就明講辦不到**並提示改走原圖放置，不要讓模型硬編。📌**相鄰**：同一次 refine 也把構圖拉近、標題重畫（違反同段的 keep framing）＝ **B2** 的再現。 |
| B64 | **能力判斷過期：原生 OpenAI 早就送得出參考圖，`supports_reference_image()` 仍回 False → 肖像參考照被整批丟棄、真人題一律退回背影** | 2026-09-16 監督本機實測「標題寫華許」時踩到 | ✅ **已修並實測通過（2026-09-16，本機 exp 工作樹，未 commit）**｜修後同一份請求 log 變成 `參考照=1 張` ＋ `[GPT image] 附 1 張參考圖，改走 images.edit`，成品畫出**正確的華許本人**（三方臉部對照：維基官方照／系統自動查照生成／王結玲自己上傳的成品，同一人）。測試 **1768 passed**。證據：`D:\Downloads\AICG\後台紀錄\20260916\王結玲聯準會\20260916_測試3_標題寫華許_修後真跑.png`、`20260916_華許三方臉部對照.jpg` | **實測**：本機 `IMAGE_BACKEND=native` 跑 YT 封面，log 為證 `[yt-cover:ai-title] portrait_subjects=['華許'] en=['Kevin Warsh'] 參考照=0 張`——**人名抓到了、維基照片也查得到（`find_reference_photo('華許')` 回 zh.wikipedia 官方肖像），但照片被丟掉**，`resolve_portraits` 因此回 `no_reference`，成品是背影。🔑**根因**：`supports_reference_image()`（main.py:2888）的 docstring 還停在「原生 OpenAI 的 images.generate 沒有參考圖通道」，**但 2026-09-10 起 `generate_gpt_image` 只要有參考圖就改走 `images.edit`**（main.py:3325-3332，`_native_reference_files` 送的是 `reference_image_data_url` ＋整個 `reference_images` 陣列）。2026-09-13 的 `b70f89d` 修了 `supports_multiple_reference_images`，**單張這一支沒跟著修**。⚠**連帶影響**：凡是在本機原生後端跑過的真人題測試，結論都可能被污染（「明明有照片還是畫背影」）。 |
| B65 | **`IMAGE_BACKEND` 的拼法在能力判斷之間不一致：`native` 與 `openai` 各認一半** | 同 B64（2026-09-16） | ✅ **已修（2026-09-16，與 B64 同一次，本機 exp 工作樹未 commit）**：抽出 `using_openrouter_images()`（與 `generate_image_raw` 同源），三支能力函式都改讀它；測試補了兩種拼法的 subTest，並更正兩處釘住舊事實的斷言（`test_portrait_rules.SupportsReferenceImageTests`、`test_native_multi_reference_20260913`）。**1768 passed** | `generate_image_raw`（main.py:3047-3057）的路由規則是「`openrouter` ＋有 key → OpenRouter，**其餘一律走原生**」；但 `supports_multiple_reference_images`（main.py:2915-2919）只認 `backend == "openai"`，**`dev-local-openai.sh` 設的卻是 `IMAGE_BACKEND=native`** → 同一次請求「路由說走原生 GPT（送得出多張）」「能力判斷說送不出去」，兩邊對不上。`supports_map_basemap`（main.py:2869）寫法又是第三種（非 openrouter 一律看 `provider == "gpt"`，這支反而是對的）。👉**三支各寫一份 `os.getenv` 判斷、拼法還互不相同**，這類漂移還會再發生。💡**修法**：抽一支與 `generate_image_raw` 完全同源的「這次走不走 OpenRouter」判斷，三支能力函式都改讀它。 |
| B66 | **查到參考照時規則要求「手繪插畫風肖像」，實際輸出是寫實照片感** | 2026-09-16 B64 修後實測順帶發現 | 🔶 **2026-09-16 已實作待實拍驗收（exp `e0f4df6`，Stage 5a `7d4748b`）**：三組 reference 常數改寫成「寫實新聞肖像為主、只帶一點插畫筆觸」。⚠**這條本來就註明要實拍驗收**，讀 prompt 判斷不了｜⚖ **2026-09-16 使用者已裁：寫實為主，但要帶一點點插畫筆觸感**——既不是純寫實照片、也不回到明顯的手繪插畫。**條文要照這個方向改寫**（現行 `PORTRAIT_WITH_REFERENCE_RULES` 的 `hand-painted editorial portrait illustration rather than a photograph`／`must be readable as an illustration` 要換成「寫實為底、保留可辨識的筆觸質地」這種措辭），並保留「AI示意圖」標籤。⚠**這條要實拍驗收**：措辭強度落在寫實與插畫之間，很難靠讀 prompt 判斷，改完要生圖給使用者看 ⚖ **2026-09-16 使用者定案：筆觸 OK，不用再修。**「寫實為底、只帶一點筆觸」改寫後的實際輸出（純寫實新聞照）**使用者接受**，本列風格部分**結案**。⚠**條文與行為仍不一致**：`PORTRAIT_WITH_REFERENCE_RULES` 現行措辭還寫著 `Keep only a light illustrative touch`，而被接受的成品沒有任何筆觸——**不影響成品，屬文件整潔問題**，且 prompt 是凍結快照，本次**不動**，留待下次有授權改 prompt 時順手改掉。**力氣轉往 B70／F43（標示）**，那才是真正的合規風險。| `PORTRAIT_WITH_REFERENCE_RULES`（news_prompt.py）明寫 `Render the portrait as a hand-painted editorial portrait illustration rather than a photograph` 與 `must be readable as an illustration`，但 2026-09-16 的實測成品（`20260916_測試3_標題寫華許_修後真跑.png`）看起來是**一張寫實新聞照**，不是插畫。⚠**這條規則當初的用意是合規**：寫實照片感＋真名＋（萬一）沒有示意圖標籤是最糟組合，所以才要求插畫化。本次成品右上角的「AI示意圖」標籤有在，但**視覺上與真實照片難以分辨**。💡**兩個方向要使用者裁**：甲｜加強插畫化措辭（代價：電視畫面上插畫風可能不好看，這正是使用者先前擔心的）；乙｜承認寫實化是想要的結果，改為強化標示（示意圖標籤位置／大小）並修正規則文字，讓條文與行為一致。**現況是條文與行為不一致，這本身就要修**。📌**樣本數 1**，裁之前若要更多樣本，可在既有正式站成品裡找有肖像的案例回看，零成本。 🔬**2026-09-16 已實拍驗收（使用者授權，4 張，全部走 F40 第 2 層維基參考照）：❌ 改寫後的措辭沒有達成使用者的裁定**。**現況是純寫實新聞照**——把臉裁出來放大到 180%／600% 看，毛孔、髮絲、高光都是照片質地，**找不到任何筆觸或繪畫感**。條文已經照「寫實為底、保留可辨識的筆觸質地」改寫（Stage 5a `7d4748b`），但模型沒有跟，`Keep only a light illustrative touch` 這種**程度副詞式的措辭對生圖模型等於沒說**。**相似度**：川普 3 張都像；黃仁勳第 1 張不太像（灰白髮、臉型偏長），第 2 張模型自己換成他招牌的黑皮衣後明顯變像，但頭髮仍是灰白。💡**下一步要裁**：甲｜承認寫實化就是結果，**條文改成明說寫實**並把力氣全部移到標示（見 **B70**，那裡才是真正的合規風險）；乙｜再加強插畫化措辭（代價是電視畫面上插畫風可能不好看，而且這一輪已經證明軟性措辭無效，要下重話才可能動得了）。📌**監督建議甲案**：這一輪同時抓到 4 張裡 2 張標籤不合格（**B70**），**標籤缺失比風格像不像危險得多**，資源該往那裡放。📁成品 `D:\Downloads\AICG\Testing_Cases\20260916_B66_*.png`。 ✅**2026-09-16 使用者已裁：定案，不用再修**——原話「B66／筆觸OK不用再修 定案」。⚠**帳本狀態欄先前仍寫「待實拍驗收」是過期的**（2026-09-17 監督盤點 R7 實拍名單時發現），使用者是看過 Stage 5a 產出的四張實拍圖之後才下的這個定案，驗收已經完成。**R7 實拍輪因此不再為本列花任何生圖額度。**|
| B67 | **B57 的塊數下限沒有「最後一次放行」出口：素材本來就單薄的稿會連撞 5 次後整條 502，使用者拿不到圖** | 2026-09-16 本 session 驗收 Stage 3（`622f570`）時發現 | ✅ **2026-09-16 使用者已裁＋當日已修（`main.py`：`DIGEST_POINT_COUNT_ATTEMPTS = 3`）**：塊數不足**試滿 3 次就放行**、印警告。🔁**使用者追問「重試五次可能太多耗時 三次可嗎？」，裁定只縮塊數這一種**——`DIGEST_ATTEMPTS` 維持 5 不動，截斷／亂碼／解析失敗那些真故障仍擋滿 5 次。⚠**刻意沒把 `DIGEST_ATTEMPTS` 一起降到 3**：`main.py:326` 的註解記著 2026-08-01 實測（上游間歇脫軌、單次成功率約 2/3、**3 次仍整組摃龜使用者收到 502**），那個 5 是實測換來的，不能因為另一個問題順手推翻。🔑**兩者可以分開處理的原因**：塊數不足對單薄稿是**每次必定不過**（重試不會變好，純等待）；上游脫軌是**隨機**（重試真的有機會好）| 🔑**B57 的防呆抓不出「模型偷懶」與「原文真的只有 3 個點」的差別**，兩者長得一模一樣。`main.py:2755` 的 `problem` 分支只會 `continue`，`DIGEST_ATTEMPTS = 5` 用完就落到 502。⚠**同一支 `generate()` 裡的 verbatim 檢查（`main.py:2743-2754`）早就處理過同一個兩難**，註解寫得很清楚：「最後一次刻意不擋——擋了就是整條 502，而這時手上的結果通常只是頭尾多了雜訊，仍比沒有圖好」。**塊數不足的情況更該放行**：手上那張 4 點的圖是完全可用的成品，不是壞資料。💰**成本面沒破口**（重試上限寫死 5 次，符合「API 呼叫次數要寫死上限」），純粹是產出行為的取捨。📌**新測試涵蓋到「短→重試→接受下限」，但沒有涵蓋「5 次全短」**，所以現況是未被測試釘住的行為。 |
| B68 | **封面推導的 material 用字面 `\n` 而不是真換行，模型收到的是一整行塞著 `\n` 的字串** | 2026-09-16 驗收 Stage 5a 時順手查到（**既有缺陷，不是這次改出來的**） | ✅ **2026-09-20 結案**（exp `7dcbfa0`，worktree commit `dbadbf6`）：`resolve_cover_visuals()` 三處全改成真換行。📌**原本擔心的「撞凍結快照」沒有發生**——驗收時查清楚了：fixture 裡根本沒有 `LEFT headline` 字樣，所有測到 `resolve_cover_visuals` 的既有測試都是 `patch.object(main, "resolve_cover_visuals", return_value=...)` 整支 mock 掉、沒有斷言內部 material 字串，所以 **rng_pins 與全部 fixture 零異動、未動用任何重凍額度**。✅ 另查證 `derive_yt_cover_plan()`（YT 封面同類函式）本來就用真換行，兩相對照確認這三處是手誤而非刻意；`EDITOR_SYSTEM_PROMPT_TEMPLATE` 裡教消化模型「JSON 內真換行要輸出成字面 \\n」的兩處是**刻意的，已確認保留**（team-lead 另外全檔掃過一次，殘留的 `\n` 全是正常換行）。新增 `tests/test_b68_cover_material_newline_20260920.py`（5 題，含一條釘住 `derive_yt_cover_plan` 不被複製貼上帶壞的回歸測試） | `main.py` 的 `resolve_cover_visuals()` 把材料組成 `'LEFT headline: {}\\nLEFT description already supplied: {}...'`——原始碼裡是 `\\n`（反斜線＋n 兩個字元），Python 解出來仍是字面 `\n`，**不是換行**。B53 新加的 news_text 段與 2026-09-08 WP1 的 instruction 段沿用同一個寫法，所以整段材料都是同一個問題。👉**Stage 5a 照既有慣例寫是對的**（不然會動到 rng_pins fixture），要修就三段一起修，而且**會改動封面推導的輸入字串＝撞凍結快照**，得先確認重凍額度。💡修法：改成真換行或 `\n`.join()，並重新產生受影響的 fixture。 |
| B69 | **F40 第 3 層（有條目沒照片→讓模型自畫）在網頁／LINE 主流程等於沒生效：消化端仍然「查不到照片就把人排出版面」** | 2026-09-16 Stage 5a 執行者主動回報，監督複核程式確認屬實 | ✅ **2026-09-16 已實作（exp `283b7fa`，Stage 5b）**：`apply_photo_availability()`（網頁）與 `resolve_digest_portraits()`（LINE）改用 `lookup_portrait_outcomes()`，**只有第 4 層（連維基條目都查不到）才 `exclude_people` 重新消化**；第 3 層（有條目沒合格照片）留在版面並回 notice。2026-08-18 那批既有測試改了三處，**都是把「任何沒照片都排除」收斂成「只有 tier 4 才排除」，沒有放寬任何斷言**，第 4 層排除與「只重試一次不進無限迴圈」仍被釘住｜⚖ **2026-09-16 使用者已裁：排進 Stage 5b 一起補**（理由：5b 本來就要做前端訊息欄，第 3 層的通知正好掛在同一個地方，同一條管線一次改完）｜⬜ **待實作**｜**這是 F40 的功能缺口，不是 bug——四層邏輯寫對了，只是沒接到主流程的入口** | **事實**：Stage 5a 把四層分流接進了 `resolve_portraits()`（通用 `/api/images/generate`）與封面／YT 共用的 `keep_subjects_with_photos()`，**但沒有接進 `apply_photo_availability()`（網頁版，main.py:2598）與 `resolve_digest_portraits()`（LINE）**。這兩支仍是 2026-08-18 的舊行為：`lookup_portrait_photos()` 回報沒照片 → 直接 `exclude_people` 重新消化一次，**把那個人整個排出版面**。👉**所以走網頁或 LINE 主流程的使用者，遇到「維基有條目但沒合格照片」的人，現在還是在消化階段就被排掉，根本走不到第 3 層的 entry_only，也不會收到 notice。**🔑**要補的話得動這兩支＋它們自己的既有測試**（2026-08-18 那批斷言釘的正是「沒照片就排除」這個行為），規模不小，所以 Stage 5a 沒有自行擴張範圍、改成回報。💡**三個選項**：甲｜排進 5b 一起做；乙｜另開一批；丙｜維持現況，第 3 層只在 `/api/images/generate` 與封面線生效。 |
| B70 | **具名肖像的「示意圖」標籤 4 張裡 2 張不合格：一張完全沒有、一張是錯字「示憊佪」** | 2026-09-16 B66 實拍驗收時發現（使用者授權的付費實拍） | ✅ **2026-09-21 前端已做，後端的洞也一起補**——使用者 0921 驗收原話「後端已修，但前端沒做」。⚙**前端**：`app.js` 新增 `disclaimerCorner`／`disclaimerSourceText` 兩個 state、四角按鈕與來源名輸入框，第一頁（一鍵生成）與第二／三頁（生圖）各一份控制項、值互相同步，兩個 fetch 都帶 `disclaimer_source_text`／`disclaimer_corner`。**一組控制、兩種標籤**：該貼「示意圖」還是「畫面來源」由後端 `resolve_image_disclaimer` 判（見 F43），前端不重判、也不給第二組按鈕。角落值直接用後端方位詞原文（`lower_right`／…），**不做 tl/br 對照層**——多一層對照就多一個會對錯的地方。🚫**追加修改不送**：`ImageRefineRequest` 沒有這兩個欄位，有測試釘住不准送。🙈**封面版型收起來**：封面走 compose 自己的 `_draw_ai_note`，不吃 `disclaimer_*`，擺出來會是按了沒反應的按鈕。做法是 `editor_formats.Capability` 新增 `disclaimer` 欄位（預設 True，六個封面版型設 False）→ `hides_for()` 推導 → app.js 鏡射——**不是直接在前端手寫**，因為 `test_format_capabilities_20260914` 的 parity 測試與 2026-09-14 凍結基準會擋下（本 session 第一版就是手寫 app.js，被那條測試抓到）。⚠**UX 提示（2026-09-21 使用者指定要做）**：AI 標籤贏的時候，使用者打的來源名會被靜默丟掉，畫面上看不出來。現在會回一則 notice 明講「你填的來源『○○○』這次沒有用上」並說明原因。🧪測試：兩個送出點都要送（count==2）、refine 不准送、四個角落值與`ImageGenerateRequest.disclaimer_corner` 的 Literal 完全相同、兩頁各一份控制列、`maxlength=40` 與後端 `Field(max_length=40)` 對齊、六個封面版型都收起來。📌**實證**：commit `5c456aa`＋`63d56c2`（exp/redesign-20260903）→ 公司 PR [#34](https://github.com/TVBS-AI/aicg-champion-daihsuan/pull/34) 已合併進 main（merge commit `d474093`，2026-09-21T10:00:21Z）；合併前後各跑一次全套，皆 **2221 題全綠**。VERSION `260921-04`。| **實拍證據（4 張全部走 F40 第 2 層 `portrait_mode=reference`／`reference_multi`，維基有合格參考照）**：①`20260916_B66_雙人川普黃仁勳_1.png` ✅ 右下角「示意圖」正確；②`20260916_B66_雙人川普黃仁勳_2.png` ❌ **整張找不到標籤**（上下兩半各自放大 70% 逐區掃過）；③`20260916_B66_單人川普_1.png` ✅ 左側「示意圖」正確；④`20260916_B66_單人川普_2.png` ⚠ 標題下方有「（示憊佪）」，**放大 600% 確認兩個字都不是正字**。🔑**根因分兩段，要分開修**：**(a) 消化端漏給**——`main.py:1757` 寫著「Always plan the 示意圖 label into variable when a person is depicted」，但同樣輸入跑兩次，②的 `variable` 就是沒有這個詞（回查 `logs/generations-20260916.jsonl` 確認）。生圖端的行為**反而是對的**：`PORTRAIT_WITH_REFERENCE_RULES` 明寫「If VARIABLE FIELDS supplies no such label, do not add one yourself」，所以消化漏給＝成品必然沒有。**純 prompt 規則、程式端零保證**，與 B55（原圖放置只靠 prompt 請求）、B63（不要捏臉只靠模型自律）是同一種病。**(b) 標籤交給模型畫就會寫錯字**——④證明即使給了詞，模型照樣畫成別的字。💡**修法方向（監督建議，待裁）**：甲｜**程式端疊標籤**——`portrait_mode` 只要是 reference／reference_multi／entry_only，就由 Pillow 後貼「AI示意圖」（十點封面線已經是這樣做，見 B30 的裁決與 `20260916_B55_十點滿版_創意0.png` 左上角那顆），順帶把錯字問題一起消滅；乙｜只補消化端的程式防呆（variable 沒有就補一行），錯字仍無解。**甲案同時解掉 (a) 與 (b)，乙案只解一半**。⚠與 **B28**（畫真人＋真名時標籤會被附圖規則洗掉）、**B30**（示意圖浮水印沒有程式疊）同一叢，建議一起做。 ⚖ **2026-09-16 使用者裁定：採甲案，示意圖標籤升級為 Pillow 程式端疊字**，並**追加一項需求：標籤位置要讓使用者自訂**（使用者反映）。 🔗**與新增的 F43「畫面來源」欄位互斥，兩條要一起設計、不可分開做**——使用者原話：「畫面來源也是 PILLOW，但跟示意圖互斥，原圖沒改圖才需要畫面來源」。 ✅**好消息：兩塊都有現成先例可抄，不是從零開始**（監督 2026-09-16 查證）：①**程式端疊標籤**——`compose.COVER_AI_NOTE`／`YT_AI_NOTE = "AI示意圖"`，`paste_cover_ai_note()`（compose.py:733）、live24 的 `compose.py:2297`（靠左貼在角標下方）都已經在跑，十點／YT 封面線的標籤一個字都不會錯；②**互斥判定**——`main.py:5826` 的 `is_ai = slot is None`（沒有使用者附圖＝AI 生的＝要標）已經是互斥邏輯的雛形，`compose.py:1676` 註解也明寫「AI 生的底圖一律標，附圖不標」。**新聞圖／肖像線缺的是把這套搬過來**，不是發明新東西。 ⚠**動工前要釘的三件事**：①**互斥的判準必須是「程式有沒有保證像素未被動過」**，不是「使用者有沒有勾原圖放置」——B55 已經證明勾了原圖放置照樣被模型整張重畫 68～93%，那種圖掛「畫面來源」等於說謊；②**自訂位置必須限制在 TVBS 標準安全框內**（1920×1080 基準 X=140／Y=109／W=1634／H=751），不能讓使用者把標籤拖到會被切掉的地方；③**要同時處理「模型自己又畫了一個」**——程式疊上去之後，模型畫的那個錯字標籤還在圖上，得靠 `editor_formats.py:262` 那種「Do NOT draw any 示意圖 label…Software adds that label afterwards」的措辭把區域讓出來，肖像線目前**沒有這句**。 📌**使用者指示：這點要考量更全面，都列管之後處理**——本次不動工。 🔁2026-09-17 使用者追加指示：在 Pillow 疊字機制正式上線前，全站消化文字流程要先設定成不可讓 AI 自己畫出或寫出「示意圖」或「來源」這兩組字——這正是本列「動工前要釘的三件事」第③點（要同時處理「模型自己又畫了一個」）的落地要求，範圍是全站而不只是先前查過的 editor_formats.py 幾個版型。尚未實作，待下一波動工時與 B70／F43 一起做，不單獨搶先動。 🔧**2026-09-20 全站化實作**：`main.resolve_image_disclaimer(portrait_mode)` 對 `reference`／`reference_multi`／`entry_only` 三態回 `"ai"`，由 `compose.paste_disclaimer_note()` 程式端壓「示意圖」；`no_reference`／`none` 兩態回 `""`（見下方待裁）。消化端（`main.py` `REAL_WORLD_FIDELITY_RULES` 第 3、5 條）與生圖端（`news_prompt.py` `REAL_WORLD_RENDERING_RULES` 第 2、4 條＋`PORTRAIT_WITH_REFERENCE_RULES`／`PORTRAIT_MULTI_WITH_REFERENCE_RULES`／`PORTRAIT_ENTRY_ONLY_RULES`）都改口告知模型「不要自己畫，軟體會後貼」，與程式端的判定對齊。使用者當面追加重凍額度，涵蓋這批對 `tests/fixtures` 的重建（八份 digest＋兩份 image prompt，只差那兩句差異，逐份 grep `CHINA OUTLINE`／`TAIWAN SEPARATION`〔B76〕確認未受影響；`tests/test_reporter_prompt_frozen.py` docstring 兩筆記錄並存，B76 在前、B70／F43 在後）。🐞**2026-09-20（網頁版回歸，team-lead 複查點名，獨立複查 gpt-5.6-sol 發現、team-lead 驗證）**：`apply_portrait_to_image_request()`（`main.py`，網頁版 `/api/images/generate` 專用路徑）從未呼叫 `resolve_image_disclaimer()`——`generate_news_image()`（LINE 路徑）有呼叫，這支網頁版專用函式沒有，兩個 `model_copy` 的 `update` 都缺 `disclaimer_kind`。結果是**編輯日常在用的網頁版** 產出的具名肖像圖完全沒有「示意圖」標籤：prompt 已經告知模型不要自己畫，程式那半又沒被叫到，標籤整個消失，性質接近 B76 那類播出風險。這是一度已經進 `exp` 的線上缺陷。已修：兩個分支的 `model_copy` 都補上 `disclaimer_kind`（無上傳肖像照那支呼叫 `resolve_image_disclaimer(mode)`；有上傳肖像照那支同時發現**比這個回歸更早就存在的洞**——這支從沒告訴模型要標「示意圖」〔`USER_REFERENCE_PORTRAIT_RULES` 完全沒提過這件事〕，一併補上措辭與 `disclaimer_kind="ai"`，依 B28 例外三：使用者親自上傳肖像照時具名真人仍要標）。新增端對端測試直接解碼成品 PNG、量測右下角像素變化，不只驗中繼欄位。🐞**2026-09-20（回填本列帳本時自己發現的第二個缺陷）**：全站化那批同時把「不要自己畫」推到 `portrait_mode="none"`（一般重建圖）與 `"no_reference"`（無人場景），但 `resolve_image_disclaimer()` 對這兩態永遠回 `""`，變成「模型不畫、程式也不壓」——標籤從「有時因模型不聽話而缺」惡化成「保證沒有」。已回退成全站化之前的安全值（`REAL_WORLD_FIDELITY_RULES` 第 3 條、`REAL_WORLD_RENDERING_RULES` 第 2、4 條與 `app.js` 鏡像、`PORTRAIT_NO_REFERENCE_RULES`：模型自己判斷、自己畫），第 5 條〔具名真人〕不回退——三個有軟體背書的 portrait_mode 仍靠它避免雙重標籤。⬜**待裁（尚未請示使用者）**：軟體端要不要也對「一般重建圖」（含 `portrait_mode="none"`／`"no_reference"`）補壓「示意圖」標籤，觸發條件是什麼？一個候選是「digest 回傳的 `variable` 裡出現「示意圖」字樣時，把 `disclaimer_kind` 設成 `"ai"`」。**現況**：模型自己判斷要不要寫、自己畫，程式完全不介入（回退後的原始行為，不是退步，是全站化之前本來就有的狀態）。 | 🔎**2026-09-20 獨立複查第二輪（gpt-5.6-sol）再抓一個同病灶的洞，已修**：上面那次回退時判定「第 5 條（具名真人，消化端）不必回退」，理由是「no_reference 不畫臉、本來就沒有字要寫」——**這個理由不成立**。第 5 條寫的是「Never write 示意圖 into "variable" for a **depicted person**」，而 `portrait_subjects` 依第 5 條自己的定義只收**會露臉**的具名真人。於是「具名真人出現在畫面上、但只畫背影／剪影／無臉替身」這種圖會落到 `portrait_mode="none"`：模型被第 5 條禁止規劃標籤，`resolve_image_disclaimer("none", "")` 又回 `("", "")` 程式也不貼 ⇒ **一個具名真人的重建畫面完全沒有示意圖標籤**，跟前一個洞同一個病灶，只是躲在第 5 條裡。**修法**：不整條回退，把豁免範圍收窄到 `portrait_subjects` 這個陣列本身，並明講沒列進去的人仍適用第 3 條（模型自己規劃標籤）；三個有軟體背書的 portrait_mode 行為不變，雙重標籤仍然擋著。**第三次重凍**：只動 `REAL_WORLD_FIDELITY_RULES` 第 5 條一句 ⇒ 八份 digest 快照各差一行（`git diff --numstat` 全是 `1 1`），兩份 image-prompt 快照這次沒變，B76 那塊逐份 grep 十份仍全在。
| B71 | 簡體字防呆漏掉「骗」，成品標題直接印出簡體字 | 2026-09-16 R6 長稿消化實測第 10 次（使用者授權的付費實拍） | 🟡 **未修**｜清單本來就註明不求完整（`main.py:2356`），**但這次是漏到成品標題上**，不是內文角落 | **實測**：R6 第 10 次（字超多）通過全部品質閘、判定成功，產出的主標是「川普稱AI是**骗**局 北京示警風險」。`骗` 不在 `DIGEST_NON_TW_CHARS` 裡，防呆放行。同一輪第 1 次也含 `骗`，是靠別的字（说、国、现、学…）才被擋下來——**`骗` 兩次都沒被抓到**。⚠**收字進清單前必須確認不是繁體正字**（2026-09-11 誤收「致」那次的教訓，同一列註解裡有寫）：`骗` 的正字是 `騙`，繁簡不同形，收進來安全。💡**最小修法**：把 `骗` 補進 `DIGEST_NON_TW_CHARS`。📌**但這只是補一個洞**——真正的問題是字元級清單永遠追不完，要不要改用詞庫（OpenCC）是既有的取捨，該列註解已經評估過並選擇不引進相依套件，維持現狀即可。 🔬**2026-09-16 免費驗證結果：這條的規模被嚴重低估，「補一個字」是錯的修法方向**（監督重播 R6 既有輸出＋子代理獨立盤點，兩邊結論一致）。**① 漏洞已重現**：R6 的 `maximum-5` 那一筆**通過了品質閘門**（outcome=ok），但它的 `variable` 裡有 **3 個「骗」**。對照 `standard-1` 之所以被擋下來，是因為它剛好同時含「说／电／风／现／陆／国／学／红／习／会」等 **10 個**在清單內的字——**擋得住是運氣，不是設計**。**② 清單規模**：`DIGEST_NON_TW_CHARS` 實算**只有 119 個字**，且全部來自過去的實測案例（出事一次補一個字）。**③ 漏網規模（兩組獨立抽樣）**：監督隨手抽 55 個常見簡體字，**漏掉 28 個**（骗术党丰乌卖画专业丛丝并丧为么亏云亚亿仅仑仓仪伦伞体余佣）；子代理另整理 88 個政經社會新聞高頻字（济／選／戰／證／決／軍／業／資 這類），**零收錄**。⚠**這 88 個是刻意挑「目前不在清單裡」的候選來量覆蓋率，所以「88/88 全漏」在定義上必然發生，不可以讀成「簡體字整體漏網率」**（子代理自己標註了這個方法論限制，記在此處以免日後誤引）。有意義的數字是：**漏網下限 88 字，已達現行清單規模的 74%**。🔑**結論：真正的失效模式是「模型大多數時候寫繁體，偶爾單一詞彙滑掉」**——漏網的是**單點**。用字元白名單擋單點，等於要求清單完整；而簡體字有數千個，**逐字補永遠補不完，而且每次都是出事之後才補**。這是機制問題，不是資料問題。💡**修法方向改判（監督建議，待裁）**：甲｜**改用簡繁對照表判定**（判斷「這個字是不是簡體形且有對應正字」），而不是列舉黑名單。⚠**但 `opencc`／`zhconv` 本機都沒安裝**，採此案要新增依賴，**這是真實成本，跟「往 frozenset 加一個字」差很遠**；乙｜維持白名單但**一次補齊**（拿盤點表當輸入），治標、且下次仍會漏；丙｜甲為主、白名單保留當快取。⚠**動工前必讀的既有教訓**：`main.py:2360` 記著「清單裡原本收了『致』，那是**誤收**——『致』是臺灣標準正字」，誤收會造成**必然 503**。所以**任何批次補字（乙案）或對照表（甲案）都必須先做臺灣正字對照**，子代理的 88 字候選表**只人工檢查過沒混進「致」這類繁簡同形正字，沒有系統性驗過**，動工拿它當輸入前要再驗一次。📁盤點方法與完整候選表 `docs/b71-simplified-char-gap-scan.md`；可重跑腳本 `tests/b71_char_scan.py`；紅燈測試 `tests/test_b71_simplified_char_gap_20260916.py`（**用 `unittest.expectedFailure` 記錄現況，B71 修好後要把那個裝飾器拿掉**）。 🧪**2026-09-16 跨模型對照（各 10 次）對「簡體字是不是 Claude 特性」這個假說的結果：三個模型全部 0/10，本輪無法分辨**。`claude-sonnet-5`（effort=low）0/10、`google/gemini-3.8-flash` 0/10、`openai/gpt-5.6-luna` 0/10，掃描集 207 字（現行防呆 119＋B71 候選 88）。 🔍**目前唯一出現簡體的是 R6 那輪**——同樣是 claude-sonnet-5，但**沒有送 effort、思考量 4005～11999**，2/10 有簡體。👉**就現有資料看，簡體字與「思考量大」的相關性比與「模型是誰」的相關性更明顯**，**但兩個假說的樣本都只有 10 次，都還不成立**，不可以拿來當結論用。 📌**對 B71 修法的意義**：**不論換不換模型，防呆本身還是要修**。本輪三個模型都沒吐簡體，只證明「今天沒踩到」，不證明清單夠用——清單只有 119 字、漏網下限 88 字這個事實沒有改變。 📊**2026-09-17 R7 免費前置批：再加 5 次，仍然 0 個簡體字**。連同 D21 的 gemini 兩輪（同稿 10 次＋十則不同稿 10 次），**`google/gemini-3.8-flash` 累計 25 次消化、簡體字掛零**；對照 `claude-sonnet-5` 在原設定下是 2/10。⚖**使用者 2026-09-17 已裁：先不修，等實拍確認**——理由是這個病灶很可能是 Claude 專屬的，換模型之後不必為它大動干戈（白名單留著當保險）。⚠**但本列的根因分析不因此作廢**：白名單方法本身就不完整（119 字、抽樣漏掉 88/88），**只要哪天退回 Claude，這個洞立刻回來**。|
| B72 | **後台稽核完全不記錄消化模型，出事查不出是哪支模型消化的** | 2026-09-16 使用者當面問「現在消化模型是？跟公司正式版一樣嗎？」時查證發現 | ✅ **2026-09-20 已修（worktree `wt/admin-fields-20260920`）** | **修法（本次做的）**：沒有照本行原本的「最小修法」只改 `log_generation()`一處，因為 `resolve_digest_model()` 是純環境設定查詢（讀 `DIGEST_MODEL`／`OPENAI_DIGEST_MODEL`／後端預設，不吃請求參數），任何時點呼叫值都相同；改成塞進 `main._outcome_meta()`——成功／失敗共用、全部 8 個會落檔端點（`web-image`／`web-refine`／`news-image`／`editor-cover`／`editor-cover-full`／`editor-yt-cover`／`editor-yt-overlay`／`digest`）都經過的單一入口——一次涵蓋，不必逐一端點各自傳，降低「新版型忘了接」的重演風險（同 F30 的教訓）。`request_log.log_generation`／`log_failure` 也加了 `digest_model` 參數（呼應本行原本建議），`generate()`（真正執行消化那支）額外把 `resolve_digest_model()` 的結果寫進 JSONL 與 `_remember_digest()`。`admin_console._row` 新增「消化模型」欄，缺值印「－」（沿用 F39 慣例）。新增測試 `tests/test_digest_model_audit_20260920.py`（寫入層／`_outcome_meta`／失敗共用函式／顯示層，共 8 條）。🔗**與 F31 同一輪順便併發的加分**：F31 那次把 `generate()`／`hybrid_digest()`／`editor_cover_titles()` 三支消化端點也接上 `_outcome_meta`／`_record_generation_failure`，這三支的成功與失敗紀錄因此**自動**帶了 `digest_model`（不用另外改），沒有額外成本。**事實（原始診斷，供對照）**：`request_log.log_generation()` 只有 `image_model` 一個模型欄位（`request_log.py:92`），沒有任何欄位記消化模型。正式站後台匯出的 200 筆（`20260916/全部_2026-09_後台原始.json`）`model` 欄全是生圖模型（`openai/gpt-image-2.5-sunburst` 124 筆、`openai/gpt-image-2` 62 筆）。👉**因此「正式站的消化模型是不是 anthropic/claude-sonnet-5」無法從紀錄證實**，只能推論（`main.py:126` 寫死 OpenRouter 後端預設為它，正式站也是 OpenRouter 後端，除非部署環境另設 `DIGEST_MODEL` 覆寫）。⚠**這對 R6 有直接影響**：正式站若用別支模型，本機量到的截斷率與 token 分布不能直接套用。💡**最小修法**：`log_generation()` 加一個 `digest_model` 欄位，由 `generate()` 把 `resolve_digest_model()` 的結果傳進來。📌另有一條零成本的替代路徑：請設定部署的同事直接回報正式站的 `DIGEST_MODEL` 值。 |
| B76 | 🔴**中國大陸輪廓圖把臺灣畫進去**——播出事故等級，使用者要求百分之百避免 | 2026-09-17 R7 實拍輪 A2（國銀對中曝險）使用者當場指認 | 🔶 **2026-09-20 規則層（①）已修，兩層都補**（exp `7dcbfa0`，worktree commit `dbadbf6`；重凍已於 2026-09-20 獲使用者授權）｜⚖ **2026-09-17 使用者裁定：這一波要修，而且要求「百分之百絕對避免」**｜⚠**「百分之百」尚未達成**：本次做的是修法方向①規則層，②出口強制層（中國地圖走程式底圖／強制人工複核）**仍待使用者裁定**，見說明欄末 🔬**驗收時根因被推翻**（team-lead 獨立查證）：帳本原本把本列當成消化端規則不足，實際上**真因在生圖端**——事故那則 `chart_type=資料圖表`，而 `news_prompt.MAP_ACCURACY_IMAGE_RULES` **只在 `type_label==地圖類` 才注入 `build_prompt()`**，所以生圖模型在那一層**連一條地理規則都拿不到**，是它自己用世界知識把臺灣填進裝飾用的中國輪廓，不是消化端的 `structure` 叫它畫的。**只補消化端擋不住。** | **使用者原話**：「剛剛國銀曝險的那一張圖，畫中國大陸輪廓圖的時候有台灣，這是絕對不可以接受的，因為台灣不屬於中國大陸，這會引起觀眾極大的反感，一定要百分之百絕對避免」。 🔬**監督已放大核對，確認屬實**：`20260917_R7_A_資料圖表_2.png` 右上角的中國大陸輪廓圖，東南外海有一座島，**與大陸本體同一種填色、包在同一圈輪廓光暈之內**——也就是被當成中國版圖的一部分畫出來。（下方另一座島是海南島，那本來就屬於中國，不是問題。）放大圖 `_B76_中國輪廓含臺灣_放大.png`。 ⚠**嚴重度說明（為什麼這條比其他版面缺陷都高）**：這不是美觀問題也不是忠實度問題，**是立場問題**。對臺灣的電視新聞台而言，把臺灣畫進中國版圖等同於在畫面上做出政治宣示，一次就足以構成播出事故。而且它**不會被現有的任何一道閘門攔下來**——安全框、簡體字白名單、塊數防呆、肖像分流都管不到地圖輪廓的內容。 📌**發生條件比看起來寬**：本例的新聞原文完全沒有提到臺灣（稿子講的是國銀對中國大陸曝險金額），**是生圖模型自己補上的世界知識**。所以觸發條件不是「稿子提到臺灣」，而是「畫面上出現中國地圖」——兩岸經貿、中國經濟、陸股、台商、關稅這幾類題材都會撞到，**頻率不低**。 🛠**修法方向（尚未實作，需先解凍）**：①**規則層**：在地圖／版圖相關規則裡明文寫死「中國大陸的輪廓或版圖不得包含臺灣、澎湖、金門、馬祖；臺灣若要出現必須是獨立的、與大陸不同填色的個體」。②**出口強制層**：規則是請求不是保證（B61 那條線的教訓），光靠 prompt 擋不到 100%，而使用者要的是「百分之百絕對避免」。⚠**但程式端很難自動驗**——「圖上那座島有沒有被算進中國」需要影像理解，監督已記取 `feedback_map_image_verification_unreliable`（AI 看圖驗地理不可靠）的教訓，**不建議用模型自動判讀當閘門**。③**因此實務上的 100% 只有兩條路**：要嘛**中國地圖類一律走程式底圖**（像地圖類型已經有的 `build_map_reference` 真實底圖路徑），不讓模型自由畫輪廓；要嘛**這類題材強制人工複核**。**這兩條都需要使用者裁定，監督不自行選。** 🔗**相關**：[B25]（AI 憑空畫地理）是同一類根因但不同後果；[B20]（大陸譯名用語擋不住）是同一種「立場外洩」風險的文字版。 🔧**2026-09-20 已實作（修法方向①規則層，兩層都補）**：①消化端 `main.py` 新增 `CHINA_TAIWAN_OUTLINE_RULES`，**獨立於既有的 `MAP_ACCURACY_RULES`／`MAP_SCOPE_GUARD_RULES` 地圖／非地圖 if-else 分流之外，兩條路一律注入** `build_digest_instructions()`——寫進任一邊都會漏掉另一邊，而本例正是漏在「非地圖」那邊；②生圖端 `news_prompt.py` 新增 `CHINA_TAIWAN_OUTLINE_IMAGE_RULES`，比照 `TEXT_PLACEMENT_RULES` **不看 `type_label` 一律注入** `build_prompt()`，這一塊才是堵住真因的那一塊；③補進 `DEDICATED_INSTRUCTION_RULES_TEMPLATE` 的「不可鬆綁」清單，堵住使用者指令欄理論上能宣稱蓋過它的路。**規則文字要點**（team-lead 逐句讀過）：點名臺灣／澎湖／金門／馬祖不得與大陸同填色、同輪廓、同一圈光暈；**明文把海南島排除在外**（海南確為 PRC 領土，若寫死「中國輪廓不得含島嶼」會製造新的地理錯誤）；涵蓋「新聞原文完全沒提到臺灣」的情境（本例正是如此）；並給退路——**沒把握就不要畫中國輪廓，改用文字描述位置與範圍**。**重凍使用情形**：10 份 fixture 受影響（8 份 digest＝記者／編輯 × standard／simplified × fullbleed／safearea，2 份 `reporter-image-prompt-{safeframe,plain}`），**全部是純新增、零行刪除**（digest 各 +3、image prompt 各 +9），插入點一致，`cover_design_brief_rng_pins_20260911.json` 等其他 fixture 零異動；`tests/test_reporter_prompt_frozen.py` docstring「快照更新記錄」已補 2026-09-20（B76）一段。新增 `tests/test_b76_china_taiwan_outline_20260920.py`（19 題，涵蓋地圖／非地圖／自動判斷、記者／編輯、密度×版面、safe_frame／no_text、規則措辭本身、以及指令覆寫清單有沒有點名這條）。⚠**仍未達成「百分之百」**：這一層是 prompt 規則，**規則是請求不是保證**（B61 那條線的教訓，本列上面也早就寫過）。要真的到 100%，還是只有說明欄上方那兩條路——中國地圖類一律走程式底圖，或這類題材強制人工複核——**兩條都要使用者裁定，本次未動。** |
| B77 | 🔴**十點雙切與 YT 整點的「AI 消化標題」一律 502**——D21 換 Gemini 的後遺症 | 2026-09-17 使用者回報「10點不一樣的AI消化標題報錯502」，Claude 以瀏覽器實測重現 | ✅ **已修待驗（exp，2026-09-17）**｜⚖ **2026-09-17 使用者裁定：schema 走 A 案（只拿掉 enum、不動 prompt），這條路徑的零重試同批補上** | **根因**：`COVER_TITLE_DIGEST_SCHEMA_TEN`（editor_formats.py:1262）與 `_YT_HOURLY`（:1353）把 enum 掛在 integer 上（`"topics": {"type": "integer", "enum": [1, 2]}`），是全 repo 唯二這樣寫的地方。Gemini 的 responseSchema 子集只吃字串 enum；D21 之後 strict ＋ `provider.require_parameters=true` 會強制路由到真的照 schema 執行的端點，整包請求被上游退成 400 → `BadRequestError` → main.py 的 `except Exception` **零重試**直接 `HTTPException(502)`。主消化那條有 `DIGEST_ATTEMPTS=5` 吸收得掉，這條沒有，所以只有這裡爆。**誤導點（值得記住）**：Cloudflare 會把 origin 回的 502 body 換成自己的 HTML 錯誤頁，前端 `_apiError` 因此讀不到 detail，只顯示通用文案「AI 服務暫時忙碌或無回應」，一度被判成基礎設施故障而不是我方 handler。**受控 A/B 隔離變因（同一段新聞、同一端點、相隔數秒）**：`ten_cover` 502／2.6 秒、`ten_cover_full` 200／5.6 秒、`yt_vstrip` 200 ⇒ 變因既不是陣列欄位也不是「兩個標題欄位」，只有 integer enum 與失敗完全同步；且失敗後數秒內同一端點即成功，證明容器從未崩潰。**修法**：兩處改成 `{"type": "integer"}`，型別、prompt 條文、`== 1` 的判定邏輯全部不動。（Codex 顧問原本建議 C 案「整個 topics 從 schema 拿掉、後端自行推導」，未採用：C 要改 editor_formats.py:1144-1151／:1336-1343 的 prompt 條文，踩到重凍額度；且那段 prompt 是拿 topics 當「先決定幾則、再動筆」的思考腳手架，拆掉是行為風險不是重構。）**同批補上有界重試**：`_cover_title_completion`（`COVER_TITLE_UPSTREAM_ATTEMPTS=3`）只重試暫時性故障（`APIConnectionError`／408／5xx），400／401／403／404／422／429 一律立即失敗——schema 400 是確定性錯誤，重送同一個 payload 必然再失敗，只會拖長等待並多燒額度；SDK 層不會疊第二層重試（`OPENAI_MAX_RETRIES = 0`）。**新測試** `tests/test_b77_digest_schema_enum_20260917.py`（8 題）守的是**整類問題**（任何非字串型別上的 enum）＋重試分類，已反向驗證：把 enum 塞回去會紅 2 題。原本擋不住是因為 cover-titles 的測試全是 mock，沒有人檢查送出去的 schema 長什麼樣。✅**本機對真實 Gemini 實跑 2/2 通過**（2026-09-17，`ten_cover`／`yt_hourly` 各打一次，`provider=Google`——正是原本會被退 400 的那條路徑；拿掉 enum 後模型照樣回合法的 `topics: 2`，單雙主題判定沒失準），全套 1998 題綠（含把 `test_format_consolidation.py:239` 那支「斷言 enum 必須存在」的過期測試改成新形狀）；**正式站尚未部署**。⏱**逾時刻意不重試**：`APITimeoutError` 是 `APIConnectionError` 的子類，放行的話一次耗盡＝3 × 90 秒＋backoff ≈ 272 秒，外層字數重試再來一輪會破 460 秒，正好重演 2026-09-10 那次 Zeabur 300 秒上限、前端卡在 35% 的事故；要救的 7.7% 本來就是秒級的 finish=error／provider 5xx，不含 90 秒卡死。📌**附帶發現（與本列無關，另行追蹤）**：`aicg.tvbs.ai` 的 app.js 是舊版（兩參數 `_apiError`，fallback 只有 "HTTP " + status），比 repo 落後一截，會讓正式站的錯誤訊息少掉中文提示。 |
| B80 | **YT 直播直標：LIVE 章與藍底不是官方素材（生圖模型重製的去背 PNG），使用者已提供原檔須重置；標題字級要再縮小、LIVE 與直標之間不該留空隙** | 2026-09-20 使用者追加（空隙那半＝B46，2026-09-15 曹雪卿已提過） | 🔶 **已上線待實拍驗收（commit 1c93504「0920 第一波」，已在公司 main；2026-09-23 帳本補登）**｜⬜ **2026-09-20 使用者列入下一批動刀**｜✅**官方素材已到手**：`G:\我的雲端硬碟\Claude共用\AICG\YT直播側標底圖PNG\`，兩張——`直播底圖(直).png`（1914×1063）與`原音呈現底圖(直).png`（1905×1070），**都是真去背**（alpha mean 0.047／0.055，本 session 以 `magick` 驗過）｜⚖**2026-09-20 使用者裁甲案**（整張官方底圖當固定層、程式只壓字、標題遷就固定色框）｜📐**官方檔幾何已量測完畢，數字見一句話現況**｜⚠**連帶要訂字級下限**（色框可用長度從 0.64h 砍到 0.456h） | 三件事，都在 `yt_vstrip`：①**素材**——`compose.py:1376` 白紙黑字寫著「LIVE 章是生圖模型重製的高清去背 PNG（`static/brand/live-badge.png`）；**官方檔到手時**……」，使用者現在把原 PNG 給了，這條技術債可以結掉，**但要先拿到檔案路徑**；藍色底同理。②**字級**——標題字級縮小＝色框整體變短，動的是 `VSTRIP_COLUMN_MAX_RATIO`（0.64）／`VSTRIP_MAIN_PITCH_RATIO`（0.070），**照 YT 塊高那次的教訓，先量現行成品再訂數字，不要憑感覺給值**。③**空隙**——根因已對上：`VSTRIP_TOP_RATIO=0.166`（一般版）與 `VSTRIP_TOP_WITH_LABEL_RATIO=0.191`（有小標時）是兩個固定比例，**沒勾小標時那一格仍然被留著**，LIVE 章與直標之間因此空一段（B46 實測約 65px）。⚠**頻道實際用字是「原音呈現」不是「原音重現」**（`tests/test_yt_cover.py:695` 釘過）。 ⚖**2026-09-20 使用者已裁：甲案**——整張官方底圖當固定層，程式只壓直排文字與來源；**標題長度反過來遷就固定色框，超出就縮字級，不准再拉長色框**。📐**官方檔量測（本 session 以 `magick` 實測，非目測，動工直接用這組數字，不要再憑感覺給值）**：`直播底圖(直).png`（1914×1063）紅章 184×71 @(31,100)、**藍色框 189×485 @(31,171)**，換算比例＝左緣 0.0162／上緣 0.1609／寬 0.0988／**高 0.4563**；`原音呈現底圖(直).png`（1905×1070）紅區（含白底小標）194×146 @(19,67)、**藍色框 194×489 @(19,213)**，比例＝左緣 0.0100／上緣 0.1991／寬 0.1018／**高 0.4570**。🔑**兩個立即可用的結論**：①**現行 `VSTRIP_TOP_RATIO=0.166`／`VSTRIP_TOP_WITH_LABEL_RATIO=0.191` 與官方的 0.1609／0.1991 幾乎一致**，代表上緣本來就抓得對，③那個空隙是「色框沒有從這個上緣開始長」造成的，不是上緣訂錯；②**官方色框高固定在約 0.456h，而現行 `VSTRIP_COLUMN_MAX_RATIO` 上限是 0.64h**——甲案等於把可用長度砍掉約三成，**長標題撞縮字級的機率會明顯變高，字級下限要一起訂**（否則會從「色框過長」變成「字小到看不清」）。⚠**對位基準：兩張的左緣與寬度都不同（0.0162 vs 0.0100），不要用畫布左上角對齊，一律以藍色框左緣為基準**。🚩**2026-09-20 看過官方素材後的原始發現（保留判斷軌跡）**：使用者給的兩張不是「LIVE 章」單件，而是**整張去背覆蓋層**——LIVE 章＋藍色雙欄色框＋右上 TVBS NEWS 字標一次到位，而且**LIVE 章與色框在原檔裡就是貼合的（零空隙）**，等於③那個問題素材自己就解掉了。**但色框長度在 PNG 裡是固定的**，與現行「色框長度隨標題字數浮動」（`VSTRIP_COLUMN_MAX_RATIO`／`VSTRIP_*_PITCH_RATIO`／`VSTRIP_VERTICAL_ANCHOR` 那一整組）**直接衝突**。兩條路：**甲｜整張官方底圖當固定層，程式只壓直排文字與來源**——長相百分之百對，但**標題長度必須反過來遷就固定色框**（超過就得縮字級，不能再拉長色框）；**乙｜只從官方檔切出 LIVE 章與色框材質，色框長度照舊由程式算**——保留浮動，但材質接縫要自己處理。⚠**另外兩張尺寸不是 1920×1080（1914×1063／1905×1070），也不相等**，貼上去之前要先決定對位基準（等比縮放到畫布、還是以 LIVE 章左上角對齊），**不要直接 paste**。 |
| B81 | **正式站 `aicg.tvbs.ai` 的 `app.js` 落後 repo 一截，錯誤訊息少掉中文提示** | 2026-09-17 修 B77 時附帶發現，2026-09-20 使用者指示一併列管 | ✅ **2026-09-20 結案（實查後否定原判斷）**——正式站的檔案沒有落後；使用者看到舊版是**瀏覽器快取**（`?v=` 被一支測試釘死在 08-20，已修）；另外查出真正落後的是 repo 的 `main`，已 merge 補齊 | ⚖**2026-09-20 使用者要求上線實查，結果與原假設相反**：`curl` 抓 `aicg.tvbs.ai` 與 `aicg.tvbs.dev` 的 `/app.js`，兩者都是 237448 bytes、sha1 `247b0193d334`，與 `origin/exp/redesign-20260903` 的 `app.js` **逐位元組相同**。正式站跑的就是 exp HEAD，**沒有部署落差**，前端改在 exp 上會直接上線。原判斷之所以錯，是因為拿 `origin/main` 當基準比對——而 `main` 的 `app.js` 只有 106104 bytes（sha1 `fcfb2c2dbb38`）、VERSION 停在 `260915-01`。**真正的缺口在 `main`**：183 檔、43259 行沒跟上，且 `static/brand/` 九個品牌 PNG 與 `static/fonts/` 兩套字型（含 21MB 台北黑體 Bold）整個沒進來，拿 `main` 部署會因為找不到字型與角標素材直接壞掉。成因是先前逐次同步只搬 `MASTER-列管清單.md`，沒把程式與素材一起帶回來。**已於 2026-09-20 以 `merge(exp→main)` 補齊（main `37e3945`，已推 origin）**：衝突四處（`VERSION`／`TODO.md`／`docs/yt-live-cover-spec.md`／`index.html`）逐段確認 HEAD 側皆為空，一律取 exp 側；`main` 獨有的 `docs/error-cases/2026-09-05-消化模型頻道洩漏…md` 保留不動。合併後 main 全量測試 **1999 題全綠**（0 失敗 0 錯誤，1 個 expected failure），且 `main`／`exp`／正式站三方 `app.js` sha1 一致。🔍**原始症狀的真正根因（2026-09-20 續查）**：使用者當初看到的「正式站錯誤訊息還是舊版」不是伺服器問題，是**瀏覽器快取**。`index.html` 的 `<script src="app.js?v=20260820a">` 停在 08-20，而 `app.js` 實際已改到 09-16；`hybrid.js?v=20260815a` 也停著、檔案已改到 09-06。瀏覽器按完整 URL 快取，字串不換＝老使用者永遠拿舊檔。**而字串之所以一直沒換，是被一支測試釘死的**——`tests/test_light_luxury_style.py::test_cache_buster_moves_forward` 名字叫「往前走」，內容卻是 `assertIn('app.js?v=20260820a', html)`，任何人想往前調都會撞紅這題。已把兩個 `?v=` 調到 `20260920a`、在 `index.html` 那行上方留警語註解，並把該測試重寫成它名字該做的事：正則抽出日期→驗格式→`assertGreaterEqual` 下限 20260920，且同時涵蓋 `app.js` 與 `hybrid.js`（往後調不用改測試，忘記調或往回調才會紅）。⚠**教訓**：`assertIn` 一個會隨版本變動的絕對值，等於把那個值凍結，而且凍結得無聲無息——以後寫這類「必須往前」的守門測試一律用不等式，不要用等值比對。 📌**附帶發現（交辦必讀）**：這個 repo **沒有安裝 pytest**（`pyproject.toml` 的 dev group 只有 ruff），測試一律是 `unittest`，而 `tests/` 沒有 `__init__.py`，`unittest discover` 要手動把 repo 根目錄與 `tests/` 掛進 `sys.path` 才跑得起來——派工時不要再寫 `python -m pytest`。 ~~原假設：正式站的 `_apiError` 還是兩參數舊版、fallback 只有 `"HTTP " + status`，所以 B77 那次前端只看得到通用文案~~（已否定：正式站就是最新版）。 |
| B82 | 🔴**「川習會」這類簡稱標題推導不出人名 → 零參考照 → 生圖模型自己編臉，畫錯人** | 2026-09-21 使用者回報正式站 `aicg.tvbs.ai` 後台 11:01:35 那筆（王結玲，id `50d0f78baccd`，十點不一樣雙切）：「川習」根本畫錯人了 | 🟢**別名表已上線並實機驗過（2026-09-22 使用者確認「川習會OK了」）**｜**不是生圖模型亂畫，是餵給它的畫面描述本身就沒講是誰**。根因是反幻覺護欄咬到自己：`editor_formats.py:1052` 規定「organisation without a personal name is not a named person — use anonymous figures」，`:1057` 再加「names copied **VERBATIM**… Never infer a name from a title, event, organisation or common knowledge」。標題寫的是「本周**川習會**」——那是**事件簡稱**，「川普」「習近平」五個字一個都沒出現，所以推導模型照規則不准認出他們，`portrait_subjects` 交白卷，畫面描述退成「兩位領導人握手致意面向鏡頭」。接著 `main.py:1932` 講明下一步：沒有逐字人名就 「the person cannot be looked up and **no face can be drawn**」——`photo_lookup` 不查、零張參考照，生圖模型手上沒有任何臉可以對照，只能編。**系統沒有別名表**，「川習」「拜習」「蔡賴」這類台灣新聞慣用簡稱一個都認不得。⚠**而且規則沒有被穩定遵守**：同一批紀錄 10:57:44 的「**川**劍指中國」推出來的卻是「**川普**在講台上面容嚴肅正視前方演說」——一樣是單字簡稱，那次模型自己補了名字。所以現況兩頭都不對：遵守規則→畫錯人；不遵守→名字對了但那正是護欄要擋的推論行為，同一功能忽有忽無，使用者無法預期。｜**建議修法＝別名表**（三條路中唯一不放寬護欄的）：維護一張封閉集合的政治人物**複合事件詞**對照（川習會→川普＋習近平、拜習會、普習會…），在推導**之前**由程式展開。關鍵是它是**確定性的人工維護表、可稽核**，不是模型推論——護欄擋的是模型腦補，不是程式查表。⚠**限複合詞，不可做單字展開**：「川」單字有真實碰撞（川普＝Sichuan-accented Mandarin 也是這兩個字），拆成單字會製造新的誤判。另兩條路都不建議：放寬 VERBATIM＝直接打開幻覺閘門；查不到人就完全不畫人＝「川習會」配一張沒人的會議廳，編輯不會接受。 | 🔍**同一則新聞連生三次，對照組自己就把因果講完了**：①11:01:35 `50d0f78baccd`（出事那張）R＝「國際會議廳內並排陳列兩國國旗，**兩位領導人**握手致意面向鏡頭」→**沒有名字**；②11:03:52 `f31955db16b2`（重生）R＝「國際會議廳外兩國旗幟並列，各國媒體長焦鏡頭群嚴陣以待」→**乾脆沒有人**；③10:57:44 `6ba926a60af4`（另一則）R＝「**川普**在講台上面容嚴肅正視前方演說」→**有名字**。三筆的輸入都只有標題（46／46／47 字），沒有新聞內文。📋**順帶查到兩個缺口**：(1) **後台完全沒有記錄 `portrait_subjects` 與參考照張數**——`grep portrait_subjects admin_console.py` 零筆；伺服器 log 有（`main.py:3773` 的 `portrait_subjects=[...] en=[...] 參考照=N 張`）但後台看不到，所以「這張為什麼畫錯人」只能反推、沒有直接證據。跟 B55 當天補的「創意等級沒存」是同一種診斷缺口，建議一併補進稽核欄。(2) **就算認出名字，習近平仍可能拿到合照**——`photo_lookup.py:20` 白紙黑字「習近平的首圖與 P18 **都是會面合照**」，而同檔第 15 行註明「**合照當肖像參考正是會讓模型畫錯臉的輸入**」。亦即此則新聞就算簡稱問題解決，還有第二關。⚠**與 D22 的關係（使用者當場提問「生圖看得到原文摘要算不算修好」）**：**不算**。D22 至今零行程式（2026-09-21 只做了 7 項裁決，計畫在 `docs/plan-d22-news-text-to-image.md`），而且**做完也救不回這一筆**——輸入只有 46 字標題、根本沒有內文，推導不出原文裡沒有的全名。D22 能救的是「內文有全名、標題用簡稱」，救不了「從頭到尾只有簡稱」，而後者正是十點不一樣這種純標題輸入版型的常態。🚨**D22 實作時必須擋掉一個後門**：VERBATIM 允許的來源含 「the supplied description」，若 `visual_context` 算進去，流程會變成①消化模型從「川習會」**推論**出人名寫進 `visual_context` → ②下游認定「來源材料有寫」而合法填入 `portrait_subjects`，**推論被洗成逐字事實**，護欄第 1 步擋的東西被第 2 步繞過。猜對沒事，猜錯就是拿一張錯的臉去查照片再畫一張很有說服力的錯圖，比現在畫兩個陌生人更難發現。故須明定 `visual_context` **不是** VERBATIM 的合法來源。✅**2026-09-22 實機驗收通過**：測試站以「川習會」標題生成，右格畫面描述確實帶出川普與習近平（使用者回覆「川習會OK了」）。⚠**上面那兩個後續仍未動**——(1) 後台沒有記 `portrait_subjects` 與參考照張數，這次沒咬到不代表下次不會；(2) 習近平的維基首圖仍是會面合照，這一筆沒踩到不等於那一關解決了。 |
| B83 | 🔴**追加修改（`/api/images/refine`）之後「畫面來源」與「示意圖」標籤整個消失** | 2026-09-22 使用者回報：「畫面來源 在追加修改後消失不見」 | 🟢**已修（2026-09-22，VERSION 260922-04）**｜真因：`ImageRefineRequest` 根本沒有 `disclaimer_*` 三個欄位，`refine_image()` 也從不呼叫 `apply_image_disclaimer`——與 B70 原始事故同一種斷線（「prompt 說軟體會壓，軟體那半從沒被叫到」），而且**「示意圖」也一起中彈**（使用者只看到「畫面來源」那半）。修法：`ImageGenerateResponse` 新增 `disclaimer_kind`／`disclaimer_source_text`／`disclaimer_corner` 記下**實際貼上**的那一組，refine 原樣帶回、後端**不重判**（refine 不送 `portrait_subjects`，重判會把「示意圖」降級成「畫面來源」＝對觀眾說謊）；`replacement_person` 有值時強制 `ai` 並清掉來源名。守門測試：`tests/test_b83_b84_f47_refine_label_and_density.py`＋`test_b70_f43_disclaimer.py`（原本那條 `test_the_refine_path_does_not_send_them` 把缺陷寫成了規格，已改寫）。⚠**未實機驗證**，等使用者實拍。 |
| B84 | 🔴**F38 的 2K 在兩條路上從來沒有成立過**——第二／三頁生圖與追加修改都漏送 `density` | 2026-09-22 查 B83 時連帶發現（使用者原報是「編輯CG 無安全框 成品變成 1748×924」） | 🟢**已修（2026-09-22，VERSION 260922-04）**｜`app.js` 第二／三頁的生圖 fetch 沒有 `density` 那格，後端收到 `""`；`ImageRefineRequest` 連欄位都沒有 → `image_generation_size()` 的高解析度閘門永遠 False，字多／字超多生的 2K 圖**只要一改就悄悄掉回 1K**，畫面上看不出來。四個產圖送出點現在都送 `density`，並用 `APP_JS.count("density: state.density") == 4` 釘住。⚠**1748×924 本身不是這條 bug**：編輯對位框（安全框 OFF）的成品就是拉伸後的對位框本身（2026-08-19 裁決，`safe_frame.apply_safe_frame` docstring），使用者該次用的是「字少／不改字」檔，屬**設計如此**；要改成整張輸出＝推翻 08-19 裁決，見 D24。 |
| B85 | 🔴**編輯CG 的「原圖放置」照片被生圖模型重畫，沒有真的放圖** | 2026-09-22 使用者回報正式站後台 10:53:03 那筆（操作者 曹雪卿） | ⬜ **待裁（D25）**｜**不是回歸，是這條路從來就沒有像素保護**：`main.py:999` 已寫明「這是 prompt 層級要求，模型仍可能有壓縮/色偏等落差，**不保證像素級一致**」，`apply_user_references_to_image_request()` 對 `asis` 只注入一段 `USER_REFERENCE_ASIS_RULES` 文字。B55 的四刀（透明底標題圖層＋四道閘＋逐像素疊圖）**只做在十點不一樣與 YT 封面**（`_cover_ai`／`protect_base`），編輯CG 完全沒接。而 B55 本身已實測證明 prompt 擋不住（創意 0 級 change_ratio 就有 68.1%）。→ 要不要把 B55 那條「透明底標題圖層＋程式疊圖」整套移植到編輯CG？見 D25。<br>**2026-09-22 追查（使用者問「原因是否出在創意階梯」）：是加重因子，已從正式站實勘證實，但不是真因。** 同一操作者同一則的前一筆 10:47:32（`99b1373ef899`，編輯／自動判斷／份量 maximum／gpt-image-2.5-sunburst）成品 prompt 的 STRUCTURE 逐字寫著：「a full-height dramatic **hero cutout image** showing the user's original supplied photograph **placed unaltered**, **intersecting an anticlockwise diagonal brush-stroke dividing edge**」。`USER_REFERENCE_ASIS_DIGEST_RULES` 有生效（「placed unaltered」是它逼出來的），`USER_REFERENCE_ASIS_RULES` 也確實在 prompt 末端（index 14231/15494）——但同一句話同時命令模型 cutout ＋ 讓照片跟斜切邊界相交，與 ASIS 規則的「Do not crop, stretch, rotate, mirror or otherwise distort」**正面矛盾**。來源是創意 4 級的 `_CG_L4_EXTRA`「ONE SIDE OF THE FRAME IS GIVEN TO A SINGLE DRAMATIC IMAGE running the full height」與 `_CG_L3_EXTRA`「THE SUBJECT IMAGE BECOMES AN OBJECT, NOT A BACKDROP: **cut the main subject out**」，外加 `_CG_DESIGN_DRAW_TEMPLATE` 的「PALETTE: work in these four and no others」（整張硬性配色＝要求改照片的顏色）。**缺口**：消化端護欄只封「depiction／illustration／rendering」這類措辭，沒封 cut out／crop／tilt／recolour 這三個動詞，所以創意 ≥3 時消化端是**被命令**寫出違反 ASIS 的版面語。⚠仍不是真因——B55 實測創意 0 級 change_ratio 就有 68.1%，把階梯關掉照樣會被重畫。<br>**2026-09-22 使用者裁「先做候選4」→ 已上線（VERSION 260922-12）**：`USER_REFERENCE_ASIS_DIGEST_RULES` 加三行——①明文 OVERRIDE 創意階梯與 DESIGN DRAW，聲明那一格不受任何版面／美術令管；②逐字點名 cut out／cutout／cropped／tilted／slanted／intersecting／tinted／colour-graded／recoloured／duotone 等動作措辭（原本只封 depiction／illustration 這類畫風形容詞）；③正面給法——描述成「正立矩形面板」，創意等級要的斜切「走這一格**周圍**」。守門測試 `tests/test_b85_asis_creativity_conflict.py`（8 題）釘住：動作詞全在、舊護欄沒被換掉、明文 OVERRIDE 在、注入點排在創意階梯**之後**（用真的 `build_digest_instructions()` 驗，不是讀原始碼）、0–4 級都注入、沒附圖時逐字元不變（凍結快照因此零異動）、以及**生圖端仍然沒有 change_ratio／protect_base**（釘住「這不是 B85 的解」）。全量 2342 題綠。⚠**B85 維持 🔴 未結案**：候選④只消掉「創意 ≥3 時的必然衝突」，像素保護仍然不存在，真正的解在 D25 候選①／②，仍待裁。**尚未實機驗證**——要等正式站跟上版本後，用同一則、同一張照片、創意 4 再跑一次比對。 |
| B87 | 🔴**記者CG 字多／字超多的安全框比例整個跑掉——交付畫布被 F38 改成 21:9** | 2026-09-22 使用者回報正式站後台 14:56:48 那筆（操作者 許岱軒）：「記者CG 字多 安全框比例完全是錯的 沒有跟著 2K 調整」 | 🟢**已修（2026-09-22，VERSION 260922-05）**｜真因：`HIGH_RES_OUTPUT_CANVASES` 把「生成尺寸」和「交付畫布」混成同一張表，理由寫的是「高解析度＝不再靠升採樣，兩者必須是同一組數字」。那句話對 16:9 成立，對 21:9 **不成立**——21:9 從來不是交付比例，它是記者開安全框時的**生成**技巧（見 `SAFE_FRAME_ASPECT_RATIO`），非高解析度那條路一律把它放進 16:9 的 `BASE_CANVAS`。F38 等於悄悄把記者的交付檔從 1920×1080 換成 3360×1440。實測 `safe_rect(*canvas,"記者")`：字少 21:9 → 框比例 2.176 ✅／字多 16:9 → 2.174 ✅／**字多 21:9 → 2.853 ❌**。修法：改成單一 `HIGH_RES_OUTPUT_CANVAS = (2560,1440)`，provider size 不動（21:9 仍送 3360x1440）。守門測試改釘不變量（交付畫布比例＝1920/1080；三個 profile × 兩個生成比例的安全框形狀與規格差 <0.01），不再釘數字——舊的 `test_output_canvas_matches_the_provider_size_pixel_for_pixel` 把缺陷寫成了規格。⚠**只有記者＋安全框 ON＋字多／字超多會中**：編輯與所有封面都釘 16:9。⚠未實機驗證。 |
| B86 | 🔴**未置框那條路（記者＋安全框 OFF）的成品會被貼上第二枚標籤** | 2026-09-22 本波自產，顧問複查在推上公司之前攔下 | 🟢**已修（2026-09-22，VERSION 260922-04）**｜`finalize_image_result` 在 `safe_frame=False` 時提早 return，`source_image_base64` 留空 → 前端 `refineSourceFromResponse()` 退而取**成品本身**，而成品已經有一枚標籤 → 再送進 refine／restamp 就多出第二枚（舊角落一枚、新角落一枚）。`ImageRestampRequest` 的 docstring 早就寫了「再貼會變兩枚」，卻沒有任何東西擋。修法：`apply_image_disclaimer` 在那一格為空時補進「貼標籤之前」那張（置框那條路已有值就不動，語意仍是「置框前原圖」）。守門測試用「同角落重貼必須逐像素相同」當 oracle。⚠**編輯身分一律置框所以碰不到，會中彈的是記者**——這也是本波自己的測試看不到它的原因（餵的是乾淨圖）。 |
| F47 | **「示意圖」／「畫面來源」標籤的位置要能成圖之後再改，不必重生一張** | 2026-09-22 使用者：「位置是 PILLOW，理論上成圖之後要讓使用者修改位置」 | 🟢**已上線（2026-09-22，VERSION 260922-04）**｜新端點 `POST /api/images/restamp-disclaimer`：拿回應裡那張**置框前原圖**（`source_image_base64`），照原參數重跑 置框→挖空框→貼標籤，**一次生圖 API 都不打**。前端四個角落按鈕在有成品時即時呼叫。守門測試釘住「函式體裡不准出現任何 `generate_*`」。限制：① 只在第一頁（一鍵生成）那條線有效——第二／三頁不維護 `refineSource`；② 播出鏡面回 400（浮水印版位綁在挖空框上，不吃 `disclaimer_corner`）；③ 封面版型走 compose 自己的 `_draw_ai_note`，不吃這組。後台分類為「合成」（同 YT 直標，純 Pillow）。⚠**未實機驗證**。 |
| B94 | 🔴**消化失敗把上游的真話整個吞掉，後台只看得到我們自己寫死的那句話** | 2026-09-22 使用者回報「DEV後臺跳多筆錯誤」「DEV全部失敗 但正式版正常」 | 🟢**已修（2026-09-22，VERSION 260922-10）**｜DEV 18:28–18:54 連倒 5 筆（許岱軒，自動判斷／記者，同一篇 471 字川習會稿），後台每一筆都只有 `error: provider_5xx · HTTP 502: AI 服務處理失敗，請確認模型權限或稍後重試`——那句是 `main.py` 寫死的常數，OpenRouter 真正回的內容只 `print` 到 stdout 就沒了。結果**額度不足／模型沒權限／provider 掛掉**三種完全不同的故障在後台長得一模一樣，只能重跑一次用猜的。生圖那條早就把 OpenRouter 的 JSON 原文帶進訊息（2026-09-17 那筆 `safety system` 的紀錄看得到），消化這條補上同樣待遇：新 `upstream_error_detail(exc)`，把**上游狀態碼＋訊息摘要**（壓成一行、截到 200 字、`sk-`／`pk-` 形狀的字串遮蔽）接在原句後面，`generate()` 與 `hybrid_digest()` 兩條 APIError 分支共用。守門測試釘「三種故障不准再產生同一個字串」與「main.py 裡那句死字串只剩四處」。⚠ **這是觀測缺陷不是故障本身**——下一筆 DEV 失敗就會直接寫出真因。⚠ `_error_type_from_http` 是看訊息文字分類的，帶上原文後含 `credits` 的 502 會被歸成 `provider_4xx`；那個分類更接近事實，不視為退步。 |
| B96 | 🔴**「伊朗外交部發言人貝卡伊」畫成無臉剪影——B73 裁定的「維基查不到也交給模型自畫」其實沒有生效** | 2026-09-23 使用者回報正式站後台 16:55:18 那筆（操作者 張正安，記者 CG，simplified，id `03f0675ac43b`），成品存 `D:\Downloads\AICG\後台紀錄\20260923\20260923_張正安_貝卡伊無臉.png` | 🔶 **已修待實拍驗收（2026-09-23，VERSION 260923-01，commit 4280aa2；sol 實作、Claude 審查，2356 題綠）** ｜甲乙丙三案都做：乙＝PORTRAIT_NO_ENTRY_FALLBACK 預設開（B73 裁定正式生效），20 題逐題分類見 commit；丙＝portrait_subjects_en_guess＋Wikidata 國籍與職務雙吻合才採用。⚠丙的剩餘風險：模型猜成同國籍同職務的前任（例如前任發言人）會通過比對，用到別人的照片；比對的是整篇原文不是逐人 | **因果鏈**：①中文維基沒有「貝卡伊」條目（巴加埃伊等譯名也查不到），`TW_PORTRAIT_NAME_ALIASES` 沒收他 → `entry_found=False`，落到第 4 層；②程式碼裡 `PORTRAIT_NO_ENTRY_FALLBACK` **預設是 false**（`main.py:4866`，註解寫明因 20 道守門測試會轉紅而刻意先關），本機 `.env` 沒設，正式站的行為（出現重新消化排除的指紋）也證實沒開 → 網頁版走 `main.py:2986` 之後的分支，**重新消化一次並注入 `EXCLUDED_PEOPLE_RULES_TEMPLATE`**（「PEOPLE WHO MUST NOT BE DRAWN」）→ 消化模型照辦，在 STYLE／STRUCTURE 寫「de-identified diplomat silhouettes」「without showing the spokesperson's face」，`portrait_subjects` 為空 → 最終 prompt 沒有任何肖像區塊，生圖端照預設畫剪影。**不是模型違規**（初判「消化模型不聽話」已更正）。⚠**帳本矛盾**：B73 列寫「②`PORTRAIT_NO_ENTRY_FALLBACK`（預設開）」，程式碼實際預設關——**B73 裁定的第二半在正式站從未生效**，所有維基查不到的人（網頁版與封面 `cover_excluded` 路徑）都會被排出版面。**附帶**：剪影旁沒有「示意圖」標籤（這人被排出名單，後端不會蓋；消化端沒照規則第 3 條自己加）。**可選修法**：甲｜譯名表加 `"貝卡伊": "Esmail Baghaei"`（英文維基有條目＋記者會照片，只治這一位，零風險）；乙｜真正打開 `PORTRAIT_NO_ENTRY_FALLBACK`＝做完 B73 那 20 道守門測試的逐題檢視（B45 風險：真名配 AI 捏的臉）；丙｜查不到條目時改查英文維基的譯名連結／全文搜尋，讓更多人回到第 2 層。 |
| B97 | 🟠**標籤重貼（F47 restamp）送的是「當下畫面設定」而不是原成品參數——切份量／角色／安全框後重貼，2K 不後製的圖會被重新套上 1748×924 對位框** | 2026-09-23 gpt-5.6-sol 獨立複查 B83–B95（`docs/回覆-20260923-Codex複查B83-B95.md` 發現 1），本 session 已核對 `app.js` restamp payload 屬實 | 🔶 **已修待實拍驗收（2026-09-23，VERSION 260923-01，commit 4280aa2；sol 實作、Claude 審查，2356 題綠）** | `restampDisclaimer()` 送 `density: state.density`、`safe_frame: state.safeFrame`、`safe_frame_profile: state.currentRole`、`aspect_ratio: currentAspectRatio()`、`image_size: state.imageSize`、`provider: effectiveImageProvider()`；`switchDigestDensity()` 等切換只改 state、不清 `refineSource`。後端再用這組值跑 `resolve_frame_plan`。修法方向：生成當下把成品參數跟著 `refineSource` 存起來，restamp 讀存下來的那組。 |
| B98 | 🟠**連按兩個角落按鈕，restamp 請求併發，較晚回來的舊請求會蓋掉新結果，toast 卻顯示新角落** | 2026-09-23 sol 複查發現 2 | 🔶 **已修待實拍驗收（2026-09-23，VERSION 260923-01，commit 4280aa2；sol 實作、Claude 審查，2356 題綠）** | 每次點按直接呼叫 `restampDisclaimer()`，沒有 request 序號或取消；回應一律 `state.refineDisplay = data`，訊息讀 `state.disclaimerCorner`。修法方向：請求序號，過期回應丟棄（或請求中停用按鈕）。 |
| B99 | 🟠**「川習」跨詞誤命中仍未擋乾淨：永川習俗／山川習性／冰川習性都被判成川普＋習近平** | 2026-09-23 sol 複查發現 3，本 session 以 `name_aliases.find_aliases()` 實測三例皆命中（四川習俗有擋住） | 🔶 **已修待實拍驗收（2026-09-23，VERSION 260923-01，commit 4280aa2；sol 實作、Claude 審查，2356 題綠）** | `BLOCKING_PREFIXES["川習"]` 只列前一字，屬逐字補洞。命中後 B91 會把展開名當 established fact 列進 `portrait_subjects`＝把川普與習近平畫進無關新聞。修法方向：前一字阻擋之外，加後一字阻擋（習俗／習性／習慣／習氣等「習」起頭的詞），兩字條目通則一起適用。 |
| B100 | 🟠**封面斷行：單一超長拉丁字串仍會被從中間硬切；`AT&T` 這類含符號的記號也可能切成 AT／&T** | 2026-09-23 sol 複查發現 4 | 🔶 **已修待實拍驗收（2026-09-23，VERSION 260923-01，commit 4280aa2；sol 實作、Claude 審查，2356 題綠）** | `compose.py` 找不到合法斷點時仍 `return text[:mid], text[mid:]`；`_ATOMIC_TOKEN_RE` 不含 `&`、`+`、`#`。測試只驗切點兩側是否都是英數字，所以切在 `&` 前不會紅。 |
| B101 | 🟡**403 等確定性權限錯誤仍重試滿 `DIGEST_ATTEMPTS` 白等；B94 測試把 403 釘成「繼續重試」** | 2026-09-23 sol 複查發現 5（改版前就有的行為，非 B95 造成） | 🔶 **已修待實拍驗收（2026-09-23，VERSION 260923-01，commit 4280aa2；sol 實作、Claude 審查，2356 題綠）** | `credits_exhausted_error()` 只攔 402；403 `PermissionDeniedError` 走通用 `APIError` 分支 sleep 後重試。`tests/test_b94_upstream_error_detail.py::test_other_statuses_keep_retrying` 把 403 列在會重試的清單。 |
| B102 | 🟠**追加修改（refine）送的是「當下畫面設定」、成品參數快照也在生成完成時才抓——生成中（約 30 秒）切份量／角色／安全框／引擎，refine 與 restamp 都會拿錯參數** | 2026-09-23 Claude 審查 B97 修正（commit 4280aa2）時發現的殘留，同屬 B97 一類 | 🔶 **已修待實拍驗收（2026-09-23，VERSION 260923-02；sol 實作、Claude 審查，2362 題綠）** | 兩處：①`resetRefineState()` 呼叫 `refineParametersFromState(display)` 讀的是**回應回來當下**的 state，不是送出請求那一刻的設定；②`app.js` refine 送出時用 `...refineParametersFromState(state.refineDisplay)` 重組，沒讀 `state.refineParameters`。修法方向：送出生成請求時就把參數快照下來、跟著回應帶進 `resetRefineState`；refine 一律讀存下來的那組（封面的 16:9／無框覆寫照舊）。 |
| B103 | 🟡**標籤重貼（restamp）`disclaimer_kind="source"` 但來源文字空白時回 HTTP 500，應為 4xx 輸入錯誤；`broadcast_hole` 的 400 在 try 之前拋出、不留失敗稽核** | 2026-09-23 sol 複查 B83–B95 發現 6（`docs/回覆-20260923-Codex複查B83-B95.md`） | 🔶 **已修待實拍驗收（2026-09-23，VERSION 260923-02；sol 實作、Claude 審查，2362 題綠）** | `disclaimer_source_text` 預設空字串、沒有跨欄位 validator，要到 `compose.paste_disclaimer_note()` 才拋錯並被包成 500。修法方向：請求模型加驗證（source 必須有非空文字）回 422／400；順帶讓事前拒絕也留失敗稽核。`safe_frame_profile`／`density`／`aspect_ratio` 任意字串問題可一併收斂成允許值。 |
| B104 | 🟡**B94 回前端的上游錯誤細節只遮 `sk-`／`pk-`，`Authorization: Bearer …`、Gemini `AIza…` 等會原樣回到瀏覽器** | 2026-09-23 sol 複查發現 7 | 🔶 **已修待實拍驗收（2026-09-23，VERSION 260923-02；sol 實作、Claude 審查，2362 題綠）** | `main.upstream_error_detail()` 自己寫的遮蔽只處理 `sk-`／`pk-`；稽核寫盤前的 `audit_archive.sanitize_error_summary()` 較完整。修法方向：回前端前共用同一支 sanitizer（或把兩者合一），補 Bearer／AIza 守門測試。 |
| B92 | 🔴**封面斷行切在拉丁字母串中間——「白宮封殺CNN遭提告」印成「白宮封殺C／NN遭提告」** | 2026-09-22 使用者回報正式站後台 18:15:07 那筆（操作者 王結玲，十點不一樣雙切，`8591ff5a3df4`）：「CNN斷句斷錯 變成 C / NN」 | 🟢**已修（2026-09-22，VERSION 260922-09）**｜成品實拍佐證，不是猜的。真因在 `compose._protected_inner_indices`：它只把**數字**記號標成「不准當斷點」（`_NUMBER_TOKEN_RE`，2026-09-09 為 9/12 建的），拉丁字母串沒人管。「白宮封殺CNN遭提告」10 字、`_split_line_near_middle` 的中點 `mid=5` 正好落在第二個 N，保底規則就從那裡切了。修法：把那條 regex 從「數字記號」放寬成**英數記號**（`_ATOMIC_TOKEN_RE = [0-9A-Za-z]+(?:[/.:\-][0-9A-Za-z]+)*`，函式改名 `_atomic_token_inner_indices`）。**一條規則同時管住三條路**——模型給的斷句 hint（`_hint_cuts` 的結果本來就被 `inner` 濾）、括號邊緣、保底中點切，所以 AI 斷句回得不好也一樣擋得住。⚠**這不算違反 2026-09-14「規則集不再加條目」的裁決**：那條針對的是 `_SPLIT_KEEP_TOGETHER` 那份會愈長愈胖的專有名詞詞典；CNN／AI／GDP／F-16／COVID-19 是**結構性的不可分記號**，跟數字同類，是封閉規則不是條目。實測：`白宮封殺｜CNN遭提告`、`爭奪AI｜霸權主導權`、`美股F-16｜軍售案過關`，9/12 那條舊案照舊不切。⚠**順帶發現但未動工**：`len()` 把 CNN 算成 3 個字，會吃掉 `COVER_TITLE_FILL_MIN_CHARS`（7）的額度——沒人回報，不在本次範圍。⚠未實機驗證。 |
| B93 | 🔴**追加修改（AI改圖）會把原本畫得好好的具名真人臉改成背影** | 2026-09-22 使用者回報正式站後台 18:23:54 那筆（操作者 王結玲）：「為何川習畫成黑臉」 | 🟡**已定位真因，未修**｜**原生成沒有錯**：18:19:48（`e81c535c1523`）兩格都是清楚的川普與習近平正臉，B91 那條病灶在這筆上沒有復發。背影是**兩次追加修改**做出來的——①18:21:27「美中爭AI霸權, 改用兩國的國旗」②18:23:51「美AI霸權, 不要習近平和川普的頭」。web-refine 的 prompt 裡有一段 `NAMED REAL PEOPLE: no reference photograph is attached to this edit, so you MUST NOT draw or complete the face of any named real person that is not already a face in the attached image.`——這條路**不接肖像鏈**（`_cover_apply_portraits` 不在它上面），原圖當初查到的參考照沒有帶進改圖呼叫。使用者只說右格，模型卻把**兩格**的臉一起改成背影，而且國旗也沒畫出來。要修的有兩件，先別動手：①改圖要不要把原生成的參考照帶進來（帶了才有資格重畫臉）②左右格要不要能分別下指令（現在改圖指令是整張的）。⚠**使用者第二句話本身就是「不要頭」**，所以「變成背影」有一半是照辦；真正的缺陷是**溢出到左格**與**改圖沒有參考照可用**。 |
| B91 | 🔴**川習仍被畫成背影——對照表有生效，是 system prompt 不承認它** | 2026-09-22 使用者回報 DEV 後台 17:29:22 那筆（操作者 許岱軒）：「川習還是畫背影」 | 🟢**已修（2026-09-22，VERSION 260922-08）**｜**真因與 B88 想的不一樣，有紀錄逐項佐證**（`bce3a1f6206c`，記者／simplified／消化模型 gemini-3.8-flash）：①**對照表有生效**——消化輸出的內文小標寫「美尋求製造回流　**川普與習近平**會談成未知數」，而「習近平」三個字新聞原文一次都沒出現（原文只有「納入**川習**之間」），模型不可能逐字抄，只能是從表裡拿的。②**但 `portrait_subjects` 交白卷**，STRUCTURE 寫了 `The named figures appear strictly via typography and data callouts without rendered photographic portraits.`——對 10510 字的最終 prompt 做關鍵字統計：`NAMED REAL PERSON` 只出現 1 次（在通則裡）、`REFERENCE PHOTOGRAPH` **0 次**。③**通則接手**：`If no such block is present, do NOT draw a recognisable face … use a back view or a plain silhouette` → 生圖模型照辦，畫出川普與習近平的背影。**所以背影不是查不到照片**——實測維基查圖，「川普」「習近平」只用中文就 `entry_found=True`、照片 YES，名字只要進得了陣列臉就畫得出來。病灶是**素材與 system prompt 打架**：對照表放在**使用者素材**裡說「當作已寫明」，`REAL_WORLD_FIDELITY_RULES` 第 5 條卻絕對禁止「never infer a person from a job title, **an event**, a country, an organisation」——「川習會」正是 event。模型的折衷就是：名字敢寫進文字，不敢列進 `portrait_subjects`；而那句 `without rendered photographic portraits` 本身就違反第 5 條「structure 不准寫 rendering treatment」。修法：第 5 條加一條**範圍寫死的例外**——只有標題為 `Abbreviation glossary supplied by the newsroom` 的區塊裡列出的人名視同素材逐字寫明，可以設計進版面且**必須**列進陣列；並明文擋掉那個折衷（不准只寫進文字不列陣列、不准在 structure 寫「without portraits」「via typography only」規避）。表以外的人 VERBATIM 護欄一個字都沒鬆。`editor_formats.py` 兩份封面推導同步補同一句。⚠**這次動到凍結的 prompt**：八份 digest 快照重凍（每份只差一行，`git diff --numstat` 全是 `1 1`），使用者當面放行；兩份 image prompt 快照沒變。守門測試釘的是**兩邊對得上**（`name_aliases` 的區塊標題字串＝prompt 裡引用的字串），因為標題一旦一邊改掉，例外會靜默失效而症狀又長得像生圖模型的問題。⚠未實機驗證。 |
| F44 | **編輯格式「預設（現行）」改名為「編輯CG」** | 2026-09-20 使用者追加 | 🔶 **已上線待實拍驗收（commit 1c93504「0920 第一波」，已在公司 main；2026-09-23 帳本補登）**｜⬜ **2026-09-20 使用者列入下一批動刀** | 純命名，成本低。要一起改的三處：`editor_formats.py:2238` 的 `"label"`、`app.js:456` 的 `label`、`docs/user-guide-page1.html`（185／217／233／382／396 五處）。⚠**只改顯示字串，不要動 format key `default`**——key 牽動版型判定、`hides` 表與既有後台紀錄的比對。 |
| F45 | **缺「純色背景／去背友善」選項——電視台要進虛擬棚，模型卻硬畫複雜背景** | 2026-09-20 正式站後台實查（9/18–9/20，91 筆）歸納出來的第一大痛點 | ⏸ **2026-09-20 已裁做法，但使用者裁定不進今晚批次**（排下一批） | ⚖**2026-09-20 使用者已裁：開放綠底，D2 例外處理。** 也就是使用者主動選「綠底去背」模式時，**整張背景允許螢光綠／chroma-key green**，D2「禁螢光綠」在這個模式下不適用於背景。⚠**但禁令對畫面主體反而要更嚴**：背景既然要被摳掉，主體裡任何一點螢光綠都會跟著破洞——使用者原話「斑馬線跟所有車輛人物都不要有綠色元素」講的正是這件事。所以這個模式的規則是**背景綠／主體零綠**，不是整張放寬。📌**連帶要處理**：D2（本表 369 行）現行裁決寫「文字／裝飾禁螢光綠」，理由是「會與綠屏打到穿幫」；本例外不推翻它，而是補上「使用者明示要綠底去背時，背景是刻意的 key 色」這個前提，動工時兩處措辭要對齊。⏸**2026-09-20 使用者裁定本條不進今晚批次**，今晚只做原定範圍。 | 🔍**實證**：三天 91 筆裡有 **36 筆是追加修改（佔 39.6%）**，而追加修改的指令原文裡**最大宗就是在要純色底／去背**。最極端的案例是記者葉怡君 09-19 15:09～15:26 為同一則「未禮讓行人罰鍰」連打 **9 次**，其中 12 句修改指令全在處理背景：「我想要純綠色的底」→「斑馬線跟所有車輛人物都不要有綠色元素」→「幫我把背景改成灰色」→「算了，你給我馬路的背景好了，比較好看」。⚠**注意這裡有個內建矛盾**：使用者想要綠底去背，但 D2 鐵則禁螢光綠／chroma-key green（見 [[feedback_aicg_no_green_text]] 的脈絡）——**所以不能直接開放「綠底」**，要嘛給可控的去背友善底色（純灰／純藍幕），要嘛直接輸出去背 PNG。📌**這條的價值在於它一次消掉四成流量裡的一大塊**：每一次追加修改都是一次付費生圖，而且重繪還會衍生新錯字（見 F46）。做法要先裁：①生成時就給「背景」選項（純色／虛擬棚友善／一般場景）；②或生成後提供程式端去背。**建議先問使用者要哪一種，不要自己選。** |
| F46 | **只想換幾個字也得整張重繪，重繪又衍生新錯字** | 2026-09-20 正式站後台實查歸納 | ⏸ **2026-09-20 新列管；使用者裁定不進今晚批次**（排下一批，動工前要先盤點哪些版型的字是程式壓的） | 🔍**實證**：大量重試的內容其實只是換球隊、換日期、改錯字，卻都得走生圖模型整張重畫。代表案例：①編輯曹雪卿 09-18 20:17～20:27 為「亞運棒球」連打 **5 次**，全在把對手從台韓→泰國→中國→菲律賓→日本換過去（同一個公版模板）；②編輯王結玲要把「10月24日 HHK 改成 NHK」——**模型自己把 NHK 畫成 HHK**，為了修這兩個字母又得重繪一次，而重繪本身就是新錯字的來源。📌**這條和 F45 是同一個病灶的兩面**：追加修改佔 39.6% 的流量，其中很大比例根本不需要動到生圖模型。**方向**：對「純文字替換」提供不經生圖模型的輕量路徑（程式端壓字的版型本來就做得到——YT 直播直標耗時只有 81～88ms 就是證明），或至少讓使用者指定「只改這幾個字、其餘畫面一律不動」。⚠**要先確認哪些版型的文字是程式壓的、哪些是模型畫進去的**，只有前者能走輕量路徑，不要承諾做不到的事。 |
| F40 | **肖像照片來源覆蓋率：維基查不到的人太多，而「查不到」現在只能退回背影／不畫臉** | 2026-09-16 使用者提出（「碰到好幾次查維基都是查錯或沒找到人」），由 B61 事故展開 | 🔶 **2026-09-16 四層分流已實作（exp `e0f4df6`，Stage 5a `7d4748b`）**：新增 `entry_only` 模式（第 3 層）與 `notices` 回應欄位，`PORTRAIT_NO_REFERENCE_RULES` 改寫成無人場景（第 4 層，措辭本身不再出現 back view／silhouette）。🔴**但只接到最終生圖決策層，沒接進消化端——見 B69，主流程實際上還走不到第 3 層**｜⚖ **2026-09-16 使用者已裁：維基維持墊底**——**不接 AP／路透圖庫、不建本地人物照片庫**（兩案暫不動工，日後覆蓋率真的擋到再議）。🔁**2026-09-16 二次裁決（取代同日稍早那次，以本段為準）：改採四層分流。**｜**第 1 層**使用者自己上傳照片 → 直接用｜**第 2 層**維基查得到照片 → 用照片（現況）｜**第 3 層**查不到照片、**但查得到這個人的維基條目** → **允許生圖模型自畫**，**⚠使用者明確裁定：不要在圖上標「長相為 AI 推測」**，改成**在訊息欄通知使用者**（訊息欄＝目前報錯用的那個紅色框）｜**第 4 層**連條目都查不到 → **畫無人場景，不要畫背影**。🔑**第 3 層的閘門刻意用「維基有沒有這個人的條目」當代理指標**，不是問模型知不知道（模型不會誠實回報自己不認識）：連條目都沒有的人，模型幾乎確定也不認識。📌**同日稍早的裁決是「查不到照片就一律無人場景」，已被本段取代**——保留那句在下面備查。（舊）查不到照片就改畫無人場景，不要畫背影。使用者原話「畫出背影就是錯的」——**不是擋下不生圖，而是從根本不安排人物**：畫大樓、標誌、場景、器物，而不是塞一個背影或剪影在那邊。⚠**這要改 `PORTRAIT_NO_REFERENCE_RULES`**（現行條文寫的正是「背view 或剪影」），並連動消化／derive 端的畫面描述——**描述階段就不該寫成人像鏡頭**，否則生圖端只能硬塞一個人進去。**這條與 B53（補畫面描述只餵標題）同一段程式，建議同批做。** 🧪**2026-09-16 對照實測（使用者指定，1 次生圖，子代理執行）：模型在完全沒有參考照的情況下，光靠上下文畫出來的華許「明顯像」。**條件：打 `/api/images/generate`（不經肖像查照機制），prompt 明寫 `Kevin Warsh, the Chairman of the U.S. Federal Reserve`，log 確認**沒有附任何參考圖**，模型 `gpt-image-2.5-sunburst`，HTTP 200／17 秒。成品與維基官方照並排：`20260916_測試4_對照.jpg`（左官方照／右生成）。**深黑側分短髮、深眉、長臉、鼻型、膚色都對得上**；差異在髮量更蓬、臉頰與下顎更寬、年齡感偏大、沒有官方照的灰白髮。🔑**這修正了一個先前講得太寬的說法**：不是「模型畫不出新任主席」——**名字明確寫出來、而且這個人在訓練資料裡時，它畫得出來**（華許 2006–2011 任聯準會理事，公開照片多）。**0916 事故真正錯的那一步是「從職稱自行推斷主席是誰」**（標題只寫「美國聯準會」→ 模型用過期知識補了鮑爾），不是「不會畫臉」。⚠**但不足以改變本條裁決，三個理由**：①**n=1**，分不出「模型真的知道」與「剛好抽對」；②訓練資料稀薄的人（台灣本地人物、小國官員）它幾乎一定會編，**而編出來的臉看起來一樣自信**；③**失敗不可偵測**這件事沒有變。📌**衍生的待裁題（尚未開列，等使用者決定要不要列管）**：「人名明確寫在標題裡時，要不要允許模型自畫、配示意圖標籤」——那等於在維基墊底之外多一條退路，但也等於承擔②的風險。 🟢**2026-09-16 補完（exp `283b7fa`，Stage 5b）：第 3 層已接進網頁／LINE 消化端**（見 B69），四層在主流程全部生效；notice 走現有紅色訊息框（nonfatal，成功後不被清掉、下一次新請求才清除），**沒有畫進圖裡**，符合使用者裁定。F42 訊息視窗依使用者指示本次未做。 | **使用者的直覺是「不如讓生圖模型自己從上下文判斷人是誰」，監督明確反對**：2026-09-16 那四張鮑爾**正是**模型自己判斷的結果——沒有人名、沒有照片、沒有規則時，它就照自己知識裡的「聯準會主席」畫，而且畫得很有自信。🔑**兩種失敗的形狀不對稱**：查不到照片＝系統知道自己不知道，**可偵測、可擋**；模型自判＝它不知道自己不知道，產出**看起來完全正常、掛著真名的假臉**，沒有任何程式檢查抓得到。而且模型知識有截止日，**新上任／新竄起的人**（正好是新聞最常要的）一律畫成前任。👉**所以要換的是照片來源，不是換成讓模型畫。**💡**三條路（待裁）**：甲｜**接 AP／路透圖庫**——新聞部每天在用、有帳號、覆蓋率與時效性遠勝維基、授權清楚，S6／S8 已有搜尋下載基礎；乙｜**本地人物照片庫**——常用官員／名人存一份，命中就用，維護成本低、新面孔要人工補；丙｜**維基維持墊底**，查得到就用。⚖**另一半同等重要（使用者原話：「畫出背影就是錯的」）**：查不到照片時，現況**還是花錢生了一張沒人能用的背影圖**。應改成**生成前就擋下**並提示「這則有指名人物，請上傳一張照片」，一次生圖錢都不花。📌**2026-09-16 已實測**：`find_reference_photo('華許')` 查得到官方肖像（zh.wikipedia／凱文·沃什），B64 修好後成品畫出正確本人——**所以維基機制本身是有效的，今天失效是被 B64／B65 擋掉**；覆蓋率問題仍然獨立存在。⚠**待補證據**：使用者說的「查錯」要區分是**查不到**還是**抓到別人的照片**；後者較嚴重（2026-08-18 已加 Wikidata `P31=Q5` 身分閘門），**若還會抓錯人請留實例**。 |
| F41 | **釘身分推斷的鐵則：人名只准「抽取」、不准「推斷」** | 2026-09-16 使用者裁定（由 B61 事故與測試 4 的結論分岔出來） | 🔶 **2026-09-16 生圖端已實作（exp `e0f4df6`，Stage 5a `7d4748b`）**：只寫職稱不寫人名的稿 `portrait_subjects` 為空，且最終 prompt 帶著禁畫具名真人臉（由 B61 的出口層執行）｜⚖ **已裁：釘死「只抽取、不推斷」**｜**下一批做，屬於真人臉那一刀（③）** | 🔑**把兩件事分開才看得懂 0916 事故**：**第一層＝這則新聞的主角是誰（身分，文字問題）**、**第二層＝那個人長什麼樣（臉，影像問題）**。**事故錯在第一層**——標題只寫「美國聯準會」，模型自己推斷出「主席＝鮑爾」並畫上去，用的是過期知識。**第二層其實沒壞**：2026-09-16 測試 4 證明名字明確寫出、人物又在訓練資料裡時，模型畫得出來（見 F40 的實測段）。✅**裁定的鐵則**：人名只能來自**使用者寫的標題或新聞原文**；**消化／derive 端可以「抽取」文中已有的人名，不准「補」文中沒有的人名**；**生圖端不准自己在畫面裡放上具名真人**（這一半由 B61 的出口強制層執行）。**兩端都要釘，而且要有測試**：餵一篇只寫職稱不寫人名的稿，斷言 `portrait_subjects` 為空且最終 prompt 帶著「不得畫具名真人臉」。 ✅**2026-09-17 R7 免費前置批驗收通過**：拿一篇通篇只有職稱、沒有任何人名的稿（央行總裁／財政部長／不具名金融業主管）連跑 2 次，**`portrait_subjects` 兩次都是空陣列**，模型沒有把職稱推斷成人名。這一輪同樣**沒有生圖**——`portrait_subjects` 是消化端的欄位，讀回應就驗得完。⚠**沒驗到的部分**：生圖端「不准畫具名真人臉」那一半要看圖，留在 R7 的 P 組。|
| F42 | **訊息欄改成訊息視窗：記錄整個過程的所有訊息，不要只閃一次** | 2026-09-16 使用者裁 F40 時追加 | 🔶 **已上線待實拍驗收（commit 1c93504「0920 第一波」，已在公司 main；2026-09-23 帳本補登）**｜⬜ **2026-09-20 使用者列入下一批動刀**（解除「本次不修」）｜0920 補充需求：「錯誤訊息停留太短，應該要建立訊息歷史紀錄欄位，紀錄錯誤訊息，**讓使用者知道現在進度**」——即訊息視窗不只收錯誤，還要當**進度顯示** | 使用者原話：「訊息欄就是報錯的紅色框，以後也要改成訊息視窗，記錄過程中的所有訊息，不要只出現一次。訊息視窗先列TODO，本次不修，只修四層分流」。**現況**：提示只有一個一閃而過的紅色框（B38 記過同樣的痛點——消化逾時只看得到一個 toast 寫「HTTP 524」）。**要做的是一個持續累積的訊息區**，把生成過程中的每一則訊息（警告、退路、逾時、第 3 層「這個人沒有參考照」之類）都留在畫面上可回看。📌**F40 第 3 層的通知就是掛在這個機制上**——在訊息視窗做好之前，先掛現有的紅色框。與 **B38**（錯誤訊息只有一閃而過的 toast）、**B42**（改圖線的錯誤橫幅）同一叢，建議一起做。 🔁**2026-09-16 使用者再次提出（本人問「這個我提過了嗎？」——有，就是這一列）**：「使用者希望現有的錯誤訊息欄位變成訊息視窗 **保留訊息歷程**」。 🔗**這次是連著 D21 一起提的，兩者確實相關**：D21 的消化截斷會讓使用者等很久（實測字超多平均 172 秒、最壞 201 秒），而現行訊息欄**只閃一次就沒了**，使用者在等的過程中看不到「系統到底在做什麼、重試了幾次、為什麼慢」。👉**訊息歷程不只是 UI 便利，它是長耗時流程的可觀測性**，在 D21 沒治本之前，它能直接降低使用者的焦慮成本。**建議與 D21 同批考慮優先序。**|
| F43 | **新增「畫面來源」欄位（Pillow 程式端壓字），與「示意圖」標籤互斥** | 2026-09-16 使用者裁 B70 時追加 | ✅ **2026-09-21 前端做了，同時發現「後端已接」是錯的、一起補**——0921 驗收原話「後端已接，但前端沒做」。🔬**實際查證後端根本沒接上**：`apply_portrait_to_image_request` 是網頁版唯一決定 `disclaimer_kind` 的地方，但它①呼叫 `resolve_image_disclaimer(mode)` 時**沒把 `disclaimer_source_text` 傳進去**、②沒有肖像題時直接 `return req`。所以「畫面沒有人臉＋使用者填了來源名」這個 F43 **最主要的情境**永遠判不出 `kind="source"`，前端補再多控制項都沒用。⚙**修法**：把 `req.disclaimer_source_text` 一起交給 resolve；沒有肖像題時改呼叫新的 `_fill_in_disclaimer_kind()`。🐛**寫的時候踩到一個回歸、被既有測試當場抓出來**：第一版讓早退路徑無條件重算 kind，會把 LINE 路徑（`generate_news_image` 自己先算好 kind 再組 request、且不傳 `portrait_subjects`，走的正是那條路）算好的 `"ai"` 洗成空字串，標籤整個消失。改成**只補不覆蓋**，並補一題釘住。互斥優先序不變：有 AI 生成／推測的人臉一律「示意圖」，來源名忽略（但現在會通知使用者，見 B70）。前端與 B70 共用同一組控制項，細節見 B70 那列。🧪測試：無肖像＋來源名→source、entry_only＋來源名→ai、no_reference＋來源名→source、沒改到東西回原物件、呼叫端已設好的 kind 不准被覆蓋。📌**實證**：commit `5c456aa`＋`63d56c2`（exp/redesign-20260903）→ 公司 PR [#34](https://github.com/TVBS-AI/aicg-champion-daihsuan/pull/34) 已合併進 main（merge commit `d474093`，2026-09-21T10:00:21Z）；合併前後各跑一次全套，皆 **2221 題全綠**。VERSION `260921-04`。| **使用者原話**：「還需要開新欄位 畫面來源也是 PILLOW，但跟示意圖互斥，原圖沒改圖才需要畫面來源。這點需要考量更全面，都列管之後處理。」 🎯**規則**：圖是 AI 生的／被 AI 改過 → 標「AI示意圖」；圖是使用者的原圖且**沒有被改** → 標「畫面來源：○○○」。**兩者互斥，不會同時出現。** ✅**已有現成實作可直接抄（監督 2026-09-16 查證）**：**直標版型（vstrip）已經做過這件事**——`compose.VSTRIP_SOURCE_PREFIX = "畫面來源："`（compose.py:2542）、`vstrip_source_text()` 正規化（compose.py:2639）、實際壓字在 `compose.py:2921`，而且已經有 **2026-09-09 使用者要的體貼設計**：使用者只填來源名（例「美聯社」），「畫面來源：」四個字由程式自動補，已經以「畫面來源」開頭的就不重複補。後端欄位是 `source_text`（`main.py:7449`，上限 40 字），**消化端甚至已經會自動判來源回填**（`main.py:6143` 的 `data.get("source")`，前端訊息見 `app.js:2680`「判不出畫面來源，請自己填」）。👉**這條的工作量主要在「把直標版的做法推廣到其他版型」＋「和示意圖標籤的互斥判定」，不是新造輪子。** ⚠**最大的設計難點（監督補）**：**互斥的判準要接到「程式有沒有保證原圖未被動過」**。目前唯一有這個保證的是 `title_mode=composite` 的單張 asis 路徑（`crop_background_16x9` 純裁切＋PIL 壓字，照片一個像素都沒動）；**B55 證明「原圖放置＋AI 標題」這條路的照片會被重畫 68～93%**，那種情況**不可以標「畫面來源」**——會變成對觀眾宣稱未經修改，但實際上整張都是模型重畫的。所以 **F43 的互斥判定直接相依於 B55 修法甲（模型只產標題圖層）有沒有做**，B55 沒解決前，AI 標題分支只能一律標「AI示意圖」。 🔗**關聯**：**B70**（示意圖標籤升級 Pillow＋自訂位置，同一次設計）、**B55**（像素保證的前提）、**B30**（示意圖浮水印沒有程式疊）、**B28**（標籤被附圖規則洗掉）。 🔁2026-09-17：同批追加「全站消化流程禁止 AI 自繪示意圖／來源文字」的前置要求，見 B70 同日補述。  ✅**2026-09-20 已接**：十點不一樣 `mode="composite"`（滿版＋雙切＋雙切「只改文字」recompose）與 YT 封面 `title_mode="composite"` 四個 layout（news／hourly／hot／live24）——這些路徑的原圖放置附圖直接進 compose 函式，完全不經過任何生圖模型，`is_ai=False` 是程式保證。互斥判定：AI 標籤永遠贏（`is_ai=True` 時 `source_text` 被忽略）。⬜**維持不接**：`mode="ai"`／`title_mode="ai"`（依賴 B55 的像素保護機制，provider="gemini" 是機率性補救、provider="gpt" 的透明底標題圖層架構上可行但閘門仍在修，兩者都不到「程式保證」門檻）；一般新聞圖／播出鏡面（沒有「原圖直接上版」這個概念，不適用）。完整版型清單、判準與逐項理由見 `docs/f43-source-label-inventory.md`（含 provider 分岔說明），該文件與程式碼同步更新，不落後於實作。 | 🔎**2026-09-20 獨立複查第二輪（gpt-5.6-sol）兩項，已修**：**(一)｜可以把 AI 圖正面標成「畫面來源」**——`YtCoverRequest.background_is_ai` 是前端把上一輪回應原樣帶回來的值，後端沒有從位元組重算。以前帶錯只會讓「AI示意圖」**漏標**（消極遺漏）；F43 之後同一個值還決定要不要貼「畫面來源：○○○」——那是對觀眾的**正面宣稱**（這張畫面沒被動過），帶錯就是說謊。sol 逐步重現：先用 `title_mode="ai"` 生一張，把成品當 `background_image_base64` 送回來，**省略** `background_is_ai`（Pydantic 預設 `False`）並填 `source_text` ⇒ AI 圖上貼出「畫面來源：路透社」。最容易踩到的不是惡意，是前端漏帶欄位。**修法**：自己帶底圖進來又要標來源時，`background_is_ai` 必須**明確**出現在請求裡（`model_fields_set`），不接受預設值，否則 400；明確送 `False` 仍照信。⚠**殘留待裁**：後端要不要改成從位元組自行判定（或改用簽章／token）來取代這個信任模型——那是比本批大的改動，今晚不動，見 `docs/f43-source-label-inventory.md`。**(二)｜長來源名會蓋掉固定元素**——`_draw_ai_note` 原本是為「AI示意圖」五個字寫死字級與右靠位置的，沒有任何寬度上限；`source_text` 允許 40 字。實測「畫面來源：」＋40 個全形字的底板是 `(259,216)-(1858,268)`，橫跨畫布 **83%**，直接蓋掉左上角的 LIVE 章與日期板 `(50,54)-(486,349)`（預設「AI示意圖」只有 139px，「畫面來源：美聯社」280px）。`_draw_live24_ai_note` 是鏡像問題（靠左、撞右上 Logo）。**修法**：抽出 `_fit_note_font()` 共用護欄——先縮字（使用者拿得到成品），縮到下限 `YT_NOTE_MIN_SIZE_RATIO` 仍塞不下才丟 ComposeError（使用者自己改得掉）；底板左緣硬界線 `YT_NOTE_MIN_LEFT_RATIO=0.40`。「AI示意圖」五個字字級零變化。
| R6 | **長稿消化失敗率實測（B39／B48／D21 的最後一哩驗收）** | 2026-09-16 使用者當面指定列管（「長稿消化測試 寫入TODO列管」） | ⏸ **等前置**｜**兩個前置都沒到**：①~~D21 的放寬修法還沒實作~~ → **已滿足**：D21 於 2026-09-16 `d8cb1ee` 實作完成（`DIGEST_MAX_TOKENS`=12000、`MAP_DIGEST_MAX_TOKENS`=16000），**剩下的前置只有 staging 權限**；②staging 對外存取 2026-09-16 已依使用者指示關閉（403）｜🗳**開跑要使用者授權，這是付費測試** | 拿 B48 那篇 **2,930 字真實稿**量失敗率與耗時分布。**至少 5 次**（B60 已裁：份量類結論一律附 5 次分布，不接受單次示範——0915 已被單樣本騙過兩次）。**對照組必須含「字多」standard**：B39 修好後「字超多」過了，**同一篇改「字多」仍然失敗**，失敗方式從 524 變成 503，**失敗率本身沒降**；字多工作量更小卻更容易掛，這個反直覺是線索、不要跳過。每次都要記：總耗時／`finish_reason`／`completion_tokens`／`reasoning_tokens`／`ratio`／OpenRouter 實際落到哪個 provider／有沒有 `[generate] attempt N/5`／有沒有 `[portrait]`。⚠**前置一：D21**（已裁甲案放寬 `DIGEST_MAX_TOKENS`，尚未實作）。B48 根因已確認＝**思考把整個 completion 預算吃光**（8 次呼叫 5 次 `finish=length`／`ratio=1.00`；`reasoning.max_tokens=2000` 送出去被接受但模型沒遵守，實測 `reasoning_tokens` 跑到 5442～6000）。**在放寬之前測只會重現已知失敗，沒有新資訊。**⚠**前置二：staging 權限**——`allUsers` 的 `roles/run.invoker` 已移除，公開網址回 403；**服務本體與 revision `00075-jgs` 完好未刪**。要測先跑 `gcloud run services add-iam-policy-binding tvbs-aicg-linebot --region=asia-east1 --member=allUsers --role=roles/run.invoker`（IAM 生效約 30 秒延遲），測完不用就再 `remove-iam-policy-binding` 關回去。💰**呼叫次數寫死上限**：建議每檔位 5 次、兩檔位合計 **10 次**，跑完就停、不准加碼（`feedback_api_call_budget_hard_cap`）。25% 的間歇失敗率等於平均燒 4 次才看得到 1 次失敗——**不要用亂打的方式找失敗**，先補 F30／F31 讓真實失敗自己留痕，比花錢重現便宜也準確。✅**驗收標準**：5 次分布全部成功且耗時沒逼近死線＝修好；**只要還有一次失敗就不算修好**，帶那次的 `finish_reason` 與 token 數回來繼續查。📌來源章節：`TODO.md`「待驗收：長稿消化失敗率實測（R6，2026-09-16 使用者指定列管）」 💵**2026-09-16 費用估算（依當日向 OpenRouter API 實查的官方價目）**：`anthropic/claude-sonnet-5` 輸入 $2/M、輸出 $10/M。實量一次編輯版消化的輸入＝規則 5,686 token（standard）／6,293（maximum）＋2,930 字稿 ≈ **8.6K token 輸入 ≈ $0.017**；輸出照 0915 實測成功時 2,815–3,275 token ≈ $0.032，吃滿 12000 則 $0.12。**單次成功 ≈ $0.05，吃滿重試那次 ≈ $0.14**。10 次跑完：全成功 ≈ **$0.5**，照 25% 失敗率推估 ≈ **$0.9**。🔑**本機 OpenRouter key 2026-09-16 實查：上限 $40、已用 $27.56、剩 $12.44**（帳號本身還有約 $187 額度，但這把 key 被 $40 卡住）。本件約吃掉剩餘的 7%。 🔬**2026-09-16 已實測（使用者授權付費實拍，10 次跑滿寫死上限）**——⚠**改在本機 exp 樹跑，沒有開 staging 權限**：`git branch --contains d8cb1ee` 證實 D21 只在 exp／stage7／stage8 上，**staging 那顆 revision `00075-jgs` 比它早**，開權限上去測到的是舊的 6000 預算，對 D21 零資訊量又白曝險一次。代價是量不到 Cloud Run 閘道的 524／503，但那是「消化把預算吃光」的下游症狀，本列要求的五個欄位本機全拿得到。**一次呼叫＝一個樣本**：繞開 `generate()` 的 `DIGEST_ATTEMPTS=5` 重試圈、自己組 prompt 後直接叫 `digest_completion`——走端點的話失敗會被重試吃掉，既多花錢又**把要數的失敗蓋掉**。📊**結果：10 次成功 5 次（字多 3/5、字超多 2/5）**。失敗的 5 次：截斷 3 次（`finish=length`、`ratio=1.00`、reasoning 10855／11998／11999）、簡體字 1 次（見 **B71**）、塊數不足 1 次（observed=0 required=7）。成功的 5 次 ratio 落在 0.45–0.85，reasoning 4005–8764。⏱**耗時**：字多 64–126 秒（平均 91）、字超多 112–201 秒（平均 172）。**字超多最壞 201 秒，一次失敗＋一次重試就爆 Cloud Run 的 300 秒**——0915 正式站的 524／503 由此而來。❌**照本列自己訂的驗收標準（5 次分布全部成功才算修好），D21 沒過**，詳見 **D21**。📁明細 `D:\Downloads\AICG\後台紀錄\20260916\R6_長稿消化_10次.jsonl` 與 `R6_長稿消化_分布表.md`。⚠**本機量到的數字能不能套到正式站，取決於正式站的消化模型是不是同一支——而那查不到，見 B72**。 |
| F32 | **追加修改的輸入欄太小，一次改多處時看不清楚** | 曹雪卿 0915 回饋文件（AI CG回饋，hikaru.tsao@gmail.com） | 🔶 **已上線待實拍驗收（commit 1c93504「0920 第一波」，已在公司 main；2026-09-23 帳本補登）**｜⬜ **2026-09-20 使用者改列下一批動刀（🌊 第二波解除）**＋**追加需求：輸入框要加大視窗，並讓 ENTER 換行（現在 ENTER 是送出）** | 使用者原話「追加修改的欄位較小，若一次要修改較多地方，容易看不清楚」。純 UI，改 textarea 高度／可拖曳即可，成本低。**建議與 B42（改圖線的錯誤橫幅）同一次動工**，都在改圖那條線的前台。 |
| F33 | **消化把「創9月2日以來新高」簡化成「逾兩周新高」，講得不對** | 曹雪卿 0915 回饋文件（AI CG回饋，hikaru.tsao@gmail.com） | ⬜ 🌊**第二波**（2026-09-15 使用者裁定：等第一波收工才動工） | 使用者原話「訊息精確度／中上第三行原提供資訊為 創9月2日以來新高，但直接簡化為"逾"兩周新高不正確」。9/2 到 9/15 是 13 天，**不滿兩周**，「逾兩周」把方向講反了。這不是字數壓縮的副作用，是**消化端在改寫數字期間時自行換算而且算錯**。要看的是 prompt 有沒有授權它把明確日期改寫成相對期間——監督傾向**不該授權**，日期原樣保留最安全，但這要裁（見 D19）。 |
| F34 | **標題把觀眾不認識的人名放進去，而不是用職稱** | 曹雪卿 0915 回饋文件（AI CG回饋，hikaru.tsao@gmail.com） | ⬜ **2026-09-20 使用者改列下一批動刀（🌊 第二波解除）**｜使用者 0920 重申並給了規則寫法：「自動消化寫標題，如果不是知名人物，就不用寫人名，改用職稱」 | 使用者原話「標題有修正空間(陳一新入標觀眾較不知他是誰)」。同一張圖的第 5 格寫的是「中國國家安全部長陳一新示警」（有職稱，看得懂），大標題卻只寫「陳一新警告人工智慧威脅政權」。規則方向：**人名在臺灣觀眾的識別度不足時，標題應改用職稱或機構**。與 B45 是同一張圖的兩個層面（B45 是臉，F34 是名字）。 |
| F35 | **播出鏡面示意圖的三個小要求** | 曹雪卿 0915 回饋文件（AI CG回饋，hikaru.tsao@gmail.com） | 🔶 **③ 已上線（commit 31a68e8：底帶要吃重點）；①示意圖標在挖框處、②示意手機機型 仍未做**（2026-09-23 帳本補登）｜⬜ **2026-09-20 使用者改列下一批動刀（🌊 第二波解除）**｜**③ 升級為明確需求**：「編輯版播出鏡面最底下的跨版行句，通常都是沒有意義、資訊不夠紮實的內容，**應該也要放重點**」——即 `<底帶>` 那行要吃重點，不是塞背景說明（條文在 `editor_formats.py:112`，現況只要求「an ordinary fact from the material」） | 使用者在挖框那兩條裡夾帶的三件事：①「播出鏡面的示意圖是否應標示在例如他挖圓框的地方?」——示意圖該不該標在挖框處；②「示意手機非iPhone 18」——示意圖畫的機型與新聞內容不符；③「最下面那行容易不是重點(eg字少版，統計時間多應為背景說明)」——最後一行常被塞進背景說明而非重點。⚖**2026-09-15 使用者裁 B47 後，①的份量升高**：既然蓋框位置沒辦法從成品反推、預設就是 OFF，那「示意圖標示哪裡能蓋」就是**讓使用者自己判斷**的替代方案，是這三項裡最該做的一項（見 B47）。②③是消化／版面規則。**拆開處理，不要當成一件事**。 |
| F36 | **YT 整點直播：日期方塊與標題的距離可以拉近** | 曹雪卿 0915 回饋文件（AI CG回饋，hikaru.tsao@gmail.com） | ✅ **2026-09-21 已修並定案**——改成**日期牌下緣固定離第一行標題墨水上緣 40px，不分字數**（`compose.YT_HOURLY_DATE_TAB_GAP_RATIO = 40/1080`）。定案過程：先問「≥6 字觸發要用多少」→ 使用者答 **40px**；再補問「<6 字呢」，產出 104／80／60／40 四張短標題樣張（`D:\Downloads\AICG\後台紀錄\20260921\F36_樣張_短標題\`）給使用者挑 → **同樣選 40px**。兩邊同一個數字，所以**「字數門檻」整個不必存在**，規則收斂成一句話。使用者當場確認：「無論字大字小都固定」「日期標跟第一行頂 40PX」。⚙**實作**：拿掉 0920 那段「用現行成品反推 GAP」的邏輯，改直接 `shift = (ink_top - 40) - tab_box[3]`；標題越長→字級越小→墨水上緣越低→日期牌跟著往下，靠 `ink_top` 本身就夠，不需要第二套判斷。**只作用於 0 級程式壓字（`draw_titles=True`）**；1–4 級 AI 畫標題時量不到模型實際字級，維持釘死在 `YT_HOURLY_DATE_TAB_BOX` 的舊行為。📐**實測四種字數全部 39~40px**（`D:\Downloads\AICG\後台紀錄\20260921\F36_定案驗證\F36_定案_四種字數驗證.png`）：4 字／5 字／7 字 字級 162→牌底 691、字頂 730；17 字 字級 100→牌底 748、字頂 787。🔬**0920 那版為什麼失敗（根因已查明，後台實證＋程式實算，不是推測）**：程式完全照設計運作，是**設計本身永遠不會生效**。①後台撈件（`aicg.tvbs.dev/admin`，許岱軒 0921「俄羅斯國會大選」`20260921-150254` 等四筆）量到日期牌下緣 y=626、標題墨水上緣 y=730，間距 104px；②程式實算 `tab_box=(50,524,568,626)`、`start=162`、反推 `GAP=104px`——**與實測一模一樣**，程式沒壞；③位移公式 `shift = max(0, (ink_top - gap) - tab_box[3])` 只有在**字級被縮小**時才會大於 0，而字級縮小的唯一條件是某行超過 `max_w=1820px`＝**每行約 11 個全形字以上**。實算六組標題，五組字級都是 162（沒縮）⇒ shift＝0 ⇒ 日期牌一個像素都沒動。👉**整點直播標題實務長度 4~8 字，永遠不會觸發**。💡落差在需求解讀：使用者說的「字數多」被實作成「字級被縮小」，這兩件事在這個版型幾乎不重疊。🧪**測試**：`tests/test_f36_hourly_date_gap_20260920.py` 整份改寫成定案版（檔名保留，免得追不到歷史），新增「短標題也必須往下移」（＝0920 被退回的那一格）、「四種字數間距都相同」、「常數改大牌子要往上退」三道守門；`tests/test_date_time_bold_20260913.py` 的三題日期字重測試原本寫死預設框座標，改成先把紅牌掃出來再在牌內量——那三題管的是字重不是位置。📌**實證**：commit `2e9bc19`（exp/redesign-20260903）→ 公司 PR [#32](https://github.com/TVBS-AI/aicg-champion-daihsuan/pull/32) 已合併進 main （merge commit `268a573`，2026-09-21T08:53:08Z）；合併前後各跑一次全套，皆 **2195 題全綠**（OK, expected failures=1）。VERSION `260921-02`。| 使用者原話「YT創意選擇規矩，日期方塊與標題距離可以拉近」。監督核對 `20260915-133528`：紅色「2026/09/15」方塊與其下標題塊之間確有明顯間距。純版面數字，**但要先量現行成品再訂數字**（比照 YT 塊高那次的教訓，不要憑感覺給值）。 |
| F38 | **提高生圖畫布尺寸——救不了錯字，但救得了「糊」：現況 1280×720 被放大成 1920×1080 交付，是升採樣** | 2026-09-15 使用者提出假說「字太小失真或錯字 → 提升解析度」，測試端實拍、監督獨立複驗 | 🔶 **已上線待實拍驗收（commit 31a68e8，已在公司 main；2026-09-23 帳本補登）**｜`HIGH_RES_EDITOR_ENABLED = True`，記者＋編輯雙角色｜⬜ **2026-09-20 使用者列入下一批動刀：把旗標打開**——原話「記者/編輯 字多/字超多的時候 生圖改 2K（因為低解析度 字可能糊掉）」。⚠**與現有命中條件有一處不同：使用者要的是記者＋編輯兩個角色，現在寫死只有編輯**（`density` 對應 standard＝字多、maximum＝字超多，這半已相符）｜🔶 **2026-09-16 程式已備齊但 feature flag 預設關閉（exp `e0f4df6`，Stage 8 `fc71891`）**：`HIGH_RES_EDITOR_ENABLED = False`，合併進來零行為改變。命中條件＝編輯＋standard／maximum，16:9→2560×1440、21:9→3360×1440；`image_generation_size(req)` 同時回傳 provider size 與 output canvas，兩支 provider 共用同一個值。1920×1080 輸出 byte-identical（golden hash 對照改動前 `compose.py` 實測過）。🗳**要開啟必須先過價格 checkpoint，而且 `2560x1440`／`3360x1440` 不在現有離散尺寸清單裡，得先確認 provider 官方吃不吃**｜⚖ **2026-09-16 使用者已裁：只有「編輯版 × 字多／字超多」走大尺寸高解析度，其他組合一律照舊。****輸出端也要跟著拉高**（使用者理由：播出時可能放大）｜⬜ **待裁：要不要為「更銳利」把畫布調大**（與錯字已脫鉤）｜**錯字那一半已結案＝解析度救不了、且錯字與字級反相關**｜裁前要先查大尺寸有沒有加價 | **測了什麼**：同一篇 276 字颱風稿、字超多、同一份消化結果（seed 1633473102）、同一組 prompt，**唯一變因是 `NATIVE_GPT_IMAGE_SIZES["16:9"]`**——A＝`1280x720`（現況）、B＝`2560x1440`。證據全份在 `D:\Downloads\AICG\後台紀錄\20260915\解析度對照\`。🔑**架構層級發現（監督已獨立以 `magick identify` 複驗，非採信轉述）**：`safe_area_spec.py:13` 的 `BASE_CANVAS=(1920,1080)` 讓編輯版安全框 ON 時，`apply_safe_frame` **不管 AI 原始畫布多大，成品一律輸出 1920×1080**。實檔為證：`reso_imageA_source.png`=1280×720 → `reso_imageA.png`=**1920×1080**；`reso_imageB_source.png`=2560×1440 → `reso_imageB.png`=**1920×1080**。👉**所以放大生成畫布不會讓字變大**——字級與版面比例是消化階段決定的，與生圖畫布無關。❌⚠**錯字這一半：本輪結論作廢，樣本沒踩到考題（2026-09-15 使用者當場指出，查證屬實）。**使用者原話：「應該要測試字超多的情況，不是測字少，字少當然看不出差別啊」。查證：送出的 `density` **確實是 `maximum`**（原始 payload `reso_digest_payload.json` 為證，且該欄是 pydantic Literal，打錯值會 422 擋下、不會靜靜落回預設），**但消化端實際只產出 6 塊／49 字**（逐塊精算，非目測），**比 D16 任何一檔實拍都輕**——連 D16 的字多（6 塊／131 字）都不到，更別說字超多（8 塊／123 字）。**錯字只在字擠爆版面、字級被壓到極小時才會出現，這輪根本沒到那個狀態**，所以「A 張 0 錯字」不是好消息，是沒考到。👉**「解析度救不了錯字」這個結論降級為未驗證**（道理上的推論仍然成立：模型畫錯的是字形本身，重新採樣不會把錯字變成對字——但那是推論，本輪沒有實證）。**「糊救得了」那一半不受影響**，升採樣糊化是幾何事實，與稿件密度無關。這個落差另開 **B57** 追。 A、B 兩張**都是 0 錯字**（監督獨立看過全圖與 300% 裁切，「銀河颱風逼近／海上警報生效／955百帕／45公尺每秒／18公里北轉／東半部南部有豪雨／停班課明晨公布／農委會盤點菜價」逐字正確）。A 本身就沒有錯字可減少，所以**這對樣本無法回答錯字問題**；而且道理上也救不了——模型畫錯的是**字形本身**，升採樣或降採樣都不會把錯字變成對字。✅**但「糊」這一半救得了，而且效果明顯**：現況是 **1280→1920 升採樣（放大 1.5 倍）**，本來就會糊；B 是 **2560→1920 降採樣（0.75 倍）**，帶著真實細節。**監督把同一句「停班課明晨公布」在各自位置裁切放大 300% 對看，B 的筆畫邊緣明顯乾淨、A 明顯有升採樣的軟邊——差距不是測試端寫的「略銳利」，是一眼看得出來。**⚠**監督對測試端回報的一處更正**：回報寫「A/B 逐塊配置一致，沒有重排」。**區塊的種類與數量確實一致（1 標題＋3 徽章＋3 卡片），但幾何配置明顯不同**——A 的三張卡片置中且較窄，B 的卡片幾乎滿寬、整體左移，徽章位置也不同。**代表畫布尺寸會連動改變模型的版面決策**，「唯一變因」只在輸入端成立、在輸出端不成立。這削弱 A/B 的可比性，也是另一個要記住的行為。💰**代價**：耗時 A 33.8 秒 → B 39.5 秒（**慢約 17%**）；單價不明——回應不帶 OpenRouter generation id，**大尺寸是否加價要從帳號後台核對**，在核對之前不要假設仍是 $0.04。💡**待裁的選項**：甲｜**把 `NATIVE_GPT_IMAGE_SIZES` 的 16:9／21:9 調大**（例如 1920×1080／2520×1080 以上），讓交付尺寸不再靠升採樣，換來全站文字與線條更銳利，代價是每張慢約 17% 與未知的單價變動；乙｜**維持現況**，接受升採樣的軟邊。**監督傾向甲，但必須先查清楚單價**——若大尺寸加價，這就從「幾乎免費的畫質提升」變成成本決策，要由使用者裁。📌**順帶結論：錯字要往別處查。** 若之後真的量到字超多有實質錯字，根因請往 D16 那組密度上限查（塊數／字數擠爆版面），不要再回頭動畫布尺寸。 🎯**2026-09-15 第二輪（記者版密集）＋監督在既有正式站成品裡找到決定性反證——「錯字」這一題結案：不是解析度問題，而且方向完全相反。****第二輪實測**（使用者授權 2 張）：沿用 B57 那份記者版消化結果（8 塊／138 字，seed 1690566148），不重新消化，唯一變因是 `NATIVE_GPT_IMAGE_SIZES["21:9"]`——A=`1680x720`、B=`3360x1440`。模型確實吃到 2 倍；**成品兩張仍皆為 1920×1080**（監督以 `magick identify` 複驗），`BASE_CANVAS` 鎖死播出尺寸的架構結論在記者版上再次成立。**A＝0 錯字、B＝0 錯字**；版面幾何這次基本一致（與編輯版那輪的位移不同）；耗時 33.7→37.9 秒（慢約 12%）。🔑**真正的答案來自零成本的既有證據**：監督翻 `D:\Downloads\AICG\後台紀錄\20260915\曹雪卿成圖\` 時，發現兩張現成的放大證據——`證據_標題騙字壞掉_放大.png` 與 `證據_同圖第1格騙字正確_放大.png`。**同一張正式站成品裡，「騙」這個字出現兩次**：**大標題那個（全圖最大的字）筆畫糊成一團、馬部下方四點變成亂筆，是壞字**；**內文區塊那個（明顯小得多）筆畫乾淨、完全正確**。👉**同一張圖、同一個字、同一次生成、同一個解析度——小的對、大的錯。****這直接推翻「字太小所以出錯字」的前提，也連帶推翻「提高解析度能救錯字」**：如果全圖最大的那個字都畫壞了，像素從來就不是限制條件。⚠**誠實補一句**：大標題與內文除了字級不同，**樣式也不同**（標題是 3D 立體擠出＋發光，內文是平面字），所以嚴格說變因不只一個。但無論如何，**「字越大越不會錯」已經被否證**，加解析度的理由不成立。✅**結論（錯字這一半，結案）**：**解析度救不了錯字，而且錯字與字級無關（甚至反相關）。**真正的候選變因是**字體樣式的複雜度**（3D 擠出／發光／描邊會把筆畫吃掉），這條要另開單查，不要再往畫布尺寸走。✅**糊／失真這一半仍然成立且不受影響**——升採樣是幾何事實，與錯字無關。**F38 的待裁選項因此收斂成：要不要為了『線條與文字更銳利』而把畫布調大，代價是慢 12–17%＋未知單價。** 跟錯字完全脫鉤。⚠**順帶一個證據檔的瑕疵**：測試端存的 `dense_imageB_scaled1680x720.png` **實際是 1280×720**（監督 `magick identify` 查到），檔名與內容不符，疑似沿用上一輪的參數。本輪結論不受影響（兩張成品本來就都是 1920×1080，直接對比即可，那個縮圖根本用不到），但**該檔名會誤導後人，要修正或刪除**。 🛠**2026-09-16 裁決後的實作注意（監督補，動工前必讀）**：①**要改兩個地方，只改一個沒有用**——`NATIVE_GPT_IMAGE_SIZES`（生成端畫布）與 `safe_area_spec.BASE_CANVAS=(1920,1080)`（輸出端，`apply_safe_frame` 現在**不管 AI 畫布多大一律吐 1920×1080**，實檔驗過 2560×1440 進去也是 1920×1080 出來）。使用者這次明講「輸出端也要配合拉高」，所以 `BASE_CANVAS` 要變成**依檔位／角色決定**，不再是常數。②**條件要綁對**：只有 `role=編輯` 且 `density in (字多, 字超多)` 才走大尺寸，其餘維持現況，**要有測試釘住這四種組合**（編輯×字多、編輯×字超多 → 大；編輯×其他、記者×全部 → 照舊）。③**成本仍未查證**：0915 記過「大尺寸是否加價要從帳號後台核對，核對前不要假設仍是 $0.04」，**這件事到現在還沒做**——使用者已裁要做，但**上線前仍建議先看一次帳單單價**，因為這個檔位組合正是量最大的那種。④耗時實測慢 12–17%。⑤安全框規格（X=140/Y=109/W=1634/H=751 以 1920×1080 為基準）是**比例**還是**絕對像素**要確認，畫布一變這組數字若沒等比換算就會錯位。 |
| F39 | **後台介面不顯示生成參數（density／role／seed）——但後端其實有記，是「看不到」不是「沒有」** | 2026-09-15 監督派工失誤後追出來的根因 | 🔶 **已修待驗（exp `000da0d`，2026-09-16）——後台已渲染 role／density／seed，未部署**｜🔻**2026-09-15 監督自我更正：原標題「不記 density」是錯的，`request_log.py` 有寫、JSONL 有這欄，缺的是後台介面與匯出**｜**嚴重度下修、但價值上修——代表舊案有機會精確重現**｜與 F30／F31 同一類，建議一起做 | **怎麼被發現的**：使用者要求重現後台紀錄 `2026-09-15 10:14:12 曹雪卿`，監督的交辦單寫「density 照該紀錄檔記載」，**但該檔根本沒有 density 欄位**（`grep -oiE '字超多|字多|字少|字極少|無字|不改字|maximum|standard|simplified' 20260915_101412.txt` 六個檔位字樣**零命中**）。執行端只能自行挑預設值 `standard`（字多），**整輪 2 張付費測試（約 $0.08）測到錯的檔位**，產出 4 個短塊，而目標成品是 8 塊、每塊兩行——**根本沒有重現到**，由使用者肉眼看出（原話：「剛剛那一批 根本不是測字超多啊 字非常少」）。**後台表頭只有 `時間｜使用者｜類型｜成圖｜內容`**（F31 已記過），**角色、density、安全框、蓋章、色調、seed 一個都沒有**。👉**後果不只是這次白花錢**：只要使用者回報「某張圖有問題」，我們就**永遠無法精確重現那張圖的生成條件**，只能靠猜或靠肉眼比對塊數回推。這是所有「重現使用者回報」類調查的共同天花板。💡**修法**：後台紀錄補上生成參數（至少 role／density／safe_frame／stamp／tone／seed／editor_format）。**seed 尤其關鍵**——有 seed 才談得上真正的重現。📌**與 F30／F31 的關係**：F31 是「失敗完全不留痕」、F30 是「成功不記耗時與 provider」，本條是「成功不記生成參數」。**三條是同一個病：紀錄的欄位不足以支撐事後調查。****建議三條合併成一次改，不要分三次動同一段程式。**⚠**流程面的連帶教訓（監督自記）**：交辦單**不准用「照某檔記載」代替字面值**，派工前要自己開檔確認欄位存在；每輪付費測試前要先過一道「產出有沒有重現到目標條件」的關卡（塊數／行數同量級），不合格就作廢重來，不拿它下結論。 🔻**2026-09-15 監督自我更正（查 OpenRouter 用量時順手查出來的）**：**本條原本的標題與診斷有誤，`request_log.py` 其實有記 `role`／`density`／`seed`。**程式碼實證：`log_generation()` 的參數列有 `density: str = ""`（line 77）、`seed: int | None = None`（line 85），寫入字典時有 `"role": role`（99）、`"density": density`（100）、`"seed": seed`（114），`log_rejection()` 同樣有（135／155／156）。本機 `logs/generations-20260915.jsonl` 也確實可以靠 `density` 欄位把當天的呼叫分檔位統計出來（實跑：`maximum` 26 筆、`standard` 377 筆等）。👉**所以真正的缺陷是「後台介面與匯出的 txt 只吐五欄」（＝F31 講的那件事），不是後端沒記。** 本條併入 F31 的範圍，**嚴重度由「無法重現」下修為「要多繞一層才拿得到」**。🔑**但價值反而上修，這點很重要**：既然 JSONL 有 `role`／`density`／`seed`／`prompt`，**正式站主機上的 `logs/generations-*.jsonl` 應該就存得下曹雪卿那幾筆的完整參數**——先前判定「10:14:12 那筆無法重現」是**基於後台匯出的 txt**，**不等於正式站的 JSONL 也沒有。**✅**下一步（零成本、不打任何 API）**：想辦法取得正式站的 `logs/generations-20260915.jsonl`（或請有主機權限的人撈出指定時間的那幾列），用 `ts` 對 `10:14:12`／`17:28:51` 把 `role`／`density`／`seed`／`prompt` 直接讀出來。**若撈得到，B58／B57 那幾輪就不必再用猜的參數重打，可以省掉整批付費重測。**⚠**注意 B29 仍然成立**：`prompt` 欄位寫入時被 `MAX_PROMPT_CHARS = 4000` 截斷，**VARIABLE FIELDS 落在截點之後**，所以 prompt 全文仍不可靠；**但 `role`／`density`／`seed` 是獨立欄位、不受那個截斷影響，這三個是可信的。**📌**本機查不到那兩筆**（實跑確認 `ts` 沒有 `10:14:12`／`17:28:51`），因為那是**正式站**產生的，本機 log 只有本機的呼叫——這也再次確認要撈的是正式站那份檔。 🔶 **2026-09-16 已修待驗（exp `000da0d`）＋「撈正式站 log」這條路已結案：撈不到，改走後台渲染。** ❌**「取得正式站 `logs/generations-*.jsonl`」不可行**：`aicg.tvbs.ai` 跑在**公司 GCP 專案**，本機 gcloud 只看得到個人專案 `tvbs-aicg-linebot`（B39 欄位早就記過這一點，本條當時沒接上）。容器磁碟上的 JSONL **沒有任何對外路徑**，`/admin` 也只掛 `/admin` 與 `/admin/image/{month}/{filename}` 兩條，**沒有 raw JSON 或匯出端點**。✅**實際撈到的**：`GET /admin?month=2026-09` 回 200，**200 筆——但那是上限不是總數**（`audit_archive.list_records()` 的 `limit: int = 200` 寫死，`/admin` 也沒有調整它的查詢參數；實撈回到 09-07 就停，更早的看不到，這點另記在 F31），其中**曹雪卿 0915 共 22 筆**（09:50:47～17:32:40），每筆有 `ts`／姓名 email／類型／`image_model`／`request_id`／**新聞原文全文**／`prompt` 前 4000 字／**成品圖（22 張全數下載完成）**。落檔：`D:\Downloads\AICG\後台紀錄\20260916\`（`admin_2026-09.html`、`曹雪卿_0915_後台原始.json`、`全部_2026-09_後台原始.json`、`F39_正式站撈log_結果.md`），成品圖補進 `D:\Downloads\AICG\後台紀錄\20260915\曹雪卿成圖\`。✅**修法（本次做的）**：`admin_console._params_line()` 把 `role`／`density`／`seed` 渲染出來，純唯讀、沒動任何寫入端；缺值印「－」而不是整段藏起來（藏起來分不出「這個版型沒帶」與「後台不顯示」）。**部署後連 0915 的舊紀錄都查得到 `role`／`density`**——那三個欄位本來就在正式站磁碟的歸檔 JSON 裡（`main.py:4222` 從 `dffef81` 起就有傳），只是沒印。⚠**`seed` 例外**：它是 F0（Stage 2-4，2026-09-15）才加的、只在 `exp`，正式站 0915 跑的版本很可能沒有，舊紀錄會是「－」。⚠**塊數仍救不回來**：見 B29，0915 那 8 筆的 `VARIABLE FIELDS` 已經被截掉，只能改從成品圖上數。🐛**順手抓到的真 bug**：`_esc()` 走 `value or ""`，`seed 0` 會被印成空白，已改 `_esc(str(seed))` 並用測試釘住。**測試 1767 passed（原 1761，+6）。** ✅**2026-09-16 已合併進公司 main（PR #25，merge `9534b11`，VERSION `260916-01`）。⚠仍未部署到正式站**——合併不等於上線，部署要使用者決定。 |
| B33 | 封面「一次 API 都不打」的零成本路徑，其實已經先打了標題斷句 | 同 B31 | ⬜｜✅ **2026-09-16 結案，但不採納本列建議的修法**（理由見敘事）| `_cover_composite` 的註解自稱「一次 API 都不打」（`main.py:4756`、`4764-4766`），但 `apply_title_break_hints` 在端點入口**無條件執行**，排在 `background_image_base64` 零 API 分支之前：`main.py:5183`（十點，零 API 分支在 `5214`）、`main.py:5896`（YT，零 API 分支在 `5958`／`5988`）。斷句模型 `gpt-5.4-mini`（`main.py:102`／`112`）timeout 8 秒。注意斷句本身有正確的短路（標題夠短就不送，`main.py:4251-4260`／`4306-4309`），**問題純粹在呼叫位置**。把呼叫移到零 API 分支之後即可 ⚠**2026-09-16：本列只抓到問題的一小半，完整版見 [B75]**。零成本路徑先打斷句只是其中一種情況；**更大的洞是 `ai` 標題模式下斷句結果根本沒人讀**，而 `ai` 是創意 1 級以上的預設。兩件事在同一處修，請一起處理。 ❌**2026-09-16 實作時發現本列建議的修法是錯的，不採納**。本列寫「把呼叫移到零 API 分支之後即可」，**前提是那條路用不到斷句——但它用得到**：零成本路徑走的是 `_cover_composite`，那正是用 Pillow 壓字的路，斷句邊界會被 `compose._split_line_near_middle` 讀走，移過去只會讓那條路的斷行品質變差。👉**真正白打的只有 AI 標題模式那條，已由 [B75] 的守衛擋掉**；本列剩下的問題純粹是註解自稱「一次 API 都不打」與事實不符，已改寫該註解。**結案。**|
| D4 | 生圖模式附圖左右指定（全線）| 待裁 |
| D9 | CG 線原圖放置的順序／位置／內文搭配（四條路）| 待裁；路線 3 會牽動 **D12** |
| B10 | 附圖尺寸失控（文件照被縮到看不清）| 併入 D9 的位置／佔比一起訂 |
| B22 | 附圖是資訊載體時仍被當裝飾 | 同上；**但修法落在創意條文**，見下方跨刀 |
| B24 | 附圖在後續生成中消失（15:39 起）| 查完：**不是** `9e747a7`。全程 type＝自動判斷，該 commit 只清十點／YT 槽、不動 `userRefImages`。消化 15:36 仍承認 supplied photograph，**15:39 已改口畫葉門地圖**。依據：正式站 15:39:01 歸檔 prompt 已無 supplied photograph（`D:\Downloads\AICG\後台紀錄\20260914\20260914_0-2_0-3量測.md`）；MASTER 原記 15:44 偏晚。剩餘不確定：15:39 當下附圖是否仍在 payload（歸檔在 asis 注入前）。觀察併 B22／B25 |
| B1 | 已合成成品再餵回去 → 日期重複 | 🔶 **已修待驗（exp）** prompt chrome；overlay 合成未動 |
| M1 | 附圖規則模組 `refs.py` | **必須排在 D4／D9 之後**——模組的形狀（插槽 vs 內文標記 vs hybrid）正是 D9 要裁的東西。先重構會白做 |
| R4 | 附圖未測組合 | 驗收，排最後 |

**可以現在就做的**：B26。B24 查證已結（不是 `9e747a7`）。其餘等裁。

---

### 第二刀：記者／編輯 CG 版的字多字少＋創意階梯

**這一刀的模組邊界最乾淨——兩者在同一個函式裡。**
`build_digest_instructions`（main.py:1487–）：density 區塊在 **1533–1541**、
`cg_creativity_rules(visual_creativity)` 在 **1565**，中間只隔版型區塊。
注入順序有語意（repo 慣例：位置在後＋明文 OVERRIDE 才壓得住前面的命令句），
**重排會改行為，不能當成單純搬字**。

**成員**

| ID | 項目 | 卡在 |
|---|---|---|
| F1 | 字多／字少級距拉開 | D3（天花板非配額要不要鬆綁）|
| D3 | ↑ 的裁決 | 待裁 |
| B19 / F23 / D16 | 大標題字數納入 density | D16（各檔字數與行數）|
| D14 / F20 | 「完全不要文字」當成 BAR 上的一檔 | D14（形式）|
| A1 | CG 線導入變化池 | D2（CG 要不要禁綠）、D5（幅度要不要訂數字）|
| A2 | CG 配件件數改由程式抽 | D5 |
| A5 | 十點創意階梯移植回通用版 | 同 A1 |
| D2 | CG 禁綠 | 待裁；**與 F16（漲跌配色綠＝下跌）正面衝突，兩者要一起裁** |
| D5 | CG 幅度數字表 | 待裁 |
| F0 | seed 欄位三條線統一 | **D1 已裁，可做**——A1／A2／A5／B5 的共同前置 |
| B3 / A3 | 前台三份創意說明表 vs 後端對不上 | 無——B3 是純錯字級修正，A3 是接到後端 |
| B4 / B5 | docstring 過期／與行為矛盾 | 無 |
| R5 | 改 A1 前的長相重複度基準 | **要先跑**，不然改完沒有對照 |

**三個一定要先做的前置**
1. **F0**（seed）——A1／A2／A5 全部靠它才驗得出「有沒有真的變化」。
2. **R5**（基準）與 F1／D16 共用的「先量現行成品」——repo 已有教訓：訂數字前先量。
3. **凍結測試要一次性重凍**。`tests/test_reporter_prompt_frozen.py` 四份 fixture **釘住每一個 density 檔位**，所以第二刀的 density 那一半**必然打破它們**，要刻意重凍——**收在這一刀的最後做一次，不要每改一項凍一次**。
   （反過來，創意那一半碰不到：fixture 的 `visual_creativity` 預設 0，`cg_creativity_rules(0)` 回空字串。）

**記者／編輯的分流其實很淺**：density 只有 `standard` 檔靠 `_STANDARD_LIMIT_CLAUSES[is_editor]` 分岔，
`maximum`／`simplified`／`minimal` 與**全部**創意條文都不分角色。使用者是以
「記者與編輯 CG 版」框這一刀的，實作時要確認每一檔要不要都分流。

---

### ✅ CP1 已放行（2026-09-14）

使用者：「都照你的建議做，你監督，實際修改動手的工人是 claudek2。」

放行範圍**只含 D16 ＋ D2 ＋ 一次重凍（含補編輯版快照）**，**F2 拆出另走 CP1b**。
分工：本 session 監督與驗收、**claudek2 實作**、「測試」session 跑生圖驗收輪。
細節與對 claudek2 的硬性約束見 `docs/plan-20260914-第一波三刀.md` 文末〈CP1 放行＋分工〉。

---

### 建議獨立成一刀：F2（最終 prompt 改 Markdown 瘦身）

改的是同一個函式，看起來該併進第二刀——**但它會重凍全部 fixture**。
跟第二刀綁在一起的話，density 的行為改動與純格式改動會混在同一批 diff 裡，
出事分不出是哪一邊。**先做完第二刀、重凍一次，再單獨做 F2、再重凍一次。**

---

### 跨兩刀 / 不屬於任何一刀

- **A6（能力矩陣加欄位）橫跨兩刀**：附圖順序／位置／內文對應／多張措辭屬第一刀，
  創意池／seed 屬第二刀。矩陣本身建議**兩刀都收尾後一次補齊**，否則欄位定義會跟著改。
- **B22 的歸屬**：症狀是附圖（第一刀），但修法是「裝飾讓位給資訊載體」，
  條文落在創意／版面那一段（第二刀）。**兩刀都要知道它**。
- **剩下的自然分成另兩組**（使用者這次沒點，先記著）：
  消化忠實度（B17／B18／B20／B21／F22／F24）、refine 越權（B13／B23）。

---

### 裁決後更新（2026-09-14，八項一次裁完）

**D2／D3／D4／D5／D9／D12／D14／D16 全部已裁**（連同先前的 D1）。
**兩刀的裁決閘門全部解除**，上面各表「卡在」欄位以 D 表的已裁內容為準。

三件裁決衍生出來的新工作，不在原本的分組表裡：

1. **附圖順序要真的傳得到生圖端**（D9 追加裁決「預設＝上傳順序」）。
   現在 CG 線 `shared_refs=True`，上傳順序在後端被攤平、從來沒進過 prompt。
   **這是第一刀的新前置**——順序欄位沒接通之前，任何順序規則都沒有依據。
   排在 B26 之後、M1 之前。

2. **封面線既有的禁綠範圍要一併收窄**（D2）。
   裁決是禁「螢光綠（chroma key 綠）」，不是禁所有綠色。
   封面線的 `COVER_NO_GREEN_RULE` 與 `COVER_PALETTES` 的「無綠」定義現在訂太寬，
   要跟 CG 線一起改成同一個窄定義。**這讓 D2 從「CG 線加規則」變成「兩條線一起收窄」。**
   連帶：F16（漲跌配色）**不再與 D2 衝突**，可以從「要一起裁」降級為一般項。

3. **大標題字數要先量現行成品**（D16）。
   裁決已定「行數不動、只調字數」，但數字未定。
   量測與 F1／R5 的「先量現行成品」是同一件事，**一次量完**：
   從後台歷史紀錄統計各 density 檔位的標題實際字數與內文字數，一併提數字。

### 追加裁決（2026-09-14）：A1／A2 的設計方向已定

使用者：「創意階梯要導入原本就有的抽素材庫 ＋ 創意 3／4 的時候可以加掛配件（對齊設定）」

**A1 定案：不另建新池，直接沿用既有的變化池。**
`creativity.py` 已有的 `COVER_PLATE_SHAPES`／`COVER_STAGGER_PATTERNS`／`COVER_TYPEFACES`／
`COVER_PALETTES`／`COVER_ANCHORS`／`COVER_TILT_DIRECTIONS`／`COVER_ACCESSORY_POOL`／
`COVER_ACCESSORY_SHAPES`／`COVER_FLAG_ACCESSORY` 就是那個素材庫，CG 線接上去用。
**這讓 A1 從「設計一套池」縮成「接線」**，工作量大幅下降，也順帶解決 A5（十點移植回通用版）
——兩者變成同一件事。

**注意池子目前分兩處**：池子本身在 `creativity.py`；件數表 `COVER_ACCESSORY_COUNTS`
與抽法 `cover_accessories()` 在 `editor_formats.py:574/649`，註解寫明「十點專屬、
跟 titles／full_width 耦合，不是跨拉桿共用的機制」。
**CG 線要共用，這個耦合就得先拆**——這是 A1 的實作前置。

**A2 定案：配件在創意 3／4 加掛，件數由程式抽（不再讓模型自選）。**
⚠ **一個要確認的小落差**：封面線現行 `COVER_ACCESSORY_COUNTS = {0:0, 1:0, 2:1, 3:2, 4:3}`
——**是從創意 2 就開始給 1 件**，不是 3 才開始。使用者說的是「3／4 加掛」。
兩種可能：(a) CG 線自己用 `{0:0,1:0,2:0,3:?,4:?}`，與封面線刻意不同；
(b)「對齊設定」的意思就是照封面線那張表。
**不擋工**——先照使用者字面的 3／4 實作，接線時把這一格標出來讓使用者確認即可。

**仍未裁的只剩 D8（畫風池細節）**，但它不屬於這兩刀，不擋。

---

### 🔴 下一批動刀名單（2026-09-16 使用者指定：「這些問題要列入下一批優先動刀，其他版型也要一起修」）

起因是 2026-09-16 正式站事故（B61）：王結玲與曹雪卿同一個早上，各自被同一個洞咬到——
標題沒寫人名時，封面各版型的生圖 prompt **完全沒有「不得畫具名真人臉」的防線**，
模型自己把新聞主角畫成過期的聯準會主席。子代理全路徑盤點後確認**不是單一版型的疏漏**。

⚠**本節 2026-09-16 合併過一次**：初版只列了 0916 事故那幾條，**漏掉使用者 2026-09-15 已經裁定的下一批**
（D21＋B50、B51＋B52＋B55、B53）。使用者當天就指出來了。**本節是下一批的唯一完整名單**，
0915 與 0916 兩次裁定都在裡面；排序沿用 0915 的「第一優先」宣告，0916 的新項目接在後面。

| 順序 | 內容 | 涵蓋的列管項 | 裁定日 | 為什麼排這個順序 |
|---|---|---|---|---|
| **① 最前面** | **消化 token 預算放寬（D21 甲案）＋ 重試不再擲骰子** | **D21**（已裁甲）、**B50** | 2026-09-15 | 使用者追裁「與 B50 同批、排在最前面」。`DIGEST_MAX_TOKENS=6000` 被思考吃滿、正文寫不出來，而五次 attempt 送的是位元組相同的請求——失敗一次跟失敗五次不獨立，重試等於白重 |
| **② 追加修改的封面例外** | 封面被置框／Logo 角標消失／十點合成版漏掉例外／原圖放置被整張重畫 | **B51**、**B52**、**B55**（規則已裁） | 2026-09-15 | 使用者「下一波優先要修」，三條同一批、同一段程式。B55 的規則使用者當場裁好了：24H LIVE 單張原圖放置＝原圖滿版，**創意階梯只能改標題設計、不得重置原圖** |
| **③ 真人臉這一整條**（0915＋0916 合併） | **B53**：補畫面描述只餵一句標題、不餵新聞原文 → 認錯同名人物、英文名拼錯 → **肖像查照因此失敗**；**B61／B62**：出口強制層（見下）；**B63**：refine 指名換臉 | **B53**（0915 已裁提前）、**B61**、**B62**、**B63** | 0915＋0916 | 🔑**B53 與 0916 事故是同一條線的上游**：查照失敗的原因之一是餵給消化的素材太少。四條一起做才是完整的一刀——**素材（B53）→ 規則（B61／B62）→ 修改（B63）** |
| ③-a | **出口強制層**：所有 `ImageGenerateRequest` 送進 `generate_image_raw` 前，一律過一支共用 baseline 注入（真人／品牌／示意圖／臺灣用語），已含就不重複；**再補一支「列舉所有生圖路徑、斷言最終 prompt 必含鐵則」的測試** | **B61**（六個封面模板）、**B62**（hybrid.js 全裸） | 2026-09-16 | 做出口層，**B61／B62 一起消失**，且從此「新版型漏帶鐵則」是紅燈不是事故。逐個模板補條款是治標，下一個新版型還會再漏 |
| ③-c | **F41 身分鐵則**：人名只抽取不推斷（消化／derive 端不補文中沒有的人名，生圖端不自行放具名真人）；**F40 四層分流**：上傳照 → 維基照片 → 有條目無照片則 AI 自畫（訊息欄通知，不標在圖上）→ 連條目都沒有則畫無人場景 | **F41**、**F40** | 2026-09-16 | 第一層（身分）是 0916 事故真正錯的那一步；第二層（臉）改成四層分流 |
| ③-b | **refine 支援指名換臉**：偵測「換成某某人」→ 查 `photo_lookup` → 附照片並改用允許換臉的規則；**查不到就明講辦不到**並提示改走原圖放置 | **B63**，與 **F37**（畫面來源欄位）、**B28**（示意圖標籤）同一叢 | 2026-09-16 | 使用者實際下的指令現在**結構上必定失敗**，失敗方式還是「悄悄捏一張臉」 |
| **④** | **照片來源覆蓋率**＋「查不到就不要生背影，生成前擋下要求上傳」 | **F40**（新開） | 2026-09-16 | 維基對台灣譯名／新面孔覆蓋率不足，是整條真人臉線的上游；使用者：「畫出背影就是錯的」 |
| **先裁再做 → 已裁** | B66＝寫實為主帶一點插畫筆觸（條文要改寫＋實拍驗收）；M5＝**只做 L1**，L2／L3 暫不動工；F40＝維基維持墊底、不接外部圖庫 | **B66**、**M5**、**F40** | 2026-09-16 已裁 | 三條 2026-09-16 一次裁完。⚠F40 仍有一半沒裁：**查不到照片時要生背影還是擋下要求上傳**，動工前要問 |
| **已修待推** | 能力判斷與路由同源 | **B64／B65**（`f07e0a0`） | 2026-09-16 | 已 commit、1768 綠，等公司 PR／部署 |

| **⑤ 收尾小刀（0916 裁定要做）** | A8 安全框殘值強制歸零｜F30＋F31 併成一次改（紀錄欄位不足）｜B56 附圖上的既有文字不再硬性禁止複製 | **A8**、**F30**、**F31**、**B56** | 2026-09-16 | 三件互不相干、都不大，可併成一次收尾。**24H LIVE 兩行使用者這次沒勾＝不列管** |
| **⑥ 份量線（0916 裁定）** | B57 **兩個一起做**（prompt 下限＋後端塊數防呆）；B60 驗收＝**同參數連打 5 次、塊數全落在目標值 ±1** | **B57**、**B60** | 2026-09-16 | ⚠**B57 會改 prompt 文字＝撞凍結快照，動工前要先要到重凍預算** |

### 🔍 2026-09-20 正式站後台實查（9/18–9/20，91 筆；全月 360 筆）

使用者指示上線撈真實紀錄，原始 HTML／JSON／完整報告落在 `D:\Downloads\AICG\後台紀錄\20260920\`。
**這一節是實測數據，不是推測**；下面每一條都改變了既有列管項的優先序或範圍。

| 發現 | 數據 | 影響到誰 |
|---|---|---|
| **後台對「消化階段」完全全盲** | 91 筆失敗數 **0**、全月 360 筆失敗數 **0**，成功率 100% 是假的。`audit_archive` **只歸檔生圖端點**；502／524（Cloudflare 逾時）／`finish_reason` 截斷全發生在 `/api/generate`、`/api/hybrid/digest`，消化失敗直接拋 `HTTPException`，一筆都沒進歸檔。09-15 實測長稿＋字超多噴 524，後台零紀錄。 | **F31 的真正根因**（原描述「失敗不留痕」低估了缺口，不是漏幾個 except，是整條管線沒接）；**B72** 同根因（沒有歸檔紀錄自然沒有模型欄位）。已於 0920 轉達給施工代理。 |
| **追加修改佔四成流量，但後台認不出來** | 真正的追加修改（prompt 開頭 `IMAGE REFINE RULES`）**36 筆＝39.6%**，後台 `type` 欄位**只有 1 筆**標成 `web-refine`，其餘 35 筆顯示成 `自動判斷`／`資料圖表`。 | **F30／F39**——「這筆是不是 refine」要做成可靠欄位，不能靠 prompt 前綴猜。 |
| **追加修改最大宗＝要純色底／去背** | 葉怡君 09-19 為同一則連打 9 次、12 句指令全在處理背景。 | **新開 F45**。 |
| **大量重試只是換幾個字** | 曹雪卿「亞運棒球」連打 5 次換對手國；王結玲要改「HHK→NHK」（模型自己把 NHK 畫成 HHK），為兩個字母整張重繪。 | **新開 F46**。 |
| **YT 直播直標有人在用，而且是純程式路徑** | 8 筆（09-18 16:11:34～16:13:07，編輯王結玲），**0 失敗**，耗時僅 **81～88ms**（Pillow 壓字，不呼叫生圖模型）。 | 佐證 **B80／B46／F36** 值得做；也證明「程式壓字的輕量路徑」在技術上成立（見 F46）。⚠設計後台欄位時注意：純程式路徑與生圖路徑的耗時差好幾個數量級，**成功／失敗判定與逾時門檻不能共用同一套**。 |
| **「預設（現行）」是編輯的主力** | 角色為編輯共 56 筆（61.5%）；走主流程的 **34 筆＝編輯的 60.7%**。特殊版型 22 筆（直標 8／十點雙切 6／YT封面 4／YT新聞直播 4）。 | 佐證 **F44** 改名「編輯CG」貼合實況，影響面不小。 |
| **前端錯誤文案確實會退化成通用句** | `app.js` 的 `_apiError()` 遇到無 detail 的 500 會退回「生成失敗（HTTP 500），請稍後再試」；09-15 曾抓到 Cloudflare 524 在部分前台路徑被靜默吞掉。 | 與 **F42**（訊息歷史）相關；也說明 B81 當初的觀感從何而來。 |

📌**實查者的優先序建議**（供參考，未經使用者裁決）：①消化階段納入歸檔（後台全盲最致命）②純色底／去背選項（一次消掉最大宗的追加修改）③輕量文字替換。

### 🔴 2026-09-20 使用者追加：下一批動刀的完整名單（本節是 0920 之後的唯一依據）

使用者 2026-09-20 一次交辦，指示**「跟上面那些都列入下一批動刀」**——包含 0917 快照留下的積壓，
加上十條新提的 TODO。**🌊 第二波的限制對本節列出的項目一併解除**（原限制見「使用規則 5b」，
2026-09-15 裁定的那批 F32／F34／F35／F36／B46 都在本節內；**沒被點到的 🌊 項仍然照舊**：B43／B44／B45／B47／F33／D19）。

| 群 | 內容 | 涵蓋的列管項 | 備註 |
|---|---|---|---|
| **甲｜0917 積壓** | B73 那 20 道守門測試逐題判斷｜B55 修法甲（單張原圖放置只產標題圖層）｜B70＋F43 示意圖標籤與畫面來源欄位（Pillow 壓字）｜B68 字面 `\n`｜B76 中國大陸輪廓圖含臺灣｜後台欄位 | **B73**、**B55**、**B70**、**F43**、**B68**、**B76**、**F30**、**F31**、**B72**、**F39** | B73 打開開關會紅 20 題，**要清醒著做不能趕**；**B76 仍卡重凍額度未授權**（見下節，只涵蓋 B57／B61／F40／F41／B66） |
| **乙｜0920 新提（全新列管）** | 編輯格式「預設（現行）」→「編輯CG」｜無字＋高創意不該有創意框｜十點兩行標題｜正式站 app.js 落後 | **F44**、**B78**、**B79**、**B81** | B79 待實證，若要改規則等於追加裁決 D16 |
| **丙｜0920 新提（併入既有列管）** | 2K 生圖開旗標且涵蓋記者｜追加修改欄加大＋ENTER 換行｜播出鏡面跨版行要放重點｜YT 直標素材／字級／空隙｜訊息歷史視窗兼進度顯示｜日期方塊依字數自動判斷｜標題非知名人物改用職稱 | **F38**、**F32**、**F35**、**B80**＋**B46**、**F42**、**F36**、**F34** | 七條都不是新問題，是既有列管被使用者升級或補上修法 |
| **待裁（不得自行動工）** | 編輯版生圖端要不要吃消化前原文｜`MAX_PORTRAIT_FACES=3` 要不要放寬 | **D22**、**R7** | R7 原誤編為第二個 R6，2026-09-20 更正；**使用者 0920 表示 R7 要裁，裁決內容待補**；**D22 已於 2026-09-21 全數裁決完畢（7 題）** |
| **0920 已授權／已裁** | B76 納入重凍額度（見下節）｜B80 官方素材已到手＋**裁甲案**（整張底圖當固定層），幾何已量測 | **B76**、**B80** | 兩件都是 0920 當天解掉的卡點，可直接動工 |

📌**動工時會撞到、但使用者這次沒點名的兩條**：**F37**（上傳真實照片沒地方填「畫面來源」、又一律被壓「AI生成」）與 **B28**（具名真人時示意圖標籤被附圖規則洗掉），
與 **B70＋F43** 是同一叢、同一段程式。**本節不替使用者解除它們的 🌊**，但做 B70／F43 時要一起設計，否則會做兩次。

⚠**本節不取代 0916 那份名單**：0916 那批（D21＋B50、B51／B52／B55、B53／B61／B62／B63、F40／F41、⑤⑥）**沒做完的部分仍然有效**，
本節是接在它後面的追加，不是重排。動工前仍要先讀「依賴」欄與 `D-` 項。

### ✅ 重凍預算已批准（2026-09-16 使用者）

`tests/test_reporter_prompt_frozen.py` 的 fixture **這一批允許重新產生一次**（B57、B61、F40、F41、B66 都要改 prompt 文字）。
**條件**：更新時要在該檔 docstring 的「快照更新記錄」補一段說明這次為什麼更新，沿用既有格式。
**其他 fixture（含 `cover_design_brief_rng_pins_20260911.json`）沒有被授權更動**，要動得另外請示。

✅**2026-09-20 追加授權：B76 也納入重凍額度**（使用者當面授權）。理由：B76 是播出事故等級、使用者要求「百分之百絕對避免」，
修法必然要動 prompt 的地圖／國界規則文字。**條件同上**：更新 fixture 時要在 `tests/test_reporter_prompt_frozen.py` 的
docstring「快照更新記錄」補一段說明，沿用既有格式。

✅**2026-09-20 再追加授權：B70／F43 的「示意圖」標籤全站化也納入重凍額度**（使用者當面授權）。背景：B70 的甲案已把
`PORTRAIT_WITH_REFERENCE_RULES`／`PORTRAIT_MULTI_WITH_REFERENCE_RULES`／`PORTRAIT_ENTRY_ONLY_RULES` 三條路改成程式端壓字，
但另外兩條路——**`portrait_mode="none"`（沒有具名人物的一般重建圖）與 `no_reference`（改畫無人場景）**——的規則寫在
`news_prompt.py` 的 `REAL_WORLD_RENDERING_RULES` 與 `main.py` 的 `REAL_WORLD_FIDELITY_RULES` 第 3／5 條，**兩者都被逐字快照凍結**
（`reporter/editor-digest-*.txt` 與 `reporter-image-prompt-*.txt` 都含這段）。不給額度就只能一半程式保證、一半靠模型自律，
標籤漏畫／錯字仍會從那兩條路漏出來。**條件同上**：重建 fixture 要在 docstring「快照更新記錄」補說明，且**只准重建因這次改動
而變的那幾份**，其他 fixture（含 `cover_design_brief_rng_pins_20260911.json`）一律不得動。

**額度現為 B57／B61／F40／F41／B66／B76／B70＋F43 共七項，其餘照舊要請示。**

### 🏭 這一批的執行方式（2026-09-16 使用者裁定）

**Codex 擬計畫 → Grok 執行 → 本 session 驗收。** 計畫落在 `docs/plan-20260916-下一批全修.md`。
執行者逐條照計畫做，不得自行擴張範圍；需要人工判斷／付費實拍／動未授權 fixture 的項目一律回報，不自行決定。

**🌊 第二波仍然不動**（B43–B47／B49／F32–F36／F37／D18–D20，2026-09-15 裁定：第一波收工前不得動工）。
**裁決不受波次限制**——D18／D19／D20 隨時可以裁，裁完仍排進第二波才動手。

**架構層面的根因與長線方向見 `M5`**——使用者 2026-09-16 提出「橫向版型、縱向模組」的矩陣化重構，
監督的評估（同意方向、但反對一次性重寫）寫在該列。

---

## D — 待使用者裁決（**未裁決不得動工**）

| ID | 項目 | 來源 | 狀態 | 一句話現況 |
|---|---|---|---|---|
| D17 | **播出鏡面的卡片張數要不要隨消化程度變** | 子代理 stage2 的 2-8 逐格證據（2026-09-15），監督獨立複驗 | ✅ **2026-09-15 已裁：要聯動，張數交給模型決定、不寫死**（待實作，下一波） | **裁決（使用者 2026-09-15）**：卡片張數**要跟消化拉桿聯動**，而且**不要另立一張級距對照表**——把張數交給模型依素材與檔位自行決定。`_broadcast_point_count()` 目前「standard／maximum 四張、其餘三張」的硬編碼要拿掉，改由 prompt 給範圍與意圖、讓模型選。⚠**實作注意**：會動到播出鏡面長相與既有測試；`editor_formats.py` 的字串若落在凍結快照內就要重凍額度，**動工前先確認重凍需求並取得授權**。本輪實拍仍跑舊行為（3／3／4／4），已告知測試端不得當缺陷。 ／ **原問題**：播出鏡面走 `broadcast` 版型時，實測四檔是 **3／3／4／4** 點，而這正是 `editor_formats._broadcast_point_count()`（`editor_formats.py:154-156`）釘死的——standard／maximum 四張、其餘三張。**把 density 的數字調大改不動它**，因為卡片堆的張數是版面實體限制，不是文字量限制。同一則素材記者版（非卡片版型）是 1／2／7／8 點，級距完全拉得開，所以這不是 F1 的數字問題。**要裁的是**：要不要讓卡片張數跟著拉桿走（例如「字極少」＝兩張、「字超多」＝五張），還是接受播出鏡面在這個維度上本來就只有「每張卡寫多長」可調。⚠**牽動版面**：卡片張數改了會動到既有的播出鏡面長相與相關測試，屬視覺變更，**未裁不得動工**。MASTER 的 F1 欄早就預告過「卡片堆版型的點數被版型釘死，拉桿本來就沒差」，這次是把它坐實成可裁的題目 |
| D18 | **YT 直播直標到底要不要使用者提供底圖？** | 曹雪卿 0915 回饋文件 | ⏸ **待裁**｜🌊**第二波**（2026-09-15 使用者裁定：等第一波收工才動工）**，但裁決不必等**——你隨時可以裁，裁完排進第二波動工 | 使用者原話「YT直播直標，我在提供底圖?」——**她不知道這個版型的產出是什麼**。監督核對成品（`20260915-131247`）：產出是一張**去背透明 PNG**，只有 LIVE 角標＋直式標題＋來源字＋TVBS logo，其餘全透明，設計上是要疊在直播畫面上的**覆蓋層**，本來就不該有底圖。所以這不是缺陷，是**產品沒把「這是覆蓋層、不是完成品」講清楚**。要裁的是走哪條路：①介面上直接標示「本版型輸出為透明 PNG 覆蓋層」並給預覽底（棋盤格或範例畫面）；②開放讓使用者可選擇上傳底圖合成；③兩者都做。**牽涉產品定位，不自行決定。** |
| D19 | **消化端可不可以把明確日期改寫成相對期間？** | 曹雪卿 0915 回饋文件（由 F33 展開） | ⏸ **待裁**｜🌊**第二波**（2026-09-15 使用者裁定：等第一波收工才動工）**，但裁決不必等**——你隨時可以裁，裁完排進第二波動工 | F33 實例：「創9月2日以來新高」被改成「逾兩周新高」，而 9/2 到 9/15 只有 13 天，**改寫後是錯的**。這類換算只要模型算錯就是事實錯誤上鏡。兩條路：①**禁止**——明確日期一律原樣保留，字數不夠就砍別的；②**有條件允許**——只在字數真的不夠時換算，並要求保守用詞（「近兩周」而非「逾兩周」）。監督建議①，理由是換算錯誤無法在成品上被發現（畫面上看不出原文寫什麼），但**這會吃掉字數預算，影響字少／字極少檔位**，所以要你裁。 |
| D20 | **「版面指令稿」要怎麼進來？——`[...]` 一個符號兩種語義，要先分家** | 由 B49 展開（許岱軒 2026-09-15 實例） | ⚖ **2026-09-15 使用者已裁：目標是「不改字＋讀得懂 `[]` 指令」**｜🌊**第二波** | 問題：使用者想貼一份**版面規格**（`[大標題] xxx`／`[內文1] yyy`／`[背景 小插畫] zzz`），要求「`[]` 裡的當指令、`[]` 外的一字不改」。但 `[...]` 目前已經是「**強調這段文字**」的語法，兩種語義撞在同一個符號上（詳見 B49 的程式證據）。**三條路，請擇一：** **甲｜換語法分家**：版面欄位改用不會撞的符號（例如 `[[大標題]]` 或 `{大標題}`），`[...]` 維持強調。優點是兩種意圖可以並存、可以在同一段裡混用；缺點是使用者要學一個新寫法。**乙｜新增一個「照排」檔位**：在現有的無字／不改字／字極少／字少／字多／字超多之外多一檔，語義是「照我給的欄位排版，文字一字不改」。優點是使用者只要選對檔位、不用學語法；缺點是檔位已經六個，再加會更難選，而且要處理「選了照排卻沒寫欄位標記」的情況。**丙｜自動偵測**：原文若整段都是 `[標籤] 內容` 的結構就自動視為版面指令稿，忽略檔位。優點是使用者什麼都不用做；**缺點是「自動」失手時使用者完全無從理解為什麼**，而且這正是 B9（自動判斷版型不穩）已經吃過的虧——**監督不建議丙**。**監督傾向甲**：語義衝突用語法解決最乾淨，而且不增加選單複雜度；乙可以之後再補。⚠**不論選哪條，B49 那個「不改字會誤殺」的程式缺陷都要修**（比對時把任何 `[...]` 都剝掉），那是獨立的，不必等這裡裁完。 ⚖**2026-09-15 使用者裁決（原話）**：「許岱軒這個案子也要列入下一波處理。理想是不改字，但讀懂 `[]` 等等符號，是指令不該入題。但要考慮使用者真的把 `[]` 當作文字不是指令時，還是要出現 `[]`。」**解讀**：①**不另造語法、不另加檔位**——甲乙丙三案都不採，目標是把「不改字」這一檔做對；②「不改字」要能**辨識 `[...]` 是版面指令並把它排除在畫面文字之外**（指令不入題）；③**但要留一條路讓使用者表達「這個中括號是字，不是指令」**，那時 `[]` 要照樣畫出來。🔧**連帶的必要修正（B49 上半）**：`verbatim_fidelity_problem()` 的逐字比對必須**兩邊同時**剝掉被判定為指令的 `[...]`，否則「不改字」對這種輸入一定誤殺（重試 5 次回 502）。⚠**剩下一個子問題還沒裁，動工前要回來問**：**使用者要怎麼表達「這個 `[]` 是字」？**常見三種——①跳脫字元（`\[`）；②整段開關（介面上一個「原文含中括號，請照字面輸出」的勾選框）；③靠語意自動判斷（**監督不建議**，理由同 B9：自動失手時使用者無從理解）。**監督傾向②**：勾選框對非技術使用者最直觀，不用學跳脫語法，而且「整篇都是版面稿」與「整篇都是含括號的正文」本來就是兩種互斥的情境，很少需要在同一篇裡混用。但這要使用者裁，**不要自己決定**。 ||
| D21 | **思考封頂失效之後，消化的 token 預算要怎麼訂？** | 由 B48／B41 展開（2026-09-15 本機實測） | ✅ **2026-09-16 已實作（exp `d8cb1ee`「fix: stabilize digest token budget and retries」）**：`DIGEST_MAX_TOKENS` 6000→**12000**、`MAP_DIGEST_MAX_TOKENS` 10000→**16000**，地圖必須大於一般這條不變式保住（`tests/test_digest_quality.py` TokenBudgetTests 釘著）。⚠**帳本先前寫「已裁未實作」是過期的**，2026-09-16 監督估實拍費用時回查程式才發現已經做了。🗳**還沒實測驗證**——放寬有沒有真的把截斷率壓下來要看 R6 的 5 次分布｜⚖ **2026-09-15 使用者已裁：甲**（放寬 `DIGEST_MAX_TOKENS`）｜**第一波，下一批第一優先**（2026-09-15 使用者追裁：與 B50 同批、排在最前面） 🔬**2026-09-16 R6 實測驗收：有效，但沒過關**。**有效的證據**：0915 是 `budget=6000`／`completion=6000`／`ratio=1.00` 連五次全截斷；放寬到 12000 後 10 次裡有 5 次順利寫完，思考吃到 8764 仍留得下正文。**沒過關的證據**：仍有 3 次 `finish=length`，而且 reasoning 直接吃到 **11998／11999／10855**——**不是差一點，是再加預算它照樣吃滿**。👉**加碼到 24000 只會讓一次失敗更貴更慢，不會提高成功率**；要治本得壓思考本身（`reasoning.max_tokens` 實測送得出去但模型不遵守，見 R6 原始記載），或改成「截斷就用較短的規則重試」這種降級策略。**下一步要裁**。｜✅ **2026-09-16 已實作（使用者裁定插隊優先，實作外包、監督驗收）**：消化改送 `reasoning.effort`（預設 `low`）、主模型換 `google/gemini-3.8-flash`、provider 改 `require_parameters`。**測試 1974 題 OK**（expected failures=1 是 B71 的）。⚠**尚未部署，正式站要確認環境變數，見敘事欄的部署檢查項**| 實測事實：`DIGEST_MAX_TOKENS=6000`，但 claude-sonnet-5 的思考在 8 次呼叫裡有 5 次直接吃滿 6000，正文一個字都寫不出來，`reasoning.max_tokens=2000` 送了也不被遵守。**三條路：** **甲｜放寬 `DIGEST_MAX_TOKENS`（監督建議）**：改到 12000～16000（`MAP_DIGEST_MAX_TOKENS` 已經是 10000，有先例）。**幾乎不花錢，甚至省錢**——token 照實際生成計費，而現在每次截斷是 6000 token 全部白付＋再付一次重試。風險：思考量會隨規則條數繼續漲，牆只是往後推。**乙｜改用 `reasoning.effort`**：OpenRouter 對 Anthropic 系另有 effort 參數（low／medium／high），可能比 `max_tokens` 有效。**要先查證這個模型吃不吃**，不要憑猜。**丙｜換模型或換 provider**：例如指定不開思考的變體。影響面最大，**本波不建議**。**監督建議甲＋乙一起做**：甲立刻止血（幾乎零成本零風險），乙才是治本。⚠**甲單獨做會有一個副作用要一併想清楚**：預算放大後，原本會「快速失敗」的呼叫會變成「跑很久才成功」，單次耗時上升，可能反而更容易撞到 `DIGEST_DEADLINE_SECONDS=230`。**所以甲若通過，要一起重新檢視死線**——但那要先查證部署形態有沒有 300 秒硬上限（見 B48 的既有警告）。 📚**2026-09-15 離線查證（測試查 OpenRouter 官方文件，監督複核程式碼）**：①文件原文「When using the `reasoning.max_tokens` parameter, that value is used directly with a minimum of 1024 tokens.」＋「max_tokens must be strictly higher than the reasoning budget…」——**我們的呼叫 6000 > 2000 完全符合前提**，文件語氣是硬上限而非建議值，**所以文件解釋不了我們的實測**。②`reasoning.effort` 走比例換算（`budget = max_tokens × ratio`，low=0.2／medium=0.5／high=0.8），Claude 系支援 low／medium／high／xhigh／max，程式目前完全沒用這條路。③**`digest_token_budget()` 已排除一條假說**：`density` 只有在 `verbatim` 時才影響上限，`standard`（字多）與 `maximum`（字超多）在資料圖表底下拿到的是**完全相同的 6000**（監督已讀 `main.py:302-316` 原文複核）。**「字多預算比較小」不成立。** 🔍**監督複核時發現兩件測試沒提、但會改變修法的事**：**(一) 實測其實是雙峰，不是一路失控**——成功那三次的 `reasoning_tokens` 是 **1603／2038／2144**，**都貼著 2000 上下**，看起來封頂是**大致有效**的（模型把當前那段想完才停，略微超出屬正常）；失敗那幾次卻是 **5442／6000**，直接吃滿。**所以不是「封頂完全無效」，是「偶爾整個失效」。**本表 B41 與 B48 先前寫的「保護不存在」要按這個修正理解：**是間歇性失效**。**(二) 正文用量非常穩定**——三次成功的正文分別是 1081／1131／1212 token（completion 減 reasoning），與 repo 自己量過的「856-1361」完全一致。**這讓甲案的把握大幅提高**：只要 `DIGEST_MAX_TOKENS` 放到 12000，**即使思考失控到 6000，仍剩 6000 給那 1200 的正文，一定寫得完**。**甲案因此不只是止血，而是能真正吃掉這個失敗模式**，且不必先搞懂封頂為何失效。➕**新增一個測試沒查到的候選（丁）**：`digest_provider_body()`（`main.py:269`）目前送的是 `{"provider": {"order": [...], "allow_fallbacks": True}}`，**沒有設 `require_parameters`**。OpenRouter 的 `provider.require_parameters` 預設是 `false`，代表**允許路由到不支援所請求參數的 provider，並靜默丟棄該參數**。設成 `true` 可強制只走真的吃 `reasoning` 的 provider——**這是「封頂為何間歇失效」目前最具體的可查候選**，而且是一行設定。⚠但它與 `allow_fallbacks` 互動需查證（收緊可能導致過載時無 provider 可用而直接失敗），**不可直接改，先查文件**。 ⚖**2026-09-15 使用者裁決：採甲。** 放寬 `DIGEST_MAX_TOKENS`。**動工邊界（監督補）**：①**只動 `DIGEST_MAX_TOKENS` 這一個數字**，`DIGEST_DEADLINE_SECONDS`／`DIGEST_TIMEOUT_SECONDS`／`DIGEST_ATTEMPTS` 一個都不准碰（那三個屬 B48 的另一半，要先查證部署形態）；②`DIGEST_REASONING_MAX_TOKENS` 與 `DIGEST_REASONING_HEADROOM` 也不動——注意 `digest_reasoning_body()` 是 `min(2000, max_output_tokens - 2500)`，放寬 `DIGEST_MAX_TOKENS` 之後這個 min 仍然取 2000，**思考封頂不會跟著變鬆**，這正是我們要的；③**不要碰 prompt**（凍結快照）；④乙（`reasoning.effort`）與丁（`provider.require_parameters`）**本次不做**，留待治本。**驗收**：`uv run pytest` 全綠；本機用同一篇 2,930 字稿×字多跑 **2 次**確認 `finish=stop`、`ratio` 明顯低於 1.00（成本約 $0.3，**跑前先報**）；並在 `TODO.md` 留下「為什麼是這個數字」的量測依據——**不要只寫一個數字沒有理由**，本專案已經吃過一次「照錯的天花板設計」的虧（B39）。 📌**2026-09-16 補註（回答使用者「白話解說現在是怎樣」時一併記下）**：R6 那 10 次是**在本機 exp 樹上跑的**，消化模型是 `.env` 指定的 `anthropic/claude-sonnet-5`。**正式站用的是不是同一個模型，目前無法證實**——`request_log.log_generation()` 只記 `image_model`，不記消化模型（見 **B72**）。所以上面的失敗率與耗時數字**只能代表本機這個設定**，推論到正式站要打折。**B72 修好之前，這條的驗收都有這個限制。** 🧠**2026-09-16 Codex（gpt-5.6-sol）評估（使用者指派，唯讀分析；交辦單 `docs/交辦-20260916-D21消化截斷與速度.md`、全文 `docs/回覆-20260916-Codex對D21的評估.md`）**：🔑**最大的收穫是找到了我們一直解釋不了的根因**——**Sonnet 5 官方明說 `reasoning.max_tokens` 會被忽略**，被忽略後模型以預設的 `high` adaptive thinking 執行。這解開了本列先前記載的矛盾（「OpenRouter 文件說是硬值，但文件解釋不了我們的實測」）：**不是 OpenRouter 沒送到，是模型端就不吃這個參數**。👉**連帶推翻了兩個既有假設**：①`require_parameters` 不是主因（R6 十次都已經路由到 Anthropic，不是 provider 輪替問題），它只值得當第二層防護，**不該拿來當第一輪付費驗證的主變因**；②**現行程式其實已經寫了截斷降級**（`main.py:2724`，`length` 之後把下一次 reasoning 設 1024），但**因為同一個原因完全不會生效**。💡**Codex 的首選建議：改用 `reasoning: {"effort": "low"}`**，`DIGEST_MAX_TOKENS` 12000／16000 保留當輸出保險、不再加碼。**不需要動 prompt**，只改 payload 組裝與相關測試。不建議先試 medium——速度是第一順位，要先驗 low 的品質底線；`enabled:false` 理論最快但漏規則風險比 low 高，只能當額外實驗組。🚫**截斷重試在同步流程不建議**：R6 的截斷都在 **125～201 秒**才發生，那時才啟動第二次呼叫**已經不可能滿足兩分鐘**。若要保留，只能在「第一次很早失敗且剩餘時間充足」時重試，而且**不准為了重試把 230 秒死線再放大**。🔄**換模型候選（使用者要求納入評估）**：`google/gemini-3.8-flash`（US$0.75／3.75 每百萬 in／out、P50 首 token 約 2 秒、184–202 token/s）當 Sonnet low 失敗後的第一順位；`google/gemini-3.1-flash-lite`（US$0.25／1.50、P50 約 0.49 秒）最快但最可能「JSON 正確、內容被品質閘門退回」，只能當測試組。⚠**風險**：本專案的 prompt 是**照 Claude 行為調出來的**，難的不是 schema 而是那份約兩萬字元、約束極多的 system prompt，以及臺灣繁中與內容忠實度。🔬**監督獨立查證（零成本，打 OpenRouter models／endpoints API）**：**① 兩個模型 ID 都真實存在，價格與 Codex 所述一致**（gemini-3.8-flash in 0.75／out 3.75；flash-lite in 0.25／out 1.50，每百萬；對照 claude-sonnet-5 是 in 2.0／out 10.0）。**換 Flash 約便宜 6 成，換 Lite 約便宜 9 成。****② Codex 提醒的 provider 白名單問題屬實**：`DIGEST_PROVIDER_ORDER` 預設是 `anthropic,claude-on-aws,azure/global,amazon-bedrock/global`（`main.py:268`），**全是 Anthropic 家族**，換 Gemini 一定要跟著切。**③ 但監督補查到一個 Codex 沒提、對決策有利的事實**：那份白名單當初存在的理由是**「三個 vertex 端點不支援 `structured_outputs`」**（`main.py:262` 註解），而實查兩支 Gemini 的**所有 provider 端點（Google／Google AI Studio）全都 `structured_outputs=True`**。👉**換 Gemini 沒有當初那個包袱**，provider 設定相對單純，不會重蹈 strict schema 被靜默降級的覆轍。💰**費用（Codex 估，保守假設每次 20K in＋12K out）**：Sonnet 5 約 US$0.16／次、Gemini 3.8 Flash 約 US$0.06／次、Flash Lite 約 US$0.023／次。「Sonnet low 10 次＋兩支 Gemini 各 10 次＋勝出者再確認 20 次」保守上限約 **US$3.6～5.6**。🆓**可以先做、完全免費的部分**：①用 mock 確認 payload 真的只送 `effort=low`；②驗 strict schema／繁中品質閘門／截斷判斷／provider routing；③**拿 R6 的原始輸出重播解析與品質檢查**（資料已經在 `D:\Downloads\AICG\後台紀錄\20260916\R6_長稿消化_10次.jsonl`）；④**補「骗」這類已知漏網字的測試案例**（見 **B71**）——否則換模型比較會低估簡體字問題。⛔**關於使用者新下的兩分鐘硬約束，Codex 的判斷是「現行架構給不了保證」**，並指出要達成至少需要：消化與生圖**各自明確的時間配額**；**依剩餘時間決定要不要重試**，而不是固定 `DIGEST_ATTEMPTS=5`；**消化超過約 45～60 秒就停手**，不能讓單次吃到 120～200 秒；主路徑直接用 low effort 或 Flash，**不能把「慢模型失敗後再換快模型」當常態**；以及**補記消化模型／provider／effort／各階段耗時與失敗原因**——「否則正式站無法驗證 SLA」，這正是 **B72**／**F30**／**F31** 的觀測缺口。👉**這點與 B72 互相印證：不先把消化模型與耗時記進稽核，任何 SLA 都是喊的。**📌**監督建議的下一步**：先把上面四項免費驗證做掉（不需要新授權、不花錢），同時 **B72 的補記欄位併進同一批**（否則付費實測跑完也無法回頭比對）；付費的部分建議**只先授權 Sonnet `effort=low` 的 10 次篩選輪（約 US$1.6）**，過了門檻再談要不要換模型——**換模型會連帶讓那份凍結 prompt 失去既有驗收基礎**，代價比改一個參數大得多。 🔍**2026-09-16 免費驗證補充（監督）：找到 `effort` 這條路為什麼從來沒被試過**。`main.py:234` 的註解寫著「OpenRouter 的統一參數 reasoning.max_tokens，**Anthropic 系走的就是這個（OpenAI 系走 effort，這裡不送）**」——**這句話是錯的**。實查 OpenRouter 的 `/models/anthropic/claude-sonnet-5/endpoints`，**五個端點（Anthropic／Claude Platform on AWS／Azure／Google／Amazon Bedrock）全部 `reasoning=True`**，Claude 系同樣吃 `effort`（本列 0915 的查證也寫過「Claude 系支援 low／medium／high／xhigh／max」，但那個結論沒有回頭修正這段註解）。👉**一個寫錯的註解，讓唯一可能有效的參數整整沒被嘗試過**。動工改 effort 時，**這段註解要一併改掉**，否則下一個人還會再被它擋一次。🧪**免費驗證已完成（子代理執行，監督驗收：產品程式碼零改動，全套 1963 題 OK／expected failures=1）**：**① 唯一真正的覆蓋缺口是 strict schema 的 payload**——原本只測「schema 常數長什麼樣」，**沒有任何測試釘住它真的被送進 `client.chat.completions.create()`**。已補 `tests/test_digest_strict_schema_wiring_20260916.py`（14 題）。**② 截斷降級只測了一半**：原本只釘「可解析但截斷」那條（`main.py:2861`），**「解析失敗＋length」那條完全不同的分支沒人測過**（`main.py:2792`），順帶發現 verbatim＋length→400 也沒測過，都已補進 `tests/test_generate_retry.py`。**③ 繁中品質閘門與 provider routing 的既有覆蓋已經足夠**，不用補。**④ 已建立改 effort 前的回歸基準**：`DefaultReasoningPayloadBaselineTests` **不 patch 環境變數**、用字面值釘住正式站現在實際送出的 `reasoning: {"max_tokens": 2000}`——之後改成 effort 時，差異會直接顯示在這條測試上。 🔬**2026-09-16 `reasoning.effort=low` 十次實測（使用者授權「測試LOW」）：**這是本列開列以來最明確的一次結果——**低 effort 幾乎解決了 D21 本身**。測法與 R6 逐項可比：同一篇 2,929 字稿、同一組 BASE 參數、同樣 standard 5＋maximum 5，**唯一變動的是送出去的 reasoning 欄位**（R6 送 `{"max_tokens": 2000}`、被模型忽略；這輪送 `{"effort": "low"}`）。**不改任何產品程式碼**，只在行程內替換 `main.digest_reasoning_body`，跑完還原，走的仍是 `digest_completion()` 全鏈路。 📊**四項全面改善**：**① 截斷 3/10 → 0/10**（`finish=length` 完全消失）；**② 耗時平均 131 秒 → 18 秒、最壞 201 秒 → 27 秒**（**快約 7 倍**，而且**最壞值 27 秒遠低於使用者訂的 120 秒上限**，也遠離 Cloud Run 的 300 秒死線）；**③ 成功率 5/10 → 7/10**；**④ 簡體字 2/10 → 0/10**（掃描集 207 字＝現行防呆 119＋B71 候選 88，**十次零命中**）。 🔑**`effort` 確實被遵守，而 `max_tokens` 確實沒有**：這輪的 `reasoning_tokens` 是 **0／0／0／95／200／206／265／296／842**，**有四次模型根本沒思考就直接寫出正確結果**；對照 R6 同樣十次是 **4005～11999，最低 4005**。👉**同一個模型、同一份 prompt，換一個參數名，思考量差了一到兩個數量級**——這是 `effort` 有效、`max_tokens` 無效最直接的證據。 ⚠**唯一的問題集中在 maximum（字超多）檔，而且它不是 low 造成的新問題**：3 次被品質閘門退，原因**全部都是同一條**——`variable [內文小標] 塊數不足（observed=0 required=7）`。**這正是 B57「字超多塌陷」**（帳本 B57 已記：根因＝`MAXIMUM_DENSITY_RULES` 只解除上限、沒有下限）。R6 那輪的 `maximum-2` 也是被同一條退的，只是 R6 的 maximum 有 3 次直接截斷、根本沒產出內容可判塊數，把這個問題蓋掉了。**低 effort 沒有製造新缺陷，它是把既有的 B57 從「被截斷掩蓋」變成「清楚可見」。** 📈**分檔看**：**standard（字多）5/5 全過、平均 17 秒、零簡體**——**這一檔的 low 已經可以直接上**；**maximum（字超多）2/5**，要**先做 B57 的後端塊數防呆**（帳本 B57 已寫「不撞 prompt 凍結快照、可立即上線、不需要新裁決就能開始設計」）才適合切換。 💡**監督建議（待裁）**：甲｜**改送 `reasoning.effort`，standard 用 low**，**maximum 暫時保留現況或用 medium，等 B57 的塊數防呆上線再一起降到 low**——這樣不會拿字超多檔的品質去換速度；乙｜全檔一律 low，速度最好看，但字超多的塊數不足會從偶發變成常見，**不建議**。⚠**動工時 `main.py:234` 那段誤稱「OpenAI 系走 effort」的註解要一併改掉**（理由見上）。 ⚖**關於換模型**：這輪結果**大幅降低了換模型的急迫性**——速度問題（2 分鐘）用一個參數就解決了，剩下的是 B57 的塊數問題，而**那是 prompt 規則缺下限造成的，換模型不會自動修好**。換模型的評估仍可做（見本列 Codex 段），但**不應該再被當成解速度的手段**。 📁明細 `D:\Downloads\AICG\後台紀錄\20260916\D21_effort_low_10次.jsonl`（**這輪有存模型回傳原文 `raw`，補上了 R6 沒存導致無法重播解析的缺陷**）。 🧪**樣本數限制要講清楚**：10 次。簡體字 2/10→0/10 這個差異**樣本太小、不足以證明「低 effort 會減少簡體字」**，只能說這輪沒有出現；若要驗證使用者提的「簡體是不是模型特性」，需要跨模型對照組。 🏁**2026-09-16 跨模型對照（使用者授權，各 10 次，實際花費約 US$0.22）**：**條件與 Sonnet 的 effort=low 那輪完全一致**——同一篇 2,929 字稿、同一組 BASE、同樣 standard 5＋maximum 5、同樣送 `reasoning.effort=low`，**唯一變動的是模型**。⚠**provider 特別處理**：改送 `require_parameters: true`（不送寫死的 Anthropic 白名單），否則 `gpt-5.6-luna` 可能被路由到**不支援 `structured_outputs` 的 Amazon Bedrock 端點**（與 **B74** 同一個坑），strict schema 被靜默丟棄、對照就髒了。**順帶實測結果：`require_parameters` 沒有造成任何「無 provider 可用」的失敗，20 次呼叫全部順利路由（Google／Google AI Studio／OpenAI），這替 B74 的乙案除掉了唯一的疑慮。** 📊**三方逐項比較**：**claude-sonnet-5 low**：成功 7/10｜截斷 0｜耗時 14–27 秒（平均 18）｜**maximum 檔 2/5**（3 次塊數 0/7）｜簡體 0/10。**google/gemini-3.8-flash low**：成功 **9/10**｜截斷 0｜耗時 **4–12 秒（平均 6）**｜**maximum 檔 5/5，每次都給滿 8 塊**｜簡體 0/10。**openai/gpt-5.6-luna low**：成功 6/10｜**截斷 4**｜耗時 8–107 秒（平均 46）｜maximum 檔 4/5｜簡體 0/10。 🥇**gemini-3.8-flash 是明顯的贏家**：成功率最高、速度最快（比 Sonnet low 再快 3 倍、比原本的 Sonnet 高思考快 20 倍以上）、**而且唯一一個把 maximum 檔穩定做對的**。 ⚠**但 luna 的失敗模式要特別記下來，因為它跟 Claude 完全不同、而且更難修**：luna 那 4 次截斷**不是思考吃光預算**——它的 `reasoning_tokens` 只有 112～220（很低），卻照樣 `completion=12000` 撞頂。看 raw 末尾就知道原因：**它在無限重複產出 `[內文小標]`，停不下來**（正文 token 高達 11,888，正常只要 800）。👉**Claude 的病是「想太久」，可以用 `effort` 控；luna 的病是「寫不停」，沒有對應的參數可以控**，只能靠 prompt 或硬切 `max_tokens`，代價是半截的 JSON。**選模時這一點比帳面價格重要得多。** ⚠**gemini 也有一個要盯的點**：唯一失敗那次是 `standard-2`，**`finish_reason=error` ＋ JSON 字串沒收尾**（不是 `length` 截斷），屬於回應被中途切斷，與 token 預算無關。10 次出現 1 次；**若這是穩定發生的 API 層行為，就是換模型的隱藏成本**，上線前要多跑幾輪確認。 ⚠**另一個沒被品質閘門攔下、但會影響上鏡的差異**：**gemini 的內文小標明顯比 Sonnet 長**（例「川普自稱騙局終結者 直斥<AI接管世界>與暖化同屬騙局」對比 Sonnet 的「川普稱AI接管世界是騙局」）。**現行品質閘門只驗塊數、不驗長度**，所以過了；**版面塞不塞得下要看成品圖才知道**，換模型前應該先生幾張圖看。 💡**監督建議（待裁）**：本輪把「換模型」從備案推上了主案，但**不建議直接換**，而是：①**先把 `reasoning.effort` 這條路做進程式**（它對三個模型都有效，且是換不換模型都要做的基礎）；②**再用 gemini-3.8-flash 生幾張實際成品圖**，確認小標長度不會撐爆版面；③確認 `finish=error` 是偶發還是常態；④**最後才談要不要換主模型**。⚠**換模型另有一項本輪沒測到的成本**：整份 prompt 是照 Claude 的行為調出來的，**本輪只驗了消化的 JSON 與塊數，沒有驗忠實度、人物、地圖與各版型規則**。 📁明細 `D21_對照_google_gemini-3-8-flash_10次.jsonl`／`D21_對照_openai_gpt-5-6-luna_10次.jsonl`（皆含 `raw`，可重播）。 🔁**2026-09-16 gemini-3.8-flash 第二輪：改用 10 則「不同」的真實稿（使用者授權）**。第一輪是同一篇跑 10 次（測同稿穩定性），再跑一次資訊量很低，所以這輪換成**泛化測試**。**稿件全部取自後台真實紀錄**（`ai_month09.json` 的 `records[].news_text`，21 則不重複的稿依長度均勻取 10 則），**211～2,330 字**，涵蓋氣象／國際／社會／財經／科技。檔位一律 `standard` 讓變因只有稿子（maximum 已在第一輪同稿測過 5/5；而且 211 字的短稿要寫滿 7 塊本來就強人所難，混進來只會製造不公平的失敗）。 📊**結果：成功 9/10、耗時 4–8 秒（平均 6）、簡體 0/10、塊數 5–6 全數過關**。🔑**最值得注意的是耗時對稿長完全不敏感**：211 字的稿 5.3 秒、2,330 字的稿 6.0 秒，**十則全部落在 4–8 秒**。對照 Sonnet 在原本設定下是稿越長越慢（最壞 201 秒），**這代表換模型之後，「字超多的長稿」不再是逾時風險來源**。 ⚠**`finish=error` 已可判定不是偶發：兩輪合計 20 次出現 2 次（10%）**。兩次的症狀一致——`JSONDecodeError: Unterminated string`，而且 **`finish_reason` 是 `error` 不是 `length`**、raw 只有 1,478 字元就斷在半句英文 style 描述，**是回應在傳輸中被切斷，不是模型寫不完**，與 token 預算無關。**嚴重性評估**：現行 `DIGEST_ATTEMPTS=5` 會重試，單次 10% 失敗在重試後可忽略；而且它是**快速失敗（4.4 秒）**，不像 Claude 的截斷要燒掉 100～200 秒才失敗。**但上線前要確認重試路徑真的涵蓋 `finish=error`**（現行的降級邏輯是針對 `length` 寫的，見 `main.py:2792`／`2861`），否則這 10% 會直接變成使用者看到的錯誤。 ⚠**監督自我更正：第一輪寫的「gemini 的內文小標明顯比 Sonnet 長」是錯的**，那是從單一例子的目視印象，沒有量過。**實際量完三輪的全部小標**：**Sonnet low 平均 23.8 字（50 條）、最長 186 字**；**gemini 同稿平均 24.7 字（64 條）、最長 32 字**；**gemini 十則平均 22.4 字（52 條）、最長 30 字**。👉**平均幾乎一樣，而且極端值反過來——失控的是 Sonnet（一條 186 字），gemini 的上限穩定壓在 30～32 字**。原本列為「換模型隱藏成本」的版面風險，**依這份量測不成立；真要擔心版面，該擔心的是 Sonnet 那條 186 字的小標**。（⚠但這仍然只是字數，**實際上鏡好不好看要生成品圖才算數**。） 📁明細 `D21_gemini38_十則不同稿.jsonl`（含 `raw`、每則的 `blocks` 與 `sub_max_chars`）。 ✅**2026-09-16 動刀完成（使用者裁定「這一波動刀插隊優先要改這個」，選定範圍＝effort ＋ 換 Gemini 主模型）**。 **新舊 payload 差異**：舊 `{"provider": {"order": ["anthropic","claude-on-aws","azure/global","amazon-bedrock/global"], "allow_fallbacks": true}, "reasoning": {"max_tokens": 2000}}` → 新 `{"provider": {"require_parameters": true, "allow_fallbacks": true}, "reasoning": {"effort": "low"}}`。 **改了什麼**：①`digest_reasoning_body()` 簽名改為 `(effort_override)`，新增環境變數 `DIGEST_REASONING_EFFORT`（預設 `low`，設空字串或 `off` ＝完全不送）與 `DIGEST_REASONING_RETRY_EFFORT = "low"`（截斷降級的底線，與預設值分離的理由是預設可被環境變數調高）；②`DIGEST_REASONING_MAX_TOKENS`／`_MIN_TOKENS`／`_HEADROOM` **三個常數整組刪除**（effort 只有三段、沒有數字可調，那套「留多少 token 給正文」的換算在新制下沒有對應物，不留死碼）；③`DIGEST_PROVIDER_ORDER` 白名單整組刪除，改送 `require_parameters`（**B74 同時解掉**）；④`DEFAULT_DIGEST_MODEL` 換 `google/gemini-3.8-flash`；⑤`main.py:234` 那段誤稱「OpenAI 系走 effort」的錯誤註解已改掉；⑥`generate()` 的重試迴圈**結構沒動**，只把降級對象從 token 數字換成 effort 字串。 🔁**回退路徑（已寫測試釘住）**：把環境變數 `DIGEST_MODEL` 設回 `anthropic/claude-sonnet-5` 即可，**不用改程式碼、不用重建映像檔**；另有 `BadRequestError` 時自動拿掉 `reasoning` 欄位重送一次的既有保護（模型不吃 effort 時不會整條線壞掉）。 🚨**部署檢查項（這條沒做，程式改了也等於沒改）**：`resolve_digest_model()` 的優先序是 **`DIGEST_MODEL` 環境變數 → `OPENAI_DIGEST_MODEL` → 程式預設**，**環境變數贏過程式預設**。本機 `.env` 已同步改成 gemini，但**正式站的環境變數不在這個 repo 裡**（部署走 `release` 分支、SSH 到 AI 部門自架主機）。👉**若正式站設著 `DIGEST_MODEL=anthropic/claude-sonnet-5`，上線後仍會跑 Claude**。部署時要一併確認的變數：`DIGEST_MODEL`（要是 gemini 或未設）、`OPENAI_DIGEST_MODEL`（同上）、`DIGEST_REASONING_EFFORT`（未設＝low）、`DIGEST_PROVIDER_REQUIRE_PARAMETERS`（未設＝開）、以及**舊的 `DIGEST_PROVIDER_ORDER` 若有設要移除**（常數已不存在，留著不會報錯但會造成誤解）。 ⚠**三項已知風險，都是明知而為，不是遺漏**：**①effort 會套到所有 `digest_completion()` 呼叫點**——斷句（`openai/gpt-5.4-mini`）、圖表分類、封面視覺／十點標題／YT 封面全部共用 `digest_reasoning_body()`。這不是本次新引入的行為（舊版 `max_tokens` 本來就無差別套用），已查證那些模型的端點都支援 reasoning、不會報錯，**但效果沒有實測**，屬搭便車的行為變化。刻意不加例外分支，避免這次改動失控。**②斷句模型沒有跟著換 gemini**：`resolve_title_break_model()` 原本的理由是「斷句要比主消化快」，**那個前提在主消化變成 4–8 秒之後已經不成立**；但 D21 四輪實測只測過主消化（長輸出、strict schema），**斷句這條短輸出路徑一次都沒量過**，換了就是拿正式站賭沒量過的組合。保守留著，待實測再決定。**③「官方文件說 max_tokens 對 Claude 不生效」這句仍是轉述 Codex 的查證，本專案沒有直接核對原文**，註解已改成如實區分「實測證據」與「未核實的轉述」。⚠**監督另修正了一句過度宣稱**：實作原本寫「reasoning_tokens 送 max_tokens 前後幾乎沒變化」，但**我們從來沒跑過「完全不送 max_tokens」的對照組**——能斷言的是「設了上限但沒擋住」（送 2000，實測 4005–11999，達上限六倍），不是「送與不送完全一樣」。已改。 ⛔**還沒驗的事（換模型的真正風險在這裡）**：四輪實測只涵蓋 **JSON 解析、塊數、簡體字、速度**。整份 prompt 是照 Claude 的行為調出來的，**忠實度、人物、地圖、各版型規則全部沒有用 Gemini 驗過**。**上正式站前建議先用 Gemini 生幾張實際成品圖**。 🧩**2026-09-16 補做斷句（使用者裁定「斷句模型也試試看 Gemini，要改就改到位」）**。D21 原本刻意沒動斷句，理由是「斷句這條短輸出路徑一次都沒量過」——**現在量了**。**方法**：19 則真實封面標題（取自後台 2026-09 紀錄的 `news` 欄，含十點雙切／YT 封面／整點直播／直標），**每則一次呼叫**（production 的 `apply_title_break_hints` 就是這個粒度；一次塞 17 段會把耗時與輸出長度灌水，而 8 秒逾時與 `finish=error` 都是每次呼叫的性質），兩模型各 78 次、共 108 段。 📊**結果**：`openai/gpt-5.4-mini` 呼叫層 0 次失敗，但**段被判不採用 29/108**，最終只有 **73%** 拿到模型斷句；`google/gemini-3.8-flash` 呼叫層 6 次失敗（4 次撞 8 秒逾時＋2 次 `finish=error`）但只有 5 段被判不採用，最終 **87%**；耗時中位 3.1 秒 → **2.2 秒**。⚖**代價要講清楚：硬失敗率從 0% 變 7.7%，而斷句這條線沒有任何重試迴圈**，失敗直接退回規則斷行。但 mini 的「回了卻被判不採用」對使用者是同一個結果，算總帳 gemini 淨勝 14 個百分點，故換。**回退**：`TITLE_BREAK_MODEL=openai/gpt-5.4-mini`，與主消化的 `DIGEST_MODEL` **各自獨立**，一條出事不會拖走另一條（已寫測試釘住）。明細 `F_斷句模型對照*.jsonl`。 ⚠**監督自曝**：第一次對照跑完 78 次才發現 `_ModelPatch` 定義了卻沒在迴圈裡套用，**兩組全打到 mini，那張差異表整張作廢**，重跑後結論才成立（作廢資料留存為 `F_斷句_mini基準78次_對照作廢.jsonl`）。現在每次呼叫都硬驗 log 裡的 model 才收資料。 🔎**2026-09-16 Codex SOL 獨立複查（使用者指定「再看一次有沒有遺漏」），9 項「該修」已處理 8 項**：①`resolve_title_break_model()` 缺 `resolve_digest_model()` 那道「帶 `/` 的 slug 只在真的走 openrouter 時才採用」防呆——**而回退指引推薦的 `openai/gpt-5.4-mini` 正是一個帶 `/` 的 slug**，在 native／Gemini 後端會被送進 api.openai.com、每張封面靜默退回規則斷行。已補。②`DIGEST_REASONING_EFFORT` 沒有白名單，環境變數拼錯會原樣送上游，而降級保護只在錯誤訊息含 `reasoning` 時才拔欄位。已加白名單，不認得退回 `low` 並印一行。③④兩處註解仍在描述已刪除的 `DIGEST_REASONING_HEADROOM` 與 max_tokens 換算（`main.py` 消化預算段、十點標題預算段），已改。⑤新加的斷句預設值測試寫成「`if` 後端是 openrouter 才斷言」，在 native 環境會整題靜默跳過，已改成三個後端各自斷言。⑥三個被刪的 token 常數沒有像 `DIGEST_PROVIDER_ORDER` 那樣反向斷言不存在，已補。⑦`README.md` 仍寫「OpenAI Responses API／預設 gpt-5.6-terra」，⑧`docs/HANDOFF.md` 同樣過期，兩份都已改寫成現況。**未採納 1 項**：Codex 認為 `DIGEST_PROVIDER_REQUIRE_PARAMETERS` 這個可關閉開關「需求沒提出要這條退路」、等於可以關掉 B74 的保證。監督判斷保留——那是路由旋鈕，真的遇到「無 provider 可用」時需要能當場關掉，而 B74 的實質保證寫在預設值（預設為開）與測試裡。另 `docs/yt-live-cover-spec.md` 的消化速度段已加 2026-09-16 更正框。 🗳**兩件連帶待裁**：**①`TITLE_BREAK_TIMEOUT_SECONDS = 8.0`**——gemini 的 4 次逾時全部卡死在這道牆，而中位數才 2.2 秒、p90 5.5 秒，放寬到 10 秒應能回收大部分；另**實測發現這個 8 秒不是總時長上限**（mini 有兩次跑了 8.29／8.96 秒仍順利回來，作廢那批甚至有一次 33.1 秒 `finish=stop`），它是 httpx 的分段逾時。**②`finish=error` 的重試**：斷句 2/78、主消化兩輪 2/20，症狀一致（`completion_tokens=0`、傳輸被切）；主消化有 `DIGEST_ATTEMPTS=5` 吸收，**斷句一次重試都沒有**。 🚨**部署檢查項追加一條（2026-09-16 斷句換 Gemini 之後）**：**`TITLE_BREAK_MODEL`**。這條的陷阱比 `DIGEST_MODEL` 更隱蔽——2026-09-14 曾裁定斷句走 `openai/gpt-5.4-mini`，**若當時有人把它寫進正式站環境變數，今天這次換 Gemini 就是白改**：正式站走 openrouter，新加的「帶 `/` 的 slug 只在走 openrouter 時才採用」防呆會**照樣採用**那個覆寫值。而且斷句失敗是被 `except` 吃掉、只印一行 log 就退回規則斷行，**不會有任何人發現斷句根本沒換模型**。👉部署時要確認 `TITLE_BREAK_MODEL` 未設（＝吃程式預設的 gemini），或明確設成 `google/gemini-3.8-flash`。||
| D1 | **創意階梯 seed 政策** | 創意階梯三條線對齊清點 G2 | ✅**2026-09-14 已裁** | **裁決（2026-09-15 補充，使用者）：seed 只存後台，不印在圖上。** 查證圖面上沒有現成的通用籤位機制（`compose.py` 只有寫死用途的「示意圖」浮水印），另做一套壓字機制要處理字型／位置／與安全框和蓋章的互動，且會在上鏡畫面多一個雜訊。D1 的目的是「撈得回來」，而使用者實際撈東西的地方就是後台紀錄，所以 seed 寫進 **API response ＋ `request_log` ＋ `audit_archive` metadata** 即達成目的。**seed 絕不進 prompt**——進了就改變生圖輸入，同一個 seed 再也複現不出原圖，功能自我否定。（以下為原始裁決）**裁決：請求帶明確 `seed` 欄位，前端「重新生成」時遞增**——既可重現又會變，並把 seed 印在成品籤上，使用者回報「這組好」時撈得回來。三條線（CG／十點／YT）統一走這條：十點的 `seed=None` 每次隨機、YT 的 `標題\|日期` 重生同貌都要改掉。**A1／A2／A3／A5／B5 封鎖解除** |
| D2 | CG 線要不要套「文字禁綠」 | 創意階梯三條線對齊清點 G5 | ✅**2026-09-14 已裁** | **裁決：文字／裝飾禁「螢光綠」（chroma key 綠），不是禁所有綠色**——會與綠屏打到穿幫的是螢光綠，深綠／墨綠／橄欖綠等不受限。漲跌數據的綠**保留**（台灣慣例不動，F16 不衝突了）。範圍：CG 線比照封面線。**連帶要修**：封面線既有的 `COVER_NO_GREEN_RULE` 與變化池的「無綠」定義也要一併收窄成螢光綠，現在訂太寬。 ➕**2026-09-20 使用者加開一個例外（見 F45）**：使用者**主動選「綠底去背」模式**時，**整張背景允許螢光綠**——那是刻意的 key 色，不是穿幫。本裁決的禁令在該模式下只約束**畫面主體**，而且要比平常更嚴（背景要被摳掉，主體有一點螢光綠就會跟著破洞）。兩處措辭在 F45 動工時要對齊。 ｜ 原現況：封面線有池內無綠＋`COVER_NO_GREEN_RULE`；CG 線沒有，且有反向規則「下跌＝green」（台灣漲跌配色）。**先查播出鏡面／記者版成品會不會疊綠屏**，會的話兩條規則正面衝突 |
| D3 | 字多／字超多的「天花板非配額」要不要鬆綁 | 字多／字少的檔位差距不夠明顯 | ✅**2026-09-14 已裁** | **裁決：鬆綁，但只允許「拆原有內容」**——字超多檔可以把一點拆成兩點，**不得新增原文沒有的事實**。實作要點：條文必須把「拆」與「補」的界線寫到模型分得出來（這正是 B21 那批忠實度問題的高風險區），驗收要比對成品與原文的事實集合是否一致。 ｜ 原現況：兩檔都明文「素材只夠三點就寫三點」，同一篇素材必然塌成同一結果。鬆綁＝拿掉擋模型自己補料的防線，要先想清楚防線怎麼補 |
| D4 | 生圖模式下附圖位置要不要讓使用者指定左右（**全線**，不只 YT） | YT 封面生圖模式…R1／自動判斷版型不穩 Y2 | ✅**2026-09-14 已裁** | **裁決：prompt 明寫哪個元素放哪側**。保留生圖風格，成本低。**但措辭推不推得動模型要實測**（repo 已有多次「許可句推不動模型」的紀錄），實測不過再回頭議 overlay。與 D9 的「預設＝上傳順序」同一條資料流。**2026-09-14 Stage 1**：`USER_REFERENCE_YT_SLOT_PLACEMENT_TEMPLATE` 已上 exp（兩格都有圖才注入；單位測試釘 LEFT／RIGHT 與編號陷阱）。**付費生圖實測未跑**（N×$0.04），措辭若推不動只回報、不升級 overlay。 ｜ 原現況：三方向：(a) 前台明講「位置由 AI 決定」不保證 (b) prompt 明寫哪個元素放哪側（措辭推不推得動要實測） (c) 有附圖一律導向 overlay。**2026-09-14 擴大範圍**：葉怡君在 `/api/generate` 自動判斷線也踩到，兩張文件照每次重生／refine 都左右互換——不是 YT 專屬。文件類附圖左右有語意，互換＝資訊錯置。**另需問王結玲她放的是哪個上傳位**（後台不存附圖，推不出來） |
| D5 | CG 線的「幅度」要不要訂數字表 | 創意階梯三條線對齊清點 G3 | ✅**2026-09-14 已裁** | **裁決：釘在「件數／區塊數」而非畫布百分比**——配件件數、強調元素數、顏色數、背景層數這類數得出來的量。CG 沒有固定版位，不訂百分比。（註：B22「裝飾佔七成畫布」因此不靠 D5 解，要另循 D9 的資訊載體優先條款。） ｜ 原現況：十點／YT 用具體百分比與倍數，CG 全是形容詞。但 CG 畫面沒有固定版位，數字要釘在什麼上面要先想清楚 |
| D6 | 換版型時標題欄要不要也清空 | 版型功能對齊盤點 | ✅**2026-09-14 已裁** | **裁決：不清，維持現狀。** 標題是使用者自己打的內容，換版型常常就是「同一則新聞換個版型再試」，清掉等於叫他重打。附圖位會清是因為不同版型的位子語意不同，標題沒有這個問題。 ｜ 原現況：附圖位已清（`9e747a7`），標題欄刻意沒清，待裁 |
| D7 | `live24_bg=full` 兩格都放原圖的正確行為 | 版型功能對齊盤點 | ✅**2026-09-14 已裁** | **裁決：維持現行，自動切格。** 使用者放了兩張就是兩張都想用。**只要把文件寫對即可，不改程式。**（查證：`main.py` 已寫 `live24_bg != FULL and 兩格都有圖` 才雙切；選 full 時前端藏右格、後端把攤平多張原圖自動切格。）**2026-09-14 Stage 1**：已改 `docs/plan-20260913-live24版型.md` 與 `docs/yt-live-cover-spec.md`，**未改 `app.js`**。待驗。 ｜ 原現況：會併成單則切兩半，文件說 full 只鋪左格——文件錯還是程式漏，已 park |
| D8 | 畫風池細節 | 畫風池 | ✅**2026-09-14 已裁** | **裁決：先搖得動再說，本波不做。** 第二刀的變化池（版形／字體／配色／落點）先上線，看實際能拉開多少。畫風是最大的一個變數，前面的還沒搖動前先動它容易整張圖跑掉。 ｜ 原現況：候選畫風清單、抽法、等級門檻、成品是否標畫風名，全待裁 |
| D9 | 一般 CG 線原圖放置的順序／位置／與內文搭配 | 一般 CG 線的原圖放置 C1–C4 | ✅**2026-09-14 已裁** | **裁決：綁到內文標記**（與 D12 同一套語法）。**追加裁決：使用者沒指定時，預設＝上傳圖片的順序。** ⚠**這條有實作後果**：CG 線現在 `shared_refs=True`，上傳順序在後端就被攤平、從來沒進過 prompt——要先讓順序真的傳得到生圖端（多帶一個序號欄位），規則才有東西可依。 ｜ 原現況：**2026-09-14 已查證**（使用者提問「附多張圖要怎麼告訴 AI 放置」）：位置只寫「自成一塊」由模型自選、**順序完全沒有這個概念**（CG 線 `slots=False`／`shared_refs=True`，上傳順序沒進 prompt）、與內文的對應由消化模型自己決定。`asis_max=0` 是「規則不適用」不是禁止。四條路待裁：最小修補／沿用封面線插槽／綁到內文標記（與 D12 一起裁）／改走 hybrid |
| D10 | hybrid 一鍵成圖：補齊還是明說它是簡化版 | 版型功能對齊盤點 缺口 5／混合版型模式 | ✅**2026-09-14 已裁** | **裁決：hybrid 冷凍中，短期不會更新，先不處理。** 不補齊、也不急著在前台標示簡化版。 ｜ 原現況：附圖、創意階梯、品質閘全沒接 |
| D11 | Cloud Run 是否恢復部署 | （2026-09-14 裁決暫停維護） | ✅**2026-09-14 已裁** | **裁決：維持暫停，之後再議。** 不訂恢復時間點。 ｜ 原現況：目前暫停，正式站停在部署前的版本；近幾批改動線上看不到 |

| D12 | 行內指令的語法與「綁定到哪一句」的機制 | 原文內嵌指令要規則化 Y7／Y8 | ✅**2026-09-14 已裁** | **裁決：沿用圓括號＋口語詞**，正式承認使用者已在用的寫法，學習成本零。待辦：訂一份可辨識的指令詞表，並處理與內容本身圓括號（原文補充說明）的區分。 ｜ 原現況：**使用者 2026-09-14 已裁示要放寬、有真實需求**，但語法未定：`(ICON)`／`(蓋字章)` 這類版面元素標記要正式對應到既有 `[標題]`／`<蓋章>` marker 體系，還是另立行內語法？**先不要自己選一套**——使用者已在用的「圓括號＋口語詞」是最低學習成本的候選 |
| D13 | 刑案／未公開身分當事人是否強制剪影去識別 | 追加修改連打語意漂移 Z5 | ✅**2026-09-14 已裁** | **裁決：可以畫人、可以掛名——這本來就是示意圖，且已標示「AI示意圖」。不強制去識別。** ⚠**但前提要補**：`apply_user_references_to_image_request` 有一條 2026-08-17 裁決——**使用者一附圖，示意圖標籤就會被拿掉**（僅肖像參考照與 AI改圖 例外保留）。所以「畫真人＋掛真名＋沒有標籤」這個組合目前做得出來。**規則要補的是：畫真人並掛真實姓名時，標籤不得被附圖規則洗掉**（登記為 B28）。 ｜ 原現況：葉怡君 11:47／11:51 把卡拉OK衝突當事人畫成寫實人物＋真實姓氏籤，觀眾可能誤認為真人照片。與既有「真實人物圖須參考真實樣貌」方向相反——這一類應該畫得**更不像**才對 |
| D14 | 「完全不要文字」檔位的形式 | 新增「完全不要文字」檔位 W1／W2 | ✅**2026-09-14 已裁** | **裁決：拉桿改六段，無字放最左端**——順序為 無字→字極少→字少→字多→字超多→不改字，把兩個極端推到拉桿兩頭，避免選錯。 ｜ 原現況：使用者已裁示要**融入字多字少 BAR 當固定格式**；待裁細節：拉桿改六段還是另設開關（「不改字」與「不要字」兩個極端擠同一側會不會選錯）、標題要不要一起不要。功能已證實做得到（戴元利 17:26／17:27 純畫面零文字） |
| D15 | 不宜播出視覺內容的清單範圍 | 不宜播出的視覺內容要有避開清單 | ✅**2026-09-14 已裁** | **裁決：除已點名的（遺體、棺材、屍袋、排泄物、尿液）外，四類全部納入**——(1) 傷口／手術／大量血跡特寫 (2) 自殘／自殺手法描繪 (3) 動物虐待／動物遺體 (4) 槍口對人／吸毒器具／遺照。實作見 F21（**不能只加負面清單**，要給正面替代法）。 ｜ 原現況：使用者已點名：遺體、棺材、屍袋、排泄物、尿液。**待確認是否納入**：手術／傷口特寫、自殘、動物虐待、槍口對人、吸毒器具、遺照——電視新聞各有處理慣例，不替使用者決定。規則放哪一層（消化／生圖／正面替代法）也要裁 |
| D16 | 大標題各檔的字數與行數 | 大標題字數要納入字多／字少 H1／H2 | ✅**2026-09-14 已裁** | **裁決（2026-09-14 二次修訂，使用者更正）：字數分級照舊，行數只鬆綁「字極少」一檔。**使用者：「編輯版的標題真的要兩行，預設的字少也要兩行，只有『字最少』可以允許一行，但不強制。」無字＝無標題｜**字極少＝允許單行但不強制**｜**字少／字多／字超多＝維持兩行**｜不改字不動。行數這半**只碰編輯版樣板**（`main.py:957`「標題強制拆兩行」是編輯版硬規則，記者版沒有）。**字數分級完全不受影響**——`simplified` 仍要新增標題字數上限，CP1 的範圍與重凍必要性不變。細節見 `docs/plan-20260914-第一波三刀.md` 文末〈D16 二次修訂〉。**級距已訂（2026-09-14，0-2 量測完成後）：10／13／18／22**（字極少／字少／字多／字超多，**去空白且去 `<>` 標記**的字元數，編輯版兩行合計共用）。不改字＝不限。依據與樣本量誠實揭露見 `docs/0-2-標題字數量測結果-20260914.md`。⚠ 量測走 `gpt-5.5`（非正式站的 `claude-sonnet-5`），**方向可靠、數字待驗**，驗收輪要在正式站再看一次。**相對護欄：使用者 2026-09-14 裁決「不要加」。** 量測發現的標題／內文比例失衡（字極少達 1.30、字多只有 0.14，見結果文件〈結論二〉）**只用絕對字數上限處理**，不另外加「標題不得長於內文」的相對條款。此案結案，不再提。⚠**實作邊界（2026-09-14 監督查證，0-2 校準時發現）：「維持兩行」不適用於播出鏡面。**`editor_formats.py` 的 `_BROADCAST_STAMP_ON`／`_OFF` 第 6 條寫死「variable must be exactly one [標題] line」，**位階在編輯版樣板之後、蓋掉 `main.py:957` 的兩行**。0-2 校準第二格（long／maximum／編輯＝broadcast）實測就是單行，**這是正確行為、不是腳本壞掉**。所以 D16 動的是編輯版樣板那一條，**不得去改 broadcast 的第 6 條**；24 格裡屬 broadcast 的 4 格（long × 四檔 × 編輯）對「行數」不提供證據，只提供**標題字數**證據，照跑不砍。（以下為前一版，行數部分已被取代）**裁決（2026-09-14 修訂）：字數各檔分級，且行數跟著動。** 使用者：「如果標題字已經很少了，本來就不需要兩行。」無字＝無標題｜**字極少／字少允許單行**｜字多／字超多維持兩行｜不改字不動。**數字仍要先量現行成品再訂**（實作步驟，不是裁決項）。⚠**更正一項先前的錯誤估算**：原裁決寫「行數不動」的主因是我說「行數一變會牽動封面塊高表 18/24/30/36% 與 `yt_title_top`，是跨線改動」——**那是錯的**。`build_digest_instructions` 全專案只有一個呼叫點（main.py:2058，`/api/generate` CG 線），**封面線根本不跑消化**，十點／YT 的標題是使用者自己打進 `title_left`（`max_length=40`）。消化產出的 `[標題]` 只影響 CG 線與播出鏡面。另：播出鏡面規則本來就寫「exactly one `[標題]` line」（`editor_formats.py:109`），所謂兩行是樣板在那一行裡塞 `\n` 的視覺指示，**不是程式依賴的結構**，放寬成單行不會打壞解析。 ｜ 原現況：**使用者已裁示要納入**，待裁數字：各檔標題字數（**訂之前先量現行成品**，別直接沿用內文級距——標題是最大的字）、行數要不要跟著動（編輯版現在強制兩行；一變就牽動封面塊高表與 `yt_title_top`，是跨線改動） |
| D22 | **編輯版生圖端要不要看到消化前的新聞原文（不只拿到消化後的 prompt）** | 2026-09-17 使用者提問 | ✅**已裁（2026-09-21）** | 監督查證（`main.py:518-526`）：消化與生圖是**兩次獨立請求**（`app.js:1145-1146` 分打 `AI_BACKEND_URL`／`IMAGE_BACKEND_URL`），生圖端 `news_text` 原本寫死 `""`，後來只為稽核用 `_remember_digest`/`_recall_digest`（user_id＋時間窗）事後回補原文存檔，**不是餵給生圖模型當輸入**；生圖模型實際收到的仍只有消化階段產出的英文 style／structure／variable 結構化 prompt。程式註解已評估過嚴格關聯（讓生圖真的吃到原文）要改 `app.js` 呼叫參數＋後端 request model，「動到一萬多行前端主檔，風險高於效益」，故意沒做。**待裁**：要不要打破這個既有取捨，讓生圖端額外收到消化前原文以提升精確度？若要做，範圍與怎麼傳（不動一萬多行前端主檔的替代方案）要先想清楚。⚠**與此無關的另一點已確認**：不論生圖端看不看得到原文，**標題文字本身在消化階段就定案，生圖階段不得改字**（忠實度規則，`news_prompt.py`／`main.py:1824`附近），這條不受此裁決影響。　**2026-09-21 使用者裁決（7 題全裁，計畫見 `docs/plan-d22-news-text-to-image.md` 第 7 節）**：①只餵消化同次產出的 `visual_context`（畫面相關描述），不送完整原文、不做 raw A/B arm；②注入硬上限 4,000 characters，transport 仍收 20,000；③截斷採 deterministic 前 3,000＋後 1,000 並加 truncation marker；④**全部版型都開**（一般CG／播出鏡面／直標／十點／四種 YT 封面），僅 AI 標題層例外；⑤驗收採 A/B 比對組，視覺準確度須提升**且**多畫文字／數字／Logo 的違規率不得變差，兩條件都過才正式開；⑥稿件配對由**前端明送**，否決 user_id＋TTL 猜測（sol 查證：多分頁／併發／重部署都會拿到別篇稿且不會報錯）；⑦**AI 標題層硬排除**——B55 守門只驗 alpha／面積／保護區，沒有 OCR，擋不住模型多畫未定案的人名或數字。排程：不併入 B70／F43 的 PR2／PR5，另開獨立 `D22-PR1`，排在 PR5 之後、不必等 PR6。開工前先查清封面既有 `news_text` 用途是否與 `visual_context` 重疊。 |
| D24 | **編輯對位框（安全框 OFF）的成品要不要改成整張輸出，而不是 1748×924 的對位框本身？** | 2026-09-22 使用者回報「編輯CG 無安全框 成品變成 1748/924 不是我們講好的」時展開 | ✅**已裁（2026-09-22）並已落地（VERSION 260922-05）** | 現況是 **2026-08-19 使用者裁決**：編輯版兩檔都是滿版生成＋後製，安全框 OFF ＝ 水平拉伸填滿對位框，「輸出就是拉伸後的安全區本身，沒有畫布、沒有四周留白」（`safe_frame.apply_safe_frame` docstring，`resolve_frame_plan` 亦然）。所以 1748×924＝`safe_area_spec.PROFILES["編輯"]` 的 `(90,70,1748,924)`，前端 `app.js:1794` 也照這樣提示。字多／字超多開了 F38 之後同一條路會變成 **2331×1232**（2560×1440 畫布上的同一塊對位框），仍然不是 1920×1080。**待裁三選一**：①維持現狀（對位框就是交付物，後製自己貼回 1920×1080）；②改成輸出完整畫布、對位框只當內容區（等於推翻 08-19，四周會回來那圈襯底——**那正是 2026-08-17 編輯反映「漸層色帶很難後製」的東西**）；③另加一個開關讓使用者自己選。⚠選②之前要先確認後製端現在到底吃哪一種。 | **2026-09-22 使用者裁決原話**：「D24 記者/編輯CG 字多/字超多 安全框OFF時 生成 16:9 2K 無任何色框」。→ 採**選項②的反面**：不是「輸出完整畫布」，是**完全不後製**。`resolve_frame_plan("編輯", False)` 改回 `needs_frame=False`（`full_bleed` 仍為 True，交付的就是模型整張畫面）。記者 OFF 本來就符合，不用動。交付尺寸＝生成尺寸：字多／字超多＝2560×1440，其餘＝該 provider 的基本尺寸（gpt 16:9＝1280×720；gemini 看 1K/2K 切換）。⚠**2026-09-22 當日修正**：第一版實作對**所有檔位**一視同仁，使用者隨即指正「**D24 只在字多 字超多生效**」。現行命中條件＝`density in HIGH_RES_EDITOR_DENSITIES`（借 F38 那個 frozenset，因為裁決原話把「字多／字超多」與「2K」綁在同一句，兩邊各寫一份遲早分岔）；其餘檔位仍走 2026-08-19 的對位框（交付 1748×924）。`resolve_frame_plan` 多收一個 `density` 參數，四個呼叫端（消化 full_bleed／生圖／追加修改／標籤重貼）全部接上，漏傳者落到改動前的行為。連帶推翻 2026-08-19 的「編輯版兩檔都是滿版生成＋後製」，六條釘舊行為的測試已改寫。
| D25 | **要不要把 B55 的「透明底標題圖層＋程式逐像素疊圖」整套移植到編輯CG 的原圖放置？** | 2026-09-22 使用者回報「編輯CG 的原圖放置功能 好像壞了」（B85）展開 | ⏸ **2026-09-22 使用者裁決後又收回：「D25 先維持原本 PROMPT 要重新考量」** | 事實：編輯CG 的 `asis` 從上線（2026-08-23）起就**只有 prompt 層要求**，`main.py:999` 白紙黑字「不保證像素級一致」，沒有任何閘門或程式疊圖；B55 的四刀只做在十點不一樣與 YT 封面。而 B55 已**實測證明 prompt 擋不住**（創意 0 級 change_ratio 68.1%），所以編輯CG 的原圖放置在「照片必須原封不動」這個期待下是結構性不成立，不是偶發。**待裁**：①移植 B55 整套（`transparent_background` 只有 `provider=="gpt"` 支援，gemini 那條仍得走差異遮罩；工作量接近 B55 整條線）；②只把照片交給程式疊、標題一律程式壓字（最省，但失去 AI 標題造型）；③維持現狀並在前台明講「原圖放置是 prompt 層要求、模型可能重畫」，把期待調對。⚠決定之前**不要動手**——這是 D- 項。 | **裁決歷程（同日）**：先裁「移植整套　記者/編輯CG都要套用」，開工前確認閘門失敗的處置時使用者反問「prompt 不能要求原圖放置嗎? 之前在記者CG編輯CG都有成功過」，隨後指示**維持現行 prompt 路徑、整題重新考量**，故未動任何程式。⚠**重新考量時要帶著這兩個事實**：① prompt 路徑確實常常成功，使用者親眼看過；② 但它**失敗時是靜默的**——`main.py:999` 寫明不保證像素級一致，B55 在封面實測創意 0 級 `change_ratio` 就有 68.1%，成品上看不出這張的照片有沒有被動過。差別是「保證 vs 機率」，不是「能用 vs 不能用」。③ 移植的技術路徑已查清可行：核心 `compose._overlay_title_layer_core` 可直接複用（`protect_boxes=[]`），底圖用既有的 `compose._cover_panel`（COVER 等比裁切不變形），範圍比照 B55 只做單張 asis＋`provider=="gpt"`（gemini 沒有 `background=transparent`，且一般 CG 沒有「字帶」可當差異遮罩的邊界，那條路不存在）。④ **唯一沒解的是閘門失敗要怎麼辦**：B55 裁的「退回程式壓字」在一般 CG 不存在（這條路沒有程式壓字），三個候選是 自動重生一次走舊 prompt 路（多付一次錢）／回 400 明講／交出未加標題的原圖。⑤ 2 張以上 asis 已裁**先只做單張**，與 B55 一致。⑥ **2026-09-22 新增事實（詳見 B85 列的追查）**：創意 ≥3 時消化端會被 `_CG_L3_EXTRA`／`_CG_L4_EXTRA`／`_CG_DESIGN_DRAW_TEMPLATE` **命令**寫出 cutout／斜切相交／整張硬性配色的版面語，正式站 10:47:32 的成品 prompt 逐字證實，與 ASIS 規則的「不准裁切旋轉變形」正面矛盾；而消化端護欄只封 depiction／illustration 類措辭，沒封 cut out／crop／tilt／recolour 這幾個動詞。→ **候選④（新）**：不移植 B55，只在 `USER_REFERENCE_ASIS_DIGEST_RULES` 補上「那一格不得 cut out／crop／tilt／recolour，斜切與配色令一律繞過它」。成本最低、可單獨先做也可與其他候選併行；它仍只是 prompt 層（B55 的 68.1% 反證照樣適用），但能把「創意 3–4 時的必然衝突」降回一般機率。**2026-09-22 使用者裁「先做候選4」，已上線（260922-12），見 B85 列。①②③ 仍待裁——候選④上線不等於 D25 結案。**
| D26 | **安全框改成「模型畫延伸背景」取代程式硬壓單底色？** | 使用者 2026-09-26 看 B106 實拍 T1／T2 | ✅ **驗收通過（2026-09-26 使用者：「驗收成功了 可以上線更新PR 但此功能按鈕預設關閉」）；同日改裁：守門不過不退回置框**——使用者原話「退回置框有問題 不要退回置框 還是生圖給使用者 但訊息跳出 警告:超出安全框 讓使用者自行決定要用還是重新生成」，260926-04 起一律交付模型的圖、不合格只發警告通知：8 張驗收（`G:\Claude共用\AICG60926_D26驗收\`）場景稿四檔全走延伸、資料稿無字延伸、字極少／字超多退回置框。⚠**觀察到守門假陽性**：資料_字極少 的紅框是左下角樹叢被 OCR det 當成文字（內容本身在框內）→ 安全但少拿到延伸效果，後續可加 rec 信心度或最小框尺寸過濾；另該張出現「增開2200班次」（120＋80＋2000 被消化端加總）屬內容忠實度問題，另案。exp `57ed09f`，260926-03：新檔 `safe_content_gate.py`、`model_extension_active()`、`finalize_model_extension()`；網頁勾選框 `p1-chkExtension`；refine／restamp 同步；4 張真圖回放測試。⚠未覆蓋：消化端 `REPORTER_LAYOUT_SAFE_AREA` 仍寫「empty margin」與生圖覆蓋「不得留白」矛盾，若實拍多張四周平塗再加延伸專用消化版面；Docker 新依賴要到測試站首跑才驗得到（失敗訊號＝通知「文字偵測失敗：ImportError」）。✅ **已裁（2026-09-26）：照 SOL 建議開工第一階段，只做記者 CG＋安全框 ON；網頁上加勾選框；守門失敗同圖 FIT 置框、不重生；加 RapidOCR 依賴；驗收 8 張。編輯版不做**（使用者：「只要處理記者版 編輯版不需要處理」）| 使用者原話：「你成功讓生圖模型畫出四周延伸背景 這就是安全框的最佳解法 比硬壓單底色還棒」。**成因**：`generate_news_image` 預設 `safe_frame=False`，記者版 `resolve_frame_plan` 回 (False, False)——不後製；但記者生圖 prompt 的 CANVAS 本來就寫「single continuous full-frame background」＋「Scale the whole design down so it fills only the central region, surrounded by a thick empty margin on every side (deeper at the bottom)」，模型於是自己把背景畫到四邊、內容縮在中央。網頁版記者安全框 ON 則是 21:9 生成後程式縮進 16:9 `BASE_CANVAS` 補單色。**代價**：留白大小由模型決定，程式不保證文字落在 TVBS 安全框內（T1／T2 目測文字在框內，T1 頂端雲略出上緣）。候選：模型延伸背景＋程式量測守門（出框才退回硬壓或重生）。編輯版現行一律滿版版面（`resolve_frame_plan` 第一值恆 True），`EDITOR_LAYOUT_SAFE_AREA` 目前沒有路徑會用到。**2026-09-26 追加實拍字多兩張**（`G:\Claude共用\AICG60926_D26延伸背景\`，腳本 `0913測試用/shoot_round11_d26.py`）：①記者現行路徑——內容在 TVBS 安全框內（換算 1920：卡片 x≈300–1622、y≈178–850），但四周是**純深藍平塗**，沒有延伸插畫背景→「延伸背景」不是穩定行為，模型可能畫成單色；②編輯實驗（patch `resolve_frame_plan` 回 (False, False)）**不是有效測試**：消化 STRUCTURE 換成了中央版面，但生圖 CANVAS 仍是「Use the whole frame」＋標題「very top of the frame」，兩段矛盾，成品滿版、標題頂端與底部橫幅超出安全框上下緣。要驗編輯版得連 CANVAS／標題位置一起換。**使用者看圖判定（2026-09-26）：記者版成功、編輯版失敗。** SOL 可行性評估：`docs/回覆-20260926-SOL評估D26延伸背景.md`（建議 MVP＝記者 CG＋安全框 ON 實驗旗標、16:9 生成、專用 prompt override 不動 CP1、RapidOCR 本機文字框＋OpenCV 守門、失敗同圖 FIT 置框不重生；首輪 8 張；編輯 ON 第二階段、播出鏡面與封面排除）|
## B — 缺陷

| ID | 項目 | 來源 | 狀態 | 一句話現況 |
|---|---|---|---|---|
| B1 | 已合成的成品再當附圖餵回去 → 日期重複 | YT 封面生圖模式…R2 | 🔶 **已修待驗（exp，2026-09-14）** | **生圖端 prompt** 已在 asis 單張／多張與 aiedit 單張／融合加上 chrome 條款（日期章／LIVE／24H LIVE／台標／示意圖已在附圖裡就不要再畫一份）。⚠ **overlay 合成未動**：`compose_yt_live24_cover` 不論 `draw_title` 都程式貼角標＋日期——那是 Stage 1 範圍外的剩餘。證據仍是王結玲 16:13:08 |

| B9 | 自動判斷版型在同一段文字上不穩 | 自動判斷版型不穩 Y1 | ⬜ | 2026-09-14 葉怡君：同一段文字三次生圖挑了資料圖表／情境示意圖／3D示意流程三種版型，每按一次換一整套版面。**先量測**（同文字跑 N 次看分佈）再決定要不要釘死或給鎖定鈕 |
| B13 | refine 移除元素時用不相關的東西補位 | 追加修改連打語意漂移 Z1 | ⬜ | 「集體火化」那格的焚化爐被換成**一疊紙箱**，語意完全掉且後續 refine 救不回來。要補：替代物必須仍承載該格文字語意，否則寧可留空 |
| B14 | 圖像層的內容忠實度沒守住（事實漂移／角色顛倒） | 追加修改連打語意漂移 Z2／Z3 | ⬜ | 「路死動物」畫成端坐的**活貓**；「黃男攻擊范男」被標成「黃男**遭**攻擊」。這種錯在播出畫面上看起來完全正常，不逐句對抓不到 |
| B16 | **完全沒有「不宜播出」的視覺規則** | 不宜播出的視覺內容要有避開清單 | ⬜ | grep 三個檔案，遺體／血腥／gore／corpse／不宜**零命中**。只要新聞文字提到就照畫——09:56 那張焚化爐**爐內有動物遺體**。**B13 是它的下游**：這條做對了，那次 refine 根本不會發生 |
| B17 | 消化階段施動者／受動者會對調 | 消化階段的主被動與順序方向 | ⬜ | 「黃男攻擊范男」被標成「黃男**遭**攻擊」。`CONTENT_FIDELITY_RULES` 九條沒有一條管「誰對誰做了什麼」；壓縮時「遭／被／受」是一個字，最容易順手加減。同一張的箭頭方向反而畫對了——**圖對字反** |
| B18 | 使用者的「→」沒有任何判定規則 | 消化階段的主被動與順序方向 | ⬜ | 同一符號兩種意思：`黃男 攻擊→ 范男`＝動作方向、`掃描→無晶片→火化`＝流程時序。grep 過只有漲跌配色那段提到箭頭，與此無關。要規則化：→ 絕不是新聞文字、怎麼分辨兩種用法、讀出來後文字與圖必須同向 |
| B20 | **大陸譯名／用語擋不住**（「也門」） | 許岱軒 0914 下午 H5 | ⬜ | `digest_quality_problem` 六道檢查裡的字形檢查只抓**簡體／異體字**；「也門」兩字都是標準正體，完全過關，而且出現在地圖最大的國名標籤上。**規則缺口不是遵守率問題**：要一份可擴充的大陸譯名詞表（整詞比對避免誤殺）＋prompt 明文要求臺灣譯名＋品質閘加一道 |
| B21 | 既有忠實度條文寫了沒守住（連環／飆破／錯字） | 許岱軒 0914 下午 H1 | ⬜ | 原文「接連遇襲」→成品「**連環**遇襲」（`CONTENT_FIDELITY` 第 8 條**點名了這個詞**）；「一度上漲逾2%，來到107.51」→標題「**飆破**107美元」（第 5 條原文就寫 THE 標題 IS WHERE THIS SLIPS）；「荷姆茲」→「賀姆茲」錯字。**先驗證為什麼不生效**（條文位置太後面？）再決定是搬位置還是加正則自動檢查 |
| B23 | refine 改動未被要求修改的文字 | 許岱軒 0914 下午 H6 | ⬜ | 16:01 那次 refine 把「佩里姆島」改成「丕林島」（15:52 還是佩里姆島，原文也是）。`IMAGE REFINE RULES` 開宗明義「Apply ONLY the change requested」。同一島的別譯，**不是事實錯誤是擅自改字**——與 B13（紙箱補位）同族：refine 不該碰沒被點名的東西 |
| B24 | 附圖在後續生成中消失（15:39 起）| 許岱軒 0914 下午 H3 | ⬜ | **2026-09-14 查證：不是 `9e747a7`。** 許岱軒 15:36–16:01 全程 type＝自動判斷（沒切十點／YT），`9e747a7` 的 `setEditorFormat` 只 `clearCoverAsis`／`clearYtAsis`，測試也只釘這四個呼叫，CG 的 `userRefImages` 不被清。15:36 消化仍寫 supplied photograph（成品 ISW 嵌圖在）；**15:39 已改口畫地圖**。依據：正式站 15:39:01 歸檔 prompt 已無 supplied photograph（`D:\Downloads\AICG\後台紀錄\20260914\20260914_0-2_0-3量測.md`）；MASTER 原記 15:44 偏晚。15:47 補回 106 字正文仍是 AI 地圖。剩餘不確定：15:39 payload 是否還帶檔（歸檔在 asis 注入前，B29 同類）。較像 B22／B25 族，不是換版型清槽副作用。 |
| B26 | **多張原圖放置的措辭是單數，很可能只放一張** | 一般 CG 線的原圖放置 C1 | 🔶 **已修待驗（exp，2026-09-14）** | asis ≥2 改走 `USER_REFERENCE_ASIS_MULTI_RULES_TEMPLATE`（張數寫死、上傳順序左→右、編號陷阱：不要用整份 request 的 attached-file index）。單張仍走 `USER_REFERENCE_ASIS_RULES`。消化端 `USER_REFERENCE_ASIS_DIGEST_RULES` 未動 |
| B105 | **記者／編輯 CG「字少」（預設）字太少，跟「字極少」差距太小** | 使用者 2026-09-24 | 🔶 **SOL 已評估（2026-09-24），目標級距與驗收張數待裁** | 真因（SOL，`docs/回覆-20260924-SOL評估D組F48-B105-B106.md`）：`SIMPLIFIED_DENSITY_RULES` 允許 1～3 點且叫模型不要硬湊，`MINIMAL`／`SIMPLIFIED` 都沒有程式守門；D16 實拍（276 字稿）字極少 3 點約41字、字少 3 點約39字倒掛。建議只提高字少：素材足夠時 min3／target4、每點約14～18字、內文約45～75字；字極少 target1、容許1～3、硬上限3（與 B34 同批）。會動 4～6 份 CP1 fixture（本機字串，免費）。**使用者 2026-09-26 問驗收能否少測**：SOL 原提 24 張；**使用者 2026-09-26 裁：12 次純消化（1 長稿×記者／編輯×字極少／字少×3 次）＋2 張生圖（記者、編輯各一張字少）**。播出鏡面等 D17 落地後另驗 |
| B106 | **「不改字」會改字／掉字** | 使用者 2026-09-24 | ✅ **①～④已修、兩站上線 260926-02、實拍 3 案通過（2026-09-26，個人 OpenRouter key，gemini-3.8-flash 消化＋預設生圖，`G:\Claude共用\AICG60926_B106不改字\`）**：T1 指示行被排除且手繪風生效、正文逐字；T2 無標記「用手繪風」被當正文印上；T3 播出鏡面＋蓋章 OFF 三行順序不變、末行成底帶。守門實際擋下 T1 漏「今天」1 次、T3 自行加字 2 次後才放行：第五次不符改回 400＋可行動訊息；指令欄有字也照比；比對前剔除「指示:／指令:」行；完稿自帶 [標題]／<蓋章>／< > 兩邊同規則剝；蓋章 OFF＋不改字只拿標記留正文。守門 `tests/test_b106_verbatim_gate.py`。**兩個行為變更**：沒標記的散文指令會被當正文、比對不過就報錯（訊息請使用者加「指示:」或改寫進指令欄）；指令欄寫「濃縮／精簡」＋不改字不再默默縮寫，改為報錯請擇一（收窄了 09-07「指令欄優先於消化程度」在不改字檔的效力）。✅**使用者 2026-09-26 兩項皆確認接受**；「備註:」不列入指令行（維持只認 指示:／指令:）。另補播出鏡面＋蓋章 OFF＋不改字時 `<蓋章>` 改標 `<底帶>`，避免底帶兜底重排正文（`002a85e`，260926-02）| **使用者裁決原話**：「不改文字不加文字 使用者貼的全部文字都要 但要排除使用者參雜的指令文字 所以還是要消化」。＝承諾在**消化文字層**：使用者貼的正文一字不改、不增不減；**只剔除夾帶的指令文字**。SOL 靜態找到的違反點（`docs/回覆-20260924-SOL評估D組F48-B105-B106.md`）：①逐字 gate 第五次不符仍放行（`main.py:3249-3254`，已人工核實）②專用指令欄非空就整個停用 gate（`main.py:3244`）③gate 拿完整 `news_text` 比，沒先剔除 `指示:`／`指令:` 行，模型正確排除指令反被判不符→掉進①④蓋章 OFF 後製刪 `<蓋章>` 行（`main.py:3281-3283`）⑤空白／換行不驗。①～④違反已裁承諾，要修；「指令文字」怎麼辨識牽涉 D20 的 `[]` 表示法，動工前要對齊。**成品像素層（生圖模型畫錯字）不在本次承諾內**，未裁 |
## A — 對齊／一致性

| ID | 項目 | 來源 | 狀態 | 依賴 | 一句話現況 |
|---|---|---|---|---|---|
| A1 | **CG 線導入變化池** | 創意階梯三條線對齊清點 G1 | 🔶 **已修待驗（exp，2026-09-15，Stage 2-6）** CG 改用既有 `creativity.draw()` 六池（plate／stagger／typeface／palette／anchor／tilt），輸出為命令句；0 級仍不注入，凍結快照未動 | ~~D1~~、D2 | 使用者回報「最高級還是不夠亮」的直接原因：CG 每級注入固定文字、無抽籤，成品每次同一長相；L4 那段又只描述一種美術。可搬的池子與位元組釘死範圍見來源章節。**好消息**：`test_reporter_prompt_frozen` 只凍 0 級，池子文字碰不到 fixture |
| A2 | CG 配件件數改由程式抽 | 創意階梯三條線對齊清點 G4 | 🔶 **已修待驗（exp，2026-09-15，Stage 2-6）：CG 已接上共用件數表，封面表一個字沒動。** ｜ **件數已定（CP4，使用者 2026-09-14 裁決）：`{0:0, 1:0, 2:1, 3:2, 4:3}`，就是封面線現行的 `COVER_ACCESSORY_COUNTS`（`editor_formats.py:574`），三條線共用這一張，CG 不另立**。⚠**更正（2026-09-15）**：先前寫成 `{2:0}` 是錯的。使用者選的是「跟封面一致」，而封面 level 2 現行就是 **1 件**。若照 `{2:0}` 做，十點／YT 的 L2 會從 1 件掉到 0 件，連帶動到 52 筆 `cover_design_brief_rng_pins_20260911.json` 與 YT fixture——那是**沒有被授權的視覺變更**，也違反 2-5「不藉重構偷改封面行為」。採用現行表之後，「共用一張表」與「不改封面件數」兩個要求同時成立，計畫內的矛盾消失。（由子代理 stage2 在動工前發現並回報） | ~~D1~~（已裁） | 現在只寫「一兩個」「至多三個」，repo 自己記過「許可句推不動模型」，等於配件不存在 |
| A6 | **能力矩陣要加欄位**：附圖順序／位置／內文對應／多張措辭／創意池／seed | 播出鏡面與編輯一般 CG 的對齊狀況 | ⬜ | D9、D1 | **2026-09-14 查證：播出鏡面與預設 CG 指向同一個 `Capability` 物件**（`DEFAULT_FORMAT: _CG_CAPABILITY`／`"broadcast": _CG_CAPABILITY`），十三個旗標結構上不可能不一致；刻意不同的只有挖空框／挖哪側／點數／浮水印位，那是版面實體差異。**真正的缺口是矩陣只有十三欄**，本輪清點出來的六軸一欄都沒有——要把它們加進矩陣，缺口才會從「找 if」變成「把表填齊」 |
| A5 | 十點創意階梯移植回通用版 | 待建立：把十點封面的創意階梯移植回通用版創意階梯 | 🔶 **已修待驗（exp，2026-09-15，Stage 2-5／2-6）** 件數表與抽籤搬進 `creativity.py`（`accessories()`），`editor_formats` 只留十點 adapter；CG 直接用同一支，沒有另立一套池 | ~~D1~~、A1 | 與 A1 高度重疊，應一起裁一起做 |

## F — 功能待建立

| ID | 項目 | 來源 | 狀態 | 一句話現況 |
|---|---|---|---|---|
| F0 | **實作 D1 的 seed 欄位**（三條線統一） | 創意階梯三條線對齊清點 G2 | 🔶 **已修待驗（exp，2026-09-15，Stage 2-4）** 三條線的 request／response 都有 `seed`，沒帶就由 `next_generation_seed()` 現抽並回報；前端三條線各存一顆、只有「重新生成」才 `bumpSeed`。seed 進 `request_log` 與 `audit_archive` metadata，**不進 prompt**、**不印在圖上**（D1 2026-09-15 補充裁決）。YT 的 `標題\|日期` seed 已移除 | D1 已裁：請求加 `seed`、前端重生遞增、成品籤印 seed。**A1／A2／A3／A5／B5 的共同前置**，要先做 |
| F20 | 「完全不要文字」檔位實作 | 新增「完全不要文字」檔位 | 🔶 **已修待驗（exp，2026-09-15，Stage 2-9）** 六段拉桿最左端；`NO_TEXT_DENSITY_RULES` ＋ 壓在最後的 `NO_TEXT_FINAL_REMINDER`（逐項點名關掉 `[標題]`／`[內文小標]`／`<蓋章>`／`<底帶>`／卡片數／創意階梯的字級條文）；無字不注入蓋章條文。品質閘原本把「variable 為空」當故障，無字會連撞 5 次重試後 502，已改成只對無字放行這一項。生圖端另加 `NO_TEXT_IMAGE_OVERRIDE`（前後端逐字同步，parity 測試守著）。**照使用者裁決仍呼叫 digest，沒有跳過** | 依賴 D14。連帶要盤點無字模式下失效的區塊（蓋章／色調／安全框文字留白／斷句／品質閘／創意階梯的字級條文），逐一決定不注入還是改寫——留矛盾句模型會挑最寬鬆的遵守。能力矩陣加旗子；無字可跳過整個 digest 呼叫（順手的 token 效益） |
| F21 | 不宜播出內容規則實作 | 不宜播出的視覺內容要有避開清單 | ⬜ | 依賴 D15。**不能只加負面清單**（commit 73ae198 記過：排除條文埋在一長串否定句中間模型照樣犯），必須同時給正面替代做法（「集體火化」→ 空的焚化爐外觀／設施外觀／象徵性火焰） |
| F22 | 主被動、順序、「→」規則化 | 消化階段的主被動與順序方向 | ⬜ | 三件：施動者／受動者不可對調、原文給的先後必須保住、「→」的判定。**與 F19 相鄰但不同**：`(ICON)` 是指令（不可出現在成品），`→` 是內容的結構標記（必須反映在排列上）。做 F19 時一併想清楚這條界線 |
| F24 | 臺灣譯名／用語詞表 | 許岱軒 0914 下午 H5 | ⬜ | 依賴 B20。也門→葉門、奧巴馬→歐巴馬、悉尼→雪梨、朝鮮→北韓、信息→資訊、視頻→影片…。做出來後**回頭掃歷史紀錄**，看還有多少大陸用語已經播出去過 🔗**2026-09-16：本列的第一塊磚已經落地**——B73 的 `TW_PORTRAIT_NAME_ALIASES`（`photo_lookup.py`）就是一張臺灣譯名對照表，目前收 13 條、全部實查過。本列要做的「臺灣譯名／用語詞表」如果開工，**應該把它併進來或以它為起點**，不要另外開第二張表——兩張表遲早會互相矛盾。|
| F1 | 字多／字少級距拉開 | 字多／字少的檔位差距不夠明顯 | 🔶 **已修待驗（exp，2026-09-15，Stage 2-8）——程式面 no-op，只補驗收契約** 點數與行長在 0-2 校準時已定、標題上限由 D16／2-1 落地、maximum 的可拆分由 D3／2-7 落地；剩下唯一可動的 `STANDARD`／`SIMPLIFIED` 兩塊逐位元存在於凍結快照裡，動它們就是破壞 CP1。新增測試釘住四個上限嚴格單調、各檔點數天花板仍在、播出鏡面卡片數不隨拉桿長。**實拍量測仍欠**，由測試 session 跑。⚠**2026-09-15 補證據後拆項**：逐格重算發現 F1 底下其實是四件事——記者版／非卡片版型**結案**（1／2／7／8 點、10／50／135／159 字，倍數級距）；短稿四檔相同是**正確行為**；播出鏡面被卡片張數釘死另立 **D17**（待裁）；編輯版 `minimal` 出 3 點違反自己的規則另立 **B34**（要重凍，本波不做）。四者都不需要動 `STANDARD`／`SIMPLIFIED`，「程式面 no-op」仍成立。詳見 R 區〈監督驗收：Stage 2-4～2-9〉的更正段 | 依賴 D3；**驗收要量測**，同篇新聞跑五檔數字數，且要分版型量（卡片堆版型的點數被版型釘死，拉桿本來就沒差） |
| F2 | 最終 prompt 改 Markdown 大瘦身 | 最終 prompt 仍有多餘符號 | ⬜ | 先量基準再改；注入順序有語意不能重排；全大寫 OVERRIDE 措辭是覆蓋機制不是裝飾 |
| F4 | 「整理包」封面（雙主題疊層） | 同名章節 | ⏸ | 等使用者給樣本 |
| F5 | YT 直播封面「氣象站」「國會風雲」 | 同名章節 | ⬜ | 未動 |
| F6 | 色調亮／暗改成 5 段拉桿 | 同名章節 | ⬜ | 未動 |
| F8 | 追蹤修訂（原圖不整張重生，只改細部） | 同名章節 | ⬜ | 未重新確認 |
| F13 | LINE 訊息內帶指令＋「已消化完稿」直出 | 同名章節 | ⏸ **使用者 2026-09-24 裁示：LINE 相關短期內都不做** | 未重新確認 |
| F14 | LINE／行動版「重新生成」 | 同名章節 | ⏸ **使用者 2026-09-24 裁示：LINE 相關短期內都不做** | 未重新確認 |
| F48 | 來源標籤位置新增「正下方」（下方置中） | 使用者 2026-09-24 | 🔶 **基準已裁（2026-09-26）：安全框下緣置中** | `center_x=(x0+x1)//2`，`bottom=y1-scaled(HOLE_INSET)`，記者／編輯／2K／D24 共用 `safe_area_spec.safe_rect()`。同步點：`compose._disclaimer_box()` 加 center 分支、`main.py` 三處 Literal、`app.js`／`index.html` 兩組按鈕、F47 restamp、refine；`news_prompt.py` 四處寫死「lower-right corner」留白要改成依位置動態注入（只在有標籤時注入，CP1 fixture 可不動）。SOL 建議播出鏡面與封面不納入、長來源標籤單行或縮字——**這兩點使用者尚未裁** |

| F25 | 生成速度：先做端到端分段計時 | 使用者 2026-09-14 問「生圖速度還有辦法變快嗎」 | ⬜ **列 TODO，之後處理** | **現況沒有分段計時，不知道消化佔幾秒、生圖佔幾秒，任何加速決策都是猜的。** 第一步是純量測、不動行為、不花錢。量完才決定動哪裡 |
| F27 | 生成速度：多張同輪次改並行 | 同 F25 | ⬜ | 若現況是一張一張序列跑，改並行是純賺（不犧牲畫質）。**要先確認現況是不是序列**，依賴 F25 的量測 |
| F29 | 生成速度：稽核歸檔移出請求路徑 | 子代理速度調查 2026-09-14 | ⬜ | `_archive_generation`（`main.py:2447`／`3899`）在**回傳成品前**同步做 GCS 兩個 blob 上傳（`gcs_archive.py:49-53`）＋本機寫圖與 JSON（`audit_archive.py:68-85`）。改成 `BackgroundTasks` 或背景 thread 即可，**不犧牲任何畫質**，使用者等待直接少掉這一段。與 B29（歸檔內容不完整）同一支函式，**若要一起改，順序是先 B29 再 F29**，免得搬到背景後更難除錯 |

### 生成速度這一族（F25–F28）的前提，寫在這裡免得日後重打

使用者 2026-09-14 問過兩次相關問題，結論記下來：

1. **prompt 瘦身（F2）不會讓生成變快。** 這個 repo 自己量過兩次
   （`main.py:155-200` 的長註解）：寫出來的正文非常穩定（856–1361 token），
   會爆的是思考（603–4873），而且**思考量跟著規則條數漲、跟 prompt 字元數無關**。
   F2 的效益是模型遵守度與成本，不是速度。
2. **生圖那段沒有免費的午餐**（見 F26），要快就要拿畫質換。
3. **慢的常常不是生圖而是消化**：實測撞過「重試後 269 秒才回應」，
   離 Cloud Run 的 300 秒硬砍只差一點，最壞還要跑五次 attempt（`DIGEST_ATTEMPTS = 5`）。
4. 所以**第一步一定是 F25 量測**，不是直接動旋鈕。

**使用者裁示：「生圖速度先列 todo，之後處理。」本波不做。**

### 子代理靜態調查結果（2026-09-14，純讀碼未實測）

**架構前提（先前理解有誤，更正）**：網頁版的 `/api/generate` **只做消化、不生圖**，
成品圖是前端再打第二支 `/api/images/generate` 拿的（`app.js:2846` → `2868`，序列），
**兩支各自吃一份 Cloud Run 300 秒**。只有 LINE 那條 `generate_news_image`（`main.py:3771`）
才把消化與生圖擠進同一個請求（`3787`／`3817`）。
另：`DIGEST_TWO_STAGE` **預設是關的**（`main.py:826`），分類那一次 20 秒往返預設不發生。

**並行現況**：全專案只有兩個並行點，都是封面版型的左右兩格底圖（各 `max_workers=2`）——
`main.py:4739`（十點雙切）、`main.py:5842`（YT 雙則）。**其餘全部序列**，包括
封面 AI 標題模式的兩段生圖首尾相接（`main.py:5233-5237`／`5990-6013`）、
YT 雙則的兩次畫面描述（`5958-5962`）、逐人查照（`3308-3321`）、
逐點 geocode（`1458`，且 `map_lookup.py:129` 每點間硬性 sleep 補滿 1 秒）。
⚠ 前端 `app.js:3131` 的 `for (round < 6)` **不是六連生**，是上傳圖的 JPEG 品質降階迴圈（`app.js:3122`）。

**現有計時：全專案只有一處真的量牆鐘時間**——標題斷句（`main.py:4268/4283/4287`）。
`log_digest_usage`（`main.py:1606-1636`）記 token 與狀態但**沒有秒數**，
`request_log.py:65/118` 落檔欄位也沒有 duration。**所以現在沒有人知道時間花在哪，這就是 F25 的理由。**

**要加分段計時，最少動這 6 點**（各包一層 `perf_counter`）：

1. `digest_completion` 的 `return response` 前（`main.py:1748`）——**CP 值最高的單一改動**，
   一處涵蓋主消化／分類／斷句／封面描述全部 5 個呼叫端，`site` 參數現成可當標籤
2. `generate()` 的 attempt 迴圈（`main.py:2097-2180`）——分開「重試放大」與「單次慢」
3. `resolve_map_points`（`main.py:1441`）——geocode 總計，含強制 sleep
4. `lookup_portrait_photos`（`main.py:3295`）——查照總計，順便暴露 B32 的重複查詢
5. `generate_image_raw`（`main.py:2738`）——生圖牆鐘
6. `finalize_image_result`（`main.py:2827`）與 `_archive_generation`（`main.py:399`）——分開量 Pillow 後製與歸檔上傳

### 加速候選清單（依 CP 值）

| # | 候選 | 犧牲畫質 | 確定性 | 已另立 |
|---|---|---|---|---|
| 1 | 歸檔移出請求路徑（`main.py:2447`／`3899`、`gcs_archive.py:49-53`、`audit_archive.py:68-85`），改 `BackgroundTasks` | N | **純靜態確定** | F29 |
| 2 | `photo_lookup` 加行程內 TTL 快取，消掉重複查照 | N | **純靜態確定** | **B32** |
| 3 | 斷句呼叫移到零 API 分支之後 | N | **純靜態確定** | **B33** |
| 4 | 明確設 `max_retries`，讓 90 秒真的是上限 | N | **純靜態確定** | **B31** |
| 5 | 第二次 `generate()` 共用同一 deadline | N | **純靜態確定** | **B31** |
| 6 | 封面斷句與畫面描述並行（`main.py:5183`→`5220`，兩者無依賴） | N | 可並行為靜態確定，秒數需實測 | F27 |
| 7 | Nominatim 1 秒節流改併發（`map_lookup.py:129`、`main.py:539`，4 點 ≥3 秒純等待） | N | 靜態確定，**但違反對方使用政策，需裁決** | D-（未開） |
| 8 | OSM 圖磚並行下載（`map_lookup.py:255-266`，24 張序列 ×8 秒） | N | 需實測（僅冷快取有感） | F27 |
| 9 | 合併／跳過封面兩段生圖的第一段（`main.py:5233-5237`／`5990-6013`） | **Y** | 需實測＋需裁決 | F26 家族 |
| 10 | `OPENAI_IMAGE_QUALITY` `medium`→`low`（`main.py:3000`）／降 `DIGEST_MAX_TOKENS`（`main.py:185-186`） | **Y** | 需實測 | **F26（已評估為不建議動）** |

**監督判讀**：第 2～5 項是**缺陷不是調校**，已改列 B31／B32／B33，
不必等 F25 的實測就能確定現行行為是錯的；其中 **B31 會讓使用者在 Cloud Run 上直接吃到逾時**，
是這一族裡唯一有使用者可見後果的。第 1 項獨立為 F29。
第 7 項要動別人的使用政策，屬裁決項，**未開，不得自行動工**。

---

### 🔴 未結的驗收債：Stage 1 與 Stage 2 合併一輪實拍（使用者 2026-09-15 裁決）

**Stage 1 七項（B26+D9／B27／D4／B1／D7／B28／1-7）目前是 🔶已修待驗**，
本機 1696 測試全綠、凍結快照未破，但**生圖實拍一張都還沒跑**。

**使用者裁決：不單獨為 Stage 1 開一輪，等 Stage 2 做完合併一次測。**
理由：Stage 1 在生圖端、Stage 2 在消化端，**兩者獨立、不會互相混淆歸因**，
合併可省一輪費用與一次交辦。代價是 Stage 1 若有問題會晚幾小時才發現，已接受。

**這一輪的交辦對象是「測試」session**（實作 session 不自行宣告視覺驗收完成）。
交辦單要涵蓋：

| 來源 | 要看什麼 |
|---|---|
| B26+D9 | 多張原圖放置時是不是真的放了多張（不是只放一張） |
| B27 | YT 封面有沒有真的讀附圖位 |
| D4 | 兩格都有圖時 LEFT／RIGHT 措辭有沒有生效 |
| B1 | 成品回餵不再出現重複日期 |
| B28 | 具名真人時「AI示意圖」標籤沒被洗掉 |
| D16 | 六檔（含無字）各量標題與內文字數，**broadcast 那幾格排除行數、保留字數**。⚠**2026-09-15 已實拍六檔，結論：內文級距成立、標題級距未驗**（$0.24，`gpt-5.5`，276 字颱風稿／記者版／非卡片，逐張由監督覆核）。內文塊數 3／3／6／8、總字數 約41／約39／131／123，底端兩檔擠在一起但與頂端差約 3 倍。**但標題實測 6／4／10／8，四檔全部遠低於上限（10／13／18／22）且不單調**——上限從未被觸及，等於 D16 的數字在這輪完全沒發生作用，調成任何值這六張都不會變。與 0-2 長稿量到的 13／13／19／20 不相符，⚠**2026-09-15 監督自我更正**：原寫「差別最可能在素材長度」，一查就推翻——**0-2 的 long 樣本是 295 字**（`scripts/fixtures/title-length-news.json`），與今天的 276 字幾乎同長、同為記者版非卡片。所以長度解釋不成立，真因待查，候選有三：①Stage 2（2-1～2-9）把標題寫短了；②量測路徑不同（0-2 走 `scripts/measure_title_length.py` 直打消化 API，今天走瀏覽器全鏈路，中間可能多了標題斷句或前端設定）；③素材題材（CPI 數字稿 vs 颱風稿）。**所以 D16 不能用這輪結案**，兩條路待裁：甲＝再跑一組 1000 字以上的長稿階梯（4 張 $0.16）驗上限會不會變成有效約束；乙＝接受 D16 只是天花板、不要求可見級距。**裁決前不得動 D16 的數字。** 完整量測 `D:\Downloads\AICG\後台紀錄\20260915\D16_六檔量測.md`。**副產品**：檔位 6 塊數比 5 多（8>6）但總字數更少（123<131），確認 D3／2-7 的「只能拆不能補」生效 |
| D2 | 綠幕情境與「下跌＝綠」資料圖各一，確認禁的只有螢光綠 |
| F0 | 同 seed 與遞增 seed 的可重現性 |
| A2 | 創意 3／4 級配件件數是不是 2／3 |
| D3 | maximum 拆分沒有新增原文沒有的事實 |

⚠**開輪前先報「N 張 × $0.04」與用哪把 key**，不得自行跑下去。
⚠**D16 的數字是用 `gpt-5.5` 量的，這一輪要在正式站 `claude-sonnet-5` 上看**；
不合格回報監督重裁，實作不得自行微調。

---

## M — 模組化／技術債

| ID | 項目 | 來源 | 狀態 | 一句話現況 |
|---|---|---|---|---|
| M1 | 附圖規則模組 `refs.py` | 模組化（第 2 步） | ⬜ | 附圖規則散在 `main.py` 五個函式＋`news_prompt.py` 六段模板 |
| M2 | 標題政策模組 `title_policy.py` | 模組化（第 3 步） | ⬜ | `title_mode_for_creativity`、`creativity_level`、日期牌歸屬、只改文字可用性 |
| M3 | 端點拆 router | 模組化（第 4 步） | ⬜ | `main.py` 6320 行／158 個 def |
| M4 | `app.js` 依版型讀能力矩陣 | 模組化（第 5 步） | ⬜ | 第 1 步（能力矩陣資料化）已做，前台仍是靜態表靠測試釘住 |

## R — 待驗收／等證據

| ID | 項目 | 來源 | 狀態 | 一句話現況 |
|---|---|---|---|---|
| R2 | 地圖繼續驗收 | 同名章節 | ⏸ | 注意「AI 看圖驗地理不可靠」，逐點對照座標，沒把握標待人工複核 |
| R3 | 入門手冊截圖解析度太差 | 同名章節 | ⬜ | 下一輪修手冊時一併 |
| R4 | 附圖未測組合 | 版型功能對齊盤點 | ⏸ | scene／portrait／map 參考、創意 2–4 配附圖、live24 三種底圖配附圖，皆未測過 |
| R5 | 創意階梯改動後的長相重複度基準 | 創意階梯三條線對齊清點 | ⬜ | **改 A1 之前**先跑一輪同篇新聞最高級 N 次當基準，否則「有沒有變亮」又是感覺題 |
| R7 | `MAX_PORTRAIT_FACES=3`（一張圖最多畫幾張具名真人臉）要不要放寬 | 2026-09-17 使用者提問「人臉問題已解決，可否鬆綁 3 人上限」 | ⚖ **2026-09-20 使用者已裁：先跑一輪 4 人／5 人實拍，看結果再訂數字，不先放寬**（採納監督建議）。**驗收標準沿用 0818 那輪**：逐張核對每一張臉是不是對的人、有沒有捏臉或換臉，**任何一張錯就不放寬**。實拍要付費，開跑前先報張數與金額 | **監督判斷：不可直接放寬**。3 這個數字的根據是 2026-08-18 實驗只驗到 3 人（9/9 張臉正確對應、0 交換、0 捏臉），**沒有 4 人以上的證據**——這是「多人場景會不會認錯人／換臉」的問題，與 B73（譯名對照表救回查不到照片的人）、`PORTRAIT_NO_ENTRY_FALLBACK`（沒照片時的安全退路，且該開關目前預設仍是關）是**兩件不同的事**，後者解決不了前者。要放寬須先排一輪比照 8/18 的多人（4 人以上）composite 實測，逐張核對臉是否對應正確人名、有無交換／捏臉，才能決定新上限 |

### 監督驗收：Stage 2-4～2-9（2026-09-15，`fda34c6`）

實作由子代理 stage2 完成，驗收由監督另跑，**沒有沿用實作者的自述**：

- **全套 1755 綠**（監督自己重跑，114.9 秒），與實作者回報一致。
- **凍結快照零接觸**：`git log --name-only 707bf12..HEAD` 顯示 7 個 commit 一份 fixture 都沒動，最後一次動它們仍是已授權的 `707bf12`。**沒有第二次重凍**（CP1 只批准一次，那一次已用掉）。
- **六個 density block 全乾淨**：對 `STANDARD`／`SIMPLIFIED`／`MINIMAL`／`MAXIMUM`／`VERBATIM`／新增的 `NO_TEXT` 逐一跑 `(one|two|three|single) lines?` 反向斷言，零命中。Stage 2 初版的行數洩漏（density block 是角色共用，寫進去就同時流給記者）沒有復發。
- **A2 確實只剩一張表**：`creativity.py:234` 單一來源，`editor_formats` 與 `main.py:1256`（CG）都指向它，另有測試釘住「CG 不得另立件數表」。封面／YT 的 RNG pin 與 fixture 全數未動。
- **seed 沒進 prompt**：對 0–4 級 × 三組 seed 實跑 `cg_creativity_rules()`，seed 數字一次都沒出現在輸出字串裡。seed 只走 API response／`request_log`／`audit_archive`，符合 D1 補充裁決。
- **無字檔位接線完整**：`build_prompt` 的唯一 Python 呼叫點與 `app.js` 的兩個呼叫點都傳了 `no_text`，沒有漏接的路徑。

**三件超出計畫範圍、實作者主動申報、監督同意的改動**：

1. `news_prompt.NO_TEXT_IMAGE_OVERRIDE` ＋ `app.js` 同名常數（不在計畫檔案表內）。**同意**：不改就是半套——空 variable 會被 `compose_variable` 換成 `[No Variables Defined]`，模型有機會把那串字面畫上去。沿用既有的 `YT_COVER_TEXT_FREE_OVERRIDE` 模式、前後端逐字同步並加了 parity 測試，沒有發明新機制。
2. `digest_quality_problem` 放行「無字的 variable 為空」（不在計畫行號內）。**同意**：不改是硬阻斷——無字會連撞 5 次重試後回 502，使用者只看到「AI 回傳內容異常」。放行範圍夠窄，型別／異常字元／頻道洩漏／簡體字四項照舊全開。
3. 招式池只改 `iconrow` 一條的措辭（那條寫死「深藍底條上方」，CG 沒有底帶）。**同意**，且**同意它沒有動** `arrow`／`magnifier`／`cutout` 的 "photograph" 字樣——那是措辭貼不貼切、不是事實錯誤，留給實拍那輪判斷，已進 TODO。

**2-8 的 no-op 監督接受**，因為它給了要求的證據而不是一句「no-op」帶過。

⚠**監督更正（2026-09-15，`86b57f9` 之後）**：上面那句原本寫的是「這屬於『已經夠開，結案』，不是『該動但不能動』」——**結論的方向對，但我的兩分法太粗**。子代理補證據後逐格拆開，**實際上是四件事**，處置各不相同：

| | 現象 | 處置 |
|---|---|---|
| ① | 記者版／非卡片版型：`long／記者` ＝ **1／2／7／8 點、10／50／135／159 字**，嚴格單調且是倍數級距 | **結案**——素材與版型給得起的時候，現行天花板是有效的 |
| ② | 短稿（17 字）四檔全塌成 1 點／8–10 字 | **正確行為，不是缺陷**——條文明文禁止湊數（`maximum` 第 3 條），這一格**不該**被拉開 |
| ③ | 編輯版播出鏡面 **3／3／4／4** 點，被 `_broadcast_point_count()` 釘死 | **另立 D17**，**2026-09-15 已裁：要聯動、張數交給模型決定不寫死**（待實作） |
| ④ | 編輯版 `minimal` 出 **3 點**，違反自己的「ONE point」 | **另立 B34**，**2026-09-15 已裁：1～3 點合格、上限 3 點**（要重凍，下一波修） |

四者**都不需要動** `STANDARD`／`SIMPLIFIED`，所以「程式面 no-op」這個結果仍然成立。

**這一段是監督自己的錯**：我先前引的是中位數（1／2／4／4 點、10／22／34／37 字），而中位數把記者／編輯、短稿／長稿、卡片／非卡片版型混在一起平均掉了，看起來像「頂端偏窄但還行」，實際上底下藏著一個缺陷（④）與一個待裁題（③）。**逐格看得到的東西，中位數會抹掉**——下次量測級距一律先看逐格再看彙總。

另記一個口徑問題：`scripts/measure_title_length.py` 的 `chars_no_ws` **只去空白、沒去 `<>`**，內文字元數因此偏高（標題欄去 `<>` 後 13→11）。marker 數量在各檔之間沒有系統性差異，**級距形狀不受影響**，但**單格 ±10% 以內的差距要當雜訊**——`long／編輯` 的 105→92 反轉就是雜訊，該格真正穩固的證據是點數被釘在 4／4。

**仍未驗的**：一切視覺實拍。D16 的數字是在 `gpt-5.5` 上量的、正式站是 `claude-sonnet-5`，併輪實拍不合格時**回頭重裁數字**，不由實作端自行微調。

---

## 已完成歸檔

完成項改 ✅ 後移到這裡，**不刪列**。詳情留在 `TODO.md` 原章節。

| ID | 項目 | 完成 | 備註 |
|---|---|---|---|
| — | 建表前已完成的項目 | — | 見 `TODO.md` 中標「已做」「已上線」「落地」的章節（能力矩陣資料化、YT 附圖位、只改文字、換版型清附圖位、創意 0 AI 標題由鎖住改開放、live24 版型、上傳模組化等） |
| B40 | 額度用完只看到 502 且查不到原因 | ✅ 已做完（清帳補登）（2026-09-23） | `26d5dcd` 已讓 402 立即停手；`6379fa8` 補消化失敗歸檔｜原列全文見 `docs/MASTER-歸檔-20260923清帳.md` |
| B41 | 消化模型會把額度全花在思考 | ✅ 已做完（清帳補登）（2026-09-23） | `9bde3c5` 改送 `reasoning.effort`；`main.py:327-383`｜原列全文見 `docs/MASTER-歸檔-20260923清帳.md` |
| B42 | 改圖失敗訊息三秒後就消失 | 🗃 已過時／被取代（2026-09-23） | 雖仍用 toast（`app.js:3934-3937`），但 F42 訊息歷史已永久保留，`app.js:4024-4046`｜原列全文見 `docs/MASTER-歸檔-20260923清帳.md` |
| B44 | 標題「騙」字看似筆畫損壞 | 🗃 已過時／被取代（2026-09-23） | 後續 B58 複驗判定不是壞字並作廢；`6eeab78`、`5ca72d3`｜原列全文見 `docs/MASTER-歸檔-20260923清帳.md` |
| B45 | 直播封面會替真人捏造陌生臉孔 | ✅ 已做完（清帳補登）（2026-09-23） | `7d4748b` 建立四層肖像退路；`main.py:5989-6015`｜原列全文見 `docs/MASTER-歸檔-20260923清帳.md` |
| B47 | 播出鏡面挖空框位置偏高 | 🗃 已過時／被取代（2026-09-23） | 使用者已裁定不修位置、預設關閉；本列原缺陷方向被推翻｜原列全文見 `docs/MASTER-歸檔-20260923清帳.md` |
| B48 | 長稿常在消化完成前逾時 | ✅ 已做完（清帳補登）（2026-09-23） | `9bde3c5` 換低思考快速模型；`main.py:327-395`｜原列全文見 `docs/MASTER-歸檔-20260923清帳.md` |
| B50 | 重試五次卻每次送相同內容 | ✅ 已做完（清帳補登）（2026-09-23） | `d8cb1ee`；重試會附前次錯因，`tests/test_generate_retry.py:288-320`｜原列全文見 `docs/MASTER-歸檔-20260923清帳.md` |
| B54 | 補畫面失敗後可能亂畫真人臉 | ✅ 已做完（清帳補登）（2026-09-23） | 四層肖像退路已涵蓋封面補述；`main.py:5989-6015`、`7d4748b`｜原列全文見 `docs/MASTER-歸檔-20260923清帳.md` |
| B73 | 維基查不到的人直接不畫 | ✅ 已做完（清帳補登）（2026-09-23） | 狀態欄已寫 2026-09-16 已實作；B96 再把退路開關打開（4280aa2）｜原列全文見 `docs/MASTER-歸檔-20260923清帳.md` |
| B74 | 消化端點白名單自相矛盾 | ✅ 已做完（清帳補登）（2026-09-23） | 狀態欄已寫 2026-09-16 隨 D21 解掉（白名單整組移除）｜原列全文見 `docs/MASTER-歸檔-20260923清帳.md` |
| B75 | AI 標題模式斷句模型白打 | ✅ 已做完（清帳補登）（2026-09-23） | 狀態欄已寫 2026-09-16 已實作並實跑驗證（composite 守衛）｜原列全文見 `docs/MASTER-歸檔-20260923清帳.md` |
| B78 | 無字模式仍畫出文字版面框架 | ✅ 已做完（清帳補登）（2026-09-23） | `2c7faad`；守門測試 `tests/test_stage2_density_seed_no_text.py:604-614`｜原列全文見 `docs/MASTER-歸檔-20260923清帳.md` |
| B79 | 十點封面只接受三行標題 | ✅ 已做完（清帳補登）（2026-09-23） | `2c7faad`；兩行已中性化，`tests/test_cover_brands_and_digest_chips.py:142-159`｜原列全文見 `docs/MASTER-歸檔-20260923清帳.md` |
| B88 | 一般 CG 不認得「川習」簡稱 | ✅ 已做完（清帳補登）（2026-09-23） | `2bb7797`、`e9d45c0`；一般 CG 已注入簡稱表，`main.py:3145-3155`｜原列全文見 `docs/MASTER-歸檔-20260923清帳.md` |
| B95 | DEV 額度不足時白等五次 | ✅ 已做完（清帳補登）（2026-09-23） | `26d5dcd`：402 不重試並顯示原始原因｜原列全文見 `docs/MASTER-歸檔-20260923清帳.md` |
| B90 | 自填來源被強加錯誤前綴 | ✅ 已做完（清帳補登）（2026-09-23） | `b84036c`；一般 CG 改逐字貼，`compose.py:455-465`｜原列全文見 `docs/MASTER-歸檔-20260923清帳.md` |
| F37 | 真實照片不能填來源且被標示意圖 | ✅ 已做完（清帳補登）（2026-09-23） | `31a68e8`、`5c456aa`；來源／示意標籤已全站化｜原列全文見 `docs/MASTER-歸檔-20260923清帳.md` |
| B2 | 改圖後尺寸縮成 1748×924 | 🗃 已過時／被取代（2026-09-23） | 後來 D24 重訂規格；高字量不置框、低字量維持舊尺寸，`2bb7797`、`tests/test_plan_three_features.py:198-225`｜原列全文見 `docs/MASTER-歸檔-20260923清帳.md` |
| B3 | 前台封面高度說明和實際不一致 | ✅ 已做完（清帳補登）（2026-09-23） | 前後端高度改由同組常數推算，`editor_formats.py:1605-1623`｜原列全文見 `docs/MASTER-歸檔-20260923清帳.md` |
| B4 | 封面標題樣式說明已過期 | ✅ 已做完（清帳補登）（2026-09-23） | `5934a30` 修正兩處過期 docstring｜原列全文見 `docs/MASTER-歸檔-20260923清帳.md` |
| B5 | 封面設計說明與實際行為相反 | ✅ 已做完（清帳補登）（2026-09-23） | `5934a30` 修正兩處過期 docstring｜原列全文見 `docs/MASTER-歸檔-20260923清帳.md` |
| B6 | 封面消化結果壞掉也直接採用 | ✅ 已做完（清帳補登）（2026-09-23） | 封面 schema、違規檢查及重試已存在；`editor_formats.py:1335-1375`、`main.py:7238-7258`｜原列全文見 `docs/MASTER-歸檔-20260923清帳.md` |
| B7 | 複雜長稿容易被消化上限截斷 | ✅ 已做完（清帳補登）（2026-09-23） | `d8cb1ee`；目前一般／地圖為 12000／16000，`main.py:411-426`｜原列全文見 `docs/MASTER-歸檔-20260923清帳.md` |
| B8 | 一般 CG 消化失敗不會重試 | ✅ 已做完（清帳補登）（2026-09-23） | `6b9689b` 首次加入重試；現行重試與死線在 `main.py:386-430`｜原列全文見 `docs/MASTER-歸檔-20260923清帳.md` |
| B10 | 文件附圖被縮小到無法閱讀 | 🗃 已過時／被取代（2026-09-23） | 已由「原圖放置」用途取代單一參考圖流程；`news_prompt.py:339-346`｜原列全文見 `docs/MASTER-歸檔-20260923清帳.md` |
| B11 | 使用者只能把指令混進新聞原文 | ✅ 已做完（清帳補登）（2026-09-23） | `3254a17` 新增獨立指令欄並接後端｜原列全文見 `docs/MASTER-歸檔-20260923清帳.md` |
| B12 | 寫在原文裡的指令時好時壞 | 🗃 已過時／被取代（2026-09-23） | 網頁版已改用獨立指令欄；`3254a17`｜原列全文見 `docs/MASTER-歸檔-20260923清帳.md` |
| B15 | 成品四周出現錯誤紫色外框 | 🗃 已過時／被取代（2026-09-23） | 安全框與滿版流程已多次重寫，現行規則明禁外框；`news_prompt.py:160-170`｜原列全文見 `docs/MASTER-歸檔-20260923清帳.md` |
| B19 | 字數拉桿管不到大標題長度 | ✅ 已做完（清帳補登）（2026-09-23） | `707bf12` 實作四檔標題上限｜原列全文見 `docs/MASTER-歸檔-20260923清帳.md` |
| B22 | 含文字附圖會被當裝飾而看不清 | ✅ 已做完（清帳補登）（2026-09-23） | 原圖放置已成預設用途；`app.js:1146`、`news_prompt.py:339-346`｜原列全文見 `docs/MASTER-歸檔-20260923清帳.md` |
| B25 | 成品會亂加原文沒有的地名 | ✅ 已做完（清帳補登）（2026-09-23） | 地圖改用驗證點位，無點位不得加標記；`news_prompt.py:307-311`、`dce64ee`｜原列全文見 `docs/MASTER-歸檔-20260923清帳.md` |
| A3 | 三處創意規則可能彼此不一致 | ✅ 已做完（清帳補登）（2026-09-23） | 抽籤池集中至 `creativity.py:335-336`；`6dab2cc`｜原列全文見 `docs/MASTER-歸檔-20260923清帳.md` |
| A4 | 不知道各版型還缺哪些功能 | 🗃 已過時／被取代（2026-09-23） | 已由能力矩陣取代人工盤點；`editor_formats.py:2493-2583`｜原列全文見 `docs/MASTER-歸檔-20260923清帳.md` |
| A7 | YT 雙則封面調創意完全沒反應 | ✅ 已做完（清帳補登）（2026-09-23） | `4491a11` 將創意階梯接上合成底圖｜原列全文見 `docs/MASTER-歸檔-20260923清帳.md` |
| A8 | 隱藏的安全框設定仍偷偷生效 | ✅ 已做完（清帳補登）（2026-09-23） | `cbd1e95`；測試 `tests/test_a8_cover_safe_frame_reset.py:43-89`｜原列全文見 `docs/MASTER-歸檔-20260923清帳.md` |
| F19 | 原文內嵌指令需要正式語法 | 🗃 已過時／被取代（2026-09-23） | 主要使用情境已改走獨立指令欄；圓括號舊方案不再是唯一入口｜原列全文見 `docs/MASTER-歸檔-20260923清帳.md` |
| F23 | 大標題長短不隨字數檔位變化 | ✅ 已做完（清帳補登）（2026-09-23） | 與 B19 同由 `707bf12` 實作｜原列全文見 `docs/MASTER-歸檔-20260923清帳.md` |
| F3 | 每次成品畫風變化不足 | ✅ 已做完（清帳補登）（2026-09-23） | 變化池已集中實作；`creativity.py:87-336`、`765dc08`｜原列全文見 `docs/MASTER-歸檔-20260923清帳.md` |
| F7 | 不能拿現有圖片作為生成基礎 | ✅ 已做完（清帳補登）（2026-09-23） | 已有 AI 改圖用途；`news_prompt.py:348-364`、`14017db`｜原列全文見 `docs/MASTER-歸檔-20260923清帳.md` |
| F9 | 真人圖沒有依照本人樣貌 | ✅ 已做完（清帳補登）（2026-09-23） | 肖像查照與最多三人參考已上線；`304d3c7`、`main.py:4699-4876`｜原列全文見 `docs/MASTER-歸檔-20260923清帳.md` |
| F10 | 成品會自行加入未提及品牌 | ✅ 已做完（清帳補登）（2026-09-23） | 未提品牌禁止規則已存在；`news_prompt.py:183-198`｜原列全文見 `docs/MASTER-歸檔-20260923清帳.md` |
| F11 | 使用者不能上傳人物或視覺參考 | ✅ 已做完（清帳補登）（2026-09-23） | `3254a17`、`14017db`；四種附圖用途已接通｜原列全文見 `docs/MASTER-歸檔-20260923清帳.md` |
| F12 | LINE 未選角色時應阻止生成 | 🗃 已過時／被取代（2026-09-23） | 現行產品改採記住角色並預設記者；`line_bot.py:54-113`｜原列全文見 `docs/MASTER-歸檔-20260923清帳.md` |
| F15 | LINE 會處理不該處理的事件 | ✅ 已做完（清帳補登）（2026-09-23） | 非文字／空白事件已過濾；`line_bot.py:393-424`、`tests/test_line_bot.py:87-99`｜原列全文見 `docs/MASTER-歸檔-20260923清帳.md` |
| F16 | 漲跌顏色不符合台灣市場習慣 | ✅ 已做完（清帳補登）（2026-09-23） | 紅漲綠跌規則已在兩階段生效；`news_prompt.py:200-206`｜原列全文見 `docs/MASTER-歸檔-20260923清帳.md` |
| F17 | 主管單頁進度報告 | 🗑 使用者裁定不做（2026-09-23） | 使用者 2026-09-23 清帳時勾選丟掉｜原列全文見 `docs/MASTER-歸檔-20260923清帳.md` |
| F18 | 主管報告投影片 | 🗑 使用者裁定不做（2026-09-23） | 使用者 2026-09-23 清帳時勾選丟掉｜原列全文見 `docs/MASTER-歸檔-20260923清帳.md` |
| F26 | 想靠品質旋鈕縮短生圖時間 | 🗃 已過時／被取代（2026-09-23） | 帳本已評估「不建議動」；現行策略改以模型、推理與解析度分流｜原列全文見 `docs/MASTER-歸檔-20260923清帳.md` |
| F28 | 想再壓低舊式思考 token 上限 | 🗃 已過時／被取代（2026-09-23） | 舊 `reasoning.max_tokens` 已被 `effort` 與 Gemini 取代；`main.py:327-383`、`9bde3c5`｜原列全文見 `docs/MASTER-歸檔-20260923清帳.md` |
| M5 | 各版型的共同規則容易漏接 | ✅ 已做完（清帳補登）（2026-09-23） | 已完成裁決範圍 L1 出口強制層；`65d4ec3`｜原列全文見 `docs/MASTER-歸檔-20260923清帳.md` |
| R1 | 十點封面缺少側邊小標籤 | ✅ 已做完（清帳補登）（2026-09-23） | `7b9ace5`；欄位與規則在 `editor_formats.py:358-378`、`main.py:6660-6676`｜原列全文見 `docs/MASTER-歸檔-20260923清帳.md` |
