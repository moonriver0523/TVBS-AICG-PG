# AICG MASTER 列管清單（唯一列管帳本）

- **建立**：2026-09-14（使用者：「把 TODO 建立成 MASTER 列管表格，這是以後要修改的指引與溝通媒介」）
- **定位**：TVBS-AICG-PG 所有待辦的**唯一狀態帳本**，不同 session／agent 之間以本表交接。
  規格、查證敘事與理由**不重抄**——看「來源」欄指向的 `TODO.md` 章節；本表只管
  **狀態、優先序、依賴與裁決**。
- **衝突時**：使用者裁決 > 本表狀態 > `TODO.md` 敘事 > 程式註解。
- **💰本機 OpenRouter key（2026-09-15 起有新額度）**：額度是新的，**不是可以隨便燒**。
  跑任何會打上游的實驗前先估花費、先報數字，不要為了「多試幾次看看」加跑。
  額度耗盡的失敗模式見 B40（會全站 502 且完全不留痕）。
- **兩份檔案的分工**：本表＝索引與狀態；`TODO.md`＝完整敘事、查證數據、根因分析與
  已完成項目的歷史。開工先讀本表，**真的要動某一項才去讀它的 `TODO.md` 章節**。

## 使用規則（每個 session／agent 都要遵守）

1. **開工先讀本表**，用「一句話現況」判斷要不要認領。要動工再展開來源章節。
2. 認領：狀態改 🔶 並寫日期；實質進度寫進 `TODO.md` 對應章節（只加不改）。
3. **不刪列、不重用 ID**。完成的改 ✅ 並移到最下方「已完成歸檔」；新項目往後編號。
4. `TODO.md` 的敘事**只加不改**——寫錯就再加一行更正，保留判斷軌跡。本表的
   「一句話現況」是摘要不是軌跡，過期就直接改寫成最新一句話。
5. **動手前檢查「依賴」欄。`D-` 開頭是裁決項，沒有使用者明示不得自行動工**，
   不論它看起來多小、多明顯。

5b. **🌊波次**：狀態欄標了「🌊**第二波**」的項目，**第一波全部收工前一律不得動工**
   （2026-09-15 使用者裁定，針對曹雪卿 0915 回饋那一批 B43-B47／F32-F36／D18-D19）。
   **裁決不受波次限制**——`D-` 項使用者隨時可以裁，裁完仍排進第二波才動手。
   哪些算第一波：本表沒有 🌊 標記的既有未結項。
6. 本表與 `TODO.md` 隨 `main` 走（見 `feedback_aicg_todo_docs_on_main`）：在功能分支
   改了要 cherry-pick 回 main，改完就 commit＋push。
7. 標「未重新確認」＝這一輪清點沒有回去驗證它現在是否還成立，**不等於「確定還沒做」**。
   要動之前先讀章節並回查程式。

**狀態代號**：⬜待做｜🔶進行中｜⏸等證據｜🗳待使用者裁決｜✅完成（移歸檔）｜❌不做

**ID 分類**：`D-`裁決｜`B-`缺陷｜`A-`對齊／一致性｜`F-`功能待建立｜`M-`模組化／技術債｜`R-`待驗收／等證據

**來源**：除另行標註外，一律指 `TODO.md` 的同名 `##` 章節。

---

## 動刀分組（2026-09-14 使用者要求：列出可以一起動刀修的內容）

> **落地順序與範圍見 `docs/plan-20260914-第一波三刀.md`**（2026-09-14 建立）。
> 下方各表的「卡在」欄位已因八項裁決全數解除，以該計畫的 Stage 順序為準。

這一節是**分組視角**，不是新的列管項——每個 ID 的狀態仍以上面各表為準。
目的：同一次改動要一次改到位，避免同一段程式被拆成好幾次修、或先重構完才發現
形狀被後來的裁決推翻。

**共通前提**：`D-` 開頭的沒有使用者明示不得動工（見使用規則第 5 條）。
下面每一刀都先列它卡在哪幾個裁決。

---

### 第一刀：附圖（reference images）

**先澄清「是不是同一模組」——前端是，後端不是。**

| 層 | 現況 |
|---|---|
| 前端 | ✅ 已模組化。`renderRefList` 共用，`app.js:3091` 註解點名 `tests/test_ref_upload_module_20260913.py` 的 parity 測試釘住，改一邊要改兩邊 |
| 後端 | ❌ **還沒**。M1（`refs.py`）仍是 ⬜——規則散在 `main.py` 五個函式＋`news_prompt.py` 六段模板 |
| 路徑 | ❌ **兩條路，不是一條**。封面線走 `slot_*` 家族（main.py:606–660：`slot_reference_list`／`slot_placement_url`／`slot_generation_refs`／`merge_mixed_slot_to_aiedit`／`lock_half_slot_asis`／`reject_excess_asis`）再進 `apply_user_references_to_image_request`；**CG 線 `slots=False`，整層插槽跳過**，只碰 main.py:3558 那一個函式 |

所以「附圖已經同一模組」這個前提只對前端成立。**第一刀的實質內容之一，就是把後端這兩條路收斂成一條**（M1）。

**成員**

| ID | 項目 | 卡在 |
|---|---|---|
| B26 | 多張原圖放置措辭是單數 | 🔶 **已修待驗（exp，2026-09-14）**——`USER_REFERENCE_ASIS_MULTI_RULES_TEMPLATE`，asis ≥2 取代單數「One of」 |
| B27 | **YT 封面生圖路徑完全不讀附圖位欄位** | 王結玲 0914 下午／使用者追問「是不是附圖順序問題」 | 🔶 **已修待驗（exp，2026-09-14）** | `_yt_cover_background`／`_yt_cover_full_image` 改讀 `slot_refs`（`_yt_cover_asis_list`／`_yt_cover_gen_refs`／`_yt_cover_all_refs`）。單則攤平路徑維持。**2026-09-14 查證（修前）**：`_yt_cover_background`(main.py:5532) 與 `_yt_cover_full_image`(5598) 兩支生圖路徑都只讀攤平的 `req.reference_images`，**從不呼叫 `slot_refs()`**，且 5571 那行把 `asis` 整個濾掉。`slot_refs()` 只出現在合成／overlay 路徑。**等於 2026-09-10 裁的「第一／第二標題底下各有自己的附圖位」在生圖模式下被靜默丟棄**，左右身分根本沒送到模型手上——這就是重生會左右互換的機制層原因，比「模型自由發揮」嚴重。與 D4 同一族但**不需要裁決**：欄位已存在、只是沒接。**2026-09-14 已撈後台確認**：王結玲 16:06–16:17 共 6 筆，版型**確定是 24H LIVE**（成品左上角 24H LIVE 角標），5 筆走 `openai/gpt-image-2.5-sunburst` 生圖、1 筆 `yt-cover:overlay`。**症狀比原記的「左右互換」更廣：是位置完全不受控**——氣象圖／颱風路徑圖在六張裡分別落在右半、中偏右、左上、中間偏左。單則滿版本來就沒有左右格，所以不是互換，是**沒有任何位置約束**。這正是 D4 要處理的，而 B27 是讓 D4 無從生效的機制層原因。⚠**證據邊界**：後台歸檔的 `prompt` 是 `f"FULL: {visual}"`（main.py:5107），**只有畫面描述、不是送進模型的完整 prompt**，所以「規則有沒有注入」後台看不出來，B27 的依據是程式碼不是後台紀錄。證據圖 `D:\Downloads\AICG\後台紀錄\20260914\20260914_王結玲_YT封面六連生.jpg` |
| B28 | **畫真人＋掛真名時「AI示意圖」標籤會被附圖規則洗掉** | D13 裁決的前提查證 | 🔶 **已修待驗（exp，2026-09-14）** | `portrait_subjects` 有非空白姓名時不注入 `USER_REFERENCE_NO_DISCLAIMER_RULES`。原：`apply_user_references_to_image_request` 的 2026-08-17 裁決：有使用者上傳就注入 `USER_REFERENCE_NO_DISCLAIMER_RULES`（不標示意圖），僅 `portrait_reference_data_urls` 與 `aiedit` 兩種例外保留標籤。D13 裁示「可以畫人可以掛名，因為已標 AI示意圖」——**那個前提在有附圖時不成立**。要補：畫真人並掛真實姓名時標籤強制保留 |
| B29 | 稽核歸檔只存畫面描述，不存送進模型的完整 prompt | 王結玲撈證時發現 | 🔶 **截斷那一半已修待驗（exp `000da0d`，2026-09-16）；「只存畫面描述」那一半仍未動** | `_archive_generation` 存的是 `f"FULL: {visual}"`（main.py:5107 等），不是 `apply_user_references_to_image_request` 注入後的完整 prompt。使用者回報「規則沒生效」時無法從後台反查，只能讀程式碼推論。不擋本波。**本波建議（不做）**：後台 `_row` 渲 `density`；歸檔改存 VARIABLE 段（或放寬 `MAX_PROMPT_CHARS=4000`），否則 D16 按檔位量測永遠 n=0。🔶 **2026-09-16 截斷那一半已修待驗（exp `000da0d`）**：`MAX_PROMPT_CHARS` 4000 → **24000**（exp `febeae2` 自我更正，先前那版寫 12000 是**估**出來的、而且是拿已截斷的殘骸去估，作廢），`request_log` 與 `audit_archive` 同步。**證據**：正式站 09-15 曹雪卿 22 筆裡 8 筆 prompt 剛好 4000 字，逐筆看截點**全部斷在 `STRUCTURE (LAYOUT RULES)` 中段，`VARIABLE FIELDS` 整段在截點之後**；本機 logs 再查，118 筆走消化生圖的紀錄**沒有一筆**落在 4000 以內，等於這條路徑一律被截。**24000 是量過的**：拿 `news_prompt.build_prompt()`（純函式）餵本機 log 最長的 style(1255)／structure(2840)／variable(336)，跑遍 role×safe_frame×no_text×type_label×portrait_mode **32 種組合，最壞 19786 字**（`VARIABLE FIELDS` 起點 5878–6305），取實測上限加兩成。**測試改成現場組最壞 prompt 再比，不釘死數字**——規則區塊只會愈加愈多，寫死數字下次會靜靜再被截一次，那正是 B29 本身的病。⚠**只對未來生效，0915 那 8 筆救不回來**。「`_row` 渲 density」那一半見 F39。`f"FULL: {visual}"` 只存畫面描述那一半**仍未動**。✅**2026-09-16 已合併進公司 main（PR #25，merge `9534b11`）；⚠仍未部署到正式站** |
| B30 | CG 線的「示意圖」浮水印沒有程式疊，只能求模型畫 | 董碧惠 2026-09-14 20:39 後台證據 | ✅ **2026-09-15 已裁：浮水印由程式負責**（待動工，展開章節見 `TODO.md` 末節） | **裁決（使用者 2026-09-15）**：CG 線的「示意圖」浮水印**改由程式疊，不再交給生圖模型畫**。⚠**兩端要一起改**：程式開始蓋之後，`news_prompt.py:194`／`229`／`238`／`261` 與 `app.js` 對應副本那幾條「keep the 示意圖 label visible」會讓模型**也**畫一個，同一個 commit 要改掉；那些條文受 `tests/test_prompt_parity.py` 逐字驗，且可能落在凍結快照內，**動工前先確認重凍需求**。四個待決細節（觸發條件、樣式、落點、防重複）已寫進 TODO 章節。 ／ **原分析**：`WATERMARK_TEXT = "示意圖"`（`compose.py:305`）只由 `apply_broadcast_hole`（`compose.py:352`）貼，**僅播出鏡面**；封面線另有程式疊的 `AI示意圖`（`compose.py:445`／`719`／`1439`）。**情境示意圖這條 CG 線兩者都沒有**——`app.js:1957` 是叫模型自己把 VARIABLE 給的示意圖標籤畫出來，等於把中文字交給生圖模型，本來就不可靠。證據：董碧惠同一則金門金牌竊案連生 4 次（20:35／20:37 重抽、20:39 改圖、20:40 再重抽），20:39 的 USER CHANGE REQUEST 原文就是「右下圖片下方 加 示意圖」，改完隔一分鐘又退回重生。**建議**：CG 線比照封面線，改成程式後疊（同一支 `compose` 已有現成字型與半透明底樣式），並在前端給開關。不擋本波。
| B31 | **消化死線守門對第一次 attempt 完全不生效，單次 attempt 最壞 270 秒** | 子代理速度調查 2026-09-14 | ✅**已修，監督驗收通過（2026-09-14）**——獨立重跑全套 `unittest discover`**1687 測試全綠**，`-w` 差異確認實質只有 34 加 6 減（其餘是包 try 的縮排），contextvar 有在 `finally` reset，prompt 字串與 `BadRequestError` 退路皆未動。⚠**尚未在 Cloud Run 線上驗證**，逾時行為要等下次部署後觀察。commit `a7e7961`（`max_retries=0`／attempt 0 也查死線／第二次 `generate()` 共用 deadline／原生 images timeout 180） | `DIGEST_DEADLINE_SECONDS=230`（`main.py:253`）的檢查寫在 `main.py:2088`，條件是 `if attempt and ...`——**attempt 0 根本不執行**。同時 `main.py:84/96/104` 三個 `OpenAI(...)` 都沒帶 `max_retries`，SDK 預設還會自己重試 2 次，所以 `payload["timeout"]=90`（`main.py:1699`）是**每通 HTTP** 的上限、不是整次呼叫的：一次 attempt 常見最壞 90×3=**270 秒**（對得上 `main.py:155-200` 註解記的「重試後 269 秒」），撞上 `BadRequestError` 退路（`main.py:1721-1756`）再乘 3 = 810 秒。**已超過 Cloud Run 300 秒硬砍**，保險在最需要的第一次就是關的。另 `apply_photo_availability`（`main.py:2033`）會再呼叫一次 `generate()`，而 `main.py:2081` 的 deadline **重新計算**，單一請求理論上限變成 230+230。**修法（純靜態即可確定，不必等實測）**：明確設 `max_retries`、把守門改成每個 attempt 前都查（含第 0 次）、第二次 `generate()` 沿用同一 deadline。生圖端反向問題：原生路徑 `images.generate`／`images.edit`（`main.py:3020`／`3028`）**完全沒傳 timeout**，吃 SDK 預設 read 600 秒 |
| B32 | 維基查照在消化與生圖各查一次，且完全沒有快取 | 同 B31 | ✅**已修，監督驗收通過（2026-09-15）**——`a2ad333` ＋ `a8467d4`。獨立複核：鎖與 `time.monotonic()` 正確、HIT 15 分／MISS 60 秒分開、筆數 256 與位元組 64 MB 雙上限（值是整張圖 base64，只用筆數會有無上界成長路徑）、`main.py` 零改動、B32 相關 65 筆測試綠、全套 1712 綠。⚠**線上未驗**：實際節省的查照時間要等 F25 分段計時才量得到。| `/api/generate` 走 `apply_photo_availability` → `lookup_portrait_photos`（`main.py:2021`）查一輪；前端接著打 `/api/images/generate`，`apply_portrait_to_image_request` → `resolve_portraits`（`main.py:2417`／`3404`／`3372`）**又查一輪**。`photo_lookup.py` 無 `lru_cache`／無 `_CACHE`。單人最壞 2 候選名 × 2 語言（`photo_lookup.py:48`）×（query＋`_is_human`＋可能 P18＋圖片下載，`photo_lookup.py:151/170/175`），每通 timeout 10 秒（`photo_lookup.py:50`），且逐人序列（`main.py:3308-3321`）。加行程內 TTL 快取即可，不犧牲任何畫質 |
| B34 | **編輯版「字極少」實際出 3 點，違反它自己的「ONE point. Not one to three — one.」** | 子代理 stage2 的 2-8 逐格證據（2026-09-15），監督獨立複驗 | 🔶 **2026-09-15 已裁：1～3 點都合格、上限 3 點；下一波修** | **裁決（使用者 2026-09-15）**：編輯版「字極少」**不必硬壓成 1 點**，**1～3 點都算合格，但最多 3 點**。所以要動的是條文而不是範例——`EDITOR_SYSTEM_PROMPT_TEMPLATE` 的「ONE point. Not one to three — one.」放寬成「至多三點」，並把 3 點釘成硬上限。**現行實測值（3 點）在新標準下即合格**，本輪實拍 1／2／3 點一律判過，已告知測試端。**仍排下一波**：改條文一樣要重凍，等下次重凍額度。 ／ **原證據**：實測 `title_length_24.json`：`mid／編輯` 與 `long／編輯` 的 `minimal` 都是 **3 點**，**同樣兩則素材的記者版都是 1 點**——差別只在角色，所以病灶在編輯樣板而不在 density block。**根因（監督複驗過，且比回報的更乾淨）**：`main.py:1005-1006` 的 variable 格式範例列了整整三行 `[內文小標]`（聯準會降息那則），而**記者樣板 `main.py:979` 根本沒有範例**——這個角色不對稱把現象解釋完了。範例是模型最強的錨點，壓過 `MINIMAL_DENSITY_RULES` 第 1 條，正是本 repo 記過多次的「具體範例壓過抽象規則」。**修法**：讓範例隨 density 變，或在 minimal 明文作廢該範例的行數。**為什麼本波不做**：`EDITOR_SYSTEM_PROMPT_TEMPLATE` 逐位元存在於 4 份編輯凍結快照（`grep 聯準會三度降息 tests/fixtures/editor-digest-*` 四中），動它就要重凍，而 CP1 批准的唯一一次重凍已在 2-3 用掉。**下次開重凍額度時一併修** |
| B37 | **D16 的標題上限條文讓標題全面變短，連沒觸及上限的檔位也縮水——級距反而被壓平** | 監督主導的重現實驗（2026-09-15），「測試」執行 | ⬜ **要重裁 D16，裁決前不得動工** | **實驗設計（②③ 已排除）**：同一支腳本 `scripts/measure_title_length.py`、同一個 295 字 `long` 樣本、同一個 `native`／`gpt-5.5` 後端、同為記者版 default，**只有程式碼日期不同**——量測路徑、素材題材、模型後端三個變因全部固定。**時間軸鐵證**：0-2 量於 `b0adc93`（2026-09-14 22:44），當時 **`HEADLINE LIMIT` 條文還不存在**；上限是 `707bf12`（2026-09-15 01:18，Stage 2-1）才進 `main.py:1032`／`1061`／`1075`／`1091`。**數據**：minimal 上限 10，0-2 為 13 → 今天 **4**（−9）；simplified 上限 13，13 → **11**（−2）；standard 上限 18，19 → **10**（−9）；maximum 上限 22，20 → **15**（−5）。**關鍵在後兩格**：`standard` 原本 19、上限 18，只差 1 個字卻掉到 10；`maximum` 原本 20、上限 22，**根本沒超標、完全不需要壓縮**，仍掉了 5 個字。**所以這不是「被上限裁掉」，是「出現上限這件事本身讓模型全面寫短」**——四格同向全為負，典型的模型看到數字上限就往遠低於上限寫。**後果**：D16 的意圖是「拉桿要讓標題看得出長短」，實測反而壓成 4／11／10／15 且不單調（standard 10 < simplified 11），**比沒有 D16 的 13／13／19／20 更沒有級距**。**內文也一起縮**：`simplified` 從 0-2 的 2 點／50 字掉到 1 點／11 字，`standard` 從 7 點掉到 6 點。**限制**：每格 n＝1，模型有隨機性；但四格同向、且兩格在上限不具約束力時仍大幅下降，不是雜訊能解釋的量級。**修法方向（待裁，不得自行動工）**：①上限改成區間或目標值（「約 N 字」而非「不超過 N 字」）；②只在超標時才提上限，不要無條件塞進 prompt；③拿掉上限，回到 0-2 的自然長度。**⚠正式站未受影響**：`707bf12` 不在 `main` 上，線上仍是 `260914-05`，所以**本件與 B36 無關**，不要混為一談。原始資料 `D:\Downloads\AICG\後台紀錄\20260915\title_long_4_20260915.json`，費用 $0.476 |
| B36 | **資料圖表同一張圖裡各資訊塊行數不一致，使用者得手動補齊** | 曹雪卿 2026-09-15 10:40／10:50 後台證據，監督查證 | ⬜ | **證據**：1444 字「超級央行週」稿，10:40 生成的六宮格資料圖表裡，**四塊是三行（標籤／數字／補充），兩塊只有兩行（標籤／數字，缺補充）**——日本央行那塊停在 `1.25%`、南韓那塊停在 `3%`，另四塊都有第三行。10:50 使用者的 USER CHANGE REQUEST 逐條就是在**補那兩塊**：「左下日圓那塊最下面加一行字：日圓14日回貶至154價位」「右下韓幣那塊最下面加一行字：美元轉強 韓幣同步面臨壓力」。改圖五條全部正確套用。**所以病灶不是「整體字太少」，而是同一個版型內部不齊**——模型把六個平行的格子填成不一樣的深度，使用者每次都要手動抓出缺的那幾格。**與 F1／D16 的差別**：那兩件管的是「拉桿能不能把整體字量拉開」，本件管的是「同一張圖內部平行元素要不要一致」，是另一個軸，**不要混在一起修**。**修法方向**：在資料圖表的版型條文裡明寫平行格子的行數必須一致（要嘛全部帶補充行、要嘛全部不帶），而不是靠模型自己平衡。⚠**歸因受阻**：後台沒記錄當時的消化檔位／角色／版型（見 F30），所以**無法判定她是拉在哪一檔**——若她本來就在低檔位，這張的稀疏有一部分是合理的。修之前要先補那個欄位，否則會對著錯的基準改。**順帶觀察（不另立項）**：她同時把「創逾2週新高」改成「創9/2以來新高」，模型把原文的具體日期概括成模糊表述；這與 D3「只能重排不能新增」同向但反面，屬忠實度議題，先記著看會不會再出現。證據圖 `D:\Downloads\AICG\後台紀錄\20260915\曹雪卿查核\` |
| F30 | **後台紀錄沒有消化檔位／角色／版型欄位，使用者回報問題時無法歸因** | 監督查 B36 時發現（2026-09-15） | ⚖ **2026-09-16 使用者已裁：與 F31 併成一次改**｜⬜ | `/admin` 每列只有 ts／who／type／news_text／最終 prompt／model／provider／request_id。**沒有 density（字多字少檔位）、沒有角色（記者／編輯）、沒有版型**。後果：使用者回報「資訊量太少」時，**無法分辨是規則有問題還是他自己拉在低檔位**——B36 就卡在這裡，只能標「歸因受阻」。生圖 prompt 有記，但那是**生圖端**的，消化端的檔位不會出現在裡面。**修法**：生成時把這三個值一起寫進紀錄列並顯示在 `/admin` 列表。成本低、對後續每一件現場回報都有用，**建議排在 B36 之前**——沒有它，B36 改了也不知道有沒有改對 |
| F31 | **後台只記成功、失敗完全不留痕，故障率無法統計** | 曹雪卿回報「生圖很容易失敗」，監督查證（2026-09-15） | ⚖ **2026-09-16 使用者已裁：與 F30 併成一次改**｜⬜ | 正式站 `/admin` 200 筆全撈：表頭只有 `時間｜使用者｜類型｜成圖｜內容`，**167 列全部都有成圖**，全檔搜不到 `失敗／錯誤／error／timeout` 任何字樣。代表**生成失敗時根本不寫紀錄列**。後果：使用者說「很容易失敗」，我們**連失敗率是 5% 還是 50% 都答不出來**，也不知道失敗集中在哪種稿長／版型／改圖，只能靠前台人工重現（已派「測試」，交辦單 `docs/交辦-20260915-曹雪卿生圖失敗重現.md`）。**修法**：失敗也寫一列，至少要有 上游 HTTP status、錯誤類別（逾時／內容政策／配額／解析失敗）、耗時、重試次數；`/admin` 用顏色或欄位標出來。**與 F30 同一個檔案、建議同一刀改完**——兩件都是「後台缺欄位導致無法歸因」，分兩次改是白做工。⚠**2026-09-16 補一個本條原本沒點到的限制：「200 筆全撈」的 200 是上限不是總數**——`audit_archive.list_records()` 的簽章是 `limit: int = 200` 寫死，`admin_index()` 也沒有把它開成查詢參數。09-16 再撈一次同樣停在 200 筆、最舊只到 09-07。所以「167 列全部都有成圖」這個分母是**最新的 200 筆**，不是全部歷史；失敗率若真要算，這個上限也得一起放開（或加分頁）。**其中 `role`／`density`／`seed` 三欄已由 F39 補上渲染（exp `000da0d`），本條剩下的是失敗列與耗時／provider** |
| B38 | **消化逾時的錯誤只有一個一閃而過的 toast，內容是「HTTP 524」** | 曹雪卿回報＋2026-09-15 正式站重現 | ✅ **2026-09-15 已修已驗（exp `9e42f96`）** | 現場：進度條卡在「消化新聞 35%」約 60 秒後**靜默歸位**成初始紅色按鈕，「生成結果」欄**仍留著上一次的舊圖**，使用者看起來像什麼都沒發生。⚠**監督查證修正**：`app.js` 的 `handleOneClickGenerate` catch 裡**有** `showToast(err.message)`，不是完全吞掉——但 `err.message` 是 `_apiError()` 對 524 產出的 **「HTTP 524」**，對使用者零意義，且 toast 一閃即逝、測試端當場沒看到。**所以病灶不是「沒提示」而是「提示等於沒提示」**。後果：使用者判定「生圖很容易失敗」然後直接重按，每重按一次就再燒一次消化。**修法**：①逾時／5xx 要給看得懂的中文與可行動建議（「新聞太長，消化超過上限，請縮短或改低一階字數」）；②失敗時結果欄要標示這是上一次的舊圖，不能沉默留著；③錯誤要持續顯示到使用者關掉，不能用 toast。與 B39 同一個現場、不同層，**兩張單要一起修才有意義** ✅**監督驗收 2026-09-15**：逐行看過 diff。三處都做對——fallback 依狀態碼給可行動中文、後端 `detail` 優先的邏輯保留、紅色橫幅可關閉不自動消失、舊圖上方標「⚠ 這是上一次的結果，本次生成失敗」、成功時一併清除；四條紅線（prompt／逾時數字／`digest_reasoning_body`／分支）都沒碰；1755 測試全綠。⚠**驗收時擋下一處並已補**：`_apiError()` 是 12 個呼叫點共用的，初版把「縮短新聞、降字數拉桿」硬套到生圖（`app.js:2975`）與改圖（`app.js:3563`）——那兩條不吃新聞稿也沒有字數拉桿，**給錯的下一步比不給更糟**，正是 B38 自己要治的病。已加第三參數 `context`，只有 `_digestFetch` 傳 `'digest'`，其餘走中性版。✅**staging 實測驗收 2026-09-15（`260915-01`）**：**沒有用 devtools 模擬，是撞上一次真實失敗**（字多對照組，約 150 秒消化逾時）。三件事全部到位——①訊息是「AI 服務這次太久沒有回應，請縮短新聞內容或稍後重試」而不是「HTTP 524」；②頂部紅色橫幅**持續顯示**不自動消失（toast 另外也有）；③舊圖右上角標「⚠ 這是上一次的結果，本次生成失敗」。**真實逾時比模擬攔截是更硬的證據**。連帶開出 **B42**（改圖線仍只有 toast） |
| B39 | **Cloudflare 約 100–120 秒就切斷 `/api/generate`（524），整套逾時預算是照錯的天花板設計的** | 2026-09-15 正式站實拍重現 | ✅ **2026-09-15 已修已驗（staging `260915-01` 實測通過）**：`/api/generate` 改成 NDJSON 串流（`main.py` 新增 `generate_stream`／`_generate_ndjson_lines`，`generate()` 本體維持純函式不變，繼續被 `generate_news_image`／`apply_photo_availability`／LINE 那條路徑直接呼叫，不經 HTTP，串流只是外層 wire format）；背景執行緒跑 `generate()`，主執行緒每 `DIGEST_HEARTBEAT_SECONDS=7` 秒送一行 `{"type":"ping"}`，結束送 `result` 或帶原 status／detail 的 `error`。前端 `_digestFetch` 改逐行解析：只有串流開始前（驗證失敗等）才看 `response.status`，開始串流後一律看內容判斷成敗，`_apiError()` 沿用 B38 那套（見 `context="digest"`），連線中途斷線另有獨立訊息。`DIGEST_DEADLINE_SECONDS`／`DIGEST_TIMEOUT_SECONDS`／`DIGEST_ATTEMPTS` 一個都沒動。`uv run pytest` 1760 全綠——查證後**既有三支測試（`test_generate_retry.py`／`test_request_log.py`／`test_admin_console_formats.py`）其實不需要改**，它們測的是 `generate()` 這個純函式或路由註冊表本身，本來就沒經過 HTTP／串流那層，已跟監督說明這點；新增 `tests/test_generate_stream_20260915.py` 五支涵蓋串流層（成功／後端錯誤／未預期例外／心跳／驗證失敗）。本機用真實 2,930 字稿（native `gpt-5.5`）實跑一次，`curl --trace-ascii` 證實 73 秒內分十次收到 ping 才收到 result，位元組確實持續流入；前端解析邏輯另用模擬 ReadableStream 驗證三種情境（成功／串流內錯誤／中途斷線）訊息互不相同。正式站驗收未做，等使用者授權那段花錢的部分。⚠**監督複驗抓到一個稽核歸檔缺陷並已修復**：`threading.Thread` 起於全新空 context，不會自動帶著 `_current_user` 這個 contextvar（Clerk middleware 在 request context 裡 `set()`）——改之前 `generate()` 跑在 Starlette 用 anyio `to_thread.run_sync` 開的執行緒，那條路徑會複製 context，換成自己開 thread 之後若不手動複製，背景執行緒裡 `current_user()` 會是空字典，`generate()` 尾端的 `_remember_digest()` 因此悄悄跳過不寫，導致生圖那發靠 `_recall_digest()` 補回的新聞原文變成空的——每一次網頁版生成的後台「內容」欄都會無聲漏記，跟 0911 查到的「後台缺欄位」同一類。**修法**：`generate_stream()` 在還沒離開 middleware context 時 `contextvars.copy_context()`，`_generate_ndjson_lines()` 背景執行緒改成 `threading.Thread(target=ctx.run, args=(worker,))`。新增回歸測試 `test_background_thread_still_sees_the_logged_in_user`（從 `generate_stream()` 進去，不是內層的 `_generate_ndjson_lines()`），已驗證：復原成沒複製 context 的版本時這支測試會紅（`{} != {'user_id': 'u1', ...}`），修好後綠。`uv run pytest` 1761 全綠。 ✅**監督驗收 2026-09-15（第二輪）**：逐行看過 diff，`copy_context()` 寫在 `generate_stream()` 這一層是對的——StreamingResponse 的 body 要到 middleware 的 `finally`（`_current_user` 已 reset）之後才被消耗，複製寫進 generator 函式體裡會太晚。**回歸測試不是空氣測試，監督獨立驗過**：把 `target=ctx.run` 改回 `target=worker` 後 `test_background_thread_still_sees_the_logged_in_user` 確實變紅，還原後綠；四支相關測試（串流／重試／請求記錄／後台格式）共 39 passed + 20 subtests。另查證 `/api/generate` **全專案只有 `app.js` 一個 HTTP 消費者**（`generate_news_image`／LINE 都是直接呼叫純函式），改成 200＋內容判成敗沒有其他破口；`generate()` 回傳的就是 `GenerateResponse` 實例，`model_dump(mode="json")` 與原本 `response_model` 的序列化欄位一致。順手改掉註解裡一個簡體字（`换`→`換`）。**尚未在正式站驗**（要花錢），與 B38 一起排隊等使用者授權。✅**staging 實測驗收 2026-09-15（`260915-01`）**：拿 09-15 在正式站打出 524 的**同一篇 2,930 字稿**×「字超多」×資料圖表，15:07:35 送出、15:11:51 完成（4 分 16 秒），`/api/generate` **全程 200，沒有 524**；在原本會斷線的 122 秒那個時間點連線仍然活著。監督獨立到 staging `/admin` 核對，**該筆成功紀錄確實在（15:11:36，資料圖表）**。**B39 的目標達成——長稿不再被 Cloudflare 切斷。** ⚠**但這不等於曹雪卿的問題解決了**：同一篇改「字多」的對照組**仍然失敗**（見 B48），只是失敗方式從「524 斷線」變成「看得懂的 503」。**B39 修的是失敗的表示法，不是失敗率本身。** 📌**留待評估、非本輪阻塞**：使用者中途關分頁時背景那個 daemon thread 會跑完全程，token 照燒。 | 實測：2,930 字稿 ×「字超多」× 資料圖表，送出後 **約 122 秒**收到 **HTTP 524**（Cloudflare "A timeout occurred"），死在**消化那發**。但程式裡的三道預算全部比它大：前端 `DIGEST_FETCH_TIMEOUT_MS=290`、後端 `DIGEST_DEADLINE_SECONDS=230`、單次 `DIGEST_TIMEOUT_SECONDS=90`，註解明寫「時間設在 Cloud Run 的 300 秒之內，讓後端的訊息永遠有機會先回來」。**那個前提是錯的**——真正的天花板是 CF 的切線，**後端根本來不及回它那個講得清楚的 503**，所以使用者永遠拿不到有用的錯誤。連帶：`DIGEST_ATTEMPTS=5 × 90 秒` 的重試設計在 CF 底下**最多只跑得完一次多**，重試等於形同虛設。**修法方向（待查證後再定）**：①把後端死線壓到 CF 切線以內（要先確認 CF 實際設定值，不要用猜的）；②消化改成非同步／輪詢，不讓單一請求扛長工；③長稿在送出前就擋下並提示。⚠**先量再修**：本機重現（無 CF）正在跑，要先知道消化端在「字超多」是不是本來就慢到 100 秒以上——若是，壓死線只會換一種失敗 ⚠**2026-09-15 本機量測排除一條假說**：測試端在原生 `gpt-5.5` 上量到「思考吃光 6000 預算→截斷→重試→耗時 38 秒變 149 秒」，看似就是 524 的成因，但**那是原生路徑沒有思考封頂的產物（見 B41），正式站走 OpenRouter 有封在 2000**，不能拿來解釋 524。❌**2026-09-15 稍晚，監督更正自己這一句：這個排除是錯的。** 本機四次實驗（見 B48）用的是 **OpenRouter／claude-sonnet-5，與正式站完全相同的後端**，同樣量到 `reasoning_tokens=6000／ratio=1.00／finish=length`——**「正式站有封在 2000」這個前提不成立，封頂根本沒生效。**測試端當初那條「思考吃光預算→截斷→重試→耗時暴增」的假說**是對的，是監督誤判把它排除掉的**。這條路徑現在是 B48 的已確認根因。正式站這一側仍未取得後端日誌（`aicg.tvbs.ai` 由公司 GCP 專案服務，本機 gcloud 只看得到個人專案 `tvbs-aicg-linebot`，該時段無任何流量）。**剩下的主嫌**：claude-sonnet-5 的 provider 輪替重試（九個端點、過載換手，每換一次就是一整次呼叫的時間） ⚖**2026-09-15 使用者裁決**：公司 Cloudflare 的 proxy timeout **不能調**，所以走串流＋心跳——`/api/generate` 開始處理就持續送位元組，CF 看得到連線活著就不會 524。**明確不採**「壓後端死線」：實測 #7 是花 95～110 秒才成功的，壓到 80 秒等於自己砍掉本來會過的長稿。完整非同步（job＋輪詢）留作底牌，本波不做。**排在 B38 之後**——錯誤表示法兩邊要一致，同時動會打架。交辦單 `docs/交辦-20260915-B39串流心跳.md` |
| B40 | **OpenRouter key 額度耗盡時整條消化全面 502，且失敗不留痕** | 2026-09-15 本機量測時撞到 | ⏸ **使用者 2026-09-15 裁：先不查正式站額度，專心修 524** | 本機 exp 的 `.env` 那把 OpenRouter key 已耗盡：104 字短稿都吐 `402 This request requires more credits... You requested up to 6000 tokens, but can only afford 334`（`limit_source: openrouter_key_limit`），`DIGEST_ATTEMPTS=5` 五次全卡在同一個 402、10.9 秒就收攤，回 502。**重點不是本機這把沒錢**（那是個人帳號、已知只剩約 $1.68），而是**這個故障模式**：額度一旦見底，消化端**全站**立刻 502，使用者只看到「HTTP 502」這種無意義字串（B38），後台照樣不留紀錄（F31），沒有人會知道原因是沒錢。而且**重試在這個情境下毫無意義**——402 不會因為再試四次就變成 200，只是把錯誤延後 10 秒。**待查（已裁緩）**：正式站用的是哪一把 key、餘額多少、有沒有餘額告警。**順手可修**：402／餘額類錯誤應該**跳過重試直接回報**，並給人看得懂的訊息 |
| B41 | **思考封頂在兩條路徑上都沒用——原生路徑根本不送，OpenRouter 路徑送了但模型不遵守** | 2026-09-15 本機量測時發現 | ⬜ **第一波，已升級**（2026-09-15 實測改寫） | `digest_reasoning_body()`（`main.py:220`）第一行就是 `if DIGEST_BACKEND != "openrouter" ... return {}`，所以 `DIGEST_REASONING_MAX_TOKENS=2000` 這道保險**只有 OpenRouter 後端吃得到**。走原生 `DIGEST_BACKEND=openai` 時**完全不送任何思考上限**，思考可以一路吃到 `DIGEST_MAX_TOKENS=6000` 為止。本機實測（`gpt-5.5`，2930 字稿）：`standard` reasoning **4606/6000（ratio 0.91）**、`maximum` 一次 **6000/6000（ratio 1.00）finish=length、raw content 空字串、parse failed**，重試第二次才拿到結果——**同一組條件耗時從 38 秒跳到 149 秒，差了快 4 倍**，變異全部來自「第一次有沒有被截斷」。註解裡寫的理由是「OpenAI 系走 effort，這裡不送」，但**實際上也沒有送 effort**，等於原生路徑裸奔。⚠**不要拿這個去解釋正式站的 524**：正式站是 OpenRouter，思考被封在 2000、留 4000 給正文（觀測正文最大 1361），這條保險在正式站是有效的。**影響範圍**：①任何人在本機用原生後端量消化，數字都不可信、且會被截斷重試汙染；②哪天正式站切回原生就會立刻踩到。**修法**：原生路徑也要有等效封頂（OpenAI 系送 `reasoning.effort`），或至少在啟動時對「原生後端＋無思考上限」印一條明確警告 ❗**2026-09-15 重大更正（本機四次實驗，見 B48）**：本列原本只說「原生路徑沒有上限」，**實測發現 OpenRouter 這條也形同虛設**——送出 `reasoning.max_tokens=2000`、請求被接受（沒有觸發 `main.py:1948` 的「模型不吃 reasoning 上限」退路），但 `reasoning_tokens` 實測 1603／2038／2144／5442／6000，**沒有一次低於 2000**。**所以這不是「保護不完整」，是「保護不存在」**，嚴重度與範圍都比原本記的大。與 **B48** 是同一個根因，**建議合併處理**。 |
| B42 | **改圖（refine）線失敗仍然只有 3 秒 toast，沒有錯誤橫幅** | B38 驗收時發現（2026-09-15） | ⬜ | B38 只把**一鍵生成**那條（`handleOneClickGenerate`）改成可關閉的錯誤橫幅，**改圖那條沒動**——`app.js:3563` 失敗後仍是 `showToast` 3 秒淡出，使用者一樣會錯過。**這不是漏做，是本單刻意框的範圍**，但不能就這樣留著：曹雪卿 2026-09-15 那 9 筆裡**有 2 筆是改圖**（10:26、10:50），改圖在實際工作流裡佔比不低。封面／YT 封面那幾條同理。**修法**：把 B38 的橫幅機制抽成共用，讓改圖與封面線共用同一套；`showGenerateErrorBanner` 目前綁死 `oneClickErrorBanner` 這個 id，要先參數化 |
| B43 | **追加修改指定了主標題斷行，「斥」仍然留在第一行** | 曹雪卿 0915 回饋文件（AI CG回饋，hikaru.tsao@gmail.com） | ⬜ 🌊**第二波**（2026-09-15 使用者裁定：等第一波收工才動工） | 使用者在追加修改處下的指令是「修正主標題為：川普致電黃仁勳　斥AI接管世界是騙局」（全形空格處即為她要的斷點），成品仍是「川普致電黃仁勳　斥／AI接管世界是騙局」，**斥被留在第一行行尾**。監督已調正式站成品核對（`20260915-102630-23b0215e41ca.png`，後台 10:26:30，資料圖表改圖），**確認屬實**。要查的是：追加修改的文字有沒有把斷行意圖帶到生圖那一層，還是 `apply_title_break_hints` 只認自己的規則、把使用者寫的空格吃掉。 |
| B44 | **大標題出現壞字：「騙」的右半邊筆畫錯亂，同一張圖裡別處的「騙」卻是對的** | 曹雪卿 0915 回饋文件（AI CG回饋，hikaru.tsao@gmail.com） | ⬜ 🌊**第二波**（2026-09-15 使用者裁定：等第一波收工才動工） | 使用者原話「騙字變怪字」。監督下載正式站成品逐字放大核對（`20260915-102630`，標題「AI接管世界是騙局」）：標題那個「騙」右半邊「扁」的結構畫壞，**但同一張圖第 1 格「全球暖化騙局」的「騙」完全正確**——所以不是字庫缺字、也不是這個字天生難畫，是**同一張圖內的逐次渲染失敗**。⚠**與已撤銷的 B35 是同一現象**（B35 當時被撤是因為那次確實是監督看錯字，不是缺陷；這次是使用者回報＋監督原尺寸放大複核，**證據等級完全不同，不要拿 B35 的結論套用**）。**動工前要先查清楚**：資料圖表的大標題到底是生圖模型畫的、還是程式壓的？YT 封面那條線是程式壓字（同一個「騙」在 `20260915-133528` 上完全正確），若資料圖表也走程式壓字，那病灶就不在模型。**這題沒查清楚就改，一定改錯地方。** |
| B45 | **YT 整點直播把一位真實具名人物配上 AI 捏造的臉** | 曹雪卿 0915 回饋文件（AI CG回饋，hikaru.tsao@gmail.com） | ⬜ 🌊**第二波**（2026-09-15 使用者裁定：等第一波收工才動工） | 使用者原話「沒有指定圖，但他給了一個不正確的人物照片(如果不是名人，或許示意圖會較好)」。監督調成品核對（`20260915-133528-e0c0cdf30aa4.png`，13:35:28，YT整點直播（雙則））：**右半張是一張 AI 生成的虛構亞洲男性面孔，正下方的標題寫著「陳一新警告人工智慧威脅政權」**——臉與姓名同框，觀眾讀起來就是「這個人是陳一新」。畫面右上角**有**掛「AI示意圖」角標，但角標不能抵銷「具名真人＋捏造臉孔」這件事。⚠**監督判斷這條的嚴重度高於使用者自己的描述**：這正是 2026-08-05 出過事、`portrait_subjects` 那整套肖像規則要擋的情境（見 `GenerateResponse.portrait_subjects` 的註解），**屬播出風險，不是美感問題**。要查：YT 整點直播這條線有沒有接上肖像流程，查不到參考照時是否應該**完全不畫臉**而不是自由發揮。 🎯**2026-09-15 使用者指示插隊調查（只查不修，B45 仍是🌊第二波）：根因已完整定位，四層。****①肖像流程有接上，不是沒接。**雙則走 `yt_dual_panel_requests` → `yt_dual_panel_plan` → `yt_dual_background` → `_cover_panel_image` → `_cover_apply_portraits`，肖像規則與參考照都會注入。**②真正斷掉的是英文名。**本機實跑 `derive_yt_cover_plan("陳一新警告人工智慧威脅政權")`，模型回 `portrait_subjects_en=['Yi-hsin Chen']`（臺灣拼法）；用它查 `find_reference_photo` **查無**，於是 `keep_subjects_with_photos` 判定查不到 → `resolve_portraits` 退回 `no_reference`，日誌原文 `[portrait] 查不到參考照（陳一新），整張退回不生成臉孔`、`參考照=0 張`。**而改用維基條目名 `Chen Yixin` 就查得到正確的人**（`https://en.wikipedia.org/wiki/Chen_Yixin`，即中國國安部長本人，照片已存 `D:\Downloads\AICG\後台紀錄60915\陳一新_參考照.jpg`）。⚠**連帶更正本帳本先前的記載**：B48 行裡寫過「離線查三人全部 FOUND」，那次是用 `Chen Yixin` 查的，**不是系統實際會用的名字**；系統實際拿到的是 `Yi-hsin Chen`，查不到。先前那句話結論正確但推不到這裡，特此更正。**③退回不畫臉沒有用，因為 visual 已經寫死要一張臉。**同一次 derive 回的 visual 是「室內昏黃燈光下,一位年長學者神情凝重端坐受訪,背景虛化的書架若隱若現,鏡頭聚焦其深鎖眉頭與嚴肅眼神。」——**畫面描述命令模型拍一張近距離人臉特寫**，肖像規則只說「不生成臉孔」，兩者直接打架，模型照著 visual 畫，於是捏出一張臉。這正是 `keep_subjects_with_photos` docstring 早就警告過的同一類矛盾。**④derive 還認錯人。**「年長學者＋書架」是淡江大學教授陳一新的形象，不是中國國安部長——`YT_COVER_DERIVE_SYSTEM` 只餵**一句標題**、不餵新聞原文，同名同姓無從分辨，英文名拼成 `Yi-hsin Chen` 也與這個誤判一致。🚩**附帶發現（更嚴重的退路）**：`derive_yt_cover_plan` 失敗時回空 dict，呼叫端退路是 `visual = 標題原句`、`subjects=[]`、參考照 0 張、**肖像規則一條都不注入**——實測撞到一次（本機金鑰 401），最終 prompt 的 Subject 直接變成「陳一新警告人工智慧威脅政權」。**這條退路比正常路徑更容易捏臉。**❌**使用者假說「跟創意階梯有關」已在程式碼層推翻**：雙則底圖用的是十點那支 `editor_formats.COVER_VISUAL_PROMPT_TEMPLATE`，**模板裡沒有 `{creativity}` 槽**（單則走的 `YT_COVER_VISUAL_PROMPT_TEMPLATE` 才有），`_cover_panel_image` 的參數列也沒有 creativity。本機實測最終 prompt **出現 `creativ` 0 次**。⚠**但這件事本身是缺陷**：同一顆創意拉桿，單則有作用、雙則完全沒作用，另開 **A7** 列管。 |
| B46 | **YT 直播直標：LIVE 角標與直標之間有一段空欄** | 曹雪卿 0915 回饋文件（AI CG回饋，hikaru.tsao@gmail.com） | ⬜ 🌊**第二波**（2026-09-15 使用者裁定：等第一波收工才動工） | 使用者原話「YT直播直標，我在提供底圖? 另外LIVE和標中間有空欄」。監督調成品核對（`20260915-131247-ec36fb8a7ae8.png`，13:12:47）：LIVE 紅框下緣約 y=175，直標深色塊上緣約 y=240，**中間約 65px 垂直空隙**，兩者左緣都對齊 x=50。前半句「我在提供底圖?」另開 D18（是產品行為問題，不是缺陷）。 |
| B47 | **播出鏡面挖左框／挖右框，位置都偏高** | 曹雪卿 0915 回饋文件（AI CG回饋，hikaru.tsao@gmail.com） | ⚖ **2026-09-15 使用者已裁：不修位置，維持蓋框預設 OFF**｜🌊**第二波**（若之後要做示意圖標示才排） | 使用者原話「播出鏡面挖左框／左邊挖框位置偏高」「播出鏡面挖右框／右邊挖框位置偏高」，兩條各附截圖（字多版、字少版）。監督當時查不到對應成品（後台 0915 曹雪卿 19 筆沒有播出鏡面類型），原本要向她問是哪一筆。⚖**使用者裁決（2026-09-15）**：「我認為不可控因素太大，蓋框位置太死，無法根據生成圖片判斷哪裡可以蓋框，所以我才預設關閉 OFF 蓋框，只讓生圖模型自己留空位。」**所以這不是缺陷，是已知的設計取捨**——蓋框位置是固定座標，但生圖的構圖每次都不同，沒有辦法從成品反推哪裡蓋得下去；與其蓋錯，不如預設關閉、由生圖模型自己在構圖時留白。**動作**：①不要去調挖框座標（調了也只是換一種蓋錯）；②要查的是**曹雪卿為什麼會遇到**——預設是 OFF，代表她自己開了，需要確認介面上有沒有講清楚開了會有什麼後果；③她另外提的「示意圖是否應標示在挖圓框的地方」（F35 ①）在這個前提下**更值得做**，因為那正是「讓使用者自己看得出哪裡能蓋」的替代方案。**本列不再追位置，關掉。** |
| B48 | **長稿消化實際上只有約 140 秒可用，超過就失敗——曹雪卿說的「很容易失敗」其實沒被 B39 解決** | staging `260915-01` 實測（2026-09-15），B39 驗收時發現 | ⬜ **第一波，優先序高** | **B39 只換掉失敗的表示法，沒有降低失敗率。** staging 兩次實測：「字超多」成功（4 分 16 秒），**同一篇改「字多」在約 150 秒失敗**——2 次 1 敗。監督到 staging `/admin` 核對，**成功那筆有紀錄（15:11:36）、失敗那筆完全沒有留痕**（順帶再證實一次 F31）。**根因已對上程式碼**：`main.py:2330` 的死線守門是 `time.monotonic() + DIGEST_TIMEOUT_SECONDS > deadline`，即 `230 - 90 = 140 秒`——**消化只要跑超過約 140 秒就一定會被擋下**，實測失敗在 ~150 秒完全吻合。⚠**注意這裡有個前提可能也是錯的**（跟 B39 當初那個「照 Cloud Run 300 秒設計」的錯誤是同一類）：`DIGEST_DEADLINE_SECONDS=230` 的註解寫的是要避開 **Cloud Run 的 300 秒硬上限**，但 `docs/HANDOVER-2026-08-25.md` 寫 staging／正式站**都跑在 AI 部門自架的 Ubuntu 上、systemd 管理、走 Cloudflare Tunnel**，不是 Cloud Run。**若實際部署形態沒有 300 秒硬上限，230 這個數字就沒有依據**，而且 B39 之後連線已經不會靜默斷線，放寬死線是安全的。**動工前必須先查證部署形態**（問同事、或看那台的 systemd／反向代理設定），**不要憑註解就改數字**——B39 就是這樣踩過一次。**候選方向**（查證後再選）：①確認無硬上限就放寬 `DIGEST_DEADLINE_SECONDS`；②查為什麼「字多」會比「字超多」慢到爆（直覺上工作量更小，這個反直覺本身就是線索，可能是 OpenRouter provider 輪替，見 B39 詳述末段）；③長稿在送出前就預估並提示。**⚠不要一上來就放寬數字**——②那個反直覺沒查清楚，放寬只是把失敗往後延。 📊**2026-09-15 第二輪數據（staging 4 次＋本機 1 次）**：staging 實際跑了 4 次，監督到 `/admin` 核對成功列為 **15:11:36／15:23:42／15:27:04 共 3 筆**，**即 4 次 3 成功 1 失敗（失敗率 25%）**——不是系統性必敗，是間歇性。**本機用同一篇、同一檔位（`standard`）、同一後端（`.env` 原樣：openrouter／claude-sonnet-5）跑一次：59.19 秒乾淨成功**，`finish=stop`（沒截斷）、`completion_tokens=4720`（預算 6000 的 79%）、`provider=Anthropic`，**stdout 完全沒有任何 `[generate] attempt N/5` 行**。⚠**監督先前那三個猜測（截斷／簡體字品質閘／其他品質閘）本輪全部沒有出現，暫時排除但未證偽**（只有 1 次樣本，且這次根本沒失敗）。🔑**真正浮出來的是速度差**：同樣的工作，**本機 59 秒，staging 失敗那次卻在 ~150 秒被死線擋下，差 2.5 倍**。兩個候選解釋，**都還沒查**：**(a) 第二段消化**——`main.py:2457` 的 `apply_photo_availability()` 在「有具名人物且查不到參考照」時**會整個重跑一次 generate()**（只重試一次）。本機那次 stdout **沒有 `[portrait]` 那行**，代表沒觸發；而那篇稿點名了川普／黃仁勳／**陳一新**，陳一新（中國國安部長）很可能維基查無條目 → staging 觸發第二段消化 → **59×2 ＋ 維基查詢與重試 sleep ≈ 140~150 秒，剛好踩到死線守門**。**這個算術對得太準，要優先查。** **(b) OpenRouter provider 輪替**——本機這次落在 `provider=Anthropic`，staging 可能落到較慢的 provider。**✅下一步是零成本的**：不要再花錢亂試（25% 的命中率等於平均要燒 4 次才看得到 1 次失敗）。先看本機那次回應裡的 `portrait_subjects` 有沒有人、再用維基查照的 helper 離線查「陳一新」查不查得到——**兩件都不花 AI 的錢**。若 (a) 成立，B48 的修法跟「放寬逾時」完全無關，而是「第二段消化要不要也吃同一條死線」的設計問題。💡**連帶結論：F30／F31 的優先序應該往前提。** 這次查不下去的根本原因是**正式站／staging 的失敗完全不留痕、成功也不記消化耗時與 provider**；把這兩項補上之後，下一次真實失敗自己就會把答案送上門，不必再花錢重現。 ❌**2026-09-15 監督自己的假說 (a) 已被推翻（零成本查證）**：本機那次的 `portrait_subjects` 只有 `["川普", "黃仁勳"]`，**消化端根本沒把陳一新列進去**（他在稿中是被引述的官員，不是版面要重建場景的人物）；而且離線用 `photo_lookup.find_reference_photo()` 查三人，**川普／黃仁勳／陳一新全部 FOUND**（監督已核對 `lookup_portrait_photos()` 就是包這支 helper，測法有效）。**所以「因為查無陳一新而觸發第二段消化」這個具體原因不成立。** ⚠**但要分清楚被推翻的是「原因」不是「機制」**：`apply_photo_availability()` 只要判定有人查不到就會整個重跑一次 `generate()`，這條路徑仍然是把耗時直接翻倍的最大單一嫌疑。而且 `main.py:3632` 寫得很清楚——**查照時任何例外（網路、逾時）都當成「這位查不到」**，所以 **staging 上只要維基查詢偶發逾時一次，就會觸發整段第二次消化**，而本機網路順暢的情況下永遠複製不出來。**這正好也符合 25% 的間歇失敗率。****這個猜測同樣不要再花錢試**——它跟前一個一樣，沒有日誌就只能空想。🚩**結論：B48 在 F30／F31 補上之前查不下去了。** 需要的三個欄位都不存在於現有紀錄：①失敗完全不留痕（F31）；②成功不記消化耗時；③不記 OpenRouter 的 provider 與有沒有跑第二段消化。**建議把 F30／F31 提到 B48 前面做**，以 25% 的失敗率算，補完後等一天就會有真實樣本自己送上門，比花錢重現便宜也準確得多。 🎯**2026-09-15 本機四次實驗，根因確認（監督已逐行核對 `D:\Downloads\AICG\後台紀錄\20260915\B48本機四次\run*.log` 原始日誌，非採信轉述）**：**病灶是思考把整個 completion 預算吃光，不是逾時、不是 portrait、不是 provider。** 四次共 8 次 API 呼叫，**5 次 `finish=length`、`ratio=1.00`**，日誌原文如 `budget=6000 completion_tokens=6000 reasoning_tokens=6000 ratio=1.00 finish=length TRUNCATED`——**思考吃掉全部 6000，留給 JSON 正文的是 0**，raw content 是空字串（run4 是寫到一半的 unterminated string），於是 `parse failed` → 重試。**run1 連撞兩次 ratio=1.00，第三次還沒開始就被 230 秒死線擋下 → 503**，訊息與 staging 那次一字不差；run3／run4 各撞一次、第二次救回（173s／180s），run2 一次過（108s）。**`[portrait]` 四次全部沒出現 → 第二段消化的猜測正式排除**；provider 八次全是 `Anthropic`，也不是輪替。❗**最關鍵：思考封頂根本沒有生效。** `digest_reasoning_body()` 確實送了 `reasoning.max_tokens=2000`（預設 2000、`.env` 未覆寫、`min(2000, 6000-2500)=2000` 成立），且**四份日誌都沒有「模型不吃 reasoning 上限」那行**（`main.py:1948` 的退路沒觸發），代表欄位是**被接受**的——**但實測 `reasoning_tokens` 跑到 5442～6000，連成功那三次也是 1603／2038／2144，全部觸頂或遠超。****結論：OpenRouter／Anthropic 收下了這個欄位但沒有遵守。** 💡**修法方向（監督建議，待裁 D21）**：`DIGEST_MAX_TOKENS=6000` 是照「total 觀測最大值再留六成」訂的，而那份觀測的前提是**思考封頂有效**。封頂既然無效，6000 對這個模型就是不夠。同一支程式的 `MAP_DIGEST_MAX_TOKENS` 已經是 **10000**，有現成先例。**放寬幾乎不花錢**——token 照實際生成量計費，沒用到不付；而現在每次截斷是**整整 6000 token 全部白付**、再付一次重試，**放寬反而省錢**。⚠**但不要只放寬就收工**：要同時查 `reasoning.max_tokens` 為何沒生效（參數名／模型是否支援／是否該改用 `reasoning.effort`），否則思考量會跟著規則條數繼續漲，放寬只是把牆往後推。 |
| B49 | **貼「版面指令稿」（整段用 `[欄位]` 標記）時，「不改字」在結構上就不可能成立——使用者無從選起** | 許岱軒 2026-09-15 15:40:39 正式站實例＋使用者回報 | ⬜ 🌊**第二波**（2026-09-15 使用者裁定列入下一波）｜**設計已定調，見 D20** | **現場**：使用者貼的不是新聞稿，是一份版面規格——`[大標題 變色強調] 第二波155架軍機`／`[次標題] 美軍為救1名飛官出動`／`[左 標籤+數字圓圈] 轟炸機／4`／`[內文1]`／`[背景 小插畫]` …，意圖是「`[]` 裡的是指令，`[]` 外的字一個都不要動」。**但這個意圖在現有的檔位選單裡無處可選**：選「不改字」應該最接近，選「字少」則會把他挑好的字改掉。✅**監督查證：這不是使用者誤解，是程式結構上就矛盾**——`main.py:2067` 的 `_VERBATIM_MARKER_RE = re.compile(r"\[標題\]|\[內文小標\]|<蓋章>|<底帶>|[<>]")` **只認這四個寫死的標記**。`verbatim_fidelity_problem()`（`main.py:2128`）拿「原文」與「輸出」各自剝掉這些標記後**逐字比對**，而使用者寫的 `[大標題 變色強調]`、`[左 標籤+數字圓圈]`、`[背景 小插畫]` **通通不在白名單裡**，所以會被當成**原文的一部分**參與比對。**結果是二選一，兩個都壞**：①模型照使用者意思把標記當指令執行、只輸出內容字 → 比對缺一大串字 → 判定不符 → 重試 5 次 → 使用者收到「AI 回傳內容異常」（`502`），**而且完全看不出是自己的寫法被擋掉**；②模型為了通過比對把 `[大標題 變色強調]` 原樣抄回去 → **最終 prompt 裡那條 `Any text written as [text] → Remove brackets → Apply highlight color` 會把「大標題 變色強調」這幾個字當成要強調的內容畫到圖上**。⚠**根本衝突**：`[...]` 這個語法**已經被系統佔用為「強調這段文字」**（見最終 prompt 的 Text Styling Rules），使用者卻拿它當「欄位標籤」。**同一個符號兩種語義，不先分開就無解。** 📌**今天那次之所以成品漂亮**（`20260915-154039-fd5b6c672642.png`，標記全部正確當成版面指令、文字逐字保留），**是因為他沒有選「不改字」**——但**後台沒有記檔位（F30），所以連他當時選了什麼都查不到**，這本身就是 F30 該做的又一個理由。**修法要先裁 D20**（語法怎麼分家），程式面最小修正是把 `_VERBATIM_MARKER_RE` 從白名單改成「任何 `[...]` 一律剝掉再比對」——只影響比對、不影響輸出，可讓「不改字」至少不再誤殺；但這會讓真正想強調的 `[重點]` 在比對階段也被剝掉（可接受，因為比對兩邊同時剝）。**與 B11（缺獨立指令欄位）／B12（行內指令時靈時不靈）同一叢，建議一起看。** ✅**2026-09-15 使用者補充：成功那次選的是「字少」（預設）。監督查證後回答「是設計上支援的用法，但不是可靠的用法」**——`USER_INSTRUCTION_RULES`（`main.py:1568`，組裝時放最後）第 4／5 條明文寫了：指令文字**永遠不得出現在 variable**，且「使用者給的已經是完稿 CG 腳本」時 variable 必須逐字重現，**「VERBATIM MODE OVERRIDES … INCLUDING THE SIMPLIFIED MODE OVERRIDE」**。所以選字少會生效不是巧合，是這條軟性 override 在擋住字少的壓縮。❌**但它沒有任何強制力，而且這次已經改了字**：使用者輸入 `[左 標籤+數字圓圈] 轟炸機／4`，**最終 prompt 裡是 `轟炸機4架, 戰機64架, 加油機48架, 搜救機13架`——消化端自己加了「架」**（監督從後台最終 prompt 原文核對，不是從成品猜的；亦即不是生圖模型加的）。規則 5 白紙黑字寫 `do not … add one single character`，它加了，**而且沒有任何閘門會抓**——`verbatim_fidelity_problem()` 只在 `density == "verbatim"` 時才跑（`main.py:2404` 附近）。**這次加「架」剛好讓圖更好讀，但同一個機制同樣可以吞掉一個數字或壓掉一句話，一樣不會被擋。** 🚩**所以現況是：「字少」是目前最不壞的選擇，不是正確答案。** 使用者要的「只改指令、其餘一字不動」在系統裡**沒有一個受保護的選項**——真正逐字受保護的只有「不改字」，而那一檔對這種輸入結構上就不成立（本列上半）。**這也是 D20 要裁的真正理由**：不是介面標示問題，是缺一個「受閘門保護的照排模式」。 ⚖**2026-09-15 使用者裁決：目標是把「不改字」做對，不是換語法也不是加檔位** ——「理想是不改字，但讀懂 `[]` 等等符號，是指令不該入題。但要考慮使用者真的把 `[]` 當作文字不是指令時，還是要出現 `[]`。」詳見 **D20**。**本列的程式面工作**：①`verbatim_fidelity_problem()` 的比對要兩邊同時剝掉指令型 `[...]`；②「不改字」要把指令型 `[...]` 排除在 variable 之外（呼應 `USER_INSTRUCTION_RULES` 第 4 條）；③加逐字閘門保護——現在選「字少」之所以能動，全靠一條沒有強制力的軟性 override，實測已經證明它會被違反（消化端擅自把「轟炸機／4」改成「轟炸機4架」）。**⚠ D20 還有一個子問題沒裁（使用者怎麼表達「這個 `[]` 是字」），動工前要先問。** |
| B50 | **重試是純擲骰子——五次 attempt 送的是位元組相同的請求，失敗一次跟失敗五次的機率不獨立也不收斂** | 監督讀 B48 四次實驗日誌後提出（2026-09-15） | ⬜ **第一波，下一批第一優先**（2026-09-15 使用者採納監督建議：與 D21 甲案同批做） | **現況**：`generate()` 的重試迴圈每一次 attempt 都用**完全相同的 payload**重送（同 model、同 prompt、同 `max_tokens`、同 `reasoning.max_tokens`）。B48 實測證明失敗模式是「思考失控吃光預算」，而這是**同一個請求的隨機性**——所以重試等於**再擲一次同一顆骰子**：run3／run4 第二次擲贏了（救回），run1 連輸兩次（真的失敗）。**重試次數 `DIGEST_ATTEMPTS=5` 看起來很多，但死線只夠擲 2 次**（見 B48）。💡**修法**：重試時**要改變條件**，讓它收斂而不是碰運氣。至少三種可疊加：①偵測到 `finish=length` 且 `reasoning_tokens ≈ budget`（這個簽章 `log_digest_usage()` 已經算得出來，`ratio` 與 `TRUNCATED` 旗標都現成）時，**下一次 attempt 改用更小的思考預算**（`reasoning.max_tokens` 降到下限 1024，或改送 `reasoning.effort=low`）；②或反向——下一次把 `max_tokens` 再加碼；③兩者擇一連續兩次仍失敗才放棄。**這比單純放寬預算更根本**：放寬只是讓骰子的壞面不那麼致命，改變重試條件才是讓它收斂。⚠**與 D21 甲案的交互作用，動工前務必一起看**：從 B48 四次的總耗時反推，單次呼叫大約 **85～105 秒**（run2 單次成功總耗時 108.59s、run3 兩次共 173.32s、run4 兩次共 180.30s），而 `DIGEST_TIMEOUT_SECONDS=90` 是**單次呼叫**的上限——**現在已經幾乎沒有餘裕**。甲案把 `DIGEST_MAX_TOKENS` 放大之後，思考失控的那次會**生成更多 token、跑更久**，**很可能從「回傳截斷結果」變成「撞上 90 秒單次逾時」**，那樣甲案就白做了。**所以甲案的驗收一定要量「單次呼叫實際耗時」，不能只看成敗**——這正是本專案一再踩到的同一種坑（B39：預算照著錯的天花板設計）。 |
| B51 | **追加修改把封面置框：五個封面版型的 Logo／角標消失＋出現四邊安全框** | 王結玲（後台紀錄，2026-09-15 使用者回報） | ⬜ **下一波優先要修**（2026-09-15 使用者裁定）｜根因已證實，Logo 那半待實證 | 使用者原話「24HLIVE 他重複修改圖之後 24HLIVE的相關LOGO都不見了 程式有BUG 沒有壓LOGO」。**程式碼初查（監督，2026-09-15）**：`compose.compose_yt_live24_cover` 裡角標、日期、兩層版 Logo 都是**無條件**貼的（只有標題受 `draw_title` 控制），註解也明寫「那三個是頻道識別，交給模型畫就會有錯字與走樣的版本」——**所以只要成品有經過這支函式，Logo 不可能消失**。因此病灶應該在**追加修改回來那條路徑有沒有再進 compose**：`app.js` 的 `refineSourceFromResponse` 只在 `data.mode === 'composite'` 時才給得出無文字底圖，AI 標題模式下拿到的很可能是**已經貼過 Logo 的成品**，送回生圖模型改圖時 Logo 被一起重畫／洗掉；若之後又走了某條不重貼固定元素的分支，Logo 就永久消失。**這正是 `_cover_ai` docstring 已經警告過的情境**（「把貼過 Logo 的成品餵回生圖模型，模型會把 Logo 一起重畫，那是播出事故」）。**下一步（零成本優先）**：①到正式站 `/admin` 撈王結玲今天 24H LIVE 的紀錄，確認是第幾次改圖開始掉、`model` 欄是不是 `yt-cover:overlay`；②本機用同一條路徑連改三次，看第幾次開始 compose 沒被呼叫。**不要先改程式**。⚠**這是播出識別消失，嚴重度等同事故，不是美感問題。** 🎯**2026-09-15 使用者補充第二個症狀，根因當場對上：「王結玲修改過後居然出現四邊安全框，這是 YT 版型沒有的東西」。****已在程式碼層證實，兩個症狀同一個源頭。** `app.js:3602-3603` 的 `handleRefine` 送出的是 `safe_frame: isCover ? false : state.safeFrame`（封面強制 false，**寫對了**）**但下一行 `safe_frame_profile: state.currentRole` 是無條件送的，沒有跟著封面例外**。後端 `refine_image` 拿這兩個值去問 `resolve_frame_plan(role, safe_frame)`（`main.py:3533`），而那支對編輯版是**「兩檔都是滿版生成＋後製，開關只決定後製方式」**——本機實跑證實：`resolve_frame_plan('編輯', False)` → `(True, True, '編輯')`，`resolve_frame_plan('記者', False)` → `(False, False, '記者')`。**也就是說：只要使用者身分是「編輯」，YT 封面按下追加修改就一定會被置框，`safe_frame=False` 完全無效。**王結玲是編輯，YT 封面又本來就是編輯在用的工具（`app.js:3064` 連生圖後端都因為編輯身分改走 gpt），**每一次改圖都會中**。**這同時解釋了 Logo 消失**：置框是把整張畫面縮放／填進對位框，程式先前貼好的角標與兩層版 Logo 會跟著被縮放或推出版面，而不是 compose 沒有貼——與前一段的初判（懷疑沒進 compose）相比，**這個解釋只需要一個原因就同時說明兩個症狀，優先度更高**。⚠**但 Logo 這一半仍是推論，尚未實證**：置框後前端是拿 `data.source_image_base64`（置框前原圖）去 recompose 的，理論上 Logo 應該還會被重貼一次，所以還缺一步——**要確認 `finalize_image_result` 置框後回的那張『置框前原圖』到底是不是真的沒被動過**，以及 recompose 那一發有沒有真的跑完。**修法方向（待裁，不得自行動工）**：`handleRefine` 在 `isCover` 時把 `safe_frame_profile` 一起改送記者（最小改動），或在 `refine_image` 端讓 `safe_frame=False` 對編輯版也有效（動到編輯版 2026-08-19 的既有裁決，**不能自己決定**）。**下一步**：①撈王結玲後台紀錄確認身分欄與 `model` 欄；②本機用編輯身分跑一次 YT 追加修改，看成品是不是同時有框、沒 Logo。 📋**2026-09-15 全版型盤查結果（使用者指示「一定不止這個版型，其他版型也查」）**：全站 8 個版型逐一查過，**`safe_frame_profile` 漏接影響的是「所有會走追加修改的封面版型」，不只 24H LIVE**——`yt_live_cover`（國內外新聞直播）／`yt_hourly_cover`（整點直播）／`yt_live24_cover`（24H LIVE）／`yt_hot_cover`（今日熱搜）四個 YT 版型，加上**十點不一樣的 AI 版**，共 **5 個版型**走同一行 `safe_frame_profile: state.currentRole`，只要使用者是編輯就一定被置框。✅**兩個版型不受影響**：`yt_vstrip`（直播直標）的 `hides` 明寫 `refine: true`、而且完全不生圖，沒有這條路徑；`default`／`broadcast` 是 CG 本身，`safe_frame` 與 profile 成對送出、語意一致，**這才是這兩個參數原本正確的用法**。❗**盤查另外揪出一個更嚴重的漏網**：**十點合成版**連封面例外都沒吃到，另開 **B52**。💡**為什麼只有「修改過後」才看得到框**：封面的**初次生成**走 `_cover_panel_image`／`_cover_ai`／`_yt_cover_full_image`，那幾支是直呼 `generate_image_raw` 並自帶 `safe_frame=False`，**繞過 `finalize_image_result`**，所以不會置框；只有 `refine_image` 這一條會問 `resolve_frame_plan`。**這與使用者「修改過後才出現」的描述完全吻合，是根因成立的旁證。** |
| B52 | **追加修改的「封面例外」漏掉十點合成版：它被當成一般 CG 改圖，改完不重貼 Logo／節目標籤，還吃 CG 的比例與安全框設定** | 由 B51 全版型盤查展開（2026-09-15，使用者指示「一定不止這個版型，其他版型也查」） | ⬜ **下一波優先要修**（2026-09-15 使用者裁定，與 B51 同批）｜程式碼路徑明確，尚未實拍證實 | `app.js:3585-3590` 的封面判定是 `isYtCover = inputs === 'yt_cover'`、**`isTenCover = inputs === 'cover' && state.tenCoverMode === 'ai'`**——**十點不一樣的「合成版」(`tenCoverMode === 'composite'`) 兩個都不符合，`isCover` 為 false**，於是整包封面例外全部失效：①`aspect_ratio` 變成 `currentAspectRatio()`（CG 的比例，編輯＋安全框 ON 時是 16:9、OFF 時也是 16:9，但那是 CG 的語意不是封面的）；②`safe_frame` 送的是 `state.safeFrame`——**而十點與 YT 五個版型的 `hides` 都寫了 `safeFrame: true`，前台根本看不到那顆開關**，值是使用者在 CG 模式留下來的殘值；③`text_free` 為 false；④**最嚴重：`app.js:3615-3617` 的 `shown = isYtCover ? recomposeYtCover : isTenCover ? recomposeTenCover : data`，合成版落到 `data`——改完的模型圖直接當成品顯示，完全不走程式後貼**，所以正版 Logo、十點節目標籤、日期全部不會被重貼回去。⚠**而且 `safe_frame` 送 false 也沒用**：見 B51，編輯身分在 `resolve_frame_plan` 一律回 `(True, True, '編輯')`。**待實證**：十點合成版的追加修改按鈕是否真的開放給使用者（`hides` 沒有關掉 refine，推測開放），以及改完的成品是不是真的沒有 Logo。 |
| B55 | **原圖放置的照片被生圖模型整張重畫：創意階梯應該只能改標題設計，不得重置原圖** | 王結玲（2026-09-15 使用者回報＋當場裁定規則） | ⬜ **下一波優先要修**（2026-09-15 使用者已裁規則）｜與 B51／B52 同一批 | **使用者裁定的規則**：24H LIVE 只上傳一張「原圖放置」＝**原圖滿版放置**，**創意階梯只能改標題設計，不可重置原圖**。**程式現況（監督逐段核對）**：意圖是對的，但**只靠 prompt 講，沒有任何驗證**。單張 asis 時 `_yt_cover_background` 走 `compose.crop_background_16x9(raws[0])`——**純裁切、不打生圖模型，這一段正確**；接著 `ai_over_base` 成立（`title_mode=ai` ＋ asis≥1），底圖當**唯一附圖**送進 `_yt_cover_full_image`，並由 `with_base_image_note` 注入 `AI_TITLE_BASE_IMAGE_NOTE`：「Reproduce it as the picture: same subjects, same framing... Do not replace it with another scene, do not re-compose, re-crop, mirror or zoom it」。❗**但這條路是把整張畫布交給生圖模型重畫**——模型不可能逐像素複製，照片必然被重新生成一次，那段 note 只是**請求**，`generate_image_raw` 之後只有 `verify_output_aspect_ratio`（只驗比例），**完全沒有任何『底圖有沒有被改掉』的檢查**。使用者看到的就是原圖被重置。💡**修法方向（監督建議，待裁）**：原圖放置＋AI 標題時，不要讓模型重畫整張。甲｜**讓模型只產標題圖層**（透明／純色底），程式把它疊到未經觸碰的原圖上——創意階梯照舊只影響標題設計，照片保證是原檔；乙｜退回程式壓字（`title_mode=composite`），代價是標題失去設計感，等於 2026-09-13 那條裁決作廢，**不建議**；丙｜維持現況但加驗證（比對輸出與 base 的相似度，不像就重試）——治標且會多花錢。⚠**甲與現有的 `_cover_ai`／`_yt_cover_full_image` 兩段式架構衝突最小，但等於新增一種輸出模式，要先裁。****待實證**：王結玲那次的 `model` 欄是不是 `yt-cover:asis` 加生圖模型（＝確實走了 ai_over_base 這條），要撈後台確認。 ⚖ **2026-09-15 使用者已裁：甲，並補上張數分流規則**（原話）：**「如果使用者只有附一張圖 要求原圖放置 就只畫標題；如果超過兩張都是原圖放置 就需要 AI 生成融合」**。即：**asis = 1 張 → 照片是原檔，模型只產標題圖層，程式疊上去，一個像素都不准動**；**asis ≥ 2 張 → 允許 AI 生成融合**（多張本來就要合成一張，模型介入是必要的）。🚩**動工前要注意：≥2 張的現況跟「融合」不是同一回事。** 目前 `_yt_cover_background` 在 2–4 張時走 `compose.split_backgrounds(raws)`，那是**程式硬切左右／三切／四切**（2026-09-06 使用者裁決），切完再進 `ai_over_base` 交給模型重畫——**是「硬切格子再被重畫」，不是「融合」**。（live24 雙則那條才有羽化 `blend_backgrounds_lr`，但那條要兩個附圖位都有東西才成立。）**所以這次裁決對 ≥2 張的部分等於改了行為，不是照現況實作**，要先確認使用者要的是①保留現有的切格版面、只是接受模型重畫；還是②真的改成讓模型把多張照片融合成一個連續場景。**這一題監督不自行決定，實作前要問。** 1 張那一半的規則明確、可直接動工。 ❓**2026-09-15 使用者追問**：「原圖放置 只要選了 能不能要求模型真的不要改原圖的這張圖，其他地方或其他圖片有指定 AI 改都可以改，但使用者選了原圖放置**這張圖**就是不可以改」。**監督回覆（已查證）**：①**「要求模型」這個槓桿本身無效**——`AI_TITLE_BASE_IMAGE_NOTE` 已經寫到不能再死（same subjects／same framing／do not re-compose／re-crop／mirror／zoom），照樣被改。生圖模型是**重畫整張**、不是編輯圖層，prompt 永遠只是請求，沒有保證。②**保證只能來自程式後製回貼**，而且回貼可以**分區**，正好對應「只鎖這張、其他照改」：`asis` 那格的矩形回貼原檔，`aiedit`／實景那格不回貼，讓模型自由發揮。③**現成的保證路徑其實已經在跑**：不勾「標題由 AI 生成」（`title_mode=composite`）時，單張 asis 走 `crop_background_16x9` 純裁切、程式用 PIL 壓字，**照片一個像素都沒動**。問題**只出在 2026-09-13 那條裁決**（原圖放置＋AI 標題不再強制程式壓字）打開的 AI 標題分支。④**三個做法**：甲｜**差異遮罩回貼**——比對模型輸出與 base 的逐像素差異，模型真的畫了字的地方保留模型像素，其餘一律還原成原檔。AI 標題的設計感全留、照片保證原檔，是唯一能同時滿足兩邊的做法；風險是模型若整張色調位移會讓遮罩整片觸發，**必須加面積防呆**（改動面積超過門檻就判定失敗，退回程式壓字，退回不用再打模型、不花錢）。乙｜**限定字帶**——AI 標題只准畫在指定帶狀區，帶外一律回貼原檔；保證最強但砍掉標題落點自由度。丙｜**分區回貼**（多圖情境）——只回貼 asis 格，其他格不動；這條是甲／乙的正交補強，**多圖時必做**。⑤**監督建議：甲＋丙，乙不採**。⚠**順帶查到的文件缺陷**：`main.py:4326` 的註解寫「有任何 asis 就強制 composite（**真照不進生圖模型**）」，但 `main.py:5397` 的 `ai_over_base = has_asis and req.mode == COVER_MODE_AI` 表示**十點的 AI 模式同樣把真照送進模型**——註解已過時且與程式相反，會誤導後續實作，動工時一併改掉。 |
| B53 | **YT／十點封面的「補畫面描述」只餵一句標題、不餵新聞原文，導致認錯同名人物並拼錯英文名，肖像查照因此失敗** | 由 B45 根因調查獨立出來（2026-09-15） | ⬜ **下一波優先要修**（2026-09-15 使用者裁定：「這個也列入下一波」，緊接在陳一新查照真因查證之後）｜原列 🌊第二波，已提前 | `derive_yt_cover_plan` 給模型的 material 只有 `Headline: <標題>`（＋分行提示、＋使用者指令），**沒有任何新聞原文**。實測後果（標題「陳一新警告人工智慧威脅政權」）：①模型把人認成淡江大學教授陳一新（visual 寫「一位年長學者⋯背景虛化的書架」），不是中國國安部長；②`portrait_subjects_en` 回 `'Yi-hsin Chen'`（臺灣拼法），**查維基查無**；改用條目名 `'Chen Yixin'` 立刻查到正確的人。**這是 B45『捏造臉孔』的上游**——查照失敗才會退回不生成臉孔，而 visual 又命令要一張臉。⚠`YT_COVER_DERIVE_SYSTEM` 第 3 條已經寫明「empty string ONLY when you genuinely do not know it；never guess a spelling you are unsure of」，**模型並沒有遵守，它猜了一個**。**候選方向（待查，不要直接改 prompt）**：①把新聞原文一起餵進去（前台這條線使用者本來就只貼標題，可能根本沒有原文可餵——要先確認）；②查照端對同一個人多試幾種拼法；③英文名拼不確定時寧可回空字串並明確走「不畫臉且 visual 不准描述臉」。 ⚖❌**2026-09-15 使用者提出防呆方向，監督查證後不成立（但查出真因）**。使用者原話：「陳一新本來就是中文名稱，用英文名稱當然會有機率找錯人，應該要防呆，**如果英文找不到就找中文**」。**查證結果：這個防呆已經存在，而且順序比使用者要求的更早——中文本來就是第一順位。** `photo_lookup.find_reference_photo()` 的候選順序是 `candidates = [name, *alt_names]`，`name` 就是中文名，`DEFAULT_LANGS=("zh","en")` 又讓**每個候選都在中英兩個維基各查一次**；函式 docstring 也明寫「中文優先（臺灣新聞的人物多半中文條目較貼近本地認知）」。**所以不是『先英文再中文』，是『中文查完查不到才輪到英文』。** **本機實測（不花錢，維基 API）**：`陳一新`＋無英文名 → **查無**；`陳一新`＋`Yi-hsin Chen`（系統實際產出的拼法）→ **查無**；`陳一新`＋`Chen Yixin` → **找到**。逐語系拆開看：`zh 陳一新 → 無`、`en 陳一新 → 無`、`zh Chen Yixin → 無`、`en Chen Yixin → 有`、`zh/en Yi-hsin Chen → 無`。🎯**真因查到了：中文維基的「陳一新」是消歧義頁，沒有首圖。** 直接打維基 API 確認：`zh.wikipedia` 的 `陳一新` 重導向到 `陈一新`，該頁 `pageprops` 帶 `"disambiguation":""`、**完全沒有 `pageimages`**，`_lookup_lang` 因此回 `None`。**中文這條路在這個人身上是死路，補幾次都一樣。** 💡**但使用者的直覺指到了一個真的缺口，只是位置不同**：**消歧義頁＝這個名字在中文世界本來就有多個人**（淡江教授／中共國安部長），而程式現在只是「查不到就算了」，**完全沒有利用這個訊號**。同一份資料其實已經告訴我們「這個名字有歧義、需要更多上下文才能定人」，那正是 B53 主症狀（只餵標題導致認錯人）的客觀證據。**建議修法（待裁）**：①**B53 本體照修**——餵新聞原文給 derive，英文拼法才會對（`Chen Yixin` vs `Yi-hsin Chen` 的差別就是有沒有上下文）；②**偵測到消歧義頁時不要沉默**，記進紀錄並視為「此人需要上下文」的明確訊號，而不是與「查無此人」混為一談；③**不要加「中文全文搜尋」當退路**——`find_reference_photo` 的 docstring 已經記載實測結果：4 個譯名裡 2 個搜到完全不相干的條目（阿拉奇→阿布拉莫維奇、巴薩尼→威尼斯商人），**抓錯人比查不到嚴重得多**，這條路先前已經被否決過，不要重蹈。 |
| B54 | **「補畫面描述」失敗時的退路完全沒有肖像保護，比正常路徑更容易捏臉** | 由 B45 根因調查獨立出來（2026-09-15，監督本機實際撞到一次） | ⬜ 🌊**第二波**（跟 B45 同一條線） | `derive_yt_cover_plan` 失敗時回空 dict，呼叫端（`resolve_yt_cover_plan`／`yt_dual_panel_plan`）的退路是 `visual = 標題原句`、`subjects = []`、`english = []`，於是 **`_cover_apply_portraits` 一條肖像規則都不注入、參考照 0 張**。監督本機實際撞到（金鑰 401），最終送給生圖模型的 Subject 就是「陳一新警告人工智慧威脅政權」這句話本身——**一個具名真人的名字直接當畫面題目，而且沒有任何「不准捏臉」的約束**。**正常路徑至少還會退回 `no_reference`（不生成臉孔）；這條退路連那層都沒有。**修法方向：推導失敗時應該視為「有具名真人但查不到照片」的最保守情形，而不是視為「沒有人」。 |
| B56 | **使用者附圖上本來就有的文字，現況被硬性要求「不要複製」，等於一律被改掉／變亂碼，而且使用者無法控制** | 2026-09-15 使用者主動提出（監督當場查證確認為真） | ⚖ **2026-09-16 使用者已裁：要做**——規則要區分「保留附圖上本來就有的字」與「不要憑空抄新字」，不再一律禁止複製｜⬜ **待查怎麼處理**（2026-09-15 使用者：「補一筆紀錄要查怎麼處理」）｜落地方式需一條 D 裁決 | **使用者原話**：「我先前沒有想過，使用者附圖的圖上本來有文字，這些文字可能被 AI 改圖改掉。我的想法是，讓使用者在指令欄補充：圖要怎麼放置、要怎麼 AI 改、字可不可以改、還是去除文字只要圖…等等」。**監督查證（`news_prompt.py`）——問題比預期嚴重，現況不是「可能被改掉」，是「規則叫模型一定要拿掉」**：①`USER_REFERENCE_AIEDIT_RULES`（單張）與 `USER_REFERENCE_AIEDIT_FUSION_RULES_TEMPLATE`（多張融合）都硬寫 `Do not copy readable text or brand marks visible inside the attached image`——**AI改圖一律去字，寫死在 prompt 裡，使用者沒有任何開關**。模型做不到「乾淨去字」時，結果就是亂碼或自創文字，正是使用者看到的現象。②對照組：`USER_REFERENCE_ASIS_MULTI_RULES_TEMPLATE`（原圖放置）反而明文允許保留——`Any brand marks, logos, readable text or real human faces already present in the attached images may remain exactly as supplied`。**同一張圖選 asis 就留字、選 aiedit 就去字，差異是硬編的、前台沒說明。**③**使用者構想的「寫在指令欄」現況會撞到兩道牆**（都在 `USER_REFERENCE_AIEDIT_INSTRUCTION_TEMPLATE`）：**牆一**——那段明文把指令欄定義成「`a direction about what to change in the picture, never words to render — do not write any of it, or any translation of it, anywhere in the image`」，所以寫「保留圖上的中文字」極可能被理解成「不要畫這幾個字」，**語意正好相反**；**牆二**——那段的權限只凌駕「the content does not change」**那一句**，並明文寫 `It does not relax the brand-mark, human-face or NAMED REAL PERSON rules above`，而去字那條是與商標同一行（`readable text or brand marks`），**指令欄吃不動它**。👉**結論：照現況直接叫使用者在指令欄寫，不會生效，必須改 prompt 架構。** 💡**待裁的落地方式（監督列，不自行決定）**：甲｜**在用途下拉旁加一個「圖上文字」小選單**（保留原字／去除文字／交給 AI 判斷），選項直接對應三段互斥的 prompt 區塊，**明確、可測、不靠模型解讀自然語言**；乙｜**純指令欄**，照使用者原構想，但要把上面兩道牆拆掉（指令欄改成可凌駕去字規則、並區分「要畫的字」與「對既有字的處置」），**彈性最大但最難驗收，也最容易被模型誤解**；丙｜甲＋乙併用，下拉定調、指令欄補特例。**監督傾向甲**——這一題的選項本來就只有三種，用自然語言表達反而失真。⚠**還要查的**：①「去除文字只要圖」在生圖模型上到底做不做得到（模型是重畫，去字區塊會被它自己重新編造，需實拍驗證，屬**要花錢的驗證**）；②`asis` 若採 B55 的回貼方案，圖上原字自然全保留，**那條路不需要這個開關**——所以本條的適用範圍只有 `aiedit`／`scene`，動工前要先把範圍講死；③前台現況完全沒有向使用者說明 asis 留字、aiedit 去字的差異，屬獨立的說明缺陷，是否併入本條待定。 |
| B57 | **字數拉桿在正式站的消化模型上疑似大幅失效：選「字超多」只拿到約「字少」的份量** | 2026-09-15 使用者看 F38 成品時當場質疑「應該要測字超多，不是測字少」，追查後浮出 | ⚖ **2026-09-16 使用者已裁：兩個一起做**（prompt 下限＋後端塊數防呆，一次到位）。⚠**會改 prompt 文字＝撞凍結快照，動工前要先要到重凍預算**｜⬜ **根因已確認，待裁修法**（2026-09-15）｜真因＝`maximum` 只解除上限、沒有下限，**10 次分布證實兩個角色都會隨機塌陷、機率相近**｜🔻**已撤回「編輯版系統性偏少」與「今天的回歸」兩個結論——都是小樣本假象**｜另發現乙型塌陷（內容在但不拆塊）｜**監督建議先做後端塊數防呆（不撞凍結快照），prompt 下限併入下次重凍** | **事實（皆有原始檔佐證，非轉述）**：本機用 `density="maximum"` 打 `/api/generate`，同一篇 D16 的 276 字颱風稿，消化端實際產出 **6 塊／內文 49 字**（含標題 61 字）——標題「銀河颱風逼近海上警報生效」12 字，六塊小標分別 9／10／8／8／7／7 字。原始檔：`D:\Downloads\AICG\後台紀錄\20260915\解析度對照\reso_digest_payload.json`＋`reso_digest_response.ndjson`。**對照 D16 同一篇稿的實拍**：字極少 約 41 字、字少 約 39 字、字多 131 字、**字超多 123 字**。👉**現在的「字超多」只拿到 49 字，落在 D16「字極少／字少」那一檔的量級，不到字超多的四成。** 若成立，等於**頂端兩檔實質塌陷**，與 D16 當初「內文兩軸級距成立」的結論相反。**request 層已排除**：`density` 是 pydantic Literal，值不合法會 422，不存在「靜靜落回 standard」；payload 實檔可驗。🚩**但監督必須先講一個還沒排除的干擾項，不要急著當成模型缺陷**：**這次與 D16 有兩個差異，不是一個。** ①消化模型：D16 是 `gpt-5.5`（`DIGEST_BACKEND=native`），現在 `.env` 是 `DIGEST_BACKEND=openrouter`／`DIGEST_MODEL=anthropic/claude-sonnet-5`（**與正式站相同**，所以若成立就是線上現行行為）；②**角色**：D16 那輪是**記者版**，這次 payload 是 `"role": "編輯"`。**編輯版本來就可能因為版面較小而給較少文字**，這個差異足以單獨解釋一部分落差，**在排除它之前不能斷言是模型問題**。✅**下一步（便宜、決定性、不用生圖）**：用**現行模型**、**同一篇 276 字颱風稿**、**記者版**、`density=maximum` 只打消化端一次，數塊數與字數。與 D16 的 123 字直接可比，**只花消化 token、不花生圖的 $0.04**。若記者版仍然只有 50 字上下 → 確認是模型／規則失效，B57 成立；若記者版回到 120 字上下 → 落差來自角色，B57 改成「編輯版的字超多偏輕」另議，嚴重度大幅下降。💡**可能的關聯（未證實）**：0911 查過「思考量隨約束數而非字元數成長」，以及 B48 的「思考吃光 completion 預算」——若 claude-sonnet-5 把預算耗在思考上，正文自然被壓縮。**這條只是候選，不要當成結論。** ✅**2026-09-15 干擾項已排除，角色是主因，模型無罪；但監督再往下挖一層，找到真正的根因。****決定性對照（只打消化端、沒生圖）**：同一篇稿、同一個模型（`claude-sonnet-5`）、`density=maximum`，**唯一改動是角色**——**記者版 8 塊／138 字**（逐塊：18／15／25／12／14／19／18／17），**編輯版 6 塊／49 字**。記者版不但回到 D16 量級，還比 D16 的 123 字略多。👉**所以 `claude-sonnet-5` 沒有把字超多做壞，D16 的級距在記者版上依然成立。**證據：`b57_digest_payload.json`／`b57_digest_response.ndjson`／`b57_char_count_report.txt`。🎯**但「編輯版就是比較輕」不是可以接受的答案——監督把兩個角色的消化 prompt 逐段 diff，找到真正的根因，而且它同時傷到兩個角色**：①先排除兩個常見嫌疑——`editor_formats.digest_rules('default', role, ...)` 對**兩個角色都回空字串**（本機實跑確認），`EDITOR_LAYOUT_SAFE_AREA` 與 `REPORTER_LAYOUT_SAFE_AREA` 也近乎逐字相同（差別只有「side panels／data charts」、多兩個禁用詞、以及提到 `<蓋章>`），**都不足以解釋三倍的落差**。②真正的差異在 `MAXIMUM_DENSITY_RULES` 本身，**它的措辭是隨角色變的**：**記者版**寫 `LINE LENGTH: There is no per-line character cap at this setting`／`TOTAL LENGTH: There is no total-length cap at this setting`；**編輯版**寫 `LINE LENGTH: The 「每行不超過 15 字」 limit above is LIFTED`／`TOTAL LENGTH: The 「總字數嚴禁超過 150-180 個字」 target above is LIFTED`。🔑**兩者都只做一件事：解除上限。沒有任何一句設下限。**「字超多」對模型而言只是「你可以寫很多」，**不是「你必須寫很多」**——模型寫 49 字完全沒有違規。而編輯版那個 `is LIFTED` 的說法又比記者版的 `There is no cap` 更弱：它把注意力導回被解除的那個數字（15 字／150-180 字），等於在提醒模型原本的天花板長什麼樣。**再疊上編輯版「scaled down／thick empty margin」的版面語氣，模型自然往少的方向收。**💡**修法方向（監督建議，待裁）**：**字超多與字多都要給下限，不能只解除上限。**例如明寫「至少 N 塊、內文總字數不少於 M 字」，並把編輯版那句 `is LIFTED` 改成與記者版同樣的肯定句式，不要再回頭引用被解除的數字。⚠**動工前必須注意兩件事**：①這是 prompt 文字，**會撞凍結快照**（`tests/test_reporter_prompt_frozen.py`），本波沒有重凍預算，要先確認誰來重凍；②**不要只改編輯版**——記者版同樣沒有下限，只是這篇稿剛好寫得夠多，換一篇稿一樣可能縮水，**兩個角色要一起修**。📌**對 D16 的連帶影響**：D16 說「內文兩軸級距成立」，但那是**記者版＋`gpt-5.5`** 的結論；**編輯版從來沒被量過**，而正式站的編輯版才是多數使用者在用的那條。D16 要不要補量編輯版，併入 D16 一起裁。📌**對 F38 的連帶影響**：要真正測「解析度能不能救錯字」，**素材條件要用記者版**（那才會產出 8 塊／138 字的密集版面）；用編輯版永遠測不到錯字出現的條件。 🔻**2026-09-15 第三輪（5+5 分布，零成本）：撤回「編輯版系統性偏少」這個結論——那是小樣本假象。但根因診斷（只解除上限、沒有下限）不但沒被推翻，反而被這組數字證實得更強。****觸發原因**：測試在舊 commit `4c11bd4` 上用同一份 payload 打出 **8 塊**，一度判為「今天的回歸」。**監督否證**：`git diff 4c11bd4..HEAD` 逐檔確認 `main.py`／`news_prompt.py`／`editor_formats.py`／`request_log.py` **全部空 diff**，改的只有 `MASTER`／`VERSION`／`app.js`＋`index.html`（分頁隱藏那 9 行）＋一份 docs。**消化端程式碼完全一致，8 vs 5 不可能是版本差異。** 已擋下 `git bisect`（會是純浪費）。**改做分布測試**：同一份完整 14 欄位 payload、同一篇 1464 字稿、在 HEAD 上，**編輯版 `maximum` 連打 5 次、記者版 `maximum` 連打 5 次**，只打消化端、不生圖。🔑**結果（10 筆全為 HTTP 200 有效樣本，非挑選；原始檔在 `D:\Downloads\AICG\後台紀錄\20260915\密度對照\`，`b57_dist_summary.txt` 有整理）**：**編輯版：8／8／8／4／8**　**記者版：1／1／8／8／8**。👉**兩組分布高度重疊——兩邊都是 5 次裡 3–4 次落在 8 塊。不是「編輯版系統性偏少、記者版穩定」這種乾淨的角色差異。****所以本條先前那個「決定性對照」（記者 8 塊／138 字 vs 編輯 6 塊／49 字）站不住——那是各抽一次，剛好一次抽到分布高峰、一次抽到塌陷，被單樣本騙了。****同理，舊 commit 那個 8 也只是落在高峰，不是版本差異的證據。**🎯**修正後的結論：`maximum` 密度下，兩個角色都有相近機率塌陷，只是塌陷的樣子不同。****甲型（編輯版那個 4）＝內容真的變少**，模型只給 4 項；**乙型（記者版那兩個 1）＝內容都在、但格式塌陷**，整篇被擠進同一個內文小標欄位、中間夾原始換行，沒有拆成 8 個獨立的 `[內文小標]` 標籤。**乙型不是「字少」，是解析層拿不到分塊**，前端會渲染成一大坨——嚴重度不比甲型低。✅**根因診斷維持不變，而且更站得住**：`MAXIMUM_DENSITY_RULES` 只解除上限、沒有任何下限，**兩個角色都沒有下限**——所以本來就預期「兩邊都會隨機塌陷」，而不是「某一個角色固定較弱」。**這正是這組分布看到的形狀。**「編輯版 `is LIFTED` 措辭較弱」這一點降級為**次要嫌疑、未證實**（10 筆看不出編輯版更容易塌陷），修法時可以順手改成肯定句，但**不要當成主因**。💡**修法方向（更新後，待裁）**：①**給下限**——明寫「至少 N 個 `[內文小標]`、內文總字數不少於 M 字」，兩個角色一起加；②**格式硬性要求**——針對乙型，明寫每一項必須各自帶一個 `[內文小標]` 標籤、不得用換行代替；③**後端補防呆**——消化回來若塊數低於下限或只解析到 1 塊，直接重打一次（現成機制：消化端本來就有 `attempt N/5` 重試），比改 prompt 更可靠、且不撞凍結快照。**監督建議 ③ 優先做（不撞快照、可立即上線），①②併入下一次重凍一起改。**⚠**測試回報的操作面事實（採信，已由程式碼佐證方向）**：背靠背連打會觸發 OpenRouter 503 逾時，需重試才拿得到 200——**這與 B59「背景執行失敗率較高」可能是同一個病**（上游逾時走進串流中斷分支），查 B59 時要把這條當已知事實一起看。📌**連帶修正**：本條先前寫給 F38 的建議「素材條件要用記者版才測得到密集版面」**作廢**——記者版一樣會塌陷（10 次裡 2 次），**F38／B58 這類需要密集版面的測試，正確做法是每次生圖前先驗塊數，不足就重打消化，不要靠選角色。**📌**對 D16 的連帶影響**：D16 的「級距成立」是單樣本結論，**在這個分布之下同樣不可靠，要不要重量併入 D16 一起裁。**✅**成本**：本輪 10 次全是消化端，**沒有生圖、沒有 $**。 |
| B58 | **大標題的壞字：3D 立體擠出＋發光的標題字會把筆畫吃掉，同一張圖的平面內文字卻正確** | 由 F38 錯字結案時分岔出來（2026-09-15，監督在既有正式站成品裡找到） | ❌ **2026-09-15 使用者裁決：結案，判定為誤報——「騙」字算繪是正確的，本條從頭到尾都不是缺陷。**｜**不修法、不追蹤、不再花錢。甲／乙／丙三個修法選項一併作廢。**｜**保留本列不刪，作為判讀誤判的教訓紀錄** | **證據（既有檔案，沒有另外花錢）**：`D:\Downloads\AICG\後台紀錄\20260915\曹雪卿成圖\證據_標題騙字壞掉_放大.png` 與 `證據_同圖第1格騙字正確_放大.png`。**同一張正式站成品裡，「騙」出現兩次**——**大標題那個（全圖最大的字）馬部下方四點變成亂筆、筆畫糊成一團，是壞字**；**內文區塊那個（小得多）筆畫乾淨、完全正確**。同圖、同字、同一次生成、同一個解析度。🎯**這是 F38 錯字結案的依據，也是本條成立的起點**：既然小的對、大的錯，**字級不是變因**，剩下最明顯的差異就是**樣式**——`REPORTER_TEXT_RULES`／`EDITOR_TEXT_RULES` 對主標題都明寫 `Rendered in bold 3D extruded typography with strong depth and lighting`，內文則只要求 `Clean and highly legible`。**假說：3D 擠出、投影、發光、描邊這些效果在筆畫密集的漢字上會把細部糊掉**，字越複雜（騙、籲、颱、蔬、警、籲）越容易壞。⚠**這只是假說，不要當結論**。已知的反面條件：這一組對照的變因**不只樣式一個**（字級也不同，只是方向與直覺相反），而且**樣本數 = 1**。✅**下一步（零成本，不要花錢生圖）**：正式站後台已經有大量成品，**直接撈既有成品逐張看標題字**，統計「標題壞字」的出現頻率，並記錄壞掉的是哪些字。要回答三件事：①頻率有多高（偶發還是常態）；②壞的是不是都集中在筆畫多的字；③內文區塊有沒有出現過同等程度的壞字（若有，樣式假說就不成立）。**這件事不需要生任何一張新圖。**💡**若假說成立，修法方向（都要先裁）**：甲｜**降低標題樣式的複雜度**（例如筆畫多的字改用較淺的擠出或取消發光）；乙｜**標題改程式壓字**（與 B55 的「模型只畫標題圖層」方向相反，這裡是反過來不讓模型畫標題），可靠但失去 AI 標題的設計感，等於動到 2026-09-13 的裁決；丙｜**在 prompt 裡加一條自檢**（要求模型檢查每個字的筆畫完整），**監督對丙持保留**——本 repo 已多次證實模型做不到的事情靠自律規則救不回來（見 `compose.py` 開頭那段安全框五輪實驗）。⚠**動工注意**：甲、丙都會改動 prompt 文字，**撞凍結快照**（`tests/test_reporter_prompt_frozen.py`），本波沒有重凍預算。 🔬**2026-09-15 監督做完零成本頻率掃描，結果比預期強：不是偶發，是穩定重現。**使用者指定重現後台紀錄 **2026-09-15 10:14:12 曹雪卿**（川普 AI 騙局，1464 字，資料圖表，編輯版）。查後台原始紀錄 `曹雪卿0915全紀錄\20260915_101412.txt` 與成品 `曹雪卿成圖\101412-31c5ea39447a.png`——**這一則就是 B58 那兩張證據圖的來源**。**關鍵發現：曹雪卿對同一則連生三次**（10:14:12／10:23:52／10:26:30），**三張的標題都含「騙局」**，監督逐張原尺寸放大 300–400% 檢視，**「騙」的馬部下方四點在每一張都塌成斜筆／糊塊，失效模式完全一致**。👉**同一個字、三次獨立生成、三次都壞——這不是隨機雜訊，是穩定重現的系統性缺陷。**（而且她連生三次這件事本身，很可能就是因為標題不能看。）✅**同圖對照再次成立**：同一張圖的第 1 格內文「全球暖化騙局」是**黃色平面字，「騙」完全正確**；標題那個是 3D 擠出＋發光，壞掉。**字級更大、樣式更複雜的那個壞，平面的那個對。**⚠**監督自己差點誤判、已避開的一個坑**：同一行標題的「黃仁**勳**」在縮圖上看起來像「黃仁動」，**原尺寸放大後確認是正確的「勳」**（灬 四點在）。這正是既有的教訓「判字要用原尺寸，縮圖會吃掉部件（勳→動）」——本條所有判定皆以原尺寸為準。📌**所以 B58 的前半段（頻率）已經不用再查，零成本部分完成**；剩下要證的是**因果**：到底是不是 3D 擠出＋發光造成的。已派決定性對照，見下。 🧪**2026-09-15 標題樣式 A/B 跑完（2 張，使用者授權）——方向支持假說，但監督複驗後認為證據強度低於測試端的說法，不足以當定論。****設計**：共用一次消化（曹雪卿 1464 字全文、編輯版、資料圖表），標題抽到「川普轟AI威脅論／是場騙局」，含「騙」；唯一變因是 `news_prompt.py` `EDITOR_TEXT_RULES` 標題樣式那一句——A＝原句 `bold 3D extruded typography with strong depth and lighting`，B＝`bold flat typography with a clean solid fill and a simple contrasting outline, no 3D extrusion, no bevel, no glow, no drop shadow`。兩張皆 1920×1080、同模型。證據：`D:\Downloads\AICG\後台紀錄\20260915\標題樣式對照\`。**監督自己看過 `b58_A_pian_only.png` 與 `b58_B_pian_only.png` 並排（非採信轉述）**：**A 的「騙」馬部下方四點，左側兩筆確實收斂糊成楔형塊；B 的同位置四筆分得開。方向成立。**⚠**但有三個理由不能把它當定論，監督要求寫進結論**：①**變因沒有乾淨隔離**——B 出來的不是預期的「平面粗體」，而是**深色填滿＋金色描邊**，**連填色與對比都變了**，不只是少了擠出。所以「B 比較好」可能有一部分來自填色，不能全歸給 3D。②**單樣本**，沒有做同一句話重複生圖的變異度控制；「騙」在正式站三連拍都壞，但 B 只生了一張，**一張乾淨不等於穩定乾淨**。③**差距是「比較清楚」不是「從錯變對」**——A 是糊、B 是清楚，兩者都還在可辨識範圍，與正式站那三張塌成糊塊的程度不完全同級。📌**另一個監督注意到、測試端沒提的觀察**：**B 的標題整體顯著性下降**（深色填滿在深色底圖上的跳脫程度不如金色立體字）。**這題不是「筆畫清楚就贏」——標題要上鏡、要搶眼，把立體與發光整個拿掉是有代價的。**所以修法不該直接跳到「拿掉 3D」，應該往**保留顯著性但降低筆畫破壞**的方向試（例如保留擠出但取消發光／降低斜角深度、或只對筆畫數高的字放寬樣式）。📌**一個未解釋的觀察**：耗時 A 84.4 秒 vs B 34.4 秒，**差 2.5 倍**。同樣的工作量不該差這麼多，**疑似 A 那次有重試**，但本輪沒有留下足以判斷的日誌。若之後要再測，請一併抓 `[generate] attempt` 那類日誌行。✅**邊界確認（監督已收到並接受）**：`news_prompt.py` 已 `git checkout --` 還原、`.env` 未動、未碰 staging／正式站、未改任何測試檔、2 張未超額。 🔻**2026-09-15 反證：3D 擠出「不足以」造成壞字——假說要收窄。**使用者指定再測後台紀錄 `2026-09-15 17:28:51 曹雪卿`（中國出入境新規，資料圖表，編輯版）。撈回原紀錄與成品（`曹雪卿0915全紀錄\20260915_172851.txt`＋`曹雪卿0915全紀錄\曹雪卿成圖\172851-3a68a9330f6d.png`）。**這筆的條件跟 10:14:12 那批同量級**：主標 2 行＋1 個強調數字塊＋7 張兩行卡片，密集版面。標題是「中國出入境新規上路／科技業出境門檻升高」，含 **「檻」（18 畫）**。**監督自己裁切放大 400% 複驗（非採信轉述）：「檻」的木部＋監部結構完整，皿部三豎清楚分開、等距，完全沒有糊塊／收斂／斜筆。整條標題無壞字。**🔑**而這張標題明顯是 3D 立體擠出（銀色金屬質感，斜角與厚度都很明顯）。**👉**所以「3D 擠出＋筆畫多 ⇒ 壞字」不成立**——同樣是 3D 擠出、同樣是高筆畫數的字，這張完全正常。**3D 擠出是必要條件的候選，但明確不是充分條件。** 假說必須收窄。💡**收窄後的候選變因（待測）**：①**發光／外光暈**——壞掉的那三張是藍色漸層＋強發光，這張是銀色金屬、沒有明顯外發光；②**特定字的結構**——「騙」的馬部底下是 **灬 四點**，**四個細小且必須彼此分離的元素**，是最容易被任何厚度效果糊在一起的結構；「檻」的皿部是三豎，筆畫較粗、間距較大，天生比較耐糊。**②這條特別值得注意：真正的風險可能不是「筆畫多」，而是「有密集小點／短撇」的部件**（灬、氵、冫、爫、宀 上的點），這跟「筆畫總數」是兩回事，先前的敘述（籲、轟、蔬）可能找錯方向。📌**這筆不適合當 B58 的測試素材**（沒有壞字就沒有可改善的空間），但**留作反證樣本很有價值**，不要刪。✅**下一輪的正確素材就是 10:14:12 那篇**——它的「騙」在三次獨立生成裡三次都壞，是目前唯一已知可穩定重現的案例。**條件必須釘死：`density="maximum"`**（見 F39，後台不記這欄，先前就是在這裡出錯測成字多）。📌**順帶記一筆**：17:32:40 那筆是對 17:28:51 的 **web-refine 追加修改**，不是獨立重生成。與 B51／B52 的追加修改缺陷同一條路徑，但本筆是 `default` 版型、不是封面，**所以不受那兩條的封面例外影響**；若要查追加修改的行為，這是一個現成的非封面樣本。 🛑**2026-09-15 第二輪（發光變因，4 張）結果與假說相反，監督據此收掉「樣式是元凶」這條路線——真正的常數是『部件』不是樣式。****實測**：同一份消化（標題「川普稱AI是騙局／力挺輝達技術」），A＝原句（3D 擠出＋lighting）×2、B＝只刪 ` and lighting`（保留 3D 擠出）×2。**監督自己四張裁切並排複驗（非採信轉述）：A 兩張的馬部四點清楚分開；B 兩張的左側兩筆收斂成尖角糊在一起，兩張位置形狀高度一致，不是隨機。**👉**拿掉發光沒有讓字變乾淨，反而穩定變糊——「發光是元凶」被否證。**🔑**把四輪樣本擺在一起，樣式這條線整個站不住**：①`17:28:51`「檻」＝3D＋銀色金屬 → **乾淨**；②`10:14:12`「騙」＝3D＋藍色漸層發光 → **三次全壞**；③B58-1 的 A＝原句 → **壞**、B＝整句換成 flat＋fill＋outline → **乾淨**；④B58-2 的 A＝原句 → **兩張都乾淨**、B＝原句減 lighting → **兩張都壞**。**③與④的 A 是同一段 prompt 文字，結果一個壞一個乾淨。****同一條規則、相反的結果 ⇒ 樣式子句根本不是決定因素，這是隨機性。**🎯**四輪下來唯一穩定成立的事實只有一個**：**壞掉的位置永遠是「馬」的 `灬`**——從來沒有壞在「扁」，也沒有壞在標題的其他字（境、業、檻、升、高、局、技、術都正常）。**所以真正的變因是『部件』：`灬` 這種「多個細小、必須彼此分離」的結構，在任何厚度／描邊處理下都容易被糊成一塊。與字級無關（F38 已證）、與解析度無關（F38 已證）、與 3D 擠出無關（①）、與發光無關（本輪）。**💡**因此修法方向要換掉，不要再調樣式文字**（四輪證明調不動）。改成三條：**甲｜標題改程式壓字**——最可靠，一勞永逸，代價是失去 AI 標題的設計感，且等於動到 2026-09-13 的裁決（與 B55 甲案的「模型只畫標題圖層、程式合成」是同一套機制，**可以合併做**）；**乙｜產出後自動驗字**——用 OCR／比對偵測標題是否與指定文字一致，不一致就重生，治標、會多花錢，但不動視覺；**丙｜規避**——消化端在選標題用字時避開含 `灬` 的字，**監督不建議**：會扭曲新聞用字（「騙局」是這則新聞的核心詞，不能為了算繪換掉）。**監督建議甲，並與 B55 合併評估。**📌**附帶查證**：`[generate] attempt N/5` 這類重試日誌**只存在於消化端**，生圖端 `/api/images/generate` 沒有對應的逐次重試日誌，HTTP 層只看得到一次 200。**所以 B58-1 那個 84.4 秒 vs 34.4 秒的 2.5 倍落差，用現有日誌無法驗證**，本輪也沒再出現離群值（最慢 38.5s、最快 33.2s，差 16%）。**這條併入 F30**（成功不記耗時與 provider）——生圖端缺重試與計時日誌，是同一個病。✅**邊界確認**：`news_prompt.py` 已還原、`.env` 未動、4 張未超額。**測試線到此為止，不要再為這題花錢**——樣式已測到盡頭，下一步是裁修法。 ✅**2026-09-15 第五輪（4 張重複生圖）——本條證據面到此結案，出現率 4/4＝100%。****條件**：記者版、`density="maximum"`、完整 14 欄位 payload、素材沿用 10:14:12 那篇 1464 字稿，**每張都先過關卡（`[內文小標]` 標籤數 ≥8 才准生圖）**，四張都是 8 塊密集版面——**這是第一次在「確定密集版面」的前提下做重複觀測，前面幾輪都沒有這道關卡。****四張的標題措辭、視覺樣式、seed 全部不同**：「川普稱AI控世界是騙局」（銀白金屬＋藍底）、「川普稱AI威脅論是騙局」×2（紅框金黃／藍白漸層）、「川普：AI接管世界是騙局」（藍白立體）。🔑**結果：四張的「騙」字，馬部下方的 `灬` 全部壞掉，右側「扁」（戶＋冊）四張全部正常。****監督自己開原圖裁切放大複驗（非採信轉述）**：r1 的四點最左兩筆收斂成尖角、交叉黏在一起；r3／r4 的四點被拉長成下垂豎筆並在底部相連。**變形的細部形狀不完全相同，但位置與性質完全一致——永遠是 `灬`，永遠是「該分開的細小元素黏成一塊」。**📌**監督在此修正測試的一個措辭**：測試回報「崩壞形狀完全一致」，**嚴格說是「位置與失效模式一致、細部形狀不同」**；這個差別不影響結論，但帳本要寫準——**不是同一個 bug pattern 被複製，是同一個結構在不同噪聲下各自糊掉。**👉**所以「樣式是元凶」徹底出局**：四種樣式、四個 seed、三種措辭，壞法一樣。**「字級／解析度是元凶」也早已出局（F38）。剩下唯一的常數就是那個部件本身。**✅**同一批的反證也一起記下（監督全圖複驗 r1）**：同一張圖裡，**「黃仁勳」的 `勳` 字（左下也是 `灬`）在小字平面體裡算繪正確**、**「習近平」三字正確**、標題其餘字（川、普、稱、控、世、界、是、局）全部正確。⚠**監督自記一個判讀教訓**：在測試提供的**放大裁切圖**上，「習近平」一度看起來像「習新平」，**回到 1920×1080 原圖才確認是正確的**。**裁切圖已經被放大過，不能拿它當判字依據——判字一律回原尺寸檔**（與既有的「縮圖會吃掉部件」同一條紀律的反面）。🎯**修法維持三選一，監督仍建議甲（程式壓字），並與 B55 合併評估。****乙（產出後驗字）在本條的數據下價值上升**：既然是 100% 重現而不是偶發，**重生一次也不會變好**，乙必須搭配「換字或改走程式壓字」的退路，單純重試無效。📌**證據**：`D:\Downloads\AICG\後台紀錄\20260915\字超多生圖\`（四張成品 `r*_image.png`、裁切 `r*_pian2.png`、各自的 `r*_prompt.txt`／`r*_digest_final.ndjson`、以及 `報告-字超多重複生圖判讀.md`）。💰**成本**：生圖 4 張＝$0.16；消化 6 次（r1 因 OpenRouter 503 重打 2 次）。 ❌**2026-09-15 使用者裁決：本條作廢，判定為誤報。****使用者原話：「我看過了 騙都是正確的 不是錯的 結案」。**👉**四張成品裡的「騙」字算繪正確，`灬` 沒有壞。****先前五輪（含監督自己的逐張裁切複驗）把立體字的厚度、斜角與陰影誤判成「四點糊在一起」。****這一條從第一輪起就是建立在錯誤的判讀上，後續所有推論（部件脆弱性、樣式無關、100% 重現）全部隨之作廢。**⚠**教訓（監督自記，比本條本身更有價值，請保留）**：①**放大裁切會製造假象**——已知「縮圖會吃掉部件」，本輪反向踩到：**放大同樣會把立體效果的斜角與陰影誇大成筆畫變形**。原尺寸檔也不夠，**判字要看「這個字在成品上實際被閱讀時的樣子」**。②**我自己複驗過四張、判斷一致，結果仍然是錯的**——**「監督親自複驗」不等於正確**，一致只代表我每次都用同一套錯誤標準。③**應該更早交給使用者肉眼確認**：這是人眼一秒可判的事，卻連續五輪用付費生圖去追。**往後「字有沒有壞」這類人眼可判的問題，先給使用者看一張，不要先開測試線。**💰**代價**：B58 這條線累計付費生圖約 10 張（$0.40 量級）＋多輪消化 token，**全部是白花的**。📌**連帶影響**：本條先前建議的「甲＝標題改程式壓字」作廢；**B55 的甲案不受影響**（那是為了「原圖放置不被改」的保證，與壞字無關），仍待裁。**F38 的「解析度救不了錯字」結論也不受影響**（那是用同圖不同字級證的，不依賴騙字壞掉）；但 F38 裡「錯字與字級反相關」這句的立論來源是本條，**已不成立，需要一併修正**。 |
| B59 | **「背景執行」時失敗機率明顯較高** | 曹雪卿 0915 回饋文件第 1 條（2026-09-15 使用者指定列管；此前整份清單只有這條與 24H LIVE 兩行那條沒被列管） | ⬜ **待查證**｜零成本的第一步是先把「背景執行」的定義問清楚 | **使用者原話**：「試了幾次背景執行，失敗機率較高（遇過幾次都是這樣，不曉得是剛好或是真的會有這樣問題）」。⚠**第一件事是釐清「背景執行」指什麼**，這決定要查哪裡，**不要憑猜開工**：①**送出後把瀏覽器分頁切到背景／切去做別的事**（最可能的解讀）；②同時開多個分頁一起送；③其他。**問清楚之前不要動手。**💡**若是解讀①，有一個很具體的技術候選**：B39 之後 `/api/generate` 改成 NDJSON 串流，前端 `_digestFetch` 用 `response.body.getReader()` **逐行持續讀**，並靠 `DIGEST_FETCH_TIMEOUT_MS=290` 的 `AbortController` 控總時長。**Chrome 對背景分頁會節流計時器與部分工作**，長工期間讀取被拖慢或中斷，就會走進 B38 那條「串流中途斷線」的錯誤分支——**使用者看到的就是失敗率變高**。這條完全符合「前景會過、背景較常失敗」的現象。📌**已知的相鄰事實**：B39 末尾記過「使用者中途關分頁時，後端那個 daemon thread 會跑完全程，token 照燒」——那是**成本**問題；本條是**成功率**問題，兩者是同一個場景的兩個面向，修的時候要一起看。✅**下一步（零成本）**：①向使用者確認「背景執行」的定義；②若為①，本機起服務用長稿實跑兩次——一次全程停在前景、一次送出後立刻切到別的分頁——比對成功率與 console 是否出現串流中斷。**不用打正式站、不用生圖，只花消化 token。**⚠**注意**：B48 已知長稿本來就有約 25% 的間歇失敗率，**所以樣本要夠才分得出「背景更容易失敗」與「本來就會失敗」**，兩次不夠下定論，先當方向指標。 |
| B60 | **同參數、同一篇稿，輸出份量毫無連貫性——`maximum` 塊數在 1～8 之間亂跳** | 2026-09-15 使用者看 B57 的 5+5 分布後當場指出「這個數字要檢討，沒有連貫」，指定列管 | ⚖ **2026-09-16 使用者已裁 驗收標準：同參數連打 5 次，塊數全部落在目標值 ±1 才算修好**；份量類結論一律附 5 次分布，不接受單次示範｜⬜ **待裁修法**｜**這是獨立於 B57 的一條**：B57 問「為什麼會變少」，本條問「為什麼同一組輸入每次不一樣」｜**監督判斷：可重現性應該當成驗收條件，不是附帶現象** | **事實（零成本實測，10 筆全為 HTTP 200 有效樣本，非挑選）**：同一份完整 14 欄位 payload、同一篇 1464 字稿、同一個 commit（HEAD）、同一個模型（`claude-sonnet-5`）、`density="maximum"`，連打 10 次——**編輯版：8／8／8／4／8**　**記者版：1／1／8／8／8**。原始檔：`D:\Downloads\AICG\後台紀錄\20260915\密度對照\`（`b57_dist_summary.txt` 有整理）。👉**輸入完全相同，輸出份量相差最多 8 倍。** 使用者按同一顆按鈕、貼同一篇稿，**拿到的成品可能是密集 8 塊，也可能是 4 塊或塌成一坨——這件事本身就是缺陷，跟「字超多夠不夠多」是兩個問題。**🔑**監督的判斷：不要把這個歸成「LLM 本來就有隨機性」就結案。** 隨機性解釋得了 8 vs 7，**解釋不了 8 vs 1**。8 倍的離散度代表規則沒有把輸出鎖進一個可接受的區間，**模型每次都在重新決定「要寫多少」，而不是在執行一個被指定的份量。**🎯**根因與 B57 同源但不同面向**：`MAXIMUM_DENSITY_RULES` 只解除上限、沒有下限，**等於沒有給出任何目標值**——沒有目標值就沒有收斂，自然每次都不一樣。**所以修 B57 的下限，理論上會同時收掉本條的離散度；但「有沒有真的收掉」必須另外量，不能假設。**⚠**這條直接影響前面所有結論的可信度，要寫在最前面當教訓**：今天有兩個結論就是被單樣本騙掉的——「編輯版系統性偏少」（各抽一次，剛好抽到兩端）、「舊 commit 是回歸」（其實消化端零改動）。**在離散度收斂之前，任何「改了 X 之後變好／變差」的判斷都不能只打一次就下結論**，**最少 5 次看分布**。這條適用於 B57／B58／F38／D16 以及之後所有消化端的對照測試。✅**下一步（零成本，建議做在修法之後當驗收）**：①先量現況基線——`standard`／`simplified` 各連打 5 次，看**離散度是不是只有 `maximum` 特別大**，還是每一檔都這樣（若每一檔都亂跳，問題比 B57 更廣）；②修法（B57 的下限＋塊數防呆）上線後，同樣連打 5 次，**驗收標準建議訂成「5 次的塊數全部落在目標值 ±1 之內」**，不到就不算修好。💡**監督建議的驗收條件（待裁）**：**份量類的規格從此不接受單次示範**，帳本上任何跟字數／塊數有關的結論，都要附上 5 次的分布，不是一個數字。 |
| B61 | **封面各版型的生圖 prompt 沒有「不得畫具名真人臉」的預設防線 → 模型自己把新聞主角畫成過期人物** | 2026-09-16 正式站事故（王結玲 09:21–09:25、曹雪卿 09:00–09:11），子代理全路徑盤點 | ⬜ **待裁修法**｜**這是 0916 事故的直接破口，兩位使用者同一個早上各自再現** | **事實**：標題「美國聯準會利率決策出爐」**不含任何人名**，畫面描述也只寫大樓與國旗，**四次生圖四次都被模型自己加上一張鮑爾（前主席）的寫實人臉**。🔑**根因鏈**：headline 無人名 → derive／消化回空 `portrait_subjects` → `keep_subjects_with_photos`（main.py:4536）`if not subjects` 直接回空 → `apply_portrait_to_image_request`（main.py:3717-3719）同樣因 subjects 空**原封不動 return** → 送進模型的最終 prompt **完全沒有一句話**限制真人臉。📋**盤點結果（子代理，唯讀）——有洞的六條**：十點雙切（`COVER_VISUAL_PROMPT_TEMPLATE`，editor_formats.py:204）、十點滿版（`COVER_VISUAL_FULL_PROMPT_TEMPLATE`:192）、十點 AI 標題（`COVER_AI_PROMPT_TEMPLATE`:236／`COVER_AI_FULL_PROMPT_TEMPLATE`:268）、YT 合成底圖（`YT_COVER_VISUAL_PROMPT_TEMPLATE`:1402）、**YT AI 標題四版型**（`YT_COVER_FULL_PROMPT_NEWS`:1535／`HOURLY`:2036／`HOT`:2160／`LIVE24`:2072，事故走的就是 NEWS）。**有防線的對照組**：`/api/news-image`（`build_prompt` 無條件注入 `REAL_WORLD_RENDERING_RULES`，news_prompt.py:560／597，預設條款在 :196）、`/api/images/refine`（`REFINE_REAL_WORLD_RULES` 無條件疊加）、`/api/images/generate` 走 app.js 時（前端自己插，app.js:1896／2008）。👉**系統性落差**：防線寫在主流程，**封面各版型自己另組 prompt 時沒帶上**，每加一個新版型就漏一次。💡**修法方向（待裁）**：六個模板補上與主流程等義的預設條款（無肖像區塊時不得畫可辨識具名真人臉，只能背影／剪影／非具名臉）。⚠**動工前先確認**：這會改 prompt 文字，要確認撞不撞凍結快照。📌**證據**：`D:\Downloads\AICG\後台紀錄\20260916\王結玲聯準會\`（四步對照圖、四筆後台原始紀錄、查因報告） |
| B62 | **`hybrid.js`（一鍵成圖的獨立前端）自組 prompt，真人與品牌防線全都沒有** | 同 B61 的盤點順帶發現（2026-09-16） | ⬜ | `hybrid.js` 的 `buildBgPrompt()`（hybrid.js:323-336）直打 `/api/images/generate`，**全檔 grep `REAL_WORLD`／`NAMED REAL PERSON`／`silhouette` 零命中**——app.js 有做同步（app.js:1881-1900），hybrid.js 沒有。**後端對 `/api/images/generate` 不補防線**（只做 `apply_portrait_to_image_request`，subjects 空就 return），所以這條路完全裸奔。與 **A4 缺口 5「hybrid 全無」**同一叢，建議一起看。 |
| B63 | **追加修改無法指名換臉：使用者說「人頭換成某某人」在結構上必定失敗** | 2026-09-16 王結玲 09:22:33 web-refine 實例 | ⬜ **待裁修法** | **實例**：她下「人頭要換成新的FED 主席華許」，結果換成**誰都不是的第三張臉**。🔑**兩個原因疊在一起**：①`news_prompt.REFINE_REAL_WORLD_RULES`（news_prompt.py:485）硬寫死「no reference photograph is attached to this edit… **Faces already present stay exactly as they are — do not restyle, replace, age, beautify or re-render them**」，**與使用者指令正面對撞**；②**這是後端事實不是前端漏傳**——`ImageRefineRequest`（main.py:3950）**沒有** `reference_images`／`portrait_subjects` 欄位，`refine_image` 也不查 `photo_lookup`，所以那句「沒有附參考照」**永遠成立，沒有任何呼叫方式能讓它不成立**。👉**模型在衝突下折衷捏一張臉，程式端沒有任何檢查會擋下或提示**。✅**對照組（同一天、同一個 bug 的另一半）**：曹雪卿 09:11:32 下「左邊的鮑爾不要!!!!」→ **刪臉成功**。**刪得掉、換不掉**，正好對上規則措辭。💡**修法方向（待裁）**：refine 加人名／參考照管道，偵測到「換成某某人」就查照片、附上並改用允許換臉的規則；**查不到就明講辦不到**並提示改走原圖放置，不要讓模型硬編。📌**相鄰**：同一次 refine 也把構圖拉近、標題重畫（違反同段的 keep framing）＝ **B2** 的再現。 |
| B64 | **能力判斷過期：原生 OpenAI 早就送得出參考圖，`supports_reference_image()` 仍回 False → 肖像參考照被整批丟棄、真人題一律退回背影** | 2026-09-16 監督本機實測「標題寫華許」時踩到 | ✅ **已修並實測通過（2026-09-16，本機 exp 工作樹，未 commit）**｜修後同一份請求 log 變成 `參考照=1 張` ＋ `[GPT image] 附 1 張參考圖，改走 images.edit`，成品畫出**正確的華許本人**（三方臉部對照：維基官方照／系統自動查照生成／王結玲自己上傳的成品，同一人）。測試 **1768 passed**。證據：`D:\Downloads\AICG\後台紀錄\20260916\王結玲聯準會\20260916_測試3_標題寫華許_修後真跑.png`、`20260916_華許三方臉部對照.jpg` | **實測**：本機 `IMAGE_BACKEND=native` 跑 YT 封面，log 為證 `[yt-cover:ai-title] portrait_subjects=['華許'] en=['Kevin Warsh'] 參考照=0 張`——**人名抓到了、維基照片也查得到（`find_reference_photo('華許')` 回 zh.wikipedia 官方肖像），但照片被丟掉**，`resolve_portraits` 因此回 `no_reference`，成品是背影。🔑**根因**：`supports_reference_image()`（main.py:2888）的 docstring 還停在「原生 OpenAI 的 images.generate 沒有參考圖通道」，**但 2026-09-10 起 `generate_gpt_image` 只要有參考圖就改走 `images.edit`**（main.py:3325-3332，`_native_reference_files` 送的是 `reference_image_data_url` ＋整個 `reference_images` 陣列）。2026-09-13 的 `b70f89d` 修了 `supports_multiple_reference_images`，**單張這一支沒跟著修**。⚠**連帶影響**：凡是在本機原生後端跑過的真人題測試，結論都可能被污染（「明明有照片還是畫背影」）。 |
| B65 | **`IMAGE_BACKEND` 的拼法在能力判斷之間不一致：`native` 與 `openai` 各認一半** | 同 B64（2026-09-16） | ✅ **已修（2026-09-16，與 B64 同一次，本機 exp 工作樹未 commit）**：抽出 `using_openrouter_images()`（與 `generate_image_raw` 同源），三支能力函式都改讀它；測試補了兩種拼法的 subTest，並更正兩處釘住舊事實的斷言（`test_portrait_rules.SupportsReferenceImageTests`、`test_native_multi_reference_20260913`）。**1768 passed** | `generate_image_raw`（main.py:3047-3057）的路由規則是「`openrouter` ＋有 key → OpenRouter，**其餘一律走原生**」；但 `supports_multiple_reference_images`（main.py:2915-2919）只認 `backend == "openai"`，**`dev-local-openai.sh` 設的卻是 `IMAGE_BACKEND=native`** → 同一次請求「路由說走原生 GPT（送得出多張）」「能力判斷說送不出去」，兩邊對不上。`supports_map_basemap`（main.py:2869）寫法又是第三種（非 openrouter 一律看 `provider == "gpt"`，這支反而是對的）。👉**三支各寫一份 `os.getenv` 判斷、拼法還互不相同**，這類漂移還會再發生。💡**修法**：抽一支與 `generate_image_raw` 完全同源的「這次走不走 OpenRouter」判斷，三支能力函式都改讀它。 |
| B66 | **查到參考照時規則要求「手繪插畫風肖像」，實際輸出是寫實照片感** | 2026-09-16 B64 修後實測順帶發現 | ⚖ **2026-09-16 使用者已裁：寫實為主，但要帶一點點插畫筆觸感**——既不是純寫實照片、也不回到明顯的手繪插畫。**條文要照這個方向改寫**（現行 `PORTRAIT_WITH_REFERENCE_RULES` 的 `hand-painted editorial portrait illustration rather than a photograph`／`must be readable as an illustration` 要換成「寫實為底、保留可辨識的筆觸質地」這種措辭），並保留「AI示意圖」標籤。⚠**這條要實拍驗收**：措辭強度落在寫實與插畫之間，很難靠讀 prompt 判斷，改完要生圖給使用者看 | `PORTRAIT_WITH_REFERENCE_RULES`（news_prompt.py）明寫 `Render the portrait as a hand-painted editorial portrait illustration rather than a photograph` 與 `must be readable as an illustration`，但 2026-09-16 的實測成品（`20260916_測試3_標題寫華許_修後真跑.png`）看起來是**一張寫實新聞照**，不是插畫。⚠**這條規則當初的用意是合規**：寫實照片感＋真名＋（萬一）沒有示意圖標籤是最糟組合，所以才要求插畫化。本次成品右上角的「AI示意圖」標籤有在，但**視覺上與真實照片難以分辨**。💡**兩個方向要使用者裁**：甲｜加強插畫化措辭（代價：電視畫面上插畫風可能不好看，這正是使用者先前擔心的）；乙｜承認寫實化是想要的結果，改為強化標示（示意圖標籤位置／大小）並修正規則文字，讓條文與行為一致。**現況是條文與行為不一致，這本身就要修**。📌**樣本數 1**，裁之前若要更多樣本，可在既有正式站成品裡找有肖像的案例回看，零成本。 |
| F40 | **肖像照片來源覆蓋率：維基查不到的人太多，而「查不到」現在只能退回背影／不畫臉** | 2026-09-16 使用者提出（「碰到好幾次查維基都是查錯或沒找到人」），由 B61 事故展開 | ⚖ **2026-09-16 使用者已裁：維基維持墊底**——**不接 AP／路透圖庫、不建本地人物照片庫**（兩案暫不動工，日後覆蓋率真的擋到再議）。🔁**2026-09-16 二次裁決（取代同日稍早那次，以本段為準）：改採四層分流。**｜**第 1 層**使用者自己上傳照片 → 直接用｜**第 2 層**維基查得到照片 → 用照片（現況）｜**第 3 層**查不到照片、**但查得到這個人的維基條目** → **允許生圖模型自畫**，**⚠使用者明確裁定：不要在圖上標「長相為 AI 推測」**，改成**在訊息欄通知使用者**（訊息欄＝目前報錯用的那個紅色框）｜**第 4 層**連條目都查不到 → **畫無人場景，不要畫背影**。🔑**第 3 層的閘門刻意用「維基有沒有這個人的條目」當代理指標**，不是問模型知不知道（模型不會誠實回報自己不認識）：連條目都沒有的人，模型幾乎確定也不認識。📌**同日稍早的裁決是「查不到照片就一律無人場景」，已被本段取代**——保留那句在下面備查。（舊）查不到照片就改畫無人場景，不要畫背影。使用者原話「畫出背影就是錯的」——**不是擋下不生圖，而是從根本不安排人物**：畫大樓、標誌、場景、器物，而不是塞一個背影或剪影在那邊。⚠**這要改 `PORTRAIT_NO_REFERENCE_RULES`**（現行條文寫的正是「背view 或剪影」），並連動消化／derive 端的畫面描述——**描述階段就不該寫成人像鏡頭**，否則生圖端只能硬塞一個人進去。**這條與 B53（補畫面描述只餵標題）同一段程式，建議同批做。** 🧪**2026-09-16 對照實測（使用者指定，1 次生圖，子代理執行）：模型在完全沒有參考照的情況下，光靠上下文畫出來的華許「明顯像」。**條件：打 `/api/images/generate`（不經肖像查照機制），prompt 明寫 `Kevin Warsh, the Chairman of the U.S. Federal Reserve`，log 確認**沒有附任何參考圖**，模型 `gpt-image-2.5-sunburst`，HTTP 200／17 秒。成品與維基官方照並排：`20260916_測試4_對照.jpg`（左官方照／右生成）。**深黑側分短髮、深眉、長臉、鼻型、膚色都對得上**；差異在髮量更蓬、臉頰與下顎更寬、年齡感偏大、沒有官方照的灰白髮。🔑**這修正了一個先前講得太寬的說法**：不是「模型畫不出新任主席」——**名字明確寫出來、而且這個人在訓練資料裡時，它畫得出來**（華許 2006–2011 任聯準會理事，公開照片多）。**0916 事故真正錯的那一步是「從職稱自行推斷主席是誰」**（標題只寫「美國聯準會」→ 模型用過期知識補了鮑爾），不是「不會畫臉」。⚠**但不足以改變本條裁決，三個理由**：①**n=1**，分不出「模型真的知道」與「剛好抽對」；②訓練資料稀薄的人（台灣本地人物、小國官員）它幾乎一定會編，**而編出來的臉看起來一樣自信**；③**失敗不可偵測**這件事沒有變。📌**衍生的待裁題（尚未開列，等使用者決定要不要列管）**：「人名明確寫在標題裡時，要不要允許模型自畫、配示意圖標籤」——那等於在維基墊底之外多一條退路，但也等於承擔②的風險。 | **使用者的直覺是「不如讓生圖模型自己從上下文判斷人是誰」，監督明確反對**：2026-09-16 那四張鮑爾**正是**模型自己判斷的結果——沒有人名、沒有照片、沒有規則時，它就照自己知識裡的「聯準會主席」畫，而且畫得很有自信。🔑**兩種失敗的形狀不對稱**：查不到照片＝系統知道自己不知道，**可偵測、可擋**；模型自判＝它不知道自己不知道，產出**看起來完全正常、掛著真名的假臉**，沒有任何程式檢查抓得到。而且模型知識有截止日，**新上任／新竄起的人**（正好是新聞最常要的）一律畫成前任。👉**所以要換的是照片來源，不是換成讓模型畫。**💡**三條路（待裁）**：甲｜**接 AP／路透圖庫**——新聞部每天在用、有帳號、覆蓋率與時效性遠勝維基、授權清楚，S6／S8 已有搜尋下載基礎；乙｜**本地人物照片庫**——常用官員／名人存一份，命中就用，維護成本低、新面孔要人工補；丙｜**維基維持墊底**，查得到就用。⚖**另一半同等重要（使用者原話：「畫出背影就是錯的」）**：查不到照片時，現況**還是花錢生了一張沒人能用的背影圖**。應改成**生成前就擋下**並提示「這則有指名人物，請上傳一張照片」，一次生圖錢都不花。📌**2026-09-16 已實測**：`find_reference_photo('華許')` 查得到官方肖像（zh.wikipedia／凱文·沃什），B64 修好後成品畫出正確本人——**所以維基機制本身是有效的，今天失效是被 B64／B65 擋掉**；覆蓋率問題仍然獨立存在。⚠**待補證據**：使用者說的「查錯」要區分是**查不到**還是**抓到別人的照片**；後者較嚴重（2026-08-18 已加 Wikidata `P31=Q5` 身分閘門），**若還會抓錯人請留實例**。 |
| F41 | **釘身分推斷的鐵則：人名只准「抽取」、不准「推斷」** | 2026-09-16 使用者裁定（由 B61 事故與測試 4 的結論分岔出來） | ⚖ **已裁：釘死「只抽取、不推斷」**｜**下一批做，屬於真人臉那一刀（③）** | 🔑**把兩件事分開才看得懂 0916 事故**：**第一層＝這則新聞的主角是誰（身分，文字問題）**、**第二層＝那個人長什麼樣（臉，影像問題）**。**事故錯在第一層**——標題只寫「美國聯準會」，模型自己推斷出「主席＝鮑爾」並畫上去，用的是過期知識。**第二層其實沒壞**：2026-09-16 測試 4 證明名字明確寫出、人物又在訓練資料裡時，模型畫得出來（見 F40 的實測段）。✅**裁定的鐵則**：人名只能來自**使用者寫的標題或新聞原文**；**消化／derive 端可以「抽取」文中已有的人名，不准「補」文中沒有的人名**；**生圖端不准自己在畫面裡放上具名真人**（這一半由 B61 的出口強制層執行）。**兩端都要釘，而且要有測試**：餵一篇只寫職稱不寫人名的稿，斷言 `portrait_subjects` 為空且最終 prompt 帶著「不得畫具名真人臉」。 |
| F42 | **訊息欄改成訊息視窗：記錄整個過程的所有訊息，不要只閃一次** | 2026-09-16 使用者裁 F40 時追加 | ⬜ **本次不修，先列 TODO**（使用者明確指示） | 使用者原話：「訊息欄就是報錯的紅色框，以後也要改成訊息視窗，記錄過程中的所有訊息，不要只出現一次。訊息視窗先列TODO，本次不修，只修四層分流」。**現況**：提示只有一個一閃而過的紅色框（B38 記過同樣的痛點——消化逾時只看得到一個 toast 寫「HTTP 524」）。**要做的是一個持續累積的訊息區**，把生成過程中的每一則訊息（警告、退路、逾時、第 3 層「這個人沒有參考照」之類）都留在畫面上可回看。📌**F40 第 3 層的通知就是掛在這個機制上**——在訊息視窗做好之前，先掛現有的紅色框。與 **B38**（錯誤訊息只有一閃而過的 toast）、**B42**（改圖線的錯誤橫幅）同一叢，建議一起做。 |
| F32 | **追加修改的輸入欄太小，一次改多處時看不清楚** | 曹雪卿 0915 回饋文件（AI CG回饋，hikaru.tsao@gmail.com） | ⬜ 🌊**第二波**（2026-09-15 使用者裁定：等第一波收工才動工） | 使用者原話「追加修改的欄位較小，若一次要修改較多地方，容易看不清楚」。純 UI，改 textarea 高度／可拖曳即可，成本低。**建議與 B42（改圖線的錯誤橫幅）同一次動工**，都在改圖那條線的前台。 |
| F33 | **消化把「創9月2日以來新高」簡化成「逾兩周新高」，講得不對** | 曹雪卿 0915 回饋文件（AI CG回饋，hikaru.tsao@gmail.com） | ⬜ 🌊**第二波**（2026-09-15 使用者裁定：等第一波收工才動工） | 使用者原話「訊息精確度／中上第三行原提供資訊為 創9月2日以來新高，但直接簡化為"逾"兩周新高不正確」。9/2 到 9/15 是 13 天，**不滿兩周**，「逾兩周」把方向講反了。這不是字數壓縮的副作用，是**消化端在改寫數字期間時自行換算而且算錯**。要看的是 prompt 有沒有授權它把明確日期改寫成相對期間——監督傾向**不該授權**，日期原樣保留最安全，但這要裁（見 D19）。 |
| F34 | **標題把觀眾不認識的人名放進去，而不是用職稱** | 曹雪卿 0915 回饋文件（AI CG回饋，hikaru.tsao@gmail.com） | ⬜ 🌊**第二波**（2026-09-15 使用者裁定：等第一波收工才動工） | 使用者原話「標題有修正空間(陳一新入標觀眾較不知他是誰)」。同一張圖的第 5 格寫的是「中國國家安全部長陳一新示警」（有職稱，看得懂），大標題卻只寫「陳一新警告人工智慧威脅政權」。規則方向：**人名在臺灣觀眾的識別度不足時，標題應改用職稱或機構**。與 B45 是同一張圖的兩個層面（B45 是臉，F34 是名字）。 |
| F35 | **播出鏡面示意圖的三個小要求** | 曹雪卿 0915 回饋文件（AI CG回饋，hikaru.tsao@gmail.com） | ⬜ 🌊**第二波**（2026-09-15 使用者裁定：等第一波收工才動工） | 使用者在挖框那兩條裡夾帶的三件事：①「播出鏡面的示意圖是否應標示在例如他挖圓框的地方?」——示意圖該不該標在挖框處；②「示意手機非iPhone 18」——示意圖畫的機型與新聞內容不符；③「最下面那行容易不是重點(eg字少版，統計時間多應為背景說明)」——最後一行常被塞進背景說明而非重點。⚖**2026-09-15 使用者裁 B47 後，①的份量升高**：既然蓋框位置沒辦法從成品反推、預設就是 OFF，那「示意圖標示哪裡能蓋」就是**讓使用者自己判斷**的替代方案，是這三項裡最該做的一項（見 B47）。②③是消化／版面規則。**拆開處理，不要當成一件事**。 |
| F36 | **YT 整點直播：日期方塊與標題的距離可以拉近** | 曹雪卿 0915 回饋文件（AI CG回饋，hikaru.tsao@gmail.com） | ⬜ 🌊**第二波**（2026-09-15 使用者裁定：等第一波收工才動工） | 使用者原話「YT創意選擇規矩，日期方塊與標題距離可以拉近」。監督核對 `20260915-133528`：紅色「2026/09/15」方塊與其下標題塊之間確有明顯間距。純版面數字，**但要先量現行成品再訂數字**（比照 YT 塊高那次的教訓，不要憑感覺給值）。 |
| F37 | **上傳真實照片時沒有地方填「畫面來源」，而且一律被壓上「AI生成示意圖」** | 王結玲 2026-09-15 LINE 回報（她用十點不一樣），使用者轉述 | ⬜ 🌊**第二波**（2026-09-15 使用者裁定：等第一波收工才動工）｜**全站通用，不是十點專屬** | **王結玲原話**：「我找到一個BUG／如果我上傳照片，不要AI示意圖，也要打上照片來源，沒有地方可以打」。**使用者回覆原話**：「沒錯，現在真的沒有這個欄位，這是漏做了。現在預設都是強制用程式壓「AI生成示意圖」，我要把這個欄位開出來，讓使用者可以選填，如果是放置原圖的話，讓使用者可以在這個欄位寫畫面來源」。⚖**使用者已定調**：欄位要開、選填、**全站通用**（她雖然是在十點不一樣遇到，但不要只做那一個版型）。✅**監督查證：輪子已經有一半，不要重造**——「畫面來源」欄位**已經實作在 YT 直播直標那條線**（`index.html:630`「只要填來源名，『畫面來源：』會自動補上」、`main.py:5218` 由 compose 補前綴；成品實例見 `D:\Downloads\AICG\後台紀錄\20260915\曹雪卿成圖\131247-ec36fb8a7ae8.png` 右上「畫面來源：美聯社」）。**本單應該是把這個既有欄位推廣到其他版型，不是新造一套。** ⚠**但有一個既有規則會擋路，動工前先確認是不是要一起改**：`main.py:3932` 的註解明寫「親自提供的真實素材——那個 override 的語意不成立，『AI示意圖』標籤照舊要留」，也就是**現在有一條刻意寫下的規則：就算使用者自己提供真實素材，AI示意圖照壓**。王結玲要的「不要AI示意圖」直接牴觸這一條。**這一條當初是為了播出合規寫的還是順手寫的，要查清楚**——若是合規要求，該做的是「有來源標示時改壓來源、而不是無條件拿掉標籤」。與 **B28**（畫真人掛真名時的 AI示意圖 標籤）同一叢，**建議一起看**。另 `main.py:4374`／`5736` 已經有「底圖是不是 AI 生的」這個旗標，判斷基礎是現成的。 |
| F38 | **提高生圖畫布尺寸——救不了錯字，但救得了「糊」：現況 1280×720 被放大成 1920×1080 交付，是升採樣** | 2026-09-15 使用者提出假說「字太小失真或錯字 → 提升解析度」，測試端實拍、監督獨立複驗 | ⬜ **待裁：要不要為「更銳利」把畫布調大**（與錯字已脫鉤）｜**錯字那一半已結案＝解析度救不了、且錯字與字級反相關**｜裁前要先查大尺寸有沒有加價 | **測了什麼**：同一篇 276 字颱風稿、字超多、同一份消化結果（seed 1633473102）、同一組 prompt，**唯一變因是 `NATIVE_GPT_IMAGE_SIZES["16:9"]`**——A＝`1280x720`（現況）、B＝`2560x1440`。證據全份在 `D:\Downloads\AICG\後台紀錄\20260915\解析度對照\`。🔑**架構層級發現（監督已獨立以 `magick identify` 複驗，非採信轉述）**：`safe_area_spec.py:13` 的 `BASE_CANVAS=(1920,1080)` 讓編輯版安全框 ON 時，`apply_safe_frame` **不管 AI 原始畫布多大，成品一律輸出 1920×1080**。實檔為證：`reso_imageA_source.png`=1280×720 → `reso_imageA.png`=**1920×1080**；`reso_imageB_source.png`=2560×1440 → `reso_imageB.png`=**1920×1080**。👉**所以放大生成畫布不會讓字變大**——字級與版面比例是消化階段決定的，與生圖畫布無關。❌⚠**錯字這一半：本輪結論作廢，樣本沒踩到考題（2026-09-15 使用者當場指出，查證屬實）。**使用者原話：「應該要測試字超多的情況，不是測字少，字少當然看不出差別啊」。查證：送出的 `density` **確實是 `maximum`**（原始 payload `reso_digest_payload.json` 為證，且該欄是 pydantic Literal，打錯值會 422 擋下、不會靜靜落回預設），**但消化端實際只產出 6 塊／49 字**（逐塊精算，非目測），**比 D16 任何一檔實拍都輕**——連 D16 的字多（6 塊／131 字）都不到，更別說字超多（8 塊／123 字）。**錯字只在字擠爆版面、字級被壓到極小時才會出現，這輪根本沒到那個狀態**，所以「A 張 0 錯字」不是好消息，是沒考到。👉**「解析度救不了錯字」這個結論降級為未驗證**（道理上的推論仍然成立：模型畫錯的是字形本身，重新採樣不會把錯字變成對字——但那是推論，本輪沒有實證）。**「糊救得了」那一半不受影響**，升採樣糊化是幾何事實，與稿件密度無關。這個落差另開 **B57** 追。 A、B 兩張**都是 0 錯字**（監督獨立看過全圖與 300% 裁切，「銀河颱風逼近／海上警報生效／955百帕／45公尺每秒／18公里北轉／東半部南部有豪雨／停班課明晨公布／農委會盤點菜價」逐字正確）。A 本身就沒有錯字可減少，所以**這對樣本無法回答錯字問題**；而且道理上也救不了——模型畫錯的是**字形本身**，升採樣或降採樣都不會把錯字變成對字。✅**但「糊」這一半救得了，而且效果明顯**：現況是 **1280→1920 升採樣（放大 1.5 倍）**，本來就會糊；B 是 **2560→1920 降採樣（0.75 倍）**，帶著真實細節。**監督把同一句「停班課明晨公布」在各自位置裁切放大 300% 對看，B 的筆畫邊緣明顯乾淨、A 明顯有升採樣的軟邊——差距不是測試端寫的「略銳利」，是一眼看得出來。**⚠**監督對測試端回報的一處更正**：回報寫「A/B 逐塊配置一致，沒有重排」。**區塊的種類與數量確實一致（1 標題＋3 徽章＋3 卡片），但幾何配置明顯不同**——A 的三張卡片置中且較窄，B 的卡片幾乎滿寬、整體左移，徽章位置也不同。**代表畫布尺寸會連動改變模型的版面決策**，「唯一變因」只在輸入端成立、在輸出端不成立。這削弱 A/B 的可比性，也是另一個要記住的行為。💰**代價**：耗時 A 33.8 秒 → B 39.5 秒（**慢約 17%**）；單價不明——回應不帶 OpenRouter generation id，**大尺寸是否加價要從帳號後台核對**，在核對之前不要假設仍是 $0.04。💡**待裁的選項**：甲｜**把 `NATIVE_GPT_IMAGE_SIZES` 的 16:9／21:9 調大**（例如 1920×1080／2520×1080 以上），讓交付尺寸不再靠升採樣，換來全站文字與線條更銳利，代價是每張慢約 17% 與未知的單價變動；乙｜**維持現況**，接受升採樣的軟邊。**監督傾向甲，但必須先查清楚單價**——若大尺寸加價，這就從「幾乎免費的畫質提升」變成成本決策，要由使用者裁。📌**順帶結論：錯字要往別處查。** 若之後真的量到字超多有實質錯字，根因請往 D16 那組密度上限查（塊數／字數擠爆版面），不要再回頭動畫布尺寸。 🎯**2026-09-15 第二輪（記者版密集）＋監督在既有正式站成品裡找到決定性反證——「錯字」這一題結案：不是解析度問題，而且方向完全相反。****第二輪實測**（使用者授權 2 張）：沿用 B57 那份記者版消化結果（8 塊／138 字，seed 1690566148），不重新消化，唯一變因是 `NATIVE_GPT_IMAGE_SIZES["21:9"]`——A=`1680x720`、B=`3360x1440`。模型確實吃到 2 倍；**成品兩張仍皆為 1920×1080**（監督以 `magick identify` 複驗），`BASE_CANVAS` 鎖死播出尺寸的架構結論在記者版上再次成立。**A＝0 錯字、B＝0 錯字**；版面幾何這次基本一致（與編輯版那輪的位移不同）；耗時 33.7→37.9 秒（慢約 12%）。🔑**真正的答案來自零成本的既有證據**：監督翻 `D:\Downloads\AICG\後台紀錄\20260915\曹雪卿成圖\` 時，發現兩張現成的放大證據——`證據_標題騙字壞掉_放大.png` 與 `證據_同圖第1格騙字正確_放大.png`。**同一張正式站成品裡，「騙」這個字出現兩次**：**大標題那個（全圖最大的字）筆畫糊成一團、馬部下方四點變成亂筆，是壞字**；**內文區塊那個（明顯小得多）筆畫乾淨、完全正確**。👉**同一張圖、同一個字、同一次生成、同一個解析度——小的對、大的錯。****這直接推翻「字太小所以出錯字」的前提，也連帶推翻「提高解析度能救錯字」**：如果全圖最大的那個字都畫壞了，像素從來就不是限制條件。⚠**誠實補一句**：大標題與內文除了字級不同，**樣式也不同**（標題是 3D 立體擠出＋發光，內文是平面字），所以嚴格說變因不只一個。但無論如何，**「字越大越不會錯」已經被否證**，加解析度的理由不成立。✅**結論（錯字這一半，結案）**：**解析度救不了錯字，而且錯字與字級無關（甚至反相關）。**真正的候選變因是**字體樣式的複雜度**（3D 擠出／發光／描邊會把筆畫吃掉），這條要另開單查，不要再往畫布尺寸走。✅**糊／失真這一半仍然成立且不受影響**——升採樣是幾何事實，與錯字無關。**F38 的待裁選項因此收斂成：要不要為了『線條與文字更銳利』而把畫布調大，代價是慢 12–17%＋未知單價。** 跟錯字完全脫鉤。⚠**順帶一個證據檔的瑕疵**：測試端存的 `dense_imageB_scaled1680x720.png` **實際是 1280×720**（監督 `magick identify` 查到），檔名與內容不符，疑似沿用上一輪的參數。本輪結論不受影響（兩張成品本來就都是 1920×1080，直接對比即可，那個縮圖根本用不到），但**該檔名會誤導後人，要修正或刪除**。 |
| F39 | **後台介面不顯示生成參數（density／role／seed）——但後端其實有記，是「看不到」不是「沒有」** | 2026-09-15 監督派工失誤後追出來的根因 | 🔶 **已修待驗（exp `000da0d`，2026-09-16）——後台已渲染 role／density／seed，未部署**｜🔻**2026-09-15 監督自我更正：原標題「不記 density」是錯的，`request_log.py` 有寫、JSONL 有這欄，缺的是後台介面與匯出**｜**嚴重度下修、但價值上修——代表舊案有機會精確重現**｜與 F30／F31 同一類，建議一起做 | **怎麼被發現的**：使用者要求重現後台紀錄 `2026-09-15 10:14:12 曹雪卿`，監督的交辦單寫「density 照該紀錄檔記載」，**但該檔根本沒有 density 欄位**（`grep -oiE '字超多|字多|字少|字極少|無字|不改字|maximum|standard|simplified' 20260915_101412.txt` 六個檔位字樣**零命中**）。執行端只能自行挑預設值 `standard`（字多），**整輪 2 張付費測試（約 $0.08）測到錯的檔位**，產出 4 個短塊，而目標成品是 8 塊、每塊兩行——**根本沒有重現到**，由使用者肉眼看出（原話：「剛剛那一批 根本不是測字超多啊 字非常少」）。**後台表頭只有 `時間｜使用者｜類型｜成圖｜內容`**（F31 已記過），**角色、density、安全框、蓋章、色調、seed 一個都沒有**。👉**後果不只是這次白花錢**：只要使用者回報「某張圖有問題」，我們就**永遠無法精確重現那張圖的生成條件**，只能靠猜或靠肉眼比對塊數回推。這是所有「重現使用者回報」類調查的共同天花板。💡**修法**：後台紀錄補上生成參數（至少 role／density／safe_frame／stamp／tone／seed／editor_format）。**seed 尤其關鍵**——有 seed 才談得上真正的重現。📌**與 F30／F31 的關係**：F31 是「失敗完全不留痕」、F30 是「成功不記耗時與 provider」，本條是「成功不記生成參數」。**三條是同一個病：紀錄的欄位不足以支撐事後調查。****建議三條合併成一次改，不要分三次動同一段程式。**⚠**流程面的連帶教訓（監督自記）**：交辦單**不准用「照某檔記載」代替字面值**，派工前要自己開檔確認欄位存在；每輪付費測試前要先過一道「產出有沒有重現到目標條件」的關卡（塊數／行數同量級），不合格就作廢重來，不拿它下結論。 🔻**2026-09-15 監督自我更正（查 OpenRouter 用量時順手查出來的）**：**本條原本的標題與診斷有誤，`request_log.py` 其實有記 `role`／`density`／`seed`。**程式碼實證：`log_generation()` 的參數列有 `density: str = ""`（line 77）、`seed: int | None = None`（line 85），寫入字典時有 `"role": role`（99）、`"density": density`（100）、`"seed": seed`（114），`log_rejection()` 同樣有（135／155／156）。本機 `logs/generations-20260915.jsonl` 也確實可以靠 `density` 欄位把當天的呼叫分檔位統計出來（實跑：`maximum` 26 筆、`standard` 377 筆等）。👉**所以真正的缺陷是「後台介面與匯出的 txt 只吐五欄」（＝F31 講的那件事），不是後端沒記。** 本條併入 F31 的範圍，**嚴重度由「無法重現」下修為「要多繞一層才拿得到」**。🔑**但價值反而上修，這點很重要**：既然 JSONL 有 `role`／`density`／`seed`／`prompt`，**正式站主機上的 `logs/generations-*.jsonl` 應該就存得下曹雪卿那幾筆的完整參數**——先前判定「10:14:12 那筆無法重現」是**基於後台匯出的 txt**，**不等於正式站的 JSONL 也沒有。**✅**下一步（零成本、不打任何 API）**：想辦法取得正式站的 `logs/generations-20260915.jsonl`（或請有主機權限的人撈出指定時間的那幾列），用 `ts` 對 `10:14:12`／`17:28:51` 把 `role`／`density`／`seed`／`prompt` 直接讀出來。**若撈得到，B58／B57 那幾輪就不必再用猜的參數重打，可以省掉整批付費重測。**⚠**注意 B29 仍然成立**：`prompt` 欄位寫入時被 `MAX_PROMPT_CHARS = 4000` 截斷，**VARIABLE FIELDS 落在截點之後**，所以 prompt 全文仍不可靠；**但 `role`／`density`／`seed` 是獨立欄位、不受那個截斷影響，這三個是可信的。**📌**本機查不到那兩筆**（實跑確認 `ts` 沒有 `10:14:12`／`17:28:51`），因為那是**正式站**產生的，本機 log 只有本機的呼叫——這也再次確認要撈的是正式站那份檔。 🔶 **2026-09-16 已修待驗（exp `000da0d`）＋「撈正式站 log」這條路已結案：撈不到，改走後台渲染。** ❌**「取得正式站 `logs/generations-*.jsonl`」不可行**：`aicg.tvbs.ai` 跑在**公司 GCP 專案**，本機 gcloud 只看得到個人專案 `tvbs-aicg-linebot`（B39 欄位早就記過這一點，本條當時沒接上）。容器磁碟上的 JSONL **沒有任何對外路徑**，`/admin` 也只掛 `/admin` 與 `/admin/image/{month}/{filename}` 兩條，**沒有 raw JSON 或匯出端點**。✅**實際撈到的**：`GET /admin?month=2026-09` 回 200，**200 筆——但那是上限不是總數**（`audit_archive.list_records()` 的 `limit: int = 200` 寫死，`/admin` 也沒有調整它的查詢參數；實撈回到 09-07 就停，更早的看不到，這點另記在 F31），其中**曹雪卿 0915 共 22 筆**（09:50:47～17:32:40），每筆有 `ts`／姓名 email／類型／`image_model`／`request_id`／**新聞原文全文**／`prompt` 前 4000 字／**成品圖（22 張全數下載完成）**。落檔：`D:\Downloads\AICG\後台紀錄\20260916\`（`admin_2026-09.html`、`曹雪卿_0915_後台原始.json`、`全部_2026-09_後台原始.json`、`F39_正式站撈log_結果.md`），成品圖補進 `D:\Downloads\AICG\後台紀錄\20260915\曹雪卿成圖\`。✅**修法（本次做的）**：`admin_console._params_line()` 把 `role`／`density`／`seed` 渲染出來，純唯讀、沒動任何寫入端；缺值印「－」而不是整段藏起來（藏起來分不出「這個版型沒帶」與「後台不顯示」）。**部署後連 0915 的舊紀錄都查得到 `role`／`density`**——那三個欄位本來就在正式站磁碟的歸檔 JSON 裡（`main.py:4222` 從 `dffef81` 起就有傳），只是沒印。⚠**`seed` 例外**：它是 F0（Stage 2-4，2026-09-15）才加的、只在 `exp`，正式站 0915 跑的版本很可能沒有，舊紀錄會是「－」。⚠**塊數仍救不回來**：見 B29，0915 那 8 筆的 `VARIABLE FIELDS` 已經被截掉，只能改從成品圖上數。🐛**順手抓到的真 bug**：`_esc()` 走 `value or ""`，`seed 0` 會被印成空白，已改 `_esc(str(seed))` 並用測試釘住。**測試 1767 passed（原 1761，+6）。** ✅**2026-09-16 已合併進公司 main（PR #25，merge `9534b11`，VERSION `260916-01`）。⚠仍未部署到正式站**——合併不等於上線，部署要使用者決定。 |
| B33 | 封面「一次 API 都不打」的零成本路徑，其實已經先打了標題斷句 | 同 B31 | ⬜ | `_cover_composite` 的註解自稱「一次 API 都不打」（`main.py:4756`、`4764-4766`），但 `apply_title_break_hints` 在端點入口**無條件執行**，排在 `background_image_base64` 零 API 分支之前：`main.py:5183`（十點，零 API 分支在 `5214`）、`main.py:5896`（YT，零 API 分支在 `5958`／`5988`）。斷句模型 `gpt-5.4-mini`（`main.py:102`／`112`）timeout 8 秒。注意斷句本身有正確的短路（標題夠短就不送，`main.py:4251-4260`／`4306-4309`），**問題純粹在呼叫位置**。把呼叫移到零 API 分支之後即可 |
| D4 | 生圖模式附圖左右指定（全線）| 待裁 |
| D9 | CG 線原圖放置的順序／位置／內文搭配（四條路）| 待裁；路線 3 會牽動 **D12** |
| B10 | 附圖尺寸失控（文件照被縮到看不清）| 併入 D9 的位置／佔比一起訂 |
| B22 | 附圖是資訊載體時仍被當裝飾 | 同上；**但修法落在創意條文**，見下方跨刀 |
| B24 | 附圖在後續生成中消失（15:39 起）| 查完：**不是** `9e747a7`。全程 type＝自動判斷，該 commit 只清十點／YT 槽、不動 `userRefImages`。消化 15:36 仍承認 supplied photograph，**15:39 已改口畫葉門地圖**。依據：正式站 15:39:01 歸檔 prompt 已無 supplied photograph（`D:\Downloads\AICG\後台紀錄\20260914\20260914_0-2_0-3量測.md`）；MASTER 原記 15:44 偏晚。剩餘不確定：15:39 當下附圖是否仍在 payload（歸檔在 asis 注入前）。觀察併 B22／B25 |
| B1 | 已合成成品再餵回去 → 日期重複 | 🔶 **已修待驗（exp）** prompt chrome；overlay 合成未動 |
| M1 | 附圖規則模組 `refs.py` | **必須排在 D4／D9 之後**——模組的形狀（插槽 vs 內文標記 vs hybrid）正是 D9 要裁的東西。先重構會白做 |
| R4 | 附圖未測組合 | 驗收，排最後 |

**可以現在就做的**：B26。B24 查證已結（不是 `9e747a7`）。其餘等裁。

---

### 第二刀：記者／編輯 CG 版的字多字少＋創意階梯

**這一刀的模組邊界最乾淨——兩者在同一個函式裡。**
`build_digest_instructions`（main.py:1487–）：density 區塊在 **1533–1541**、
`cg_creativity_rules(visual_creativity)` 在 **1565**，中間只隔版型區塊。
注入順序有語意（repo 慣例：位置在後＋明文 OVERRIDE 才壓得住前面的命令句），
**重排會改行為，不能當成單純搬字**。

**成員**

| ID | 項目 | 卡在 |
|---|---|---|
| F1 | 字多／字少級距拉開 | D3（天花板非配額要不要鬆綁）|
| D3 | ↑ 的裁決 | 待裁 |
| B19 / F23 / D16 | 大標題字數納入 density | D16（各檔字數與行數）|
| D14 / F20 | 「完全不要文字」當成 BAR 上的一檔 | D14（形式）|
| A1 | CG 線導入變化池 | D2（CG 要不要禁綠）、D5（幅度要不要訂數字）|
| A2 | CG 配件件數改由程式抽 | D5 |
| A5 | 十點創意階梯移植回通用版 | 同 A1 |
| D2 | CG 禁綠 | 待裁；**與 F16（漲跌配色綠＝下跌）正面衝突，兩者要一起裁** |
| D5 | CG 幅度數字表 | 待裁 |
| F0 | seed 欄位三條線統一 | **D1 已裁，可做**——A1／A2／A5／B5 的共同前置 |
| B3 / A3 | 前台三份創意說明表 vs 後端對不上 | 無——B3 是純錯字級修正，A3 是接到後端 |
| B4 / B5 | docstring 過期／與行為矛盾 | 無 |
| R5 | 改 A1 前的長相重複度基準 | **要先跑**，不然改完沒有對照 |

**三個一定要先做的前置**
1. **F0**（seed）——A1／A2／A5 全部靠它才驗得出「有沒有真的變化」。
2. **R5**（基準）與 F1／D16 共用的「先量現行成品」——repo 已有教訓：訂數字前先量。
3. **凍結測試要一次性重凍**。`tests/test_reporter_prompt_frozen.py` 四份 fixture **釘住每一個 density 檔位**，所以第二刀的 density 那一半**必然打破它們**，要刻意重凍——**收在這一刀的最後做一次，不要每改一項凍一次**。
   （反過來，創意那一半碰不到：fixture 的 `visual_creativity` 預設 0，`cg_creativity_rules(0)` 回空字串。）

**記者／編輯的分流其實很淺**：density 只有 `standard` 檔靠 `_STANDARD_LIMIT_CLAUSES[is_editor]` 分岔，
`maximum`／`simplified`／`minimal` 與**全部**創意條文都不分角色。使用者是以
「記者與編輯 CG 版」框這一刀的，實作時要確認每一檔要不要都分流。

---

### ✅ CP1 已放行（2026-09-14）

使用者：「都照你的建議做，你監督，實際修改動手的工人是 claudek2。」

放行範圍**只含 D16 ＋ D2 ＋ 一次重凍（含補編輯版快照）**，**F2 拆出另走 CP1b**。
分工：本 session 監督與驗收、**claudek2 實作**、「測試」session 跑生圖驗收輪。
細節與對 claudek2 的硬性約束見 `docs/plan-20260914-第一波三刀.md` 文末〈CP1 放行＋分工〉。

---

### 建議獨立成一刀：F2（最終 prompt 改 Markdown 瘦身）

改的是同一個函式，看起來該併進第二刀——**但它會重凍全部 fixture**。
跟第二刀綁在一起的話，density 的行為改動與純格式改動會混在同一批 diff 裡，
出事分不出是哪一邊。**先做完第二刀、重凍一次，再單獨做 F2、再重凍一次。**

---

### 跨兩刀 / 不屬於任何一刀

- **A6（能力矩陣加欄位）橫跨兩刀**：附圖順序／位置／內文對應／多張措辭屬第一刀，
  創意池／seed 屬第二刀。矩陣本身建議**兩刀都收尾後一次補齊**，否則欄位定義會跟著改。
- **B22 的歸屬**：症狀是附圖（第一刀），但修法是「裝飾讓位給資訊載體」，
  條文落在創意／版面那一段（第二刀）。**兩刀都要知道它**。
- **剩下的自然分成另兩組**（使用者這次沒點，先記著）：
  消化忠實度（B17／B18／B20／B21／F22／F24）、refine 越權（B13／B23）。

---

### 裁決後更新（2026-09-14，八項一次裁完）

**D2／D3／D4／D5／D9／D12／D14／D16 全部已裁**（連同先前的 D1）。
**兩刀的裁決閘門全部解除**，上面各表「卡在」欄位以 D 表的已裁內容為準。

三件裁決衍生出來的新工作，不在原本的分組表裡：

1. **附圖順序要真的傳得到生圖端**（D9 追加裁決「預設＝上傳順序」）。
   現在 CG 線 `shared_refs=True`，上傳順序在後端被攤平、從來沒進過 prompt。
   **這是第一刀的新前置**——順序欄位沒接通之前，任何順序規則都沒有依據。
   排在 B26 之後、M1 之前。

2. **封面線既有的禁綠範圍要一併收窄**（D2）。
   裁決是禁「螢光綠（chroma key 綠）」，不是禁所有綠色。
   封面線的 `COVER_NO_GREEN_RULE` 與 `COVER_PALETTES` 的「無綠」定義現在訂太寬，
   要跟 CG 線一起改成同一個窄定義。**這讓 D2 從「CG 線加規則」變成「兩條線一起收窄」。**
   連帶：F16（漲跌配色）**不再與 D2 衝突**，可以從「要一起裁」降級為一般項。

3. **大標題字數要先量現行成品**（D16）。
   裁決已定「行數不動、只調字數」，但數字未定。
   量測與 F1／R5 的「先量現行成品」是同一件事，**一次量完**：
   從後台歷史紀錄統計各 density 檔位的標題實際字數與內文字數，一併提數字。

### 追加裁決（2026-09-14）：A1／A2 的設計方向已定

使用者：「創意階梯要導入原本就有的抽素材庫 ＋ 創意 3／4 的時候可以加掛配件（對齊設定）」

**A1 定案：不另建新池，直接沿用既有的變化池。**
`creativity.py` 已有的 `COVER_PLATE_SHAPES`／`COVER_STAGGER_PATTERNS`／`COVER_TYPEFACES`／
`COVER_PALETTES`／`COVER_ANCHORS`／`COVER_TILT_DIRECTIONS`／`COVER_ACCESSORY_POOL`／
`COVER_ACCESSORY_SHAPES`／`COVER_FLAG_ACCESSORY` 就是那個素材庫，CG 線接上去用。
**這讓 A1 從「設計一套池」縮成「接線」**，工作量大幅下降，也順帶解決 A5（十點移植回通用版）
——兩者變成同一件事。

**注意池子目前分兩處**：池子本身在 `creativity.py`；件數表 `COVER_ACCESSORY_COUNTS`
與抽法 `cover_accessories()` 在 `editor_formats.py:574/649`，註解寫明「十點專屬、
跟 titles／full_width 耦合，不是跨拉桿共用的機制」。
**CG 線要共用，這個耦合就得先拆**——這是 A1 的實作前置。

**A2 定案：配件在創意 3／4 加掛，件數由程式抽（不再讓模型自選）。**
⚠ **一個要確認的小落差**：封面線現行 `COVER_ACCESSORY_COUNTS = {0:0, 1:0, 2:1, 3:2, 4:3}`
——**是從創意 2 就開始給 1 件**，不是 3 才開始。使用者說的是「3／4 加掛」。
兩種可能：(a) CG 線自己用 `{0:0,1:0,2:0,3:?,4:?}`，與封面線刻意不同；
(b)「對齊設定」的意思就是照封面線那張表。
**不擋工**——先照使用者字面的 3／4 實作，接線時把這一格標出來讓使用者確認即可。

**仍未裁的只剩 D8（畫風池細節）**，但它不屬於這兩刀，不擋。

---

### 🔴 下一批動刀名單（2026-09-16 使用者指定：「這些問題要列入下一批優先動刀，其他版型也要一起修」）

起因是 2026-09-16 正式站事故（B61）：王結玲與曹雪卿同一個早上，各自被同一個洞咬到——
標題沒寫人名時，封面各版型的生圖 prompt **完全沒有「不得畫具名真人臉」的防線**，
模型自己把新聞主角畫成過期的聯準會主席。子代理全路徑盤點後確認**不是單一版型的疏漏**。

⚠**本節 2026-09-16 合併過一次**：初版只列了 0916 事故那幾條，**漏掉使用者 2026-09-15 已經裁定的下一批**
（D21＋B50、B51＋B52＋B55、B53）。使用者當天就指出來了。**本節是下一批的唯一完整名單**，
0915 與 0916 兩次裁定都在裡面；排序沿用 0915 的「第一優先」宣告，0916 的新項目接在後面。

| 順序 | 內容 | 涵蓋的列管項 | 裁定日 | 為什麼排這個順序 |
|---|---|---|---|---|
| **① 最前面** | **消化 token 預算放寬（D21 甲案）＋ 重試不再擲骰子** | **D21**（已裁甲）、**B50** | 2026-09-15 | 使用者追裁「與 B50 同批、排在最前面」。`DIGEST_MAX_TOKENS=6000` 被思考吃滿、正文寫不出來，而五次 attempt 送的是位元組相同的請求——失敗一次跟失敗五次不獨立，重試等於白重 |
| **② 追加修改的封面例外** | 封面被置框／Logo 角標消失／十點合成版漏掉例外／原圖放置被整張重畫 | **B51**、**B52**、**B55**（規則已裁） | 2026-09-15 | 使用者「下一波優先要修」，三條同一批、同一段程式。B55 的規則使用者當場裁好了：24H LIVE 單張原圖放置＝原圖滿版，**創意階梯只能改標題設計、不得重置原圖** |
| **③ 真人臉這一整條**（0915＋0916 合併） | **B53**：補畫面描述只餵一句標題、不餵新聞原文 → 認錯同名人物、英文名拼錯 → **肖像查照因此失敗**；**B61／B62**：出口強制層（見下）；**B63**：refine 指名換臉 | **B53**（0915 已裁提前）、**B61**、**B62**、**B63** | 0915＋0916 | 🔑**B53 與 0916 事故是同一條線的上游**：查照失敗的原因之一是餵給消化的素材太少。四條一起做才是完整的一刀——**素材（B53）→ 規則（B61／B62）→ 修改（B63）** |
| ③-a | **出口強制層**：所有 `ImageGenerateRequest` 送進 `generate_image_raw` 前，一律過一支共用 baseline 注入（真人／品牌／示意圖／臺灣用語），已含就不重複；**再補一支「列舉所有生圖路徑、斷言最終 prompt 必含鐵則」的測試** | **B61**（六個封面模板）、**B62**（hybrid.js 全裸） | 2026-09-16 | 做出口層，**B61／B62 一起消失**，且從此「新版型漏帶鐵則」是紅燈不是事故。逐個模板補條款是治標，下一個新版型還會再漏 |
| ③-c | **F41 身分鐵則**：人名只抽取不推斷（消化／derive 端不補文中沒有的人名，生圖端不自行放具名真人）；**F40 四層分流**：上傳照 → 維基照片 → 有條目無照片則 AI 自畫（訊息欄通知，不標在圖上）→ 連條目都沒有則畫無人場景 | **F41**、**F40** | 2026-09-16 | 第一層（身分）是 0916 事故真正錯的那一步；第二層（臉）改成四層分流 |
| ③-b | **refine 支援指名換臉**：偵測「換成某某人」→ 查 `photo_lookup` → 附照片並改用允許換臉的規則；**查不到就明講辦不到**並提示改走原圖放置 | **B63**，與 **F37**（畫面來源欄位）、**B28**（示意圖標籤）同一叢 | 2026-09-16 | 使用者實際下的指令現在**結構上必定失敗**，失敗方式還是「悄悄捏一張臉」 |
| **④** | **照片來源覆蓋率**＋「查不到就不要生背影，生成前擋下要求上傳」 | **F40**（新開） | 2026-09-16 | 維基對台灣譯名／新面孔覆蓋率不足，是整條真人臉線的上游；使用者：「畫出背影就是錯的」 |
| **先裁再做 → 已裁** | B66＝寫實為主帶一點插畫筆觸（條文要改寫＋實拍驗收）；M5＝**只做 L1**，L2／L3 暫不動工；F40＝維基維持墊底、不接外部圖庫 | **B66**、**M5**、**F40** | 2026-09-16 已裁 | 三條 2026-09-16 一次裁完。⚠F40 仍有一半沒裁：**查不到照片時要生背影還是擋下要求上傳**，動工前要問 |
| **已修待推** | 能力判斷與路由同源 | **B64／B65**（`f07e0a0`） | 2026-09-16 | 已 commit、1768 綠，等公司 PR／部署 |

| **⑤ 收尾小刀（0916 裁定要做）** | A8 安全框殘值強制歸零｜F30＋F31 併成一次改（紀錄欄位不足）｜B56 附圖上的既有文字不再硬性禁止複製 | **A8**、**F30**、**F31**、**B56** | 2026-09-16 | 三件互不相干、都不大，可併成一次收尾。**24H LIVE 兩行使用者這次沒勾＝不列管** |
| **⑥ 份量線（0916 裁定）** | B57 **兩個一起做**（prompt 下限＋後端塊數防呆）；B60 驗收＝**同參數連打 5 次、塊數全落在目標值 ±1** | **B57**、**B60** | 2026-09-16 | ⚠**B57 會改 prompt 文字＝撞凍結快照，動工前要先要到重凍預算** |

**🌊 第二波仍然不動**（B43–B47／B49／F32–F36／F37／D18–D20，2026-09-15 裁定：第一波收工前不得動工）。
**裁決不受波次限制**——D18／D19／D20 隨時可以裁，裁完仍排進第二波才動手。

**架構層面的根因與長線方向見 `M5`**——使用者 2026-09-16 提出「橫向版型、縱向模組」的矩陣化重構，
監督的評估（同意方向、但反對一次性重寫）寫在該列。

---

## D — 待使用者裁決（**未裁決不得動工**）

| ID | 項目 | 來源 | 狀態 | 一句話現況 |
|---|---|---|---|---|
| D17 | **播出鏡面的卡片張數要不要隨消化程度變** | 子代理 stage2 的 2-8 逐格證據（2026-09-15），監督獨立複驗 | ✅ **2026-09-15 已裁：要聯動，張數交給模型決定、不寫死**（待實作，下一波） | **裁決（使用者 2026-09-15）**：卡片張數**要跟消化拉桿聯動**，而且**不要另立一張級距對照表**——把張數交給模型依素材與檔位自行決定。`_broadcast_point_count()` 目前「standard／maximum 四張、其餘三張」的硬編碼要拿掉，改由 prompt 給範圍與意圖、讓模型選。⚠**實作注意**：會動到播出鏡面長相與既有測試；`editor_formats.py` 的字串若落在凍結快照內就要重凍額度，**動工前先確認重凍需求並取得授權**。本輪實拍仍跑舊行為（3／3／4／4），已告知測試端不得當缺陷。 ／ **原問題**：播出鏡面走 `broadcast` 版型時，實測四檔是 **3／3／4／4** 點，而這正是 `editor_formats._broadcast_point_count()`（`editor_formats.py:154-156`）釘死的——standard／maximum 四張、其餘三張。**把 density 的數字調大改不動它**，因為卡片堆的張數是版面實體限制，不是文字量限制。同一則素材記者版（非卡片版型）是 1／2／7／8 點，級距完全拉得開，所以這不是 F1 的數字問題。**要裁的是**：要不要讓卡片張數跟著拉桿走（例如「字極少」＝兩張、「字超多」＝五張），還是接受播出鏡面在這個維度上本來就只有「每張卡寫多長」可調。⚠**牽動版面**：卡片張數改了會動到既有的播出鏡面長相與相關測試，屬視覺變更，**未裁不得動工**。MASTER 的 F1 欄早就預告過「卡片堆版型的點數被版型釘死，拉桿本來就沒差」，這次是把它坐實成可裁的題目 |
| D18 | **YT 直播直標到底要不要使用者提供底圖？** | 曹雪卿 0915 回饋文件 | ⏸ **待裁**｜🌊**第二波**（2026-09-15 使用者裁定：等第一波收工才動工）**，但裁決不必等**——你隨時可以裁，裁完排進第二波動工 | 使用者原話「YT直播直標，我在提供底圖?」——**她不知道這個版型的產出是什麼**。監督核對成品（`20260915-131247`）：產出是一張**去背透明 PNG**，只有 LIVE 角標＋直式標題＋來源字＋TVBS logo，其餘全透明，設計上是要疊在直播畫面上的**覆蓋層**，本來就不該有底圖。所以這不是缺陷，是**產品沒把「這是覆蓋層、不是完成品」講清楚**。要裁的是走哪條路：①介面上直接標示「本版型輸出為透明 PNG 覆蓋層」並給預覽底（棋盤格或範例畫面）；②開放讓使用者可選擇上傳底圖合成；③兩者都做。**牽涉產品定位，不自行決定。** |
| D19 | **消化端可不可以把明確日期改寫成相對期間？** | 曹雪卿 0915 回饋文件（由 F33 展開） | ⏸ **待裁**｜🌊**第二波**（2026-09-15 使用者裁定：等第一波收工才動工）**，但裁決不必等**——你隨時可以裁，裁完排進第二波動工 | F33 實例：「創9月2日以來新高」被改成「逾兩周新高」，而 9/2 到 9/15 只有 13 天，**改寫後是錯的**。這類換算只要模型算錯就是事實錯誤上鏡。兩條路：①**禁止**——明確日期一律原樣保留，字數不夠就砍別的；②**有條件允許**——只在字數真的不夠時換算，並要求保守用詞（「近兩周」而非「逾兩周」）。監督建議①，理由是換算錯誤無法在成品上被發現（畫面上看不出原文寫什麼），但**這會吃掉字數預算，影響字少／字極少檔位**，所以要你裁。 |
| D20 | **「版面指令稿」要怎麼進來？——`[...]` 一個符號兩種語義，要先分家** | 由 B49 展開（許岱軒 2026-09-15 實例） | ⚖ **2026-09-15 使用者已裁：目標是「不改字＋讀得懂 `[]` 指令」**｜🌊**第二波** | 問題：使用者想貼一份**版面規格**（`[大標題] xxx`／`[內文1] yyy`／`[背景 小插畫] zzz`），要求「`[]` 裡的當指令、`[]` 外的一字不改」。但 `[...]` 目前已經是「**強調這段文字**」的語法，兩種語義撞在同一個符號上（詳見 B49 的程式證據）。**三條路，請擇一：** **甲｜換語法分家**：版面欄位改用不會撞的符號（例如 `[[大標題]]` 或 `{大標題}`），`[...]` 維持強調。優點是兩種意圖可以並存、可以在同一段裡混用；缺點是使用者要學一個新寫法。**乙｜新增一個「照排」檔位**：在現有的無字／不改字／字極少／字少／字多／字超多之外多一檔，語義是「照我給的欄位排版，文字一字不改」。優點是使用者只要選對檔位、不用學語法；缺點是檔位已經六個，再加會更難選，而且要處理「選了照排卻沒寫欄位標記」的情況。**丙｜自動偵測**：原文若整段都是 `[標籤] 內容` 的結構就自動視為版面指令稿，忽略檔位。優點是使用者什麼都不用做；**缺點是「自動」失手時使用者完全無從理解為什麼**，而且這正是 B9（自動判斷版型不穩）已經吃過的虧——**監督不建議丙**。**監督傾向甲**：語義衝突用語法解決最乾淨，而且不增加選單複雜度；乙可以之後再補。⚠**不論選哪條，B49 那個「不改字會誤殺」的程式缺陷都要修**（比對時把任何 `[...]` 都剝掉），那是獨立的，不必等這裡裁完。 ⚖**2026-09-15 使用者裁決（原話）**：「許岱軒這個案子也要列入下一波處理。理想是不改字，但讀懂 `[]` 等等符號，是指令不該入題。但要考慮使用者真的把 `[]` 當作文字不是指令時，還是要出現 `[]`。」**解讀**：①**不另造語法、不另加檔位**——甲乙丙三案都不採，目標是把「不改字」這一檔做對；②「不改字」要能**辨識 `[...]` 是版面指令並把它排除在畫面文字之外**（指令不入題）；③**但要留一條路讓使用者表達「這個中括號是字，不是指令」**，那時 `[]` 要照樣畫出來。🔧**連帶的必要修正（B49 上半）**：`verbatim_fidelity_problem()` 的逐字比對必須**兩邊同時**剝掉被判定為指令的 `[...]`，否則「不改字」對這種輸入一定誤殺（重試 5 次回 502）。⚠**剩下一個子問題還沒裁，動工前要回來問**：**使用者要怎麼表達「這個 `[]` 是字」？**常見三種——①跳脫字元（`\[`）；②整段開關（介面上一個「原文含中括號，請照字面輸出」的勾選框）；③靠語意自動判斷（**監督不建議**，理由同 B9：自動失手時使用者無從理解）。**監督傾向②**：勾選框對非技術使用者最直觀，不用學跳脫語法，而且「整篇都是版面稿」與「整篇都是含括號的正文」本來就是兩種互斥的情境，很少需要在同一篇裡混用。但這要使用者裁，**不要自己決定**。 ||
| D21 | **思考封頂失效之後，消化的 token 預算要怎麼訂？** | 由 B48／B41 展開（2026-09-15 本機實測） | ⚖ **2026-09-15 使用者已裁：甲**（放寬 `DIGEST_MAX_TOKENS`）｜**第一波，下一批第一優先**（2026-09-15 使用者追裁：與 B50 同批、排在最前面） | 實測事實：`DIGEST_MAX_TOKENS=6000`，但 claude-sonnet-5 的思考在 8 次呼叫裡有 5 次直接吃滿 6000，正文一個字都寫不出來，`reasoning.max_tokens=2000` 送了也不被遵守。**三條路：** **甲｜放寬 `DIGEST_MAX_TOKENS`（監督建議）**：改到 12000～16000（`MAP_DIGEST_MAX_TOKENS` 已經是 10000，有先例）。**幾乎不花錢，甚至省錢**——token 照實際生成計費，而現在每次截斷是 6000 token 全部白付＋再付一次重試。風險：思考量會隨規則條數繼續漲，牆只是往後推。**乙｜改用 `reasoning.effort`**：OpenRouter 對 Anthropic 系另有 effort 參數（low／medium／high），可能比 `max_tokens` 有效。**要先查證這個模型吃不吃**，不要憑猜。**丙｜換模型或換 provider**：例如指定不開思考的變體。影響面最大，**本波不建議**。**監督建議甲＋乙一起做**：甲立刻止血（幾乎零成本零風險），乙才是治本。⚠**甲單獨做會有一個副作用要一併想清楚**：預算放大後，原本會「快速失敗」的呼叫會變成「跑很久才成功」，單次耗時上升，可能反而更容易撞到 `DIGEST_DEADLINE_SECONDS=230`。**所以甲若通過，要一起重新檢視死線**——但那要先查證部署形態有沒有 300 秒硬上限（見 B48 的既有警告）。 📚**2026-09-15 離線查證（測試查 OpenRouter 官方文件，監督複核程式碼）**：①文件原文「When using the `reasoning.max_tokens` parameter, that value is used directly with a minimum of 1024 tokens.」＋「max_tokens must be strictly higher than the reasoning budget…」——**我們的呼叫 6000 > 2000 完全符合前提**，文件語氣是硬上限而非建議值，**所以文件解釋不了我們的實測**。②`reasoning.effort` 走比例換算（`budget = max_tokens × ratio`，low=0.2／medium=0.5／high=0.8），Claude 系支援 low／medium／high／xhigh／max，程式目前完全沒用這條路。③**`digest_token_budget()` 已排除一條假說**：`density` 只有在 `verbatim` 時才影響上限，`standard`（字多）與 `maximum`（字超多）在資料圖表底下拿到的是**完全相同的 6000**（監督已讀 `main.py:302-316` 原文複核）。**「字多預算比較小」不成立。** 🔍**監督複核時發現兩件測試沒提、但會改變修法的事**：**(一) 實測其實是雙峰，不是一路失控**——成功那三次的 `reasoning_tokens` 是 **1603／2038／2144**，**都貼著 2000 上下**，看起來封頂是**大致有效**的（模型把當前那段想完才停，略微超出屬正常）；失敗那幾次卻是 **5442／6000**，直接吃滿。**所以不是「封頂完全無效」，是「偶爾整個失效」。**本表 B41 與 B48 先前寫的「保護不存在」要按這個修正理解：**是間歇性失效**。**(二) 正文用量非常穩定**——三次成功的正文分別是 1081／1131／1212 token（completion 減 reasoning），與 repo 自己量過的「856-1361」完全一致。**這讓甲案的把握大幅提高**：只要 `DIGEST_MAX_TOKENS` 放到 12000，**即使思考失控到 6000，仍剩 6000 給那 1200 的正文，一定寫得完**。**甲案因此不只是止血，而是能真正吃掉這個失敗模式**，且不必先搞懂封頂為何失效。➕**新增一個測試沒查到的候選（丁）**：`digest_provider_body()`（`main.py:269`）目前送的是 `{"provider": {"order": [...], "allow_fallbacks": True}}`，**沒有設 `require_parameters`**。OpenRouter 的 `provider.require_parameters` 預設是 `false`，代表**允許路由到不支援所請求參數的 provider，並靜默丟棄該參數**。設成 `true` 可強制只走真的吃 `reasoning` 的 provider——**這是「封頂為何間歇失效」目前最具體的可查候選**，而且是一行設定。⚠但它與 `allow_fallbacks` 互動需查證（收緊可能導致過載時無 provider 可用而直接失敗），**不可直接改，先查文件**。 ⚖**2026-09-15 使用者裁決：採甲。** 放寬 `DIGEST_MAX_TOKENS`。**動工邊界（監督補）**：①**只動 `DIGEST_MAX_TOKENS` 這一個數字**，`DIGEST_DEADLINE_SECONDS`／`DIGEST_TIMEOUT_SECONDS`／`DIGEST_ATTEMPTS` 一個都不准碰（那三個屬 B48 的另一半，要先查證部署形態）；②`DIGEST_REASONING_MAX_TOKENS` 與 `DIGEST_REASONING_HEADROOM` 也不動——注意 `digest_reasoning_body()` 是 `min(2000, max_output_tokens - 2500)`，放寬 `DIGEST_MAX_TOKENS` 之後這個 min 仍然取 2000，**思考封頂不會跟著變鬆**，這正是我們要的；③**不要碰 prompt**（凍結快照）；④乙（`reasoning.effort`）與丁（`provider.require_parameters`）**本次不做**，留待治本。**驗收**：`uv run pytest` 全綠；本機用同一篇 2,930 字稿×字多跑 **2 次**確認 `finish=stop`、`ratio` 明顯低於 1.00（成本約 $0.3，**跑前先報**）；並在 `TODO.md` 留下「為什麼是這個數字」的量測依據——**不要只寫一個數字沒有理由**，本專案已經吃過一次「照錯的天花板設計」的虧（B39）。 ||
| D1 | **創意階梯 seed 政策** | 創意階梯三條線對齊清點 G2 | ✅**2026-09-14 已裁** | **裁決（2026-09-15 補充，使用者）：seed 只存後台，不印在圖上。** 查證圖面上沒有現成的通用籤位機制（`compose.py` 只有寫死用途的「示意圖」浮水印），另做一套壓字機制要處理字型／位置／與安全框和蓋章的互動，且會在上鏡畫面多一個雜訊。D1 的目的是「撈得回來」，而使用者實際撈東西的地方就是後台紀錄，所以 seed 寫進 **API response ＋ `request_log` ＋ `audit_archive` metadata** 即達成目的。**seed 絕不進 prompt**——進了就改變生圖輸入，同一個 seed 再也複現不出原圖，功能自我否定。
（以下為原始裁決）**裁決：請求帶明確 `seed` 欄位，前端「重新生成」時遞增**——既可重現又會變，並把 seed 印在成品籤上，使用者回報「這組好」時撈得回來。三條線（CG／十點／YT）統一走這條：十點的 `seed=None` 每次隨機、YT 的 `標題\|日期` 重生同貌都要改掉。**A1／A2／A3／A5／B5 封鎖解除** |
| D2 | CG 線要不要套「文字禁綠」 | 創意階梯三條線對齊清點 G5 | ✅**2026-09-14 已裁** | **裁決：文字／裝飾禁「螢光綠」（chroma key 綠），不是禁所有綠色**——會與綠屏打到穿幫的是螢光綠，深綠／墨綠／橄欖綠等不受限。漲跌數據的綠**保留**（台灣慣例不動，F16 不衝突了）。範圍：CG 線比照封面線。**連帶要修**：封面線既有的 `COVER_NO_GREEN_RULE` 與變化池的「無綠」定義也要一併收窄成螢光綠，現在訂太寬。 ｜ 原現況：封面線有池內無綠＋`COVER_NO_GREEN_RULE`；CG 線沒有，且有反向規則「下跌＝green」（台灣漲跌配色）。**先查播出鏡面／記者版成品會不會疊綠屏**，會的話兩條規則正面衝突 |
| D3 | 字多／字超多的「天花板非配額」要不要鬆綁 | 字多／字少的檔位差距不夠明顯 | ✅**2026-09-14 已裁** | **裁決：鬆綁，但只允許「拆原有內容」**——字超多檔可以把一點拆成兩點，**不得新增原文沒有的事實**。實作要點：條文必須把「拆」與「補」的界線寫到模型分得出來（這正是 B21 那批忠實度問題的高風險區），驗收要比對成品與原文的事實集合是否一致。 ｜ 原現況：兩檔都明文「素材只夠三點就寫三點」，同一篇素材必然塌成同一結果。鬆綁＝拿掉擋模型自己補料的防線，要先想清楚防線怎麼補 |
| D4 | 生圖模式下附圖位置要不要讓使用者指定左右（**全線**，不只 YT） | YT 封面生圖模式…R1／自動判斷版型不穩 Y2 | ✅**2026-09-14 已裁** | **裁決：prompt 明寫哪個元素放哪側**。保留生圖風格，成本低。**但措辭推不推得動模型要實測**（repo 已有多次「許可句推不動模型」的紀錄），實測不過再回頭議 overlay。與 D9 的「預設＝上傳順序」同一條資料流。**2026-09-14 Stage 1**：`USER_REFERENCE_YT_SLOT_PLACEMENT_TEMPLATE` 已上 exp（兩格都有圖才注入；單位測試釘 LEFT／RIGHT 與編號陷阱）。**付費生圖實測未跑**（N×$0.04），措辭若推不動只回報、不升級 overlay。 ｜ 原現況：三方向：(a) 前台明講「位置由 AI 決定」不保證 (b) prompt 明寫哪個元素放哪側（措辭推不推得動要實測） (c) 有附圖一律導向 overlay。**2026-09-14 擴大範圍**：葉怡君在 `/api/generate` 自動判斷線也踩到，兩張文件照每次重生／refine 都左右互換——不是 YT 專屬。文件類附圖左右有語意，互換＝資訊錯置。**另需問王結玲她放的是哪個上傳位**（後台不存附圖，推不出來） |
| D5 | CG 線的「幅度」要不要訂數字表 | 創意階梯三條線對齊清點 G3 | ✅**2026-09-14 已裁** | **裁決：釘在「件數／區塊數」而非畫布百分比**——配件件數、強調元素數、顏色數、背景層數這類數得出來的量。CG 沒有固定版位，不訂百分比。（註：B22「裝飾佔七成畫布」因此不靠 D5 解，要另循 D9 的資訊載體優先條款。） ｜ 原現況：十點／YT 用具體百分比與倍數，CG 全是形容詞。但 CG 畫面沒有固定版位，數字要釘在什麼上面要先想清楚 |
| D6 | 換版型時標題欄要不要也清空 | 版型功能對齊盤點 | ✅**2026-09-14 已裁** | **裁決：不清，維持現狀。** 標題是使用者自己打的內容，換版型常常就是「同一則新聞換個版型再試」，清掉等於叫他重打。附圖位會清是因為不同版型的位子語意不同，標題沒有這個問題。 ｜ 原現況：附圖位已清（`9e747a7`），標題欄刻意沒清，待裁 |
| D7 | `live24_bg=full` 兩格都放原圖的正確行為 | 版型功能對齊盤點 | ✅**2026-09-14 已裁** | **裁決：維持現行，自動切格。** 使用者放了兩張就是兩張都想用。**只要把文件寫對即可，不改程式。**（查證：`main.py` 已寫 `live24_bg != FULL and 兩格都有圖` 才雙切；選 full 時前端藏右格、後端把攤平多張原圖自動切格。）**2026-09-14 Stage 1**：已改 `docs/plan-20260913-live24版型.md` 與 `docs/yt-live-cover-spec.md`，**未改 `app.js`**。待驗。 ｜ 原現況：會併成單則切兩半，文件說 full 只鋪左格——文件錯還是程式漏，已 park |
| D8 | 畫風池細節 | 畫風池 | ✅**2026-09-14 已裁** | **裁決：先搖得動再說，本波不做。** 第二刀的變化池（版形／字體／配色／落點）先上線，看實際能拉開多少。畫風是最大的一個變數，前面的還沒搖動前先動它容易整張圖跑掉。 ｜ 原現況：候選畫風清單、抽法、等級門檻、成品是否標畫風名，全待裁 |
| D9 | 一般 CG 線原圖放置的順序／位置／與內文搭配 | 一般 CG 線的原圖放置 C1–C4 | ✅**2026-09-14 已裁** | **裁決：綁到內文標記**（與 D12 同一套語法）。**追加裁決：使用者沒指定時，預設＝上傳圖片的順序。** ⚠**這條有實作後果**：CG 線現在 `shared_refs=True`，上傳順序在後端就被攤平、從來沒進過 prompt——要先讓順序真的傳得到生圖端（多帶一個序號欄位），規則才有東西可依。 ｜ 原現況：**2026-09-14 已查證**（使用者提問「附多張圖要怎麼告訴 AI 放置」）：位置只寫「自成一塊」由模型自選、**順序完全沒有這個概念**（CG 線 `slots=False`／`shared_refs=True`，上傳順序沒進 prompt）、與內文的對應由消化模型自己決定。`asis_max=0` 是「規則不適用」不是禁止。四條路待裁：最小修補／沿用封面線插槽／綁到內文標記（與 D12 一起裁）／改走 hybrid |
| D10 | hybrid 一鍵成圖：補齊還是明說它是簡化版 | 版型功能對齊盤點 缺口 5／混合版型模式 | ✅**2026-09-14 已裁** | **裁決：hybrid 冷凍中，短期不會更新，先不處理。** 不補齊、也不急著在前台標示簡化版。 ｜ 原現況：附圖、創意階梯、品質閘全沒接 |
| D11 | Cloud Run 是否恢復部署 | （2026-09-14 裁決暫停維護） | ✅**2026-09-14 已裁** | **裁決：維持暫停，之後再議。** 不訂恢復時間點。 ｜ 原現況：目前暫停，正式站停在部署前的版本；近幾批改動線上看不到 |

| D12 | 行內指令的語法與「綁定到哪一句」的機制 | 原文內嵌指令要規則化 Y7／Y8 | ✅**2026-09-14 已裁** | **裁決：沿用圓括號＋口語詞**，正式承認使用者已在用的寫法，學習成本零。待辦：訂一份可辨識的指令詞表，並處理與內容本身圓括號（原文補充說明）的區分。 ｜ 原現況：**使用者 2026-09-14 已裁示要放寬、有真實需求**，但語法未定：`(ICON)`／`(蓋字章)` 這類版面元素標記要正式對應到既有 `[標題]`／`<蓋章>` marker 體系，還是另立行內語法？**先不要自己選一套**——使用者已在用的「圓括號＋口語詞」是最低學習成本的候選 |
| D13 | 刑案／未公開身分當事人是否強制剪影去識別 | 追加修改連打語意漂移 Z5 | ✅**2026-09-14 已裁** | **裁決：可以畫人、可以掛名——這本來就是示意圖，且已標示「AI示意圖」。不強制去識別。** ⚠**但前提要補**：`apply_user_references_to_image_request` 有一條 2026-08-17 裁決——**使用者一附圖，示意圖標籤就會被拿掉**（僅肖像參考照與 AI改圖 例外保留）。所以「畫真人＋掛真名＋沒有標籤」這個組合目前做得出來。**規則要補的是：畫真人並掛真實姓名時，標籤不得被附圖規則洗掉**（登記為 B28）。 ｜ 原現況：葉怡君 11:47／11:51 把卡拉OK衝突當事人畫成寫實人物＋真實姓氏籤，觀眾可能誤認為真人照片。與既有「真實人物圖須參考真實樣貌」方向相反——這一類應該畫得**更不像**才對 |
| D14 | 「完全不要文字」檔位的形式 | 新增「完全不要文字」檔位 W1／W2 | ✅**2026-09-14 已裁** | **裁決：拉桿改六段，無字放最左端**——順序為 無字→字極少→字少→字多→字超多→不改字，把兩個極端推到拉桿兩頭，避免選錯。 ｜ 原現況：使用者已裁示要**融入字多字少 BAR 當固定格式**；待裁細節：拉桿改六段還是另設開關（「不改字」與「不要字」兩個極端擠同一側會不會選錯）、標題要不要一起不要。功能已證實做得到（戴元利 17:26／17:27 純畫面零文字） |
| D15 | 不宜播出視覺內容的清單範圍 | 不宜播出的視覺內容要有避開清單 | ✅**2026-09-14 已裁** | **裁決：除已點名的（遺體、棺材、屍袋、排泄物、尿液）外，四類全部納入**——(1) 傷口／手術／大量血跡特寫 (2) 自殘／自殺手法描繪 (3) 動物虐待／動物遺體 (4) 槍口對人／吸毒器具／遺照。實作見 F21（**不能只加負面清單**，要給正面替代法）。 ｜ 原現況：使用者已點名：遺體、棺材、屍袋、排泄物、尿液。**待確認是否納入**：手術／傷口特寫、自殘、動物虐待、槍口對人、吸毒器具、遺照——電視新聞各有處理慣例，不替使用者決定。規則放哪一層（消化／生圖／正面替代法）也要裁 |
| D16 | 大標題各檔的字數與行數 | 大標題字數要納入字多／字少 H1／H2 | ✅**2026-09-14 已裁** | **裁決（2026-09-14 二次修訂，使用者更正）：字數分級照舊，行數只鬆綁「字極少」一檔。**使用者：「編輯版的標題真的要兩行，預設的字少也要兩行，只有『字最少』可以允許一行，但不強制。」無字＝無標題｜**字極少＝允許單行但不強制**｜**字少／字多／字超多＝維持兩行**｜不改字不動。行數這半**只碰編輯版樣板**（`main.py:957`「標題強制拆兩行」是編輯版硬規則，記者版沒有）。**字數分級完全不受影響**——`simplified` 仍要新增標題字數上限，CP1 的範圍與重凍必要性不變。細節見 `docs/plan-20260914-第一波三刀.md` 文末〈D16 二次修訂〉。
**級距已訂（2026-09-14，0-2 量測完成後）：10／13／18／22**（字極少／字少／字多／字超多，**去空白且去 `<>` 標記**的字元數，編輯版兩行合計共用）。不改字＝不限。依據與樣本量誠實揭露見 `docs/0-2-標題字數量測結果-20260914.md`。⚠ 量測走 `gpt-5.5`（非正式站的 `claude-sonnet-5`），**方向可靠、數字待驗**，驗收輪要在正式站再看一次。
**相對護欄：使用者 2026-09-14 裁決「不要加」。** 量測發現的標題／內文比例失衡（字極少達 1.30、字多只有 0.14，見結果文件〈結論二〉）**只用絕對字數上限處理**，不另外加「標題不得長於內文」的相對條款。此案結案，不再提。
⚠**實作邊界（2026-09-14 監督查證，0-2 校準時發現）：「維持兩行」不適用於播出鏡面。**`editor_formats.py` 的 `_BROADCAST_STAMP_ON`／`_OFF` 第 6 條寫死「variable must be exactly one [標題] line」，**位階在編輯版樣板之後、蓋掉 `main.py:957` 的兩行**。0-2 校準第二格（long／maximum／編輯＝broadcast）實測就是單行，**這是正確行為、不是腳本壞掉**。所以 D16 動的是編輯版樣板那一條，**不得去改 broadcast 的第 6 條**；24 格裡屬 broadcast 的 4 格（long × 四檔 × 編輯）對「行數」不提供證據，只提供**標題字數**證據，照跑不砍。
（以下為前一版，行數部分已被取代）
**裁決（2026-09-14 修訂）：字數各檔分級，且行數跟著動。** 使用者：「如果標題字已經很少了，本來就不需要兩行。」無字＝無標題｜**字極少／字少允許單行**｜字多／字超多維持兩行｜不改字不動。**數字仍要先量現行成品再訂**（實作步驟，不是裁決項）。
⚠**更正一項先前的錯誤估算**：原裁決寫「行數不動」的主因是我說「行數一變會牽動封面塊高表 18/24/30/36% 與 `yt_title_top`，是跨線改動」——**那是錯的**。`build_digest_instructions` 全專案只有一個呼叫點（main.py:2058，`/api/generate` CG 線），**封面線根本不跑消化**，十點／YT 的標題是使用者自己打進 `title_left`（`max_length=40`）。消化產出的 `[標題]` 只影響 CG 線與播出鏡面。另：播出鏡面規則本來就寫「exactly one `[標題]` line」（`editor_formats.py:109`），所謂兩行是樣板在那一行裡塞 `\n` 的視覺指示，**不是程式依賴的結構**，放寬成單行不會打壞解析。 ｜ 原現況：**使用者已裁示要納入**，待裁數字：各檔標題字數（**訂之前先量現行成品**，別直接沿用內文級距——標題是最大的字）、行數要不要跟著動（編輯版現在強制兩行；一變就牽動封面塊高表與 `yt_title_top`，是跨線改動） |
## B — 缺陷

| ID | 項目 | 來源 | 狀態 | 一句話現況 |
|---|---|---|---|---|
| B1 | 已合成的成品再當附圖餵回去 → 日期重複 | YT 封面生圖模式…R2 | 🔶 **已修待驗（exp，2026-09-14）** | **生圖端 prompt** 已在 asis 單張／多張與 aiedit 單張／融合加上 chrome 條款（日期章／LIVE／24H LIVE／台標／示意圖已在附圖裡就不要再畫一份）。⚠ **overlay 合成未動**：`compose_yt_live24_cover` 不論 `draw_title` 都程式貼角標＋日期——那是 Stage 1 範圍外的剩餘。證據仍是王結玲 16:13:08 |
| B2 | web-refine 輸出改掉尺寸（1920×1080 → 1748×924） | YT 封面生圖模式…R3 | ⬜ | 非 16:9，後續任何疊圖都會歪。可能是 B1 的上游真因；要查 refine 線有沒有鎖長寬比 |
| B3 | 前台 YT 塊高說明與後端表對不上 | 創意階梯三條線對齊清點 G6 | ⬜ | app.js `YT_CREATIVITY` 寫 26/31/36/41%，`YT_BRIEF_SPECS` 是 32/36/40/44%。`git log -S'"32%"'` 指到 `b2e0e98`「YT 塊高表訂錯改正」——**表改了、前台說明沒跟上**。純文字修正，零風險，可單獨先送 |
| B4 | `cover_ai_title_style_clause` docstring 過期 | 創意階梯三條線對齊清點 G8 | ⬜ | 還寫「招式段夾在設計條文與 FIXED 之間」，但招式 2026-09-11 第二輪就搬進 `cover_design_brief` 了 |
| B5 | `cover_design_brief` docstring 與行為矛盾 | 創意階梯三條線對齊清點 G2 | ⬜ | docstring 寫「一顆 seed ＝一種長相，重現得出來」，實際呼叫端沒傳 seed（吃系統熵）。D1 已裁走「請求帶 seed」，改完行為後 docstring 就對了 |
| B6 | 消化品質閘只裝在 `/api/generate`，封面線全部裸奔 | 消化品質閘只裝在 `/api/generate` | ⬜ | 截斷／字元污染／頻道洩漏／簡體異體字四道檢查，五個 `digest_completion` 呼叫點一道都沒過 |
| B7 | `DIGEST_MAX_TOKENS` 上限太緊，複雜稿件常截斷 | 同名章節 | ⬜ | 未重新確認 |
| B8 | `/api/generate` 缺重試機制 | 同名章節 | ⬜ | 未重新確認 |

| B9 | 自動判斷版型在同一段文字上不穩 | 自動判斷版型不穩 Y1 | ⬜ | 2026-09-14 葉怡君：同一段文字三次生圖挑了資料圖表／情境示意圖／3D示意流程三種版型，每按一次換一整套版面。**先量測**（同文字跑 N 次看分佈）再決定要不要釘死或給鎖定鈕 |
| B10 | 附圖尺寸失控：文件類附圖被縮到看不清 | 自動判斷版型不穩 Y3 | ⬜ | 同一批裡兩個極端：16:30 佔滿上半部但下方留大空白、16:43 縮成左側小圖完全看不清。使用者明寫「有兩張照片可以使用」，系統沒有「文件類附圖要大到看得清楚」這條規則 |
| B11 | 網頁版缺獨立指令欄位，使用者把指令寫進新聞原文 | 自動判斷版型不穩 Y4 | ⬜ | `(有兩張照片可以使用)`／`(粗體字)`／`(ICON)`／`(蓋字章)`。目前沒被畫進圖裡、粗體確實生效，但需求訊號明確；與 F13「LINE 訊息內帶指令」是同一件事的網頁版 |
| B12 | 行內指令時靈時不靈 | 原文內嵌指令要規則化 Y6 | ⬜ | 現況：文內解析規則第 1 條已涵蓋「plain prose」指令，與專用指令欄**並存機制早就明文寫死**（兩者都遵守、衝突時專用欄優先、互不取消）。問題在**圓括號夾注**沒有明文承認，靠模型自律 → `(ICON)` 四次只中一次、`(蓋字章)` 完全沒中。基準已量：1/4 |
| B13 | refine 移除元素時用不相關的東西補位 | 追加修改連打語意漂移 Z1 | ⬜ | 「集體火化」那格的焚化爐被換成**一疊紙箱**，語意完全掉且後續 refine 救不回來。要補：替代物必須仍承載該格文字語意，否則寧可留空 |
| B14 | 圖像層的內容忠實度沒守住（事實漂移／角色顛倒） | 追加修改連打語意漂移 Z2／Z3 | ⬜ | 「路死動物」畫成端坐的**活貓**；「黃男攻擊范男」被標成「黃男**遭**攻擊」。這種錯在播出畫面上看起來完全正常，不逐句對抓不到 |
| B15 | 外框配色跑掉（紫色 letterbox） | 追加修改連打語意漂移 Z4 | ⬜ | 11:47 那張上下留白帶是紫色，其餘都是標準深藍。要查是版型底色還是模型把背景畫成紫色後被裁切帶進來 |
| B16 | **完全沒有「不宜播出」的視覺規則** | 不宜播出的視覺內容要有避開清單 | ⬜ | grep 三個檔案，遺體／血腥／gore／corpse／不宜**零命中**。只要新聞文字提到就照畫——09:56 那張焚化爐**爐內有動物遺體**。**B13 是它的下游**：這條做對了，那次 refine 根本不會發生 |
| B17 | 消化階段施動者／受動者會對調 | 消化階段的主被動與順序方向 | ⬜ | 「黃男攻擊范男」被標成「黃男**遭**攻擊」。`CONTENT_FIDELITY_RULES` 九條沒有一條管「誰對誰做了什麼」；壓縮時「遭／被／受」是一個字，最容易順手加減。同一張的箭頭方向反而畫對了——**圖對字反** |
| B18 | 使用者的「→」沒有任何判定規則 | 消化階段的主被動與順序方向 | ⬜ | 同一符號兩種意思：`黃男 攻擊→ 范男`＝動作方向、`掃描→無晶片→火化`＝流程時序。grep 過只有漲跌配色那段提到箭頭，與此無關。要規則化：→ 絕不是新聞文字、怎麼分辨兩種用法、讀出來後文字與圖必須同向 |
| B19 | density 完全管不到大標題長度 | 大標題字數要納入字多／字少 | ⬜ | 編輯版樣板只寫「大標題強制拆分為兩行」**沒寫字數**；「每行不超過 15 字」管的是 [內文小標]；四個 density 區塊（含 MINIMAL 的 12 字）全部只釘內文與總字數。**拉桿從最左拉到最右，畫面上最大的那行字理論上一個字都不會變**——與 F1「字多字少差距不夠」是同一病灶的另一面 |
| B20 | **大陸譯名／用語擋不住**（「也門」） | 許岱軒 0914 下午 H5 | ⬜ | `digest_quality_problem` 六道檢查裡的字形檢查只抓**簡體／異體字**；「也門」兩字都是標準正體，完全過關，而且出現在地圖最大的國名標籤上。**規則缺口不是遵守率問題**：要一份可擴充的大陸譯名詞表（整詞比對避免誤殺）＋prompt 明文要求臺灣譯名＋品質閘加一道 |
| B21 | 既有忠實度條文寫了沒守住（連環／飆破／錯字） | 許岱軒 0914 下午 H1 | ⬜ | 原文「接連遇襲」→成品「**連環**遇襲」（`CONTENT_FIDELITY` 第 8 條**點名了這個詞**）；「一度上漲逾2%，來到107.51」→標題「**飆破**107美元」（第 5 條原文就寫 THE 標題 IS WHERE THIS SLIPS）；「荷姆茲」→「賀姆茲」錯字。**先驗證為什麼不生效**（條文位置太後面？）再決定是搬位置還是加正則自動檢查 |
| B22 | 附圖是資訊載體時仍被當裝飾素材 | 許岱軒 0914 下午 H2 | ⬜ | **B10 的加強版**：他附的真 ISW 勢力圖就是這張圖存在的理由（原文寫明「來源: ISW」），卻被縮成中央小圖，約**七成畫布**給了 AI 戰情室裝飾背景，地圖英文完全看不清。要補的是**佔比與主從**規則：附圖被判定為資訊載體（地圖／圖表／文件／截圖）時它就是主視覺 |
| B23 | refine 改動未被要求修改的文字 | 許岱軒 0914 下午 H6 | ⬜ | 16:01 那次 refine 把「佩里姆島」改成「丕林島」（15:52 還是佩里姆島，原文也是）。`IMAGE REFINE RULES` 開宗明義「Apply ONLY the change requested」。同一島的別譯，**不是事實錯誤是擅自改字**——與 B13（紙箱補位）同族：refine 不該碰沒被點名的東西 |
| B24 | 附圖在後續生成中消失（15:39 起）| 許岱軒 0914 下午 H3 | ⬜ | **2026-09-14 查證：不是 `9e747a7`。** 許岱軒 15:36–16:01 全程 type＝自動判斷（沒切十點／YT），`9e747a7` 的 `setEditorFormat` 只 `clearCoverAsis`／`clearYtAsis`，測試也只釘這四個呼叫，CG 的 `userRefImages` 不被清。15:36 消化仍寫 supplied photograph（成品 ISW 嵌圖在）；**15:39 已改口畫地圖**。依據：正式站 15:39:01 歸檔 prompt 已無 supplied photograph（`D:\Downloads\AICG\後台紀錄\20260914\20260914_0-2_0-3量測.md`）；MASTER 原記 15:44 偏晚。15:47 補回 106 字正文仍是 AI 地圖。剩餘不確定：15:39 payload 是否還帶檔（歸檔在 asis 注入前，B29 同類）。較像 B22／B25 族，不是換版型清槽副作用。 |
| B25 | AI 憑空畫地理＋自行加入原文沒有的地名 | 許岱軒 0914 下午 H4 | ⬜ | 15:44 後全是 AI 畫的葉門國界與勢力範圍（最高風險項，見 `docs/error-cases/2026-09-10-台灣行政區界-錯誤-分析.md`）。**地理正確性標待人工複核不自行判定**；但可確定成品出現原文沒有的地名「沙那」，違反 `CONTENT_FIDELITY` 第 1 條 |
| B26 | **多張原圖放置的措辭是單數，很可能只放一張** | 一般 CG 線的原圖放置 C1 | 🔶 **已修待驗（exp，2026-09-14）** | asis ≥2 改走 `USER_REFERENCE_ASIS_MULTI_RULES_TEMPLATE`（張數寫死、上傳順序左→右、編號陷阱：不要用整份 request 的 attached-file index）。單張仍走 `USER_REFERENCE_ASIS_RULES`。消化端 `USER_REFERENCE_ASIS_DIGEST_RULES` 未動 |
## A — 對齊／一致性

| ID | 項目 | 來源 | 狀態 | 依賴 | 一句話現況 |
|---|---|---|---|---|---|
| A1 | **CG 線導入變化池** | 創意階梯三條線對齊清點 G1 | 🔶 **已修待驗（exp，2026-09-15，Stage 2-6）** CG 改用既有 `creativity.draw()` 六池（plate／stagger／typeface／palette／anchor／tilt），輸出為命令句；0 級仍不注入，凍結快照未動 | ~~D1~~、D2 | 使用者回報「最高級還是不夠亮」的直接原因：CG 每級注入固定文字、無抽籤，成品每次同一長相；L4 那段又只描述一種美術。可搬的池子與位元組釘死範圍見來源章節。**好消息**：`test_reporter_prompt_frozen` 只凍 0 級，池子文字碰不到 fixture |
| A2 | CG 配件件數改由程式抽 | 創意階梯三條線對齊清點 G4 | 🔶 **已修待驗（exp，2026-09-15，Stage 2-6）：CG 已接上共用件數表，封面表一個字沒動。** ｜ **件數已定（CP4，使用者 2026-09-14 裁決）：`{0:0, 1:0, 2:1, 3:2, 4:3}`，就是封面線現行的 `COVER_ACCESSORY_COUNTS`（`editor_formats.py:574`），三條線共用這一張，CG 不另立**。⚠**更正（2026-09-15）**：先前寫成 `{2:0}` 是錯的。使用者選的是「跟封面一致」，而封面 level 2 現行就是 **1 件**。若照 `{2:0}` 做，十點／YT 的 L2 會從 1 件掉到 0 件，連帶動到 52 筆 `cover_design_brief_rng_pins_20260911.json` 與 YT fixture——那是**沒有被授權的視覺變更**，也違反 2-5「不藉重構偷改封面行為」。採用現行表之後，「共用一張表」與「不改封面件數」兩個要求同時成立，計畫內的矛盾消失。（由子代理 stage2 在動工前發現並回報） | ~~D1~~（已裁） | 現在只寫「一兩個」「至多三個」，repo 自己記過「許可句推不動模型」，等於配件不存在 |
| A3 | 三份創意說明表的單一真相源 | 創意階梯三條線對齊清點 G7 | ⬜ | B3 | `CG_CREATIVITY`／`COVER_TITLE_CREATIVITY`／`YT_CREATIVITY` 三份手寫陣列，沒有東西釘住它們跟後端表一致——B3 就是這樣漏掉的 |
| A4 | 版型功能對齊盤點剩餘缺口 | 版型功能對齊盤點 | ⬜ | D9、D10 | 缺口 1、2 已補；剩 3（CG asis 未定義）與 5（hybrid 全無），兩者都已拆成 D9／D10 |
| A6 | **能力矩陣要加欄位**：附圖順序／位置／內文對應／多張措辭／創意池／seed | 播出鏡面與編輯一般 CG 的對齊狀況 | ⬜ | D9、D1 | **2026-09-14 查證：播出鏡面與預設 CG 指向同一個 `Capability` 物件**（`DEFAULT_FORMAT: _CG_CAPABILITY`／`"broadcast": _CG_CAPABILITY`），十三個旗標結構上不可能不一致；刻意不同的只有挖空框／挖哪側／點數／浮水印位，那是版面實體差異。**真正的缺口是矩陣只有十三欄**，本輪清點出來的六軸一欄都沒有——要把它們加進矩陣，缺口才會從「找 if」變成「把表填齊」 |
| A7 | **創意拉桿在 YT 雙則底圖上完全沒有作用（單則有、雙則沒有）** | 由 B45 調查展開（2026-09-15，監督本機實測） | ⬜ 🌊**第二波**（跟 B45 同一條線） | 單則走 `_yt_cover_background` → `editor_formats.YT_COVER_VISUAL_PROMPT_TEMPLATE`，模板有 `{creativity}` 槽；**雙則走 `yt_dual_background` → `_cover_panel_image` → 十點那支 `COVER_VISUAL_PROMPT_TEMPLATE`，模板沒有 `{creativity}` 槽，函式參數列也收不到 creativity**。本機實測雙則面板的最終 prompt 出現 `creativ` **0 次**。使用者在前台拉了拉桿、雙則的底圖長相不會有任何改變——這與 2026-09-13 使用者對 live24 的裁決「標題完全沒有被創意階梯影響 這是錯的」是同一種病，只是這次發生在**底圖**而不是標題。⚠**修之前要先裁**：雙則兩格是半版方圖，十點那支模板是為方圖寫的；直接換成 YT 那支 16:9 模板會連構圖規則一起換掉。 |
| A8 | **封面五版型的安全框開關在前台是隱藏的，但它的殘值仍會被送進後端** | 由 B51／B52 盤查展開（2026-09-15） | ⚖ **2026-09-16 使用者已裁：要做**——切到封面版型時 `state.safeFrame`（與同樣被隱藏的 `stamp`）強制歸零，不再把看不見的殘值偷渡進請求｜⬜ **待裁** | `ten_cover`／`yt_live_cover`／`yt_hourly_cover`／`yt_live24_cover`／`yt_hot_cover` 五個版型的 `hides` 都有 `safeFrame: true`，使用者在封面版型下**看不到也改不了**那顆開關；但 `state.safeFrame` 不會因為切版型而重置，追加修改（B52 的合成版路徑）與任何讀 `state.safeFrame` 的地方拿到的是**使用者上一次在 CG 模式留下的值**。**隱藏而不歸零＝把一個看不見的狀態偷渡進請求**，使用者無從得知也無從更正。待裁：切到封面版型時是否應該把 `state.safeFrame`（以及同樣被隱藏的 `stamp`）強制歸零／或在送出時一律忽略。 |
| A5 | 十點創意階梯移植回通用版 | 待建立：把十點封面的創意階梯移植回通用版創意階梯 | 🔶 **已修待驗（exp，2026-09-15，Stage 2-5／2-6）** 件數表與抽籤搬進 `creativity.py`（`accessories()`），`editor_formats` 只留十點 adapter；CG 直接用同一支，沒有另立一套池 | ~~D1~~、A1 | 與 A1 高度重疊，應一起裁一起做 |

## F — 功能待建立

| ID | 項目 | 來源 | 狀態 | 一句話現況 |
|---|---|---|---|---|
| F0 | **實作 D1 的 seed 欄位**（三條線統一） | 創意階梯三條線對齊清點 G2 | 🔶 **已修待驗（exp，2026-09-15，Stage 2-4）** 三條線的 request／response 都有 `seed`，沒帶就由 `next_generation_seed()` 現抽並回報；前端三條線各存一顆、只有「重新生成」才 `bumpSeed`。seed 進 `request_log` 與 `audit_archive` metadata，**不進 prompt**、**不印在圖上**（D1 2026-09-15 補充裁決）。YT 的 `標題\|日期` seed 已移除 | D1 已裁：請求加 `seed`、前端重生遞增、成品籤印 seed。**A1／A2／A3／A5／B5 的共同前置**，要先做 |
| F19 | 行內指令規則化實作 | 原文內嵌指令要規則化 | ⬜ | 依賴 D12。三塊：明文承認圓括號夾注（含與內容括號的區分反例）、局部指令綁定到相鄰句子、版面元素標記對應 marker。驗收：同段文字跑 N 次統計標記命中率 |
| F20 | 「完全不要文字」檔位實作 | 新增「完全不要文字」檔位 | 🔶 **已修待驗（exp，2026-09-15，Stage 2-9）** 六段拉桿最左端；`NO_TEXT_DENSITY_RULES` ＋ 壓在最後的 `NO_TEXT_FINAL_REMINDER`（逐項點名關掉 `[標題]`／`[內文小標]`／`<蓋章>`／`<底帶>`／卡片數／創意階梯的字級條文）；無字不注入蓋章條文。品質閘原本把「variable 為空」當故障，無字會連撞 5 次重試後 502，已改成只對無字放行這一項。生圖端另加 `NO_TEXT_IMAGE_OVERRIDE`（前後端逐字同步，parity 測試守著）。**照使用者裁決仍呼叫 digest，沒有跳過** | 依賴 D14。連帶要盤點無字模式下失效的區塊（蓋章／色調／安全框文字留白／斷句／品質閘／創意階梯的字級條文），逐一決定不注入還是改寫——留矛盾句模型會挑最寬鬆的遵守。能力矩陣加旗子；無字可跳過整個 digest 呼叫（順手的 token 效益） |
| F21 | 不宜播出內容規則實作 | 不宜播出的視覺內容要有避開清單 | ⬜ | 依賴 D15。**不能只加負面清單**（commit 73ae198 記過：排除條文埋在一長串否定句中間模型照樣犯），必須同時給正面替代做法（「集體火化」→ 空的焚化爐外觀／設施外觀／象徵性火焰） |
| F22 | 主被動、順序、「→」規則化 | 消化階段的主被動與順序方向 | ⬜ | 三件：施動者／受動者不可對調、原文給的先後必須保住、「→」的判定。**與 F19 相鄰但不同**：`(ICON)` 是指令（不可出現在成品），`→` 是內容的結構標記（必須反映在排列上）。做 F19 時一併想清楚這條界線 |
| F23 | 大標題字數納入 density 實作 | 大標題字數要納入字多／字少 | ⬜ | 依賴 D16。注意封面線另有 pydantic 40／60 字與 compose「標題太長」400 三道**不同層**的上限（那是使用者自己打的標題），別打架；逐字模式與無字檔兩個端點要明確排除。**與 F1 同一批驗收**，不要分兩次跑 |
| F24 | 臺灣譯名／用語詞表 | 許岱軒 0914 下午 H5 | ⬜ | 依賴 B20。也門→葉門、奧巴馬→歐巴馬、悉尼→雪梨、朝鮮→北韓、信息→資訊、視頻→影片…。做出來後**回頭掃歷史紀錄**，看還有多少大陸用語已經播出去過 |
| F1 | 字多／字少級距拉開 | 字多／字少的檔位差距不夠明顯 | 🔶 **已修待驗（exp，2026-09-15，Stage 2-8）——程式面 no-op，只補驗收契約** 點數與行長在 0-2 校準時已定、標題上限由 D16／2-1 落地、maximum 的可拆分由 D3／2-7 落地；剩下唯一可動的 `STANDARD`／`SIMPLIFIED` 兩塊逐位元存在於凍結快照裡，動它們就是破壞 CP1。新增測試釘住四個上限嚴格單調、各檔點數天花板仍在、播出鏡面卡片數不隨拉桿長。**實拍量測仍欠**，由測試 session 跑。⚠**2026-09-15 補證據後拆項**：逐格重算發現 F1 底下其實是四件事——記者版／非卡片版型**結案**（1／2／7／8 點、10／50／135／159 字，倍數級距）；短稿四檔相同是**正確行為**；播出鏡面被卡片張數釘死另立 **D17**（待裁）；編輯版 `minimal` 出 3 點違反自己的規則另立 **B34**（要重凍，本波不做）。四者都不需要動 `STANDARD`／`SIMPLIFIED`，「程式面 no-op」仍成立。詳見 R 區〈監督驗收：Stage 2-4～2-9〉的更正段 | 依賴 D3；**驗收要量測**，同篇新聞跑五檔數字數，且要分版型量（卡片堆版型的點數被版型釘死，拉桿本來就沒差） |
| F2 | 最終 prompt 改 Markdown 大瘦身 | 最終 prompt 仍有多餘符號 | ⬜ | 先量基準再改；注入順序有語意不能重排；全大寫 OVERRIDE 措辭是覆蓋機制不是裝飾 |
| F3 | 畫風池實作 | 畫風池 | ⬜ | 依賴 D8 |
| F4 | 「整理包」封面（雙主題疊層） | 同名章節 | ⏸ | 等使用者給樣本 |
| F5 | YT 直播封面「氣象站」「國會風雲」 | 同名章節 | ⬜ | 未動 |
| F6 | 色調亮／暗改成 5 段拉桿 | 同名章節 | ⬜ | 未動 |
| F7 | 「以圖生圖」 | 同名章節 | ⬜ | 未重新確認 |
| F8 | 追蹤修訂（原圖不整張重生，只改細部） | 同名章節 | ⬜ | 未重新確認 |
| F9 | 真實人物圖須參考真實樣貌 | 同名章節 | ⬜ | 未重新確認 |
| F10 | 禁止生成未提及的品牌 LOGO | 同名章節 | ⬜ | 未重新確認 |
| F11 | 使用者上傳參考圖（人物照＋一般視覺參考） | 同名章節 | ⬜ | 未重新確認 |
| F12 | LINE 編輯版＋未選角色一律擋下 | 兩個同名章節（07-31、08-04） | ⬜ | 未重新確認 |
| F13 | LINE 訊息內帶指令＋「已消化完稿」直出 | 同名章節 | ⬜ | 未重新確認 |
| F14 | LINE／行動版「重新生成」 | 同名章節 | ⬜ | 未重新確認 |
| F15 | Webhook 訊息過濾器 | 同名章節 | ⬜ | 未重新確認 |
| F16 | 漲跌配色符合台灣習慣 | 同名章節 | ⬜ | 規則已注入 CG 線；**與 D2 的禁綠正面衝突**，兩者要一起裁 |
| F17 | 給主管的專案進度報告（單頁 HTML） | 同名章節 | ⬜ | 未重新確認 |
| F18 | 以主管報告為底稿做投影片 | 同名章節 | ⬜ | 未重新確認 |

| F25 | 生成速度：先做端到端分段計時 | 使用者 2026-09-14 問「生圖速度還有辦法變快嗎」 | ⬜ **列 TODO，之後處理** | **現況沒有分段計時，不知道消化佔幾秒、生圖佔幾秒，任何加速決策都是猜的。** 第一步是純量測、不動行為、不花錢。量完才決定動哪裡 |
| F26 | 生成速度：生圖端的兩個旋鈕（已評估，暫不動） | 同 F25 | ⬜ **不建議動** | `quality` 現為 `medium`（`main.py:3000`，吃 `OPENAI_IMAGE_QUALITY`），降 `low` 會快但畫質掉；換 `flare` 比 `sunburst` 快，但 `main.py:2619` 註解記明當初就是為畫質選 sunburst。**兩個都是拿畫質換速度**，上鏡的東西不值得。留紀錄避免日後有人重新發現又重新評估一次 |
| F27 | 生成速度：多張同輪次改並行 | 同 F25 | ⬜ | 若現況是一張一張序列跑，改並行是純賺（不犧牲畫質）。**要先確認現況是不是序列**，依賴 F25 的量測 |
| F28 | 生成速度：思考上限可能還有空間 | 同 F25 | ⬜ | `DIGEST_REASONING_MAX_TOKENS = 2000`（`main.py:202`）是保守值。註解裡觀測到的思考 603–4873 是**封頂前**的數字，封頂後實際用多少沒量過。往下壓要小心截斷，**依賴 F25 的量測**。與 F20（無字檔位直接跳過整個 digest 呼叫）同向 |
| F29 | 生成速度：稽核歸檔移出請求路徑 | 子代理速度調查 2026-09-14 | ⬜ | `_archive_generation`（`main.py:2447`／`3899`）在**回傳成品前**同步做 GCS 兩個 blob 上傳（`gcs_archive.py:49-53`）＋本機寫圖與 JSON（`audit_archive.py:68-85`）。改成 `BackgroundTasks` 或背景 thread 即可，**不犧牲任何畫質**，使用者等待直接少掉這一段。與 B29（歸檔內容不完整）同一支函式，**若要一起改，順序是先 B29 再 F29**，免得搬到背景後更難除錯 |

### 生成速度這一族（F25–F28）的前提，寫在這裡免得日後重打

使用者 2026-09-14 問過兩次相關問題，結論記下來：

1. **prompt 瘦身（F2）不會讓生成變快。** 這個 repo 自己量過兩次
   （`main.py:155-200` 的長註解）：寫出來的正文非常穩定（856–1361 token），
   會爆的是思考（603–4873），而且**思考量跟著規則條數漲、跟 prompt 字元數無關**。
   F2 的效益是模型遵守度與成本，不是速度。
2. **生圖那段沒有免費的午餐**（見 F26），要快就要拿畫質換。
3. **慢的常常不是生圖而是消化**：實測撞過「重試後 269 秒才回應」，
   離 Cloud Run 的 300 秒硬砍只差一點，最壞還要跑五次 attempt（`DIGEST_ATTEMPTS = 5`）。
4. 所以**第一步一定是 F25 量測**，不是直接動旋鈕。

**使用者裁示：「生圖速度先列 todo，之後處理。」本波不做。**

### 子代理靜態調查結果（2026-09-14，純讀碼未實測）

**架構前提（先前理解有誤，更正）**：網頁版的 `/api/generate` **只做消化、不生圖**，
成品圖是前端再打第二支 `/api/images/generate` 拿的（`app.js:2846` → `2868`，序列），
**兩支各自吃一份 Cloud Run 300 秒**。只有 LINE 那條 `generate_news_image`（`main.py:3771`）
才把消化與生圖擠進同一個請求（`3787`／`3817`）。
另：`DIGEST_TWO_STAGE` **預設是關的**（`main.py:826`），分類那一次 20 秒往返預設不發生。

**並行現況**：全專案只有兩個並行點，都是封面版型的左右兩格底圖（各 `max_workers=2`）——
`main.py:4739`（十點雙切）、`main.py:5842`（YT 雙則）。**其餘全部序列**，包括
封面 AI 標題模式的兩段生圖首尾相接（`main.py:5233-5237`／`5990-6013`）、
YT 雙則的兩次畫面描述（`5958-5962`）、逐人查照（`3308-3321`）、
逐點 geocode（`1458`，且 `map_lookup.py:129` 每點間硬性 sleep 補滿 1 秒）。
⚠ 前端 `app.js:3131` 的 `for (round < 6)` **不是六連生**，是上傳圖的 JPEG 品質降階迴圈（`app.js:3122`）。

**現有計時：全專案只有一處真的量牆鐘時間**——標題斷句（`main.py:4268/4283/4287`）。
`log_digest_usage`（`main.py:1606-1636`）記 token 與狀態但**沒有秒數**，
`request_log.py:65/118` 落檔欄位也沒有 duration。**所以現在沒有人知道時間花在哪，這就是 F25 的理由。**

**要加分段計時，最少動這 6 點**（各包一層 `perf_counter`）：

1. `digest_completion` 的 `return response` 前（`main.py:1748`）——**CP 值最高的單一改動**，
   一處涵蓋主消化／分類／斷句／封面描述全部 5 個呼叫端，`site` 參數現成可當標籤
2. `generate()` 的 attempt 迴圈（`main.py:2097-2180`）——分開「重試放大」與「單次慢」
3. `resolve_map_points`（`main.py:1441`）——geocode 總計，含強制 sleep
4. `lookup_portrait_photos`（`main.py:3295`）——查照總計，順便暴露 B32 的重複查詢
5. `generate_image_raw`（`main.py:2738`）——生圖牆鐘
6. `finalize_image_result`（`main.py:2827`）與 `_archive_generation`（`main.py:399`）——分開量 Pillow 後製與歸檔上傳

### 加速候選清單（依 CP 值）

| # | 候選 | 犧牲畫質 | 確定性 | 已另立 |
|---|---|---|---|---|
| 1 | 歸檔移出請求路徑（`main.py:2447`／`3899`、`gcs_archive.py:49-53`、`audit_archive.py:68-85`），改 `BackgroundTasks` | N | **純靜態確定** | F29 |
| 2 | `photo_lookup` 加行程內 TTL 快取，消掉重複查照 | N | **純靜態確定** | **B32** |
| 3 | 斷句呼叫移到零 API 分支之後 | N | **純靜態確定** | **B33** |
| 4 | 明確設 `max_retries`，讓 90 秒真的是上限 | N | **純靜態確定** | **B31** |
| 5 | 第二次 `generate()` 共用同一 deadline | N | **純靜態確定** | **B31** |
| 6 | 封面斷句與畫面描述並行（`main.py:5183`→`5220`，兩者無依賴） | N | 可並行為靜態確定，秒數需實測 | F27 |
| 7 | Nominatim 1 秒節流改併發（`map_lookup.py:129`、`main.py:539`，4 點 ≥3 秒純等待） | N | 靜態確定，**但違反對方使用政策，需裁決** | D-（未開） |
| 8 | OSM 圖磚並行下載（`map_lookup.py:255-266`，24 張序列 ×8 秒） | N | 需實測（僅冷快取有感） | F27 |
| 9 | 合併／跳過封面兩段生圖的第一段（`main.py:5233-5237`／`5990-6013`） | **Y** | 需實測＋需裁決 | F26 家族 |
| 10 | `OPENAI_IMAGE_QUALITY` `medium`→`low`（`main.py:3000`）／降 `DIGEST_MAX_TOKENS`（`main.py:185-186`） | **Y** | 需實測 | **F26（已評估為不建議動）** |

**監督判讀**：第 2～5 項是**缺陷不是調校**，已改列 B31／B32／B33，
不必等 F25 的實測就能確定現行行為是錯的；其中 **B31 會讓使用者在 Cloud Run 上直接吃到逾時**，
是這一族裡唯一有使用者可見後果的。第 1 項獨立為 F29。
第 7 項要動別人的使用政策，屬裁決項，**未開，不得自行動工**。

---

### 🔴 未結的驗收債：Stage 1 與 Stage 2 合併一輪實拍（使用者 2026-09-15 裁決）

**Stage 1 七項（B26+D9／B27／D4／B1／D7／B28／1-7）目前是 🔶已修待驗**，
本機 1696 測試全綠、凍結快照未破，但**生圖實拍一張都還沒跑**。

**使用者裁決：不單獨為 Stage 1 開一輪，等 Stage 2 做完合併一次測。**
理由：Stage 1 在生圖端、Stage 2 在消化端，**兩者獨立、不會互相混淆歸因**，
合併可省一輪費用與一次交辦。代價是 Stage 1 若有問題會晚幾小時才發現，已接受。

**這一輪的交辦對象是「測試」session**（實作 session 不自行宣告視覺驗收完成）。
交辦單要涵蓋：

| 來源 | 要看什麼 |
|---|---|
| B26+D9 | 多張原圖放置時是不是真的放了多張（不是只放一張） |
| B27 | YT 封面有沒有真的讀附圖位 |
| D4 | 兩格都有圖時 LEFT／RIGHT 措辭有沒有生效 |
| B1 | 成品回餵不再出現重複日期 |
| B28 | 具名真人時「AI示意圖」標籤沒被洗掉 |
| D16 | 六檔（含無字）各量標題與內文字數，**broadcast 那幾格排除行數、保留字數**。⚠**2026-09-15 已實拍六檔，結論：內文級距成立、標題級距未驗**（$0.24，`gpt-5.5`，276 字颱風稿／記者版／非卡片，逐張由監督覆核）。內文塊數 3／3／6／8、總字數 約41／約39／131／123，底端兩檔擠在一起但與頂端差約 3 倍。**但標題實測 6／4／10／8，四檔全部遠低於上限（10／13／18／22）且不單調**——上限從未被觸及，等於 D16 的數字在這輪完全沒發生作用，調成任何值這六張都不會變。與 0-2 長稿量到的 13／13／19／20 不相符，⚠**2026-09-15 監督自我更正**：原寫「差別最可能在素材長度」，一查就推翻——**0-2 的 long 樣本是 295 字**（`scripts/fixtures/title-length-news.json`），與今天的 276 字幾乎同長、同為記者版非卡片。所以長度解釋不成立，真因待查，候選有三：①Stage 2（2-1～2-9）把標題寫短了；②量測路徑不同（0-2 走 `scripts/measure_title_length.py` 直打消化 API，今天走瀏覽器全鏈路，中間可能多了標題斷句或前端設定）；③素材題材（CPI 數字稿 vs 颱風稿）。**所以 D16 不能用這輪結案**，兩條路待裁：甲＝再跑一組 1000 字以上的長稿階梯（4 張 $0.16）驗上限會不會變成有效約束；乙＝接受 D16 只是天花板、不要求可見級距。**裁決前不得動 D16 的數字。** 完整量測 `D:\Downloads\AICG\後台紀錄\20260915\D16_六檔量測.md`。**副產品**：檔位 6 塊數比 5 多（8>6）但總字數更少（123<131），確認 D3／2-7 的「只能拆不能補」生效 |
| D2 | 綠幕情境與「下跌＝綠」資料圖各一，確認禁的只有螢光綠 |
| F0 | 同 seed 與遞增 seed 的可重現性 |
| A2 | 創意 3／4 級配件件數是不是 2／3 |
| D3 | maximum 拆分沒有新增原文沒有的事實 |

⚠**開輪前先報「N 張 × $0.04」與用哪把 key**，不得自行跑下去。
⚠**D16 的數字是用 `gpt-5.5` 量的，這一輪要在正式站 `claude-sonnet-5` 上看**；
不合格回報監督重裁，實作不得自行微調。

---

## M — 模組化／技術債

| ID | 項目 | 來源 | 狀態 | 一句話現況 |
|---|---|---|---|---|
| M1 | 附圖規則模組 `refs.py` | 模組化（第 2 步） | ⬜ | 附圖規則散在 `main.py` 五個函式＋`news_prompt.py` 六段模板 |
| M2 | 標題政策模組 `title_policy.py` | 模組化（第 3 步） | ⬜ | `title_mode_for_creativity`、`creativity_level`、日期牌歸屬、只改文字可用性 |
| M3 | 端點拆 router | 模組化（第 4 步） | ⬜ | `main.py` 6320 行／158 個 def |
| M4 | `app.js` 依版型讀能力矩陣 | 模組化（第 5 步） | ⬜ | 第 1 步（能力矩陣資料化）已做，前台仍是靜態表靠測試釘住 |
| M5 | **架構矩陣化：橫向＝版型、縱向＝模組，改模組即連動所有版型** | 2026-09-16 使用者在 B61 事故後提出 | ⚖ **2026-09-16 使用者已裁：先做 L1 就好**（出口強制層止血，解掉 B61／B62）。**L2 矩陣擴充與 L3 前端不再自組 prompt 暫不動工**，等 L1 上線看效果再談，屆時重新提案。⚠ 本列其餘內容是當時的評估紀錄，保留備查 | **使用者原話**：「現在多了各種版型，常常是 A 版型有修、B 版型沒有收到，這代表我當初的架構沒有設好……把功能拆成模組化，橫向是不同版型，縱向是不同模組，以後修改就改模組連動」。✅**監督：問題判斷完全正確，而且今天有六個獨立證據**——B61（六個封面模板各缺真人條款）、B62（hybrid.js 與 app.js 不同步）、B65（三支能力函式各寫一份環境判斷、拼法還不同）、A3（三份創意說明表沒有單一真相源）、A4（版型功能對齊盤點剩兩個缺口）、A7（創意拉桿在雙則底圖完全沒作用）。**這些全都是同一個形狀：同一條規則被複製到 N 個地方，新版型只複製了構圖沒複製規則。**⚠**但監督反對「一次性重新架構整個網站」，理由三條**：①**風險**——1768 個測試、prompt 凍結快照、天天上線在用，一次性重寫會讓所有進行中的修法停擺數週，而 prompt 行為的回歸**無法自動驗證**（視覺輸出要人眼看）；②**今天的洞不需要等重構才能補**——出口強制層一刀就能讓 B61／B62 同時消失；③**起點已經有了**：`editor_formats.FORMAT_CAPABILITIES`（2026-09-14 模組化第 1 步）**就是使用者說的那張橫縱表**，只是目前 13 欄、只管前端顯示與旗標，**沒有管 prompt 規則**。應該是**擴充它**，不是另起爐灶。💡**監督建議的漸進三層（每層可單獨上線、單獨驗收）**：**L1 出口強制層**（＝下一批第一刀）——鐵則在出口一次注入，版型模板只留構圖差異，配一支「所有生圖路徑都必須含鐵則」的測試；**L2 矩陣擴充**——把 prompt 模組（真人／附圖／標題／日期牌／創意／安全框／示意圖）變成矩陣的欄，版型只宣告用哪個模組、參數多少，M1（`refs.py`）／M2（`title_policy.py`）是它的前兩塊；**L3 前端不再自組 prompt**——`app.js` 與 `hybrid.js` 現在各自拼 prompt、靠 parity 測試逐字比對維持同步（B62 就是這樣漏的），最終要改成後端提供組裝、前端只送參數。⚠**硬限制**：L1／L2 都會改到 prompt 文字，**撞 `tests/test_reporter_prompt_frozen.py` 的凍結快照**，動工前要先要到重凍預算。📌**待裁**：甲＝照上面三層漸進（監督建議）；乙＝使用者仍要一次性重寫，那要先凍結新功能、排一段專用時間，並接受期間不上新版型。 |

## R — 待驗收／等證據

| ID | 項目 | 來源 | 狀態 | 一句話現況 |
|---|---|---|---|---|
| R1 | 十點封面「側邊小標籤」 | 同名章節 | ⏸ | 前端已先藏，等驗收 |
| R2 | 地圖繼續驗收 | 同名章節 | ⏸ | 注意「AI 看圖驗地理不可靠」，逐點對照座標，沒把握標待人工複核 |
| R3 | 入門手冊截圖解析度太差 | 同名章節 | ⬜ | 下一輪修手冊時一併 |
| R4 | 附圖未測組合 | 版型功能對齊盤點 | ⏸ | scene／portrait／map 參考、創意 2–4 配附圖、live24 三種底圖配附圖，皆未測過 |
| R5 | 創意階梯改動後的長相重複度基準 | 創意階梯三條線對齊清點 | ⬜ | **改 A1 之前**先跑一輪同篇新聞最高級 N 次當基準，否則「有沒有變亮」又是感覺題 |

### 監督驗收：Stage 2-4～2-9（2026-09-15，`fda34c6`）

實作由子代理 stage2 完成，驗收由監督另跑，**沒有沿用實作者的自述**：

- **全套 1755 綠**（監督自己重跑，114.9 秒），與實作者回報一致。
- **凍結快照零接觸**：`git log --name-only 707bf12..HEAD` 顯示 7 個 commit 一份 fixture 都沒動，最後一次動它們仍是已授權的 `707bf12`。**沒有第二次重凍**（CP1 只批准一次，那一次已用掉）。
- **六個 density block 全乾淨**：對 `STANDARD`／`SIMPLIFIED`／`MINIMAL`／`MAXIMUM`／`VERBATIM`／新增的 `NO_TEXT` 逐一跑 `(one|two|three|single) lines?` 反向斷言，零命中。Stage 2 初版的行數洩漏（density block 是角色共用，寫進去就同時流給記者）沒有復發。
- **A2 確實只剩一張表**：`creativity.py:234` 單一來源，`editor_formats` 與 `main.py:1256`（CG）都指向它，另有測試釘住「CG 不得另立件數表」。封面／YT 的 RNG pin 與 fixture 全數未動。
- **seed 沒進 prompt**：對 0–4 級 × 三組 seed 實跑 `cg_creativity_rules()`，seed 數字一次都沒出現在輸出字串裡。seed 只走 API response／`request_log`／`audit_archive`，符合 D1 補充裁決。
- **無字檔位接線完整**：`build_prompt` 的唯一 Python 呼叫點與 `app.js` 的兩個呼叫點都傳了 `no_text`，沒有漏接的路徑。

**三件超出計畫範圍、實作者主動申報、監督同意的改動**：

1. `news_prompt.NO_TEXT_IMAGE_OVERRIDE` ＋ `app.js` 同名常數（不在計畫檔案表內）。**同意**：不改就是半套——空 variable 會被 `compose_variable` 換成 `[No Variables Defined]`，模型有機會把那串字面畫上去。沿用既有的 `YT_COVER_TEXT_FREE_OVERRIDE` 模式、前後端逐字同步並加了 parity 測試，沒有發明新機制。
2. `digest_quality_problem` 放行「無字的 variable 為空」（不在計畫行號內）。**同意**：不改是硬阻斷——無字會連撞 5 次重試後回 502，使用者只看到「AI 回傳內容異常」。放行範圍夠窄，型別／異常字元／頻道洩漏／簡體字四項照舊全開。
3. 招式池只改 `iconrow` 一條的措辭（那條寫死「深藍底條上方」，CG 沒有底帶）。**同意**，且**同意它沒有動** `arrow`／`magnifier`／`cutout` 的 "photograph" 字樣——那是措辭貼不貼切、不是事實錯誤，留給實拍那輪判斷，已進 TODO。

**2-8 的 no-op 監督接受**，因為它給了要求的證據而不是一句「no-op」帶過。

⚠**監督更正（2026-09-15，`86b57f9` 之後）**：上面那句原本寫的是「這屬於『已經夠開，結案』，不是『該動但不能動』」——**結論的方向對，但我的兩分法太粗**。子代理補證據後逐格拆開，**實際上是四件事**，處置各不相同：

| | 現象 | 處置 |
|---|---|---|
| ① | 記者版／非卡片版型：`long／記者` ＝ **1／2／7／8 點、10／50／135／159 字**，嚴格單調且是倍數級距 | **結案**——素材與版型給得起的時候，現行天花板是有效的 |
| ② | 短稿（17 字）四檔全塌成 1 點／8–10 字 | **正確行為，不是缺陷**——條文明文禁止湊數（`maximum` 第 3 條），這一格**不該**被拉開 |
| ③ | 編輯版播出鏡面 **3／3／4／4** 點，被 `_broadcast_point_count()` 釘死 | **另立 D17**，**2026-09-15 已裁：要聯動、張數交給模型決定不寫死**（待實作） |
| ④ | 編輯版 `minimal` 出 **3 點**，違反自己的「ONE point」 | **另立 B34**，**2026-09-15 已裁：1～3 點合格、上限 3 點**（要重凍，下一波修） |

四者**都不需要動** `STANDARD`／`SIMPLIFIED`，所以「程式面 no-op」這個結果仍然成立。

**這一段是監督自己的錯**：我先前引的是中位數（1／2／4／4 點、10／22／34／37 字），而中位數把記者／編輯、短稿／長稿、卡片／非卡片版型混在一起平均掉了，看起來像「頂端偏窄但還行」，實際上底下藏著一個缺陷（④）與一個待裁題（③）。**逐格看得到的東西，中位數會抹掉**——下次量測級距一律先看逐格再看彙總。

另記一個口徑問題：`scripts/measure_title_length.py` 的 `chars_no_ws` **只去空白、沒去 `<>`**，內文字元數因此偏高（標題欄去 `<>` 後 13→11）。marker 數量在各檔之間沒有系統性差異，**級距形狀不受影響**，但**單格 ±10% 以內的差距要當雜訊**——`long／編輯` 的 105→92 反轉就是雜訊，該格真正穩固的證據是點數被釘在 4／4。

**仍未驗的**：一切視覺實拍。D16 的數字是在 `gpt-5.5` 上量的、正式站是 `claude-sonnet-5`，併輪實拍不合格時**回頭重裁數字**，不由實作端自行微調。

---

## 已完成歸檔

完成項改 ✅ 後移到這裡，**不刪列**。詳情留在 `TODO.md` 原章節。

| ID | 項目 | 完成 | 備註 |
|---|---|---|---|
| — | 建表前已完成的項目 | — | 見 `TODO.md` 中標「已做」「已上線」「落地」的章節（能力矩陣資料化、YT 附圖位、只改文字、換版型清附圖位、創意 0 AI 標題由鎖住改開放、live24 版型、上傳模組化等） |
