# TVBS AICG — 淺底配色可行性規劃書

> 給接手實作的 agent。撰寫於 2026-08-19，對應 commit `54f3a01`（線上 revision `tvbs-aicg-linebot-00026-4k6`）。
>
> 起因：使用者觀察到「自動消化出來的幾乎都是深色底＋亮色字，很少看見淡色底＋深色字」，
> 要求查清楚是不是刻意要求的。**查證結果：不是。** 本規劃書記錄查證過程、成因分析，
> 以及三個待使用者裁決的問題。**目前尚未動工，也不該在裁決前動工。**
>
> 上一份規劃書（2026-08-17 三項功能：追加修改／指令拆欄／上傳參考圖）已全數上線，
> 內容見 commit `5af4a42` 的 `PLAN.md`。本檔的 §0 共通規則由那份延續並更新。

---

## 0. 先讀這段：不做會出事的共通規則

### 0.1 記者版一個字都不能動

編輯與記者共用大量 prompt 常數，改一邊很容易誤傷另一邊，而且**誤傷不會有任何執行期錯誤**——只會讓記者悄悄出不一樣的圖。

`tests/test_reporter_prompt_frozen.py` 把改動前的記者 prompt 原文存成 fixture 逐字比對（`tests/fixtures/reporter-*.txt`，消化 4 種組合 + 生圖 2 種）。**這條紅了就是誤傷，去改程式，不要改 fixture。** 真的要動記者 prompt 時，才連同 fixture 一起更新，並在 commit 訊息說明。

> ⚠️ **本規劃書的選項 B 和 C 會正面撞到這條。** 見 §3 的風險欄。

### 0.2 prompt 規則有兩份來源，必須同步

`news_prompt.py`（LINE Bot 用，後端組）與 `app.js`（網頁版用，前端組）是同一套規則的兩個實作。只改一邊不會報錯，只會讓兩條路徑出不一樣的圖。

`tests/test_prompt_parity.py` 會逐字比對這些常數。新增 prompt 常數時，**兩邊都要加，並在 parity 測試註冊**。

> 注意：**消化階段的 prompt 只有後端一份**（`main.py` 的 `SYSTEM_PROMPT_TEMPLATE` /
> `EDITOR_SYSTEM_PROMPT_TEMPLATE`），沒有前端副本。本規劃書要動的正是消化階段，
> 所以 parity 測試多半不會被觸發——**別因為它綠燈就以為兩條路徑同步了**。

### 0.3 網頁版改動要進 cache-buster

`index.html` 的 `<script src="app.js?v=YYYYMMDDx">`。改了 `app.js` 就要往前跳一個字母，**不要重用舊字串**（舊字串可能還在使用者瀏覽器快取裡）。目前是 `20260819a`。

### 0.4 生圖要花錢

每次生圖都是真實付費呼叫。驗證新功能時：

- 能用幾何／既有成品算出來的，就不要生圖（很多問題可以純數學驗證）
- 需要生圖時，**先跟使用者說明要生幾張、為什麼**，取得同意再跑
- 測試一律 mock 掉生圖呼叫（見 `tests/test_line_bot.py` 開頭的說明）

**OpenRouter 那把 key 有 $50 美元／週的硬上限**（每週重置）。這是實際的爆炸半徑天花板，
但也表示大量實拍驗收會吃掉當週配額——排實驗前先確認剩餘額度。

### 0.5 零成本驗證迴路（2026-08-19 新增，做視覺類改動前必讀）

**已置框的成品裡，內容就精準落在 `safe_rect`。把那塊裁出來就等於還原生成圖**，
可以離線重組任何襯底／置框方案並排比較，一毛錢都不用花。歷史成品在：

```
G:\我的雲端硬碟\Claude共用\AICG\TVBS AICG 網頁版 歷史紀錄\YYYYMMDD\
```

每則有 `.png`（成品）與 `.json`（含完整 prompt、provider、model）。
2026-08-19 的襯底改版整條路都是這樣驗的，零付費生圖。

**但這招驗不到 prompt 改動**——它只能重組既有像素，無法回答「模型拿到新指示會畫成什麼」。
本規劃書要改的正是 prompt，**所以這招在這裡幫不上忙，必須實拍**。這是本案成本高的根本原因。

### 0.6 部署與回滾

```powershell
$g = "C:\Users\User\AppData\Local\Google\Cloud SDK\google-cloud-sdk\bin\gcloud.cmd"
& $g run deploy tvbs-aicg-linebot --source . --region asia-east1 `
    --project tvbs-aicg-linebot --quiet
```

> `gcloud` **不在 PATH**，一定要用完整路徑。

改環境變數用 `--update-secrets`（合併），**不要用 `--set-secrets`**（會整組覆蓋、洗掉其他設定）。

回滾不需重新建置：`.\rollback.ps1 -Revision <舊 revision>`，只切流量，幾秒生效。

**部署後怎麼驗證線上真的是這版**（不要只看時間戳猜）：

```bash
curl -s $BASE/openapi.json          # 與本機 main.app.openapi() 逐鍵比對
curl -s $BASE/app.js                # 與本機 app.js 比，應只差 API key 注入那行
curl -s $BASE/ | grep -o 'app.js?v=[0-9a-z]*'   # cache-buster 有沒有推
```

### 0.7 跑測試

沒有 pytest，用 unittest。有幾支會打真實 API，日常請跳過：

```bash
./.venv/Scripts/python.exe -m unittest discover -s tests -q      # 全部（含會打 API 的）
```

目前 411 個測試。**改 prompt 前先跑一次取得基準**，不然分不清紅燈是你造成的還是本來就紅。

---

## 1. 查證結果：深色不是被要求的

### 1.1 程式碼裡對顏色的全部指示

全庫只有三處提到主視覺配色：

| 位置 | 內容 | 影響範圍 |
|---|---|---|
| `main.py:355` | 「選擇主色調與畫面風格（**例如：深藍色科技感、紅白色警戒感**）」 | 編輯版消化 |
| `main.py:331` | 「Choose a professional visual style appropriate for a "{type_label}"」——**完全沒提顏色** | 記者版消化 |
| `main.py:943` | `dark navy broadcast tone preferred` | **只有**混合版型（第三頁 hybrid），與主流程無關 |

`news_prompt.py:45` / `:71` 的 "solid background color (e.g. red background with white text)"
是特定情境的排版規則，不是全域色調指示。

**沒有任何一處寫「要用深色底」。**

### 1.2 實證：43 筆歷史 prompt 的用詞分布

掃 `歷史紀錄\*\*.json` 的 `prompt` 欄位：

| 項目 | 數字 |
|---|---|
| 總筆數 | 43 |
| 含深色詞（dark/deep/navy/charcoal/midnight/dim） | **37（86%）** |
| 含淺底描述（light/bright/white/pale + background） | **0** |

出現次數：`navy` 28、`deep` 20、`dark` 17、`charcoal` 9、`midnight` 3、`dim` 2。

### 1.3 成因分析（按影響力排序）

| # | 成因 | 可控性 |
|---|---|---|
| 1 | **模型先驗**：prompt 自稱 "elite broadcast news graphics director"，加上「鏡面 CG／anchor-wall」語境。電視新聞 CG 在訓練分布裡壓倒性偏深色 | 難改，也不該改——這正是我們要的專業感 |
| 2 | **Few-shot 錨定**：編輯版唯一的顏色例子，第一個就是「深藍色科技感」 | 一行可改 |
| 3 | **記者版沒有顏色指示**：模型完全自由發揮，直接落回 #1 的先驗 | 要新增 |
| 4 | 混合版型明寫 `dark navy preferred` | 一行可改，但那條路獨立 |

> **關鍵判斷：#1 是根本原因，#2 和 #3 只是沒有拉住它。**
> 就算把範例改成一深一淺，模型仍會偏深。**不要把「改了範例＝問題解決」寫進驗收標準**——
> 這是本案最容易產生錯誤預期的地方。

---

## 2. 三個必須由使用者裁決的問題

**在這三題有答案之前不要動工。**

### Q1：要「能選」還是「預設就變」？

| | 「能選」 | 「預設就變」 |
|---|---|---|
| 做法 | 使用者主動指示才走淺底 | 讓模型依新聞調性自行判斷 |
| 風險 | 幾乎為零，不影響現有使用者 | 動到所有人的出圖 |
| 現成機制 | **已經有了**——`main.py:449` 的 USER INSTRUCTIONS 區塊本來就會把「用手繪風」「不要科技藍」這類指示帶進 `style` | 無 |

> ⚠️ **先確認 Q1 的「能選」是不是其實已經能用了。** 使用者指示機制已上線，
> 理論上打「用米白色底深色字」就會生效。**動工前先實測一張**，
> 若本來就有效，本案可能整個不需要改 prompt，只需要教學文件。

### Q2：淺底在電視上真的可行嗎？（**只有使用者能回答**）

這不是技術問題。要考慮的至少有：

- 主播鏡面實際打出來的觀感
- 跑馬燈壓過去時的可讀性
- `<蓋章>` 金框橫幅的配色是設計給深底的，換淺底可能整組要重調
- `<文字>` 標示的變色關鍵詞同上
- `TW_DIRECTIONAL_COLOR_RULES`（漲紅跌綠）在淺底上的對比度

**若 Q2 的答案是「不太可行」，本案應該直接結案，只在文件裡記錄「深色是刻意維持的現狀」。**

### Q3：範圍？只編輯版、只記者版，還是兩邊？

記者版動到就會撞 `test_reporter_prompt_frozen`（見 §0.1）。

---

## 3. 三個選項（Q1–Q3 有答案後才選）

### 選項 A：只鬆開錨定（**建議先做這個**）

把 `main.py:355` 的範例從單一深色改成一深一淺：

```
（例如：深藍色科技感、紅白色警戒感）
→（例如：深藍色科技感、紅白色警戒感、米白色簡潔感）
```

| 項目 | 說明 |
|---|---|
| 改動量 | 1 行，只動編輯版 |
| 記者 fixture | **不受影響**（沒動記者 prompt） |
| 可逆性 | 完全可逆 |
| 預期效果 | **不確定，可能很小**——見 §1.3 的關鍵判斷 |
| 驗收 | 見 §4 |

### 選項 B：加明文指示

在 `main.py:355` 補一句「依新聞調性可選淺底深字，不必一律深底」。

| 項目 | 說明 |
|---|---|
| 改動量 | 1–2 行 |
| 風險 | 比 A 強，但也更可能過頭——溫馨題材可能全變淺底 |
| 若含記者版 | **會撞 `test_reporter_prompt_frozen`**，要連 fixture 一起更新並在 commit 說明 |

### 選項 C：獨立的「淺底模式」開關

比照 `safe_frame` 做一個 UI 開關，帶進消化階段分流。

| 項目 | 說明 |
|---|---|
| 改動量 | 大：`GenerateRequest` 新欄位 → `build_digest_instructions` 分流 → `app.js` UI → cache-buster → LINE 版要不要跟進 |
| 風險 | 記者 fixture 會多出組合數（目前 4 種 × 2 = 8 種），需要重新產生 |
| 何時才值得 | **只有在 A/B 實測證明「模型聽得懂淺底指示且成品可用」之後**。開關本身不解決「模型偏深」，只是把選擇權交出去 |

---

## 4. 驗收標準（三個選項共用）

**本案的成本大宗在驗收，不在寫 code。** §0.5 那招在這裡幫不上忙，必須實拍。

- [ ] **先跑 §2 Q1 的前置實測**：用現有的使用者指示機制打一張淺底，確認是否本來就能用。
      **1 張**。若成功，先回報使用者再決定要不要繼續。
- [ ] 改動後同題材各生 **1 張**（深色題材 1 張確認沒被帶歪 ＋ 適合淺底的題材 1 張），
      共 **2 張**。不要一次生一批——先確認方向對再擴大。
- [ ] 由**使用者**判斷可否上鏡（見 Q2，agent 不能代判）
- [ ] 記者版若有動：`tests/test_reporter_prompt_frozen.py` 必須刻意更新 fixture，
      不可默默改
- [ ] 全套測試綠燈（目前 411 個）

### ⚠️ 不要用「歷史 prompt 深色詞比例」當驗收指標

那是**現況診斷**用的，n=43 且全是改動前的樣本。改動後要重新累積夠多樣本才有統計意義，
而每一筆都要付費。**用它當驗收標準會逼出一堆沒必要的生圖花費。**

---

## 5. 建議順序與理由

1. **先問 §2 的 Q1／Q2／Q3**，尤其 Q2——若電視上不可行，後面全都不用做。
2. **跑 Q1 的前置實測（1 張）**——可能發現功能本來就有，整案結案。
3. 真要改就從**選項 A** 起步：最小、可逆、不碰記者 fixture。
4. 看幾天實際出圖分布再決定要不要進 B。
5. **C 放最後**，而且只在 A/B 證明模型聽得懂之後才考慮。

理由：這件事的**驗收成本遠高於實作成本**，而且「好不好看」只有使用者判斷得了。
一次改太多會分不清是哪一條規則造成的效果，付了錢還得不到結論。

---

## 附錄：2026-08-19 這天已經完成的相關改動

不是本案範圍，但改到同一批檔案，接手時要知道：

- **襯底改為取自 CG 內容上緣的單色**（`safe_frame.py`）。原本是「整張外圈平均 × 固定係數」，
  線上 10 張成品每一張襯底都比 CG 亮。現在 10/10 兩邊都改善（上 7.6／下 7.1）。
- **編輯版兩種框模式對調**，新增四邊 3% 薄框（`safe_area_spec.py`、`main.resolve_frame_plan`）。
- **留白容不下投影時自動停用投影**（通則，不寫死 profile 名）。

完整記錄在 `TODO.md` 開頭那一節。**已知缺口：薄框那檔的 prompt 從
`EDITOR_FULL_BLEED_RULES` 換成 `FULL_BLEED_RULES`，這一換零真實生圖證據**——
若本案要實拍，可以順便一起驗，省一次生圖錢。
