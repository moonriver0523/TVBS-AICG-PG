# AICG 進度存檔（2026-09-20）

## 分支狀態
- 本機分支：`exp/redesign-20260903`，HEAD `e52c450`，已推 `origin/exp/redesign-20260903`
- `main`（worktree 在 `E:\GitHub\TVBS-AICG-PG-main`）：`297b200`，已推 `origin/main`
- 工作副本乾淨（0917 那份未 commit 的 `MASTER-列管清單.md` 改動已包進 `e52c450`）

## B77 已結案
十點雙切／YT 整點的 AI 消化標題 502（D21 換 Gemini 後，integer 上掛 enum 被上游退 400）已修、
公司 PR #28 已 merge，**2026-09-20 使用者確認正式站已部署上線**，不必再查 Zeabur。

## 本次做的事：0920 列管盤點
使用者交辦十條新 TODO，要求與 0917 積壓「都列入下一批動刀」。已全部進 `MASTER-列管清單.md`：

**新列管五條**
- `F44` 編輯格式「預設（現行）」改名「編輯CG」（三處字串：`editor_formats.py:2238`／`app.js:456`／手冊五處，**不動 format key `default`**）
- `B78` 無字檔位在創意 ≥1 時仍出現創意框／設計（F20 只關掉字級條文，沒關版面）
- `B79` 十點創意階梯疑似鎖三行——**待實證**，嫌疑是 `COVER_TITLE_LINE_COLOURS` 白黃紅三行的固定視覺逼出來的，不是行數上限
- `B80` YT 直標：LIVE 章／藍底要換官方 PNG（現況是生圖模型重製，`compose.py:1376` 早就註明等官方檔）、標題字級縮小、LIVE 與直標空隙（根因＝`VSTRIP_TOP_RATIO` 沒勾小標時仍留位）
- `B81` 正式站 `app.js` 落後 repo（B77 附帶發現，原本沒獨立列管）

**併入既有列管七條**：F38（2K 旗標要開，且使用者要涵蓋記者、現行寫死只有編輯）、F32（＋ENTER 換行）、
F35③（`<底帶>` 跨版行要吃重點）、F36（日期方塊改依字數自動判斷）、F42（訊息視窗兼進度顯示）、
F34（非知名人物改用職稱）、B46（與 B80 同批）。

**其他**：新增「🔴 2026-09-20 使用者追加：下一批動刀的完整名單」一節（甲/乙/丙/待裁四群）；
0915 的 🌊 第二波對本節點名的項目解除（B43／B44／B45／F33／F37／D18／D19 仍照舊）；
重號的第二個 `R6` 更正為 `R7`。

## ✅ 0920 當天裁完的三件卡點
1. **B76 重凍額度已授權**（額度現為 B57／B61／F40／F41／B66／B76 六條），fixture 更新要補 docstring 說明
2. **B80 官方素材已到手**：`G:\我的雲端硬碟\Claude共用\AICG\YT直播側標底圖PNG\`，`直播底圖(直).png`（1914×1063）與`原音呈現底圖(直).png`（1905×1070），都是真去背（alpha 已驗）。
   ⚖**已裁甲案**：整張官方底圖當固定層，程式只壓直排文字與來源，**標題長度反過來遷就固定色框，超出就縮字級、不准再拉長色框**。
   📐**幾何已用 `magick` 實測**（動工直接用，不要再憑感覺）：直播底圖 藍框 189×485@(31,171)＝上緣 0.1609／寬 0.0988／高 0.4563；
   原音呈現底圖 藍框 194×489@(19,213)＝上緣 0.1991／寬 0.1018／高 0.4570。
   **兩個連帶結論**：①現行 `VSTRIP_TOP_RATIO=0.166`／`WITH_LABEL=0.191` 與官方幾乎一致 → B46 的空隙不是上緣訂錯，是色框沒從那個上緣開始長；
   ②官方色框固定約 0.456h、現行上限 0.64h，**可用長度砍掉約三成，字級下限要一起訂**。**對位一律以藍框左緣為基準**（兩張左緣與寬度都不同）
3. **R7 已裁**：先跑一輪 4 人／5 人實拍再訂數字，不先放寬；驗收沿用 0818 標準（逐張核對，一張錯就不放寬）。**實拍付費，開跑前先報張數與金額**
4. **D22 暫緩**（使用者這次沒裁）

## 下一步
B80 的甲／乙裁完就可以照新名單甲→丙動工；B73 那 20 道守門測試要清醒著逐題判斷，不要趕。
