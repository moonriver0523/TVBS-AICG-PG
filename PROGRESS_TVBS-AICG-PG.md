# AICG 進度存檔（2026-09-21 凌晨）

## 分支狀態
- `exp/redesign-20260903` HEAD `26576ec`，已推 `origin`
- **公司 `aicg-champion/main` `bb02e1f`：PR #30 已合併**（sync 分支 `sync/personal-20260921-b55`）
- 全量 **2176 題綠**（0 失敗 0 錯誤，1 expected failure）；合併公司 main 之後有重跑過一次才推
- 工作副本乾淨

## 這一波做完了什麼（0917 積壓，使用者要求「除 B73 外今晚分波都要完成」）

| 項目 | 結果 |
|---|---|
| **B55** | 修法甲上線：`provider=="gpt"` 改走透明底標題圖層，模型只回標題、照片由程式逐像素疊。四道閘＋閘門失敗退回程式壓字。🔶 **不結案**（見下） |
| **B68** | 結案 |
| **B70** | 具名肖像「示意圖」改由程式後貼；修掉網頁版標籤完全消失的回歸 |
| **B76** | 中國大陸輪廓誤含臺灣：消化端＋生圖端各補一塊規則。🔶 仍未到使用者要的「百分之百」 |
| **F43** | 新增「畫面來源」（與示意圖互斥）：十點 composite 全路徑＋YT 封面四版型 |

出波段的：B73（擱置）、F34（之後單獨處理）、F45／F46（不進這波）、D22（待裁）。

## ⚠️ B55 為什麼不能結案
**從未經過任何一次付費實拍驗證。** 四道閘、退回機制、0.1% 下限全部只用 Pillow 假圖層驗過
程式邏輯；「生圖模型會不會真的吐出乾淨的透明底（alpha 乾淨、沒有半透明背景色塊、
文字品質不因為沒有背景參照而變差）」沒有任何實證。**下次開工第一件事就是實拍驗這個。**
`provider=="gemini"` 完全沒動，仍是舊的差異遮罩路徑（實測 change_ratio 常態 68%↑，等同擋死）。

## 獨立複查（gpt-5.6-sol）三輪，抓到 9 個真缺陷
**全都是在測試全綠的狀態下抓到的，沒有一個是測試抓到的。** 其中 4 個會讓標籤整個消失、
1 個會貼出假的來源標示——都是播出等級。全部已修並各自補了守門測試。

1. alpha ≤ 16 的薄層可穿過閘門染到照片（實測產生 `(239, 0, 16)`）
2. 完全透明的圖層三道閘全過，靜默回傳一張沒有標題的原圖
3. 網頁版 `/api/images/generate` 具名肖像標籤完全消失（**一度真的進了 `exp` 並推上 origin**）
4. 透明圖層路徑的底圖標成 `purpose="aiedit"`，把「重畫這張照片」接在「不要重畫這張照片」後面
5. `portrait_mode=none`／`no_reference` 被要求「不要自己畫」，但 `resolve_image_disclaimer()`
   從不替這兩條路壓字 → 兩邊都沒人負責（在併進 `exp` 之前攔下）
6. 第 5 條的豁免範圍比 `portrait_subjects` 寬 → 具名真人的**背影／剪影**完全沒有標籤
7. 前端帶回的 `background_is_ai` 漏帶時 Pydantic 預設 `False` → AI 圖可被標上「畫面來源」
8. 40 字來源名的底板橫跨畫布 83%，蓋掉 LIVE 章與日期板
9. 退回程式壓字時沒補打斷句，會把詞腰斬（「供應鏈」→「供／應鏈」）

**這件事本身是結論**：這個專案的測試擋不住「東西是壞的但全綠」這一類缺陷，
之後每一波都該排獨立複查，範圍限縮在「新接的東西＋刪掉的行」。

## 本波使用者裁決（已落地）
- **B55 閘門失敗** → 不回 400，**退回程式壓字並在 `notices` 明講**。退回時 `background_is_ai`
  翻 `False`、`title_mode` 照實回 `"composite"`（不然就是對觀眾說謊）
- **B55「畫得極少」** → 要擋。先裁 1%，複查用**實際字形**量過後**改判 0.1%**
  （1% 會誤擋合法的一～二字標題：十點一字 0.50%、二字一行 0.99%、YT news 每行一字 0.98%）
- **sol 複查範圍** → 只審高風險那兩塊，不審全 diff

## 還沒裁的（下次開工要問）
1. **B76 中國地圖**：現在只到 prompt 層，模型仍可能不聽。要「百分之百」得走程式底圖
   或強制人工複核——要不要做？
2. **B70 一般重建圖**：軟體端要不要也對 `portrait_mode="none"`／`no_reference` 補壓「示意圖」？
   觸發條件是什麼（候選：variable 裡出現「示意圖」就設 `disclaimer_kind='ai'`）？
   現況是「模型自己判斷、程式不介入」
3. **F43 信任邊界**：`background_is_ai` 目前仍由前端說了算，只擋掉「漏帶欄位」那條路。
   要不要改成後端從位元組自行判定（或簽章）？比這波大
4. **B55 剩兩項**：Gemini 路徑要不要補／`asis≥2` 的「融合」語意
5. **D22**（0920 就暫緩，使用者一直沒裁）

---

# 以下沿用 0920 存檔，仍然有效

## 0920 列管盤點（十條新 TODO，已全部進 `MASTER-列管清單.md`）

**新列管五條**
- `F44` 編輯格式「預設（現行）」改名「編輯CG」（三處字串：`editor_formats.py:2238`／`app.js:456`／
  手冊五處，**不動 format key `default`**）
- `B78` 無字檔位在創意 ≥1 時仍出現創意框／設計（F20 只關掉字級條文，沒關版面）
- `B79` 十點創意階梯疑似鎖三行——**待實證**，嫌疑是 `COVER_TITLE_LINE_COLOURS` 白黃紅三行的
  固定視覺逼出來的，不是行數上限
- `B80` YT 直標：LIVE 章／藍底要換官方 PNG（現況是生圖模型重製，`compose.py:1376` 早就註明
  等官方檔）、標題字級縮小、LIVE 與直標空隙（根因＝`VSTRIP_TOP_RATIO` 沒勾小標時仍留位）
- `B81` 正式站 `app.js` 落後 repo（B77 附帶發現，原本沒獨立列管）

**併入既有列管七條**：F38（2K 旗標要開，且使用者要涵蓋記者、現行寫死只有編輯）、F32（＋ENTER 換行）、
F35③（`<底帶>` 跨版行要吃重點）、F36（日期方塊改依字數自動判斷）、F42（訊息視窗兼進度顯示）、
F34（非知名人物改用職稱）、B46（與 B80 同批）。

**其他**：新增「🔴 2026-09-20 使用者追加：下一批動刀的完整名單」一節（甲/乙/丙/待裁四群）；
0915 的 🌊 第二波對本節點名的項目解除（B43／B44／B45／F33／F37／D18／D19 仍照舊）；
重號的第二個 `R6` 更正為 `R7`。

## B80 官方素材與幾何（動工直接用，不要再憑感覺）
素材：`G:\我的雲端硬碟\Claude共用\AICG\YT直播側標底圖PNG\`，
`直播底圖(直).png`（1914×1063）與 `原音呈現底圖(直).png`（1905×1070），都是真去背（alpha 已驗）。

⚖**已裁甲案**：整張官方底圖當固定層，程式只壓直排文字與來源，
**標題長度反過來遷就固定色框，超出就縮字級、不准再拉長色框**。

📐**`magick` 實測**：直播底圖 藍框 189×485@(31,171)＝上緣 0.1609／寬 0.0988／高 0.4563；
原音呈現底圖 藍框 194×489@(19,213)＝上緣 0.1991／寬 0.1018／高 0.4570。

**兩個連帶結論**：
① 現行 `VSTRIP_TOP_RATIO=0.166`／`WITH_LABEL=0.191` 與官方幾乎一致 →
  B46 的空隙不是上緣訂錯，是色框沒從那個上緣開始長；
② 官方色框固定約 0.456h、現行上限 0.64h，**可用長度砍掉約三成，字級下限要一起訂**。
**對位一律以藍框左緣為基準**（兩張左緣與寬度都不同）。

## R7 已裁
先跑一輪 4 人／5 人實拍再訂數字，不先放寬；驗收沿用 0818 標準（逐張核對，一張錯就不放寬）。
**實拍付費，開跑前先報張數與金額。**

---

## 下次開工順序建議
1. **B55 實拍驗證**（唯一能讓它結案的事，也是這波最大的未知）
2. 上面五題裁決
3. 0920 那批新列管：F44／B78／B79／B80（素材與幾何都齊了）／B81
4. B73 的 20 道守門測試（一直被擱置，要清醒著逐題判斷，不要趕）

## 踩到的坑（別再踩）
- **worktree 沒有自己的 `.venv`**，要借主 repo 的：
  `/e/GitHub/TVBS-AICG-PG/.venv/Scripts/python.exe -X utf8 "<run_tests.py>" "<worktree 絕對路徑>"`
- repo **沒有 pytest**，一律用 scratchpad 的 `run_tests.py`
- 全量輸出很長，**只 grep 摘要行**（`^Ran |^OK|^FAILED|^=== 合計`），不要整段讀進 context
- `MASTER-列管清單.md` 每一列是一行超長文字，**不要整份 Read**，用 `grep -n`／`awk -F'|'`
  （`$2`=ID、`$5`=狀態、`$6`=說明）
- **子代理很會只改說明欄、不改狀態欄**（`ladder`、`digest-prompt`、`spec` 三次都犯），
  驗收時一定要單獨看 `$5`
- **子代理會捏造「使用者裁示」**（`spec` 犯過一次），合併前 `grep -n "裁示\|裁定\|team-lead"` 掃一遍
- `git add -A` 會把未追蹤檔一起提交（這份進度檔就是這樣被誤推上去的）——**一律列明路徑**
